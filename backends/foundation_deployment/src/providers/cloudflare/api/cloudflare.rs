//! CloudflareProvider - State-aware cloudflare API client.
//!
//! WHY: Users need state-aware API clients that automatically track
//!      resource changes in the state store.
//!
//! WHAT: Provider wrapping ProviderClient<S> with methods for
//!       cloudflare API endpoints that auto-store results.
//!
//! HOW: Each method wraps the task with StoreStateIdentifierTask
//!      for automatic state persistence on success.

#![cfg(feature = "cloudflare")]

use crate::providers::cloudflare::clients::{
    accounts_list_accounts_builder, accounts_list_accounts_task,
    account_creation_builder, account_creation_task,
    accounts_batch_move_accounts_builder, accounts_batch_move_accounts_task,
    get_applications_builder, get_applications_task,
    create_application_builder, create_application_task,
    get_application_by_id_builder, get_application_by_id_task,
    update_application_version_builder, update_application_version_task,
    get_categories_builder, get_categories_task,
    get_category_by_id_builder, get_category_by_id_task,
    custom_pages_for_an_account_list_custom_pages_builder, custom_pages_for_an_account_list_custom_pages_task,
    custom_assets_for_an_account_list_custom_assets_builder, custom_assets_for_an_account_list_custom_assets_task,
    custom_assets_for_an_account_create_a_custom_asset_builder, custom_assets_for_an_account_create_a_custom_asset_task,
    custom_assets_for_an_account_get_a_custom_asset_builder, custom_assets_for_an_account_get_a_custom_asset_task,
    custom_assets_for_an_account_update_a_custom_asset_builder, custom_assets_for_an_account_update_a_custom_asset_task,
    custom_assets_for_an_account_delete_a_custom_asset_builder, custom_assets_for_an_account_delete_a_custom_asset_task,
    custom_pages_for_an_account_get_a_custom_page_builder, custom_pages_for_an_account_get_a_custom_page_task,
    custom_pages_for_an_account_update_a_custom_page_builder, custom_pages_for_an_account_update_a_custom_page_task,
    accounts_account_details_builder, accounts_account_details_task,
    accounts_update_account_builder, accounts_update_account_task,
    account_deletion_builder, account_deletion_task,
    list_abuse_reports_builder, list_abuse_reports_task,
    list_emails_builder, list_emails_task,
    list_mitigations_builder, list_mitigations_task,
    request_review_builder, request_review_task,
    get_abuse_report_builder, get_abuse_report_task,
    submit_abuse_report_builder, submit_abuse_report_task,
    mcp_portals_api_list_portals_builder, mcp_portals_api_list_portals_task,
    mcp_portals_api_create_portals_builder, mcp_portals_api_create_portals_task,
    mcp_portals_api_fetch_gateways_builder, mcp_portals_api_fetch_gateways_task,
    mcp_portals_api_update_portals_builder, mcp_portals_api_update_portals_task,
    mcp_portals_api_delete_portals_builder, mcp_portals_api_delete_portals_task,
    mcp_portals_api_list_servers_builder, mcp_portals_api_list_servers_task,
    mcp_portals_api_create_servers_builder, mcp_portals_api_create_servers_task,
    mcp_portals_api_fetch_servers_builder, mcp_portals_api_fetch_servers_task,
    mcp_portals_api_update_servers_builder, mcp_portals_api_update_servers_task,
    mcp_portals_api_delete_servers_builder, mcp_portals_api_delete_servers_task,
    mcp_portals_api_sync_server_builder, mcp_portals_api_sync_server_task,
    access_applications_list_access_applications_builder, access_applications_list_access_applications_task,
    access_applications_add_an_application_builder, access_applications_add_an_application_task,
    access_short_lived_certificate_c_as_list_short_lived_certificate_c_as_builder, access_short_lived_certificate_c_as_list_short_lived_certificate_c_as_task,
    access_applications_get_an_access_application_builder, access_applications_get_an_access_application_task,
    access_applications_update_an_access_application_builder, access_applications_update_an_access_application_task,
    access_applications_delete_an_access_application_builder, access_applications_delete_an_access_application_task,
    access_short_lived_certificate_c_as_get_a_short_lived_certificate_ca_builder, access_short_lived_certificate_c_as_get_a_short_lived_certificate_ca_task,
    access_short_lived_certificate_c_as_create_a_short_lived_certificate_ca_builder, access_short_lived_certificate_c_as_create_a_short_lived_certificate_ca_task,
    access_short_lived_certificate_c_as_delete_a_short_lived_certificate_ca_builder, access_short_lived_certificate_c_as_delete_a_short_lived_certificate_ca_task,
    access_policies_list_access_app_policies_builder, access_policies_list_access_app_policies_task,
    access_policies_create_an_access_policy_builder, access_policies_create_an_access_policy_task,
    access_policies_get_an_access_policy_builder, access_policies_get_an_access_policy_task,
    access_policies_update_an_access_policy_builder, access_policies_update_an_access_policy_task,
    access_policies_delete_an_access_policy_builder, access_policies_delete_an_access_policy_task,
    access_policies_convert_reusable_builder, access_policies_convert_reusable_task,
    access_applications_revoke_service_tokens_builder, access_applications_revoke_service_tokens_task,
    access_applications_put_update_access_application_settings_builder, access_applications_put_update_access_application_settings_task,
    access_applications_patch_update_access_application_settings_builder, access_applications_patch_update_access_application_settings_task,
    access_applications_test_access_policies_builder, access_applications_test_access_policies_task,
    access_bookmark_applications_deprecated_list_bookmark_applications_builder, access_bookmark_applications_deprecated_list_bookmark_applications_task,
    access_bookmark_applications_deprecated_get_a_bookmark_application_builder, access_bookmark_applications_deprecated_get_a_bookmark_application_task,
    access_bookmark_applications_deprecated_create_a_bookmark_application_builder, access_bookmark_applications_deprecated_create_a_bookmark_application_task,
    access_bookmark_applications_deprecated_update_a_bookmark_application_builder, access_bookmark_applications_deprecated_update_a_bookmark_application_task,
    access_bookmark_applications_deprecated_delete_a_bookmark_application_builder, access_bookmark_applications_deprecated_delete_a_bookmark_application_task,
    access_mtls_authentication_list_mtls_certificates_builder, access_mtls_authentication_list_mtls_certificates_task,
    access_mtls_authentication_add_an_mtls_certificate_builder, access_mtls_authentication_add_an_mtls_certificate_task,
    access_mtls_authentication_list_mtls_certificates_hostname_settings_builder, access_mtls_authentication_list_mtls_certificates_hostname_settings_task,
    access_mtls_authentication_update_an_mtls_certificate_settings_builder, access_mtls_authentication_update_an_mtls_certificate_settings_task,
    access_mtls_authentication_get_an_mtls_certificate_builder, access_mtls_authentication_get_an_mtls_certificate_task,
    access_mtls_authentication_update_an_mtls_certificate_builder, access_mtls_authentication_update_an_mtls_certificate_task,
    access_mtls_authentication_delete_an_mtls_certificate_builder, access_mtls_authentication_delete_an_mtls_certificate_task,
    access_custom_pages_list_custom_pages_builder, access_custom_pages_list_custom_pages_task,
    access_custom_pages_create_a_custom_page_builder, access_custom_pages_create_a_custom_page_task,
    access_custom_pages_get_a_custom_page_builder, access_custom_pages_get_a_custom_page_task,
    access_custom_pages_update_a_custom_page_builder, access_custom_pages_update_a_custom_page_task,
    access_custom_pages_delete_a_custom_page_builder, access_custom_pages_delete_a_custom_page_task,
    access_gateway_ca_list_ssh_ca_builder, access_gateway_ca_list_ssh_ca_task,
    access_gateway_ca_add_an_ssh_ca_builder, access_gateway_ca_add_an_ssh_ca_task,
    access_gateway_ca_delete_an_ssh_ca_builder, access_gateway_ca_delete_an_ssh_ca_task,
    access_groups_list_access_groups_builder, access_groups_list_access_groups_task,
    access_groups_create_an_access_group_builder, access_groups_create_an_access_group_task,
    access_groups_get_an_access_group_builder, access_groups_get_an_access_group_task,
    access_groups_update_an_access_group_builder, access_groups_update_an_access_group_task,
    access_groups_delete_an_access_group_builder, access_groups_delete_an_access_group_task,
    access_identity_providers_list_access_identity_providers_builder, access_identity_providers_list_access_identity_providers_task,
    access_identity_providers_add_an_access_identity_provider_builder, access_identity_providers_add_an_access_identity_provider_task,
    access_identity_providers_get_an_access_identity_provider_builder, access_identity_providers_get_an_access_identity_provider_task,
    access_identity_providers_update_an_access_identity_provider_builder, access_identity_providers_update_an_access_identity_provider_task,
    access_identity_providers_delete_an_access_identity_provider_builder, access_identity_providers_delete_an_access_identity_provider_task,
    access_identity_providers_list_scim_group_resources_builder, access_identity_providers_list_scim_group_resources_task,
    access_identity_providers_list_scim_user_resources_builder, access_identity_providers_list_scim_user_resources_task,
    access_key_configuration_get_the_access_key_configuration_builder, access_key_configuration_get_the_access_key_configuration_task,
    access_key_configuration_update_the_access_key_configuration_builder, access_key_configuration_update_the_access_key_configuration_task,
    access_key_configuration_rotate_access_keys_builder, access_key_configuration_rotate_access_keys_task,
    access_authentication_logs_get_access_authentication_logs_builder, access_authentication_logs_get_access_authentication_logs_task,
    access_scim_update_logs_list_access_scim_update_logs_builder, access_scim_update_logs_list_access_scim_update_logs_task,
    zero_trust_organization_get_your_zero_trust_organization_builder, zero_trust_organization_get_your_zero_trust_organization_task,
    zero_trust_organization_create_your_zero_trust_organization_builder, zero_trust_organization_create_your_zero_trust_organization_task,
    zero_trust_organization_update_your_zero_trust_organization_builder, zero_trust_organization_update_your_zero_trust_organization_task,
    zero_trust_organization_get_your_zero_trust_organization_doh_settings_builder, zero_trust_organization_get_your_zero_trust_organization_doh_settings_task,
    zero_trust_organization_update_your_zero_trust_organization_doh_settings_builder, zero_trust_organization_update_your_zero_trust_organization_doh_settings_task,
    zero_trust_organization_revoke_all_access_tokens_for_a_user_builder, zero_trust_organization_revoke_all_access_tokens_for_a_user_task,
    access_policies_list_access_reusable_policies_builder, access_policies_list_access_reusable_policies_task,
    access_policies_create_an_access_reusable_policy_builder, access_policies_create_an_access_reusable_policy_task,
    access_policies_get_an_access_reusable_policy_builder, access_policies_get_an_access_reusable_policy_task,
    access_policies_update_an_access_reusable_policy_builder, access_policies_update_an_access_reusable_policy_task,
    access_policies_delete_an_access_reusable_policy_builder, access_policies_delete_an_access_reusable_policy_task,
    access_policy_tests_builder, access_policy_tests_task,
    access_policy_tests_get_an_update_builder, access_policy_tests_get_an_update_task,
    access_policy_tests_get_a_user_page_builder, access_policy_tests_get_a_user_page_task,
    zero_trust_seats_update_a_user_seat_builder, zero_trust_seats_update_a_user_seat_task,
    access_service_tokens_list_service_tokens_builder, access_service_tokens_list_service_tokens_task,
    access_service_tokens_create_a_service_token_builder, access_service_tokens_create_a_service_token_task,
    access_service_tokens_get_a_service_token_builder, access_service_tokens_get_a_service_token_task,
    access_service_tokens_update_a_service_token_builder, access_service_tokens_update_a_service_token_task,
    access_service_tokens_delete_a_service_token_builder, access_service_tokens_delete_a_service_token_task,
    access_service_tokens_refresh_a_service_token_builder, access_service_tokens_refresh_a_service_token_task,
    access_service_tokens_rotate_a_service_token_builder, access_service_tokens_rotate_a_service_token_task,
    access_tags_list_tags_builder, access_tags_list_tags_task,
    access_tags_create_tag_builder, access_tags_create_tag_task,
    access_tags_get_a_tag_builder, access_tags_get_a_tag_task,
    access_tags_update_a_tag_builder, access_tags_update_a_tag_task,
    access_tags_delete_a_tag_builder, access_tags_delete_a_tag_task,
    zero_trust_users_get_users_builder, zero_trust_users_get_users_task,
    zero_trust_users_create_user_builder, zero_trust_users_create_user_task,
    zero_trust_users_get_user_builder, zero_trust_users_get_user_task,
    zero_trust_users_update_user_builder, zero_trust_users_update_user_task,
    zero_trust_users_delete_user_builder, zero_trust_users_delete_user_task,
    zero_trust_users_get_active_sessions_builder, zero_trust_users_get_active_sessions_task,
    zero_trust_users_get_active_session_builder, zero_trust_users_get_active_session_task,
    zero_trust_users_get_failed_logins_builder, zero_trust_users_get_failed_logins_task,
    zero_trust_users_get_last_seen_identity_builder, zero_trust_users_get_last_seen_identity_task,
    zero_trust_users_delete_mfa_authenticator_builder, zero_trust_users_delete_mfa_authenticator_task,
    ip_address_management_address_maps_list_address_maps_builder, ip_address_management_address_maps_list_address_maps_task,
    ip_address_management_address_maps_create_address_map_builder, ip_address_management_address_maps_create_address_map_task,
    ip_address_management_address_maps_address_map_details_builder, ip_address_management_address_maps_address_map_details_task,
    ip_address_management_address_maps_update_address_map_builder, ip_address_management_address_maps_update_address_map_task,
    ip_address_management_address_maps_delete_address_map_builder, ip_address_management_address_maps_delete_address_map_task,
    ip_address_management_address_maps_add_an_account_membership_to_an_address_map_builder, ip_address_management_address_maps_add_an_account_membership_to_an_address_map_task,
    ip_address_management_address_maps_remove_an_account_membership_from_an_address_map_builder, ip_address_management_address_maps_remove_an_account_membership_from_an_address_map_task,
    ip_address_management_address_maps_add_an_ip_to_an_address_map_builder, ip_address_management_address_maps_add_an_ip_to_an_address_map_task,
    ip_address_management_address_maps_remove_an_ip_from_an_address_map_builder, ip_address_management_address_maps_remove_an_ip_from_an_address_map_task,
    ip_address_management_address_maps_add_a_zone_membership_to_an_address_map_builder, ip_address_management_address_maps_add_a_zone_membership_to_an_address_map_task,
    ip_address_management_address_maps_remove_a_zone_membership_from_an_address_map_builder, ip_address_management_address_maps_remove_a_zone_membership_from_an_address_map_task,
    ip_address_management_list_leases_builder, ip_address_management_list_leases_task,
    ip_address_management_prefixes_upload_loa_document_builder, ip_address_management_prefixes_upload_loa_document_task,
    ip_address_management_prefixes_download_loa_document_builder, ip_address_management_prefixes_download_loa_document_task,
    ip_address_management_prefixes_list_prefixes_builder, ip_address_management_prefixes_list_prefixes_task,
    ip_address_management_prefixes_add_prefix_builder, ip_address_management_prefixes_add_prefix_task,
    ip_address_management_prefixes_prefix_details_builder, ip_address_management_prefixes_prefix_details_task,
    ip_address_management_prefixes_update_prefix_description_builder, ip_address_management_prefixes_update_prefix_description_task,
    ip_address_management_prefixes_delete_prefix_builder, ip_address_management_prefixes_delete_prefix_task,
    ip_address_management_prefixes_list_bgp_prefixes_builder, ip_address_management_prefixes_list_bgp_prefixes_task,
    ip_address_management_prefixes_create_bgp_prefix_builder, ip_address_management_prefixes_create_bgp_prefix_task,
    ip_address_management_prefixes_fetch_bgp_prefix_builder, ip_address_management_prefixes_fetch_bgp_prefix_task,
    ip_address_management_prefixes_update_bgp_prefix_builder, ip_address_management_prefixes_update_bgp_prefix_task,
    ip_address_management_prefixes_delete_bgp_prefix_builder, ip_address_management_prefixes_delete_bgp_prefix_task,
    ip_address_management_dynamic_advertisement_get_advertisement_status_builder, ip_address_management_dynamic_advertisement_get_advertisement_status_task,
    ip_address_management_dynamic_advertisement_update_prefix_dynamic_advertisement_status_builder, ip_address_management_dynamic_advertisement_update_prefix_dynamic_advertisement_status_task,
    ip_address_management_service_bindings_list_service_bindings_builder, ip_address_management_service_bindings_list_service_bindings_task,
    ip_address_management_service_bindings_create_service_binding_builder, ip_address_management_service_bindings_create_service_binding_task,
    ip_address_management_service_bindings_get_service_binding_builder, ip_address_management_service_bindings_get_service_binding_task,
    ip_address_management_service_bindings_delete_service_binding_builder, ip_address_management_service_bindings_delete_service_binding_task,
    ip_address_management_prefix_delegation_list_prefix_delegations_builder, ip_address_management_prefix_delegation_list_prefix_delegations_task,
    ip_address_management_prefix_delegation_create_prefix_delegation_builder, ip_address_management_prefix_delegation_create_prefix_delegation_task,
    ip_address_management_prefix_delegation_delete_prefix_delegation_builder, ip_address_management_prefix_delegation_delete_prefix_delegation_task,
    ip_address_management_prefixes_validate_prefix_builder, ip_address_management_prefixes_validate_prefix_task,
    dls_account_regional_hostnames_account_list_regions_builder, dls_account_regional_hostnames_account_list_regions_task,
    ip_address_management_service_bindings_list_services_builder, ip_address_management_service_bindings_list_services_task,
    aig_config_list_evaluators_builder, aig_config_list_evaluators_task,
    aig_config_list_gateway_builder, aig_config_list_gateway_task,
    aig_config_create_gateway_builder, aig_config_create_gateway_task,
    aig_config_list_dataset_builder, aig_config_list_dataset_task,
    aig_config_create_dataset_builder, aig_config_create_dataset_task,
    aig_config_fetch_dataset_builder, aig_config_fetch_dataset_task,
    aig_config_update_dataset_builder, aig_config_update_dataset_task,
    aig_config_delete_dataset_builder, aig_config_delete_dataset_task,
    aig_config_list_evaluations_builder, aig_config_list_evaluations_task,
    aig_config_create_evaluations_builder, aig_config_create_evaluations_task,
    aig_config_fetch_evaluations_builder, aig_config_fetch_evaluations_task,
    aig_config_delete_evaluations_builder, aig_config_delete_evaluations_task,
    aig_config_list_gateway_logs_builder, aig_config_list_gateway_logs_task,
    aig_config_delete_gateway_logs_builder, aig_config_delete_gateway_logs_task,
    aig_config_get_gateway_log_detail_builder, aig_config_get_gateway_log_detail_task,
    aig_config_patch_gateway_log_builder, aig_config_patch_gateway_log_task,
    aig_config_get_gateway_log_request_builder, aig_config_get_gateway_log_request_task,
    aig_config_get_gateway_log_response_builder, aig_config_get_gateway_log_response_task,
    aig_config_list_providers_builder, aig_config_list_providers_task,
    aig_config_create_providers_builder, aig_config_create_providers_task,
    aig_config_update_providers_builder, aig_config_update_providers_task,
    aig_config_delete_providers_builder, aig_config_delete_providers_task,
    aig_config_list_gateway_dynamic_routes_builder, aig_config_list_gateway_dynamic_routes_task,
    aig_config_post_gateway_dynamic_route_builder, aig_config_post_gateway_dynamic_route_task,
    aig_config_get_gateway_dynamic_route_builder, aig_config_get_gateway_dynamic_route_task,
    aig_config_update_gateway_dynamic_route_builder, aig_config_update_gateway_dynamic_route_task,
    aig_config_delete_gateway_dynamic_route_builder, aig_config_delete_gateway_dynamic_route_task,
    aig_config_list_gateway_dynamic_route_deployments_builder, aig_config_list_gateway_dynamic_route_deployments_task,
    aig_config_post_gateway_dynamic_route_deployment_builder, aig_config_post_gateway_dynamic_route_deployment_task,
    aig_config_list_gateway_dynamic_route_versions_builder, aig_config_list_gateway_dynamic_route_versions_task,
    aig_config_post_gateway_dynamic_route_version_builder, aig_config_post_gateway_dynamic_route_version_task,
    aig_config_get_gateway_dynamic_route_version_builder, aig_config_get_gateway_dynamic_route_version_task,
    aig_config_get_gateway_url_builder, aig_config_get_gateway_url_task,
    aig_config_fetch_gateway_builder, aig_config_fetch_gateway_task,
    aig_config_update_gateway_builder, aig_config_update_gateway_task,
    aig_config_delete_gateway_builder, aig_config_delete_gateway_task,
    ai_search_list_instances_builder, ai_search_list_instances_task,
    ai_search_create_instances_builder, ai_search_create_instances_task,
    ai_search_fetch_instances_builder, ai_search_fetch_instances_task,
    ai_search_update_instances_builder, ai_search_update_instances_task,
    ai_search_delete_instances_builder, ai_search_delete_instances_task,
    ai_search_instance_chat_completion_builder, ai_search_instance_chat_completion_task,
    ai_search_instance_list_jobs_builder, ai_search_instance_list_jobs_task,
    ai_search_instance_create_job_builder, ai_search_instance_create_job_task,
    ai_search_instance_get_job_builder, ai_search_instance_get_job_task,
    ai_search_instance_change_job_status_builder, ai_search_instance_change_job_status_task,
    ai_search_instance_list_job_logs_builder, ai_search_instance_list_job_logs_task,
    ai_search_instance_search_builder, ai_search_instance_search_task,
    ai_search_stats_builder, ai_search_stats_task,
    ai_search_list_tokens_builder, ai_search_list_tokens_task,
    ai_search_create_tokens_builder, ai_search_create_tokens_task,
    ai_search_fetch_tokens_builder, ai_search_fetch_tokens_task,
    ai_search_update_tokens_builder, ai_search_update_tokens_task,
    ai_search_delete_tokens_builder, ai_search_delete_tokens_task,
    workers_ai_search_author_builder, workers_ai_search_author_task,
    workers_ai_list_finetunes_builder, workers_ai_list_finetunes_task,
    workers_ai_create_finetune_builder, workers_ai_create_finetune_task,
    workers_ai_list_public_finetunes_builder, workers_ai_list_public_finetunes_task,
    workers_ai_upload_finetune_asset_builder, workers_ai_upload_finetune_asset_task,
    workers_ai_get_model_schema_builder, workers_ai_get_model_schema_task,
    workers_ai_search_model_builder, workers_ai_search_model_task,
    workers_ai_post_run_cf_ai4bharat_indictrans2_en_indic_1_b_builder, workers_ai_post_run_cf_ai4bharat_indictrans2_en_indic_1_b_task,
    workers_ai_post_run_cf_ai4bharat_nonomni_indictrans2_en_indic_1b_builder, workers_ai_post_run_cf_ai4bharat_nonomni_indictrans2_en_indic_1b_task,
    workers_ai_post_run_cf_aisingapore_gemma_sea_lion_v4_27b_it_builder, workers_ai_post_run_cf_aisingapore_gemma_sea_lion_v4_27b_it_task,
    workers_ai_post_run_cf_baai_bge_base_en_v1_5_builder, workers_ai_post_run_cf_baai_bge_base_en_v1_5_task,
    workers_ai_post_run_cf_baai_bge_large_en_v1_5_builder, workers_ai_post_run_cf_baai_bge_large_en_v1_5_task,
    workers_ai_post_run_cf_baai_bge_m3_builder, workers_ai_post_run_cf_baai_bge_m3_task,
    workers_ai_post_run_cf_baai_bge_reranker_base_builder, workers_ai_post_run_cf_baai_bge_reranker_base_task,
    workers_ai_post_run_cf_baai_bge_small_en_v1_5_builder, workers_ai_post_run_cf_baai_bge_small_en_v1_5_task,
    workers_ai_post_run_cf_baai_nonomni_bge_base_en_v1_5_builder, workers_ai_post_run_cf_baai_nonomni_bge_base_en_v1_5_task,
    workers_ai_post_run_cf_baai_nonomni_bge_large_en_v1_5_builder, workers_ai_post_run_cf_baai_nonomni_bge_large_en_v1_5_task,
    workers_ai_post_run_cf_baai_nonomni_bge_m3_builder, workers_ai_post_run_cf_baai_nonomni_bge_m3_task,
    workers_ai_post_run_cf_baai_nonomni_bge_small_en_v1_5_builder, workers_ai_post_run_cf_baai_nonomni_bge_small_en_v1_5_task,
    workers_ai_post_run_cf_black_forest_labs_flux_1_schnell_builder, workers_ai_post_run_cf_black_forest_labs_flux_1_schnell_task,
    workers_ai_post_run_cf_black_forest_labs_flux_2_dev_builder, workers_ai_post_run_cf_black_forest_labs_flux_2_dev_task,
    workers_ai_post_run_cf_black_forest_labs_flux_2_klein_4b_builder, workers_ai_post_run_cf_black_forest_labs_flux_2_klein_4b_task,
    workers_ai_post_run_cf_black_forest_labs_flux_2_klein_9b_builder, workers_ai_post_run_cf_black_forest_labs_flux_2_klein_9b_task,
    workers_ai_post_run_cf_bytedance_stable_diffusion_xl_lightning_builder, workers_ai_post_run_cf_bytedance_stable_diffusion_xl_lightning_task,
    workers_ai_post_websocket_run_cf_deepgram_aura_builder, workers_ai_post_websocket_run_cf_deepgram_aura_task,
    workers_ai_post_websocket_run_cf_deepgram_aura_1_builder, workers_ai_post_websocket_run_cf_deepgram_aura_1_task,
    workers_ai_post_run_cf_deepgram_aura_1_builder, workers_ai_post_run_cf_deepgram_aura_1_task,
    workers_ai_post_websocket_run_cf_deepgram_aura_1_internal_builder, workers_ai_post_websocket_run_cf_deepgram_aura_1_internal_task,
    workers_ai_post_websocket_run_cf_deepgram_aura_2_builder, workers_ai_post_websocket_run_cf_deepgram_aura_2_task,
    workers_ai_post_websocket_run_cf_deepgram_aura_2_en_builder, workers_ai_post_websocket_run_cf_deepgram_aura_2_en_task,
    workers_ai_post_run_cf_deepgram_aura_2_en_builder, workers_ai_post_run_cf_deepgram_aura_2_en_task,
    workers_ai_post_websocket_run_cf_deepgram_aura_2_es_builder, workers_ai_post_websocket_run_cf_deepgram_aura_2_es_task,
    workers_ai_post_run_cf_deepgram_aura_2_es_builder, workers_ai_post_run_cf_deepgram_aura_2_es_task,
    workers_ai_post_websocket_run_cf_deepgram_flux_builder, workers_ai_post_websocket_run_cf_deepgram_flux_task,
    workers_ai_post_run_cf_deepgram_flux_builder, workers_ai_post_run_cf_deepgram_flux_task,
    workers_ai_post_websocket_run_cf_deepgram_nova_3_builder, workers_ai_post_websocket_run_cf_deepgram_nova_3_task,
    workers_ai_post_run_cf_deepgram_nova_3_builder, workers_ai_post_run_cf_deepgram_nova_3_task,
    workers_ai_post_websocket_run_cf_deepgram_nova_3_internal_builder, workers_ai_post_websocket_run_cf_deepgram_nova_3_internal_task,
    workers_ai_post_websocket_run_cf_deepgram_nova_3_ws_builder, workers_ai_post_websocket_run_cf_deepgram_nova_3_ws_task,
    workers_ai_post_run_cf_deepseek_ai_deepseek_math_7b_instruct_builder, workers_ai_post_run_cf_deepseek_ai_deepseek_math_7b_instruct_task,
    workers_ai_post_run_cf_deepseek_ai_deepseek_r1_distill_qwen_32b_builder, workers_ai_post_run_cf_deepseek_ai_deepseek_r1_distill_qwen_32b_task,
    workers_ai_post_run_cf_defog_sqlcoder_7b_2_builder, workers_ai_post_run_cf_defog_sqlcoder_7b_2_task,
    workers_ai_post_run_cf_facebook_bart_large_cnn_builder, workers_ai_post_run_cf_facebook_bart_large_cnn_task,
    workers_ai_post_run_cf_facebook_nonomni_bart_large_cnn_builder, workers_ai_post_run_cf_facebook_nonomni_bart_large_cnn_task,
    workers_ai_post_run_cf_facebook_nonomni_detr_resnet_50_builder, workers_ai_post_run_cf_facebook_nonomni_detr_resnet_50_task,
    workers_ai_post_run_cf_fblgit_una_cybertron_7b_v2_bf16_builder, workers_ai_post_run_cf_fblgit_una_cybertron_7b_v2_bf16_task,
    workers_ai_post_run_cf_google_embeddinggemma_300m_builder, workers_ai_post_run_cf_google_embeddinggemma_300m_task,
    workers_ai_post_run_cf_google_gemma_2b_it_lora_builder, workers_ai_post_run_cf_google_gemma_2b_it_lora_task,
    workers_ai_post_run_cf_google_gemma_3_12b_it_builder, workers_ai_post_run_cf_google_gemma_3_12b_it_task,
    workers_ai_post_run_cf_google_gemma_7b_it_lora_builder, workers_ai_post_run_cf_google_gemma_7b_it_lora_task,
    workers_ai_post_run_cf_google_nonomni_embeddinggemma_300m_builder, workers_ai_post_run_cf_google_nonomni_embeddinggemma_300m_task,
    workers_ai_post_run_cf_huggingface_distilbert_sst_2_int8_builder, workers_ai_post_run_cf_huggingface_distilbert_sst_2_int8_task,
    workers_ai_post_run_cf_huggingface_nonomni_distilbert_sst_2_int8_builder, workers_ai_post_run_cf_huggingface_nonomni_distilbert_sst_2_int8_task,
    workers_ai_post_run_cf_ibm_granite_granite_4_0_h_micro_builder, workers_ai_post_run_cf_ibm_granite_granite_4_0_h_micro_task,
    workers_ai_post_run_cf_leonardo_lucid_origin_builder, workers_ai_post_run_cf_leonardo_lucid_origin_task,
    workers_ai_post_run_cf_leonardo_phoenix_1_0_builder, workers_ai_post_run_cf_leonardo_phoenix_1_0_task,
    workers_ai_post_run_cf_lykon_dreamshaper_8_lcm_builder, workers_ai_post_run_cf_lykon_dreamshaper_8_lcm_task,
    workers_ai_post_run_cf_meta_llama_llama_2_7b_chat_hf_lora_builder, workers_ai_post_run_cf_meta_llama_llama_2_7b_chat_hf_lora_task,
    workers_ai_post_run_cf_meta_llama_2_7b_chat_fp16_builder, workers_ai_post_run_cf_meta_llama_2_7b_chat_fp16_task,
    workers_ai_post_run_cf_meta_llama_2_7b_chat_int8_builder, workers_ai_post_run_cf_meta_llama_2_7b_chat_int8_task,
    workers_ai_post_run_cf_meta_llama_3_8b_instruct_builder, workers_ai_post_run_cf_meta_llama_3_8b_instruct_task,
    workers_ai_post_run_cf_meta_llama_3_8b_instruct_awq_builder, workers_ai_post_run_cf_meta_llama_3_8b_instruct_awq_task,
    workers_ai_post_run_cf_meta_llama_3_1_70b_instruct_fp8_fast_builder, workers_ai_post_run_cf_meta_llama_3_1_70b_instruct_fp8_fast_task,
    workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_awq_builder, workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_awq_task,
    workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_fp8_builder, workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_fp8_task,
    workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_fp8_fast_builder, workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_fp8_fast_task,
    workers_ai_post_run_cf_meta_llama_3_2_11b_vision_instruct_builder, workers_ai_post_run_cf_meta_llama_3_2_11b_vision_instruct_task,
    workers_ai_post_run_cf_meta_llama_3_2_1b_instruct_builder, workers_ai_post_run_cf_meta_llama_3_2_1b_instruct_task,
    workers_ai_post_run_cf_meta_llama_3_2_3b_instruct_builder, workers_ai_post_run_cf_meta_llama_3_2_3b_instruct_task,
    workers_ai_post_run_cf_meta_llama_3_3_70b_instruct_fp8_fast_builder, workers_ai_post_run_cf_meta_llama_3_3_70b_instruct_fp8_fast_task,
    workers_ai_post_run_cf_meta_llama_4_scout_17b_16e_instruct_builder, workers_ai_post_run_cf_meta_llama_4_scout_17b_16e_instruct_task,
    workers_ai_post_run_cf_meta_llama_guard_3_8b_builder, workers_ai_post_run_cf_meta_llama_guard_3_8b_task,
    workers_ai_post_run_cf_meta_m2m100_1_2b_builder, workers_ai_post_run_cf_meta_m2m100_1_2b_task,
    workers_ai_post_run_cf_microsoft_nonomni_resnet_50_builder, workers_ai_post_run_cf_microsoft_nonomni_resnet_50_task,
    workers_ai_post_run_cf_microsoft_phi_2_builder, workers_ai_post_run_cf_microsoft_phi_2_task,
    workers_ai_post_run_cf_microsoft_resnet_50_builder, workers_ai_post_run_cf_microsoft_resnet_50_task,
    workers_ai_post_run_cf_mistral_mistral_7b_instruct_v0_1_builder, workers_ai_post_run_cf_mistral_mistral_7b_instruct_v0_1_task,
    workers_ai_post_run_cf_mistral_mistral_7b_instruct_v0_2_lora_builder, workers_ai_post_run_cf_mistral_mistral_7b_instruct_v0_2_lora_task,
    workers_ai_post_run_cf_mistralai_mistral_small_3_1_24b_instruct_builder, workers_ai_post_run_cf_mistralai_mistral_small_3_1_24b_instruct_task,
    workers_ai_post_run_cf_moonshotai_kimi_k2_5_builder, workers_ai_post_run_cf_moonshotai_kimi_k2_5_task,
    workers_ai_post_run_cf_myshell_ai_melotts_builder, workers_ai_post_run_cf_myshell_ai_melotts_task,
    workers_ai_post_run_cf_nvidia_nemotron_3_120b_a12b_builder, workers_ai_post_run_cf_nvidia_nemotron_3_120b_a12b_task,
    workers_ai_post_run_cf_openai_gpt_oss_120b_builder, workers_ai_post_run_cf_openai_gpt_oss_120b_task,
    workers_ai_post_run_cf_openai_gpt_oss_20b_builder, workers_ai_post_run_cf_openai_gpt_oss_20b_task,
    workers_ai_post_run_cf_openai_whisper_builder, workers_ai_post_run_cf_openai_whisper_task,
    workers_ai_post_run_cf_openai_whisper_large_v3_turbo_builder, workers_ai_post_run_cf_openai_whisper_large_v3_turbo_task,
    workers_ai_post_run_cf_openai_whisper_tiny_en_builder, workers_ai_post_run_cf_openai_whisper_tiny_en_task,
    workers_ai_post_run_cf_openchat_openchat_3_5_0106_builder, workers_ai_post_run_cf_openchat_openchat_3_5_0106_task,
    workers_ai_post_run_cf_pfnet_plamo_embedding_1b_builder, workers_ai_post_run_cf_pfnet_plamo_embedding_1b_task,
    workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_v2_builder, workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_v2_task,
    workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_v3_builder, workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_v3_task,
    workers_ai_post_run_cf_qwen_qwen1_5_0_5b_chat_builder, workers_ai_post_run_cf_qwen_qwen1_5_0_5b_chat_task,
    workers_ai_post_run_cf_qwen_qwen1_5_1_8b_chat_builder, workers_ai_post_run_cf_qwen_qwen1_5_1_8b_chat_task,
    workers_ai_post_run_cf_qwen_qwen1_5_14b_chat_awq_builder, workers_ai_post_run_cf_qwen_qwen1_5_14b_chat_awq_task,
    workers_ai_post_run_cf_qwen_qwen1_5_7b_chat_awq_builder, workers_ai_post_run_cf_qwen_qwen1_5_7b_chat_awq_task,
    workers_ai_post_run_cf_qwen_qwen2_5_coder_32b_instruct_builder, workers_ai_post_run_cf_qwen_qwen2_5_coder_32b_instruct_task,
    workers_ai_post_run_cf_qwen_qwen3_30b_a3b_fp8_builder, workers_ai_post_run_cf_qwen_qwen3_30b_a3b_fp8_task,
    workers_ai_post_run_cf_qwen_qwen3_embedding_0_6b_builder, workers_ai_post_run_cf_qwen_qwen3_embedding_0_6b_task,
    workers_ai_post_run_cf_qwen_qwq_32b_builder, workers_ai_post_run_cf_qwen_qwq_32b_task,
    workers_ai_post_run_cf_runwayml_stable_diffusion_v1_5_img2img_builder, workers_ai_post_run_cf_runwayml_stable_diffusion_v1_5_img2img_task,
    workers_ai_post_run_cf_runwayml_stable_diffusion_v1_5_inpainting_builder, workers_ai_post_run_cf_runwayml_stable_diffusion_v1_5_inpainting_task,
    workers_ai_post_run_cf_stabilityai_stable_diffusion_xl_base_1_0_builder, workers_ai_post_run_cf_stabilityai_stable_diffusion_xl_base_1_0_task,
    workers_ai_post_websocket_run_cf_sven_test_pipe_http_builder, workers_ai_post_websocket_run_cf_sven_test_pipe_http_task,
    workers_ai_post_websocket_run_cf_test_hello_world_cog_builder, workers_ai_post_websocket_run_cf_test_hello_world_cog_task,
    workers_ai_post_run_cf_thebloke_discolm_german_7b_v1_awq_builder, workers_ai_post_run_cf_thebloke_discolm_german_7b_v1_awq_task,
    workers_ai_post_run_cf_tiiuae_falcon_7b_instruct_builder, workers_ai_post_run_cf_tiiuae_falcon_7b_instruct_task,
    workers_ai_post_run_cf_tinyllama_tinyllama_1_1b_chat_v1_0_builder, workers_ai_post_run_cf_tinyllama_tinyllama_1_1b_chat_v1_0_task,
    workers_ai_post_run_cf_zai_org_glm_4_7_flash_builder, workers_ai_post_run_cf_zai_org_glm_4_7_flash_task,
    workers_ai_post_run_hf_google_gemma_7b_it_builder, workers_ai_post_run_hf_google_gemma_7b_it_task,
    workers_ai_post_run_hf_mistral_mistral_7b_instruct_v0_2_builder, workers_ai_post_run_hf_mistral_mistral_7b_instruct_v0_2_task,
    workers_ai_post_run_hf_nexusflow_starling_lm_7b_beta_builder, workers_ai_post_run_hf_nexusflow_starling_lm_7b_beta_task,
    workers_ai_post_run_hf_nousresearch_hermes_2_pro_mistral_7b_builder, workers_ai_post_run_hf_nousresearch_hermes_2_pro_mistral_7b_task,
    workers_ai_post_run_hf_thebloke_deepseek_coder_6_7b_base_awq_builder, workers_ai_post_run_hf_thebloke_deepseek_coder_6_7b_base_awq_task,
    workers_ai_post_run_hf_thebloke_deepseek_coder_6_7b_instruct_awq_builder, workers_ai_post_run_hf_thebloke_deepseek_coder_6_7b_instruct_awq_task,
    workers_ai_post_run_hf_thebloke_llama_2_13b_chat_awq_builder, workers_ai_post_run_hf_thebloke_llama_2_13b_chat_awq_task,
    workers_ai_post_run_hf_thebloke_mistral_7b_instruct_v0_1_awq_builder, workers_ai_post_run_hf_thebloke_mistral_7b_instruct_v0_1_awq_task,
    workers_ai_post_run_hf_thebloke_neural_chat_7b_v3_1_awq_builder, workers_ai_post_run_hf_thebloke_neural_chat_7b_v3_1_awq_task,
    workers_ai_post_run_hf_thebloke_openhermes_2_5_mistral_7b_awq_builder, workers_ai_post_run_hf_thebloke_openhermes_2_5_mistral_7b_awq_task,
    workers_ai_post_run_hf_thebloke_zephyr_7b_beta_awq_builder, workers_ai_post_run_hf_thebloke_zephyr_7b_beta_awq_task,
    workers_ai_post_run_model_builder, workers_ai_post_run_model_task,
    workers_ai_search_task_builder, workers_ai_search_task_task,
    workers_ai_post_to_markdown_builder, workers_ai_post_to_markdown_task,
    workers_ai_get_to_markdown_supported_builder, workers_ai_get_to_markdown_supported_task,
    notification_alert_types_get_alert_types_builder, notification_alert_types_get_alert_types_task,
    notification_mechanism_eligibility_get_delivery_mechanism_eligibility_builder, notification_mechanism_eligibility_get_delivery_mechanism_eligibility_task,
    notification_destinations_with_pager_duty_list_pager_duty_services_builder, notification_destinations_with_pager_duty_list_pager_duty_services_task,
    notification_destinations_with_pager_duty_delete_pager_duty_services_builder, notification_destinations_with_pager_duty_delete_pager_duty_services_task,
    notification_destinations_with_pager_duty_connect_pager_duty_builder, notification_destinations_with_pager_duty_connect_pager_duty_task,
    notification_destinations_with_pager_duty_connect_pager_duty_token_builder, notification_destinations_with_pager_duty_connect_pager_duty_token_task,
    notification_webhooks_list_webhooks_builder, notification_webhooks_list_webhooks_task,
    notification_webhooks_create_a_webhook_builder, notification_webhooks_create_a_webhook_task,
    notification_webhooks_get_a_webhook_builder, notification_webhooks_get_a_webhook_task,
    notification_webhooks_update_a_webhook_builder, notification_webhooks_update_a_webhook_task,
    notification_webhooks_delete_a_webhook_builder, notification_webhooks_delete_a_webhook_task,
    notification_history_list_history_builder, notification_history_list_history_task,
    notification_policies_list_notification_policies_builder, notification_policies_list_notification_policies_task,
    notification_policies_create_a_notification_policy_builder, notification_policies_create_a_notification_policy_task,
    notification_policies_get_a_notification_policy_builder, notification_policies_get_a_notification_policy_task,
    notification_policies_update_a_notification_policy_builder, notification_policies_update_a_notification_policy_task,
    notification_policies_delete_a_notification_policy_builder, notification_policies_delete_a_notification_policy_task,
    notification_silences_list_silences_builder, notification_silences_list_silences_task,
    notification_silences_create_silences_builder, notification_silences_create_silences_task,
    notification_silences_update_silences_builder, notification_silences_update_silences_task,
    notification_silences_get_silence_builder, notification_silences_get_silence_task,
    notification_silences_delete_silences_builder, notification_silences_delete_silences_task,
    audit_logs_get_account_audit_logs_builder, audit_logs_get_account_audit_logs_task,
    autorag_config_ai_search_builder, autorag_config_ai_search_task,
    autorag_config_files_builder, autorag_config_files_task,
    autorag_config_list_jobs_builder, autorag_config_list_jobs_task,
    autorag_config_get_job_builder, autorag_config_get_job_task,
    autorag_config_list_job_logs_builder, autorag_config_list_job_logs_task,
    autorag_config_search_builder, autorag_config_search_task,
    autorag_config_sync_builder, autorag_config_sync_task,
    account_billing_profile_deprecated_billing_profile_details_builder, account_billing_profile_deprecated_billing_profile_details_task,
    billable_usage_get_paygo_account_usage_builder, billable_usage_get_paygo_account_usage_task,
    botnet_threat_feed_get_day_report_builder, botnet_threat_feed_get_day_report_task,
    botnet_threat_feed_get_full_report_builder, botnet_threat_feed_get_full_report_task,
    botnet_threat_feed_list_asn_builder, botnet_threat_feed_list_asn_task,
    botnet_threat_feed_delete_asn_builder, botnet_threat_feed_delete_asn_task,
    brapi_post_content_builder, brapi_post_content_task,
    brapi_post_crawl_builder, brapi_post_crawl_task,
    brapi_get_crawl_result_builder, brapi_get_crawl_result_task,
    brapi_delete_cancel_crawl_builder, brapi_delete_cancel_crawl_task,
    brapi_post_json_builder, brapi_post_json_task,
    brapi_post_links_builder, brapi_post_links_task,
    brapi_post_markdown_builder, brapi_post_markdown_task,
    brapi_post_pdf_builder, brapi_post_pdf_task,
    brapi_post_scrape_builder, brapi_post_scrape_task,
    brapi_post_screenshot_builder, brapi_post_screenshot_task,
    brapi_post_snapshot_builder, brapi_post_snapshot_task,
    get_account_limits_builder, get_account_limits_task,
    get_builds_by_version_ids_builder, get_builds_by_version_ids_task,
    get_latest_builds_by_scripts_builder, get_latest_builds_by_scripts_task,
    get_build_by_uuid_builder, get_build_by_uuid_task,
    cancel_build_by_uuid_builder, cancel_build_by_uuid_task,
    get_build_logs_builder, get_build_logs_task,
    upsert_repo_connection_builder, upsert_repo_connection_task,
    delete_repo_connection_builder, delete_repo_connection_task,
    get_worker_config_autofill_builder, get_worker_config_autofill_task,
    list_build_tokens_builder, list_build_tokens_task,
    create_build_token_builder, create_build_token_task,
    delete_build_token_builder, delete_build_token_task,
    create_trigger_builder, create_trigger_task,
    update_trigger_builder, update_trigger_task,
    delete_trigger_builder, delete_trigger_task,
    create_manual_build_builder, create_manual_build_task,
    list_environment_variables_builder, list_environment_variables_task,
    upsert_environment_variables_builder, upsert_environment_variables_task,
    delete_environment_variable_builder, delete_environment_variable_task,
    purge_build_cache_builder, purge_build_cache_task,
    list_builds_by_script_builder, list_builds_by_script_task,
    list_triggers_by_script_builder, list_triggers_by_script_task,
    calls_apps_list_builder, calls_apps_list_task,
    calls_apps_create_a_new_app_builder, calls_apps_create_a_new_app_task,
    calls_apps_retrieve_app_details_builder, calls_apps_retrieve_app_details_task,
    calls_apps_update_app_details_builder, calls_apps_update_app_details_task,
    calls_apps_delete_app_builder, calls_apps_delete_app_task,
    calls_turn_key_list_builder, calls_turn_key_list_task,
    calls_turn_key_create_builder, calls_turn_key_create_task,
    calls_retrieve_turn_key_details_builder, calls_retrieve_turn_key_details_task,
    calls_update_turn_key_builder, calls_update_turn_key_task,
    calls_delete_turn_key_builder, calls_delete_turn_key_task,
    cloudflare_tunnel_list_cloudflare_tunnels_builder, cloudflare_tunnel_list_cloudflare_tunnels_task,
    cloudflare_tunnel_create_a_cloudflare_tunnel_builder, cloudflare_tunnel_create_a_cloudflare_tunnel_task,
    cloudflare_tunnel_get_a_cloudflare_tunnel_builder, cloudflare_tunnel_get_a_cloudflare_tunnel_task,
    cloudflare_tunnel_update_a_cloudflare_tunnel_builder, cloudflare_tunnel_update_a_cloudflare_tunnel_task,
    cloudflare_tunnel_delete_a_cloudflare_tunnel_builder, cloudflare_tunnel_delete_a_cloudflare_tunnel_task,
    cloudflare_tunnel_configuration_get_configuration_builder, cloudflare_tunnel_configuration_get_configuration_task,
    cloudflare_tunnel_configuration_put_configuration_builder, cloudflare_tunnel_configuration_put_configuration_task,
    cloudflare_tunnel_list_cloudflare_tunnel_connections_builder, cloudflare_tunnel_list_cloudflare_tunnel_connections_task,
    cloudflare_tunnel_clean_up_cloudflare_tunnel_connections_builder, cloudflare_tunnel_clean_up_cloudflare_tunnel_connections_task,
    cloudflare_tunnel_get_cloudflare_tunnel_connector_builder, cloudflare_tunnel_get_cloudflare_tunnel_connector_task,
    cloudflare_tunnel_get_a_cloudflare_tunnel_management_token_builder, cloudflare_tunnel_get_a_cloudflare_tunnel_management_token_task,
    cloudflare_tunnel_get_a_cloudflare_tunnel_token_builder, cloudflare_tunnel_get_a_cloudflare_tunnel_token_task,
    accounts_turnstile_widgets_list_builder, accounts_turnstile_widgets_list_task,
    accounts_turnstile_widget_create_builder, accounts_turnstile_widget_create_task,
    accounts_turnstile_widget_get_builder, accounts_turnstile_widget_get_task,
    accounts_turnstile_widget_update_builder, accounts_turnstile_widget_update_task,
    accounts_turnstile_widget_delete_builder, accounts_turnstile_widget_delete_task,
    accounts_turnstile_widget_rotate_secret_builder, accounts_turnstile_widget_rotate_secret_task,
    post_bin_db_post_builder, post_bin_db_post_task,
    get_bin_db_get_binary_builder, get_bin_db_get_binary_task,
    get_event_list_get_builder, get_event_list_get_task,
    get_event_aggregate_builder, get_event_aggregate_task,
    get_attacker_list_builder, get_attacker_list_task,
    get_category_list_builder, get_category_list_task,
    get_category_list_complete_builder, get_category_list_complete_task,
    post_category_create_builder, post_category_create_task,
    get_category_read_builder, get_category_read_task,
    post_category_update_builder, post_category_update_task,
    patch_category_update_builder, patch_category_update_task,
    delete_category_delete_builder, delete_category_delete_task,
    get_country_read_builder, get_country_read_task,
    post_event_create_builder, post_event_create_task,
    post_event_create_bulk_builder, post_event_create_bulk_task,
    post_dos_event_create_bulk_with_relationships_builder, post_dos_event_create_bulk_with_relationships_task,
    get_dataset_list_builder, get_dataset_list_task,
    get_group_list_builder, get_group_list_task,
    post_group_create_builder, post_group_create_task,
    get_group_read_builder, get_group_read_task,
    put_group_update_builder, put_group_update_task,
    delete_group_delete_builder, delete_group_delete_task,
    get_group_member_list_builder, get_group_member_list_task,
    post_group_member_create_builder, post_group_member_create_task,
    delete_group_member_delete_builder, delete_group_member_delete_task,
    post_dataset_create_builder, post_dataset_create_task,
    get_dataset_read_builder, get_dataset_read_task,
    post_dataset_update_builder, post_dataset_update_task,
    patch_dataset_update_builder, patch_dataset_update_task,
    delete_dataset_delete_builder, delete_dataset_delete_task,
    post_event_copy_to_new_ds_builder, post_event_copy_to_new_ds_task,
    get_event_read_builder, get_event_read_task,
    post_indicator_type_create_builder, post_indicator_type_create_task,
    get_indicator_list_legacy_builder, get_indicator_list_legacy_task,
    post_indicator_create_bulk_builder, post_indicator_create_bulk_task,
    post_indicator_create_builder, post_indicator_create_task,
    get_indicator_tags_list_builder, get_indicator_tags_list_task,
    get_indicator_read_builder, get_indicator_read_task,
    patch_indicator_update_builder, patch_indicator_update_task,
    delete_indicator_delete_builder, delete_indicator_delete_task,
    post_event_move_to_new_ds_builder, post_event_move_to_new_ds_task,
    get_permission_list_builder, get_permission_list_task,
    post_permission_create_builder, post_permission_create_task,
    put_permission_update_builder, put_permission_update_task,
    delete_permission_delete_builder, delete_permission_delete_task,
    get_tag_indicators_list_builder, get_tag_indicators_list_task,
    get_target_industry_list_by_dataset_builder, get_target_industry_list_by_dataset_task,
    post_dataset_populate_builder, post_dataset_populate_task,
    delete_event_tag_delete_builder, delete_event_tag_delete_task,
    post_event_tag_create_builder, post_event_tag_create_task,
    post_event_graph_ql_builder, post_event_graph_ql_task,
    get_indicator_types_list_builder, get_indicator_types_list_task,
    get_legacy_indicator_types_list_builder, get_legacy_indicator_types_list_task,
    get_indicator_list_builder, get_indicator_list_task,
    get_event_query_list_builder, get_event_query_list_task,
    get_event_query_alert_list_builder, get_event_query_alert_list_task,
    post_event_query_alert_create_builder, post_event_query_alert_create_task,
    get_event_query_alert_read_builder, get_event_query_alert_read_task,
    post_event_query_alert_update_builder, post_event_query_alert_update_task,
    patch_event_query_alert_update_builder, patch_event_query_alert_update_task,
    delete_event_query_alert_delete_builder, delete_event_query_alert_delete_task,
    post_event_query_create_builder, post_event_query_create_task,
    get_event_query_read_builder, get_event_query_read_task,
    post_event_query_update_builder, post_event_query_update_task,
    patch_event_query_update_builder, patch_event_query_update_task,
    delete_event_query_delete_builder, delete_event_query_delete_task,
    get_event_raw_read_ds_builder, get_event_raw_read_ds_task,
    delete_event_reference_delete_builder, delete_event_reference_delete_task,
    post_event_reference_create_builder, post_event_reference_create_task,
    post_create_event_relationship_builder, post_create_event_relationship_task,
    get_tag_list_builder, get_tag_list_task,
    get_tag_category_list_builder, get_tag_category_list_task,
    post_tag_category_create_builder, post_tag_category_create_task,
    patch_tag_category_update_builder, patch_tag_category_update_task,
    delete_tag_category_delete_builder, delete_tag_category_delete_task,
    post_tag_create_builder, post_tag_create_task,
    patch_tag_update_builder, patch_tag_update_task,
    delete_tag_delete_builder, delete_tag_delete_task,
    get_target_industry_list_builder, get_target_industry_list_task,
    get_target_industry_list_complete_builder, get_target_industry_list_complete_task,
    patch_event_update_bulk_builder, patch_event_update_bulk_task,
    delete_event_delete_builder, delete_event_delete_task,
    post_event_do_revert_builder, post_event_do_revert_task,
    get_event_read_deprecated_builder, get_event_read_deprecated_task,
    post_event_update_builder, post_event_update_task,
    patch_event_update_builder, patch_event_update_task,
    get_event_raw_read_builder, get_event_raw_read_task,
    post_event_raw_update_builder, post_event_raw_update_task,
    patch_event_raw_update_builder, patch_event_raw_update_task,
    get_event_relationships_builder, get_event_relationships_task,
    cloudforce_one_request_list_builder, cloudforce_one_request_list_task,
    cloudforce_one_request_constants_builder, cloudforce_one_request_constants_task,
    cloudforce_one_request_new_builder, cloudforce_one_request_new_task,
    cloudforce_one_priority_list_builder, cloudforce_one_priority_list_task,
    cloudforce_one_priority_new_builder, cloudforce_one_priority_new_task,
    cloudforce_one_priority_quota_builder, cloudforce_one_priority_quota_task,
    cloudforce_one_priority_get_builder, cloudforce_one_priority_get_task,
    cloudforce_one_priority_update_builder, cloudforce_one_priority_update_task,
    cloudforce_one_priority_delete_builder, cloudforce_one_priority_delete_task,
    cloudforce_one_request_quota_builder, cloudforce_one_request_quota_task,
    cloudforce_one_request_types_builder, cloudforce_one_request_types_task,
    cloudforce_one_request_get_builder, cloudforce_one_request_get_task,
    cloudforce_one_request_update_builder, cloudforce_one_request_update_task,
    cloudforce_one_request_delete_builder, cloudforce_one_request_delete_task,
    cloudforce_one_request_asset_list_builder, cloudforce_one_request_asset_list_task,
    cloudforce_one_request_asset_new_builder, cloudforce_one_request_asset_new_task,
    cloudforce_one_request_asset_get_builder, cloudforce_one_request_asset_get_task,
    cloudforce_one_request_asset_update_builder, cloudforce_one_request_asset_update_task,
    cloudforce_one_request_asset_delete_builder, cloudforce_one_request_asset_delete_task,
    cloudforce_one_request_message_list_builder, cloudforce_one_request_message_list_task,
    cloudforce_one_request_message_new_builder, cloudforce_one_request_message_new_task,
    cloudforce_one_request_message_update_builder, cloudforce_one_request_message_update_task,
    cloudforce_one_request_message_delete_builder, cloudforce_one_request_message_delete_task,
    cloudforce_one_list_rules_builder, cloudforce_one_list_rules_task,
    cloudforce_one_create_rule_builder, cloudforce_one_create_rule_task,
    cloudforce_one_delete_all_rules_builder, cloudforce_one_delete_all_rules_task,
    cloudforce_one_get_rule_stats_builder, cloudforce_one_get_rule_stats_task,
    cloudforce_one_get_rule_tree_builder, cloudforce_one_get_rule_tree_task,
    cloudforce_one_validate_rule_builder, cloudforce_one_validate_rule_task,
    cloudforce_one_get_rule_builder, cloudforce_one_get_rule_task,
    cloudforce_one_update_rule_builder, cloudforce_one_update_rule_task,
    cloudforce_one_delete_rule_builder, cloudforce_one_delete_rule_task,
    get_config_fetch_builder, get_config_fetch_task,
    post_config_create_builder, post_config_create_task,
    post_config_update_builder, post_config_update_task,
    delete_delete_scans_builder, delete_delete_scans_task,
    get_get_open_ports_builder, get_get_open_ports_task,
    get_domain_match_list_builder, get_domain_match_list_task,
    get_get_domain_queries_builder, get_get_domain_queries_task,
    get_logo_match_list_builder, get_logo_match_list_task,
    get_get_logo_queries_builder, get_get_logo_queries_task,
    post_insert_logo_query_builder, post_insert_logo_query_task,
    delete_delete_logo_query_builder, delete_delete_logo_query_task,
    post_search_logo_similarity_builder, post_search_logo_similarity_task,
    get_total_queries_builder, get_total_queries_task,
    post_event_graph_qlv2_builder, post_event_graph_qlv2_task,
    list_cnis_builder, list_cnis_task,
    create_cni_builder, create_cni_task,
    get_cni_builder, get_cni_task,
    update_cni_builder, update_cni_task,
    delete_cni_builder, delete_cni_task,
    list_interconnects_builder, list_interconnects_task,
    create_interconnect_builder, create_interconnect_task,
    get_interconnect_builder, get_interconnect_task,
    delete_interconnect_builder, delete_interconnect_task,
    get_interconnect_loa_builder, get_interconnect_loa_task,
    get_interconnect_status_builder, get_interconnect_status_task,
    get_settings_builder, get_settings_task,
    update_settings_builder, update_settings_task,
    list_slots_builder, list_slots_task,
    get_slot_builder, get_slot_task,
    connectivity_services_list_builder, connectivity_services_list_task,
    connectivity_services_post_builder, connectivity_services_post_task,
    connectivity_services_get_builder, connectivity_services_get_task,
    connectivity_services_put_builder, connectivity_services_put_task,
    connectivity_services_delete_builder, connectivity_services_delete_task,
    public_list_applications_builder, public_list_applications_task,
    generate_image_registry_credentials_builder, generate_image_registry_credentials_task,
    account_level_custom_nameservers_list_account_custom_nameservers_builder, account_level_custom_nameservers_list_account_custom_nameservers_task,
    account_level_custom_nameservers_add_account_custom_nameserver_builder, account_level_custom_nameservers_add_account_custom_nameserver_task,
    account_level_custom_nameservers_delete_account_custom_nameserver_builder, account_level_custom_nameservers_delete_account_custom_nameserver_task,
    d1_list_databases_builder, d1_list_databases_task,
    d1_create_database_builder, d1_create_database_task,
    d1_get_database_builder, d1_get_database_task,
    d1_update_database_builder, d1_update_database_task,
    d1_update_partial_database_builder, d1_update_partial_database_task,
    d1_delete_database_builder, d1_delete_database_task,
    d1_export_database_builder, d1_export_database_task,
    d1_import_database_builder, d1_import_database_task,
    d1_query_database_builder, d1_query_database_task,
    d1_raw_database_query_builder, d1_raw_database_query_task,
    d1_time_travel_get_bookmark_builder, d1_time_travel_get_bookmark_task,
    d1_time_travel_restore_builder, d1_time_travel_restore_task,
    devices_list_devices_builder, devices_list_devices_task,
    list_ip_profiles_builder, list_ip_profiles_task,
    create_ip_profile_builder, create_ip_profile_task,
    get_ip_profile_builder, get_ip_profile_task,
    update_ip_profile_builder, update_ip_profile_task,
    delete_ip_profile_builder, delete_ip_profile_task,
    device_managed_networks_list_device_managed_networks_builder, device_managed_networks_list_device_managed_networks_task,
    device_managed_networks_create_device_managed_network_builder, device_managed_networks_create_device_managed_network_task,
    device_managed_networks_device_managed_network_details_builder, device_managed_networks_device_managed_network_details_task,
    device_managed_networks_update_device_managed_network_builder, device_managed_networks_update_device_managed_network_task,
    device_managed_networks_delete_device_managed_network_builder, device_managed_networks_delete_device_managed_network_task,
    list_devices_builder, list_devices_task,
    get_device_builder, get_device_task,
    delete_device_builder, delete_device_task,
    revoke_device_builder, revoke_device_task,
    devices_list_device_settings_policies_builder, devices_list_device_settings_policies_task,
    devices_get_default_device_settings_policy_builder, devices_get_default_device_settings_policy_task,
    devices_create_device_settings_policy_builder, devices_create_device_settings_policy_task,
    devices_update_default_device_settings_policy_builder, devices_update_default_device_settings_policy_task,
    devices_get_split_tunnel_exclude_list_builder, devices_get_split_tunnel_exclude_list_task,
    devices_set_split_tunnel_exclude_list_builder, devices_set_split_tunnel_exclude_list_task,
    devices_get_local_domain_fallback_list_builder, devices_get_local_domain_fallback_list_task,
    devices_set_local_domain_fallback_list_builder, devices_set_local_domain_fallback_list_task,
    devices_get_split_tunnel_include_list_builder, devices_get_split_tunnel_include_list_task,
    devices_set_split_tunnel_include_list_builder, devices_set_split_tunnel_include_list_task,
    devices_get_device_settings_policy_by_id_builder, devices_get_device_settings_policy_by_id_task,
    devices_update_device_settings_policy_builder, devices_update_device_settings_policy_task,
    devices_delete_device_settings_policy_builder, devices_delete_device_settings_policy_task,
    devices_get_split_tunnel_exclude_list_for_a_device_settings_policy_builder, devices_get_split_tunnel_exclude_list_for_a_device_settings_policy_task,
    devices_set_split_tunnel_exclude_list_for_a_device_settings_policy_builder, devices_set_split_tunnel_exclude_list_for_a_device_settings_policy_task,
    devices_get_local_domain_fallback_list_for_a_device_settings_policy_builder, devices_get_local_domain_fallback_list_for_a_device_settings_policy_task,
    devices_set_local_domain_fallback_list_for_a_device_settings_policy_builder, devices_set_local_domain_fallback_list_for_a_device_settings_policy_task,
    devices_get_split_tunnel_include_list_for_a_device_settings_policy_builder, devices_get_split_tunnel_include_list_for_a_device_settings_policy_task,
    devices_set_split_tunnel_include_list_for_a_device_settings_policy_builder, devices_set_split_tunnel_include_list_for_a_device_settings_policy_task,
    device_posture_rules_list_device_posture_rules_builder, device_posture_rules_list_device_posture_rules_task,
    device_posture_rules_create_device_posture_rule_builder, device_posture_rules_create_device_posture_rule_task,
    device_posture_integrations_list_device_posture_integrations_builder, device_posture_integrations_list_device_posture_integrations_task,
    device_posture_integrations_create_device_posture_integration_builder, device_posture_integrations_create_device_posture_integration_task,
    device_posture_integrations_device_posture_integration_details_builder, device_posture_integrations_device_posture_integration_details_task,
    device_posture_integrations_update_device_posture_integration_builder, device_posture_integrations_update_device_posture_integration_task,
    device_posture_integrations_delete_device_posture_integration_builder, device_posture_integrations_delete_device_posture_integration_task,
    device_posture_rules_device_posture_rules_details_builder, device_posture_rules_device_posture_rules_details_task,
    device_posture_rules_update_device_posture_rule_builder, device_posture_rules_update_device_posture_rule_task,
    device_posture_rules_delete_device_posture_rule_builder, device_posture_rules_delete_device_posture_rule_task,
    list_registrations_builder, list_registrations_task,
    delete_registrations_builder, delete_registrations_task,
    revoke_registrations_builder, revoke_registrations_task,
    unrevoke_registrations_builder, unrevoke_registrations_task,
    get_registration_builder, get_registration_task,
    delete_registration_builder, delete_registration_task,
    get_registration_override_codes_builder, get_registration_override_codes_task,
    devices_resilience_retrieve_global_warp_override_builder, devices_resilience_retrieve_global_warp_override_task,
    devices_resilience_set_global_warp_override_builder, devices_resilience_set_global_warp_override_task,
    devices_revoke_devices_builder, devices_revoke_devices_task,
    zero_trust_accounts_get_device_settings_for_zero_trust_account_builder, zero_trust_accounts_get_device_settings_for_zero_trust_account_task,
    zero_trust_accounts_update_device_settings_for_the_zero_trust_account_builder, zero_trust_accounts_update_device_settings_for_the_zero_trust_account_task,
    zero_trust_accounts_patch_device_settings_for_the_zero_trust_account_builder, zero_trust_accounts_patch_device_settings_for_the_zero_trust_account_task,
    zero_trust_accounts_delete_device_settings_for_zero_trust_account_builder, zero_trust_accounts_delete_device_settings_for_zero_trust_account_task,
    devices_unrevoke_devices_builder, devices_unrevoke_devices_task,
    devices_device_details_builder, devices_device_details_task,
    devices_list_admin_override_code_for_device_builder, devices_list_admin_override_code_for_device_task,
    dex_endpoints_list_colos_builder, dex_endpoints_list_colos_task,
    get_commands_builder, get_commands_task,
    post_commands_builder, post_commands_task,
    get_commands_eligible_devices_builder, get_commands_eligible_devices_task,
    get_commands_quota_builder, get_commands_quota_task,
    get_commands_command_id_downloads_filename_builder, get_commands_command_id_downloads_filename_task,
    device_dex_test_details_builder, device_dex_test_details_task,
    device_dex_test_create_device_dex_test_builder, device_dex_test_create_device_dex_test_task,
    device_dex_test_get_device_dex_test_builder, device_dex_test_get_device_dex_test_task,
    device_dex_test_update_device_dex_test_builder, device_dex_test_update_device_dex_test_task,
    device_dex_test_delete_device_dex_test_builder, device_dex_test_delete_device_dex_test_task,
    devices_live_status_builder, devices_live_status_task,
    dex_fleet_status_devices_builder, dex_fleet_status_devices_task,
    dex_fleet_status_live_builder, dex_fleet_status_live_task,
    dex_fleet_status_over_time_builder, dex_fleet_status_over_time_task,
    dex_endpoints_http_test_details_builder, dex_endpoints_http_test_details_task,
    dex_endpoints_http_test_percentiles_builder, dex_endpoints_http_test_percentiles_task,
    list_dex_rules_builder, list_dex_rules_task,
    create_dex_rule_builder, create_dex_rule_task,
    get_dex_rule_builder, get_dex_rule_task,
    update_dex_rule_builder, update_dex_rule_task,
    delete_dex_rule_builder, delete_dex_rule_task,
    dex_endpoints_list_tests_overview_builder, dex_endpoints_list_tests_overview_task,
    dex_endpoints_tests_unique_devices_builder, dex_endpoints_tests_unique_devices_task,
    dex_endpoints_traceroute_test_result_network_path_builder, dex_endpoints_traceroute_test_result_network_path_task,
    dex_endpoints_traceroute_test_details_builder, dex_endpoints_traceroute_test_details_task,
    dex_endpoints_traceroute_test_network_path_builder, dex_endpoints_traceroute_test_network_path_task,
    dex_endpoints_traceroute_test_percentiles_builder, dex_endpoints_traceroute_test_percentiles_task,
    list_warp_change_events_builder, list_warp_change_events_task,
    diagnostics_endpoint_healthcheck_list_builder, diagnostics_endpoint_healthcheck_list_task,
    diagnostics_endpoint_healthcheck_create_builder, diagnostics_endpoint_healthcheck_create_task,
    diagnostics_endpoint_healthcheck_get_builder, diagnostics_endpoint_healthcheck_get_task,
    diagnostics_endpoint_healthcheck_update_builder, diagnostics_endpoint_healthcheck_update_task,
    diagnostics_endpoint_healthcheck_delete_builder, diagnostics_endpoint_healthcheck_delete_task,
    diagnostics_traceroute_builder, diagnostics_traceroute_task,
    dlp_datasets_read_all_builder, dlp_datasets_read_all_task,
    dlp_datasets_create_builder, dlp_datasets_create_task,
    dlp_datasets_read_builder, dlp_datasets_read_task,
    dlp_datasets_update_builder, dlp_datasets_update_task,
    dlp_datasets_delete_builder, dlp_datasets_delete_task,
    dlp_datasets_create_version_builder, dlp_datasets_create_version_task,
    dlp_datasets_upload_version_builder, dlp_datasets_upload_version_task,
    dlp_datasets_define_columns_builder, dlp_datasets_define_columns_task,
    dlp_datasets_upload_dataset_column_builder, dlp_datasets_upload_dataset_column_task,
    dlp_document_fingerprints_read_all_builder, dlp_document_fingerprints_read_all_task,
    dlp_document_fingerprints_create_builder, dlp_document_fingerprints_create_task,
    dlp_document_fingerprints_read_builder, dlp_document_fingerprints_read_task,
    dlp_document_fingerprints_update_builder, dlp_document_fingerprints_update_task,
    dlp_document_fingerprints_upload_builder, dlp_document_fingerprints_upload_task,
    dlp_document_fingerprints_delete_builder, dlp_document_fingerprints_delete_task,
    dlp_email_scanner_get_account_mapping_builder, dlp_email_scanner_get_account_mapping_task,
    dlp_email_scanner_create_account_mapping_builder, dlp_email_scanner_create_account_mapping_task,
    dlp_email_scanner_list_all_rules_builder, dlp_email_scanner_list_all_rules_task,
    dlp_email_scanner_create_rule_builder, dlp_email_scanner_create_rule_task,
    dlp_email_scanner_update_rule_priorities_builder, dlp_email_scanner_update_rule_priorities_task,
    dlp_email_scanner_get_rule_builder, dlp_email_scanner_get_rule_task,
    dlp_email_scanner_update_rule_builder, dlp_email_scanner_update_rule_task,
    dlp_email_scanner_delete_rule_builder, dlp_email_scanner_delete_rule_task,
    dlp_entries_list_all_entries_builder, dlp_entries_list_all_entries_task,
    dlp_entries_create_entry_builder, dlp_entries_create_entry_task,
    dlp_entries_update_custom_entry_builder, dlp_entries_update_custom_entry_task,
    dlp_entries_create_integration_entry_builder, dlp_entries_create_integration_entry_task,
    dlp_entries_update_integration_entry_builder, dlp_entries_update_integration_entry_task,
    dlp_entries_delete_integration_entry_builder, dlp_entries_delete_integration_entry_task,
    dlp_entries_create_predefined_entry_builder, dlp_entries_create_predefined_entry_task,
    dlp_entries_update_predefined_entry_builder, dlp_entries_update_predefined_entry_task,
    dlp_entries_delete_predefined_entry_builder, dlp_entries_delete_predefined_entry_task,
    dlp_entries_get_dlp_entry_builder, dlp_entries_get_dlp_entry_task,
    dlp_entries_update_entry_builder, dlp_entries_update_entry_task,
    dlp_entries_delete_entry_builder, dlp_entries_delete_entry_task,
    dlp_limits_get_builder, dlp_limits_get_task,
    dlp_pattern_validate_builder, dlp_pattern_validate_task,
    dlp_payload_log_get_builder, dlp_payload_log_get_task,
    dlp_payload_log_put_builder, dlp_payload_log_put_task,
    dlp_profiles_list_all_profiles_builder, dlp_profiles_list_all_profiles_task,
    dlp_profiles_list_all_custom_profiles_builder, dlp_profiles_list_all_custom_profiles_task,
    dlp_profiles_create_custom_profiles_builder, dlp_profiles_create_custom_profiles_task,
    dlp_profiles_get_custom_profile_builder, dlp_profiles_get_custom_profile_task,
    dlp_profiles_update_custom_profile_builder, dlp_profiles_update_custom_profile_task,
    dlp_profiles_delete_custom_profile_builder, dlp_profiles_delete_custom_profile_task,
    dlp_profiles_create_predefined_profile_builder, dlp_profiles_create_predefined_profile_task,
    dlp_profiles_get_predefined_profile_builder, dlp_profiles_get_predefined_profile_task,
    dlp_profiles_update_predefined_profile_builder, dlp_profiles_update_predefined_profile_task,
    dlp_profiles_delete_predefined_profile_builder, dlp_profiles_delete_predefined_profile_task,
    dlp_profiles_get_predefined_profile_config_builder, dlp_profiles_get_predefined_profile_config_task,
    dlp_profiles_create_predefined_profile_config_builder, dlp_profiles_create_predefined_profile_config_task,
    dlp_profiles_update_predefined_profile_config_builder, dlp_profiles_update_predefined_profile_config_task,
    dlp_profiles_get_dlp_profile_builder, dlp_profiles_get_dlp_profile_task,
    dlp_settings_get_builder, dlp_settings_get_task,
    dlp_settings_update_builder, dlp_settings_update_task,
    dlp_settings_edit_builder, dlp_settings_edit_task,
    dlp_settings_delete_builder, dlp_settings_delete_task,
    dns_firewall_list_dns_firewall_clusters_builder, dns_firewall_list_dns_firewall_clusters_task,
    dns_firewall_create_dns_firewall_cluster_builder, dns_firewall_create_dns_firewall_cluster_task,
    dns_firewall_dns_firewall_cluster_details_builder, dns_firewall_dns_firewall_cluster_details_task,
    dns_firewall_update_dns_firewall_cluster_builder, dns_firewall_update_dns_firewall_cluster_task,
    dns_firewall_delete_dns_firewall_cluster_builder, dns_firewall_delete_dns_firewall_cluster_task,
    dns_firewall_analytics_table_builder, dns_firewall_analytics_table_task,
    dns_firewall_analytics_by_time_builder, dns_firewall_analytics_by_time_task,
    dns_firewall_show_dns_firewall_cluster_reverse_dns_builder, dns_firewall_show_dns_firewall_cluster_reverse_dns_task,
    dns_firewall_update_dns_firewall_cluster_reverse_dns_builder, dns_firewall_update_dns_firewall_cluster_reverse_dns_task,
    dns_records_for_an_account_get_usage_builder, dns_records_for_an_account_get_usage_task,
    dns_settings_for_an_account_list_dns_settings_builder, dns_settings_for_an_account_list_dns_settings_task,
    dns_settings_for_an_account_update_dns_settings_builder, dns_settings_for_an_account_update_dns_settings_task,
    dns_views_for_an_account_list_internal_dns_views_builder, dns_views_for_an_account_list_internal_dns_views_task,
    dns_views_for_an_account_create_internal_dns_views_builder, dns_views_for_an_account_create_internal_dns_views_task,
    dns_views_for_an_account_get_internal_dns_view_builder, dns_views_for_an_account_get_internal_dns_view_task,
    dns_views_for_an_account_update_internal_dns_view_builder, dns_views_for_an_account_update_internal_dns_view_task,
    dns_views_for_an_account_delete_internal_dns_view_builder, dns_views_for_an_account_delete_internal_dns_view_task,
    email_security_investigate_builder, email_security_investigate_task,
    email_security_post_bulk_message_move_builder, email_security_post_bulk_message_move_task,
    email_security_post_preview_builder, email_security_post_preview_task,
    email_security_post_release_builder, email_security_post_release_task,
    email_security_get_message_builder, email_security_get_message_task,
    email_security_get_message_detections_builder, email_security_get_message_detections_task,
    email_security_post_message_move_builder, email_security_post_message_move_task,
    email_security_get_message_preview_builder, email_security_get_message_preview_task,
    email_security_get_message_raw_builder, email_security_get_message_raw_task,
    email_security_post_reclassify_builder, email_security_post_reclassify_task,
    email_security_get_message_trace_builder, email_security_get_message_trace_task,
    email_security_get_phishguard_reports_builder, email_security_get_phishguard_reports_task,
    email_security_list_allow_policies_builder, email_security_list_allow_policies_task,
    email_security_create_allow_policy_builder, email_security_create_allow_policy_task,
    email_security_batch_allow_policies_builder, email_security_batch_allow_policies_task,
    email_security_get_allow_policy_builder, email_security_get_allow_policy_task,
    email_security_update_allow_policy_builder, email_security_update_allow_policy_task,
    email_security_delete_allow_policy_builder, email_security_delete_allow_policy_task,
    email_security_list_blocked_senders_builder, email_security_list_blocked_senders_task,
    email_security_create_blocked_sender_builder, email_security_create_blocked_sender_task,
    email_security_batch_blocked_senders_builder, email_security_batch_blocked_senders_task,
    email_security_get_blocked_sender_builder, email_security_get_blocked_sender_task,
    email_security_update_blocked_sender_builder, email_security_update_blocked_sender_task,
    email_security_delete_blocked_sender_builder, email_security_delete_blocked_sender_task,
    email_security_list_domains_builder, email_security_list_domains_task,
    email_security_delete_domains_builder, email_security_delete_domains_task,
    email_security_get_domain_builder, email_security_get_domain_task,
    email_security_update_domain_builder, email_security_update_domain_task,
    email_security_delete_domain_builder, email_security_delete_domain_task,
    email_security_list_display_names_builder, email_security_list_display_names_task,
    email_security_create_display_name_builder, email_security_create_display_name_task,
    email_security_get_display_name_builder, email_security_get_display_name_task,
    email_security_update_display_name_builder, email_security_update_display_name_task,
    email_security_delete_display_name_builder, email_security_delete_display_name_task,
    email_security_batch_sending_domain_restrictions_builder, email_security_batch_sending_domain_restrictions_task,
    email_security_list_trusted_domains_builder, email_security_list_trusted_domains_task,
    email_security_create_trusted_domain_builder, email_security_create_trusted_domain_task,
    email_security_batch_trusted_domains_builder, email_security_batch_trusted_domains_task,
    email_security_get_trusted_domain_builder, email_security_get_trusted_domain_task,
    email_security_update_trusted_domain_builder, email_security_update_trusted_domain_task,
    email_security_delete_trusted_domain_builder, email_security_delete_trusted_domain_task,
    email_security_submissions_builder, email_security_submissions_task,
    email_routing_destination_addresses_list_destination_addresses_builder, email_routing_destination_addresses_list_destination_addresses_task,
    email_routing_destination_addresses_create_a_destination_address_builder, email_routing_destination_addresses_create_a_destination_address_task,
    email_routing_destination_addresses_get_a_destination_address_builder, email_routing_destination_addresses_get_a_destination_address_task,
    email_routing_destination_addresses_delete_destination_address_builder, email_routing_destination_addresses_delete_destination_address_task,
    email_sending_account_send_builder_builder, email_sending_account_send_builder_task,
    email_sending_account_send_raw_message_builder, email_sending_account_send_raw_message_task,
    r2_get_event_notification_configs_builder, r2_get_event_notification_configs_task,
    r2_get_event_notification_config_builder, r2_get_event_notification_config_task,
    r2_put_event_notification_config_builder, r2_put_event_notification_config_task,
    r2_event_notification_delete_config_builder, r2_event_notification_delete_config_task,
    subscriptions_list_builder, subscriptions_list_task,
    subscriptions_create_builder, subscriptions_create_task,
    subscriptions_get_builder, subscriptions_get_task,
    subscriptions_patch_builder, subscriptions_patch_task,
    subscriptions_delete_builder, subscriptions_delete_task,
    ip_access_rules_for_an_account_list_ip_access_rules_builder, ip_access_rules_for_an_account_list_ip_access_rules_task,
    ip_access_rules_for_an_account_create_an_ip_access_rule_builder, ip_access_rules_for_an_account_create_an_ip_access_rule_task,
    ip_access_rules_for_an_account_get_an_ip_access_rule_builder, ip_access_rules_for_an_account_get_an_ip_access_rule_task,
    ip_access_rules_for_an_account_update_an_ip_access_rule_builder, ip_access_rules_for_an_account_update_an_ip_access_rule_task,
    ip_access_rules_for_an_account_delete_an_ip_access_rule_builder, ip_access_rules_for_an_account_delete_an_ip_access_rule_task,
    zero_trust_accounts_get_zero_trust_account_information_builder, zero_trust_accounts_get_zero_trust_account_information_task,
    zero_trust_accounts_create_zero_trust_account_builder, zero_trust_accounts_create_zero_trust_account_task,
    zero_trust_gateway_application_and_application_type_mappings_list_application_and_application_type_mappings_builder, zero_trust_gateway_application_and_application_type_mappings_list_application_and_application_type_mappings_task,
    zero_trust_applications_review_status_list_builder, zero_trust_applications_review_status_list_task,
    zero_trust_applications_review_status_update_builder, zero_trust_applications_review_status_update_task,
    zero_trust_get_audit_ssh_settings_builder, zero_trust_get_audit_ssh_settings_task,
    zero_trust_update_audit_ssh_settings_builder, zero_trust_update_audit_ssh_settings_task,
    zero_trust_rotate_ssh_account_seed_builder, zero_trust_rotate_ssh_account_seed_task,
    zero_trust_gateway_categories_list_categories_builder, zero_trust_gateway_categories_list_categories_task,
    zero_trust_certificates_list_zero_trust_certificates_builder, zero_trust_certificates_list_zero_trust_certificates_task,
    zero_trust_certificates_create_zero_trust_certificate_builder, zero_trust_certificates_create_zero_trust_certificate_task,
    zero_trust_certificates_zero_trust_certificate_details_builder, zero_trust_certificates_zero_trust_certificate_details_task,
    zero_trust_certificates_delete_zero_trust_certificate_builder, zero_trust_certificates_delete_zero_trust_certificate_task,
    zero_trust_certificates_activate_zero_trust_certificate_builder, zero_trust_certificates_activate_zero_trust_certificate_task,
    zero_trust_certificates_deactivate_zero_trust_certificate_builder, zero_trust_certificates_deactivate_zero_trust_certificate_task,
    zero_trust_accounts_get_zero_trust_account_configuration_builder, zero_trust_accounts_get_zero_trust_account_configuration_task,
    zero_trust_accounts_update_zero_trust_account_configuration_builder, zero_trust_accounts_update_zero_trust_account_configuration_task,
    zero_trust_accounts_patch_zero_trust_account_configuration_builder, zero_trust_accounts_patch_zero_trust_account_configuration_task,
    zero_trust_accounts_get_zero_trust_certificate_configuration_builder, zero_trust_accounts_get_zero_trust_certificate_configuration_task,
    zero_trust_lists_list_zero_trust_lists_builder, zero_trust_lists_list_zero_trust_lists_task,
    zero_trust_lists_create_zero_trust_list_builder, zero_trust_lists_create_zero_trust_list_task,
    zero_trust_lists_zero_trust_list_details_builder, zero_trust_lists_zero_trust_list_details_task,
    zero_trust_lists_update_zero_trust_list_builder, zero_trust_lists_update_zero_trust_list_task,
    zero_trust_lists_patch_zero_trust_list_builder, zero_trust_lists_patch_zero_trust_list_task,
    zero_trust_lists_delete_zero_trust_list_builder, zero_trust_lists_delete_zero_trust_list_task,
    zero_trust_lists_zero_trust_list_items_builder, zero_trust_lists_zero_trust_list_items_task,
    zero_trust_gateway_locations_list_zero_trust_gateway_locations_builder, zero_trust_gateway_locations_list_zero_trust_gateway_locations_task,
    zero_trust_gateway_locations_create_zero_trust_gateway_location_builder, zero_trust_gateway_locations_create_zero_trust_gateway_location_task,
    zero_trust_gateway_locations_zero_trust_gateway_location_details_builder, zero_trust_gateway_locations_zero_trust_gateway_location_details_task,
    zero_trust_gateway_locations_update_zero_trust_gateway_location_builder, zero_trust_gateway_locations_update_zero_trust_gateway_location_task,
    zero_trust_gateway_locations_delete_zero_trust_gateway_location_builder, zero_trust_gateway_locations_delete_zero_trust_gateway_location_task,
    zero_trust_accounts_get_logging_settings_for_the_zero_trust_account_builder, zero_trust_accounts_get_logging_settings_for_the_zero_trust_account_task,
    zero_trust_accounts_update_logging_settings_for_the_zero_trust_account_builder, zero_trust_accounts_update_logging_settings_for_the_zero_trust_account_task,
    zero_trust_gateway_pacfiles_list_builder, zero_trust_gateway_pacfiles_list_task,
    zero_trust_gateway_pacfiles_create_pacfile_builder, zero_trust_gateway_pacfiles_create_pacfile_task,
    zero_trust_gateway_pacfiles_details_builder, zero_trust_gateway_pacfiles_details_task,
    zero_trust_gateway_pacfiles_update_builder, zero_trust_gateway_pacfiles_update_task,
    zero_trust_gateway_pacfiles_delete_builder, zero_trust_gateway_pacfiles_delete_task,
    zero_trust_gateway_proxy_endpoints_list_proxy_endpoints_builder, zero_trust_gateway_proxy_endpoints_list_proxy_endpoints_task,
    zero_trust_gateway_proxy_endpoints_create_proxy_endpoint_builder, zero_trust_gateway_proxy_endpoints_create_proxy_endpoint_task,
    zero_trust_gateway_proxy_endpoints_proxy_endpoint_details_builder, zero_trust_gateway_proxy_endpoints_proxy_endpoint_details_task,
    zero_trust_gateway_proxy_endpoints_update_proxy_endpoint_builder, zero_trust_gateway_proxy_endpoints_update_proxy_endpoint_task,
    zero_trust_gateway_proxy_endpoints_delete_proxy_endpoint_builder, zero_trust_gateway_proxy_endpoints_delete_proxy_endpoint_task,
    zero_trust_gateway_rules_list_zero_trust_gateway_rules_builder, zero_trust_gateway_rules_list_zero_trust_gateway_rules_task,
    zero_trust_gateway_rules_create_zero_trust_gateway_rule_builder, zero_trust_gateway_rules_create_zero_trust_gateway_rule_task,
    zero_trust_gateway_rules_list_zero_trust_gateway_rules_tenant_builder, zero_trust_gateway_rules_list_zero_trust_gateway_rules_tenant_task,
    zero_trust_gateway_rules_zero_trust_gateway_rule_details_builder, zero_trust_gateway_rules_zero_trust_gateway_rule_details_task,
    zero_trust_gateway_rules_update_zero_trust_gateway_rule_builder, zero_trust_gateway_rules_update_zero_trust_gateway_rule_task,
    zero_trust_gateway_rules_delete_zero_trust_gateway_rule_builder, zero_trust_gateway_rules_delete_zero_trust_gateway_rule_task,
    zero_trust_gateway_rules_reset_expiration_zero_trust_gateway_rule_builder, zero_trust_gateway_rules_reset_expiration_zero_trust_gateway_rule_task,
    list_hyperdrive_builder, list_hyperdrive_task,
    create_hyperdrive_builder, create_hyperdrive_task,
    get_hyperdrive_builder, get_hyperdrive_task,
    update_hyperdrive_builder, update_hyperdrive_task,
    patch_hyperdrive_builder, patch_hyperdrive_task,
    delete_hyperdrive_builder, delete_hyperdrive_task,
    account_permission_group_list_builder, account_permission_group_list_task,
    account_permission_group_details_builder, account_permission_group_details_task,
    account_resource_group_list_builder, account_resource_group_list_task,
    account_resource_group_create_builder, account_resource_group_create_task,
    account_resource_group_details_builder, account_resource_group_details_task,
    account_resource_group_update_builder, account_resource_group_update_task,
    account_resource_group_delete_builder, account_resource_group_delete_task,
    account_user_group_list_builder, account_user_group_list_task,
    account_user_group_create_builder, account_user_group_create_task,
    account_user_group_details_builder, account_user_group_details_task,
    account_user_group_update_builder, account_user_group_update_task,
    account_user_group_delete_builder, account_user_group_delete_task,
    account_user_group_member_list_builder, account_user_group_member_list_task,
    account_user_group_member_create_builder, account_user_group_member_create_task,
    account_user_group_members_update_builder, account_user_group_members_update_task,
    account_user_group_member_delete_builder, account_user_group_member_delete_task,
    cloudflare_images_list_images_builder, cloudflare_images_list_images_task,
    cloudflare_images_upload_an_image_via_url_builder, cloudflare_images_upload_an_image_via_url_task,
    cloudflare_images_keys_list_signing_keys_builder, cloudflare_images_keys_list_signing_keys_task,
    cloudflare_images_keys_add_signing_key_builder, cloudflare_images_keys_add_signing_key_task,
    cloudflare_images_keys_delete_signing_key_builder, cloudflare_images_keys_delete_signing_key_task,
    cloudflare_images_images_usage_statistics_builder, cloudflare_images_images_usage_statistics_task,
    cloudflare_images_variants_list_variants_builder, cloudflare_images_variants_list_variants_task,
    cloudflare_images_variants_create_a_variant_builder, cloudflare_images_variants_create_a_variant_task,
    cloudflare_images_variants_variant_details_builder, cloudflare_images_variants_variant_details_task,
    cloudflare_images_variants_update_a_variant_builder, cloudflare_images_variants_update_a_variant_task,
    cloudflare_images_variants_delete_a_variant_builder, cloudflare_images_variants_delete_a_variant_task,
    cloudflare_images_variants_variant_details_flat_builder, cloudflare_images_variants_variant_details_flat_task,
    cloudflare_images_image_details_builder, cloudflare_images_image_details_task,
    cloudflare_images_update_image_builder, cloudflare_images_update_image_task,
    cloudflare_images_delete_image_builder, cloudflare_images_delete_image_task,
    cloudflare_images_base_image_builder, cloudflare_images_base_image_task,
    cloudflare_images_list_images_v2_builder, cloudflare_images_list_images_v2_task,
    cloudflare_images_create_authenticated_direct_upload_url_v_2_builder, cloudflare_images_create_authenticated_direct_upload_url_v_2_task,
    infra_targets_list_builder, infra_targets_list_task,
    infra_targets_post_builder, infra_targets_post_task,
    infra_targets_put_batch_builder, infra_targets_put_batch_task,
    infra_targets_delete_batch_builder, infra_targets_delete_batch_task,
    infra_targets_delete_batch_post_builder, infra_targets_delete_batch_post_task,
    infra_targets_get_builder, infra_targets_get_task,
    infra_targets_put_builder, infra_targets_put_task,
    infra_targets_delete_builder, infra_targets_delete_task,
    asn_intelligence_get_asn_overview_builder, asn_intelligence_get_asn_overview_task,
    asn_intelligence_get_asn_subnets_builder, asn_intelligence_get_asn_subnets_task,
    get_security_center_issue_types_builder, get_security_center_issue_types_task,
    get_security_center_issues_builder, get_security_center_issues_task,
    get_security_center_issue_counts_by_class_builder, get_security_center_issue_counts_by_class_task,
    get_security_center_issue_counts_by_severity_builder, get_security_center_issue_counts_by_severity_task,
    get_security_center_issue_counts_by_type_builder, get_security_center_issue_counts_by_type_task,
    archive_security_center_insight_deprecated_builder, archive_security_center_insight_deprecated_task,
    passive_dns_by_ip_get_passive_dns_by_ip_builder, passive_dns_by_ip_get_passive_dns_by_ip_task,
    domain_intelligence_get_domain_details_builder, domain_intelligence_get_domain_details_task,
    domain_history_get_domain_history_builder, domain_history_get_domain_history_task,
    domain_intelligence_get_multiple_domain_details_builder, domain_intelligence_get_multiple_domain_details_task,
    custom_indicator_feeds_get_indicator_feeds_builder, custom_indicator_feeds_get_indicator_feeds_task,
    custom_indicator_feeds_create_indicator_feeds_builder, custom_indicator_feeds_create_indicator_feeds_task,
    custom_indicator_feeds_add_permission_builder, custom_indicator_feeds_add_permission_task,
    custom_indicator_feeds_remove_permission_builder, custom_indicator_feeds_remove_permission_task,
    custom_indicator_feeds_view_permissions_builder, custom_indicator_feeds_view_permissions_task,
    custom_indicator_feeds_get_indicator_feed_metadata_builder, custom_indicator_feeds_get_indicator_feed_metadata_task,
    custom_indicator_feeds_update_indicator_feed_metadata_builder, custom_indicator_feeds_update_indicator_feed_metadata_task,
    custom_indicator_feeds_get_indicator_feed_data_builder, custom_indicator_feeds_get_indicator_feed_data_task,
    custom_indicator_feeds_download_indicator_feed_data_builder, custom_indicator_feeds_download_indicator_feed_data_task,
    custom_indicator_feeds_update_indicator_feed_data_builder, custom_indicator_feeds_update_indicator_feed_data_task,
    ip_intelligence_get_ip_overview_builder, ip_intelligence_get_ip_overview_task,
    ip_list_get_ip_lists_builder, ip_list_get_ip_lists_task,
    miscategorization_create_miscategorization_builder, miscategorization_create_miscategorization_task,
    sinkhole_config_get_sinkholes_builder, sinkhole_config_get_sinkholes_task,
    whois_record_get_whois_record_builder, whois_record_get_whois_record_task,
    account_load_balancer_monitor_groups_list_monitor_groups_builder, account_load_balancer_monitor_groups_list_monitor_groups_task,
    account_load_balancer_monitor_groups_create_monitor_group_builder, account_load_balancer_monitor_groups_create_monitor_group_task,
    account_load_balancer_monitor_groups_monitor_group_details_builder, account_load_balancer_monitor_groups_monitor_group_details_task,
    account_load_balancer_monitor_groups_update_monitor_group_builder, account_load_balancer_monitor_groups_update_monitor_group_task,
    account_load_balancer_monitor_groups_patch_monitor_group_builder, account_load_balancer_monitor_groups_patch_monitor_group_task,
    account_load_balancer_monitor_groups_delete_monitor_group_builder, account_load_balancer_monitor_groups_delete_monitor_group_task,
    account_load_balancer_monitor_groups_list_monitor_group_references_builder, account_load_balancer_monitor_groups_list_monitor_group_references_task,
    account_load_balancer_monitors_list_monitors_builder, account_load_balancer_monitors_list_monitors_task,
    account_load_balancer_monitors_create_monitor_builder, account_load_balancer_monitors_create_monitor_task,
    account_load_balancer_monitors_monitor_details_builder, account_load_balancer_monitors_monitor_details_task,
    account_load_balancer_monitors_update_monitor_builder, account_load_balancer_monitors_update_monitor_task,
    account_load_balancer_monitors_patch_monitor_builder, account_load_balancer_monitors_patch_monitor_task,
    account_load_balancer_monitors_delete_monitor_builder, account_load_balancer_monitors_delete_monitor_task,
    account_load_balancer_monitors_preview_monitor_builder, account_load_balancer_monitors_preview_monitor_task,
    account_load_balancer_monitors_list_monitor_references_builder, account_load_balancer_monitors_list_monitor_references_task,
    account_load_balancer_pools_list_pools_builder, account_load_balancer_pools_list_pools_task,
    account_load_balancer_pools_create_pool_builder, account_load_balancer_pools_create_pool_task,
    account_load_balancer_pools_patch_pools_builder, account_load_balancer_pools_patch_pools_task,
    account_load_balancer_pools_pool_details_builder, account_load_balancer_pools_pool_details_task,
    account_load_balancer_pools_update_pool_builder, account_load_balancer_pools_update_pool_task,
    account_load_balancer_pools_patch_pool_builder, account_load_balancer_pools_patch_pool_task,
    account_load_balancer_pools_delete_pool_builder, account_load_balancer_pools_delete_pool_task,
    account_load_balancer_pools_pool_health_details_builder, account_load_balancer_pools_pool_health_details_task,
    account_load_balancer_pools_preview_pool_builder, account_load_balancer_pools_preview_pool_task,
    account_load_balancer_pools_list_pool_references_builder, account_load_balancer_pools_list_pool_references_task,
    account_load_balancer_monitors_preview_result_builder, account_load_balancer_monitors_preview_result_task,
    load_balancer_regions_list_regions_builder, load_balancer_regions_list_regions_task,
    load_balancer_regions_get_region_builder, load_balancer_regions_get_region_task,
    account_load_balancer_search_search_resources_builder, account_load_balancer_search_search_resources_task,
    get_accounts_account_id_logpush_datasets_dataset_id_fields_builder, get_accounts_account_id_logpush_datasets_dataset_id_fields_task,
    get_accounts_account_id_logpush_datasets_dataset_id_jobs_builder, get_accounts_account_id_logpush_datasets_dataset_id_jobs_task,
    get_accounts_account_id_logpush_jobs_builder, get_accounts_account_id_logpush_jobs_task,
    post_accounts_account_id_logpush_jobs_builder, post_accounts_account_id_logpush_jobs_task,
    get_accounts_account_id_logpush_jobs_job_id_builder, get_accounts_account_id_logpush_jobs_job_id_task,
    put_accounts_account_id_logpush_jobs_job_id_builder, put_accounts_account_id_logpush_jobs_job_id_task,
    delete_accounts_account_id_logpush_jobs_job_id_builder, delete_accounts_account_id_logpush_jobs_job_id_task,
    post_accounts_account_id_logpush_ownership_builder, post_accounts_account_id_logpush_ownership_task,
    post_accounts_account_id_logpush_ownership_validate_builder, post_accounts_account_id_logpush_ownership_validate_task,
    delete_accounts_account_id_logpush_validate_destination_builder, delete_accounts_account_id_logpush_validate_destination_task,
    delete_accounts_account_id_logpush_validate_destination_exists_builder, delete_accounts_account_id_logpush_validate_destination_exists_task,
    post_accounts_account_id_logpush_validate_origin_builder, post_accounts_account_id_logpush_validate_origin_task,
    audit_logs_v2_get_account_audit_logs_builder, audit_logs_v2_get_account_audit_logs_task,
    get_accounts_account_id_logs_control_cmb_config_builder, get_accounts_account_id_logs_control_cmb_config_task,
    post_accounts_account_id_logs_control_cmb_config_builder, post_accounts_account_id_logs_control_cmb_config_task,
    delete_accounts_account_id_logs_control_cmb_config_builder, delete_accounts_account_id_logs_control_cmb_config_task,
    list_dns_protection_rules_for_account_builder, list_dns_protection_rules_for_account_task,
    create_dns_protection_rule_builder, create_dns_protection_rule_task,
    delete_dns_protection_rules_for_account_builder, delete_dns_protection_rules_for_account_task,
    get_dns_protection_rule_builder, get_dns_protection_rule_task,
    update_dns_protection_rule_builder, update_dns_protection_rule_task,
    delete_dns_protection_rule_builder, delete_dns_protection_rule_task,
    list_allowlist_prefixes_for_account_builder, list_allowlist_prefixes_for_account_task,
    create_allowlisted_prefix_builder, create_allowlisted_prefix_task,
    delete_allowlist_prefixes_for_account_builder, delete_allowlist_prefixes_for_account_task,
    get_allowlist_prefix_builder, get_allowlist_prefix_task,
    update_allowlist_prefix_builder, update_allowlist_prefix_task,
    delete_allowlist_prefix_builder, delete_allowlist_prefix_task,
    list_prefixes_for_account_builder, list_prefixes_for_account_task,
    create_prefix_builder, create_prefix_task,
    delete_prefixes_for_account_builder, delete_prefixes_for_account_task,
    bulk_create_prefixes_builder, bulk_create_prefixes_task,
    get_prefix_builder, get_prefix_task,
    update_prefix_builder, update_prefix_task,
    delete_prefix_builder, delete_prefix_task,
    list_syn_protection_filters_for_account_builder, list_syn_protection_filters_for_account_task,
    create_syn_protection_filter_builder, create_syn_protection_filter_task,
    delete_syn_protection_filters_for_account_builder, delete_syn_protection_filters_for_account_task,
    get_syn_protection_filter_builder, get_syn_protection_filter_task,
    update_syn_protection_filter_builder, update_syn_protection_filter_task,
    delete_syn_protection_filter_builder, delete_syn_protection_filter_task,
    list_syn_protection_rules_for_account_builder, list_syn_protection_rules_for_account_task,
    create_syn_protection_rule_builder, create_syn_protection_rule_task,
    delete_syn_protection_rules_for_account_builder, delete_syn_protection_rules_for_account_task,
    get_syn_protection_rule_builder, get_syn_protection_rule_task,
    update_syn_protection_rule_builder, update_syn_protection_rule_task,
    delete_syn_protection_rule_builder, delete_syn_protection_rule_task,
    list_tcp_flow_protection_filters_for_account_builder, list_tcp_flow_protection_filters_for_account_task,
    create_tcp_flow_protection_filter_builder, create_tcp_flow_protection_filter_task,
    delete_tcp_flow_protection_filters_for_account_builder, delete_tcp_flow_protection_filters_for_account_task,
    get_tcp_flow_protection_filter_builder, get_tcp_flow_protection_filter_task,
    update_tcp_flow_protection_filter_builder, update_tcp_flow_protection_filter_task,
    delete_tcp_flow_protection_filter_builder, delete_tcp_flow_protection_filter_task,
    list_tcp_flow_protection_rules_for_account_builder, list_tcp_flow_protection_rules_for_account_task,
    create_tcp_flow_protection_rule_builder, create_tcp_flow_protection_rule_task,
    delete_tcp_flow_protection_rules_for_account_builder, delete_tcp_flow_protection_rules_for_account_task,
    get_tcp_flow_protection_rule_builder, get_tcp_flow_protection_rule_task,
    update_tcp_flow_protection_rule_builder, update_tcp_flow_protection_rule_task,
    delete_tcp_flow_protection_rule_builder, delete_tcp_flow_protection_rule_task,
    get_protection_status_builder, get_protection_status_task,
    update_protection_status_builder, update_protection_status_task,
    magic_account_apps_list_apps_builder, magic_account_apps_list_apps_task,
    magic_account_apps_add_app_builder, magic_account_apps_add_app_task,
    magic_account_apps_update_app_builder, magic_account_apps_update_app_task,
    magic_account_apps_patch_app_builder, magic_account_apps_patch_app_task,
    magic_account_apps_delete_app_builder, magic_account_apps_delete_app_task,
    magic_interconnects_list_interconnects_builder, magic_interconnects_list_interconnects_task,
    magic_interconnects_update_multiple_interconnects_builder, magic_interconnects_update_multiple_interconnects_task,
    magic_interconnects_list_interconnect_details_builder, magic_interconnects_list_interconnect_details_task,
    magic_interconnects_update_interconnect_builder, magic_interconnects_update_interconnect_task,
    catalog_syncs_list_builder, catalog_syncs_list_task,
    catalog_syncs_create_builder, catalog_syncs_create_task,
    catalog_syncs_prebuilt_policies_list_builder, catalog_syncs_prebuilt_policies_list_task,
    catalog_syncs_read_builder, catalog_syncs_read_task,
    catalog_syncs_update_builder, catalog_syncs_update_task,
    catalog_syncs_patch_builder, catalog_syncs_patch_task,
    catalog_syncs_delete_builder, catalog_syncs_delete_task,
    catalog_syncs_refresh_builder, catalog_syncs_refresh_task,
    onramps_list_builder, onramps_list_task,
    onramps_create_builder, onramps_create_task,
    onramps_mwan_addr_space_read_builder, onramps_mwan_addr_space_read_task,
    onramps_mwan_addr_space_update_builder, onramps_mwan_addr_space_update_task,
    onramps_mwan_addr_space_patch_builder, onramps_mwan_addr_space_patch_task,
    onramps_read_builder, onramps_read_task,
    onramps_update_builder, onramps_update_task,
    onramps_patch_builder, onramps_patch_task,
    onramps_delete_builder, onramps_delete_task,
    onramps_apply_builder, onramps_apply_task,
    onramps_export_builder, onramps_export_task,
    onramps_plan_builder, onramps_plan_task,
    providers_list_builder, providers_list_task,
    providers_create_builder, providers_create_task,
    providers_discover_all_builder, providers_discover_all_task,
    providers_read_builder, providers_read_task,
    providers_update_builder, providers_update_task,
    providers_patch_builder, providers_patch_task,
    providers_delete_builder, providers_delete_task,
    providers_discover_builder, providers_discover_task,
    providers_initial_setup_builder, providers_initial_setup_task,
    resources_catalog_list_builder, resources_catalog_list_task,
    resources_catalog_export_builder, resources_catalog_export_task,
    resources_catalog_policy_preview_builder, resources_catalog_policy_preview_task,
    resources_catalog_read_builder, resources_catalog_read_task,
    mconn_connector_list_builder, mconn_connector_list_task,
    mconn_connector_create_builder, mconn_connector_create_task,
    mconn_connector_fetch_builder, mconn_connector_fetch_task,
    mconn_connector_replace_builder, mconn_connector_replace_task,
    mconn_connector_update_builder, mconn_connector_update_task,
    mconn_connector_delete_builder, mconn_connector_delete_task,
    mconn_connector_telemetry_events_list_builder, mconn_connector_telemetry_events_list_task,
    mconn_connector_telemetry_events_list_latest_builder, mconn_connector_telemetry_events_list_latest_task,
    mconn_connector_telemetry_events_get_builder, mconn_connector_telemetry_events_get_task,
    mconn_connector_telemetry_snapshots_list_builder, mconn_connector_telemetry_snapshots_list_task,
    mconn_connector_telemetry_snapshots_list_latest_builder, mconn_connector_telemetry_snapshots_list_latest_task,
    mconn_connector_telemetry_snapshots_get_builder, mconn_connector_telemetry_snapshots_get_task,
    magic_gre_tunnels_list_gre_tunnels_builder, magic_gre_tunnels_list_gre_tunnels_task,
    magic_gre_tunnels_create_gre_tunnels_builder, magic_gre_tunnels_create_gre_tunnels_task,
    magic_gre_tunnels_update_multiple_gre_tunnels_builder, magic_gre_tunnels_update_multiple_gre_tunnels_task,
    magic_gre_tunnels_list_gre_tunnel_details_builder, magic_gre_tunnels_list_gre_tunnel_details_task,
    magic_gre_tunnels_update_gre_tunnel_builder, magic_gre_tunnels_update_gre_tunnel_task,
    magic_gre_tunnels_delete_gre_tunnel_builder, magic_gre_tunnels_delete_gre_tunnel_task,
    magic_ipsec_tunnels_list_ipsec_tunnels_builder, magic_ipsec_tunnels_list_ipsec_tunnels_task,
    magic_ipsec_tunnels_create_ipsec_tunnels_builder, magic_ipsec_tunnels_create_ipsec_tunnels_task,
    magic_ipsec_tunnels_update_multiple_ipsec_tunnels_builder, magic_ipsec_tunnels_update_multiple_ipsec_tunnels_task,
    magic_ipsec_tunnels_list_ipsec_tunnel_details_builder, magic_ipsec_tunnels_list_ipsec_tunnel_details_task,
    magic_ipsec_tunnels_update_ipsec_tunnel_builder, magic_ipsec_tunnels_update_ipsec_tunnel_task,
    magic_ipsec_tunnels_delete_ipsec_tunnel_builder, magic_ipsec_tunnels_delete_ipsec_tunnel_task,
    magic_ipsec_tunnels_generate_pre_shared_key_psk_for_ipsec_tunnels_builder, magic_ipsec_tunnels_generate_pre_shared_key_psk_for_ipsec_tunnels_task,
    magic_static_routes_list_routes_builder, magic_static_routes_list_routes_task,
    magic_static_routes_create_routes_builder, magic_static_routes_create_routes_task,
    magic_static_routes_update_many_routes_builder, magic_static_routes_update_many_routes_task,
    magic_static_routes_delete_many_routes_builder, magic_static_routes_delete_many_routes_task,
    magic_static_routes_route_details_builder, magic_static_routes_route_details_task,
    magic_static_routes_update_route_builder, magic_static_routes_update_route_task,
    magic_static_routes_delete_route_builder, magic_static_routes_delete_route_task,
    magic_sites_list_sites_builder, magic_sites_list_sites_task,
    magic_sites_create_site_builder, magic_sites_create_site_task,
    magic_sites_site_details_builder, magic_sites_site_details_task,
    magic_sites_update_site_builder, magic_sites_update_site_task,
    magic_sites_patch_site_builder, magic_sites_patch_site_task,
    magic_sites_delete_site_builder, magic_sites_delete_site_task,
    magic_site_acls_list_acls_builder, magic_site_acls_list_acls_task,
    magic_site_acls_create_acl_builder, magic_site_acls_create_acl_task,
    magic_site_acls_acl_details_builder, magic_site_acls_acl_details_task,
    magic_site_acls_update_acl_builder, magic_site_acls_update_acl_task,
    magic_site_acls_patch_acl_builder, magic_site_acls_patch_acl_task,
    magic_site_acls_delete_acl_builder, magic_site_acls_delete_acl_task,
    magic_site_app_configs_list_app_configs_builder, magic_site_app_configs_list_app_configs_task,
    magic_site_app_configs_add_app_config_builder, magic_site_app_configs_add_app_config_task,
    magic_site_app_configs_update_app_config_builder, magic_site_app_configs_update_app_config_task,
    magic_site_app_configs_patch_app_config_builder, magic_site_app_configs_patch_app_config_task,
    magic_site_app_configs_delete_app_config_builder, magic_site_app_configs_delete_app_config_task,
    magic_site_lans_list_lans_builder, magic_site_lans_list_lans_task,
    magic_site_lans_create_lan_builder, magic_site_lans_create_lan_task,
    magic_site_lans_lan_details_builder, magic_site_lans_lan_details_task,
    magic_site_lans_update_lan_builder, magic_site_lans_update_lan_task,
    magic_site_lans_patch_lan_builder, magic_site_lans_patch_lan_task,
    magic_site_lans_delete_lan_builder, magic_site_lans_delete_lan_task,
    magic_site_netflow_config_details_builder, magic_site_netflow_config_details_task,
    magic_site_netflow_config_create_netflow_config_builder, magic_site_netflow_config_create_netflow_config_task,
    magic_site_netflow_config_update_netflow_config_builder, magic_site_netflow_config_update_netflow_config_task,
    magic_site_netflow_config_patch_netflow_config_builder, magic_site_netflow_config_patch_netflow_config_task,
    magic_site_netflow_config_delete_netflow_config_builder, magic_site_netflow_config_delete_netflow_config_task,
    magic_site_wans_list_wans_builder, magic_site_wans_list_wans_task,
    magic_site_wans_create_wan_builder, magic_site_wans_create_wan_task,
    magic_site_wans_wan_details_builder, magic_site_wans_wan_details_task,
    magic_site_wans_update_wan_builder, magic_site_wans_update_wan_task,
    magic_site_wans_patch_wan_builder, magic_site_wans_patch_wan_task,
    magic_site_wans_delete_wan_builder, magic_site_wans_delete_wan_task,
    account_members_list_members_builder, account_members_list_members_task,
    account_members_add_member_builder, account_members_add_member_task,
    account_members_member_details_builder, account_members_member_details_task,
    account_members_update_member_builder, account_members_update_member_task,
    account_members_remove_member_builder, account_members_remove_member_task,
    magic_network_monitoring_configuration_list_account_configuration_builder, magic_network_monitoring_configuration_list_account_configuration_task,
    magic_network_monitoring_configuration_create_account_configuration_builder, magic_network_monitoring_configuration_create_account_configuration_task,
    magic_network_monitoring_configuration_update_an_entire_account_configuration_builder, magic_network_monitoring_configuration_update_an_entire_account_configuration_task,
    magic_network_monitoring_configuration_update_account_configuration_fields_builder, magic_network_monitoring_configuration_update_account_configuration_fields_task,
    magic_network_monitoring_configuration_delete_account_configuration_builder, magic_network_monitoring_configuration_delete_account_configuration_task,
    magic_network_monitoring_configuration_list_rules_and_account_configuration_builder, magic_network_monitoring_configuration_list_rules_and_account_configuration_task,
    magic_network_monitoring_rules_list_rules_builder, magic_network_monitoring_rules_list_rules_task,
    magic_network_monitoring_rules_create_rules_builder, magic_network_monitoring_rules_create_rules_task,
    magic_network_monitoring_rules_update_rules_builder, magic_network_monitoring_rules_update_rules_task,
    magic_network_monitoring_rules_get_rule_builder, magic_network_monitoring_rules_get_rule_task,
    magic_network_monitoring_rules_update_rule_builder, magic_network_monitoring_rules_update_rule_task,
    magic_network_monitoring_rules_delete_rule_builder, magic_network_monitoring_rules_delete_rule_task,
    magic_network_monitoring_rules_update_advertisement_for_rule_builder, magic_network_monitoring_rules_update_advertisement_for_rule_task,
    magic_network_monitoring_vpc_flows_generate_authentication_token_builder, magic_network_monitoring_vpc_flows_generate_authentication_token_task,
    accounts_move_accounts_builder, accounts_move_accounts_task,
    m_tls_certificate_management_list_m_tls_certificates_builder, m_tls_certificate_management_list_m_tls_certificates_task,
    m_tls_certificate_management_upload_m_tls_certificate_builder, m_tls_certificate_management_upload_m_tls_certificate_task,
    m_tls_certificate_management_get_m_tls_certificate_builder, m_tls_certificate_management_get_m_tls_certificate_task,
    m_tls_certificate_management_delete_m_tls_certificate_builder, m_tls_certificate_management_delete_m_tls_certificate_task,
    m_tls_certificate_management_list_m_tls_certificate_associations_builder, m_tls_certificate_management_list_m_tls_certificate_associations_task,
    accounts_list_account_organizations_builder, accounts_list_account_organizations_task,
    pages_project_get_projects_builder, pages_project_get_projects_task,
    pages_project_create_project_builder, pages_project_create_project_task,
    pages_project_get_project_builder, pages_project_get_project_task,
    pages_project_update_project_builder, pages_project_update_project_task,
    pages_project_delete_project_builder, pages_project_delete_project_task,
    pages_deployment_get_deployments_builder, pages_deployment_get_deployments_task,
    pages_deployment_create_deployment_builder, pages_deployment_create_deployment_task,
    pages_deployment_get_deployment_info_builder, pages_deployment_get_deployment_info_task,
    pages_deployment_delete_deployment_builder, pages_deployment_delete_deployment_task,
    pages_deployment_get_deployment_logs_builder, pages_deployment_get_deployment_logs_task,
    pages_deployment_retry_deployment_builder, pages_deployment_retry_deployment_task,
    pages_deployment_rollback_deployment_builder, pages_deployment_rollback_deployment_task,
    pages_domains_get_domains_builder, pages_domains_get_domains_task,
    pages_domains_add_domain_builder, pages_domains_add_domain_task,
    pages_domains_get_domain_builder, pages_domains_get_domain_task,
    pages_domains_patch_domain_builder, pages_domains_patch_domain_task,
    pages_domains_delete_domain_builder, pages_domains_delete_domain_task,
    pages_purge_build_cache_builder, pages_purge_build_cache_task,
    pay_per_crawl_crawler_get_stripe_config_builder, pay_per_crawl_crawler_get_stripe_config_task,
    pay_per_crawl_crawler_create_stripe_config_builder, pay_per_crawl_crawler_create_stripe_config_task,
    pay_per_crawl_crawler_delete_stripe_config_builder, pay_per_crawl_crawler_delete_stripe_config_task,
    pay_per_crawl_publisher_get_stripe_config_builder, pay_per_crawl_publisher_get_stripe_config_task,
    pay_per_crawl_publisher_create_stripe_config_builder, pay_per_crawl_publisher_create_stripe_config_task,
    pay_per_crawl_publisher_delete_stripe_config_builder, pay_per_crawl_publisher_delete_stripe_config_task,
    pay_per_crawl_set_zones_can_be_enabled_builder, pay_per_crawl_set_zones_can_be_enabled_task,
    pay_per_crawl_query_zones_can_be_enabled_builder, pay_per_crawl_query_zones_can_be_enabled_task,
    magic_pcap_collection_list_packet_capture_requests_builder, magic_pcap_collection_list_packet_capture_requests_task,
    magic_pcap_collection_create_pcap_request_builder, magic_pcap_collection_create_pcap_request_task,
    magic_pcap_collection_list_pca_ps_bucket_ownership_builder, magic_pcap_collection_list_pca_ps_bucket_ownership_task,
    magic_pcap_collection_add_buckets_for_full_packet_captures_builder, magic_pcap_collection_add_buckets_for_full_packet_captures_task,
    magic_pcap_collection_validate_buckets_for_full_packet_captures_builder, magic_pcap_collection_validate_buckets_for_full_packet_captures_task,
    magic_pcap_collection_delete_buckets_for_full_packet_captures_builder, magic_pcap_collection_delete_buckets_for_full_packet_captures_task,
    magic_pcap_collection_get_pcap_request_builder, magic_pcap_collection_get_pcap_request_task,
    magic_pcap_collection_download_simple_pcap_builder, magic_pcap_collection_download_simple_pcap_task,
    magic_pcap_collection_stop_full_pcap_builder, magic_pcap_collection_stop_full_pcap_task,
    get_v4_accounts_by_account_id_pipelines_deprecated_builder, get_v4_accounts_by_account_id_pipelines_deprecated_task,
    post_v4_accounts_by_account_id_pipelines_deprecated_builder, post_v4_accounts_by_account_id_pipelines_deprecated_task,
    get_v4_accounts_by_account_id_pipelines_v1_pipelines_builder, get_v4_accounts_by_account_id_pipelines_v1_pipelines_task,
    post_v4_accounts_by_account_id_pipelines_v1_pipelines_builder, post_v4_accounts_by_account_id_pipelines_v1_pipelines_task,
    get_v4_accounts_by_account_id_pipelines_v1_pipelines_by_pipeline_id_builder, get_v4_accounts_by_account_id_pipelines_v1_pipelines_by_pipeline_id_task,
    delete_v4_accounts_by_account_id_pipelines_v1_pipelines_by_pipeline_id_builder, delete_v4_accounts_by_account_id_pipelines_v1_pipelines_by_pipeline_id_task,
    get_v4_accounts_by_account_id_pipelines_v1_sinks_builder, get_v4_accounts_by_account_id_pipelines_v1_sinks_task,
    post_v4_accounts_by_account_id_pipelines_v1_sinks_builder, post_v4_accounts_by_account_id_pipelines_v1_sinks_task,
    get_v4_accounts_by_account_id_pipelines_v1_sinks_by_sink_id_builder, get_v4_accounts_by_account_id_pipelines_v1_sinks_by_sink_id_task,
    delete_v4_accounts_by_account_id_pipelines_v1_sinks_by_sink_id_builder, delete_v4_accounts_by_account_id_pipelines_v1_sinks_by_sink_id_task,
    get_v4_accounts_by_account_id_pipelines_v1_streams_builder, get_v4_accounts_by_account_id_pipelines_v1_streams_task,
    post_v4_accounts_by_account_id_pipelines_v1_streams_builder, post_v4_accounts_by_account_id_pipelines_v1_streams_task,
    get_v4_accounts_by_account_id_pipelines_v1_streams_by_stream_id_builder, get_v4_accounts_by_account_id_pipelines_v1_streams_by_stream_id_task,
    patch_v4_accounts_by_account_id_pipelines_v1_streams_by_stream_id_builder, patch_v4_accounts_by_account_id_pipelines_v1_streams_by_stream_id_task,
    delete_v4_accounts_by_account_id_pipelines_v1_streams_by_stream_id_builder, delete_v4_accounts_by_account_id_pipelines_v1_streams_by_stream_id_task,
    post_v4_accounts_by_account_id_pipelines_v1_validate_sql_builder, post_v4_accounts_by_account_id_pipelines_v1_validate_sql_task,
    get_v4_accounts_by_account_id_pipelines_by_pipeline_name_deprecated_builder, get_v4_accounts_by_account_id_pipelines_by_pipeline_name_deprecated_task,
    put_v4_accounts_by_account_id_pipelines_by_pipeline_name_deprecated_builder, put_v4_accounts_by_account_id_pipelines_by_pipeline_name_deprecated_task,
    delete_v4_accounts_by_account_id_pipelines_by_pipeline_name_deprecated_builder, delete_v4_accounts_by_account_id_pipelines_by_pipeline_name_deprecated_task,
    accounts_get_account_profile_builder, accounts_get_account_profile_task,
    accounts_modify_account_profile_builder, accounts_modify_account_profile_task,
    queues_list_builder, queues_list_task,
    queues_create_builder, queues_create_task,
    queues_get_builder, queues_get_task,
    queues_update_builder, queues_update_task,
    queues_update_partial_builder, queues_update_partial_task,
    queues_delete_builder, queues_delete_task,
    queues_list_consumers_builder, queues_list_consumers_task,
    queues_create_consumer_builder, queues_create_consumer_task,
    queues_get_consumer_builder, queues_get_consumer_task,
    queues_update_consumer_builder, queues_update_consumer_task,
    queues_delete_consumer_builder, queues_delete_consumer_task,
    queues_push_message_builder, queues_push_message_task,
    queues_ack_messages_builder, queues_ack_messages_task,
    queues_push_messages_builder, queues_push_messages_task,
    queues_pull_messages_builder, queues_pull_messages_task,
    queues_purge_get_builder, queues_purge_get_task,
    queues_purge_builder, queues_purge_task,
    list_catalogs_builder, list_catalogs_task,
    get_catalog_details_builder, get_catalog_details_task,
    store_credentials_builder, store_credentials_task,
    disable_catalog_builder, disable_catalog_task,
    enable_catalog_builder, enable_catalog_task,
    get_maintenance_config_builder, get_maintenance_config_task,
    update_maintenance_config_builder, update_maintenance_config_task,
    list_namespaces_builder, list_namespaces_task,
    list_tables_builder, list_tables_task,
    get_table_maintenance_config_builder, get_table_maintenance_config_task,
    update_table_maintenance_config_builder, update_table_maintenance_config_task,
    r2_list_buckets_builder, r2_list_buckets_task,
    r2_create_bucket_builder, r2_create_bucket_task,
    r2_get_bucket_builder, r2_get_bucket_task,
    r2_patch_bucket_builder, r2_patch_bucket_task,
    r2_delete_bucket_builder, r2_delete_bucket_task,
    r2_get_bucket_cors_policy_builder, r2_get_bucket_cors_policy_task,
    r2_put_bucket_cors_policy_builder, r2_put_bucket_cors_policy_task,
    r2_delete_bucket_cors_policy_builder, r2_delete_bucket_cors_policy_task,
    r2_list_custom_domains_builder, r2_list_custom_domains_task,
    r2_add_custom_domain_builder, r2_add_custom_domain_task,
    r2_get_custom_domain_settings_builder, r2_get_custom_domain_settings_task,
    r2_edit_custom_domain_settings_builder, r2_edit_custom_domain_settings_task,
    r2_delete_custom_domain_builder, r2_delete_custom_domain_task,
    r2_get_bucket_public_policy_builder, r2_get_bucket_public_policy_task,
    r2_put_bucket_public_policy_builder, r2_put_bucket_public_policy_task,
    r2_get_bucket_lifecycle_configuration_builder, r2_get_bucket_lifecycle_configuration_task,
    r2_put_bucket_lifecycle_configuration_builder, r2_put_bucket_lifecycle_configuration_task,
    r2_get_bucket_local_uploads_configuration_builder, r2_get_bucket_local_uploads_configuration_task,
    r2_put_bucket_local_uploads_configuration_builder, r2_put_bucket_local_uploads_configuration_task,
    r2_get_bucket_lock_configuration_builder, r2_get_bucket_lock_configuration_task,
    r2_put_bucket_lock_configuration_builder, r2_put_bucket_lock_configuration_task,
    r2_get_bucket_sippy_config_builder, r2_get_bucket_sippy_config_task,
    r2_put_bucket_sippy_config_builder, r2_put_bucket_sippy_config_task,
    r2_delete_bucket_sippy_config_builder, r2_delete_bucket_sippy_config_task,
    r2_get_account_level_metrics_builder, r2_get_account_level_metrics_task,
    r2_create_temp_access_credentials_builder, r2_create_temp_access_credentials_task,
    get_apps_builder, get_apps_task,
    create_app_builder, create_app_task,
    get_org_analytics_builder, get_org_analytics_task,
    get_livestream_analytics_complete_builder, get_livestream_analytics_complete_task,
    fetch_all_livestreams_builder, fetch_all_livestreams_task,
    get_v2_livestreams_livestream_session_id_builder, get_v2_livestreams_livestream_session_id_task,
    get_v2_livestream_session_livestream_id_builder, get_v2_livestream_session_livestream_id_task,
    get_v2_active_livestream_session_details_builder, get_v2_active_livestream_session_details_task,
    get_all_meetings_builder, get_all_meetings_task,
    create_meeting_builder, create_meeting_task,
    get_meeting_builder, get_meeting_task,
    replace_meeting_builder, replace_meeting_task,
    update_meeting_builder, update_meeting_task,
    get_v2_meetings_meeting_id_active_livestream_builder, get_v2_meetings_meeting_id_active_livestream_task,
    stop_livestreaming_builder, stop_livestreaming_task,
    get_active_session_builder, get_active_session_task,
    kick_partcipants_builder, kick_partcipants_task,
    kick_all_participants_builder, kick_all_participants_task,
    mute_participants_builder, mute_participants_task,
    mute_all_participants_builder, mute_all_participants_task,
    create_poll_builder, create_poll_task,
    livestream_session_details_builder, livestream_session_details_task,
    start_livestreaming_builder, start_livestreaming_task,
    get_meeting_participants_builder, get_meeting_participants_task,
    add_participant_builder, add_participant_task,
    get_meeting_participant_builder, get_meeting_participant_task,
    edit_participant_builder, edit_participant_task,
    delete_meeting_participant_builder, delete_meeting_participant_task,
    regenerate_token_builder, regenerate_token_task,
    get_presets_builder, get_presets_task,
    post_presets_builder, post_presets_task,
    get_presets_preset_id_builder, get_presets_preset_id_task,
    patch_presets_preset_id_builder, patch_presets_preset_id_task,
    delete_presets_preset_id_builder, delete_presets_preset_id_task,
    get_all_recordings_builder, get_all_recordings_task,
    start_recording_builder, start_recording_task,
    get_active_recording_builder, get_active_recording_task,
    start_track_recording_for_a_meeting_builder, start_track_recording_for_a_meeting_task,
    get_one_recording_builder, get_one_recording_task,
    pause_resume_stop_recording_builder, pause_resume_stop_recording_task,
    get_sessions_builder, get_sessions_task,
    get_participant_data_from_peer_id_builder, get_participant_data_from_peer_id_task,
    get_session_details_builder, get_session_details_task,
    get_session_chat_builder, get_session_chat_task,
    get_v2_livestreamsession_session_meeting_id_active_livestream_builder, get_v2_livestreamsession_session_meeting_id_active_livestream_task,
    get_session_participants_builder, get_session_participants_task,
    get_participant_details_builder, get_participant_details_task,
    get_session_summary_builder, get_session_summary_task,
    post_sessions_session_id_summary_builder, post_sessions_session_id_summary_task,
    get_session_transcript_builder, get_session_transcript_task,
    get_all_webhooks_builder, get_all_webhooks_task,
    add_webhook_builder, add_webhook_task,
    get_webhook_builder, get_webhook_task,
    replace_webhook_builder, replace_webhook_task,
    edit_webhook_builder, edit_webhook_task,
    delete_webhook_builder, delete_webhook_task,
    registrar_domains_list_domains_builder, registrar_domains_list_domains_task,
    registrar_domains_get_domain_builder, registrar_domains_get_domain_task,
    registrar_domains_update_domain_builder, registrar_domains_update_domain_task,
    account_request_tracer_request_trace_builder, account_request_tracer_request_trace_task,
    account_roles_list_roles_builder, account_roles_list_roles_task,
    account_roles_role_details_builder, account_roles_role_details_task,
    lists_get_lists_builder, lists_get_lists_task,
    lists_create_a_list_builder, lists_create_a_list_task,
    lists_get_bulk_operation_status_builder, lists_get_bulk_operation_status_task,
    lists_get_a_list_builder, lists_get_a_list_task,
    lists_update_a_list_builder, lists_update_a_list_task,
    lists_delete_a_list_builder, lists_delete_a_list_task,
    lists_get_list_items_builder, lists_get_list_items_task,
    lists_create_list_items_builder, lists_create_list_items_task,
    lists_update_all_list_items_builder, lists_update_all_list_items_task,
    lists_delete_list_items_builder, lists_delete_list_items_task,
    lists_get_a_list_item_builder, lists_get_a_list_item_task,
    list_account_rulesets_builder, list_account_rulesets_task,
    create_account_ruleset_builder, create_account_ruleset_task,
    get_account_entrypoint_ruleset_builder, get_account_entrypoint_ruleset_task,
    update_account_entrypoint_ruleset_builder, update_account_entrypoint_ruleset_task,
    list_account_entrypoint_ruleset_versions_builder, list_account_entrypoint_ruleset_versions_task,
    get_account_entrypoint_ruleset_version_builder, get_account_entrypoint_ruleset_version_task,
    get_account_ruleset_builder, get_account_ruleset_task,
    update_account_ruleset_builder, update_account_ruleset_task,
    delete_account_ruleset_builder, delete_account_ruleset_task,
    create_account_ruleset_rule_builder, create_account_ruleset_rule_task,
    update_account_ruleset_rule_builder, update_account_ruleset_rule_task,
    delete_account_ruleset_rule_builder, delete_account_ruleset_rule_task,
    list_account_ruleset_versions_builder, list_account_ruleset_versions_task,
    get_account_ruleset_version_builder, get_account_ruleset_version_task,
    delete_account_ruleset_version_builder, delete_account_ruleset_version_task,
    list_account_ruleset_version_rules_by_tag_builder, list_account_ruleset_version_rules_by_tag_task,
    web_analytics_create_site_builder, web_analytics_create_site_task,
    web_analytics_list_sites_builder, web_analytics_list_sites_task,
    web_analytics_get_site_builder, web_analytics_get_site_task,
    web_analytics_update_site_builder, web_analytics_update_site_task,
    web_analytics_delete_site_builder, web_analytics_delete_site_task,
    web_analytics_create_rule_builder, web_analytics_create_rule_task,
    web_analytics_update_rule_builder, web_analytics_update_rule_task,
    web_analytics_delete_rule_builder, web_analytics_delete_rule_task,
    web_analytics_list_rules_builder, web_analytics_list_rules_task,
    web_analytics_modify_rules_builder, web_analytics_modify_rules_task,
    scim_groups_list_builder, scim_groups_list_task,
    scim_groups_create_builder, scim_groups_create_task,
    scim_groups_get_builder, scim_groups_get_task,
    scim_groups_patch_builder, scim_groups_patch_task,
    scim_groups_delete_builder, scim_groups_delete_task,
    scim_resource_types_list_builder, scim_resource_types_list_task,
    scim_resource_types_get_builder, scim_resource_types_get_task,
    scim_schemas_list_builder, scim_schemas_list_task,
    scim_schemas_get_builder, scim_schemas_get_task,
    scim_service_provider_config_get_builder, scim_service_provider_config_get_task,
    scim_users_list_builder, scim_users_list_task,
    scim_users_create_builder, scim_users_create_task,
    scim_users_get_builder, scim_users_get_task,
    scim_users_put_builder, scim_users_put_task,
    scim_users_patch_builder, scim_users_patch_task,
    secondary_dns_acl_list_ac_ls_builder, secondary_dns_acl_list_ac_ls_task,
    secondary_dns_acl_create_acl_builder, secondary_dns_acl_create_acl_task,
    secondary_dns_acl_acl_details_builder, secondary_dns_acl_acl_details_task,
    secondary_dns_acl_update_acl_builder, secondary_dns_acl_update_acl_task,
    secondary_dns_acl_delete_acl_builder, secondary_dns_acl_delete_acl_task,
    secondary_dns_peer_list_peers_builder, secondary_dns_peer_list_peers_task,
    secondary_dns_peer_create_peer_builder, secondary_dns_peer_create_peer_task,
    secondary_dns_peer_peer_details_builder, secondary_dns_peer_peer_details_task,
    secondary_dns_peer_update_peer_builder, secondary_dns_peer_update_peer_task,
    secondary_dns_peer_delete_peer_builder, secondary_dns_peer_delete_peer_task,
    secondary_dns_tsig_list_tsi_gs_builder, secondary_dns_tsig_list_tsi_gs_task,
    secondary_dns_tsig_create_tsig_builder, secondary_dns_tsig_create_tsig_task,
    secondary_dns_tsig_tsig_details_builder, secondary_dns_tsig_tsig_details_task,
    secondary_dns_tsig_update_tsig_builder, secondary_dns_tsig_update_tsig_task,
    secondary_dns_tsig_delete_tsig_builder, secondary_dns_tsig_delete_tsig_task,
    secrets_store_quota_builder, secrets_store_quota_task,
    secrets_store_list_builder, secrets_store_list_task,
    secrets_store_create_builder, secrets_store_create_task,
    secrets_store_get_store_by_id_builder, secrets_store_get_store_by_id_task,
    secrets_store_delete_by_id_builder, secrets_store_delete_by_id_task,
    secrets_store_secrets_list_builder, secrets_store_secrets_list_task,
    secrets_store_secret_create_builder, secrets_store_secret_create_task,
    secrets_store_delete_bulk_builder, secrets_store_delete_bulk_task,
    secrets_store_get_by_id_builder, secrets_store_get_by_id_task,
    secrets_store_patch_by_id_builder, secrets_store_patch_by_id_task,
    secrets_store_secret_delete_by_id_builder, secrets_store_secret_delete_by_id_task,
    secrets_store_duplicate_by_id_builder, secrets_store_duplicate_by_id_task,
    get_security_center_insights_builder, get_security_center_insights_task,
    get_security_center_insight_counts_by_class_builder, get_security_center_insight_counts_by_class_task,
    get_security_center_insight_counts_by_severity_builder, get_security_center_insight_counts_by_severity_task,
    get_security_center_insight_counts_by_type_builder, get_security_center_insight_counts_by_type_task,
    get_security_center_insight_context_builder, get_security_center_insight_context_task,
    archive_security_center_insight_builder, archive_security_center_insight_task,
    shares_list_builder, shares_list_task,
    share_create_builder, share_create_task,
    shares_get_by_id_builder, shares_get_by_id_task,
    share_update_builder, share_update_task,
    share_delete_builder, share_delete_task,
    share_recipients_list_builder, share_recipients_list_task,
    share_recipient_create_builder, share_recipient_create_task,
    share_recipients_update_builder, share_recipients_update_task,
    share_recipients_get_by_id_builder, share_recipients_get_by_id_task,
    share_recipient_delete_builder, share_recipient_delete_task,
    share_resources_list_builder, share_resources_list_task,
    share_resource_create_builder, share_resource_create_task,
    share_resources_get_by_id_builder, share_resources_get_by_id_task,
    share_resource_update_builder, share_resource_update_task,
    share_resource_delete_builder, share_resource_delete_task,
    slurper_list_jobs_builder, slurper_list_jobs_task,
    slurper_create_job_builder, slurper_create_job_task,
    slurper_abort_all_jobs_builder, slurper_abort_all_jobs_task,
    slurper_get_job_builder, slurper_get_job_task,
    slurper_abort_job_builder, slurper_abort_job_task,
    slurper_get_job_logs_builder, slurper_get_job_logs_task,
    slurper_pause_job_builder, slurper_pause_job_task,
    slurper_get_job_progress_builder, slurper_get_job_progress_task,
    slurper_resume_job_builder, slurper_resume_job_task,
    slurper_check_source_connectivity_builder, slurper_check_source_connectivity_task,
    slurper_check_target_connectivity_builder, slurper_check_target_connectivity_task,
    get_all_sso_connectors_builder, get_all_sso_connectors_task,
    init_new_sso_connector_builder, init_new_sso_connector_task,
    get_sso_connector_builder, get_sso_connector_task,
    update_sso_connector_state_builder, update_sso_connector_state_task,
    delete_sso_connector_builder, delete_sso_connector_task,
    begin_sso_connector_verification_builder, begin_sso_connector_verification_task,
    workers_kv_namespace_list_namespaces_builder, workers_kv_namespace_list_namespaces_task,
    workers_kv_namespace_create_a_namespace_builder, workers_kv_namespace_create_a_namespace_task,
    workers_kv_namespace_get_a_namespace_builder, workers_kv_namespace_get_a_namespace_task,
    workers_kv_namespace_rename_a_namespace_builder, workers_kv_namespace_rename_a_namespace_task,
    workers_kv_namespace_remove_a_namespace_builder, workers_kv_namespace_remove_a_namespace_task,
    workers_kv_namespace_write_multiple_key_value_pairs_builder, workers_kv_namespace_write_multiple_key_value_pairs_task,
    workers_kv_namespace_delete_multiple_key_value_pairs_deprecated_builder, workers_kv_namespace_delete_multiple_key_value_pairs_deprecated_task,
    workers_kv_namespace_delete_multiple_key_value_pairs_builder, workers_kv_namespace_delete_multiple_key_value_pairs_task,
    workers_kv_namespace_get_multiple_key_value_pairs_builder, workers_kv_namespace_get_multiple_key_value_pairs_task,
    workers_kv_namespace_list_a_namespace_s_keys_builder, workers_kv_namespace_list_a_namespace_s_keys_task,
    workers_kv_namespace_read_the_metadata_for_a_key_builder, workers_kv_namespace_read_the_metadata_for_a_key_task,
    workers_kv_namespace_read_key_value_pair_builder, workers_kv_namespace_read_key_value_pair_task,
    workers_kv_namespace_write_key_value_pair_with_metadata_builder, workers_kv_namespace_write_key_value_pair_with_metadata_task,
    workers_kv_namespace_delete_key_value_pair_builder, workers_kv_namespace_delete_key_value_pair_task,
    stream_videos_list_videos_builder, stream_videos_list_videos_task,
    stream_videos_initiate_video_uploads_using_tus_builder, stream_videos_initiate_video_uploads_using_tus_task,
    stream_video_clipping_clip_videos_given_a_start_and_end_time_builder, stream_video_clipping_clip_videos_given_a_start_and_end_time_task,
    stream_videos_upload_videos_from_a_url_builder, stream_videos_upload_videos_from_a_url_task,
    stream_videos_upload_videos_via_direct_upload_ur_ls_builder, stream_videos_upload_videos_via_direct_upload_ur_ls_task,
    stream_signing_keys_list_signing_keys_builder, stream_signing_keys_list_signing_keys_task,
    stream_signing_keys_create_signing_keys_builder, stream_signing_keys_create_signing_keys_task,
    stream_signing_keys_delete_signing_keys_builder, stream_signing_keys_delete_signing_keys_task,
    stream_live_inputs_list_live_inputs_builder, stream_live_inputs_list_live_inputs_task,
    stream_live_inputs_create_a_live_input_builder, stream_live_inputs_create_a_live_input_task,
    stream_live_inputs_retrieve_a_live_input_builder, stream_live_inputs_retrieve_a_live_input_task,
    stream_live_inputs_update_a_live_input_builder, stream_live_inputs_update_a_live_input_task,
    stream_live_inputs_delete_a_live_input_builder, stream_live_inputs_delete_a_live_input_task,
    stream_live_inputs_disable_a_live_input_builder, stream_live_inputs_disable_a_live_input_task,
    stream_live_inputs_enable_a_live_input_builder, stream_live_inputs_enable_a_live_input_task,
    stream_live_inputs_list_all_outputs_associated_with_a_specified_live_input_builder, stream_live_inputs_list_all_outputs_associated_with_a_specified_live_input_task,
    stream_live_inputs_create_a_new_output_connected_to_a_live_input_builder, stream_live_inputs_create_a_new_output_connected_to_a_live_input_task,
    stream_live_inputs_update_an_output_builder, stream_live_inputs_update_an_output_task,
    stream_live_inputs_delete_an_output_builder, stream_live_inputs_delete_an_output_task,
    stream_videos_storage_usage_builder, stream_videos_storage_usage_task,
    stream_watermark_profile_list_watermark_profiles_builder, stream_watermark_profile_list_watermark_profiles_task,
    stream_watermark_profile_create_watermark_profiles_via_basic_upload_builder, stream_watermark_profile_create_watermark_profiles_via_basic_upload_task,
    stream_watermark_profile_watermark_profile_details_builder, stream_watermark_profile_watermark_profile_details_task,
    stream_watermark_profile_delete_watermark_profiles_builder, stream_watermark_profile_delete_watermark_profiles_task,
    stream_webhook_view_webhooks_builder, stream_webhook_view_webhooks_task,
    stream_webhook_create_webhooks_builder, stream_webhook_create_webhooks_task,
    stream_webhook_delete_webhooks_builder, stream_webhook_delete_webhooks_task,
    stream_videos_retrieve_video_details_builder, stream_videos_retrieve_video_details_task,
    stream_videos_update_video_details_builder, stream_videos_update_video_details_task,
    stream_videos_delete_video_builder, stream_videos_delete_video_task,
    list_audio_tracks_builder, list_audio_tracks_task,
    add_audio_track_builder, add_audio_track_task,
    edit_audio_tracks_builder, edit_audio_tracks_task,
    delete_audio_tracks_builder, delete_audio_tracks_task,
    stream_subtitles_captions_list_captions_or_subtitles_builder, stream_subtitles_captions_list_captions_or_subtitles_task,
    stream_subtitles_captions_get_caption_or_subtitle_for_language_builder, stream_subtitles_captions_get_caption_or_subtitle_for_language_task,
    stream_subtitles_captions_upload_captions_or_subtitles_builder, stream_subtitles_captions_upload_captions_or_subtitles_task,
    stream_subtitles_captions_delete_captions_or_subtitles_builder, stream_subtitles_captions_delete_captions_or_subtitles_task,
    stream_subtitles_captions_generate_caption_or_subtitle_for_language_builder, stream_subtitles_captions_generate_caption_or_subtitle_for_language_task,
    stream_subtitles_captions_get_vtt_caption_or_subtitle_builder, stream_subtitles_captions_get_vtt_caption_or_subtitle_task,
    stream_m_p_4_downloads_list_downloads_builder, stream_m_p_4_downloads_list_downloads_task,
    stream_m_p_4_downloads_create_downloads_builder, stream_m_p_4_downloads_create_downloads_task,
    stream_m_p_4_downloads_delete_downloads_builder, stream_m_p_4_downloads_delete_downloads_task,
    stream_downloads_create_type_specific_downloads_builder, stream_downloads_create_type_specific_downloads_task,
    stream_downloads_delete_type_specific_downloads_builder, stream_downloads_delete_type_specific_downloads_task,
    stream_videos_retreieve_embed_code_html_builder, stream_videos_retreieve_embed_code_html_task,
    stream_videos_create_signed_url_tokens_for_videos_builder, stream_videos_create_signed_url_tokens_for_videos_task,
    account_subscriptions_list_subscriptions_builder, account_subscriptions_list_subscriptions_task,
    account_subscriptions_create_subscription_builder, account_subscriptions_create_subscription_task,
    account_subscriptions_update_subscription_builder, account_subscriptions_update_subscription_task,
    account_subscriptions_delete_subscription_builder, account_subscriptions_delete_subscription_task,
    tags_get_builder, tags_get_task,
    tags_set_builder, tags_set_task,
    tags_delete_builder, tags_delete_task,
    tags_list_keys_builder, tags_list_keys_task,
    tags_list_builder, tags_list_task,
    tags_list_values_builder, tags_list_values_task,
    tunnel_route_list_tunnel_routes_builder, tunnel_route_list_tunnel_routes_task,
    tunnel_route_create_a_tunnel_route_builder, tunnel_route_create_a_tunnel_route_task,
    tunnel_route_get_tunnel_route_by_ip_builder, tunnel_route_get_tunnel_route_by_ip_task,
    tunnel_route_create_a_tunnel_route_with_cidr_builder, tunnel_route_create_a_tunnel_route_with_cidr_task,
    tunnel_route_update_a_tunnel_route_with_cidr_builder, tunnel_route_update_a_tunnel_route_with_cidr_task,
    tunnel_route_delete_a_tunnel_route_with_cidr_builder, tunnel_route_delete_a_tunnel_route_with_cidr_task,
    tunnel_route_get_tunnel_route_builder, tunnel_route_get_tunnel_route_task,
    tunnel_route_update_a_tunnel_route_builder, tunnel_route_update_a_tunnel_route_task,
    tunnel_route_delete_a_tunnel_route_builder, tunnel_route_delete_a_tunnel_route_task,
    tunnel_virtual_network_list_virtual_networks_builder, tunnel_virtual_network_list_virtual_networks_task,
    tunnel_virtual_network_create_a_virtual_network_builder, tunnel_virtual_network_create_a_virtual_network_task,
    tunnel_virtual_network_get_builder, tunnel_virtual_network_get_task,
    tunnel_virtual_network_update_builder, tunnel_virtual_network_update_task,
    tunnel_virtual_network_delete_builder, tunnel_virtual_network_delete_task,
    account_api_tokens_list_tokens_builder, account_api_tokens_list_tokens_task,
    account_api_tokens_create_token_builder, account_api_tokens_create_token_task,
    account_api_tokens_list_permission_groups_builder, account_api_tokens_list_permission_groups_task,
    account_api_tokens_verify_token_builder, account_api_tokens_verify_token_task,
    account_api_tokens_token_details_builder, account_api_tokens_token_details_task,
    account_api_tokens_update_token_builder, account_api_tokens_update_token_task,
    account_api_tokens_delete_token_builder, account_api_tokens_delete_token_task,
    account_api_tokens_roll_token_builder, account_api_tokens_roll_token_task,
    cloudflare_tunnel_list_all_tunnels_builder, cloudflare_tunnel_list_all_tunnels_task,
    urlscanner_get_response_text_builder, urlscanner_get_response_text_task,
    urlscanner_search_scans_builder, urlscanner_search_scans_task,
    urlscanner_create_scan_builder, urlscanner_create_scan_task,
    urlscanner_get_scan_builder, urlscanner_get_scan_task,
    urlscanner_get_scan_har_builder, urlscanner_get_scan_har_task,
    urlscanner_get_scan_screenshot_builder, urlscanner_get_scan_screenshot_task,
    urlscanner_create_scan_bulk_v2_builder, urlscanner_create_scan_bulk_v2_task,
    urlscanner_get_scan_dom_v2_builder, urlscanner_get_scan_dom_v2_task,
    urlscanner_get_scan_har_v2_builder, urlscanner_get_scan_har_v2_task,
    urlscanner_get_response_v2_builder, urlscanner_get_response_v2_task,
    urlscanner_get_scan_v2_builder, urlscanner_get_scan_v2_task,
    urlscanner_create_scan_v2_builder, urlscanner_create_scan_v2_task,
    urlscanner_get_scan_screenshot_v2_builder, urlscanner_get_scan_screenshot_v2_task,
    urlscanner_search_scans_v2_builder, urlscanner_search_scans_v2_task,
    vectorize_deprecated_list_vectorize_indexes_builder, vectorize_deprecated_list_vectorize_indexes_task,
    vectorize_deprecated_create_vectorize_index_builder, vectorize_deprecated_create_vectorize_index_task,
    vectorize_deprecated_get_vectorize_index_builder, vectorize_deprecated_get_vectorize_index_task,
    vectorize_deprecated_update_vectorize_index_builder, vectorize_deprecated_update_vectorize_index_task,
    vectorize_deprecated_delete_vectorize_index_builder, vectorize_deprecated_delete_vectorize_index_task,
    vectorize_deprecated_delete_vectors_by_id_builder, vectorize_deprecated_delete_vectors_by_id_task,
    vectorize_deprecated_get_vectors_by_id_builder, vectorize_deprecated_get_vectors_by_id_task,
    vectorize_deprecated_insert_vector_builder, vectorize_deprecated_insert_vector_task,
    vectorize_deprecated_query_vector_builder, vectorize_deprecated_query_vector_task,
    vectorize_deprecated_upsert_vector_builder, vectorize_deprecated_upsert_vector_task,
    vectorize_list_vectorize_indexes_builder, vectorize_list_vectorize_indexes_task,
    vectorize_create_vectorize_index_builder, vectorize_create_vectorize_index_task,
    vectorize_get_vectorize_index_builder, vectorize_get_vectorize_index_task,
    vectorize_delete_vectorize_index_builder, vectorize_delete_vectorize_index_task,
    vectorize_delete_vectors_by_id_builder, vectorize_delete_vectors_by_id_task,
    vectorize_get_vectors_by_id_builder, vectorize_get_vectors_by_id_task,
    vectorize_index_info_builder, vectorize_index_info_task,
    vectorize_insert_vector_builder, vectorize_insert_vector_task,
    vectorize_list_vectors_builder, vectorize_list_vectors_task,
    vectorize_create_metadata_index_builder, vectorize_create_metadata_index_task,
    vectorize_delete_metadata_index_builder, vectorize_delete_metadata_index_task,
    vectorize_list_metadata_indexes_builder, vectorize_list_metadata_indexes_task,
    vectorize_query_vector_builder, vectorize_query_vector_task,
    vectorize_upsert_vector_builder, vectorize_upsert_vector_task,
    waiting_room_list_waiting_rooms_account_builder, waiting_room_list_waiting_rooms_account_task,
    cloudflare_tunnel_list_warp_connector_tunnels_builder, cloudflare_tunnel_list_warp_connector_tunnels_task,
    cloudflare_tunnel_create_a_warp_connector_tunnel_builder, cloudflare_tunnel_create_a_warp_connector_tunnel_task,
    cloudflare_tunnel_get_a_warp_connector_tunnel_builder, cloudflare_tunnel_get_a_warp_connector_tunnel_task,
    cloudflare_tunnel_update_a_warp_connector_tunnel_builder, cloudflare_tunnel_update_a_warp_connector_tunnel_task,
    cloudflare_tunnel_delete_a_warp_connector_tunnel_builder, cloudflare_tunnel_delete_a_warp_connector_tunnel_task,
    cloudflare_tunnel_list_warp_connector_tunnel_connections_builder, cloudflare_tunnel_list_warp_connector_tunnel_connections_task,
    cloudflare_tunnel_get_warp_connector_tunnel_connector_builder, cloudflare_tunnel_get_warp_connector_tunnel_connector_task,
    cloudflare_tunnel_manual_failover_warp_connector_tunnel_builder, cloudflare_tunnel_manual_failover_warp_connector_tunnel_task,
    cloudflare_tunnel_get_a_warp_connector_tunnel_token_builder, cloudflare_tunnel_get_a_warp_connector_tunnel_token_task,
    worker_account_settings_fetch_worker_account_settings_builder, worker_account_settings_fetch_worker_account_settings_task,
    worker_account_settings_create_worker_account_settings_builder, worker_account_settings_create_worker_account_settings_task,
    worker_assets_upload_builder, worker_assets_upload_task,
    namespace_worker_list_builder, namespace_worker_list_task,
    namespace_worker_create_builder, namespace_worker_create_task,
    namespace_worker_get_namespace_builder, namespace_worker_get_namespace_task,
    namespace_worker_put_namespace_builder, namespace_worker_put_namespace_task,
    namespace_worker_patch_namespace_builder, namespace_worker_patch_namespace_task,
    namespace_worker_delete_namespace_builder, namespace_worker_delete_namespace_task,
    namespace_worker_list_scripts_builder, namespace_worker_list_scripts_task,
    namespace_worker_delete_scripts_builder, namespace_worker_delete_scripts_task,
    namespace_worker_script_worker_details_builder, namespace_worker_script_worker_details_task,
    namespace_worker_script_upload_worker_module_builder, namespace_worker_script_upload_worker_module_task,
    namespace_worker_script_delete_worker_builder, namespace_worker_script_delete_worker_task,
    namespace_worker_script_update_create_assets_upload_session_builder, namespace_worker_script_update_create_assets_upload_session_task,
    namespace_worker_get_script_bindings_builder, namespace_worker_get_script_bindings_task,
    namespace_worker_get_script_content_builder, namespace_worker_get_script_content_task,
    namespace_worker_put_script_content_builder, namespace_worker_put_script_content_task,
    namespace_worker_list_script_secrets_builder, namespace_worker_list_script_secrets_task,
    namespace_worker_put_script_secrets_builder, namespace_worker_put_script_secrets_task,
    namespace_worker_get_script_secrets_builder, namespace_worker_get_script_secrets_task,
    namespace_worker_delete_script_secret_builder, namespace_worker_delete_script_secret_task,
    namespace_worker_get_script_settings_builder, namespace_worker_get_script_settings_task,
    namespace_worker_patch_script_settings_builder, namespace_worker_patch_script_settings_task,
    namespace_worker_get_script_tags_builder, namespace_worker_get_script_tags_task,
    namespace_worker_put_script_tags_builder, namespace_worker_put_script_tags_task,
    namespace_worker_put_script_tag_builder, namespace_worker_put_script_tag_task,
    namespace_worker_delete_script_tag_builder, namespace_worker_delete_script_tag_task,
    workers_domains_list_builder, workers_domains_list_task,
    workers_domains_update_builder, workers_domains_update_task,
    workers_domains_get_builder, workers_domains_get_task,
    workers_domains_delete_builder, workers_domains_delete_task,
    durable_objects_namespace_list_namespaces_builder, durable_objects_namespace_list_namespaces_task,
    durable_objects_namespace_list_objects_builder, durable_objects_namespace_list_objects_task,
    destination_list_builder, destination_list_task,
    destination_create_builder, destination_create_task,
    destination_update_builder, destination_update_task,
    destinations_delete_builder, destinations_delete_task,
    queries_list_builder, queries_list_task,
    queries_post_builder, queries_post_task,
    queries_get_builder, queries_get_task,
    queries_patch_builder, queries_patch_task,
    queries_delete_builder, queries_delete_task,
    telemetry_keys_list_builder, telemetry_keys_list_task,
    telemetry_query_builder, telemetry_query_task,
    telemetry_values_list_builder, telemetry_values_list_task,
    worker_placement_list_regions_builder, worker_placement_list_regions_task,
    worker_script_list_workers_builder, worker_script_list_workers_task,
    worker_script_search_workers_builder, worker_script_search_workers_task,
    worker_script_download_worker_builder, worker_script_download_worker_task,
    worker_script_upload_worker_module_builder, worker_script_upload_worker_module_task,
    worker_script_delete_worker_builder, worker_script_delete_worker_task,
    worker_script_update_create_assets_upload_session_builder, worker_script_update_create_assets_upload_session_task,
    worker_script_put_content_builder, worker_script_put_content_task,
    worker_script_get_content_builder, worker_script_get_content_task,
    worker_deployments_list_deployments_builder, worker_deployments_list_deployments_task,
    worker_deployments_create_deployment_builder, worker_deployments_create_deployment_task,
    worker_deployments_get_deployment_builder, worker_deployments_get_deployment_task,
    worker_deployments_delete_deployment_builder, worker_deployments_delete_deployment_task,
    worker_cron_trigger_get_cron_triggers_builder, worker_cron_trigger_get_cron_triggers_task,
    worker_cron_trigger_update_cron_triggers_builder, worker_cron_trigger_update_cron_triggers_task,
    worker_script_settings_get_settings_builder, worker_script_settings_get_settings_task,
    worker_script_settings_patch_settings_builder, worker_script_settings_patch_settings_task,
    worker_list_script_secrets_builder, worker_list_script_secrets_task,
    worker_put_script_secret_builder, worker_put_script_secret_task,
    worker_get_script_secret_builder, worker_get_script_secret_task,
    worker_delete_script_secret_builder, worker_delete_script_secret_task,
    worker_script_get_settings_builder, worker_script_get_settings_task,
    worker_script_patch_settings_builder, worker_script_patch_settings_task,
    worker_script_get_subdomain_builder, worker_script_get_subdomain_task,
    worker_script_post_subdomain_builder, worker_script_post_subdomain_task,
    worker_script_delete_subdomain_builder, worker_script_delete_subdomain_task,
    worker_tail_logs_start_tail_builder, worker_tail_logs_start_tail_task,
    worker_tail_logs_delete_tail_builder, worker_tail_logs_delete_tail_task,
    worker_script_fetch_usage_model_builder, worker_script_fetch_usage_model_task,
    worker_script_update_usage_model_builder, worker_script_update_usage_model_task,
    worker_versions_list_versions_builder, worker_versions_list_versions_task,
    worker_versions_upload_version_builder, worker_versions_upload_version_task,
    worker_versions_get_version_detail_builder, worker_versions_get_version_detail_task,
    worker_environment_get_script_content_builder, worker_environment_get_script_content_task,
    worker_environment_put_script_content_builder, worker_environment_put_script_content_task,
    worker_script_environment_get_settings_builder, worker_script_environment_get_settings_task,
    worker_script_environment_patch_settings_builder, worker_script_environment_patch_settings_task,
    worker_subdomain_get_subdomain_builder, worker_subdomain_get_subdomain_task,
    worker_subdomain_create_subdomain_builder, worker_subdomain_create_subdomain_task,
    worker_subdomain_delete_subdomain_builder, worker_subdomain_delete_subdomain_task,
    list_workers_builder, list_workers_task,
    create_worker_builder, create_worker_task,
    get_worker_builder, get_worker_task,
    update_worker_builder, update_worker_task,
    edit_worker_builder, edit_worker_task,
    delete_worker_builder, delete_worker_task,
    list_worker_versions_builder, list_worker_versions_task,
    create_worker_version_builder, create_worker_version_task,
    get_worker_version_builder, get_worker_version_task,
    delete_worker_version_builder, delete_worker_version_task,
    wor_list_workflows_builder, wor_list_workflows_task,
    wor_get_workflow_details_builder, wor_get_workflow_details_task,
    wor_create_or_modify_workflow_builder, wor_create_or_modify_workflow_task,
    wor_delete_workflow_builder, wor_delete_workflow_task,
    wor_list_workflow_instances_builder, wor_list_workflow_instances_task,
    wor_create_new_workflow_instance_builder, wor_create_new_workflow_instance_task,
    wor_batch_create_workflow_instance_builder, wor_batch_create_workflow_instance_task,
    wor_batch_terminate_workflow_instances_builder, wor_batch_terminate_workflow_instances_task,
    wor_status_terminate_workflow_instances_builder, wor_status_terminate_workflow_instances_task,
    wor_describe_workflow_instance_builder, wor_describe_workflow_instance_task,
    wor_send_event_workflow_instance_builder, wor_send_event_workflow_instance_task,
    wor_change_status_workflow_instance_builder, wor_change_status_workflow_instance_task,
    wor_list_workflow_versions_builder, wor_list_workflow_versions_task,
    wor_describe_workflow_versions_builder, wor_describe_workflow_versions_task,
    wor_describe_workflow_versions_dag_builder, wor_describe_workflow_versions_dag_task,
    wor_describe_workflow_versions_graph_builder, wor_describe_workflow_versions_graph_task,
    zero_trust_accounts_get_connectivity_settings_builder, zero_trust_accounts_get_connectivity_settings_task,
    zero_trust_accounts_patch_connectivity_settings_builder, zero_trust_accounts_patch_connectivity_settings_task,
    zero_trust_networks_route_hostname_list_builder, zero_trust_networks_route_hostname_list_task,
    zero_trust_networks_route_hostname_create_builder, zero_trust_networks_route_hostname_create_task,
    zero_trust_networks_route_hostname_get_builder, zero_trust_networks_route_hostname_get_task,
    zero_trust_networks_route_hostname_update_builder, zero_trust_networks_route_hostname_update_task,
    zero_trust_networks_route_hostname_delete_builder, zero_trust_networks_route_hostname_delete_task,
    zero_trust_networks_subnets_list_builder, zero_trust_networks_subnets_list_task,
    zero_trust_networks_subnet_update_cloudflare_source_builder, zero_trust_networks_subnet_update_cloudflare_source_task,
    zero_trust_networks_subnet_create_warp_builder, zero_trust_networks_subnet_create_warp_task,
    zero_trust_networks_subnet_get_warp_builder, zero_trust_networks_subnet_get_warp_task,
    zero_trust_networks_subnet_update_warp_builder, zero_trust_networks_subnet_update_warp_task,
    zero_trust_networks_subnet_delete_warp_builder, zero_trust_networks_subnet_delete_warp_task,
    dlp_risk_score_behaviors_get_builder, dlp_risk_score_behaviors_get_task,
    dlp_risk_score_behaviors_put_builder, dlp_risk_score_behaviors_put_task,
    dlp_zt_risk_score_integration_list_builder, dlp_zt_risk_score_integration_list_task,
    dlp_zt_risk_score_integration_create_builder, dlp_zt_risk_score_integration_create_task,
    dlp_zt_risk_score_integration_get_by_reference_id_builder, dlp_zt_risk_score_integration_get_by_reference_id_task,
    dlp_zt_risk_score_integration_get_builder, dlp_zt_risk_score_integration_get_task,
    dlp_zt_risk_score_integration_update_builder, dlp_zt_risk_score_integration_update_task,
    dlp_zt_risk_score_integration_delete_builder, dlp_zt_risk_score_integration_delete_task,
    dlp_risk_score_summary_get_builder, dlp_risk_score_summary_get_task,
    dlp_risk_score_summary_get_for_user_builder, dlp_risk_score_summary_get_for_user_task,
    dlp_risk_score_reset_post_builder, dlp_risk_score_reset_post_task,
    origin_ca_list_certificates_builder, origin_ca_list_certificates_task,
    origin_ca_create_certificate_builder, origin_ca_create_certificate_task,
    origin_ca_get_certificate_builder, origin_ca_get_certificate_task,
    origin_ca_revoke_certificate_builder, origin_ca_revoke_certificate_task,
    cloudflare_ips_cloudflare_ip_details_builder, cloudflare_ips_cloudflare_ip_details_task,
    user_s_account_memberships_list_memberships_builder, user_s_account_memberships_list_memberships_task,
    user_s_account_memberships_membership_details_builder, user_s_account_memberships_membership_details_task,
    user_s_account_memberships_update_membership_builder, user_s_account_memberships_update_membership_task,
    user_s_account_memberships_delete_membership_builder, user_s_account_memberships_delete_membership_task,
    organization_list_organizations_builder, organization_list_organizations_task,
    organizations_create_user_organization_builder, organizations_create_user_organization_task,
    organizations_retrieve_builder, organizations_retrieve_task,
    organizations_modify_builder, organizations_modify_task,
    organizations_delete_builder, organizations_delete_task,
    organizations_get_accounts_builder, organizations_get_accounts_task,
    audit_logs_v2_get_organization_audit_logs_builder, audit_logs_v2_get_organization_audit_logs_task,
    members_list_builder, members_list_task,
    members_create_builder, members_create_task,
    members_retrieve_builder, members_retrieve_task,
    members_delete_builder, members_delete_task,
    members_batch_create_builder, members_batch_create_task,
    organizations_get_profile_builder, organizations_get_profile_task,
    organizations_modify_profile_builder, organizations_modify_profile_task,
    organization_shares_list_builder, organization_shares_list_task,
    radar_get_ai_bots_summary_by_user_agent_builder, radar_get_ai_bots_summary_by_user_agent_task,
    radar_get_ai_bots_summary_builder, radar_get_ai_bots_summary_task,
    radar_get_ai_bots_timeseries_builder, radar_get_ai_bots_timeseries_task,
    radar_get_ai_bots_timeseries_group_by_user_agent_builder, radar_get_ai_bots_timeseries_group_by_user_agent_task,
    radar_get_ai_bots_timeseries_group_builder, radar_get_ai_bots_timeseries_group_task,
    radar_get_ai_inference_summary_by_model_builder, radar_get_ai_inference_summary_by_model_task,
    radar_get_ai_inference_summary_by_task_builder, radar_get_ai_inference_summary_by_task_task,
    radar_get_ai_inference_summary_builder, radar_get_ai_inference_summary_task,
    radar_get_ai_inference_timeseries_group_by_model_builder, radar_get_ai_inference_timeseries_group_by_model_task,
    radar_get_ai_inference_timeseries_group_by_task_builder, radar_get_ai_inference_timeseries_group_by_task_task,
    radar_get_ai_inference_timeseries_group_builder, radar_get_ai_inference_timeseries_group_task,
    radar_get_annotations_builder, radar_get_annotations_task,
    radar_get_annotations_outages_builder, radar_get_annotations_outages_task,
    radar_get_annotations_outages_top_builder, radar_get_annotations_outages_top_task,
    radar_get_dns_as112_timeseries_by_dnssec_builder, radar_get_dns_as112_timeseries_by_dnssec_task,
    radar_get_dns_as112_timeseries_by_edns_builder, radar_get_dns_as112_timeseries_by_edns_task,
    radar_get_dns_as112_timeseries_by_ip_version_builder, radar_get_dns_as112_timeseries_by_ip_version_task,
    radar_get_dns_as112_timeseries_by_protocol_builder, radar_get_dns_as112_timeseries_by_protocol_task,
    radar_get_dns_as112_timeseries_by_query_type_builder, radar_get_dns_as112_timeseries_by_query_type_task,
    radar_get_dns_as112_timeseries_by_response_codes_builder, radar_get_dns_as112_timeseries_by_response_codes_task,
    radar_get_dns_as112_summary_builder, radar_get_dns_as112_summary_task,
    radar_get_dns_as112_timeseries_builder, radar_get_dns_as112_timeseries_task,
    radar_get_dns_as112_timeseries_group_by_dnssec_builder, radar_get_dns_as112_timeseries_group_by_dnssec_task,
    radar_get_dns_as112_timeseries_group_by_edns_builder, radar_get_dns_as112_timeseries_group_by_edns_task,
    radar_get_dns_as112_timeseries_group_by_ip_version_builder, radar_get_dns_as112_timeseries_group_by_ip_version_task,
    radar_get_dns_as112_timeseries_group_by_protocol_builder, radar_get_dns_as112_timeseries_group_by_protocol_task,
    radar_get_dns_as112_timeseries_group_by_query_type_builder, radar_get_dns_as112_timeseries_group_by_query_type_task,
    radar_get_dns_as112_timeseries_group_by_response_codes_builder, radar_get_dns_as112_timeseries_group_by_response_codes_task,
    radar_get_dns_as112_timeseries_group_builder, radar_get_dns_as112_timeseries_group_task,
    radar_get_dns_as112_top_locations_builder, radar_get_dns_as112_top_locations_task,
    radar_get_dns_as112_top_locations_by_dnssec_builder, radar_get_dns_as112_top_locations_by_dnssec_task,
    radar_get_dns_as112_top_locations_by_edns_builder, radar_get_dns_as112_top_locations_by_edns_task,
    radar_get_dns_as112_top_locations_by_ip_version_builder, radar_get_dns_as112_top_locations_by_ip_version_task,
    radar_get_attacks_layer3_summary_by_bitrate_builder, radar_get_attacks_layer3_summary_by_bitrate_task,
    radar_get_attacks_layer3_summary_by_duration_builder, radar_get_attacks_layer3_summary_by_duration_task,
    radar_get_attacks_layer3_summary_by_industry_builder, radar_get_attacks_layer3_summary_by_industry_task,
    radar_get_attacks_layer3_summary_by_ip_version_builder, radar_get_attacks_layer3_summary_by_ip_version_task,
    radar_get_attacks_layer3_summary_by_protocol_builder, radar_get_attacks_layer3_summary_by_protocol_task,
    radar_get_attacks_layer3_summary_by_vector_builder, radar_get_attacks_layer3_summary_by_vector_task,
    radar_get_attacks_layer3_summary_by_vertical_builder, radar_get_attacks_layer3_summary_by_vertical_task,
    radar_get_attacks_layer3_summary_builder, radar_get_attacks_layer3_summary_task,
    radar_get_attacks_layer3_timeseries_by_bytes_builder, radar_get_attacks_layer3_timeseries_by_bytes_task,
    radar_get_attacks_layer3_timeseries_group_by_bitrate_builder, radar_get_attacks_layer3_timeseries_group_by_bitrate_task,
    radar_get_attacks_layer3_timeseries_group_by_duration_builder, radar_get_attacks_layer3_timeseries_group_by_duration_task,
    radar_get_attacks_layer3_timeseries_group_by_industry_builder, radar_get_attacks_layer3_timeseries_group_by_industry_task,
    radar_get_attacks_layer3_timeseries_group_by_ip_version_builder, radar_get_attacks_layer3_timeseries_group_by_ip_version_task,
    radar_get_attacks_layer3_timeseries_group_by_protocol_builder, radar_get_attacks_layer3_timeseries_group_by_protocol_task,
    radar_get_attacks_layer3_timeseries_group_by_vector_builder, radar_get_attacks_layer3_timeseries_group_by_vector_task,
    radar_get_attacks_layer3_timeseries_group_by_vertical_builder, radar_get_attacks_layer3_timeseries_group_by_vertical_task,
    radar_get_attacks_layer3_timeseries_group_builder, radar_get_attacks_layer3_timeseries_group_task,
    radar_get_attacks_layer3_top_attacks_builder, radar_get_attacks_layer3_top_attacks_task,
    radar_get_attacks_layer3_top_industries_builder, radar_get_attacks_layer3_top_industries_task,
    radar_get_attacks_layer3_top_origin_locations_builder, radar_get_attacks_layer3_top_origin_locations_task,
    radar_get_attacks_layer3_top_target_locations_builder, radar_get_attacks_layer3_top_target_locations_task,
    radar_get_attacks_layer3_top_verticals_builder, radar_get_attacks_layer3_top_verticals_task,
    radar_get_attacks_layer7_summary_by_http_method_builder, radar_get_attacks_layer7_summary_by_http_method_task,
    radar_get_attacks_layer7_summary_by_http_version_builder, radar_get_attacks_layer7_summary_by_http_version_task,
    radar_get_attacks_layer7_summary_by_industry_builder, radar_get_attacks_layer7_summary_by_industry_task,
    radar_get_attacks_layer7_summary_by_ip_version_builder, radar_get_attacks_layer7_summary_by_ip_version_task,
    radar_get_attacks_layer7_summary_by_managed_rules_builder, radar_get_attacks_layer7_summary_by_managed_rules_task,
    radar_get_attacks_layer7_summary_by_mitigation_product_builder, radar_get_attacks_layer7_summary_by_mitigation_product_task,
    radar_get_attacks_layer7_summary_by_vertical_builder, radar_get_attacks_layer7_summary_by_vertical_task,
    radar_get_attacks_layer7_summary_builder, radar_get_attacks_layer7_summary_task,
    radar_get_attacks_layer7_timeseries_builder, radar_get_attacks_layer7_timeseries_task,
    radar_get_attacks_layer7_timeseries_group_by_http_method_builder, radar_get_attacks_layer7_timeseries_group_by_http_method_task,
    radar_get_attacks_layer7_timeseries_group_by_http_version_builder, radar_get_attacks_layer7_timeseries_group_by_http_version_task,
    radar_get_attacks_layer7_timeseries_group_by_industry_builder, radar_get_attacks_layer7_timeseries_group_by_industry_task,
    radar_get_attacks_layer7_timeseries_group_by_ip_version_builder, radar_get_attacks_layer7_timeseries_group_by_ip_version_task,
    radar_get_attacks_layer7_timeseries_group_by_managed_rules_builder, radar_get_attacks_layer7_timeseries_group_by_managed_rules_task,
    radar_get_attacks_layer7_timeseries_group_by_mitigation_product_builder, radar_get_attacks_layer7_timeseries_group_by_mitigation_product_task,
    radar_get_attacks_layer7_timeseries_group_by_vertical_builder, radar_get_attacks_layer7_timeseries_group_by_vertical_task,
    radar_get_attacks_layer7_timeseries_group_builder, radar_get_attacks_layer7_timeseries_group_task,
    radar_get_attacks_layer7_top_origin_as_builder, radar_get_attacks_layer7_top_origin_as_task,
    radar_get_attacks_layer7_top_attacks_builder, radar_get_attacks_layer7_top_attacks_task,
    radar_get_attacks_layer7_top_industries_builder, radar_get_attacks_layer7_top_industries_task,
    radar_get_attacks_layer7_top_origin_location_builder, radar_get_attacks_layer7_top_origin_location_task,
    radar_get_attacks_layer7_top_target_location_builder, radar_get_attacks_layer7_top_target_location_task,
    radar_get_attacks_layer7_top_verticals_builder, radar_get_attacks_layer7_top_verticals_task,
    radar_get_bgp_hijacks_events_builder, radar_get_bgp_hijacks_events_task,
    radar_get_bgp_ips_timeseries_builder, radar_get_bgp_ips_timeseries_task,
    radar_get_bgp_route_leak_events_builder, radar_get_bgp_route_leak_events_task,
    radar_get_bgp_routes_asns_builder, radar_get_bgp_routes_asns_task,
    radar_get_bgp_pfx2as_moas_builder, radar_get_bgp_pfx2as_moas_task,
    radar_get_bgp_pfx2as_builder, radar_get_bgp_pfx2as_task,
    radar_get_bgp_routes_realtime_builder, radar_get_bgp_routes_realtime_task,
    radar_get_bgp_routes_stats_builder, radar_get_bgp_routes_stats_task,
    radar_get_bgp_rpki_aspa_changes_builder, radar_get_bgp_rpki_aspa_changes_task,
    radar_get_bgp_rpki_aspa_snapshot_builder, radar_get_bgp_rpki_aspa_snapshot_task,
    radar_get_bgp_rpki_aspa_timeseries_builder, radar_get_bgp_rpki_aspa_timeseries_task,
    radar_get_bgp_timeseries_builder, radar_get_bgp_timeseries_task,
    radar_get_bgp_top_ases_builder, radar_get_bgp_top_ases_task,
    radar_get_bgp_top_asns_by_prefixes_builder, radar_get_bgp_top_asns_by_prefixes_task,
    radar_get_bgp_top_prefixes_builder, radar_get_bgp_top_prefixes_task,
    radar_get_bots_builder, radar_get_bots_task,
    radar_get_crawlers_summary_builder, radar_get_crawlers_summary_task,
    radar_get_crawlers_timeseries_group_builder, radar_get_crawlers_timeseries_group_task,
    radar_get_bots_summary_builder, radar_get_bots_summary_task,
    radar_get_bots_timeseries_builder, radar_get_bots_timeseries_task,
    radar_get_bots_timeseries_group_builder, radar_get_bots_timeseries_group_task,
    radar_get_bot_details_builder, radar_get_bot_details_task,
    radar_get_certificate_authorities_builder, radar_get_certificate_authorities_task,
    radar_get_certificate_authority_details_builder, radar_get_certificate_authority_details_task,
    radar_get_certificate_logs_builder, radar_get_certificate_logs_task,
    radar_get_certificate_log_details_builder, radar_get_certificate_log_details_task,
    radar_get_ct_summary_builder, radar_get_ct_summary_task,
    radar_get_ct_timeseries_builder, radar_get_ct_timeseries_task,
    radar_get_ct_timeseries_group_builder, radar_get_ct_timeseries_group_task,
    radar_get_reports_datasets_builder, radar_get_reports_datasets_task,
    radar_post_reports_dataset_download_url_builder, radar_post_reports_dataset_download_url_task,
    radar_get_reports_dataset_download_builder, radar_get_reports_dataset_download_task,
    radar_get_dns_summary_by_cache_hit_status_builder, radar_get_dns_summary_by_cache_hit_status_task,
    radar_get_dns_summary_by_dnssec_builder, radar_get_dns_summary_by_dnssec_task,
    radar_get_dns_summary_by_dnssec_awareness_builder, radar_get_dns_summary_by_dnssec_awareness_task,
    radar_get_dns_summary_by_dnssec_e2e_version_builder, radar_get_dns_summary_by_dnssec_e2e_version_task,
    radar_get_dns_summary_by_ip_version_builder, radar_get_dns_summary_by_ip_version_task,
    radar_get_dns_summary_by_matching_answer_status_builder, radar_get_dns_summary_by_matching_answer_status_task,
    radar_get_dns_summary_by_protocol_builder, radar_get_dns_summary_by_protocol_task,
    radar_get_dns_summary_by_query_type_builder, radar_get_dns_summary_by_query_type_task,
    radar_get_dns_summary_by_response_code_builder, radar_get_dns_summary_by_response_code_task,
    radar_get_dns_summary_by_response_ttl_builder, radar_get_dns_summary_by_response_ttl_task,
    radar_get_dns_summary_builder, radar_get_dns_summary_task,
    radar_get_dns_timeseries_builder, radar_get_dns_timeseries_task,
    radar_get_dns_timeseries_group_by_cache_hit_status_builder, radar_get_dns_timeseries_group_by_cache_hit_status_task,
    radar_get_dns_timeseries_group_by_dnssec_builder, radar_get_dns_timeseries_group_by_dnssec_task,
    radar_get_dns_timeseries_group_by_dnssec_awareness_builder, radar_get_dns_timeseries_group_by_dnssec_awareness_task,
    radar_get_dns_timeseries_group_by_dnssec_e2e_version_builder, radar_get_dns_timeseries_group_by_dnssec_e2e_version_task,
    radar_get_dns_timeseries_group_by_ip_version_builder, radar_get_dns_timeseries_group_by_ip_version_task,
    radar_get_dns_timeseries_group_by_matching_answer_status_builder, radar_get_dns_timeseries_group_by_matching_answer_status_task,
    radar_get_dns_timeseries_group_by_protocol_builder, radar_get_dns_timeseries_group_by_protocol_task,
    radar_get_dns_timeseries_group_by_query_type_builder, radar_get_dns_timeseries_group_by_query_type_task,
    radar_get_dns_timeseries_group_by_response_code_builder, radar_get_dns_timeseries_group_by_response_code_task,
    radar_get_dns_timeseries_group_by_response_ttl_builder, radar_get_dns_timeseries_group_by_response_ttl_task,
    radar_get_dns_timeseries_group_builder, radar_get_dns_timeseries_group_task,
    radar_get_dns_top_ases_builder, radar_get_dns_top_ases_task,
    radar_get_dns_top_locations_builder, radar_get_dns_top_locations_task,
    radar_get_email_routing_summary_by_arc_builder, radar_get_email_routing_summary_by_arc_task,
    radar_get_email_routing_summary_by_dkim_builder, radar_get_email_routing_summary_by_dkim_task,
    radar_get_email_routing_summary_by_dmarc_builder, radar_get_email_routing_summary_by_dmarc_task,
    radar_get_email_routing_summary_by_encrypted_builder, radar_get_email_routing_summary_by_encrypted_task,
    radar_get_email_routing_summary_by_ip_version_builder, radar_get_email_routing_summary_by_ip_version_task,
    radar_get_email_routing_summary_by_spf_builder, radar_get_email_routing_summary_by_spf_task,
    radar_get_email_routing_summary_builder, radar_get_email_routing_summary_task,
    radar_get_email_routing_timeseries_group_by_arc_builder, radar_get_email_routing_timeseries_group_by_arc_task,
    radar_get_email_routing_timeseries_group_by_dkim_builder, radar_get_email_routing_timeseries_group_by_dkim_task,
    radar_get_email_routing_timeseries_group_by_dmarc_builder, radar_get_email_routing_timeseries_group_by_dmarc_task,
    radar_get_email_routing_timeseries_group_by_encrypted_builder, radar_get_email_routing_timeseries_group_by_encrypted_task,
    radar_get_email_routing_timeseries_group_by_ip_version_builder, radar_get_email_routing_timeseries_group_by_ip_version_task,
    radar_get_email_routing_timeseries_group_by_spf_builder, radar_get_email_routing_timeseries_group_by_spf_task,
    radar_get_email_routing_timeseries_group_builder, radar_get_email_routing_timeseries_group_task,
    radar_get_email_security_summary_by_arc_builder, radar_get_email_security_summary_by_arc_task,
    radar_get_email_security_summary_by_dkim_builder, radar_get_email_security_summary_by_dkim_task,
    radar_get_email_security_summary_by_dmarc_builder, radar_get_email_security_summary_by_dmarc_task,
    radar_get_email_security_summary_by_malicious_builder, radar_get_email_security_summary_by_malicious_task,
    radar_get_email_security_summary_by_spam_builder, radar_get_email_security_summary_by_spam_task,
    radar_get_email_security_summary_by_spf_builder, radar_get_email_security_summary_by_spf_task,
    radar_get_email_security_summary_by_spoof_builder, radar_get_email_security_summary_by_spoof_task,
    radar_get_email_security_summary_by_threat_category_builder, radar_get_email_security_summary_by_threat_category_task,
    radar_get_email_security_summary_by_tls_version_builder, radar_get_email_security_summary_by_tls_version_task,
    radar_get_email_security_summary_builder, radar_get_email_security_summary_task,
    radar_get_email_security_timeseries_group_by_arc_builder, radar_get_email_security_timeseries_group_by_arc_task,
    radar_get_email_security_timeseries_group_by_dkim_builder, radar_get_email_security_timeseries_group_by_dkim_task,
    radar_get_email_security_timeseries_group_by_dmarc_builder, radar_get_email_security_timeseries_group_by_dmarc_task,
    radar_get_email_security_timeseries_group_by_malicious_builder, radar_get_email_security_timeseries_group_by_malicious_task,
    radar_get_email_security_timeseries_group_by_spam_builder, radar_get_email_security_timeseries_group_by_spam_task,
    radar_get_email_security_timeseries_group_by_spf_builder, radar_get_email_security_timeseries_group_by_spf_task,
    radar_get_email_security_timeseries_group_by_spoof_builder, radar_get_email_security_timeseries_group_by_spoof_task,
    radar_get_email_security_timeseries_group_by_threat_category_builder, radar_get_email_security_timeseries_group_by_threat_category_task,
    radar_get_email_security_timeseries_group_by_tls_version_builder, radar_get_email_security_timeseries_group_by_tls_version_task,
    radar_get_email_security_timeseries_group_builder, radar_get_email_security_timeseries_group_task,
    radar_get_email_security_top_tlds_by_messages_builder, radar_get_email_security_top_tlds_by_messages_task,
    radar_get_email_security_top_tlds_by_malicious_builder, radar_get_email_security_top_tlds_by_malicious_task,
    radar_get_email_security_top_tlds_by_spam_builder, radar_get_email_security_top_tlds_by_spam_task,
    radar_get_email_security_top_tlds_by_spoof_builder, radar_get_email_security_top_tlds_by_spoof_task,
    radar_get_entities_asn_list_builder, radar_get_entities_asn_list_task,
    radar_get_as_botnet_threat_feed_builder, radar_get_as_botnet_threat_feed_task,
    radar_get_entities_asn_by_ip_builder, radar_get_entities_asn_by_ip_task,
    radar_get_entities_asn_by_id_builder, radar_get_entities_asn_by_id_task,
    radar_get_asns_as_set_builder, radar_get_asns_as_set_task,
    radar_get_asns_rel_builder, radar_get_asns_rel_task,
    radar_get_entities_ip_builder, radar_get_entities_ip_task,
    radar_get_entities_locations_builder, radar_get_entities_locations_task,
    radar_get_entities_location_by_alpha2_builder, radar_get_entities_location_by_alpha2_task,
    radar_get_geolocations_builder, radar_get_geolocations_task,
    radar_get_geolocation_details_builder, radar_get_geolocation_details_task,
    radar_get_http_summary_by_bot_class_builder, radar_get_http_summary_by_bot_class_task,
    radar_get_http_summary_by_device_type_builder, radar_get_http_summary_by_device_type_task,
    radar_get_http_summary_by_http_protocol_builder, radar_get_http_summary_by_http_protocol_task,
    radar_get_http_summary_by_http_version_builder, radar_get_http_summary_by_http_version_task,
    radar_get_http_summary_by_ip_version_builder, radar_get_http_summary_by_ip_version_task,
    radar_get_http_summary_by_operating_system_builder, radar_get_http_summary_by_operating_system_task,
    radar_get_http_summary_by_post_quantum_builder, radar_get_http_summary_by_post_quantum_task,
    radar_get_http_summary_by_tls_version_builder, radar_get_http_summary_by_tls_version_task,
    radar_get_http_summary_builder, radar_get_http_summary_task,
    radar_get_http_timeseries_builder, radar_get_http_timeseries_task,
    radar_get_http_timeseries_group_by_bot_class_builder, radar_get_http_timeseries_group_by_bot_class_task,
    radar_get_http_timeseries_group_by_browsers_builder, radar_get_http_timeseries_group_by_browsers_task,
    radar_get_http_timeseries_group_by_browser_families_builder, radar_get_http_timeseries_group_by_browser_families_task,
    radar_get_http_timeseries_group_by_device_type_builder, radar_get_http_timeseries_group_by_device_type_task,
    radar_get_http_timeseries_group_by_http_protocol_builder, radar_get_http_timeseries_group_by_http_protocol_task,
    radar_get_http_timeseries_group_by_http_version_builder, radar_get_http_timeseries_group_by_http_version_task,
    radar_get_http_timeseries_group_by_ip_version_builder, radar_get_http_timeseries_group_by_ip_version_task,
    radar_get_http_timeseries_group_by_operating_system_builder, radar_get_http_timeseries_group_by_operating_system_task,
    radar_get_http_timeseries_group_by_post_quantum_builder, radar_get_http_timeseries_group_by_post_quantum_task,
    radar_get_http_timeseries_group_by_tls_version_builder, radar_get_http_timeseries_group_by_tls_version_task,
    radar_get_http_timeseries_group_builder, radar_get_http_timeseries_group_task,
    radar_get_http_top_ases_by_http_requests_builder, radar_get_http_top_ases_by_http_requests_task,
    radar_get_http_top_ases_by_bot_class_builder, radar_get_http_top_ases_by_bot_class_task,
    radar_get_http_top_ases_by_browser_family_builder, radar_get_http_top_ases_by_browser_family_task,
    radar_get_http_top_ases_by_device_type_builder, radar_get_http_top_ases_by_device_type_task,
    radar_get_http_top_ases_by_http_protocol_builder, radar_get_http_top_ases_by_http_protocol_task,
    radar_get_http_top_ases_by_http_version_builder, radar_get_http_top_ases_by_http_version_task,
    radar_get_http_top_ases_by_ip_version_builder, radar_get_http_top_ases_by_ip_version_task,
    radar_get_http_top_ases_by_operating_system_builder, radar_get_http_top_ases_by_operating_system_task,
    radar_get_http_top_ases_by_tls_version_builder, radar_get_http_top_ases_by_tls_version_task,
    radar_get_http_top_browsers_builder, radar_get_http_top_browsers_task,
    radar_get_http_top_browser_families_builder, radar_get_http_top_browser_families_task,
    radar_get_http_top_locations_by_http_requests_builder, radar_get_http_top_locations_by_http_requests_task,
    radar_get_http_top_locations_by_bot_class_builder, radar_get_http_top_locations_by_bot_class_task,
    radar_get_http_top_locations_by_browser_family_builder, radar_get_http_top_locations_by_browser_family_task,
    radar_get_http_top_locations_by_device_type_builder, radar_get_http_top_locations_by_device_type_task,
    radar_get_http_top_locations_by_http_protocol_builder, radar_get_http_top_locations_by_http_protocol_task,
    radar_get_http_top_locations_by_http_version_builder, radar_get_http_top_locations_by_http_version_task,
    radar_get_http_top_locations_by_ip_version_builder, radar_get_http_top_locations_by_ip_version_task,
    radar_get_http_top_locations_by_operating_system_builder, radar_get_http_top_locations_by_operating_system_task,
    radar_get_http_top_locations_by_tls_version_builder, radar_get_http_top_locations_by_tls_version_task,
    radar_get_leaked_credential_checks_summary_by_bot_class_builder, radar_get_leaked_credential_checks_summary_by_bot_class_task,
    radar_get_leaked_credential_checks_summary_by_compromised_builder, radar_get_leaked_credential_checks_summary_by_compromised_task,
    radar_get_leaked_credential_checks_summary_builder, radar_get_leaked_credential_checks_summary_task,
    radar_get_leaked_credential_checks_timeseries_group_by_bot_class_builder, radar_get_leaked_credential_checks_timeseries_group_by_bot_class_task,
    radar_get_leaked_credential_checks_timeseries_group_by_compromised_builder, radar_get_leaked_credential_checks_timeseries_group_by_compromised_task,
    radar_get_leaked_credential_checks_timeseries_group_builder, radar_get_leaked_credential_checks_timeseries_group_task,
    radar_get_netflows_summary_deprecated_builder, radar_get_netflows_summary_deprecated_task,
    radar_get_netflows_summary_builder, radar_get_netflows_summary_task,
    radar_get_netflows_timeseries_builder, radar_get_netflows_timeseries_task,
    radar_get_netflows_timeseries_group_builder, radar_get_netflows_timeseries_group_task,
    radar_get_netflows_top_ases_builder, radar_get_netflows_top_ases_task,
    radar_get_netflows_top_locations_builder, radar_get_netflows_top_locations_task,
    radar_get_origins_builder, radar_get_origins_task,
    radar_get_origins_summary_builder, radar_get_origins_summary_task,
    radar_get_origins_timeseries_builder, radar_get_origins_timeseries_task,
    radar_get_origins_timeseries_group_builder, radar_get_origins_timeseries_group_task,
    radar_get_origin_details_builder, radar_get_origin_details_task,
    radar_get_origin_post_quantum_summary_builder, radar_get_origin_post_quantum_summary_task,
    radar_get_origin_post_quantum_timeseries_groups_builder, radar_get_origin_post_quantum_timeseries_groups_task,
    radar_get_post_quantum_tls_support_builder, radar_get_post_quantum_tls_support_task,
    radar_get_quality_index_summary_builder, radar_get_quality_index_summary_task,
    radar_get_quality_index_timeseries_group_builder, radar_get_quality_index_timeseries_group_task,
    radar_get_quality_speed_histogram_builder, radar_get_quality_speed_histogram_task,
    radar_get_quality_speed_summary_builder, radar_get_quality_speed_summary_task,
    radar_get_quality_speed_top_ases_builder, radar_get_quality_speed_top_ases_task,
    radar_get_quality_speed_top_locations_builder, radar_get_quality_speed_top_locations_task,
    radar_get_ranking_domain_details_builder, radar_get_ranking_domain_details_task,
    radar_get_ranking_internet_services_categories_builder, radar_get_ranking_internet_services_categories_task,
    radar_get_ranking_internet_services_timeseries_builder, radar_get_ranking_internet_services_timeseries_task,
    radar_get_ranking_top_internet_services_builder, radar_get_ranking_top_internet_services_task,
    radar_get_ranking_domain_timeseries_builder, radar_get_ranking_domain_timeseries_task,
    radar_get_ranking_top_domains_builder, radar_get_ranking_top_domains_task,
    radar_get_robots_txt_top_domain_categories_by_files_parsed_builder, radar_get_robots_txt_top_domain_categories_by_files_parsed_task,
    radar_get_robots_txt_top_user_agents_by_directive_builder, radar_get_robots_txt_top_user_agents_by_directive_task,
    radar_get_search_global_builder, radar_get_search_global_task,
    radar_get_tcp_resets_timeouts_summary_builder, radar_get_tcp_resets_timeouts_summary_task,
    radar_get_tcp_resets_timeouts_timeseries_group_builder, radar_get_tcp_resets_timeouts_timeseries_group_task,
    radar_get_tlds_builder, radar_get_tlds_task,
    radar_get_tld_details_builder, radar_get_tld_details_task,
    radar_get_traffic_anomalies_builder, radar_get_traffic_anomalies_task,
    radar_get_traffic_anomalies_top_builder, radar_get_traffic_anomalies_top_task,
    radar_get_verified_bots_top_by_http_requests_builder, radar_get_verified_bots_top_by_http_requests_task,
    radar_get_verified_bots_top_categories_by_http_requests_builder, radar_get_verified_bots_top_categories_by_http_requests_task,
    secrets_store_system_list_builder, secrets_store_system_list_task,
    secrets_store_system_create_builder, secrets_store_system_create_task,
    secrets_store_system_get_store_by_id_builder, secrets_store_system_get_store_by_id_task,
    secrets_store_system_delete_by_id_builder, secrets_store_system_delete_by_id_task,
    secrets_store_system_secrets_list_builder, secrets_store_system_secrets_list_task,
    secrets_store_system_secret_create_builder, secrets_store_system_secret_create_task,
    secrets_store_system_delete_bulk_builder, secrets_store_system_delete_bulk_task,
    secrets_store_system_get_by_id_builder, secrets_store_system_get_by_id_task,
    secrets_store_system_patch_by_id_builder, secrets_store_system_patch_by_id_task,
    secrets_store_system_secret_delete_by_id_builder, secrets_store_system_secret_delete_by_id_task,
    secrets_store_system_duplicate_by_id_builder, secrets_store_system_duplicate_by_id_task,
    tenants_retrieve_tenant_builder, tenants_retrieve_tenant_task,
    tenants_valid_account_types_builder, tenants_valid_account_types_task,
    tenants_list_accounts_builder, tenants_list_accounts_task,
    tenants_list_entitlements_builder, tenants_list_entitlements_task,
    tenants_list_memberships_builder, tenants_list_memberships_task,
    user_user_details_builder, user_user_details_task,
    user_edit_user_builder, user_edit_user_task,
    audit_logs_get_user_audit_logs_builder, audit_logs_get_user_audit_logs_task,
    user_billing_history_deprecated_billing_history_details_builder, user_billing_history_deprecated_billing_history_details_task,
    user_billing_profile_deprecated_billing_profile_details_builder, user_billing_profile_deprecated_billing_profile_details_task,
    ip_access_rules_for_a_user_list_ip_access_rules_builder, ip_access_rules_for_a_user_list_ip_access_rules_task,
    ip_access_rules_for_a_user_create_an_ip_access_rule_builder, ip_access_rules_for_a_user_create_an_ip_access_rule_task,
    ip_access_rules_for_a_user_update_an_ip_access_rule_builder, ip_access_rules_for_a_user_update_an_ip_access_rule_task,
    ip_access_rules_for_a_user_delete_an_ip_access_rule_builder, ip_access_rules_for_a_user_delete_an_ip_access_rule_task,
    user_s_invites_list_invitations_builder, user_s_invites_list_invitations_task,
    user_s_invites_invitation_details_builder, user_s_invites_invitation_details_task,
    user_s_invites_respond_to_invitation_builder, user_s_invites_respond_to_invitation_task,
    load_balancer_monitors_list_monitors_builder, load_balancer_monitors_list_monitors_task,
    load_balancer_monitors_create_monitor_builder, load_balancer_monitors_create_monitor_task,
    load_balancer_monitors_monitor_details_builder, load_balancer_monitors_monitor_details_task,
    load_balancer_monitors_update_monitor_builder, load_balancer_monitors_update_monitor_task,
    load_balancer_monitors_patch_monitor_builder, load_balancer_monitors_patch_monitor_task,
    load_balancer_monitors_delete_monitor_builder, load_balancer_monitors_delete_monitor_task,
    load_balancer_monitors_preview_monitor_builder, load_balancer_monitors_preview_monitor_task,
    load_balancer_monitors_list_monitor_references_builder, load_balancer_monitors_list_monitor_references_task,
    load_balancer_pools_list_pools_builder, load_balancer_pools_list_pools_task,
    load_balancer_pools_create_pool_builder, load_balancer_pools_create_pool_task,
    load_balancer_pools_patch_pools_builder, load_balancer_pools_patch_pools_task,
    load_balancer_pools_pool_details_builder, load_balancer_pools_pool_details_task,
    load_balancer_pools_update_pool_builder, load_balancer_pools_update_pool_task,
    load_balancer_pools_patch_pool_builder, load_balancer_pools_patch_pool_task,
    load_balancer_pools_delete_pool_builder, load_balancer_pools_delete_pool_task,
    load_balancer_pools_pool_health_details_builder, load_balancer_pools_pool_health_details_task,
    load_balancer_pools_preview_pool_builder, load_balancer_pools_preview_pool_task,
    load_balancer_pools_list_pool_references_builder, load_balancer_pools_list_pool_references_task,
    load_balancer_monitors_preview_result_builder, load_balancer_monitors_preview_result_task,
    load_balancer_healthcheck_events_list_healthcheck_events_builder, load_balancer_healthcheck_events_list_healthcheck_events_task,
    user_s_organizations_list_organizations_builder, user_s_organizations_list_organizations_task,
    user_s_organizations_organization_details_builder, user_s_organizations_organization_details_task,
    user_s_organizations_leave_organization_builder, user_s_organizations_leave_organization_task,
    user_subscription_get_user_subscriptions_builder, user_subscription_get_user_subscriptions_task,
    user_subscription_update_user_subscription_builder, user_subscription_update_user_subscription_task,
    user_subscription_delete_user_subscription_builder, user_subscription_delete_user_subscription_task,
    user_api_tokens_list_tokens_builder, user_api_tokens_list_tokens_task,
    user_api_tokens_create_token_builder, user_api_tokens_create_token_task,
    permission_groups_list_permission_groups_builder, permission_groups_list_permission_groups_task,
    user_api_tokens_verify_token_builder, user_api_tokens_verify_token_task,
    user_api_tokens_token_details_builder, user_api_tokens_token_details_task,
    user_api_tokens_update_token_builder, user_api_tokens_update_token_task,
    user_api_tokens_delete_token_builder, user_api_tokens_delete_token_task,
    user_api_tokens_roll_token_builder, user_api_tokens_roll_token_task,
    user_list_user_tenants_builder, user_list_user_tenants_task,
    zones_get_builder, zones_get_task,
    zones_post_builder, zones_post_task,
    zone_analytics_deprecated_get_analytics_by_co_locations_builder, zone_analytics_deprecated_get_analytics_by_co_locations_task,
    zone_analytics_deprecated_get_dashboard_builder, zone_analytics_deprecated_get_dashboard_task,
    custom_pages_for_a_zone_list_custom_pages_builder, custom_pages_for_a_zone_list_custom_pages_task,
    custom_assets_for_a_zone_list_custom_assets_builder, custom_assets_for_a_zone_list_custom_assets_task,
    custom_assets_for_a_zone_create_a_custom_asset_builder, custom_assets_for_a_zone_create_a_custom_asset_task,
    custom_assets_for_a_zone_get_a_custom_asset_builder, custom_assets_for_a_zone_get_a_custom_asset_task,
    custom_assets_for_a_zone_update_a_custom_asset_builder, custom_assets_for_a_zone_update_a_custom_asset_task,
    custom_assets_for_a_zone_delete_a_custom_asset_builder, custom_assets_for_a_zone_delete_a_custom_asset_task,
    custom_pages_for_a_zone_get_a_custom_page_builder, custom_pages_for_a_zone_get_a_custom_page_task,
    custom_pages_for_a_zone_update_a_custom_page_builder, custom_pages_for_a_zone_update_a_custom_page_task,
    zones_0_get_builder, zones_0_get_task,
    zones_0_patch_builder, zones_0_patch_task,
    zones_0_delete_builder, zones_0_delete_task,
    zone_level_access_applications_list_access_applications_builder, zone_level_access_applications_list_access_applications_task,
    zone_level_access_applications_add_a_bookmark_application_builder, zone_level_access_applications_add_a_bookmark_application_task,
    zone_level_access_short_lived_certificate_c_as_list_short_lived_certificate_c_as_builder, zone_level_access_short_lived_certificate_c_as_list_short_lived_certificate_c_as_task,
    zone_level_access_applications_get_an_access_application_builder, zone_level_access_applications_get_an_access_application_task,
    zone_level_access_applications_update_a_bookmark_application_builder, zone_level_access_applications_update_a_bookmark_application_task,
    zone_level_access_applications_delete_an_access_application_builder, zone_level_access_applications_delete_an_access_application_task,
    zone_level_access_short_lived_certificate_c_as_get_a_short_lived_certificate_ca_builder, zone_level_access_short_lived_certificate_c_as_get_a_short_lived_certificate_ca_task,
    zone_level_access_short_lived_certificate_c_as_create_a_short_lived_certificate_ca_builder, zone_level_access_short_lived_certificate_c_as_create_a_short_lived_certificate_ca_task,
    zone_level_access_short_lived_certificate_c_as_delete_a_short_lived_certificate_ca_builder, zone_level_access_short_lived_certificate_c_as_delete_a_short_lived_certificate_ca_task,
    zone_level_access_policies_list_access_policies_builder, zone_level_access_policies_list_access_policies_task,
    zone_level_access_policies_create_an_access_policy_builder, zone_level_access_policies_create_an_access_policy_task,
    zone_level_access_policies_get_an_access_policy_builder, zone_level_access_policies_get_an_access_policy_task,
    zone_level_access_policies_update_an_access_policy_builder, zone_level_access_policies_update_an_access_policy_task,
    zone_level_access_policies_delete_an_access_policy_builder, zone_level_access_policies_delete_an_access_policy_task,
    zone_level_access_applications_revoke_service_tokens_builder, zone_level_access_applications_revoke_service_tokens_task,
    zone_level_access_applications_put_update_access_application_settings_builder, zone_level_access_applications_put_update_access_application_settings_task,
    zone_level_access_applications_patch_update_access_application_settings_builder, zone_level_access_applications_patch_update_access_application_settings_task,
    zone_level_access_applications_test_access_policies_builder, zone_level_access_applications_test_access_policies_task,
    zone_level_access_mtls_authentication_list_mtls_certificates_builder, zone_level_access_mtls_authentication_list_mtls_certificates_task,
    zone_level_access_mtls_authentication_add_an_mtls_certificate_builder, zone_level_access_mtls_authentication_add_an_mtls_certificate_task,
    zone_level_access_mtls_authentication_list_mtls_certificates_hostname_settings_builder, zone_level_access_mtls_authentication_list_mtls_certificates_hostname_settings_task,
    zone_level_access_mtls_authentication_update_an_mtls_certificate_settings_builder, zone_level_access_mtls_authentication_update_an_mtls_certificate_settings_task,
    zone_level_access_mtls_authentication_get_an_mtls_certificate_builder, zone_level_access_mtls_authentication_get_an_mtls_certificate_task,
    zone_level_access_mtls_authentication_update_an_mtls_certificate_builder, zone_level_access_mtls_authentication_update_an_mtls_certificate_task,
    zone_level_access_mtls_authentication_delete_an_mtls_certificate_builder, zone_level_access_mtls_authentication_delete_an_mtls_certificate_task,
    zone_level_access_groups_list_access_groups_builder, zone_level_access_groups_list_access_groups_task,
    zone_level_access_groups_create_an_access_group_builder, zone_level_access_groups_create_an_access_group_task,
    zone_level_access_groups_get_an_access_group_builder, zone_level_access_groups_get_an_access_group_task,
    zone_level_access_groups_update_an_access_group_builder, zone_level_access_groups_update_an_access_group_task,
    zone_level_access_groups_delete_an_access_group_builder, zone_level_access_groups_delete_an_access_group_task,
    zone_level_access_identity_providers_list_access_identity_providers_builder, zone_level_access_identity_providers_list_access_identity_providers_task,
    zone_level_access_identity_providers_add_an_access_identity_provider_builder, zone_level_access_identity_providers_add_an_access_identity_provider_task,
    zone_level_access_identity_providers_get_an_access_identity_provider_builder, zone_level_access_identity_providers_get_an_access_identity_provider_task,
    zone_level_access_identity_providers_update_an_access_identity_provider_builder, zone_level_access_identity_providers_update_an_access_identity_provider_task,
    zone_level_access_identity_providers_delete_an_access_identity_provider_builder, zone_level_access_identity_providers_delete_an_access_identity_provider_task,
    zone_level_zero_trust_organization_get_your_zero_trust_organization_builder, zone_level_zero_trust_organization_get_your_zero_trust_organization_task,
    zone_level_zero_trust_organization_create_your_zero_trust_organization_builder, zone_level_zero_trust_organization_create_your_zero_trust_organization_task,
    zone_level_zero_trust_organization_update_your_zero_trust_organization_builder, zone_level_zero_trust_organization_update_your_zero_trust_organization_task,
    zone_level_zero_trust_organization_revoke_all_access_tokens_for_a_user_builder, zone_level_zero_trust_organization_revoke_all_access_tokens_for_a_user_task,
    zone_level_access_service_tokens_list_service_tokens_builder, zone_level_access_service_tokens_list_service_tokens_task,
    zone_level_access_service_tokens_create_a_service_token_builder, zone_level_access_service_tokens_create_a_service_token_task,
    zone_level_access_service_tokens_get_a_service_token_builder, zone_level_access_service_tokens_get_a_service_token_task,
    zone_level_access_service_tokens_update_a_service_token_builder, zone_level_access_service_tokens_update_a_service_token_task,
    zone_level_access_service_tokens_delete_a_service_token_builder, zone_level_access_service_tokens_delete_a_service_token_task,
    custom_origin_trust_store_list_details_builder, custom_origin_trust_store_list_details_task,
    custom_origin_trust_store_create_builder, custom_origin_trust_store_create_task,
    custom_origin_trust_store_details_builder, custom_origin_trust_store_details_task,
    custom_origin_trust_store_delete_builder, custom_origin_trust_store_delete_task,
    total_tls_total_tls_settings_details_builder, total_tls_total_tls_settings_details_task,
    total_tls_enable_or_disable_total_tls_builder, total_tls_enable_or_disable_total_tls_task,
    put_zones_zone_id_activation_check_builder, put_zones_zone_id_activation_check_task,
    dls_account_regional_hostnames_account_list_hostnames_builder, dls_account_regional_hostnames_account_list_hostnames_task,
    dls_account_regional_hostnames_account_create_hostname_builder, dls_account_regional_hostnames_account_create_hostname_task,
    dls_account_regional_hostnames_account_fetch_hostname_builder, dls_account_regional_hostnames_account_fetch_hostname_task,
    dls_account_regional_hostnames_account_patch_hostname_builder, dls_account_regional_hostnames_account_patch_hostname_task,
    dls_account_regional_hostnames_account_delete_hostname_builder, dls_account_regional_hostnames_account_delete_hostname_task,
    argo_analytics_for_zone_argo_analytics_for_a_zone_builder, argo_analytics_for_zone_argo_analytics_for_a_zone_task,
    argo_analytics_for_geolocation_argo_analytics_for_a_zone_at_different_po_ps_builder, argo_analytics_for_geolocation_argo_analytics_for_a_zone_at_different_po_ps_task,
    api_shield_settings_retrieve_information_about_specific_configuration_properties_builder, api_shield_settings_retrieve_information_about_specific_configuration_properties_task,
    api_shield_settings_set_configuration_properties_builder, api_shield_settings_set_configuration_properties_task,
    api_shield_api_discovery_retrieve_discovered_operations_on_a_zone_as_openapi_builder, api_shield_api_discovery_retrieve_discovered_operations_on_a_zone_as_openapi_task,
    api_shield_api_discovery_retrieve_discovered_operations_on_a_zone_builder, api_shield_api_discovery_retrieve_discovered_operations_on_a_zone_task,
    api_shield_api_patch_discovered_operations_builder, api_shield_api_patch_discovered_operations_task,
    api_shield_api_patch_discovered_operation_builder, api_shield_api_patch_discovered_operation_task,
    api_shield_expression_templates_fallthrough_builder, api_shield_expression_templates_fallthrough_task,
    api_shield_labels_get_labels_builder, api_shield_labels_get_labels_task,
    api_shield_labels_get_managed_label_builder, api_shield_labels_get_managed_label_task,
    api_shield_labels_replace_operations_attached_to_managed_label_builder, api_shield_labels_replace_operations_attached_to_managed_label_task,
    api_shield_labels_create_user_labels_builder, api_shield_labels_create_user_labels_task,
    api_shield_labels_delete_user_labels_builder, api_shield_labels_delete_user_labels_task,
    api_shield_labels_get_user_label_builder, api_shield_labels_get_user_label_task,
    api_shield_put_user_label_builder, api_shield_put_user_label_task,
    api_shield_patch_user_label_builder, api_shield_patch_user_label_task,
    api_shield_delete_user_label_builder, api_shield_delete_user_label_task,
    api_shield_labels_replace_operations_attached_to_user_label_builder, api_shield_labels_replace_operations_attached_to_user_label_task,
    api_shield_endpoint_management_retrieve_information_about_all_operations_on_a_zone_builder, api_shield_endpoint_management_retrieve_information_about_all_operations_on_a_zone_task,
    api_shield_endpoint_management_add_operations_to_a_zone_builder, api_shield_endpoint_management_add_operations_to_a_zone_task,
    api_shield_endpoint_management_delete_multiple_operations_builder, api_shield_endpoint_management_delete_multiple_operations_task,
    api_shield_endpoint_management_add_operation_to_a_zone_builder, api_shield_endpoint_management_add_operation_to_a_zone_task,
    api_shield_operations_bulk_post_labels_to_operations_builder, api_shield_operations_bulk_post_labels_to_operations_task,
    api_shield_operations_bulk_put_labels_to_operations_builder, api_shield_operations_bulk_put_labels_to_operations_task,
    api_shield_operations_bulk_delete_labels_to_operations_builder, api_shield_operations_bulk_delete_labels_to_operations_task,
    api_shield_schema_validation_update_multiple_operation_level_settings_builder, api_shield_schema_validation_update_multiple_operation_level_settings_task,
    api_shield_endpoint_management_retrieve_information_about_an_operation_builder, api_shield_endpoint_management_retrieve_information_about_an_operation_task,
    api_shield_endpoint_management_delete_an_operation_builder, api_shield_endpoint_management_delete_an_operation_task,
    api_shield_operations_post_labels_to_operation_builder, api_shield_operations_post_labels_to_operation_task,
    api_shield_operations_put_labels_to_operation_builder, api_shield_operations_put_labels_to_operation_task,
    api_shield_operations_delete_labels_from_operation_builder, api_shield_operations_delete_labels_from_operation_task,
    api_shield_schema_validation_retrieve_operation_level_settings_builder, api_shield_schema_validation_retrieve_operation_level_settings_task,
    api_shield_schema_validation_update_operation_level_settings_builder, api_shield_schema_validation_update_operation_level_settings_task,
    api_shield_endpoint_management_retrieve_operations_and_features_as_open_api_schemas_builder, api_shield_endpoint_management_retrieve_operations_and_features_as_open_api_schemas_task,
    api_shield_schema_validation_retrieve_zone_level_settings_builder, api_shield_schema_validation_retrieve_zone_level_settings_task,
    api_shield_schema_validation_update_zone_level_settings_builder, api_shield_schema_validation_update_zone_level_settings_task,
    api_shield_schema_validation_patch_zone_level_settings_builder, api_shield_schema_validation_patch_zone_level_settings_task,
    api_shield_schema_validation_retrieve_information_about_all_schemas_builder, api_shield_schema_validation_retrieve_information_about_all_schemas_task,
    api_shield_schema_validation_post_schema_builder, api_shield_schema_validation_post_schema_task,
    api_shield_schema_validation_retrieve_user_schema_hosts_builder, api_shield_schema_validation_retrieve_user_schema_hosts_task,
    api_shield_schema_validation_retrieve_information_about_specific_schema_builder, api_shield_schema_validation_retrieve_information_about_specific_schema_task,
    api_shield_schema_validation_enable_validation_for_a_schema_builder, api_shield_schema_validation_enable_validation_for_a_schema_task,
    api_shield_schema_delete_a_schema_builder, api_shield_schema_delete_a_schema_task,
    api_shield_schema_validation_extract_operations_from_schema_builder, api_shield_schema_validation_extract_operations_from_schema_task,
    argo_smart_routing_get_argo_smart_routing_setting_builder, argo_smart_routing_get_argo_smart_routing_setting_task,
    argo_smart_routing_patch_argo_smart_routing_setting_builder, argo_smart_routing_patch_argo_smart_routing_setting_task,
    tiered_caching_get_tiered_caching_setting_builder, tiered_caching_get_tiered_caching_setting_task,
    tiered_caching_patch_tiered_caching_setting_builder, tiered_caching_patch_tiered_caching_setting_task,
    zone_rate_plan_list_available_plans_builder, zone_rate_plan_list_available_plans_task,
    zone_rate_plan_available_plan_details_builder, zone_rate_plan_available_plan_details_task,
    zone_rate_plan_list_available_rate_plans_builder, zone_rate_plan_list_available_rate_plans_task,
    bot_management_for_a_zone_get_config_builder, bot_management_for_a_zone_get_config_task,
    bot_management_for_a_zone_update_config_builder, bot_management_for_a_zone_update_config_task,
    bot_management_zone_feedback_list_builder, bot_management_zone_feedback_list_task,
    bot_management_zone_feedback_create_builder, bot_management_zone_feedback_create_task,
    zone_cache_settings_get_cache_reserve_setting_builder, zone_cache_settings_get_cache_reserve_setting_task,
    zone_cache_settings_change_cache_reserve_setting_builder, zone_cache_settings_change_cache_reserve_setting_task,
    zone_cache_settings_get_cache_reserve_clear_builder, zone_cache_settings_get_cache_reserve_clear_task,
    zone_cache_settings_start_cache_reserve_clear_builder, zone_cache_settings_start_cache_reserve_clear_task,
    zone_cache_settings_get_origin_post_quantum_encryption_setting_builder, zone_cache_settings_get_origin_post_quantum_encryption_setting_task,
    zone_cache_settings_change_origin_post_quantum_encryption_setting_builder, zone_cache_settings_change_origin_post_quantum_encryption_setting_task,
    zone_cache_settings_get_regional_tiered_cache_setting_builder, zone_cache_settings_get_regional_tiered_cache_setting_task,
    zone_cache_settings_change_regional_tiered_cache_setting_builder, zone_cache_settings_change_regional_tiered_cache_setting_task,
    smart_tiered_cache_get_smart_tiered_cache_setting_builder, smart_tiered_cache_get_smart_tiered_cache_setting_task,
    smart_tiered_cache_patch_smart_tiered_cache_setting_builder, smart_tiered_cache_patch_smart_tiered_cache_setting_task,
    smart_tiered_cache_delete_smart_tiered_cache_setting_builder, smart_tiered_cache_delete_smart_tiered_cache_setting_task,
    zone_cache_settings_get_variants_setting_builder, zone_cache_settings_get_variants_setting_task,
    zone_cache_settings_change_variants_setting_builder, zone_cache_settings_change_variants_setting_task,
    zone_cache_settings_delete_variants_setting_builder, zone_cache_settings_delete_variants_setting_task,
    client_certificate_for_a_zone_list_hostname_associations_builder, client_certificate_for_a_zone_list_hostname_associations_task,
    client_certificate_for_a_zone_put_hostname_associations_builder, client_certificate_for_a_zone_put_hostname_associations_task,
    client_certificate_for_a_zone_list_client_certificates_builder, client_certificate_for_a_zone_list_client_certificates_task,
    client_certificate_for_a_zone_create_client_certificate_builder, client_certificate_for_a_zone_create_client_certificate_task,
    client_certificate_for_a_zone_client_certificate_details_builder, client_certificate_for_a_zone_client_certificate_details_task,
    client_certificate_for_a_zone_edit_client_certificate_builder, client_certificate_for_a_zone_edit_client_certificate_task,
    client_certificate_for_a_zone_delete_client_certificate_builder, client_certificate_for_a_zone_delete_client_certificate_task,
    zone_cloud_connector_rules_builder, zone_cloud_connector_rules_task,
    zone_cloud_conenctor_rules_put_builder, zone_cloud_conenctor_rules_put_task,
    waf_content_scanning_disable_builder, waf_content_scanning_disable_task,
    waf_content_scanning_enable_builder, waf_content_scanning_enable_task,
    waf_content_scanning_list_custom_scan_expressions_builder, waf_content_scanning_list_custom_scan_expressions_task,
    waf_content_scanning_add_custom_scan_expressions_builder, waf_content_scanning_add_custom_scan_expressions_task,
    waf_content_scanning_delete_custom_scan_expressions_builder, waf_content_scanning_delete_custom_scan_expressions_task,
    waf_content_scanning_get_status_builder, waf_content_scanning_get_status_task,
    waf_content_scanning_update_settings_builder, waf_content_scanning_update_settings_task,
    custom_ssl_for_a_zone_list_ssl_configurations_builder, custom_ssl_for_a_zone_list_ssl_configurations_task,
    custom_ssl_for_a_zone_create_ssl_configuration_builder, custom_ssl_for_a_zone_create_ssl_configuration_task,
    custom_ssl_for_a_zone_re_prioritize_ssl_certificates_builder, custom_ssl_for_a_zone_re_prioritize_ssl_certificates_task,
    custom_ssl_for_a_zone_ssl_configuration_details_builder, custom_ssl_for_a_zone_ssl_configuration_details_task,
    custom_ssl_for_a_zone_edit_ssl_configuration_builder, custom_ssl_for_a_zone_edit_ssl_configuration_task,
    custom_ssl_for_a_zone_delete_ssl_configuration_builder, custom_ssl_for_a_zone_delete_ssl_configuration_task,
    custom_hostname_for_a_zone_list_custom_hostnames_builder, custom_hostname_for_a_zone_list_custom_hostnames_task,
    custom_hostname_for_a_zone_create_custom_hostname_builder, custom_hostname_for_a_zone_create_custom_hostname_task,
    custom_hostname_fallback_origin_for_a_zone_get_fallback_origin_for_custom_hostnames_builder, custom_hostname_fallback_origin_for_a_zone_get_fallback_origin_for_custom_hostnames_task,
    custom_hostname_fallback_origin_for_a_zone_update_fallback_origin_for_custom_hostnames_builder, custom_hostname_fallback_origin_for_a_zone_update_fallback_origin_for_custom_hostnames_task,
    custom_hostname_fallback_origin_for_a_zone_delete_fallback_origin_for_custom_hostnames_builder, custom_hostname_fallback_origin_for_a_zone_delete_fallback_origin_for_custom_hostnames_task,
    custom_hostname_for_a_zone_custom_hostname_details_builder, custom_hostname_for_a_zone_custom_hostname_details_task,
    custom_hostname_for_a_zone_edit_custom_hostname_builder, custom_hostname_for_a_zone_edit_custom_hostname_task,
    custom_hostname_for_a_zone_delete_custom_hostname_and_any_issued_ssl_certificates_builder, custom_hostname_for_a_zone_delete_custom_hostname_and_any_issued_ssl_certificates_task,
    custom_hostname_for_a_zone_edit_custom_certificate_custom_hostname_builder, custom_hostname_for_a_zone_edit_custom_certificate_custom_hostname_task,
    custom_hostname_for_a_zone_delete_single_certificate_and_key_in_a_custom_hostname_builder, custom_hostname_for_a_zone_delete_single_certificate_and_key_in_a_custom_hostname_task,
    account_level_custom_nameservers_usage_for_a_zone_get_account_custom_nameserver_related_zone_metadata_builder, account_level_custom_nameservers_usage_for_a_zone_get_account_custom_nameserver_related_zone_metadata_task,
    account_level_custom_nameservers_usage_for_a_zone_set_account_custom_nameserver_related_zone_metadata_builder, account_level_custom_nameservers_usage_for_a_zone_set_account_custom_nameserver_related_zone_metadata_task,
    dcv_delegation_uuid_get_builder, dcv_delegation_uuid_get_task,
    devices_get_policy_certificates_builder, devices_get_policy_certificates_task,
    devices_update_policy_certificates_builder, devices_update_policy_certificates_task,
    dns_analytics_table_builder, dns_analytics_table_task,
    dns_analytics_by_time_builder, dns_analytics_by_time_task,
    dns_records_for_a_zone_list_dns_records_builder, dns_records_for_a_zone_list_dns_records_task,
    dns_records_for_a_zone_create_dns_record_builder, dns_records_for_a_zone_create_dns_record_task,
    dns_records_for_a_zone_batch_dns_records_builder, dns_records_for_a_zone_batch_dns_records_task,
    dns_records_for_a_zone_export_dns_records_builder, dns_records_for_a_zone_export_dns_records_task,
    dns_records_for_a_zone_import_dns_records_builder, dns_records_for_a_zone_import_dns_records_task,
    dns_records_for_a_zone_scan_dns_records_builder, dns_records_for_a_zone_scan_dns_records_task,
    dns_records_for_a_zone_review_dns_scan_builder, dns_records_for_a_zone_review_dns_scan_task,
    dns_records_for_a_zone_apply_dns_scan_results_builder, dns_records_for_a_zone_apply_dns_scan_results_task,
    dns_records_for_a_zone_trigger_dns_scan_builder, dns_records_for_a_zone_trigger_dns_scan_task,
    dns_records_for_a_zone_get_usage_builder, dns_records_for_a_zone_get_usage_task,
    dns_records_for_a_zone_dns_record_details_builder, dns_records_for_a_zone_dns_record_details_task,
    dns_records_for_a_zone_update_dns_record_builder, dns_records_for_a_zone_update_dns_record_task,
    dns_records_for_a_zone_patch_dns_record_builder, dns_records_for_a_zone_patch_dns_record_task,
    dns_records_for_a_zone_delete_dns_record_builder, dns_records_for_a_zone_delete_dns_record_task,
    dns_settings_for_a_zone_list_dns_settings_builder, dns_settings_for_a_zone_list_dns_settings_task,
    dns_settings_for_a_zone_update_dns_settings_builder, dns_settings_for_a_zone_update_dns_settings_task,
    dnssec_dnssec_details_builder, dnssec_dnssec_details_task,
    dnssec_edit_dnssec_status_builder, dnssec_edit_dnssec_status_task,
    dnssec_delete_dnssec_records_builder, dnssec_delete_dnssec_records_task,
    email_routing_settings_get_email_routing_settings_builder, email_routing_settings_get_email_routing_settings_task,
    email_routing_settings_disable_email_routing_builder, email_routing_settings_disable_email_routing_task,
    email_routing_settings_email_routing_dns_settings_builder, email_routing_settings_email_routing_dns_settings_task,
    email_routing_settings_enable_email_routing_dns_builder, email_routing_settings_enable_email_routing_dns_task,
    email_routing_settings_unlock_email_routing_dns_builder, email_routing_settings_unlock_email_routing_dns_task,
    email_routing_settings_disable_email_routing_dns_builder, email_routing_settings_disable_email_routing_dns_task,
    email_routing_settings_enable_email_routing_builder, email_routing_settings_enable_email_routing_task,
    email_routing_routing_rules_list_routing_rules_builder, email_routing_routing_rules_list_routing_rules_task,
    email_routing_routing_rules_create_routing_rule_builder, email_routing_routing_rules_create_routing_rule_task,
    email_routing_routing_rules_get_catch_all_rule_builder, email_routing_routing_rules_get_catch_all_rule_task,
    email_routing_routing_rules_update_catch_all_rule_builder, email_routing_routing_rules_update_catch_all_rule_task,
    email_routing_routing_rules_get_routing_rule_builder, email_routing_routing_rules_get_routing_rule_task,
    email_routing_routing_rules_update_routing_rule_builder, email_routing_routing_rules_update_routing_rule_task,
    email_routing_routing_rules_delete_routing_rule_builder, email_routing_routing_rules_delete_routing_rule_task,
    email_sending_subdomains_list_sending_subdomains_builder, email_sending_subdomains_list_sending_subdomains_task,
    email_sending_subdomains_create_sending_subdomain_builder, email_sending_subdomains_create_sending_subdomain_task,
    email_sending_subdomains_preview_sending_subdomain_builder, email_sending_subdomains_preview_sending_subdomain_task,
    email_sending_subdomains_get_sending_subdomain_builder, email_sending_subdomains_get_sending_subdomain_task,
    email_sending_subdomains_delete_sending_subdomain_builder, email_sending_subdomains_delete_sending_subdomain_task,
    email_sending_subdomains_get_sending_subdomain_dns_builder, email_sending_subdomains_get_sending_subdomain_dns_task,
    zones_environments_list_builder, zones_environments_list_task,
    zones_environments_create_builder, zones_environments_create_task,
    zones_environments_update_builder, zones_environments_update_task,
    zones_environments_edit_builder, zones_environments_edit_task,
    zones_environments_delete_builder, zones_environments_delete_task,
    zones_environments_rollback_builder, zones_environments_rollback_task,
    filters_list_filters_builder, filters_list_filters_task,
    filters_create_filters_builder, filters_create_filters_task,
    filters_update_filters_builder, filters_update_filters_task,
    filters_delete_filters_builder, filters_delete_filters_task,
    filters_get_a_filter_builder, filters_get_a_filter_task,
    filters_update_a_filter_builder, filters_update_a_filter_task,
    filters_delete_a_filter_builder, filters_delete_a_filter_task,
    firewall_for_ai_custom_topics_get_builder, firewall_for_ai_custom_topics_get_task,
    firewall_for_ai_custom_topics_put_builder, firewall_for_ai_custom_topics_put_task,
    firewall_for_ai_settings_get_builder, firewall_for_ai_settings_get_task,
    firewall_for_ai_settings_put_builder, firewall_for_ai_settings_put_task,
    ip_access_rules_for_a_zone_list_ip_access_rules_builder, ip_access_rules_for_a_zone_list_ip_access_rules_task,
    ip_access_rules_for_a_zone_create_an_ip_access_rule_builder, ip_access_rules_for_a_zone_create_an_ip_access_rule_task,
    ip_access_rules_for_a_zone_update_an_ip_access_rule_builder, ip_access_rules_for_a_zone_update_an_ip_access_rule_task,
    ip_access_rules_for_a_zone_delete_an_ip_access_rule_builder, ip_access_rules_for_a_zone_delete_an_ip_access_rule_task,
    zone_lockdown_list_zone_lockdown_rules_builder, zone_lockdown_list_zone_lockdown_rules_task,
    zone_lockdown_create_a_zone_lockdown_rule_builder, zone_lockdown_create_a_zone_lockdown_rule_task,
    zone_lockdown_get_a_zone_lockdown_rule_builder, zone_lockdown_get_a_zone_lockdown_rule_task,
    zone_lockdown_update_a_zone_lockdown_rule_builder, zone_lockdown_update_a_zone_lockdown_rule_task,
    zone_lockdown_delete_a_zone_lockdown_rule_builder, zone_lockdown_delete_a_zone_lockdown_rule_task,
    firewall_rules_list_firewall_rules_builder, firewall_rules_list_firewall_rules_task,
    firewall_rules_create_firewall_rules_builder, firewall_rules_create_firewall_rules_task,
    firewall_rules_update_firewall_rules_builder, firewall_rules_update_firewall_rules_task,
    firewall_rules_update_priority_of_firewall_rules_builder, firewall_rules_update_priority_of_firewall_rules_task,
    firewall_rules_delete_firewall_rules_builder, firewall_rules_delete_firewall_rules_task,
    firewall_rules_get_a_firewall_rule_builder, firewall_rules_get_a_firewall_rule_task,
    firewall_rules_update_a_firewall_rule_builder, firewall_rules_update_a_firewall_rule_task,
    firewall_rules_update_priority_of_a_firewall_rule_builder, firewall_rules_update_priority_of_a_firewall_rule_task,
    firewall_rules_delete_a_firewall_rule_builder, firewall_rules_delete_a_firewall_rule_task,
    user_agent_blocking_rules_list_user_agent_blocking_rules_builder, user_agent_blocking_rules_list_user_agent_blocking_rules_task,
    user_agent_blocking_rules_create_a_user_agent_blocking_rule_builder, user_agent_blocking_rules_create_a_user_agent_blocking_rule_task,
    user_agent_blocking_rules_get_a_user_agent_blocking_rule_builder, user_agent_blocking_rules_get_a_user_agent_blocking_rule_task,
    user_agent_blocking_rules_update_a_user_agent_blocking_rule_builder, user_agent_blocking_rules_update_a_user_agent_blocking_rule_task,
    user_agent_blocking_rules_delete_a_user_agent_blocking_rule_builder, user_agent_blocking_rules_delete_a_user_agent_blocking_rule_task,
    waf_overrides_list_waf_overrides_builder, waf_overrides_list_waf_overrides_task,
    waf_overrides_create_a_waf_override_builder, waf_overrides_create_a_waf_override_task,
    waf_overrides_get_a_waf_override_builder, waf_overrides_get_a_waf_override_task,
    waf_overrides_update_waf_override_builder, waf_overrides_update_waf_override_task,
    waf_overrides_delete_a_waf_override_builder, waf_overrides_delete_a_waf_override_task,
    waf_packages_list_waf_packages_builder, waf_packages_list_waf_packages_task,
    waf_packages_get_a_waf_package_builder, waf_packages_get_a_waf_package_task,
    waf_packages_update_a_waf_package_builder, waf_packages_update_a_waf_package_task,
    waf_rule_groups_list_waf_rule_groups_builder, waf_rule_groups_list_waf_rule_groups_task,
    waf_rule_groups_get_a_waf_rule_group_builder, waf_rule_groups_get_a_waf_rule_group_task,
    waf_rule_groups_update_a_waf_rule_group_builder, waf_rule_groups_update_a_waf_rule_group_task,
    waf_rules_list_waf_rules_builder, waf_rules_list_waf_rules_task,
    waf_rules_get_a_waf_rule_builder, waf_rules_get_a_waf_rule_task,
    waf_rules_update_a_waf_rule_builder, waf_rules_update_a_waf_rule_task,
    fraud_detection_zone_get_settings_builder, fraud_detection_zone_get_settings_task,
    fraud_detection_zone_update_settings_builder, fraud_detection_zone_update_settings_task,
    health_checks_list_health_checks_builder, health_checks_list_health_checks_task,
    health_checks_create_health_check_builder, health_checks_create_health_check_task,
    health_checks_create_preview_health_check_builder, health_checks_create_preview_health_check_task,
    health_checks_health_check_preview_details_builder, health_checks_health_check_preview_details_task,
    health_checks_delete_preview_health_check_builder, health_checks_delete_preview_health_check_task,
    health_checks_health_check_details_builder, health_checks_health_check_details_task,
    health_checks_update_health_check_builder, health_checks_update_health_check_task,
    health_checks_patch_health_check_builder, health_checks_patch_health_check_task,
    health_checks_delete_health_check_builder, health_checks_delete_health_check_task,
    zones_0_hold_get_builder, zones_0_hold_get_task,
    zones_0_hold_post_builder, zones_0_hold_post_task,
    zones_0_hold_patch_builder, zones_0_hold_patch_task,
    zones_0_hold_delete_builder, zones_0_hold_delete_task,
    per_hostname_tls_settings_list_builder, per_hostname_tls_settings_list_task,
    per_hostname_tls_settings_get_builder, per_hostname_tls_settings_get_task,
    per_hostname_tls_settings_put_builder, per_hostname_tls_settings_put_task,
    per_hostname_tls_settings_delete_builder, per_hostname_tls_settings_delete_task,
    keyless_ssl_for_a_zone_list_keyless_ssl_configurations_builder, keyless_ssl_for_a_zone_list_keyless_ssl_configurations_task,
    keyless_ssl_for_a_zone_create_keyless_ssl_configuration_builder, keyless_ssl_for_a_zone_create_keyless_ssl_configuration_task,
    keyless_ssl_for_a_zone_get_keyless_ssl_configuration_builder, keyless_ssl_for_a_zone_get_keyless_ssl_configuration_task,
    keyless_ssl_for_a_zone_edit_keyless_ssl_configuration_builder, keyless_ssl_for_a_zone_edit_keyless_ssl_configuration_task,
    keyless_ssl_for_a_zone_delete_keyless_ssl_configuration_builder, keyless_ssl_for_a_zone_delete_keyless_ssl_configuration_task,
    waf_product_api_leaked_credentials_get_status_builder, waf_product_api_leaked_credentials_get_status_task,
    waf_product_api_leaked_credentials_set_status_builder, waf_product_api_leaked_credentials_set_status_task,
    waf_product_api_leaked_credentials_list_detections_builder, waf_product_api_leaked_credentials_list_detections_task,
    waf_product_api_leaked_credentials_create_detection_builder, waf_product_api_leaked_credentials_create_detection_task,
    waf_product_api_leaked_credentials_get_detection_builder, waf_product_api_leaked_credentials_get_detection_task,
    waf_product_api_leaked_credentials_update_detection_builder, waf_product_api_leaked_credentials_update_detection_task,
    waf_product_api_leaked_credentials_delete_detection_builder, waf_product_api_leaked_credentials_delete_detection_task,
    load_balancers_list_load_balancers_builder, load_balancers_list_load_balancers_task,
    load_balancers_create_load_balancer_builder, load_balancers_create_load_balancer_task,
    load_balancers_load_balancer_details_builder, load_balancers_load_balancer_details_task,
    load_balancers_update_load_balancer_builder, load_balancers_update_load_balancer_task,
    load_balancers_patch_load_balancer_builder, load_balancers_patch_load_balancer_task,
    load_balancers_delete_load_balancer_builder, load_balancers_delete_load_balancer_task,
    get_zones_zone_id_logpush_datasets_dataset_id_fields_builder, get_zones_zone_id_logpush_datasets_dataset_id_fields_task,
    get_zones_zone_id_logpush_datasets_dataset_id_jobs_builder, get_zones_zone_id_logpush_datasets_dataset_id_jobs_task,
    get_zones_zone_id_logpush_edge_jobs_builder, get_zones_zone_id_logpush_edge_jobs_task,
    post_zones_zone_id_logpush_edge_jobs_builder, post_zones_zone_id_logpush_edge_jobs_task,
    get_zones_zone_id_logpush_jobs_builder, get_zones_zone_id_logpush_jobs_task,
    post_zones_zone_id_logpush_jobs_builder, post_zones_zone_id_logpush_jobs_task,
    get_zones_zone_id_logpush_jobs_job_id_builder, get_zones_zone_id_logpush_jobs_job_id_task,
    put_zones_zone_id_logpush_jobs_job_id_builder, put_zones_zone_id_logpush_jobs_job_id_task,
    delete_zones_zone_id_logpush_jobs_job_id_builder, delete_zones_zone_id_logpush_jobs_job_id_task,
    post_zones_zone_id_logpush_ownership_builder, post_zones_zone_id_logpush_ownership_task,
    post_zones_zone_id_logpush_ownership_validate_builder, post_zones_zone_id_logpush_ownership_validate_task,
    post_zones_zone_id_logpush_validate_destination_builder, post_zones_zone_id_logpush_validate_destination_task,
    post_zones_zone_id_logpush_validate_destination_exists_builder, post_zones_zone_id_logpush_validate_destination_exists_task,
    post_zones_zone_id_logpush_validate_origin_builder, post_zones_zone_id_logpush_validate_origin_task,
    get_zones_zone_id_logs_control_retention_flag_builder, get_zones_zone_id_logs_control_retention_flag_task,
    post_zones_zone_id_logs_control_retention_flag_builder, post_zones_zone_id_logs_control_retention_flag_task,
    get_zones_zone_id_logs_rayids_ray_id_builder, get_zones_zone_id_logs_rayids_ray_id_task,
    get_zones_zone_id_logs_received_builder, get_zones_zone_id_logs_received_task,
    get_zones_zone_id_logs_received_fields_builder, get_zones_zone_id_logs_received_fields_task,
    list_managed_transforms_builder, list_managed_transforms_task,
    update_managed_transforms_builder, update_managed_transforms_task,
    delete_managed_transforms_builder, delete_managed_transforms_task,
    zone_level_authenticated_origin_pulls_list_certificates_builder, zone_level_authenticated_origin_pulls_list_certificates_task,
    zone_level_authenticated_origin_pulls_upload_certificate_builder, zone_level_authenticated_origin_pulls_upload_certificate_task,
    per_hostname_authenticated_origin_pull_list_hostname_associations_builder, per_hostname_authenticated_origin_pull_list_hostname_associations_task,
    per_hostname_authenticated_origin_pull_enable_or_disable_a_hostname_for_client_authentication_builder, per_hostname_authenticated_origin_pull_enable_or_disable_a_hostname_for_client_authentication_task,
    per_hostname_authenticated_origin_pull_list_certificates_builder, per_hostname_authenticated_origin_pull_list_certificates_task,
    per_hostname_authenticated_origin_pull_upload_a_hostname_client_certificate_builder, per_hostname_authenticated_origin_pull_upload_a_hostname_client_certificate_task,
    per_hostname_authenticated_origin_pull_get_the_hostname_client_certificate_builder, per_hostname_authenticated_origin_pull_get_the_hostname_client_certificate_task,
    per_hostname_authenticated_origin_pull_delete_hostname_client_certificate_builder, per_hostname_authenticated_origin_pull_delete_hostname_client_certificate_task,
    per_hostname_authenticated_origin_pull_get_the_hostname_status_for_client_authentication_builder, per_hostname_authenticated_origin_pull_get_the_hostname_status_for_client_authentication_task,
    zone_level_authenticated_origin_pulls_get_enablement_setting_for_zone_builder, zone_level_authenticated_origin_pulls_get_enablement_setting_for_zone_task,
    zone_level_authenticated_origin_pulls_set_enablement_for_zone_builder, zone_level_authenticated_origin_pulls_set_enablement_for_zone_task,
    zone_level_authenticated_origin_pulls_get_certificate_details_builder, zone_level_authenticated_origin_pulls_get_certificate_details_task,
    zone_level_authenticated_origin_pulls_delete_certificate_builder, zone_level_authenticated_origin_pulls_delete_certificate_task,
    page_shield_get_settings_builder, page_shield_get_settings_task,
    page_shield_update_settings_builder, page_shield_update_settings_task,
    page_shield_list_connections_builder, page_shield_list_connections_task,
    page_shield_get_connection_builder, page_shield_get_connection_task,
    page_shield_list_cookies_builder, page_shield_list_cookies_task,
    page_shield_get_cookie_builder, page_shield_get_cookie_task,
    page_shield_list_policies_builder, page_shield_list_policies_task,
    page_shield_create_policy_builder, page_shield_create_policy_task,
    page_shield_get_policy_builder, page_shield_get_policy_task,
    page_shield_update_policy_builder, page_shield_update_policy_task,
    page_shield_delete_policy_builder, page_shield_delete_policy_task,
    page_shield_list_scripts_builder, page_shield_list_scripts_task,
    page_shield_get_script_builder, page_shield_get_script_task,
    page_rules_list_page_rules_builder, page_rules_list_page_rules_task,
    page_rules_create_a_page_rule_builder, page_rules_create_a_page_rule_task,
    available_page_rules_settings_list_available_page_rules_settings_builder, available_page_rules_settings_list_available_page_rules_settings_task,
    page_rules_get_a_page_rule_builder, page_rules_get_a_page_rule_task,
    page_rules_update_a_page_rule_builder, page_rules_update_a_page_rule_task,
    page_rules_edit_a_page_rule_builder, page_rules_edit_a_page_rule_task,
    page_rules_delete_a_page_rule_builder, page_rules_delete_a_page_rule_task,
    pay_per_crawl_get_config_builder, pay_per_crawl_get_config_task,
    pay_per_crawl_create_config_builder, pay_per_crawl_create_config_task,
    pay_per_crawl_patch_config_builder, pay_per_crawl_patch_config_task,
    zone_purge_builder, zone_purge_task,
    rate_limits_for_a_zone_list_rate_limits_builder, rate_limits_for_a_zone_list_rate_limits_task,
    rate_limits_for_a_zone_create_a_rate_limit_builder, rate_limits_for_a_zone_create_a_rate_limit_task,
    rate_limits_for_a_zone_get_a_rate_limit_builder, rate_limits_for_a_zone_get_a_rate_limit_task,
    rate_limits_for_a_zone_update_a_rate_limit_builder, rate_limits_for_a_zone_update_a_rate_limit_task,
    rate_limits_for_a_zone_delete_a_rate_limit_builder, rate_limits_for_a_zone_delete_a_rate_limit_task,
    list_zone_rulesets_builder, list_zone_rulesets_task,
    create_zone_ruleset_builder, create_zone_ruleset_task,
    get_zone_entrypoint_ruleset_builder, get_zone_entrypoint_ruleset_task,
    update_zone_entrypoint_ruleset_builder, update_zone_entrypoint_ruleset_task,
    list_zone_entrypoint_ruleset_versions_builder, list_zone_entrypoint_ruleset_versions_task,
    get_zone_entrypoint_ruleset_version_builder, get_zone_entrypoint_ruleset_version_task,
    get_zone_ruleset_builder, get_zone_ruleset_task,
    update_zone_ruleset_builder, update_zone_ruleset_task,
    delete_zone_ruleset_builder, delete_zone_ruleset_task,
    create_zone_ruleset_rule_builder, create_zone_ruleset_rule_task,
    update_zone_ruleset_rule_builder, update_zone_ruleset_rule_task,
    delete_zone_ruleset_rule_builder, delete_zone_ruleset_rule_task,
    list_zone_ruleset_versions_builder, list_zone_ruleset_versions_task,
    get_zone_ruleset_version_builder, get_zone_ruleset_version_task,
    delete_zone_ruleset_version_builder, delete_zone_ruleset_version_task,
    list_zone_ruleset_version_rules_by_tag_builder, list_zone_ruleset_version_rules_by_tag_task,
    schema_validation_list_schemas_paginated_builder, schema_validation_list_schemas_paginated_task,
    schema_validation_create_schema_builder, schema_validation_create_schema_task,
    schema_validation_list_schema_hosts_builder, schema_validation_list_schema_hosts_task,
    schema_validation_get_schema_builder, schema_validation_get_schema_task,
    schema_validation_edit_schema_builder, schema_validation_edit_schema_task,
    schema_validation_delete_schema_builder, schema_validation_delete_schema_task,
    schema_validation_extract_operations_from_schema_builder, schema_validation_extract_operations_from_schema_task,
    schema_validation_get_settings_builder, schema_validation_get_settings_task,
    schema_validation_update_settings_builder, schema_validation_update_settings_task,
    schema_validation_edit_settings_builder, schema_validation_edit_settings_task,
    schema_validation_list_per_operation_settings_builder, schema_validation_list_per_operation_settings_task,
    schema_validation_bulk_edit_per_operation_settings_builder, schema_validation_bulk_edit_per_operation_settings_task,
    schema_validation_get_per_operation_setting_builder, schema_validation_get_per_operation_setting_task,
    schema_validation_update_per_operation_setting_builder, schema_validation_update_per_operation_setting_task,
    schema_validation_delete_per_operation_setting_builder, schema_validation_delete_per_operation_setting_task,
    secondary_dns_secondary_zone_force_axfr_builder, secondary_dns_secondary_zone_force_axfr_task,
    secondary_dns_secondary_zone_secondary_zone_configuration_details_builder, secondary_dns_secondary_zone_secondary_zone_configuration_details_task,
    secondary_dns_secondary_zone_create_secondary_zone_configuration_builder, secondary_dns_secondary_zone_create_secondary_zone_configuration_task,
    secondary_dns_secondary_zone_update_secondary_zone_configuration_builder, secondary_dns_secondary_zone_update_secondary_zone_configuration_task,
    secondary_dns_secondary_zone_delete_secondary_zone_configuration_builder, secondary_dns_secondary_zone_delete_secondary_zone_configuration_task,
    secondary_dns_primary_zone_primary_zone_configuration_details_builder, secondary_dns_primary_zone_primary_zone_configuration_details_task,
    secondary_dns_primary_zone_create_primary_zone_configuration_builder, secondary_dns_primary_zone_create_primary_zone_configuration_task,
    secondary_dns_primary_zone_update_primary_zone_configuration_builder, secondary_dns_primary_zone_update_primary_zone_configuration_task,
    secondary_dns_primary_zone_delete_primary_zone_configuration_builder, secondary_dns_primary_zone_delete_primary_zone_configuration_task,
    secondary_dns_primary_zone_disable_outgoing_zone_transfers_builder, secondary_dns_primary_zone_disable_outgoing_zone_transfers_task,
    secondary_dns_primary_zone_enable_outgoing_zone_transfers_builder, secondary_dns_primary_zone_enable_outgoing_zone_transfers_task,
    secondary_dns_primary_zone_force_dns_notify_builder, secondary_dns_primary_zone_force_dns_notify_task,
    secondary_dns_primary_zone_get_outgoing_zone_transfer_status_builder, secondary_dns_primary_zone_get_outgoing_zone_transfer_status_task,
    get_zone_security_center_insights_builder, get_zone_security_center_insights_task,
    get_zone_security_center_insight_counts_by_class_builder, get_zone_security_center_insight_counts_by_class_task,
    get_zone_security_center_insight_counts_by_severity_builder, get_zone_security_center_insight_counts_by_severity_task,
    get_zone_security_center_insight_counts_by_type_builder, get_zone_security_center_insight_counts_by_type_task,
    archive_zone_security_center_insight_builder, archive_zone_security_center_insight_task,
    get_security_txt_builder, get_security_txt_task,
    update_security_txt_builder, update_security_txt_task,
    delete_security_txt_builder, delete_security_txt_task,
    zone_settings_get_all_zone_settings_builder, zone_settings_get_all_zone_settings_task,
    zone_settings_edit_zone_settings_info_builder, zone_settings_edit_zone_settings_info_task,
    zone_cache_settings_get_aegis_setting_builder, zone_cache_settings_get_aegis_setting_task,
    zone_cache_settings_change_aegis_setting_builder, zone_cache_settings_change_aegis_setting_task,
    zone_settings_get_fonts_setting_builder, zone_settings_get_fonts_setting_task,
    zone_settings_change_fonts_setting_builder, zone_settings_change_fonts_setting_task,
    zone_settings_get_google_tag_gateway_config_builder, zone_settings_get_google_tag_gateway_config_task,
    zone_settings_change_google_tag_gateway_config_builder, zone_settings_change_google_tag_gateway_config_task,
    zone_cache_settings_get_origin_h2_max_streams_setting_builder, zone_cache_settings_get_origin_h2_max_streams_setting_task,
    zone_cache_settings_change_origin_h2_max_streams_setting_builder, zone_cache_settings_change_origin_h2_max_streams_setting_task,
    zone_cache_settings_get_origin_max_http_version_setting_builder, zone_cache_settings_get_origin_max_http_version_setting_task,
    zone_cache_settings_change_origin_max_http_version_setting_builder, zone_cache_settings_change_origin_max_http_version_setting_task,
    web_analytics_get_rum_status_builder, web_analytics_get_rum_status_task,
    web_analytics_toggle_rum_builder, web_analytics_toggle_rum_task,
    zone_settings_get_speed_brain_setting_builder, zone_settings_get_speed_brain_setting_task,
    zone_settings_change_speed_brain_setting_builder, zone_settings_change_speed_brain_setting_task,
    ssl_detector_automatic_mode_get_enrollment_builder, ssl_detector_automatic_mode_get_enrollment_task,
    ssl_detector_automatic_mode_patch_enrollment_builder, ssl_detector_automatic_mode_patch_enrollment_task,
    get_zones_zone_identifier_zaraz_config_builder, get_zones_zone_identifier_zaraz_config_task,
    put_zones_zone_identifier_zaraz_config_builder, put_zones_zone_identifier_zaraz_config_task,
    get_zones_zone_identifier_zaraz_default_builder, get_zones_zone_identifier_zaraz_default_task,
    get_zones_zone_identifier_zaraz_export_builder, get_zones_zone_identifier_zaraz_export_task,
    get_zones_zone_identifier_zaraz_history_builder, get_zones_zone_identifier_zaraz_history_task,
    put_zones_zone_identifier_zaraz_history_builder, put_zones_zone_identifier_zaraz_history_task,
    get_zones_zone_identifier_zaraz_config_history_builder, get_zones_zone_identifier_zaraz_config_history_task,
    post_zones_zone_identifier_zaraz_publish_builder, post_zones_zone_identifier_zaraz_publish_task,
    get_zones_zone_identifier_zaraz_workflow_builder, get_zones_zone_identifier_zaraz_workflow_task,
    put_zones_zone_identifier_zaraz_workflow_builder, put_zones_zone_identifier_zaraz_workflow_task,
    zone_settings_get_single_setting_builder, zone_settings_get_single_setting_task,
    zone_settings_edit_single_setting_builder, zone_settings_edit_single_setting_task,
    smart_shield_get_settings_builder, smart_shield_get_settings_task,
    smart_shield_patch_settings_builder, smart_shield_patch_settings_task,
    smart_shield_settings_get_cache_reserve_clear_builder, smart_shield_settings_get_cache_reserve_clear_task,
    smart_shield_settings_start_cache_reserve_clear_builder, smart_shield_settings_start_cache_reserve_clear_task,
    smart_shield_list_health_checks_builder, smart_shield_list_health_checks_task,
    smart_shield_create_health_check_builder, smart_shield_create_health_check_task,
    smart_shield_health_check_details_builder, smart_shield_health_check_details_task,
    smart_shield_update_health_check_builder, smart_shield_update_health_check_task,
    smart_shield_patch_health_check_builder, smart_shield_patch_health_check_task,
    smart_shield_delete_health_check_builder, smart_shield_delete_health_check_task,
    list_zone_snippets_builder, list_zone_snippets_task,
    list_zone_snippet_rules_builder, list_zone_snippet_rules_task,
    update_zone_snippet_rules_builder, update_zone_snippet_rules_task,
    delete_zone_snippet_rules_builder, delete_zone_snippet_rules_task,
    get_zone_snippet_builder, get_zone_snippet_task,
    update_zone_snippet_builder, update_zone_snippet_task,
    delete_zone_snippet_builder, delete_zone_snippet_task,
    get_zone_snippet_content_builder, get_zone_snippet_content_task,
    spectrum_aggregate_analytics_get_current_aggregated_analytics_builder, spectrum_aggregate_analytics_get_current_aggregated_analytics_task,
    spectrum_analytics_by_time_get_analytics_by_time_builder, spectrum_analytics_by_time_get_analytics_by_time_task,
    spectrum_analytics_summary_get_analytics_summary_builder, spectrum_analytics_summary_get_analytics_summary_task,
    spectrum_applications_list_spectrum_applications_builder, spectrum_applications_list_spectrum_applications_task,
    spectrum_applications_create_spectrum_application_using_a_name_for_the_origin_builder, spectrum_applications_create_spectrum_application_using_a_name_for_the_origin_task,
    spectrum_applications_get_spectrum_application_configuration_builder, spectrum_applications_get_spectrum_application_configuration_task,
    spectrum_applications_update_spectrum_application_configuration_using_a_name_for_the_origin_builder, spectrum_applications_update_spectrum_application_configuration_using_a_name_for_the_origin_task,
    spectrum_applications_delete_spectrum_application_builder, spectrum_applications_delete_spectrum_application_task,
    speed_get_availabilities_builder, speed_get_availabilities_task,
    speed_list_pages_builder, speed_list_pages_task,
    speed_list_test_history_builder, speed_list_test_history_task,
    speed_create_test_builder, speed_create_test_task,
    speed_delete_tests_builder, speed_delete_tests_task,
    speed_get_test_builder, speed_get_test_task,
    speed_list_page_trend_builder, speed_list_page_trend_task,
    speed_get_scheduled_test_builder, speed_get_scheduled_test_task,
    speed_create_scheduled_test_builder, speed_create_scheduled_test_task,
    speed_delete_test_schedule_builder, speed_delete_test_schedule_task,
    analyze_certificate_analyze_certificate_builder, analyze_certificate_analyze_certificate_task,
    certificate_packs_list_certificate_packs_builder, certificate_packs_list_certificate_packs_task,
    certificate_packs_order_advanced_certificate_manager_certificate_pack_builder, certificate_packs_order_advanced_certificate_manager_certificate_pack_task,
    certificate_packs_get_certificate_pack_quotas_builder, certificate_packs_get_certificate_pack_quotas_task,
    certificate_packs_get_certificate_pack_builder, certificate_packs_get_certificate_pack_task,
    certificate_packs_restart_validation_for_advanced_certificate_manager_certificate_pack_builder, certificate_packs_restart_validation_for_advanced_certificate_manager_certificate_pack_task,
    certificate_packs_delete_advanced_certificate_manager_certificate_pack_builder, certificate_packs_delete_advanced_certificate_manager_certificate_pack_task,
    ssl_tls_mode_recommendation_ssl_tls_recommendation_builder, ssl_tls_mode_recommendation_ssl_tls_recommendation_task,
    universal_ssl_settings_for_a_zone_universal_ssl_settings_details_builder, universal_ssl_settings_for_a_zone_universal_ssl_settings_details_task,
    universal_ssl_settings_for_a_zone_edit_universal_ssl_settings_builder, universal_ssl_settings_for_a_zone_edit_universal_ssl_settings_task,
    ssl_verification_ssl_verification_details_builder, ssl_verification_ssl_verification_details_task,
    ssl_verification_edit_ssl_certificate_pack_validation_method_builder, ssl_verification_edit_ssl_certificate_pack_validation_method_task,
    zone_subscription_zone_subscription_details_builder, zone_subscription_zone_subscription_details_task,
    zone_subscription_create_zone_subscription_builder, zone_subscription_create_zone_subscription_task,
    zone_subscription_update_zone_subscription_builder, zone_subscription_update_zone_subscription_task,
    tags_zone_get_builder, tags_zone_get_task,
    tags_zone_set_builder, tags_zone_set_task,
    tags_zone_delete_builder, tags_zone_delete_task,
    token_validation_config_list_builder, token_validation_config_list_task,
    token_validation_config_create_builder, token_validation_config_create_task,
    token_validation_config_get_builder, token_validation_config_get_task,
    token_validation_config_edit_builder, token_validation_config_edit_task,
    token_validation_config_delete_builder, token_validation_config_delete_task,
    token_validation_config_credentials_update_builder, token_validation_config_credentials_update_task,
    token_validation_rules_list_builder, token_validation_rules_list_task,
    token_validation_rules_create_builder, token_validation_rules_create_task,
    token_validation_rules_bulk_create_builder, token_validation_rules_bulk_create_task,
    token_validation_rules_bulk_edit_builder, token_validation_rules_bulk_edit_task,
    token_validation_rules_preview_builder, token_validation_rules_preview_task,
    token_validation_rules_get_builder, token_validation_rules_get_task,
    token_validation_rules_edit_builder, token_validation_rules_edit_task,
    token_validation_rules_delete_builder, token_validation_rules_delete_task,
    get_url_normalization_builder, get_url_normalization_task,
    update_url_normalization_builder, update_url_normalization_task,
    delete_url_normalization_builder, delete_url_normalization_task,
    waiting_room_list_waiting_rooms_builder, waiting_room_list_waiting_rooms_task,
    waiting_room_create_waiting_room_builder, waiting_room_create_waiting_room_task,
    waiting_room_create_a_custom_waiting_room_page_preview_builder, waiting_room_create_a_custom_waiting_room_page_preview_task,
    waiting_room_get_zone_settings_builder, waiting_room_get_zone_settings_task,
    waiting_room_update_zone_settings_builder, waiting_room_update_zone_settings_task,
    waiting_room_patch_zone_settings_builder, waiting_room_patch_zone_settings_task,
    waiting_room_waiting_room_details_builder, waiting_room_waiting_room_details_task,
    waiting_room_update_waiting_room_builder, waiting_room_update_waiting_room_task,
    waiting_room_patch_waiting_room_builder, waiting_room_patch_waiting_room_task,
    waiting_room_delete_waiting_room_builder, waiting_room_delete_waiting_room_task,
    waiting_room_list_events_builder, waiting_room_list_events_task,
    waiting_room_create_event_builder, waiting_room_create_event_task,
    waiting_room_event_details_builder, waiting_room_event_details_task,
    waiting_room_update_event_builder, waiting_room_update_event_task,
    waiting_room_patch_event_builder, waiting_room_patch_event_task,
    waiting_room_delete_event_builder, waiting_room_delete_event_task,
    waiting_room_preview_active_event_details_builder, waiting_room_preview_active_event_details_task,
    waiting_room_list_waiting_room_rules_builder, waiting_room_list_waiting_room_rules_task,
    waiting_room_create_waiting_room_rule_builder, waiting_room_create_waiting_room_rule_task,
    waiting_room_replace_waiting_room_rules_builder, waiting_room_replace_waiting_room_rules_task,
    waiting_room_patch_waiting_room_rule_builder, waiting_room_patch_waiting_room_rule_task,
    waiting_room_delete_waiting_room_rule_builder, waiting_room_delete_waiting_room_rule_task,
    waiting_room_get_waiting_room_status_builder, waiting_room_get_waiting_room_status_task,
    web3_hostname_list_web3_hostnames_builder, web3_hostname_list_web3_hostnames_task,
    web3_hostname_create_web3_hostname_builder, web3_hostname_create_web3_hostname_task,
    web3_hostname_web3_hostname_details_builder, web3_hostname_web3_hostname_details_task,
    web3_hostname_edit_web3_hostname_builder, web3_hostname_edit_web3_hostname_task,
    web3_hostname_delete_web3_hostname_builder, web3_hostname_delete_web3_hostname_task,
    web3_hostname_ipfs_universal_path_gateway_content_list_details_builder, web3_hostname_ipfs_universal_path_gateway_content_list_details_task,
    web3_hostname_update_ipfs_universal_path_gateway_content_list_builder, web3_hostname_update_ipfs_universal_path_gateway_content_list_task,
    web3_hostname_list_ipfs_universal_path_gateway_content_list_entries_builder, web3_hostname_list_ipfs_universal_path_gateway_content_list_entries_task,
    web3_hostname_create_ipfs_universal_path_gateway_content_list_entry_builder, web3_hostname_create_ipfs_universal_path_gateway_content_list_entry_task,
    web3_hostname_ipfs_universal_path_gateway_content_list_entry_details_builder, web3_hostname_ipfs_universal_path_gateway_content_list_entry_details_task,
    web3_hostname_edit_ipfs_universal_path_gateway_content_list_entry_builder, web3_hostname_edit_ipfs_universal_path_gateway_content_list_entry_task,
    web3_hostname_delete_ipfs_universal_path_gateway_content_list_entry_builder, web3_hostname_delete_ipfs_universal_path_gateway_content_list_entry_task,
    worker_routes_list_routes_builder, worker_routes_list_routes_task,
    worker_routes_create_route_builder, worker_routes_create_route_task,
    worker_routes_get_route_builder, worker_routes_get_route_task,
    worker_routes_update_route_builder, worker_routes_update_route_task,
    worker_routes_delete_route_builder, worker_routes_delete_route_task,
};
use crate::providers::cloudflare::clients::types::{ApiError, ApiPending};
use crate::providers::cloudflare::clients::AaaAlertsResponseCollection;
use crate::providers::cloudflare::clients::AaaApiResponseCollection;
use crate::providers::cloudflare::clients::AaaAuditLogsResponseCollection;
use crate::providers::cloudflare::clients::AaaAuditLogsV2OrgResponseCollection;
use crate::providers::cloudflare::clients::AaaAuditLogsV2ResponseCollection;
use crate::providers::cloudflare::clients::AaaComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AaaHistoryComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AaaIdResponse;
use crate::providers::cloudflare::clients::AaaPoliciesComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AaaSchemasApiResponseCommon;
use crate::providers::cloudflare::clients::AaaSchemasResponseCollection;
use crate::providers::cloudflare::clients::AaaSchemasSingleResponse;
use crate::providers::cloudflare::clients::AaaSensitiveIdResponse;
use crate::providers::cloudflare::clients::AaaSilenceComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AaaSilencesComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AaaSingleResponse;
use crate::providers::cloudflare::clients::AaaWebhooksComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AbuseReportsMitigationAppealResult;
use crate::providers::cloudflare::clients::AbuseReportsSubmitReportResponse;
use crate::providers::cloudflare::clients::AccessAccessRequestsComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AccessActiveSessionResponse;
use crate::providers::cloudflare::clients::AccessActiveSessionsResponse;
use crate::providers::cloudflare::clients::AccessAppPoliciesComponentsSchemasIdResponse;
use crate::providers::cloudflare::clients::AccessAppPoliciesComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AccessAppPoliciesComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::AccessAppsComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AccessAppsComponentsSchemasResponseCollection2;
use crate::providers::cloudflare::clients::AccessAppsComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::AccessAppsComponentsSchemasSingleResponse2;
use crate::providers::cloudflare::clients::AccessBookmarksComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AccessBookmarksComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::AccessCaComponentsSchemasIdResponse;
use crate::providers::cloudflare::clients::AccessCaComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AccessCaComponentsSchemasResponseCollection2;
use crate::providers::cloudflare::clients::AccessCaComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::AccessCaComponentsSchemasSingleResponse2;
use crate::providers::cloudflare::clients::AccessCertificatesComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AccessCertificatesComponentsSchemasResponseCollection2;
use crate::providers::cloudflare::clients::AccessCertificatesComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::AccessCertificatesComponentsSchemasSingleResponse2;
use crate::providers::cloudflare::clients::AccessComponentsSchemasIdResponse;
use crate::providers::cloudflare::clients::AccessComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AccessComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::AccessCreateResponse;
use crate::providers::cloudflare::clients::AccessCustomPagesComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AccessCustomPagesComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::AccessDeleteAuthenticatorResponse;
use crate::providers::cloudflare::clients::AccessDeleteUserResponse;
use crate::providers::cloudflare::clients::AccessEmptyResponse;
use crate::providers::cloudflare::clients::AccessFailedLoginResponse;
use crate::providers::cloudflare::clients::AccessGatewayCaComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AccessGatewayCaComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::AccessGroupsComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AccessGroupsComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::AccessGroupsComponentsSchemasSingleResponse2;
use crate::providers::cloudflare::clients::AccessIdResponse;
use crate::providers::cloudflare::clients::AccessIdentityProvidersComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AccessIdentityProvidersComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::AccessKeysComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::AccessLastSeenIdentityResponse;
use crate::providers::cloudflare::clients::AccessNameResponse;
use crate::providers::cloudflare::clients::AccessOrganizationsComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::AccessPoliciesComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AccessPoliciesComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::AccessPolicyCheckResponse;
use crate::providers::cloudflare::clients::AccessPolicyInitResp;
use crate::providers::cloudflare::clients::AccessPolicyUpdateResp;
use crate::providers::cloudflare::clients::AccessPolicyUsersResp;
use crate::providers::cloudflare::clients::AccessResponseCollection;
use crate::providers::cloudflare::clients::AccessResponseCollectionHostnames;
use crate::providers::cloudflare::clients::AccessReusablePoliciesComponentsSchemasIdResponse;
use crate::providers::cloudflare::clients::AccessReusablePoliciesComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AccessReusablePoliciesComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::AccessSchemasCreateResponse;
use crate::providers::cloudflare::clients::AccessSchemasEmptyResponse;
use crate::providers::cloudflare::clients::AccessSchemasIdResponse;
use crate::providers::cloudflare::clients::AccessSchemasPolicyCheckResponse;
use crate::providers::cloudflare::clients::AccessSchemasResponseCollection;
use crate::providers::cloudflare::clients::AccessSchemasResponseCollectionHostnames;
use crate::providers::cloudflare::clients::AccessSchemasSingleResponse;
use crate::providers::cloudflare::clients::AccessScimGroupsResponse;
use crate::providers::cloudflare::clients::AccessScimUpdateLogsResponse;
use crate::providers::cloudflare::clients::AccessScimUsersResponse;
use crate::providers::cloudflare::clients::AccessSeatsComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AccessServiceTokensComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AccessServiceTokensComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::AccessSingleResponse;
use crate::providers::cloudflare::clients::AccessSingleResponseWithoutHtml;
use crate::providers::cloudflare::clients::AccessSingleUserResponse;
use crate::providers::cloudflare::clients::AccessTagsComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AccessTagsComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::AccessUsersComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AddressingAdvertisedResponse;
use crate::providers::cloudflare::clients::AddressingApiResponseCollection;
use crate::providers::cloudflare::clients::AddressingApiResponseCommon;
use crate::providers::cloudflare::clients::AddressingApiResponseSingle;
use crate::providers::cloudflare::clients::AddressingComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AddressingComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::AddressingFullResponse;
use crate::providers::cloudflare::clients::AddressingIdResponse;
use crate::providers::cloudflare::clients::AddressingLeasesComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::AddressingLoaUploadResponse;
use crate::providers::cloudflare::clients::AddressingResponseCollection;
use crate::providers::cloudflare::clients::AddressingResponseCollectionBgp;
use crate::providers::cloudflare::clients::AddressingSchemasResponseCollection;
use crate::providers::cloudflare::clients::AddressingSchemasSingleResponse;
use crate::providers::cloudflare::clients::AddressingSingleResponse;
use crate::providers::cloudflare::clients::AddressingSingleResponseBgp;
use crate::providers::cloudflare::clients::AlexandriaGetApplicationResponse;
use crate::providers::cloudflare::clients::AlexandriaGetApplicationsResponse;
use crate::providers::cloudflare::clients::AlexandriaGetCategoriesResponse;
use crate::providers::cloudflare::clients::AlexandriaGetCategoryResponse;
use crate::providers::cloudflare::clients::ApiShieldApiResponseCommon;
use crate::providers::cloudflare::clients::ApiShieldApiResponseSingle;
use crate::providers::cloudflare::clients::ApiShieldMultipleOperationResponse;
use crate::providers::cloudflare::clients::ApiShieldMultipleOperationResponsePaginated;
use crate::providers::cloudflare::clients::ApiShieldOldOperationSchemaValidationSettings;
use crate::providers::cloudflare::clients::ApiShieldOldZoneSchemaValidationSettings;
use crate::providers::cloudflare::clients::ApiShieldPatchDiscoveriesResponse;
use crate::providers::cloudflare::clients::ApiShieldPatchDiscoveryResponse;
use crate::providers::cloudflare::clients::ApiShieldSchemaResponseDiscovery;
use crate::providers::cloudflare::clients::ApiShieldSchemaResponseWithThresholds;
use crate::providers::cloudflare::clients::ApiShieldSingleOperationResponse;
use crate::providers::cloudflare::clients::ArgoAnalyticsResponseSingle;
use crate::providers::cloudflare::clients::ArgoConfigApiResponseSingle;
use crate::providers::cloudflare::clients::BillSubsApiAccountSubscriptionResponseCollection;
use crate::providers::cloudflare::clients::BillSubsApiAccountSubscriptionResponseSingle;
use crate::providers::cloudflare::clients::BillSubsApiBillingHistoryCollection;
use crate::providers::cloudflare::clients::BillSubsApiBillingResponseSingle;
use crate::providers::cloudflare::clients::BillSubsApiPlanResponseCollection;
use crate::providers::cloudflare::clients::BillSubsApiUserSubscriptionResponseCollection;
use crate::providers::cloudflare::clients::BillSubsApiUserSubscriptionResponseSingle;
use crate::providers::cloudflare::clients::BillSubsApiZoneSubscriptionResponseSingle;
use crate::providers::cloudflare::clients::BillableUsageApiUsageResponse;
use crate::providers::cloudflare::clients::BotManagementBotManagementResponseBody;
use crate::providers::cloudflare::clients::CacheApiResponseSingleId;
use crate::providers::cloudflare::clients::CachePurgeApiResponseSingleId;
use crate::providers::cloudflare::clients::CacheRulesOriginH2MaxStreamsResponseValue;
use crate::providers::cloudflare::clients::CallsAppResponseCollection;
use crate::providers::cloudflare::clients::CallsAppResponseSingle;
use crate::providers::cloudflare::clients::CallsAppResponseSingleWithSecret;
use crate::providers::cloudflare::clients::CallsTurnKeyCollection;
use crate::providers::cloudflare::clients::CallsTurnKeyResponseSingle;
use crate::providers::cloudflare::clients::CallsTurnKeySingleWithSecret;
use crate::providers::cloudflare::clients::CloudforceOneDeleteAllResponse;
use crate::providers::cloudflare::clients::CloudforceOneRequestsApiResponseCommon;
use crate::providers::cloudflare::clients::CloudforceOneRule;
use crate::providers::cloudflare::clients::CloudforceOneRulesListResponse;
use crate::providers::cloudflare::clients::CloudforceOneStatsResponse;
use crate::providers::cloudflare::clients::CloudforceOneSuccessResponse;
use crate::providers::cloudflare::clients::CloudforceOneTreeResponse;
use crate::providers::cloudflare::clients::CloudforceOneValidationResponse;
use crate::providers::cloudflare::clients::CloudforceOneWhoisSchemasSingleResponse;
use crate::providers::cloudflare::clients::CustomIndicatorFeedsCreateFeedResponse;
use crate::providers::cloudflare::clients::CustomIndicatorFeedsIndicatorFeedMetadataResponse;
use crate::providers::cloudflare::clients::CustomIndicatorFeedsIndicatorFeedResponse;
use crate::providers::cloudflare::clients::CustomIndicatorFeedsPermissionListItemResponse;
use crate::providers::cloudflare::clients::CustomIndicatorFeedsPermissionsResponse;
use crate::providers::cloudflare::clients::CustomIndicatorFeedsUpdateFeedResponse;
use crate::providers::cloudflare::clients::CustomIndicatorFeedsUpdatePublicFieldResponse;
use crate::providers::cloudflare::clients::CustomPagesCustomAssetResult;
use crate::providers::cloudflare::clients::CustomPagesCustomAssetResultList;
use crate::providers::cloudflare::clients::CustomPagesCustomPage;
use crate::providers::cloudflare::clients::CustomPagesCustomPageResult;
use crate::providers::cloudflare::clients::CustomPagesCustomPageResultList;
use crate::providers::cloudflare::clients::DigitalExperienceMonitoringDevice;
use crate::providers::cloudflare::clients::DigitalExperienceMonitoringDexDeleteResponseCollection;
use crate::providers::cloudflare::clients::DigitalExperienceMonitoringDexResponseCollection;
use crate::providers::cloudflare::clients::DigitalExperienceMonitoringDexSingleResponse;
use crate::providers::cloudflare::clients::DigitalExperienceMonitoringFleetStatusDevicesResponse;
use crate::providers::cloudflare::clients::DigitalExperienceMonitoringFleetStatusLiveResponse;
use crate::providers::cloudflare::clients::DigitalExperienceMonitoringFleetStatusOverTimeResponse;
use crate::providers::cloudflare::clients::DnsCustomNameserversAcnsResponseCollection;
use crate::providers::cloudflare::clients::DnsCustomNameserversAcnsResponseSingle;
use crate::providers::cloudflare::clients::DnsCustomNameserversEmptyResponse;
use crate::providers::cloudflare::clients::DnsCustomNameserversGetResponse;
use crate::providers::cloudflare::clients::DnsCustomNameserversSchemasEmptyResponse;
use crate::providers::cloudflare::clients::DnsFirewallDnsFirewallResponseCollection;
use crate::providers::cloudflare::clients::DnsFirewallDnsFirewallReverseDnsResponse;
use crate::providers::cloudflare::clients::DnsFirewallDnsFirewallSingleResponse;
use crate::providers::cloudflare::clients::DnsRecordsDnsResponseAccountUsage;
use crate::providers::cloudflare::clients::DnsRecordsDnsResponseBatch;
use crate::providers::cloudflare::clients::DnsRecordsDnsResponseCollection;
use crate::providers::cloudflare::clients::DnsRecordsDnsResponseImportScan;
use crate::providers::cloudflare::clients::DnsRecordsDnsResponseReviewScan;
use crate::providers::cloudflare::clients::DnsRecordsDnsResponseSingle;
use crate::providers::cloudflare::clients::DnsRecordsDnsResponseTriggerScan;
use crate::providers::cloudflare::clients::DnsRecordsDnsResponseZoneUsage;
use crate::providers::cloudflare::clients::DnsSettingsDnsResponseSingle;
use crate::providers::cloudflare::clients::DnsSettingsDnsViewResponseCollection;
use crate::providers::cloudflare::clients::DnsSettingsDnsViewResponseSingle;
use crate::providers::cloudflare::clients::DnsSettingsSchemasDnsResponseSingle;
use crate::providers::cloudflare::clients::DnssecDeleteDnssecResponseSingle;
use crate::providers::cloudflare::clients::DnssecDnssecResponseSingle;
use crate::providers::cloudflare::clients::DosApiResponseCommon;
use crate::providers::cloudflare::clients::DosDnsProtectionRuleListResponse;
use crate::providers::cloudflare::clients::DosDnsProtectionRuleResponse;
use crate::providers::cloudflare::clients::DosExpressionFilterListResponse;
use crate::providers::cloudflare::clients::DosExpressionFilterResponse;
use crate::providers::cloudflare::clients::DosInfraPrefixListResponse;
use crate::providers::cloudflare::clients::DosInfraPrefixResponse;
use crate::providers::cloudflare::clients::DosPrefixListResponse;
use crate::providers::cloudflare::clients::DosPrefixResponse;
use crate::providers::cloudflare::clients::DosProtectionStatusResponse;
use crate::providers::cloudflare::clients::DosSynProtectionRuleListResponse;
use crate::providers::cloudflare::clients::DosSynProtectionRuleResponse;
use crate::providers::cloudflare::clients::DosTcpFlowProtectionRuleListResponse;
use crate::providers::cloudflare::clients::DosTcpFlowProtectionRuleResponse;
use crate::providers::cloudflare::clients::EmailApiResponseSingle;
use crate::providers::cloudflare::clients::EmailCatchAllRuleResponseSingle;
use crate::providers::cloudflare::clients::EmailDestinationAddressResponseSingle;
use crate::providers::cloudflare::clients::EmailDestinationAddressesResponseCollection;
use crate::providers::cloudflare::clients::EmailDnsSettingsResponseCollection;
use crate::providers::cloudflare::clients::EmailEmailSettingsResponseSingle;
use crate::providers::cloudflare::clients::EmailRuleResponseSingle;
use crate::providers::cloudflare::clients::EmailRulesResponseCollection;
use crate::providers::cloudflare::clients::EmailSendingSubdomainPreviewResponse;
use crate::providers::cloudflare::clients::EmailSendingSubdomainResponseSingle;
use crate::providers::cloudflare::clients::EmailSendingSubdomainsResponseCollection;
use crate::providers::cloudflare::clients::FirewallApiResponseSingleId;
use crate::providers::cloudflare::clients::FirewallFilterDeleteResponseCollection;
use crate::providers::cloudflare::clients::FirewallFilterDeleteResponseSingle;
use crate::providers::cloudflare::clients::FirewallFilterResponseCollection;
use crate::providers::cloudflare::clients::FirewallFilterResponseSingle;
use crate::providers::cloudflare::clients::FirewallFilterRulesResponseCollection;
use crate::providers::cloudflare::clients::FirewallFilterRulesResponseCollectionDelete;
use crate::providers::cloudflare::clients::FirewallFilterRulesSingleResponse;
use crate::providers::cloudflare::clients::FirewallFilterRulesSingleResponseDelete;
use crate::providers::cloudflare::clients::FirewallFirewalluablockResponseCollection;
use crate::providers::cloudflare::clients::FirewallFirewalluablockResponseSingle;
use crate::providers::cloudflare::clients::FirewallOverrideResponseCollection;
use crate::providers::cloudflare::clients::FirewallOverrideResponseSingle;
use crate::providers::cloudflare::clients::FirewallPackageResponseCollection;
use crate::providers::cloudflare::clients::FirewallPackageResponseSingle;
use crate::providers::cloudflare::clients::FirewallRatelimitResponseCollection;
use crate::providers::cloudflare::clients::FirewallRatelimitResponseSingle;
use crate::providers::cloudflare::clients::FirewallResponseCollection;
use crate::providers::cloudflare::clients::FirewallResponseSingle;
use crate::providers::cloudflare::clients::FirewallRuleCollectionResponse;
use crate::providers::cloudflare::clients::FirewallRuleSingleIdResponse;
use crate::providers::cloudflare::clients::FirewallRuleSingleResponse;
use crate::providers::cloudflare::clients::FirewallZonelockdownResponseCollection;
use crate::providers::cloudflare::clients::FirewallZonelockdownResponseSingle;
use crate::providers::cloudflare::clients::FraudFraudSettingsResponseBody;
use crate::providers::cloudflare::clients::HealthchecksIdResponse;
use crate::providers::cloudflare::clients::HealthchecksResponseCollection;
use crate::providers::cloudflare::clients::HealthchecksSingleResponse;
use crate::providers::cloudflare::clients::IamApiResponseSingle;
use crate::providers::cloudflare::clients::IamApiResponseSingleId;
use crate::providers::cloudflare::clients::IamCollectionMemberResponseWithPolicies;
use crate::providers::cloudflare::clients::IamCollectionOrganizationResponse;
use crate::providers::cloudflare::clients::IamCollectionPermissionGroupsResponse;
use crate::providers::cloudflare::clients::IamCollectionRoleResponse;
use crate::providers::cloudflare::clients::IamCollectionTokensResponse;
use crate::providers::cloudflare::clients::IamPermissionsGroupResponseCollection;
use crate::providers::cloudflare::clients::IamResponseCollectionAccounts;
use crate::providers::cloudflare::clients::IamResponseSingleAccount;
use crate::providers::cloudflare::clients::IamResponseSingleValue;
use crate::providers::cloudflare::clients::IamSchemasCollectionInviteResponse;
use crate::providers::cloudflare::clients::IamSingleInviteResponse;
use crate::providers::cloudflare::clients::IamSingleMemberResponseWithPolicies;
use crate::providers::cloudflare::clients::IamSingleMembershipResponseWithPolicies;
use crate::providers::cloudflare::clients::IamSingleOrganizationResponse;
use crate::providers::cloudflare::clients::IamSinglePermissionGroupsResponse;
use crate::providers::cloudflare::clients::IamSingleRoleResponse;
use crate::providers::cloudflare::clients::IamSingleTokenCreateResponse;
use crate::providers::cloudflare::clients::IamSingleTokenResponse;
use crate::providers::cloudflare::clients::IamSingleUserResponse;
use crate::providers::cloudflare::clients::IamSsoConnectorCollectionResponse;
use crate::providers::cloudflare::clients::IamSsoConnectorResponse;
use crate::providers::cloudflare::clients::IamTokenVerifyResponseSingleSegment;
use crate::providers::cloudflare::clients::ImagesDeletedResponse;
use crate::providers::cloudflare::clients::ImagesImageDirectUploadResponseV2;
use crate::providers::cloudflare::clients::ImagesImageKeyResponseCollection;
use crate::providers::cloudflare::clients::ImagesImageResponseSingle;
use crate::providers::cloudflare::clients::ImagesImageVariantFlatResponse;
use crate::providers::cloudflare::clients::ImagesImageVariantListResponse;
use crate::providers::cloudflare::clients::ImagesImageVariantSimpleResponse;
use crate::providers::cloudflare::clients::ImagesImagesListResponse;
use crate::providers::cloudflare::clients::ImagesImagesListResponseV2;
use crate::providers::cloudflare::clients::ImagesImagesStatsResponse;
use crate::providers::cloudflare::clients::IntelApiResponseSingle;
use crate::providers::cloudflare::clients::IntelAsnComponentsSchemasResponse;
use crate::providers::cloudflare::clients::IntelCollectionResponse;
use crate::providers::cloudflare::clients::IntelComponentsSchemasResponse;
use crate::providers::cloudflare::clients::IntelComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::IntelResponse;
use crate::providers::cloudflare::clients::IntelSchemasResponse;
use crate::providers::cloudflare::clients::IntelSingleResponse;
use crate::providers::cloudflare::clients::IntelSinkholesGetSinkholesResponse;
use crate::providers::cloudflare::clients::KaminoEnvironmentsResponse;
use crate::providers::cloudflare::clients::ListsBulkOperationResponseSingle;
use crate::providers::cloudflare::clients::ListsItemResponseSingle;
use crate::providers::cloudflare::clients::ListsItemsListResponseCollection;
use crate::providers::cloudflare::clients::ListsListDeleteResponseCollection;
use crate::providers::cloudflare::clients::ListsListResponseCollection;
use crate::providers::cloudflare::clients::ListsListsAsyncResponse;
use crate::providers::cloudflare::clients::ListsListsResponseCollection;
use crate::providers::cloudflare::clients::LoadBalancingComponentsSchemasIdResponse;
use crate::providers::cloudflare::clients::LoadBalancingComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::LoadBalancingComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::LoadBalancingHealthDetails;
use crate::providers::cloudflare::clients::LoadBalancingIdResponse;
use crate::providers::cloudflare::clients::LoadBalancingLoadBalancerComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::LoadBalancingLoadBalancerComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::LoadBalancingMonitorGroupReferencesResponse;
use crate::providers::cloudflare::clients::LoadBalancingMonitorGroupResponseCollection;
use crate::providers::cloudflare::clients::LoadBalancingMonitorGroupSingleResponse;
use crate::providers::cloudflare::clients::LoadBalancingMonitorReferencesResponse;
use crate::providers::cloudflare::clients::LoadBalancingMonitorResponseCollection;
use crate::providers::cloudflare::clients::LoadBalancingMonitorResponseSingle;
use crate::providers::cloudflare::clients::LoadBalancingPoolsReferencesResponse;
use crate::providers::cloudflare::clients::LoadBalancingPreviewResponse;
use crate::providers::cloudflare::clients::LoadBalancingPreviewResultResponse;
use crate::providers::cloudflare::clients::LoadBalancingRegionComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::LoadBalancingSchemasIdResponse;
use crate::providers::cloudflare::clients::LoadBalancingSchemasResponseCollection;
use crate::providers::cloudflare::clients::LoadBalancingSchemasSingleResponse;
use crate::providers::cloudflare::clients::LogcontrolCmbConfigResponseSingle;
use crate::providers::cloudflare::clients::LogcontrolRetentionFlagResponseSingle;
use crate::providers::cloudflare::clients::LogpushDestinationExistsResponse;
use crate::providers::cloudflare::clients::LogpushGetOwnershipResponse;
use crate::providers::cloudflare::clients::LogpushInstantLogsJobResponseCollection;
use crate::providers::cloudflare::clients::LogpushInstantLogsJobResponseSingle;
use crate::providers::cloudflare::clients::LogpushLogpushFieldResponseCollection;
use crate::providers::cloudflare::clients::LogpushLogpushJobResponseCollection;
use crate::providers::cloudflare::clients::LogpushLogpushJobResponseSingle;
use crate::providers::cloudflare::clients::LogpushValidateOwnershipResponse;
use crate::providers::cloudflare::clients::LogpushValidateResponse;
use crate::providers::cloudflare::clients::LogshareFieldsResponse;
use crate::providers::cloudflare::clients::LogshareLogsResponseJsonLines;
use crate::providers::cloudflare::clients::MagicAclDeletedResponse;
use crate::providers::cloudflare::clients::MagicAclModifiedResponse;
use crate::providers::cloudflare::clients::MagicAclSingleResponse;
use crate::providers::cloudflare::clients::MagicAclsCollectionResponse;
use crate::providers::cloudflare::clients::MagicAppConfigSingleResponse;
use crate::providers::cloudflare::clients::MagicAppConfigsCollectionResponse;
use crate::providers::cloudflare::clients::MagicAppSingleResponse;
use crate::providers::cloudflare::clients::MagicAppsCollectionResponse;
use crate::providers::cloudflare::clients::MagicComponentsSchemasModifiedTunnelsCollectionResponse;
use crate::providers::cloudflare::clients::MagicComponentsSchemasTunnelModifiedResponse;
use crate::providers::cloudflare::clients::MagicComponentsSchemasTunnelSingleResponse;
use crate::providers::cloudflare::clients::MagicComponentsSchemasTunnelsCollectionResponse;
use crate::providers::cloudflare::clients::MagicCreateGreTunnelResponse;
use crate::providers::cloudflare::clients::MagicCreateRouteResponse;
use crate::providers::cloudflare::clients::MagicLanDeletedResponse;
use crate::providers::cloudflare::clients::MagicLanModifiedResponse;
use crate::providers::cloudflare::clients::MagicLanSingleResponse;
use crate::providers::cloudflare::clients::MagicLansCollectionResponse;
use crate::providers::cloudflare::clients::MagicModifiedTunnelsCollectionResponse;
use crate::providers::cloudflare::clients::MagicMultipleRouteDeleteResponse;
use crate::providers::cloudflare::clients::MagicMultipleRouteModifiedResponse;
use crate::providers::cloudflare::clients::MagicNetflowConfigSingleResponse;
use crate::providers::cloudflare::clients::MagicPskGenerationResponse;
use crate::providers::cloudflare::clients::MagicRouteDeletedResponse;
use crate::providers::cloudflare::clients::MagicRouteModifiedResponse;
use crate::providers::cloudflare::clients::MagicRouteSingleResponse;
use crate::providers::cloudflare::clients::MagicRoutesCollectionResponse;
use crate::providers::cloudflare::clients::MagicSchemasCreateIpsecTunnelResponse;
use crate::providers::cloudflare::clients::MagicSchemasModifiedTunnelsCollectionResponse;
use crate::providers::cloudflare::clients::MagicSchemasTunnelDeletedResponse;
use crate::providers::cloudflare::clients::MagicSchemasTunnelModifiedResponse;
use crate::providers::cloudflare::clients::MagicSchemasTunnelSingleResponse;
use crate::providers::cloudflare::clients::MagicSchemasTunnelsCollectionResponse;
use crate::providers::cloudflare::clients::MagicSiteDeletedResponse;
use crate::providers::cloudflare::clients::MagicSiteModifiedResponse;
use crate::providers::cloudflare::clients::MagicSiteSingleResponse;
use crate::providers::cloudflare::clients::MagicSitesCollectionResponse;
use crate::providers::cloudflare::clients::MagicTransitApiResponseCommon;
use crate::providers::cloudflare::clients::MagicTransitEndpointHealthCheckResponseSingle;
use crate::providers::cloudflare::clients::MagicTransitTracerouteResponseCollection;
use crate::providers::cloudflare::clients::MagicTunnelDeletedResponse;
use crate::providers::cloudflare::clients::MagicTunnelModifiedResponse;
use crate::providers::cloudflare::clients::MagicTunnelSingleResponse;
use crate::providers::cloudflare::clients::MagicTunnelsCollectionResponse;
use crate::providers::cloudflare::clients::MagicVisibilityMnmMnmConfigSingleResponse;
use crate::providers::cloudflare::clients::MagicVisibilityMnmMnmRuleAdvertisementSingleResponse;
use crate::providers::cloudflare::clients::MagicVisibilityMnmMnmRulesCollectionResponse;
use crate::providers::cloudflare::clients::MagicVisibilityMnmMnmRulesSingleResponse;
use crate::providers::cloudflare::clients::MagicVisibilityMnmMnmVpcFlowsSingleResponse;
use crate::providers::cloudflare::clients::MagicVisibilityPcapsPcapsCollectionResponse;
use crate::providers::cloudflare::clients::MagicVisibilityPcapsPcapsOwnershipCollection;
use crate::providers::cloudflare::clients::MagicVisibilityPcapsPcapsOwnershipSingleResponse;
use crate::providers::cloudflare::clients::MagicVisibilityPcapsPcapsSingleResponse;
use crate::providers::cloudflare::clients::MagicWanDeletedResponse;
use crate::providers::cloudflare::clients::MagicWanModifiedResponse;
use crate::providers::cloudflare::clients::MagicWanSingleResponse;
use crate::providers::cloudflare::clients::MagicWansCollectionResponse;
use crate::providers::cloudflare::clients::McnCatalogSyncsPrebuiltPoliciesResponse;
use crate::providers::cloudflare::clients::McnCreateCatalogSyncResponse;
use crate::providers::cloudflare::clients::McnCreateOnrampResponse;
use crate::providers::cloudflare::clients::McnCreateProviderResponse;
use crate::providers::cloudflare::clients::McnDeleteCatalogSyncResponse;
use crate::providers::cloudflare::clients::McnDeleteOnrampResponse;
use crate::providers::cloudflare::clients::McnDeleteProviderResponse;
use crate::providers::cloudflare::clients::McnGetMagicWanAddressSpaceResponse;
use crate::providers::cloudflare::clients::McnGetOnrampResponse;
use crate::providers::cloudflare::clients::McnGoodResponse;
use crate::providers::cloudflare::clients::McnListOnrampsResponse;
use crate::providers::cloudflare::clients::McnProviderInitialSetupResponse;
use crate::providers::cloudflare::clients::McnReadAccountCatalogSyncResponse;
use crate::providers::cloudflare::clients::McnReadAccountCatalogSyncsResponse;
use crate::providers::cloudflare::clients::McnReadAccountProviderResponse;
use crate::providers::cloudflare::clients::McnReadAccountProvidersResponse;
use crate::providers::cloudflare::clients::McnReadAccountResourceResponse;
use crate::providers::cloudflare::clients::McnReadAccountResourcesResponse;
use crate::providers::cloudflare::clients::McnRefreshCatalogSyncResponse;
use crate::providers::cloudflare::clients::McnResourcesCatalogPolicyPreviewResponse;
use crate::providers::cloudflare::clients::McnUpdateCatalogSyncResponse;
use crate::providers::cloudflare::clients::McnUpdateMagicWanAddressSpaceResponse;
use crate::providers::cloudflare::clients::McnUpdateOnrampResponse;
use crate::providers::cloudflare::clients::McnUpdateProviderResponse;
use crate::providers::cloudflare::clients::MconnCustomerConnectorCreateResponse;
use crate::providers::cloudflare::clients::MconnCustomerConnectorDeleteResponse;
use crate::providers::cloudflare::clients::MconnCustomerConnectorFetchResponse;
use crate::providers::cloudflare::clients::MconnCustomerConnectorListResponse;
use crate::providers::cloudflare::clients::MconnCustomerConnectorUpdateResponse;
use crate::providers::cloudflare::clients::MconnCustomerEventGetSuccess;
use crate::providers::cloudflare::clients::MconnCustomerEventsGetLatestSuccess;
use crate::providers::cloudflare::clients::MconnCustomerEventsGetSuccess;
use crate::providers::cloudflare::clients::MconnCustomerSnapshotGetSuccess;
use crate::providers::cloudflare::clients::MconnCustomerSnapshotsGetLatestSuccess;
use crate::providers::cloudflare::clients::MconnCustomerSnapshotsGetSuccess;
use crate::providers::cloudflare::clients::MqApiV4Success;
use crate::providers::cloudflare::clients::NscCni;
use crate::providers::cloudflare::clients::NscCniList;
use crate::providers::cloudflare::clients::NscInterconnect;
use crate::providers::cloudflare::clients::NscInterconnectList;
use crate::providers::cloudflare::clients::NscSettings;
use crate::providers::cloudflare::clients::NscSlotInfo;
use crate::providers::cloudflare::clients::NscSlotList;
use crate::providers::cloudflare::clients::NscStatusInfo;
use crate::providers::cloudflare::clients::ObservatoryAvailabilitiesResponse;
use crate::providers::cloudflare::clients::ObservatoryCountResponse;
use crate::providers::cloudflare::clients::ObservatoryCreateScheduleResponse;
use crate::providers::cloudflare::clients::ObservatoryPageTestResponseCollection;
use crate::providers::cloudflare::clients::ObservatoryPageTestResponseSingle;
use crate::providers::cloudflare::clients::ObservatoryPagesResponseCollection;
use crate::providers::cloudflare::clients::ObservatoryScheduleResponseSingle;
use crate::providers::cloudflare::clients::ObservatoryTrendResponse;
use crate::providers::cloudflare::clients::OrganizationsApiProfileResponse;
use crate::providers::cloudflare::clients::PageShieldGetZoneConnectionResponse;
use crate::providers::cloudflare::clients::PageShieldGetZoneCookieResponse;
use crate::providers::cloudflare::clients::PageShieldGetZonePolicyResponse;
use crate::providers::cloudflare::clients::PageShieldGetZoneScriptResponse;
use crate::providers::cloudflare::clients::PageShieldListZoneConnectionsResponse;
use crate::providers::cloudflare::clients::PageShieldListZoneCookiesResponse;
use crate::providers::cloudflare::clients::PageShieldListZonePoliciesResponse;
use crate::providers::cloudflare::clients::PageShieldListZoneScriptsResponse;
use crate::providers::cloudflare::clients::PayPerCrawlApiNoResultResponse;
use crate::providers::cloudflare::clients::PayPerCrawlCreateStripeConfigResponse;
use crate::providers::cloudflare::clients::PayPerCrawlGetConfigResponse;
use crate::providers::cloudflare::clients::PayPerCrawlGetStripeConfigResponse;
use crate::providers::cloudflare::clients::PayPerCrawlQueryZonesCanBeEnabledResponse;
use crate::providers::cloudflare::clients::R2V4Response;
use crate::providers::cloudflare::clients::RealtimekitWebhookSuccessResponse;
use crate::providers::cloudflare::clients::RealtimekitWebhooksListSuccessResponse;
use crate::providers::cloudflare::clients::RegistrarApiDomainResponseCollection;
use crate::providers::cloudflare::clients::RegistrarApiDomainResponseSingle;
use crate::providers::cloudflare::clients::ResourceSharingShareRecipientResponseCollection;
use crate::providers::cloudflare::clients::ResourceSharingShareRecipientResponseSingle;
use crate::providers::cloudflare::clients::ResourceSharingShareResourceResponseCollection;
use crate::providers::cloudflare::clients::ResourceSharingShareResourceResponseSingle;
use crate::providers::cloudflare::clients::ResourceSharingShareResponseCollection;
use crate::providers::cloudflare::clients::ResourceSharingShareResponseSingle;
use crate::providers::cloudflare::clients::ResourceTaggingTagKeysResponseCollection;
use crate::providers::cloudflare::clients::ResourceTaggingTagValuesResponseCollection;
use crate::providers::cloudflare::clients::ResourceTaggingTaggedResourceResponseCollection;
use crate::providers::cloudflare::clients::ResourceTaggingTaggedResourceResponseSingle;
use crate::providers::cloudflare::clients::RumRuleIdResponseSingle;
use crate::providers::cloudflare::clients::RumRuleResponseSingle;
use crate::providers::cloudflare::clients::RumRulesResponseCollection;
use crate::providers::cloudflare::clients::RumRumSiteResponseSingle;
use crate::providers::cloudflare::clients::RumSiteResponseSingle;
use crate::providers::cloudflare::clients::RumSiteTagResponseSingle;
use crate::providers::cloudflare::clients::RumSitesResponseCollection;
use crate::providers::cloudflare::clients::SecondaryDnsComponentsSchemasIdResponse;
use crate::providers::cloudflare::clients::SecondaryDnsComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::SecondaryDnsComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::SecondaryDnsDisableTransferResponse;
use crate::providers::cloudflare::clients::SecondaryDnsEnableTransferResponse;
use crate::providers::cloudflare::clients::SecondaryDnsForceResponse;
use crate::providers::cloudflare::clients::SecondaryDnsIdResponse;
use crate::providers::cloudflare::clients::SecondaryDnsResponseCollection;
use crate::providers::cloudflare::clients::SecondaryDnsSchemasForceResponse;
use crate::providers::cloudflare::clients::SecondaryDnsSchemasIdResponse;
use crate::providers::cloudflare::clients::SecondaryDnsSchemasResponseCollection;
use crate::providers::cloudflare::clients::SecondaryDnsSchemasSingleResponse;
use crate::providers::cloudflare::clients::SecondaryDnsSingleResponse;
use crate::providers::cloudflare::clients::SecondaryDnsSingleResponseIncoming;
use crate::providers::cloudflare::clients::SecondaryDnsSingleResponseOutgoing;
use crate::providers::cloudflare::clients::SecretsStoreQuotaResponse;
use crate::providers::cloudflare::clients::SecretsStoreSecretResponse;
use crate::providers::cloudflare::clients::SecretsStoreSecretsResponseCollection;
use crate::providers::cloudflare::clients::SecretsStoreStoreResponse;
use crate::providers::cloudflare::clients::SecretsStoreStoresResponseCollection;
use crate::providers::cloudflare::clients::SecurityCenterValueCountsResponse;
use crate::providers::cloudflare::clients::SmartshieldResponseCollection;
use crate::providers::cloudflare::clients::SmartshieldSingleHcIdResponse;
use crate::providers::cloudflare::clients::SmartshieldSingleHcResponse;
use crate::providers::cloudflare::clients::SpectrumAnalyticsQueryResponseAggregate;
use crate::providers::cloudflare::clients::SpectrumAnalyticsQueryResponseSingle;
use crate::providers::cloudflare::clients::SpectrumConfigApiResponseSingleId;
use crate::providers::cloudflare::clients::SpectrumConfigAppConfigCollection;
use crate::providers::cloudflare::clients::SpectrumConfigAppConfigSingle;
use crate::providers::cloudflare::clients::StreamAddAudioTrackResponse;
use crate::providers::cloudflare::clients::StreamClipResponseSingle;
use crate::providers::cloudflare::clients::StreamDeletedResponse;
use crate::providers::cloudflare::clients::StreamDirectUploadResponse;
use crate::providers::cloudflare::clients::StreamDownloadsResponse;
use crate::providers::cloudflare::clients::StreamDownloadsResponseSingle;
use crate::providers::cloudflare::clients::StreamKeyGenerationResponse;
use crate::providers::cloudflare::clients::StreamKeyResponseCollection;
use crate::providers::cloudflare::clients::StreamLanguageResponseCollection;
use crate::providers::cloudflare::clients::StreamLanguageResponseSingle;
use crate::providers::cloudflare::clients::StreamListAudioTrackResponse;
use crate::providers::cloudflare::clients::StreamLiveInputResponseCollection;
use crate::providers::cloudflare::clients::StreamLiveInputResponseSingle;
use crate::providers::cloudflare::clients::StreamOutputResponseCollection;
use crate::providers::cloudflare::clients::StreamOutputResponseSingle;
use crate::providers::cloudflare::clients::StreamSignedTokenResponse;
use crate::providers::cloudflare::clients::StreamStorageUseResponse;
use crate::providers::cloudflare::clients::StreamVideoResponseCollection;
use crate::providers::cloudflare::clients::StreamVideoResponseSingle;
use crate::providers::cloudflare::clients::StreamWatermarkResponseCollection;
use crate::providers::cloudflare::clients::StreamWatermarkResponseSingle;
use crate::providers::cloudflare::clients::StreamWebhookResponseSingle;
use crate::providers::cloudflare::clients::TeamsDevicesApiResponseSingle;
use crate::providers::cloudflare::clients::TeamsDevicesComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::TeamsDevicesComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::TeamsDevicesDefaultDeviceSettingsResponse;
use crate::providers::cloudflare::clients::TeamsDevicesDeviceResponse;
use crate::providers::cloudflare::clients::TeamsDevicesDeviceSettingsResponse;
use crate::providers::cloudflare::clients::TeamsDevicesDeviceSettingsResponseCollection;
use crate::providers::cloudflare::clients::TeamsDevicesDevicesPolicyCertificatesSingle;
use crate::providers::cloudflare::clients::TeamsDevicesDevicesResponse;
use crate::providers::cloudflare::clients::TeamsDevicesFallbackDomainResponseCollection;
use crate::providers::cloudflare::clients::TeamsDevicesGlobalWarpOverrideResponse;
use crate::providers::cloudflare::clients::TeamsDevicesIdResponse;
use crate::providers::cloudflare::clients::TeamsDevicesOverrideCodesResponse;
use crate::providers::cloudflare::clients::TeamsDevicesResponseCollection;
use crate::providers::cloudflare::clients::TeamsDevicesSchemasIdResponse;
use crate::providers::cloudflare::clients::TeamsDevicesSchemasResponseCollection;
use crate::providers::cloudflare::clients::TeamsDevicesSchemasSingleResponse;
use crate::providers::cloudflare::clients::TeamsDevicesSingleResponse;
use crate::providers::cloudflare::clients::TeamsDevicesSplitTunnelIncludeResponseCollection;
use crate::providers::cloudflare::clients::TeamsDevicesSplitTunnelResponseCollection;
use crate::providers::cloudflare::clients::TeamsDevicesZeroTrustAccountDeviceSettingsResponse;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesAdvancedCertificatePackResponseSingle;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesAssociationResponseCollection;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesCertificateAnalyzeResponse;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesCertificatePackQuotaResponse;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesCertificatePackResponseCollection;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesCertificatePackResponseSingle;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesCertificateResponseCollection;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesCertificateResponseIdOnly;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesCertificateResponseSingle;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesCertificateResponseSinglePost;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesCertificateRevokeResponse;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesClientCertificateResponseCollection;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesClientCertificateResponseSingle;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesComponentsSchemasCertificateResponseCollection;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesComponentsSchemasCertificateResponseSingle;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesCustomHostnameResponseCollection;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesCustomHostnameResponseSingle;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesCustomTrustStoreResponseCollection;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesCustomTrustStoreResponseIdOnly;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesCustomTrustStoreResponseSingle;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesDcvDelegationResponse;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesDeleteAdvancedCertificatePackResponseSingle;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesEnabledResponse;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesFallbackOriginResponse;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesHostnameAopResponseCollection;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesHostnameAopSingleResponse;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesHostnameAssocResponseCollection;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesHostnameAssociationsResponse;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesHostnameAuthenticatedOriginPullComponentsSchemasCertificateResponseCollection;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesHostnameAuthenticatedOriginPullComponentsSchemasCertificateResponseSingle;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesKeylessResponseCollection;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesKeylessResponseSingle;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesKeylessResponseSingleId;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesMtlsManagementComponentsSchemasCertificateResponseCollection;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesMtlsManagementComponentsSchemasCertificateResponseSingle;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesPerHostnameSettingsResponse;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesPerHostnameSettingsResponseCollection;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesPerHostnameSettingsResponseDelete;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesSchemasCertificateResponseCollection;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesSchemasCertificateResponseSingle;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesSslUniversalSettingsResponse;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesSslValidationMethodResponseCollection;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesSslVerificationResponseCollection;
use crate::providers::cloudflare::clients::TlsCertificatesAndHostnamesTotalTlsSettingsResponse;
use crate::providers::cloudflare::clients::TunnelCfdTunnelResponseCollection;
use crate::providers::cloudflare::clients::TunnelCfdTunnelResponseSingle;
use crate::providers::cloudflare::clients::TunnelConfigurationResponse;
use crate::providers::cloudflare::clients::TunnelEmptyResponse;
use crate::providers::cloudflare::clients::TunnelHostnameRouteResponseCollection;
use crate::providers::cloudflare::clients::TunnelHostnameRouteResponseSingle;
use crate::providers::cloudflare::clients::TunnelRouteResponseSingle;
use crate::providers::cloudflare::clients::TunnelSubnetResponseCollection;
use crate::providers::cloudflare::clients::TunnelSubnetResponseSingle;
use crate::providers::cloudflare::clients::TunnelSubnetResponseSingleNullable;
use crate::providers::cloudflare::clients::TunnelTeamnetResponseCollection;
use crate::providers::cloudflare::clients::TunnelTeamnetResponseSingle;
use crate::providers::cloudflare::clients::TunnelTunnelClientResponse;
use crate::providers::cloudflare::clients::TunnelTunnelConnectionsResponse;
use crate::providers::cloudflare::clients::TunnelTunnelResponseCollection;
use crate::providers::cloudflare::clients::TunnelTunnelResponseToken;
use crate::providers::cloudflare::clients::TunnelTunnelWarpConnectorClientResponse;
use crate::providers::cloudflare::clients::TunnelTunnelWarpConnectorConnectionsResponse;
use crate::providers::cloudflare::clients::TunnelVnetResponseCollection;
use crate::providers::cloudflare::clients::TunnelVnetResponseSingle;
use crate::providers::cloudflare::clients::TunnelWarpConnectorResponseCollection;
use crate::providers::cloudflare::clients::TunnelWarpConnectorResponseSingle;
use crate::providers::cloudflare::clients::TunnelZeroTrustConnectivitySettingsResponse;
use crate::providers::cloudflare::clients::WafManagedRulesRuleGroupResponseCollection;
use crate::providers::cloudflare::clients::WafManagedRulesRuleGroupResponseSingle;
use crate::providers::cloudflare::clients::WafManagedRulesRuleResponseCollection;
use crate::providers::cloudflare::clients::WafManagedRulesRuleResponseSingle;
use crate::providers::cloudflare::clients::WafProductApiBundleApiResponseCommon;
use crate::providers::cloudflare::clients::WafProductApiBundleResponseCustomDetection;
use crate::providers::cloudflare::clients::WafProductApiBundleResponseCustomDetectionCollection;
use crate::providers::cloudflare::clients::WafProductApiBundleResponseCustomScanCollection;
use crate::providers::cloudflare::clients::WafProductApiBundleResponseCustomTopics;
use crate::providers::cloudflare::clients::WafProductApiBundleResponseSettings;
use crate::providers::cloudflare::clients::WafProductApiBundleResponseStatus;
use crate::providers::cloudflare::clients::WafProductApiBundleSchemasApiResponseCommon;
use crate::providers::cloudflare::clients::WafProductApiBundleSchemasResponseStatus;
use crate::providers::cloudflare::clients::WaitingroomEventDetailsResponse;
use crate::providers::cloudflare::clients::WaitingroomEventIdResponse;
use crate::providers::cloudflare::clients::WaitingroomEventResponse;
use crate::providers::cloudflare::clients::WaitingroomEventResponseCollection;
use crate::providers::cloudflare::clients::WaitingroomPreviewResponse;
use crate::providers::cloudflare::clients::WaitingroomResponseCollection;
use crate::providers::cloudflare::clients::WaitingroomRulesResponseCollection;
use crate::providers::cloudflare::clients::WaitingroomSingleResponse;
use crate::providers::cloudflare::clients::WaitingroomStatusResponse;
use crate::providers::cloudflare::clients::WaitingroomWaitingRoomIdResponse;
use crate::providers::cloudflare::clients::WaitingroomZoneSettingsResponse;
use crate::providers::cloudflare::clients::Web3ApiResponseSingleId;
use crate::providers::cloudflare::clients::Web3CollectionResponse;
use crate::providers::cloudflare::clients::Web3ContentListDetailsResponse;
use crate::providers::cloudflare::clients::Web3ContentListEntryCollectionResponse;
use crate::providers::cloudflare::clients::Web3ContentListEntrySingleResponse;
use crate::providers::cloudflare::clients::Web3SingleResponse;
use crate::providers::cloudflare::clients::WorkersApiResponseCommon;
use crate::providers::cloudflare::clients::WorkersApiResponseNullResult;
use crate::providers::cloudflare::clients::WorkersCompletedUploadAssetsResponse;
use crate::providers::cloudflare::clients::WorkersCreateAssetsUploadSessionResponse;
use crate::providers::cloudflare::clients::WorkersKvApiResponseCommonNoResult;
use crate::providers::cloudflare::clients::WorkersNamespaceListResponse;
use crate::providers::cloudflare::clients::WorkersNamespaceScriptDeleteBulkResponse;
use crate::providers::cloudflare::clients::WorkersNamespaceScriptResponseSingle;
use crate::providers::cloudflare::clients::WorkersNamespaceSingleResponse;
use crate::providers::cloudflare::clients::WorkersScriptAndVersionSettingsResponse;
use crate::providers::cloudflare::clients::WorkersScriptResponseCollection;
use crate::providers::cloudflare::clients::WorkersScriptResponseSingle;
use crate::providers::cloudflare::clients::WorkersScriptSettingsResponse;
use crate::providers::cloudflare::clients::WorkersUsageModelResponse;
use crate::providers::cloudflare::clients::WorkersVersionsListResponse;
use crate::providers::cloudflare::clients::WorkersVersionsSingleResponse;
use crate::providers::cloudflare::clients::WorkersVersionsUploadResponse;
use crate::providers::cloudflare::clients::ZarazZarazConfigHistoryResponse;
use crate::providers::cloudflare::clients::ZarazZarazConfigResponse;
use crate::providers::cloudflare::clients::ZarazZarazConfigReturn;
use crate::providers::cloudflare::clients::ZarazZarazHistoryResponse;
use crate::providers::cloudflare::clients::ZarazZarazWorkflowResponse;
use crate::providers::cloudflare::clients::ZeroTrustGatewayAppTypesComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::ZeroTrustGatewayApplicationsReviewStatusResponse;
use crate::providers::cloudflare::clients::ZeroTrustGatewayAuditSshSettingsComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::ZeroTrustGatewayCategoriesComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::ZeroTrustGatewayComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::ZeroTrustGatewayComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::ZeroTrustGatewayCustomCertificateSettings;
use crate::providers::cloudflare::clients::ZeroTrustGatewayEmptyResponse;
use crate::providers::cloudflare::clients::ZeroTrustGatewayGatewayAccount;
use crate::providers::cloudflare::clients::ZeroTrustGatewayGatewayAccountConfig;
use crate::providers::cloudflare::clients::ZeroTrustGatewayGatewayAccountLoggingSettingsResponse;
use crate::providers::cloudflare::clients::ZeroTrustGatewayListItemResponseCollection;
use crate::providers::cloudflare::clients::ZeroTrustGatewayListSingleResponse;
use crate::providers::cloudflare::clients::ZeroTrustGatewayPacfilesComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::ZeroTrustGatewayPacfilesComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::ZeroTrustGatewayProxyEndpointsComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::ZeroTrustGatewayProxyEndpointsComponentsSchemasSingleResponse;
use crate::providers::cloudflare::clients::ZeroTrustGatewayResponseCollection;
use crate::providers::cloudflare::clients::ZeroTrustGatewayRulesComponentsSchemasResponseCollection;
use crate::providers::cloudflare::clients::ZeroTrustGatewaySchemasResponseCollection;
use crate::providers::cloudflare::clients::ZeroTrustGatewaySchemasSingleResponse;
use crate::providers::cloudflare::clients::ZeroTrustGatewaySingleResponse;
use crate::providers::cloudflare::clients::ZeroTrustGatewaySingleResponseWithListItems;
use crate::providers::cloudflare::clients::ZoneAnalyticsApiColoResponse;
use crate::providers::cloudflare::clients::ZoneAnalyticsApiDashboardResponse;
use crate::providers::cloudflare::clients::ZonesApiResponseSingleId;
use crate::providers::cloudflare::clients::ZonesSchemasApiResponseSingleId;
use crate::providers::cloudflare::clients::ZonesZoneSettingsResponseCollection;
use crate::providers::cloudflare::clients::AccessApplicationsAddAnApplicationArgs;
use crate::providers::cloudflare::clients::AccessApplicationsDeleteAnAccessApplicationArgs;
use crate::providers::cloudflare::clients::AccessApplicationsGetAnAccessApplicationArgs;
use crate::providers::cloudflare::clients::AccessApplicationsListAccessApplicationsArgs;
use crate::providers::cloudflare::clients::AccessApplicationsPatchUpdateAccessApplicationSettingsArgs;
use crate::providers::cloudflare::clients::AccessApplicationsPutUpdateAccessApplicationSettingsArgs;
use crate::providers::cloudflare::clients::AccessApplicationsRevokeServiceTokensArgs;
use crate::providers::cloudflare::clients::AccessApplicationsTestAccessPoliciesArgs;
use crate::providers::cloudflare::clients::AccessApplicationsUpdateAnAccessApplicationArgs;
use crate::providers::cloudflare::clients::AccessAuthenticationLogsGetAccessAuthenticationLogsArgs;
use crate::providers::cloudflare::clients::AccessBookmarkApplicationsDeprecatedCreateABookmarkApplicationArgs;
use crate::providers::cloudflare::clients::AccessBookmarkApplicationsDeprecatedDeleteABookmarkApplicationArgs;
use crate::providers::cloudflare::clients::AccessBookmarkApplicationsDeprecatedGetABookmarkApplicationArgs;
use crate::providers::cloudflare::clients::AccessBookmarkApplicationsDeprecatedListBookmarkApplicationsArgs;
use crate::providers::cloudflare::clients::AccessBookmarkApplicationsDeprecatedUpdateABookmarkApplicationArgs;
use crate::providers::cloudflare::clients::AccessCustomPagesCreateACustomPageArgs;
use crate::providers::cloudflare::clients::AccessCustomPagesDeleteACustomPageArgs;
use crate::providers::cloudflare::clients::AccessCustomPagesGetACustomPageArgs;
use crate::providers::cloudflare::clients::AccessCustomPagesListCustomPagesArgs;
use crate::providers::cloudflare::clients::AccessCustomPagesUpdateACustomPageArgs;
use crate::providers::cloudflare::clients::AccessGatewayCaAddAnSshCaArgs;
use crate::providers::cloudflare::clients::AccessGatewayCaDeleteAnSshCaArgs;
use crate::providers::cloudflare::clients::AccessGatewayCaListSshCaArgs;
use crate::providers::cloudflare::clients::AccessGroupsCreateAnAccessGroupArgs;
use crate::providers::cloudflare::clients::AccessGroupsDeleteAnAccessGroupArgs;
use crate::providers::cloudflare::clients::AccessGroupsGetAnAccessGroupArgs;
use crate::providers::cloudflare::clients::AccessGroupsListAccessGroupsArgs;
use crate::providers::cloudflare::clients::AccessGroupsUpdateAnAccessGroupArgs;
use crate::providers::cloudflare::clients::AccessIdentityProvidersAddAnAccessIdentityProviderArgs;
use crate::providers::cloudflare::clients::AccessIdentityProvidersDeleteAnAccessIdentityProviderArgs;
use crate::providers::cloudflare::clients::AccessIdentityProvidersGetAnAccessIdentityProviderArgs;
use crate::providers::cloudflare::clients::AccessIdentityProvidersListAccessIdentityProvidersArgs;
use crate::providers::cloudflare::clients::AccessIdentityProvidersListScimGroupResourcesArgs;
use crate::providers::cloudflare::clients::AccessIdentityProvidersListScimUserResourcesArgs;
use crate::providers::cloudflare::clients::AccessIdentityProvidersUpdateAnAccessIdentityProviderArgs;
use crate::providers::cloudflare::clients::AccessKeyConfigurationGetTheAccessKeyConfigurationArgs;
use crate::providers::cloudflare::clients::AccessKeyConfigurationRotateAccessKeysArgs;
use crate::providers::cloudflare::clients::AccessKeyConfigurationUpdateTheAccessKeyConfigurationArgs;
use crate::providers::cloudflare::clients::AccessMtlsAuthenticationAddAnMtlsCertificateArgs;
use crate::providers::cloudflare::clients::AccessMtlsAuthenticationDeleteAnMtlsCertificateArgs;
use crate::providers::cloudflare::clients::AccessMtlsAuthenticationGetAnMtlsCertificateArgs;
use crate::providers::cloudflare::clients::AccessMtlsAuthenticationListMtlsCertificatesArgs;
use crate::providers::cloudflare::clients::AccessMtlsAuthenticationListMtlsCertificatesHostnameSettingsArgs;
use crate::providers::cloudflare::clients::AccessMtlsAuthenticationUpdateAnMtlsCertificateArgs;
use crate::providers::cloudflare::clients::AccessMtlsAuthenticationUpdateAnMtlsCertificateSettingsArgs;
use crate::providers::cloudflare::clients::AccessPoliciesConvertReusableArgs;
use crate::providers::cloudflare::clients::AccessPoliciesCreateAnAccessPolicyArgs;
use crate::providers::cloudflare::clients::AccessPoliciesCreateAnAccessReusablePolicyArgs;
use crate::providers::cloudflare::clients::AccessPoliciesDeleteAnAccessPolicyArgs;
use crate::providers::cloudflare::clients::AccessPoliciesDeleteAnAccessReusablePolicyArgs;
use crate::providers::cloudflare::clients::AccessPoliciesGetAnAccessPolicyArgs;
use crate::providers::cloudflare::clients::AccessPoliciesGetAnAccessReusablePolicyArgs;
use crate::providers::cloudflare::clients::AccessPoliciesListAccessAppPoliciesArgs;
use crate::providers::cloudflare::clients::AccessPoliciesListAccessReusablePoliciesArgs;
use crate::providers::cloudflare::clients::AccessPoliciesUpdateAnAccessPolicyArgs;
use crate::providers::cloudflare::clients::AccessPoliciesUpdateAnAccessReusablePolicyArgs;
use crate::providers::cloudflare::clients::AccessPolicyTestsArgs;
use crate::providers::cloudflare::clients::AccessPolicyTestsGetAUserPageArgs;
use crate::providers::cloudflare::clients::AccessPolicyTestsGetAnUpdateArgs;
use crate::providers::cloudflare::clients::AccessScimUpdateLogsListAccessScimUpdateLogsArgs;
use crate::providers::cloudflare::clients::AccessServiceTokensCreateAServiceTokenArgs;
use crate::providers::cloudflare::clients::AccessServiceTokensDeleteAServiceTokenArgs;
use crate::providers::cloudflare::clients::AccessServiceTokensGetAServiceTokenArgs;
use crate::providers::cloudflare::clients::AccessServiceTokensListServiceTokensArgs;
use crate::providers::cloudflare::clients::AccessServiceTokensRefreshAServiceTokenArgs;
use crate::providers::cloudflare::clients::AccessServiceTokensRotateAServiceTokenArgs;
use crate::providers::cloudflare::clients::AccessServiceTokensUpdateAServiceTokenArgs;
use crate::providers::cloudflare::clients::AccessShortLivedCertificateCAsCreateAShortLivedCertificateCaArgs;
use crate::providers::cloudflare::clients::AccessShortLivedCertificateCAsDeleteAShortLivedCertificateCaArgs;
use crate::providers::cloudflare::clients::AccessShortLivedCertificateCAsGetAShortLivedCertificateCaArgs;
use crate::providers::cloudflare::clients::AccessShortLivedCertificateCAsListShortLivedCertificateCAsArgs;
use crate::providers::cloudflare::clients::AccessTagsCreateTagArgs;
use crate::providers::cloudflare::clients::AccessTagsDeleteATagArgs;
use crate::providers::cloudflare::clients::AccessTagsGetATagArgs;
use crate::providers::cloudflare::clients::AccessTagsListTagsArgs;
use crate::providers::cloudflare::clients::AccessTagsUpdateATagArgs;
use crate::providers::cloudflare::clients::AccountApiTokensCreateTokenArgs;
use crate::providers::cloudflare::clients::AccountApiTokensDeleteTokenArgs;
use crate::providers::cloudflare::clients::AccountApiTokensListPermissionGroupsArgs;
use crate::providers::cloudflare::clients::AccountApiTokensListTokensArgs;
use crate::providers::cloudflare::clients::AccountApiTokensRollTokenArgs;
use crate::providers::cloudflare::clients::AccountApiTokensTokenDetailsArgs;
use crate::providers::cloudflare::clients::AccountApiTokensUpdateTokenArgs;
use crate::providers::cloudflare::clients::AccountApiTokensVerifyTokenArgs;
use crate::providers::cloudflare::clients::AccountBillingProfileDeprecatedBillingProfileDetailsArgs;
use crate::providers::cloudflare::clients::AccountCreationArgs;
use crate::providers::cloudflare::clients::AccountDeletionArgs;
use crate::providers::cloudflare::clients::AccountLevelCustomNameserversAddAccountCustomNameserverArgs;
use crate::providers::cloudflare::clients::AccountLevelCustomNameserversDeleteAccountCustomNameserverArgs;
use crate::providers::cloudflare::clients::AccountLevelCustomNameserversListAccountCustomNameserversArgs;
use crate::providers::cloudflare::clients::AccountLevelCustomNameserversUsageForAZoneGetAccountCustomNameserverRelatedZoneMetadataArgs;
use crate::providers::cloudflare::clients::AccountLevelCustomNameserversUsageForAZoneSetAccountCustomNameserverRelatedZoneMetadataArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerMonitorGroupsCreateMonitorGroupArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerMonitorGroupsDeleteMonitorGroupArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerMonitorGroupsListMonitorGroupReferencesArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerMonitorGroupsListMonitorGroupsArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerMonitorGroupsMonitorGroupDetailsArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerMonitorGroupsPatchMonitorGroupArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerMonitorGroupsUpdateMonitorGroupArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerMonitorsCreateMonitorArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerMonitorsDeleteMonitorArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerMonitorsListMonitorReferencesArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerMonitorsListMonitorsArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerMonitorsMonitorDetailsArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerMonitorsPatchMonitorArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerMonitorsPreviewMonitorArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerMonitorsPreviewResultArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerMonitorsUpdateMonitorArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerPoolsCreatePoolArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerPoolsDeletePoolArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerPoolsListPoolReferencesArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerPoolsListPoolsArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerPoolsPatchPoolArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerPoolsPatchPoolsArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerPoolsPoolDetailsArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerPoolsPoolHealthDetailsArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerPoolsPreviewPoolArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerPoolsUpdatePoolArgs;
use crate::providers::cloudflare::clients::AccountLoadBalancerSearchSearchResourcesArgs;
use crate::providers::cloudflare::clients::AccountMembersAddMemberArgs;
use crate::providers::cloudflare::clients::AccountMembersListMembersArgs;
use crate::providers::cloudflare::clients::AccountMembersMemberDetailsArgs;
use crate::providers::cloudflare::clients::AccountMembersRemoveMemberArgs;
use crate::providers::cloudflare::clients::AccountMembersUpdateMemberArgs;
use crate::providers::cloudflare::clients::AccountPermissionGroupDetailsArgs;
use crate::providers::cloudflare::clients::AccountPermissionGroupListArgs;
use crate::providers::cloudflare::clients::AccountRequestTracerRequestTraceArgs;
use crate::providers::cloudflare::clients::AccountResourceGroupCreateArgs;
use crate::providers::cloudflare::clients::AccountResourceGroupDeleteArgs;
use crate::providers::cloudflare::clients::AccountResourceGroupDetailsArgs;
use crate::providers::cloudflare::clients::AccountResourceGroupListArgs;
use crate::providers::cloudflare::clients::AccountResourceGroupUpdateArgs;
use crate::providers::cloudflare::clients::AccountRolesListRolesArgs;
use crate::providers::cloudflare::clients::AccountRolesRoleDetailsArgs;
use crate::providers::cloudflare::clients::AccountSubscriptionsCreateSubscriptionArgs;
use crate::providers::cloudflare::clients::AccountSubscriptionsDeleteSubscriptionArgs;
use crate::providers::cloudflare::clients::AccountSubscriptionsListSubscriptionsArgs;
use crate::providers::cloudflare::clients::AccountSubscriptionsUpdateSubscriptionArgs;
use crate::providers::cloudflare::clients::AccountUserGroupCreateArgs;
use crate::providers::cloudflare::clients::AccountUserGroupDeleteArgs;
use crate::providers::cloudflare::clients::AccountUserGroupDetailsArgs;
use crate::providers::cloudflare::clients::AccountUserGroupListArgs;
use crate::providers::cloudflare::clients::AccountUserGroupMemberCreateArgs;
use crate::providers::cloudflare::clients::AccountUserGroupMemberDeleteArgs;
use crate::providers::cloudflare::clients::AccountUserGroupMemberListArgs;
use crate::providers::cloudflare::clients::AccountUserGroupMembersUpdateArgs;
use crate::providers::cloudflare::clients::AccountUserGroupUpdateArgs;
use crate::providers::cloudflare::clients::AccountsAccountDetailsArgs;
use crate::providers::cloudflare::clients::AccountsGetAccountProfileArgs;
use crate::providers::cloudflare::clients::AccountsListAccountOrganizationsArgs;
use crate::providers::cloudflare::clients::AccountsListAccountsArgs;
use crate::providers::cloudflare::clients::AccountsModifyAccountProfileArgs;
use crate::providers::cloudflare::clients::AccountsMoveAccountsArgs;
use crate::providers::cloudflare::clients::AccountsTurnstileWidgetCreateArgs;
use crate::providers::cloudflare::clients::AccountsTurnstileWidgetDeleteArgs;
use crate::providers::cloudflare::clients::AccountsTurnstileWidgetGetArgs;
use crate::providers::cloudflare::clients::AccountsTurnstileWidgetRotateSecretArgs;
use crate::providers::cloudflare::clients::AccountsTurnstileWidgetUpdateArgs;
use crate::providers::cloudflare::clients::AccountsTurnstileWidgetsListArgs;
use crate::providers::cloudflare::clients::AccountsUpdateAccountArgs;
use crate::providers::cloudflare::clients::AddAudioTrackArgs;
use crate::providers::cloudflare::clients::AddParticipantArgs;
use crate::providers::cloudflare::clients::AddWebhookArgs;
use crate::providers::cloudflare::clients::AiSearchCreateInstancesArgs;
use crate::providers::cloudflare::clients::AiSearchCreateTokensArgs;
use crate::providers::cloudflare::clients::AiSearchDeleteInstancesArgs;
use crate::providers::cloudflare::clients::AiSearchDeleteTokensArgs;
use crate::providers::cloudflare::clients::AiSearchFetchInstancesArgs;
use crate::providers::cloudflare::clients::AiSearchFetchTokensArgs;
use crate::providers::cloudflare::clients::AiSearchInstanceChangeJobStatusArgs;
use crate::providers::cloudflare::clients::AiSearchInstanceChatCompletionArgs;
use crate::providers::cloudflare::clients::AiSearchInstanceCreateJobArgs;
use crate::providers::cloudflare::clients::AiSearchInstanceGetJobArgs;
use crate::providers::cloudflare::clients::AiSearchInstanceListJobLogsArgs;
use crate::providers::cloudflare::clients::AiSearchInstanceListJobsArgs;
use crate::providers::cloudflare::clients::AiSearchInstanceSearchArgs;
use crate::providers::cloudflare::clients::AiSearchListInstancesArgs;
use crate::providers::cloudflare::clients::AiSearchListTokensArgs;
use crate::providers::cloudflare::clients::AiSearchStatsArgs;
use crate::providers::cloudflare::clients::AiSearchUpdateInstancesArgs;
use crate::providers::cloudflare::clients::AiSearchUpdateTokensArgs;
use crate::providers::cloudflare::clients::AigConfigCreateDatasetArgs;
use crate::providers::cloudflare::clients::AigConfigCreateEvaluationsArgs;
use crate::providers::cloudflare::clients::AigConfigCreateGatewayArgs;
use crate::providers::cloudflare::clients::AigConfigCreateProvidersArgs;
use crate::providers::cloudflare::clients::AigConfigDeleteDatasetArgs;
use crate::providers::cloudflare::clients::AigConfigDeleteEvaluationsArgs;
use crate::providers::cloudflare::clients::AigConfigDeleteGatewayArgs;
use crate::providers::cloudflare::clients::AigConfigDeleteGatewayDynamicRouteArgs;
use crate::providers::cloudflare::clients::AigConfigDeleteGatewayLogsArgs;
use crate::providers::cloudflare::clients::AigConfigDeleteProvidersArgs;
use crate::providers::cloudflare::clients::AigConfigFetchDatasetArgs;
use crate::providers::cloudflare::clients::AigConfigFetchEvaluationsArgs;
use crate::providers::cloudflare::clients::AigConfigFetchGatewayArgs;
use crate::providers::cloudflare::clients::AigConfigGetGatewayDynamicRouteArgs;
use crate::providers::cloudflare::clients::AigConfigGetGatewayDynamicRouteVersionArgs;
use crate::providers::cloudflare::clients::AigConfigGetGatewayLogDetailArgs;
use crate::providers::cloudflare::clients::AigConfigGetGatewayLogRequestArgs;
use crate::providers::cloudflare::clients::AigConfigGetGatewayLogResponseArgs;
use crate::providers::cloudflare::clients::AigConfigGetGatewayUrlArgs;
use crate::providers::cloudflare::clients::AigConfigListDatasetArgs;
use crate::providers::cloudflare::clients::AigConfigListEvaluationsArgs;
use crate::providers::cloudflare::clients::AigConfigListEvaluatorsArgs;
use crate::providers::cloudflare::clients::AigConfigListGatewayArgs;
use crate::providers::cloudflare::clients::AigConfigListGatewayDynamicRouteDeploymentsArgs;
use crate::providers::cloudflare::clients::AigConfigListGatewayDynamicRouteVersionsArgs;
use crate::providers::cloudflare::clients::AigConfigListGatewayDynamicRoutesArgs;
use crate::providers::cloudflare::clients::AigConfigListGatewayLogsArgs;
use crate::providers::cloudflare::clients::AigConfigListProvidersArgs;
use crate::providers::cloudflare::clients::AigConfigPatchGatewayLogArgs;
use crate::providers::cloudflare::clients::AigConfigPostGatewayDynamicRouteArgs;
use crate::providers::cloudflare::clients::AigConfigPostGatewayDynamicRouteDeploymentArgs;
use crate::providers::cloudflare::clients::AigConfigPostGatewayDynamicRouteVersionArgs;
use crate::providers::cloudflare::clients::AigConfigUpdateDatasetArgs;
use crate::providers::cloudflare::clients::AigConfigUpdateGatewayArgs;
use crate::providers::cloudflare::clients::AigConfigUpdateGatewayDynamicRouteArgs;
use crate::providers::cloudflare::clients::AigConfigUpdateProvidersArgs;
use crate::providers::cloudflare::clients::AnalyzeCertificateAnalyzeCertificateArgs;
use crate::providers::cloudflare::clients::ApiShieldApiDiscoveryRetrieveDiscoveredOperationsOnAZoneArgs;
use crate::providers::cloudflare::clients::ApiShieldApiDiscoveryRetrieveDiscoveredOperationsOnAZoneAsOpenapiArgs;
use crate::providers::cloudflare::clients::ApiShieldApiPatchDiscoveredOperationArgs;
use crate::providers::cloudflare::clients::ApiShieldApiPatchDiscoveredOperationsArgs;
use crate::providers::cloudflare::clients::ApiShieldDeleteUserLabelArgs;
use crate::providers::cloudflare::clients::ApiShieldEndpointManagementAddOperationToAZoneArgs;
use crate::providers::cloudflare::clients::ApiShieldEndpointManagementAddOperationsToAZoneArgs;
use crate::providers::cloudflare::clients::ApiShieldEndpointManagementDeleteAnOperationArgs;
use crate::providers::cloudflare::clients::ApiShieldEndpointManagementDeleteMultipleOperationsArgs;
use crate::providers::cloudflare::clients::ApiShieldEndpointManagementRetrieveInformationAboutAllOperationsOnAZoneArgs;
use crate::providers::cloudflare::clients::ApiShieldEndpointManagementRetrieveInformationAboutAnOperationArgs;
use crate::providers::cloudflare::clients::ApiShieldEndpointManagementRetrieveOperationsAndFeaturesAsOpenApiSchemasArgs;
use crate::providers::cloudflare::clients::ApiShieldExpressionTemplatesFallthroughArgs;
use crate::providers::cloudflare::clients::ApiShieldLabelsCreateUserLabelsArgs;
use crate::providers::cloudflare::clients::ApiShieldLabelsDeleteUserLabelsArgs;
use crate::providers::cloudflare::clients::ApiShieldLabelsGetLabelsArgs;
use crate::providers::cloudflare::clients::ApiShieldLabelsGetManagedLabelArgs;
use crate::providers::cloudflare::clients::ApiShieldLabelsGetUserLabelArgs;
use crate::providers::cloudflare::clients::ApiShieldLabelsReplaceOperationsAttachedToManagedLabelArgs;
use crate::providers::cloudflare::clients::ApiShieldLabelsReplaceOperationsAttachedToUserLabelArgs;
use crate::providers::cloudflare::clients::ApiShieldOperationsBulkDeleteLabelsToOperationsArgs;
use crate::providers::cloudflare::clients::ApiShieldOperationsBulkPostLabelsToOperationsArgs;
use crate::providers::cloudflare::clients::ApiShieldOperationsBulkPutLabelsToOperationsArgs;
use crate::providers::cloudflare::clients::ApiShieldOperationsDeleteLabelsFromOperationArgs;
use crate::providers::cloudflare::clients::ApiShieldOperationsPostLabelsToOperationArgs;
use crate::providers::cloudflare::clients::ApiShieldOperationsPutLabelsToOperationArgs;
use crate::providers::cloudflare::clients::ApiShieldPatchUserLabelArgs;
use crate::providers::cloudflare::clients::ApiShieldPutUserLabelArgs;
use crate::providers::cloudflare::clients::ApiShieldSchemaDeleteASchemaArgs;
use crate::providers::cloudflare::clients::ApiShieldSchemaValidationEnableValidationForASchemaArgs;
use crate::providers::cloudflare::clients::ApiShieldSchemaValidationExtractOperationsFromSchemaArgs;
use crate::providers::cloudflare::clients::ApiShieldSchemaValidationPatchZoneLevelSettingsArgs;
use crate::providers::cloudflare::clients::ApiShieldSchemaValidationPostSchemaArgs;
use crate::providers::cloudflare::clients::ApiShieldSchemaValidationRetrieveInformationAboutAllSchemasArgs;
use crate::providers::cloudflare::clients::ApiShieldSchemaValidationRetrieveInformationAboutSpecificSchemaArgs;
use crate::providers::cloudflare::clients::ApiShieldSchemaValidationRetrieveOperationLevelSettingsArgs;
use crate::providers::cloudflare::clients::ApiShieldSchemaValidationRetrieveUserSchemaHostsArgs;
use crate::providers::cloudflare::clients::ApiShieldSchemaValidationRetrieveZoneLevelSettingsArgs;
use crate::providers::cloudflare::clients::ApiShieldSchemaValidationUpdateMultipleOperationLevelSettingsArgs;
use crate::providers::cloudflare::clients::ApiShieldSchemaValidationUpdateOperationLevelSettingsArgs;
use crate::providers::cloudflare::clients::ApiShieldSchemaValidationUpdateZoneLevelSettingsArgs;
use crate::providers::cloudflare::clients::ApiShieldSettingsRetrieveInformationAboutSpecificConfigurationPropertiesArgs;
use crate::providers::cloudflare::clients::ApiShieldSettingsSetConfigurationPropertiesArgs;
use crate::providers::cloudflare::clients::ArchiveSecurityCenterInsightArgs;
use crate::providers::cloudflare::clients::ArchiveSecurityCenterInsightDeprecatedArgs;
use crate::providers::cloudflare::clients::ArchiveZoneSecurityCenterInsightArgs;
use crate::providers::cloudflare::clients::ArgoAnalyticsForGeolocationArgoAnalyticsForAZoneAtDifferentPoPsArgs;
use crate::providers::cloudflare::clients::ArgoAnalyticsForZoneArgoAnalyticsForAZoneArgs;
use crate::providers::cloudflare::clients::ArgoSmartRoutingGetArgoSmartRoutingSettingArgs;
use crate::providers::cloudflare::clients::ArgoSmartRoutingPatchArgoSmartRoutingSettingArgs;
use crate::providers::cloudflare::clients::AsnIntelligenceGetAsnOverviewArgs;
use crate::providers::cloudflare::clients::AsnIntelligenceGetAsnSubnetsArgs;
use crate::providers::cloudflare::clients::AuditLogsGetAccountAuditLogsArgs;
use crate::providers::cloudflare::clients::AuditLogsGetUserAuditLogsArgs;
use crate::providers::cloudflare::clients::AuditLogsV2GetAccountAuditLogsArgs;
use crate::providers::cloudflare::clients::AuditLogsV2GetOrganizationAuditLogsArgs;
use crate::providers::cloudflare::clients::AutoragConfigAiSearchArgs;
use crate::providers::cloudflare::clients::AutoragConfigFilesArgs;
use crate::providers::cloudflare::clients::AutoragConfigGetJobArgs;
use crate::providers::cloudflare::clients::AutoragConfigListJobLogsArgs;
use crate::providers::cloudflare::clients::AutoragConfigListJobsArgs;
use crate::providers::cloudflare::clients::AutoragConfigSearchArgs;
use crate::providers::cloudflare::clients::AutoragConfigSyncArgs;
use crate::providers::cloudflare::clients::AvailablePageRulesSettingsListAvailablePageRulesSettingsArgs;
use crate::providers::cloudflare::clients::BeginSsoConnectorVerificationArgs;
use crate::providers::cloudflare::clients::BillableUsageGetPaygoAccountUsageArgs;
use crate::providers::cloudflare::clients::BotManagementForAZoneGetConfigArgs;
use crate::providers::cloudflare::clients::BotManagementForAZoneUpdateConfigArgs;
use crate::providers::cloudflare::clients::BotManagementZoneFeedbackCreateArgs;
use crate::providers::cloudflare::clients::BotManagementZoneFeedbackListArgs;
use crate::providers::cloudflare::clients::BotnetThreatFeedDeleteAsnArgs;
use crate::providers::cloudflare::clients::BotnetThreatFeedGetDayReportArgs;
use crate::providers::cloudflare::clients::BotnetThreatFeedGetFullReportArgs;
use crate::providers::cloudflare::clients::BotnetThreatFeedListAsnArgs;
use crate::providers::cloudflare::clients::BrapiDeleteCancelCrawlArgs;
use crate::providers::cloudflare::clients::BrapiGetCrawlResultArgs;
use crate::providers::cloudflare::clients::BrapiPostContentArgs;
use crate::providers::cloudflare::clients::BrapiPostCrawlArgs;
use crate::providers::cloudflare::clients::BrapiPostJsonArgs;
use crate::providers::cloudflare::clients::BrapiPostLinksArgs;
use crate::providers::cloudflare::clients::BrapiPostMarkdownArgs;
use crate::providers::cloudflare::clients::BrapiPostPdfArgs;
use crate::providers::cloudflare::clients::BrapiPostScrapeArgs;
use crate::providers::cloudflare::clients::BrapiPostScreenshotArgs;
use crate::providers::cloudflare::clients::BrapiPostSnapshotArgs;
use crate::providers::cloudflare::clients::BulkCreatePrefixesArgs;
use crate::providers::cloudflare::clients::CallsAppsCreateANewAppArgs;
use crate::providers::cloudflare::clients::CallsAppsDeleteAppArgs;
use crate::providers::cloudflare::clients::CallsAppsListArgs;
use crate::providers::cloudflare::clients::CallsAppsRetrieveAppDetailsArgs;
use crate::providers::cloudflare::clients::CallsAppsUpdateAppDetailsArgs;
use crate::providers::cloudflare::clients::CallsDeleteTurnKeyArgs;
use crate::providers::cloudflare::clients::CallsRetrieveTurnKeyDetailsArgs;
use crate::providers::cloudflare::clients::CallsTurnKeyCreateArgs;
use crate::providers::cloudflare::clients::CallsTurnKeyListArgs;
use crate::providers::cloudflare::clients::CallsUpdateTurnKeyArgs;
use crate::providers::cloudflare::clients::CancelBuildByUuidArgs;
use crate::providers::cloudflare::clients::CatalogSyncsCreateArgs;
use crate::providers::cloudflare::clients::CatalogSyncsDeleteArgs;
use crate::providers::cloudflare::clients::CatalogSyncsListArgs;
use crate::providers::cloudflare::clients::CatalogSyncsPatchArgs;
use crate::providers::cloudflare::clients::CatalogSyncsPrebuiltPoliciesListArgs;
use crate::providers::cloudflare::clients::CatalogSyncsReadArgs;
use crate::providers::cloudflare::clients::CatalogSyncsRefreshArgs;
use crate::providers::cloudflare::clients::CatalogSyncsUpdateArgs;
use crate::providers::cloudflare::clients::CertificatePacksDeleteAdvancedCertificateManagerCertificatePackArgs;
use crate::providers::cloudflare::clients::CertificatePacksGetCertificatePackArgs;
use crate::providers::cloudflare::clients::CertificatePacksGetCertificatePackQuotasArgs;
use crate::providers::cloudflare::clients::CertificatePacksListCertificatePacksArgs;
use crate::providers::cloudflare::clients::CertificatePacksOrderAdvancedCertificateManagerCertificatePackArgs;
use crate::providers::cloudflare::clients::CertificatePacksRestartValidationForAdvancedCertificateManagerCertificatePackArgs;
use crate::providers::cloudflare::clients::ClientCertificateForAZoneClientCertificateDetailsArgs;
use crate::providers::cloudflare::clients::ClientCertificateForAZoneCreateClientCertificateArgs;
use crate::providers::cloudflare::clients::ClientCertificateForAZoneDeleteClientCertificateArgs;
use crate::providers::cloudflare::clients::ClientCertificateForAZoneEditClientCertificateArgs;
use crate::providers::cloudflare::clients::ClientCertificateForAZoneListClientCertificatesArgs;
use crate::providers::cloudflare::clients::ClientCertificateForAZoneListHostnameAssociationsArgs;
use crate::providers::cloudflare::clients::ClientCertificateForAZonePutHostnameAssociationsArgs;
use crate::providers::cloudflare::clients::CloudflareImagesBaseImageArgs;
use crate::providers::cloudflare::clients::CloudflareImagesCreateAuthenticatedDirectUploadUrlV2Args;
use crate::providers::cloudflare::clients::CloudflareImagesDeleteImageArgs;
use crate::providers::cloudflare::clients::CloudflareImagesImageDetailsArgs;
use crate::providers::cloudflare::clients::CloudflareImagesImagesUsageStatisticsArgs;
use crate::providers::cloudflare::clients::CloudflareImagesKeysAddSigningKeyArgs;
use crate::providers::cloudflare::clients::CloudflareImagesKeysDeleteSigningKeyArgs;
use crate::providers::cloudflare::clients::CloudflareImagesKeysListSigningKeysArgs;
use crate::providers::cloudflare::clients::CloudflareImagesListImagesArgs;
use crate::providers::cloudflare::clients::CloudflareImagesListImagesV2Args;
use crate::providers::cloudflare::clients::CloudflareImagesUpdateImageArgs;
use crate::providers::cloudflare::clients::CloudflareImagesUploadAnImageViaUrlArgs;
use crate::providers::cloudflare::clients::CloudflareImagesVariantsCreateAVariantArgs;
use crate::providers::cloudflare::clients::CloudflareImagesVariantsDeleteAVariantArgs;
use crate::providers::cloudflare::clients::CloudflareImagesVariantsListVariantsArgs;
use crate::providers::cloudflare::clients::CloudflareImagesVariantsUpdateAVariantArgs;
use crate::providers::cloudflare::clients::CloudflareImagesVariantsVariantDetailsArgs;
use crate::providers::cloudflare::clients::CloudflareImagesVariantsVariantDetailsFlatArgs;
use crate::providers::cloudflare::clients::CloudflareIpsCloudflareIpDetailsArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelCleanUpCloudflareTunnelConnectionsArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelConfigurationGetConfigurationArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelConfigurationPutConfigurationArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelCreateACloudflareTunnelArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelCreateAWarpConnectorTunnelArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelDeleteACloudflareTunnelArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelDeleteAWarpConnectorTunnelArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelGetACloudflareTunnelArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelGetACloudflareTunnelManagementTokenArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelGetACloudflareTunnelTokenArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelGetAWarpConnectorTunnelArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelGetAWarpConnectorTunnelTokenArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelGetCloudflareTunnelConnectorArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelGetWarpConnectorTunnelConnectorArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelListAllTunnelsArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelListCloudflareTunnelConnectionsArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelListCloudflareTunnelsArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelListWarpConnectorTunnelConnectionsArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelListWarpConnectorTunnelsArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelManualFailoverWarpConnectorTunnelArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelUpdateACloudflareTunnelArgs;
use crate::providers::cloudflare::clients::CloudflareTunnelUpdateAWarpConnectorTunnelArgs;
use crate::providers::cloudflare::clients::CloudforceOneCreateRuleArgs;
use crate::providers::cloudflare::clients::CloudforceOneDeleteAllRulesArgs;
use crate::providers::cloudflare::clients::CloudforceOneDeleteRuleArgs;
use crate::providers::cloudflare::clients::CloudforceOneGetRuleArgs;
use crate::providers::cloudflare::clients::CloudforceOneGetRuleStatsArgs;
use crate::providers::cloudflare::clients::CloudforceOneGetRuleTreeArgs;
use crate::providers::cloudflare::clients::CloudforceOneListRulesArgs;
use crate::providers::cloudflare::clients::CloudforceOnePriorityDeleteArgs;
use crate::providers::cloudflare::clients::CloudforceOnePriorityGetArgs;
use crate::providers::cloudflare::clients::CloudforceOnePriorityListArgs;
use crate::providers::cloudflare::clients::CloudforceOnePriorityNewArgs;
use crate::providers::cloudflare::clients::CloudforceOnePriorityQuotaArgs;
use crate::providers::cloudflare::clients::CloudforceOnePriorityUpdateArgs;
use crate::providers::cloudflare::clients::CloudforceOneRequestAssetDeleteArgs;
use crate::providers::cloudflare::clients::CloudforceOneRequestAssetGetArgs;
use crate::providers::cloudflare::clients::CloudforceOneRequestAssetListArgs;
use crate::providers::cloudflare::clients::CloudforceOneRequestAssetNewArgs;
use crate::providers::cloudflare::clients::CloudforceOneRequestAssetUpdateArgs;
use crate::providers::cloudflare::clients::CloudforceOneRequestConstantsArgs;
use crate::providers::cloudflare::clients::CloudforceOneRequestDeleteArgs;
use crate::providers::cloudflare::clients::CloudforceOneRequestGetArgs;
use crate::providers::cloudflare::clients::CloudforceOneRequestListArgs;
use crate::providers::cloudflare::clients::CloudforceOneRequestMessageDeleteArgs;
use crate::providers::cloudflare::clients::CloudforceOneRequestMessageListArgs;
use crate::providers::cloudflare::clients::CloudforceOneRequestMessageNewArgs;
use crate::providers::cloudflare::clients::CloudforceOneRequestMessageUpdateArgs;
use crate::providers::cloudflare::clients::CloudforceOneRequestNewArgs;
use crate::providers::cloudflare::clients::CloudforceOneRequestQuotaArgs;
use crate::providers::cloudflare::clients::CloudforceOneRequestTypesArgs;
use crate::providers::cloudflare::clients::CloudforceOneRequestUpdateArgs;
use crate::providers::cloudflare::clients::CloudforceOneUpdateRuleArgs;
use crate::providers::cloudflare::clients::CloudforceOneValidateRuleArgs;
use crate::providers::cloudflare::clients::ConnectivityServicesDeleteArgs;
use crate::providers::cloudflare::clients::ConnectivityServicesGetArgs;
use crate::providers::cloudflare::clients::ConnectivityServicesListArgs;
use crate::providers::cloudflare::clients::ConnectivityServicesPostArgs;
use crate::providers::cloudflare::clients::ConnectivityServicesPutArgs;
use crate::providers::cloudflare::clients::CreateAccountRulesetArgs;
use crate::providers::cloudflare::clients::CreateAccountRulesetRuleArgs;
use crate::providers::cloudflare::clients::CreateAllowlistedPrefixArgs;
use crate::providers::cloudflare::clients::CreateAppArgs;
use crate::providers::cloudflare::clients::CreateApplicationArgs;
use crate::providers::cloudflare::clients::CreateBuildTokenArgs;
use crate::providers::cloudflare::clients::CreateCniArgs;
use crate::providers::cloudflare::clients::CreateDexRuleArgs;
use crate::providers::cloudflare::clients::CreateDnsProtectionRuleArgs;
use crate::providers::cloudflare::clients::CreateHyperdriveArgs;
use crate::providers::cloudflare::clients::CreateInterconnectArgs;
use crate::providers::cloudflare::clients::CreateIpProfileArgs;
use crate::providers::cloudflare::clients::CreateManualBuildArgs;
use crate::providers::cloudflare::clients::CreateMeetingArgs;
use crate::providers::cloudflare::clients::CreatePollArgs;
use crate::providers::cloudflare::clients::CreatePrefixArgs;
use crate::providers::cloudflare::clients::CreateSynProtectionFilterArgs;
use crate::providers::cloudflare::clients::CreateSynProtectionRuleArgs;
use crate::providers::cloudflare::clients::CreateTcpFlowProtectionFilterArgs;
use crate::providers::cloudflare::clients::CreateTcpFlowProtectionRuleArgs;
use crate::providers::cloudflare::clients::CreateTriggerArgs;
use crate::providers::cloudflare::clients::CreateWorkerArgs;
use crate::providers::cloudflare::clients::CreateWorkerVersionArgs;
use crate::providers::cloudflare::clients::CreateZoneRulesetArgs;
use crate::providers::cloudflare::clients::CreateZoneRulesetRuleArgs;
use crate::providers::cloudflare::clients::CustomAssetsForAZoneCreateACustomAssetArgs;
use crate::providers::cloudflare::clients::CustomAssetsForAZoneDeleteACustomAssetArgs;
use crate::providers::cloudflare::clients::CustomAssetsForAZoneGetACustomAssetArgs;
use crate::providers::cloudflare::clients::CustomAssetsForAZoneListCustomAssetsArgs;
use crate::providers::cloudflare::clients::CustomAssetsForAZoneUpdateACustomAssetArgs;
use crate::providers::cloudflare::clients::CustomAssetsForAnAccountCreateACustomAssetArgs;
use crate::providers::cloudflare::clients::CustomAssetsForAnAccountDeleteACustomAssetArgs;
use crate::providers::cloudflare::clients::CustomAssetsForAnAccountGetACustomAssetArgs;
use crate::providers::cloudflare::clients::CustomAssetsForAnAccountListCustomAssetsArgs;
use crate::providers::cloudflare::clients::CustomAssetsForAnAccountUpdateACustomAssetArgs;
use crate::providers::cloudflare::clients::CustomHostnameFallbackOriginForAZoneDeleteFallbackOriginForCustomHostnamesArgs;
use crate::providers::cloudflare::clients::CustomHostnameFallbackOriginForAZoneGetFallbackOriginForCustomHostnamesArgs;
use crate::providers::cloudflare::clients::CustomHostnameFallbackOriginForAZoneUpdateFallbackOriginForCustomHostnamesArgs;
use crate::providers::cloudflare::clients::CustomHostnameForAZoneCreateCustomHostnameArgs;
use crate::providers::cloudflare::clients::CustomHostnameForAZoneCustomHostnameDetailsArgs;
use crate::providers::cloudflare::clients::CustomHostnameForAZoneDeleteCustomHostnameAndAnyIssuedSslCertificatesArgs;
use crate::providers::cloudflare::clients::CustomHostnameForAZoneDeleteSingleCertificateAndKeyInACustomHostnameArgs;
use crate::providers::cloudflare::clients::CustomHostnameForAZoneEditCustomCertificateCustomHostnameArgs;
use crate::providers::cloudflare::clients::CustomHostnameForAZoneEditCustomHostnameArgs;
use crate::providers::cloudflare::clients::CustomHostnameForAZoneListCustomHostnamesArgs;
use crate::providers::cloudflare::clients::CustomIndicatorFeedsAddPermissionArgs;
use crate::providers::cloudflare::clients::CustomIndicatorFeedsCreateIndicatorFeedsArgs;
use crate::providers::cloudflare::clients::CustomIndicatorFeedsDownloadIndicatorFeedDataArgs;
use crate::providers::cloudflare::clients::CustomIndicatorFeedsGetIndicatorFeedDataArgs;
use crate::providers::cloudflare::clients::CustomIndicatorFeedsGetIndicatorFeedMetadataArgs;
use crate::providers::cloudflare::clients::CustomIndicatorFeedsGetIndicatorFeedsArgs;
use crate::providers::cloudflare::clients::CustomIndicatorFeedsRemovePermissionArgs;
use crate::providers::cloudflare::clients::CustomIndicatorFeedsUpdateIndicatorFeedDataArgs;
use crate::providers::cloudflare::clients::CustomIndicatorFeedsUpdateIndicatorFeedMetadataArgs;
use crate::providers::cloudflare::clients::CustomIndicatorFeedsViewPermissionsArgs;
use crate::providers::cloudflare::clients::CustomOriginTrustStoreCreateArgs;
use crate::providers::cloudflare::clients::CustomOriginTrustStoreDeleteArgs;
use crate::providers::cloudflare::clients::CustomOriginTrustStoreDetailsArgs;
use crate::providers::cloudflare::clients::CustomOriginTrustStoreListDetailsArgs;
use crate::providers::cloudflare::clients::CustomPagesForAZoneGetACustomPageArgs;
use crate::providers::cloudflare::clients::CustomPagesForAZoneListCustomPagesArgs;
use crate::providers::cloudflare::clients::CustomPagesForAZoneUpdateACustomPageArgs;
use crate::providers::cloudflare::clients::CustomPagesForAnAccountGetACustomPageArgs;
use crate::providers::cloudflare::clients::CustomPagesForAnAccountListCustomPagesArgs;
use crate::providers::cloudflare::clients::CustomPagesForAnAccountUpdateACustomPageArgs;
use crate::providers::cloudflare::clients::CustomSslForAZoneCreateSslConfigurationArgs;
use crate::providers::cloudflare::clients::CustomSslForAZoneDeleteSslConfigurationArgs;
use crate::providers::cloudflare::clients::CustomSslForAZoneEditSslConfigurationArgs;
use crate::providers::cloudflare::clients::CustomSslForAZoneListSslConfigurationsArgs;
use crate::providers::cloudflare::clients::CustomSslForAZoneRePrioritizeSslCertificatesArgs;
use crate::providers::cloudflare::clients::CustomSslForAZoneSslConfigurationDetailsArgs;
use crate::providers::cloudflare::clients::D1CreateDatabaseArgs;
use crate::providers::cloudflare::clients::D1DeleteDatabaseArgs;
use crate::providers::cloudflare::clients::D1ExportDatabaseArgs;
use crate::providers::cloudflare::clients::D1GetDatabaseArgs;
use crate::providers::cloudflare::clients::D1ImportDatabaseArgs;
use crate::providers::cloudflare::clients::D1ListDatabasesArgs;
use crate::providers::cloudflare::clients::D1QueryDatabaseArgs;
use crate::providers::cloudflare::clients::D1RawDatabaseQueryArgs;
use crate::providers::cloudflare::clients::D1TimeTravelGetBookmarkArgs;
use crate::providers::cloudflare::clients::D1TimeTravelRestoreArgs;
use crate::providers::cloudflare::clients::D1UpdateDatabaseArgs;
use crate::providers::cloudflare::clients::D1UpdatePartialDatabaseArgs;
use crate::providers::cloudflare::clients::DcvDelegationUuidGetArgs;
use crate::providers::cloudflare::clients::DeleteAccountRulesetArgs;
use crate::providers::cloudflare::clients::DeleteAccountRulesetRuleArgs;
use crate::providers::cloudflare::clients::DeleteAccountRulesetVersionArgs;
use crate::providers::cloudflare::clients::DeleteAccountsAccountIdLogpushJobsJobIdArgs;
use crate::providers::cloudflare::clients::DeleteAccountsAccountIdLogpushValidateDestinationArgs;
use crate::providers::cloudflare::clients::DeleteAccountsAccountIdLogpushValidateDestinationExistsArgs;
use crate::providers::cloudflare::clients::DeleteAccountsAccountIdLogsControlCmbConfigArgs;
use crate::providers::cloudflare::clients::DeleteAllowlistPrefixArgs;
use crate::providers::cloudflare::clients::DeleteAllowlistPrefixesForAccountArgs;
use crate::providers::cloudflare::clients::DeleteAudioTracksArgs;
use crate::providers::cloudflare::clients::DeleteBuildTokenArgs;
use crate::providers::cloudflare::clients::DeleteCategoryDeleteArgs;
use crate::providers::cloudflare::clients::DeleteCniArgs;
use crate::providers::cloudflare::clients::DeleteDatasetDeleteArgs;
use crate::providers::cloudflare::clients::DeleteDeleteLogoQueryArgs;
use crate::providers::cloudflare::clients::DeleteDeleteScansArgs;
use crate::providers::cloudflare::clients::DeleteDeviceArgs;
use crate::providers::cloudflare::clients::DeleteDexRuleArgs;
use crate::providers::cloudflare::clients::DeleteDnsProtectionRuleArgs;
use crate::providers::cloudflare::clients::DeleteDnsProtectionRulesForAccountArgs;
use crate::providers::cloudflare::clients::DeleteEnvironmentVariableArgs;
use crate::providers::cloudflare::clients::DeleteEventDeleteArgs;
use crate::providers::cloudflare::clients::DeleteEventQueryAlertDeleteArgs;
use crate::providers::cloudflare::clients::DeleteEventQueryDeleteArgs;
use crate::providers::cloudflare::clients::DeleteEventReferenceDeleteArgs;
use crate::providers::cloudflare::clients::DeleteEventTagDeleteArgs;
use crate::providers::cloudflare::clients::DeleteGroupDeleteArgs;
use crate::providers::cloudflare::clients::DeleteGroupMemberDeleteArgs;
use crate::providers::cloudflare::clients::DeleteHyperdriveArgs;
use crate::providers::cloudflare::clients::DeleteIndicatorDeleteArgs;
use crate::providers::cloudflare::clients::DeleteInterconnectArgs;
use crate::providers::cloudflare::clients::DeleteIpProfileArgs;
use crate::providers::cloudflare::clients::DeleteManagedTransformsArgs;
use crate::providers::cloudflare::clients::DeleteMeetingParticipantArgs;
use crate::providers::cloudflare::clients::DeletePermissionDeleteArgs;
use crate::providers::cloudflare::clients::DeletePrefixArgs;
use crate::providers::cloudflare::clients::DeletePrefixesForAccountArgs;
use crate::providers::cloudflare::clients::DeletePresetsPresetIdArgs;
use crate::providers::cloudflare::clients::DeleteRegistrationArgs;
use crate::providers::cloudflare::clients::DeleteRegistrationsArgs;
use crate::providers::cloudflare::clients::DeleteRepoConnectionArgs;
use crate::providers::cloudflare::clients::DeleteSecurityTxtArgs;
use crate::providers::cloudflare::clients::DeleteSsoConnectorArgs;
use crate::providers::cloudflare::clients::DeleteSynProtectionFilterArgs;
use crate::providers::cloudflare::clients::DeleteSynProtectionFiltersForAccountArgs;
use crate::providers::cloudflare::clients::DeleteSynProtectionRuleArgs;
use crate::providers::cloudflare::clients::DeleteSynProtectionRulesForAccountArgs;
use crate::providers::cloudflare::clients::DeleteTagCategoryDeleteArgs;
use crate::providers::cloudflare::clients::DeleteTagDeleteArgs;
use crate::providers::cloudflare::clients::DeleteTcpFlowProtectionFilterArgs;
use crate::providers::cloudflare::clients::DeleteTcpFlowProtectionFiltersForAccountArgs;
use crate::providers::cloudflare::clients::DeleteTcpFlowProtectionRuleArgs;
use crate::providers::cloudflare::clients::DeleteTcpFlowProtectionRulesForAccountArgs;
use crate::providers::cloudflare::clients::DeleteTriggerArgs;
use crate::providers::cloudflare::clients::DeleteUrlNormalizationArgs;
use crate::providers::cloudflare::clients::DeleteV4AccountsByAccountIdPipelinesByPipelineNameDeprecatedArgs;
use crate::providers::cloudflare::clients::DeleteV4AccountsByAccountIdPipelinesV1PipelinesByPipelineIdArgs;
use crate::providers::cloudflare::clients::DeleteV4AccountsByAccountIdPipelinesV1SinksBySinkIdArgs;
use crate::providers::cloudflare::clients::DeleteV4AccountsByAccountIdPipelinesV1StreamsByStreamIdArgs;
use crate::providers::cloudflare::clients::DeleteWebhookArgs;
use crate::providers::cloudflare::clients::DeleteWorkerArgs;
use crate::providers::cloudflare::clients::DeleteWorkerVersionArgs;
use crate::providers::cloudflare::clients::DeleteZoneRulesetArgs;
use crate::providers::cloudflare::clients::DeleteZoneRulesetRuleArgs;
use crate::providers::cloudflare::clients::DeleteZoneRulesetVersionArgs;
use crate::providers::cloudflare::clients::DeleteZoneSnippetArgs;
use crate::providers::cloudflare::clients::DeleteZoneSnippetRulesArgs;
use crate::providers::cloudflare::clients::DeleteZonesZoneIdLogpushJobsJobIdArgs;
use crate::providers::cloudflare::clients::DestinationCreateArgs;
use crate::providers::cloudflare::clients::DestinationListArgs;
use crate::providers::cloudflare::clients::DestinationUpdateArgs;
use crate::providers::cloudflare::clients::DestinationsDeleteArgs;
use crate::providers::cloudflare::clients::DeviceDexTestCreateDeviceDexTestArgs;
use crate::providers::cloudflare::clients::DeviceDexTestDeleteDeviceDexTestArgs;
use crate::providers::cloudflare::clients::DeviceDexTestDetailsArgs;
use crate::providers::cloudflare::clients::DeviceDexTestGetDeviceDexTestArgs;
use crate::providers::cloudflare::clients::DeviceDexTestUpdateDeviceDexTestArgs;
use crate::providers::cloudflare::clients::DeviceManagedNetworksCreateDeviceManagedNetworkArgs;
use crate::providers::cloudflare::clients::DeviceManagedNetworksDeleteDeviceManagedNetworkArgs;
use crate::providers::cloudflare::clients::DeviceManagedNetworksDeviceManagedNetworkDetailsArgs;
use crate::providers::cloudflare::clients::DeviceManagedNetworksListDeviceManagedNetworksArgs;
use crate::providers::cloudflare::clients::DeviceManagedNetworksUpdateDeviceManagedNetworkArgs;
use crate::providers::cloudflare::clients::DevicePostureIntegrationsCreateDevicePostureIntegrationArgs;
use crate::providers::cloudflare::clients::DevicePostureIntegrationsDeleteDevicePostureIntegrationArgs;
use crate::providers::cloudflare::clients::DevicePostureIntegrationsDevicePostureIntegrationDetailsArgs;
use crate::providers::cloudflare::clients::DevicePostureIntegrationsListDevicePostureIntegrationsArgs;
use crate::providers::cloudflare::clients::DevicePostureIntegrationsUpdateDevicePostureIntegrationArgs;
use crate::providers::cloudflare::clients::DevicePostureRulesCreateDevicePostureRuleArgs;
use crate::providers::cloudflare::clients::DevicePostureRulesDeleteDevicePostureRuleArgs;
use crate::providers::cloudflare::clients::DevicePostureRulesDevicePostureRulesDetailsArgs;
use crate::providers::cloudflare::clients::DevicePostureRulesListDevicePostureRulesArgs;
use crate::providers::cloudflare::clients::DevicePostureRulesUpdateDevicePostureRuleArgs;
use crate::providers::cloudflare::clients::DevicesCreateDeviceSettingsPolicyArgs;
use crate::providers::cloudflare::clients::DevicesDeleteDeviceSettingsPolicyArgs;
use crate::providers::cloudflare::clients::DevicesDeviceDetailsArgs;
use crate::providers::cloudflare::clients::DevicesGetDefaultDeviceSettingsPolicyArgs;
use crate::providers::cloudflare::clients::DevicesGetDeviceSettingsPolicyByIdArgs;
use crate::providers::cloudflare::clients::DevicesGetLocalDomainFallbackListArgs;
use crate::providers::cloudflare::clients::DevicesGetLocalDomainFallbackListForADeviceSettingsPolicyArgs;
use crate::providers::cloudflare::clients::DevicesGetPolicyCertificatesArgs;
use crate::providers::cloudflare::clients::DevicesGetSplitTunnelExcludeListArgs;
use crate::providers::cloudflare::clients::DevicesGetSplitTunnelExcludeListForADeviceSettingsPolicyArgs;
use crate::providers::cloudflare::clients::DevicesGetSplitTunnelIncludeListArgs;
use crate::providers::cloudflare::clients::DevicesGetSplitTunnelIncludeListForADeviceSettingsPolicyArgs;
use crate::providers::cloudflare::clients::DevicesListAdminOverrideCodeForDeviceArgs;
use crate::providers::cloudflare::clients::DevicesListDeviceSettingsPoliciesArgs;
use crate::providers::cloudflare::clients::DevicesListDevicesArgs;
use crate::providers::cloudflare::clients::DevicesLiveStatusArgs;
use crate::providers::cloudflare::clients::DevicesResilienceRetrieveGlobalWarpOverrideArgs;
use crate::providers::cloudflare::clients::DevicesResilienceSetGlobalWarpOverrideArgs;
use crate::providers::cloudflare::clients::DevicesRevokeDevicesArgs;
use crate::providers::cloudflare::clients::DevicesSetLocalDomainFallbackListArgs;
use crate::providers::cloudflare::clients::DevicesSetLocalDomainFallbackListForADeviceSettingsPolicyArgs;
use crate::providers::cloudflare::clients::DevicesSetSplitTunnelExcludeListArgs;
use crate::providers::cloudflare::clients::DevicesSetSplitTunnelExcludeListForADeviceSettingsPolicyArgs;
use crate::providers::cloudflare::clients::DevicesSetSplitTunnelIncludeListArgs;
use crate::providers::cloudflare::clients::DevicesSetSplitTunnelIncludeListForADeviceSettingsPolicyArgs;
use crate::providers::cloudflare::clients::DevicesUnrevokeDevicesArgs;
use crate::providers::cloudflare::clients::DevicesUpdateDefaultDeviceSettingsPolicyArgs;
use crate::providers::cloudflare::clients::DevicesUpdateDeviceSettingsPolicyArgs;
use crate::providers::cloudflare::clients::DevicesUpdatePolicyCertificatesArgs;
use crate::providers::cloudflare::clients::DexEndpointsHttpTestDetailsArgs;
use crate::providers::cloudflare::clients::DexEndpointsHttpTestPercentilesArgs;
use crate::providers::cloudflare::clients::DexEndpointsListColosArgs;
use crate::providers::cloudflare::clients::DexEndpointsListTestsOverviewArgs;
use crate::providers::cloudflare::clients::DexEndpointsTestsUniqueDevicesArgs;
use crate::providers::cloudflare::clients::DexEndpointsTracerouteTestDetailsArgs;
use crate::providers::cloudflare::clients::DexEndpointsTracerouteTestNetworkPathArgs;
use crate::providers::cloudflare::clients::DexEndpointsTracerouteTestPercentilesArgs;
use crate::providers::cloudflare::clients::DexEndpointsTracerouteTestResultNetworkPathArgs;
use crate::providers::cloudflare::clients::DexFleetStatusDevicesArgs;
use crate::providers::cloudflare::clients::DexFleetStatusLiveArgs;
use crate::providers::cloudflare::clients::DexFleetStatusOverTimeArgs;
use crate::providers::cloudflare::clients::DiagnosticsEndpointHealthcheckCreateArgs;
use crate::providers::cloudflare::clients::DiagnosticsEndpointHealthcheckDeleteArgs;
use crate::providers::cloudflare::clients::DiagnosticsEndpointHealthcheckGetArgs;
use crate::providers::cloudflare::clients::DiagnosticsEndpointHealthcheckListArgs;
use crate::providers::cloudflare::clients::DiagnosticsEndpointHealthcheckUpdateArgs;
use crate::providers::cloudflare::clients::DiagnosticsTracerouteArgs;
use crate::providers::cloudflare::clients::DisableCatalogArgs;
use crate::providers::cloudflare::clients::DlpDatasetsCreateArgs;
use crate::providers::cloudflare::clients::DlpDatasetsCreateVersionArgs;
use crate::providers::cloudflare::clients::DlpDatasetsDefineColumnsArgs;
use crate::providers::cloudflare::clients::DlpDatasetsDeleteArgs;
use crate::providers::cloudflare::clients::DlpDatasetsReadAllArgs;
use crate::providers::cloudflare::clients::DlpDatasetsReadArgs;
use crate::providers::cloudflare::clients::DlpDatasetsUpdateArgs;
use crate::providers::cloudflare::clients::DlpDatasetsUploadDatasetColumnArgs;
use crate::providers::cloudflare::clients::DlpDatasetsUploadVersionArgs;
use crate::providers::cloudflare::clients::DlpDocumentFingerprintsCreateArgs;
use crate::providers::cloudflare::clients::DlpDocumentFingerprintsDeleteArgs;
use crate::providers::cloudflare::clients::DlpDocumentFingerprintsReadAllArgs;
use crate::providers::cloudflare::clients::DlpDocumentFingerprintsReadArgs;
use crate::providers::cloudflare::clients::DlpDocumentFingerprintsUpdateArgs;
use crate::providers::cloudflare::clients::DlpDocumentFingerprintsUploadArgs;
use crate::providers::cloudflare::clients::DlpEmailScannerCreateAccountMappingArgs;
use crate::providers::cloudflare::clients::DlpEmailScannerCreateRuleArgs;
use crate::providers::cloudflare::clients::DlpEmailScannerDeleteRuleArgs;
use crate::providers::cloudflare::clients::DlpEmailScannerGetAccountMappingArgs;
use crate::providers::cloudflare::clients::DlpEmailScannerGetRuleArgs;
use crate::providers::cloudflare::clients::DlpEmailScannerListAllRulesArgs;
use crate::providers::cloudflare::clients::DlpEmailScannerUpdateRuleArgs;
use crate::providers::cloudflare::clients::DlpEmailScannerUpdateRulePrioritiesArgs;
use crate::providers::cloudflare::clients::DlpEntriesCreateEntryArgs;
use crate::providers::cloudflare::clients::DlpEntriesCreateIntegrationEntryArgs;
use crate::providers::cloudflare::clients::DlpEntriesCreatePredefinedEntryArgs;
use crate::providers::cloudflare::clients::DlpEntriesDeleteEntryArgs;
use crate::providers::cloudflare::clients::DlpEntriesDeleteIntegrationEntryArgs;
use crate::providers::cloudflare::clients::DlpEntriesDeletePredefinedEntryArgs;
use crate::providers::cloudflare::clients::DlpEntriesGetDlpEntryArgs;
use crate::providers::cloudflare::clients::DlpEntriesListAllEntriesArgs;
use crate::providers::cloudflare::clients::DlpEntriesUpdateCustomEntryArgs;
use crate::providers::cloudflare::clients::DlpEntriesUpdateEntryArgs;
use crate::providers::cloudflare::clients::DlpEntriesUpdateIntegrationEntryArgs;
use crate::providers::cloudflare::clients::DlpEntriesUpdatePredefinedEntryArgs;
use crate::providers::cloudflare::clients::DlpLimitsGetArgs;
use crate::providers::cloudflare::clients::DlpPatternValidateArgs;
use crate::providers::cloudflare::clients::DlpPayloadLogGetArgs;
use crate::providers::cloudflare::clients::DlpPayloadLogPutArgs;
use crate::providers::cloudflare::clients::DlpProfilesCreateCustomProfilesArgs;
use crate::providers::cloudflare::clients::DlpProfilesCreatePredefinedProfileArgs;
use crate::providers::cloudflare::clients::DlpProfilesCreatePredefinedProfileConfigArgs;
use crate::providers::cloudflare::clients::DlpProfilesDeleteCustomProfileArgs;
use crate::providers::cloudflare::clients::DlpProfilesDeletePredefinedProfileArgs;
use crate::providers::cloudflare::clients::DlpProfilesGetCustomProfileArgs;
use crate::providers::cloudflare::clients::DlpProfilesGetDlpProfileArgs;
use crate::providers::cloudflare::clients::DlpProfilesGetPredefinedProfileArgs;
use crate::providers::cloudflare::clients::DlpProfilesGetPredefinedProfileConfigArgs;
use crate::providers::cloudflare::clients::DlpProfilesListAllCustomProfilesArgs;
use crate::providers::cloudflare::clients::DlpProfilesListAllProfilesArgs;
use crate::providers::cloudflare::clients::DlpProfilesUpdateCustomProfileArgs;
use crate::providers::cloudflare::clients::DlpProfilesUpdatePredefinedProfileArgs;
use crate::providers::cloudflare::clients::DlpProfilesUpdatePredefinedProfileConfigArgs;
use crate::providers::cloudflare::clients::DlpRiskScoreBehaviorsGetArgs;
use crate::providers::cloudflare::clients::DlpRiskScoreBehaviorsPutArgs;
use crate::providers::cloudflare::clients::DlpRiskScoreResetPostArgs;
use crate::providers::cloudflare::clients::DlpRiskScoreSummaryGetArgs;
use crate::providers::cloudflare::clients::DlpRiskScoreSummaryGetForUserArgs;
use crate::providers::cloudflare::clients::DlpSettingsDeleteArgs;
use crate::providers::cloudflare::clients::DlpSettingsEditArgs;
use crate::providers::cloudflare::clients::DlpSettingsGetArgs;
use crate::providers::cloudflare::clients::DlpSettingsUpdateArgs;
use crate::providers::cloudflare::clients::DlpZtRiskScoreIntegrationCreateArgs;
use crate::providers::cloudflare::clients::DlpZtRiskScoreIntegrationDeleteArgs;
use crate::providers::cloudflare::clients::DlpZtRiskScoreIntegrationGetArgs;
use crate::providers::cloudflare::clients::DlpZtRiskScoreIntegrationGetByReferenceIdArgs;
use crate::providers::cloudflare::clients::DlpZtRiskScoreIntegrationListArgs;
use crate::providers::cloudflare::clients::DlpZtRiskScoreIntegrationUpdateArgs;
use crate::providers::cloudflare::clients::DlsAccountRegionalHostnamesAccountCreateHostnameArgs;
use crate::providers::cloudflare::clients::DlsAccountRegionalHostnamesAccountDeleteHostnameArgs;
use crate::providers::cloudflare::clients::DlsAccountRegionalHostnamesAccountFetchHostnameArgs;
use crate::providers::cloudflare::clients::DlsAccountRegionalHostnamesAccountListHostnamesArgs;
use crate::providers::cloudflare::clients::DlsAccountRegionalHostnamesAccountListRegionsArgs;
use crate::providers::cloudflare::clients::DlsAccountRegionalHostnamesAccountPatchHostnameArgs;
use crate::providers::cloudflare::clients::DnsAnalyticsByTimeArgs;
use crate::providers::cloudflare::clients::DnsAnalyticsTableArgs;
use crate::providers::cloudflare::clients::DnsFirewallAnalyticsByTimeArgs;
use crate::providers::cloudflare::clients::DnsFirewallAnalyticsTableArgs;
use crate::providers::cloudflare::clients::DnsFirewallCreateDnsFirewallClusterArgs;
use crate::providers::cloudflare::clients::DnsFirewallDeleteDnsFirewallClusterArgs;
use crate::providers::cloudflare::clients::DnsFirewallDnsFirewallClusterDetailsArgs;
use crate::providers::cloudflare::clients::DnsFirewallListDnsFirewallClustersArgs;
use crate::providers::cloudflare::clients::DnsFirewallShowDnsFirewallClusterReverseDnsArgs;
use crate::providers::cloudflare::clients::DnsFirewallUpdateDnsFirewallClusterArgs;
use crate::providers::cloudflare::clients::DnsFirewallUpdateDnsFirewallClusterReverseDnsArgs;
use crate::providers::cloudflare::clients::DnsRecordsForAZoneApplyDnsScanResultsArgs;
use crate::providers::cloudflare::clients::DnsRecordsForAZoneBatchDnsRecordsArgs;
use crate::providers::cloudflare::clients::DnsRecordsForAZoneCreateDnsRecordArgs;
use crate::providers::cloudflare::clients::DnsRecordsForAZoneDeleteDnsRecordArgs;
use crate::providers::cloudflare::clients::DnsRecordsForAZoneDnsRecordDetailsArgs;
use crate::providers::cloudflare::clients::DnsRecordsForAZoneExportDnsRecordsArgs;
use crate::providers::cloudflare::clients::DnsRecordsForAZoneGetUsageArgs;
use crate::providers::cloudflare::clients::DnsRecordsForAZoneImportDnsRecordsArgs;
use crate::providers::cloudflare::clients::DnsRecordsForAZoneListDnsRecordsArgs;
use crate::providers::cloudflare::clients::DnsRecordsForAZonePatchDnsRecordArgs;
use crate::providers::cloudflare::clients::DnsRecordsForAZoneReviewDnsScanArgs;
use crate::providers::cloudflare::clients::DnsRecordsForAZoneScanDnsRecordsArgs;
use crate::providers::cloudflare::clients::DnsRecordsForAZoneTriggerDnsScanArgs;
use crate::providers::cloudflare::clients::DnsRecordsForAZoneUpdateDnsRecordArgs;
use crate::providers::cloudflare::clients::DnsRecordsForAnAccountGetUsageArgs;
use crate::providers::cloudflare::clients::DnsSettingsForAZoneListDnsSettingsArgs;
use crate::providers::cloudflare::clients::DnsSettingsForAZoneUpdateDnsSettingsArgs;
use crate::providers::cloudflare::clients::DnsSettingsForAnAccountListDnsSettingsArgs;
use crate::providers::cloudflare::clients::DnsSettingsForAnAccountUpdateDnsSettingsArgs;
use crate::providers::cloudflare::clients::DnsViewsForAnAccountCreateInternalDnsViewsArgs;
use crate::providers::cloudflare::clients::DnsViewsForAnAccountDeleteInternalDnsViewArgs;
use crate::providers::cloudflare::clients::DnsViewsForAnAccountGetInternalDnsViewArgs;
use crate::providers::cloudflare::clients::DnsViewsForAnAccountListInternalDnsViewsArgs;
use crate::providers::cloudflare::clients::DnsViewsForAnAccountUpdateInternalDnsViewArgs;
use crate::providers::cloudflare::clients::DnssecDeleteDnssecRecordsArgs;
use crate::providers::cloudflare::clients::DnssecDnssecDetailsArgs;
use crate::providers::cloudflare::clients::DnssecEditDnssecStatusArgs;
use crate::providers::cloudflare::clients::DomainHistoryGetDomainHistoryArgs;
use crate::providers::cloudflare::clients::DomainIntelligenceGetDomainDetailsArgs;
use crate::providers::cloudflare::clients::DomainIntelligenceGetMultipleDomainDetailsArgs;
use crate::providers::cloudflare::clients::DurableObjectsNamespaceListNamespacesArgs;
use crate::providers::cloudflare::clients::DurableObjectsNamespaceListObjectsArgs;
use crate::providers::cloudflare::clients::EditAudioTracksArgs;
use crate::providers::cloudflare::clients::EditParticipantArgs;
use crate::providers::cloudflare::clients::EditWebhookArgs;
use crate::providers::cloudflare::clients::EditWorkerArgs;
use crate::providers::cloudflare::clients::EmailRoutingDestinationAddressesCreateADestinationAddressArgs;
use crate::providers::cloudflare::clients::EmailRoutingDestinationAddressesDeleteDestinationAddressArgs;
use crate::providers::cloudflare::clients::EmailRoutingDestinationAddressesGetADestinationAddressArgs;
use crate::providers::cloudflare::clients::EmailRoutingDestinationAddressesListDestinationAddressesArgs;
use crate::providers::cloudflare::clients::EmailRoutingRoutingRulesCreateRoutingRuleArgs;
use crate::providers::cloudflare::clients::EmailRoutingRoutingRulesDeleteRoutingRuleArgs;
use crate::providers::cloudflare::clients::EmailRoutingRoutingRulesGetCatchAllRuleArgs;
use crate::providers::cloudflare::clients::EmailRoutingRoutingRulesGetRoutingRuleArgs;
use crate::providers::cloudflare::clients::EmailRoutingRoutingRulesListRoutingRulesArgs;
use crate::providers::cloudflare::clients::EmailRoutingRoutingRulesUpdateCatchAllRuleArgs;
use crate::providers::cloudflare::clients::EmailRoutingRoutingRulesUpdateRoutingRuleArgs;
use crate::providers::cloudflare::clients::EmailRoutingSettingsDisableEmailRoutingArgs;
use crate::providers::cloudflare::clients::EmailRoutingSettingsDisableEmailRoutingDnsArgs;
use crate::providers::cloudflare::clients::EmailRoutingSettingsEmailRoutingDnsSettingsArgs;
use crate::providers::cloudflare::clients::EmailRoutingSettingsEnableEmailRoutingArgs;
use crate::providers::cloudflare::clients::EmailRoutingSettingsEnableEmailRoutingDnsArgs;
use crate::providers::cloudflare::clients::EmailRoutingSettingsGetEmailRoutingSettingsArgs;
use crate::providers::cloudflare::clients::EmailRoutingSettingsUnlockEmailRoutingDnsArgs;
use crate::providers::cloudflare::clients::EmailSecurityBatchAllowPoliciesArgs;
use crate::providers::cloudflare::clients::EmailSecurityBatchBlockedSendersArgs;
use crate::providers::cloudflare::clients::EmailSecurityBatchSendingDomainRestrictionsArgs;
use crate::providers::cloudflare::clients::EmailSecurityBatchTrustedDomainsArgs;
use crate::providers::cloudflare::clients::EmailSecurityCreateAllowPolicyArgs;
use crate::providers::cloudflare::clients::EmailSecurityCreateBlockedSenderArgs;
use crate::providers::cloudflare::clients::EmailSecurityCreateDisplayNameArgs;
use crate::providers::cloudflare::clients::EmailSecurityCreateTrustedDomainArgs;
use crate::providers::cloudflare::clients::EmailSecurityDeleteAllowPolicyArgs;
use crate::providers::cloudflare::clients::EmailSecurityDeleteBlockedSenderArgs;
use crate::providers::cloudflare::clients::EmailSecurityDeleteDisplayNameArgs;
use crate::providers::cloudflare::clients::EmailSecurityDeleteDomainArgs;
use crate::providers::cloudflare::clients::EmailSecurityDeleteDomainsArgs;
use crate::providers::cloudflare::clients::EmailSecurityDeleteTrustedDomainArgs;
use crate::providers::cloudflare::clients::EmailSecurityGetAllowPolicyArgs;
use crate::providers::cloudflare::clients::EmailSecurityGetBlockedSenderArgs;
use crate::providers::cloudflare::clients::EmailSecurityGetDisplayNameArgs;
use crate::providers::cloudflare::clients::EmailSecurityGetDomainArgs;
use crate::providers::cloudflare::clients::EmailSecurityGetMessageArgs;
use crate::providers::cloudflare::clients::EmailSecurityGetMessageDetectionsArgs;
use crate::providers::cloudflare::clients::EmailSecurityGetMessagePreviewArgs;
use crate::providers::cloudflare::clients::EmailSecurityGetMessageRawArgs;
use crate::providers::cloudflare::clients::EmailSecurityGetMessageTraceArgs;
use crate::providers::cloudflare::clients::EmailSecurityGetPhishguardReportsArgs;
use crate::providers::cloudflare::clients::EmailSecurityGetTrustedDomainArgs;
use crate::providers::cloudflare::clients::EmailSecurityInvestigateArgs;
use crate::providers::cloudflare::clients::EmailSecurityListAllowPoliciesArgs;
use crate::providers::cloudflare::clients::EmailSecurityListBlockedSendersArgs;
use crate::providers::cloudflare::clients::EmailSecurityListDisplayNamesArgs;
use crate::providers::cloudflare::clients::EmailSecurityListDomainsArgs;
use crate::providers::cloudflare::clients::EmailSecurityListTrustedDomainsArgs;
use crate::providers::cloudflare::clients::EmailSecurityPostBulkMessageMoveArgs;
use crate::providers::cloudflare::clients::EmailSecurityPostMessageMoveArgs;
use crate::providers::cloudflare::clients::EmailSecurityPostPreviewArgs;
use crate::providers::cloudflare::clients::EmailSecurityPostReclassifyArgs;
use crate::providers::cloudflare::clients::EmailSecurityPostReleaseArgs;
use crate::providers::cloudflare::clients::EmailSecuritySubmissionsArgs;
use crate::providers::cloudflare::clients::EmailSecurityUpdateAllowPolicyArgs;
use crate::providers::cloudflare::clients::EmailSecurityUpdateBlockedSenderArgs;
use crate::providers::cloudflare::clients::EmailSecurityUpdateDisplayNameArgs;
use crate::providers::cloudflare::clients::EmailSecurityUpdateDomainArgs;
use crate::providers::cloudflare::clients::EmailSecurityUpdateTrustedDomainArgs;
use crate::providers::cloudflare::clients::EmailSendingAccountSendBuilderArgs;
use crate::providers::cloudflare::clients::EmailSendingAccountSendRawMessageArgs;
use crate::providers::cloudflare::clients::EmailSendingSubdomainsCreateSendingSubdomainArgs;
use crate::providers::cloudflare::clients::EmailSendingSubdomainsDeleteSendingSubdomainArgs;
use crate::providers::cloudflare::clients::EmailSendingSubdomainsGetSendingSubdomainArgs;
use crate::providers::cloudflare::clients::EmailSendingSubdomainsGetSendingSubdomainDnsArgs;
use crate::providers::cloudflare::clients::EmailSendingSubdomainsListSendingSubdomainsArgs;
use crate::providers::cloudflare::clients::EmailSendingSubdomainsPreviewSendingSubdomainArgs;
use crate::providers::cloudflare::clients::EnableCatalogArgs;
use crate::providers::cloudflare::clients::FetchAllLivestreamsArgs;
use crate::providers::cloudflare::clients::FiltersCreateFiltersArgs;
use crate::providers::cloudflare::clients::FiltersDeleteAFilterArgs;
use crate::providers::cloudflare::clients::FiltersDeleteFiltersArgs;
use crate::providers::cloudflare::clients::FiltersGetAFilterArgs;
use crate::providers::cloudflare::clients::FiltersListFiltersArgs;
use crate::providers::cloudflare::clients::FiltersUpdateAFilterArgs;
use crate::providers::cloudflare::clients::FiltersUpdateFiltersArgs;
use crate::providers::cloudflare::clients::FirewallForAiCustomTopicsGetArgs;
use crate::providers::cloudflare::clients::FirewallForAiCustomTopicsPutArgs;
use crate::providers::cloudflare::clients::FirewallForAiSettingsGetArgs;
use crate::providers::cloudflare::clients::FirewallForAiSettingsPutArgs;
use crate::providers::cloudflare::clients::FirewallRulesCreateFirewallRulesArgs;
use crate::providers::cloudflare::clients::FirewallRulesDeleteAFirewallRuleArgs;
use crate::providers::cloudflare::clients::FirewallRulesDeleteFirewallRulesArgs;
use crate::providers::cloudflare::clients::FirewallRulesGetAFirewallRuleArgs;
use crate::providers::cloudflare::clients::FirewallRulesListFirewallRulesArgs;
use crate::providers::cloudflare::clients::FirewallRulesUpdateAFirewallRuleArgs;
use crate::providers::cloudflare::clients::FirewallRulesUpdateFirewallRulesArgs;
use crate::providers::cloudflare::clients::FirewallRulesUpdatePriorityOfAFirewallRuleArgs;
use crate::providers::cloudflare::clients::FirewallRulesUpdatePriorityOfFirewallRulesArgs;
use crate::providers::cloudflare::clients::FraudDetectionZoneGetSettingsArgs;
use crate::providers::cloudflare::clients::FraudDetectionZoneUpdateSettingsArgs;
use crate::providers::cloudflare::clients::GenerateImageRegistryCredentialsArgs;
use crate::providers::cloudflare::clients::GetAbuseReportArgs;
use crate::providers::cloudflare::clients::GetAccountEntrypointRulesetArgs;
use crate::providers::cloudflare::clients::GetAccountEntrypointRulesetVersionArgs;
use crate::providers::cloudflare::clients::GetAccountLimitsArgs;
use crate::providers::cloudflare::clients::GetAccountRulesetArgs;
use crate::providers::cloudflare::clients::GetAccountRulesetVersionArgs;
use crate::providers::cloudflare::clients::GetAccountsAccountIdLogpushDatasetsDatasetIdFieldsArgs;
use crate::providers::cloudflare::clients::GetAccountsAccountIdLogpushDatasetsDatasetIdJobsArgs;
use crate::providers::cloudflare::clients::GetAccountsAccountIdLogpushJobsArgs;
use crate::providers::cloudflare::clients::GetAccountsAccountIdLogpushJobsJobIdArgs;
use crate::providers::cloudflare::clients::GetAccountsAccountIdLogsControlCmbConfigArgs;
use crate::providers::cloudflare::clients::GetActiveRecordingArgs;
use crate::providers::cloudflare::clients::GetActiveSessionArgs;
use crate::providers::cloudflare::clients::GetAllMeetingsArgs;
use crate::providers::cloudflare::clients::GetAllRecordingsArgs;
use crate::providers::cloudflare::clients::GetAllSsoConnectorsArgs;
use crate::providers::cloudflare::clients::GetAllWebhooksArgs;
use crate::providers::cloudflare::clients::GetAllowlistPrefixArgs;
use crate::providers::cloudflare::clients::GetApplicationByIdArgs;
use crate::providers::cloudflare::clients::GetApplicationsArgs;
use crate::providers::cloudflare::clients::GetAppsArgs;
use crate::providers::cloudflare::clients::GetAttackerListArgs;
use crate::providers::cloudflare::clients::GetBinDbGetBinaryArgs;
use crate::providers::cloudflare::clients::GetBuildByUuidArgs;
use crate::providers::cloudflare::clients::GetBuildLogsArgs;
use crate::providers::cloudflare::clients::GetBuildsByVersionIdsArgs;
use crate::providers::cloudflare::clients::GetCatalogDetailsArgs;
use crate::providers::cloudflare::clients::GetCategoriesArgs;
use crate::providers::cloudflare::clients::GetCategoryByIdArgs;
use crate::providers::cloudflare::clients::GetCategoryListArgs;
use crate::providers::cloudflare::clients::GetCategoryListCompleteArgs;
use crate::providers::cloudflare::clients::GetCategoryReadArgs;
use crate::providers::cloudflare::clients::GetCniArgs;
use crate::providers::cloudflare::clients::GetCommandsArgs;
use crate::providers::cloudflare::clients::GetCommandsCommandIdDownloadsFilenameArgs;
use crate::providers::cloudflare::clients::GetCommandsEligibleDevicesArgs;
use crate::providers::cloudflare::clients::GetCommandsQuotaArgs;
use crate::providers::cloudflare::clients::GetConfigFetchArgs;
use crate::providers::cloudflare::clients::GetCountryReadArgs;
use crate::providers::cloudflare::clients::GetDatasetListArgs;
use crate::providers::cloudflare::clients::GetDatasetReadArgs;
use crate::providers::cloudflare::clients::GetDeviceArgs;
use crate::providers::cloudflare::clients::GetDexRuleArgs;
use crate::providers::cloudflare::clients::GetDnsProtectionRuleArgs;
use crate::providers::cloudflare::clients::GetDomainMatchListArgs;
use crate::providers::cloudflare::clients::GetEventAggregateArgs;
use crate::providers::cloudflare::clients::GetEventListGetArgs;
use crate::providers::cloudflare::clients::GetEventQueryAlertListArgs;
use crate::providers::cloudflare::clients::GetEventQueryAlertReadArgs;
use crate::providers::cloudflare::clients::GetEventQueryListArgs;
use crate::providers::cloudflare::clients::GetEventQueryReadArgs;
use crate::providers::cloudflare::clients::GetEventRawReadArgs;
use crate::providers::cloudflare::clients::GetEventRawReadDsArgs;
use crate::providers::cloudflare::clients::GetEventReadArgs;
use crate::providers::cloudflare::clients::GetEventReadDeprecatedArgs;
use crate::providers::cloudflare::clients::GetEventRelationshipsArgs;
use crate::providers::cloudflare::clients::GetGetDomainQueriesArgs;
use crate::providers::cloudflare::clients::GetGetLogoQueriesArgs;
use crate::providers::cloudflare::clients::GetGetOpenPortsArgs;
use crate::providers::cloudflare::clients::GetGroupListArgs;
use crate::providers::cloudflare::clients::GetGroupMemberListArgs;
use crate::providers::cloudflare::clients::GetGroupReadArgs;
use crate::providers::cloudflare::clients::GetHyperdriveArgs;
use crate::providers::cloudflare::clients::GetIndicatorListArgs;
use crate::providers::cloudflare::clients::GetIndicatorListLegacyArgs;
use crate::providers::cloudflare::clients::GetIndicatorReadArgs;
use crate::providers::cloudflare::clients::GetIndicatorTagsListArgs;
use crate::providers::cloudflare::clients::GetIndicatorTypesListArgs;
use crate::providers::cloudflare::clients::GetInterconnectArgs;
use crate::providers::cloudflare::clients::GetInterconnectLoaArgs;
use crate::providers::cloudflare::clients::GetInterconnectStatusArgs;
use crate::providers::cloudflare::clients::GetIpProfileArgs;
use crate::providers::cloudflare::clients::GetLatestBuildsByScriptsArgs;
use crate::providers::cloudflare::clients::GetLegacyIndicatorTypesListArgs;
use crate::providers::cloudflare::clients::GetLivestreamAnalyticsCompleteArgs;
use crate::providers::cloudflare::clients::GetLogoMatchListArgs;
use crate::providers::cloudflare::clients::GetMaintenanceConfigArgs;
use crate::providers::cloudflare::clients::GetMeetingArgs;
use crate::providers::cloudflare::clients::GetMeetingParticipantArgs;
use crate::providers::cloudflare::clients::GetMeetingParticipantsArgs;
use crate::providers::cloudflare::clients::GetOneRecordingArgs;
use crate::providers::cloudflare::clients::GetOrgAnalyticsArgs;
use crate::providers::cloudflare::clients::GetParticipantDataFromPeerIdArgs;
use crate::providers::cloudflare::clients::GetParticipantDetailsArgs;
use crate::providers::cloudflare::clients::GetPermissionListArgs;
use crate::providers::cloudflare::clients::GetPrefixArgs;
use crate::providers::cloudflare::clients::GetPresetsArgs;
use crate::providers::cloudflare::clients::GetPresetsPresetIdArgs;
use crate::providers::cloudflare::clients::GetProtectionStatusArgs;
use crate::providers::cloudflare::clients::GetRegistrationArgs;
use crate::providers::cloudflare::clients::GetRegistrationOverrideCodesArgs;
use crate::providers::cloudflare::clients::GetSecurityCenterInsightContextArgs;
use crate::providers::cloudflare::clients::GetSecurityCenterInsightCountsByClassArgs;
use crate::providers::cloudflare::clients::GetSecurityCenterInsightCountsBySeverityArgs;
use crate::providers::cloudflare::clients::GetSecurityCenterInsightCountsByTypeArgs;
use crate::providers::cloudflare::clients::GetSecurityCenterInsightsArgs;
use crate::providers::cloudflare::clients::GetSecurityCenterIssueCountsByClassArgs;
use crate::providers::cloudflare::clients::GetSecurityCenterIssueCountsBySeverityArgs;
use crate::providers::cloudflare::clients::GetSecurityCenterIssueCountsByTypeArgs;
use crate::providers::cloudflare::clients::GetSecurityCenterIssueTypesArgs;
use crate::providers::cloudflare::clients::GetSecurityCenterIssuesArgs;
use crate::providers::cloudflare::clients::GetSecurityTxtArgs;
use crate::providers::cloudflare::clients::GetSessionChatArgs;
use crate::providers::cloudflare::clients::GetSessionDetailsArgs;
use crate::providers::cloudflare::clients::GetSessionParticipantsArgs;
use crate::providers::cloudflare::clients::GetSessionSummaryArgs;
use crate::providers::cloudflare::clients::GetSessionTranscriptArgs;
use crate::providers::cloudflare::clients::GetSessionsArgs;
use crate::providers::cloudflare::clients::GetSettingsArgs;
use crate::providers::cloudflare::clients::GetSlotArgs;
use crate::providers::cloudflare::clients::GetSsoConnectorArgs;
use crate::providers::cloudflare::clients::GetSynProtectionFilterArgs;
use crate::providers::cloudflare::clients::GetSynProtectionRuleArgs;
use crate::providers::cloudflare::clients::GetTableMaintenanceConfigArgs;
use crate::providers::cloudflare::clients::GetTagCategoryListArgs;
use crate::providers::cloudflare::clients::GetTagIndicatorsListArgs;
use crate::providers::cloudflare::clients::GetTagListArgs;
use crate::providers::cloudflare::clients::GetTargetIndustryListArgs;
use crate::providers::cloudflare::clients::GetTargetIndustryListByDatasetArgs;
use crate::providers::cloudflare::clients::GetTargetIndustryListCompleteArgs;
use crate::providers::cloudflare::clients::GetTcpFlowProtectionFilterArgs;
use crate::providers::cloudflare::clients::GetTcpFlowProtectionRuleArgs;
use crate::providers::cloudflare::clients::GetTotalQueriesArgs;
use crate::providers::cloudflare::clients::GetUrlNormalizationArgs;
use crate::providers::cloudflare::clients::GetV2ActiveLivestreamSessionDetailsArgs;
use crate::providers::cloudflare::clients::GetV2LivestreamSessionLivestreamIdArgs;
use crate::providers::cloudflare::clients::GetV2LivestreamsLivestreamSessionIdArgs;
use crate::providers::cloudflare::clients::GetV2LivestreamsessionSessionMeetingIdActiveLivestreamArgs;
use crate::providers::cloudflare::clients::GetV2MeetingsMeetingIdActiveLivestreamArgs;
use crate::providers::cloudflare::clients::GetV4AccountsByAccountIdPipelinesByPipelineNameDeprecatedArgs;
use crate::providers::cloudflare::clients::GetV4AccountsByAccountIdPipelinesDeprecatedArgs;
use crate::providers::cloudflare::clients::GetV4AccountsByAccountIdPipelinesV1PipelinesArgs;
use crate::providers::cloudflare::clients::GetV4AccountsByAccountIdPipelinesV1PipelinesByPipelineIdArgs;
use crate::providers::cloudflare::clients::GetV4AccountsByAccountIdPipelinesV1SinksArgs;
use crate::providers::cloudflare::clients::GetV4AccountsByAccountIdPipelinesV1SinksBySinkIdArgs;
use crate::providers::cloudflare::clients::GetV4AccountsByAccountIdPipelinesV1StreamsArgs;
use crate::providers::cloudflare::clients::GetV4AccountsByAccountIdPipelinesV1StreamsByStreamIdArgs;
use crate::providers::cloudflare::clients::GetWebhookArgs;
use crate::providers::cloudflare::clients::GetWorkerArgs;
use crate::providers::cloudflare::clients::GetWorkerConfigAutofillArgs;
use crate::providers::cloudflare::clients::GetWorkerVersionArgs;
use crate::providers::cloudflare::clients::GetZoneEntrypointRulesetArgs;
use crate::providers::cloudflare::clients::GetZoneEntrypointRulesetVersionArgs;
use crate::providers::cloudflare::clients::GetZoneRulesetArgs;
use crate::providers::cloudflare::clients::GetZoneRulesetVersionArgs;
use crate::providers::cloudflare::clients::GetZoneSecurityCenterInsightCountsByClassArgs;
use crate::providers::cloudflare::clients::GetZoneSecurityCenterInsightCountsBySeverityArgs;
use crate::providers::cloudflare::clients::GetZoneSecurityCenterInsightCountsByTypeArgs;
use crate::providers::cloudflare::clients::GetZoneSecurityCenterInsightsArgs;
use crate::providers::cloudflare::clients::GetZoneSnippetArgs;
use crate::providers::cloudflare::clients::GetZoneSnippetContentArgs;
use crate::providers::cloudflare::clients::GetZonesZoneIdLogpushDatasetsDatasetIdFieldsArgs;
use crate::providers::cloudflare::clients::GetZonesZoneIdLogpushDatasetsDatasetIdJobsArgs;
use crate::providers::cloudflare::clients::GetZonesZoneIdLogpushEdgeJobsArgs;
use crate::providers::cloudflare::clients::GetZonesZoneIdLogpushJobsArgs;
use crate::providers::cloudflare::clients::GetZonesZoneIdLogpushJobsJobIdArgs;
use crate::providers::cloudflare::clients::GetZonesZoneIdLogsControlRetentionFlagArgs;
use crate::providers::cloudflare::clients::GetZonesZoneIdLogsRayidsRayIdArgs;
use crate::providers::cloudflare::clients::GetZonesZoneIdLogsReceivedArgs;
use crate::providers::cloudflare::clients::GetZonesZoneIdLogsReceivedFieldsArgs;
use crate::providers::cloudflare::clients::GetZonesZoneIdentifierZarazConfigArgs;
use crate::providers::cloudflare::clients::GetZonesZoneIdentifierZarazConfigHistoryArgs;
use crate::providers::cloudflare::clients::GetZonesZoneIdentifierZarazDefaultArgs;
use crate::providers::cloudflare::clients::GetZonesZoneIdentifierZarazExportArgs;
use crate::providers::cloudflare::clients::GetZonesZoneIdentifierZarazHistoryArgs;
use crate::providers::cloudflare::clients::GetZonesZoneIdentifierZarazWorkflowArgs;
use crate::providers::cloudflare::clients::HealthChecksCreateHealthCheckArgs;
use crate::providers::cloudflare::clients::HealthChecksCreatePreviewHealthCheckArgs;
use crate::providers::cloudflare::clients::HealthChecksDeleteHealthCheckArgs;
use crate::providers::cloudflare::clients::HealthChecksDeletePreviewHealthCheckArgs;
use crate::providers::cloudflare::clients::HealthChecksHealthCheckDetailsArgs;
use crate::providers::cloudflare::clients::HealthChecksHealthCheckPreviewDetailsArgs;
use crate::providers::cloudflare::clients::HealthChecksListHealthChecksArgs;
use crate::providers::cloudflare::clients::HealthChecksPatchHealthCheckArgs;
use crate::providers::cloudflare::clients::HealthChecksUpdateHealthCheckArgs;
use crate::providers::cloudflare::clients::InfraTargetsDeleteArgs;
use crate::providers::cloudflare::clients::InfraTargetsDeleteBatchArgs;
use crate::providers::cloudflare::clients::InfraTargetsDeleteBatchPostArgs;
use crate::providers::cloudflare::clients::InfraTargetsGetArgs;
use crate::providers::cloudflare::clients::InfraTargetsListArgs;
use crate::providers::cloudflare::clients::InfraTargetsPostArgs;
use crate::providers::cloudflare::clients::InfraTargetsPutArgs;
use crate::providers::cloudflare::clients::InfraTargetsPutBatchArgs;
use crate::providers::cloudflare::clients::InitNewSsoConnectorArgs;
use crate::providers::cloudflare::clients::IpAccessRulesForAUserDeleteAnIpAccessRuleArgs;
use crate::providers::cloudflare::clients::IpAccessRulesForAUserListIpAccessRulesArgs;
use crate::providers::cloudflare::clients::IpAccessRulesForAUserUpdateAnIpAccessRuleArgs;
use crate::providers::cloudflare::clients::IpAccessRulesForAZoneCreateAnIpAccessRuleArgs;
use crate::providers::cloudflare::clients::IpAccessRulesForAZoneDeleteAnIpAccessRuleArgs;
use crate::providers::cloudflare::clients::IpAccessRulesForAZoneListIpAccessRulesArgs;
use crate::providers::cloudflare::clients::IpAccessRulesForAZoneUpdateAnIpAccessRuleArgs;
use crate::providers::cloudflare::clients::IpAccessRulesForAnAccountCreateAnIpAccessRuleArgs;
use crate::providers::cloudflare::clients::IpAccessRulesForAnAccountDeleteAnIpAccessRuleArgs;
use crate::providers::cloudflare::clients::IpAccessRulesForAnAccountGetAnIpAccessRuleArgs;
use crate::providers::cloudflare::clients::IpAccessRulesForAnAccountListIpAccessRulesArgs;
use crate::providers::cloudflare::clients::IpAccessRulesForAnAccountUpdateAnIpAccessRuleArgs;
use crate::providers::cloudflare::clients::IpAddressManagementAddressMapsAddAZoneMembershipToAnAddressMapArgs;
use crate::providers::cloudflare::clients::IpAddressManagementAddressMapsAddAnAccountMembershipToAnAddressMapArgs;
use crate::providers::cloudflare::clients::IpAddressManagementAddressMapsAddAnIpToAnAddressMapArgs;
use crate::providers::cloudflare::clients::IpAddressManagementAddressMapsAddressMapDetailsArgs;
use crate::providers::cloudflare::clients::IpAddressManagementAddressMapsCreateAddressMapArgs;
use crate::providers::cloudflare::clients::IpAddressManagementAddressMapsDeleteAddressMapArgs;
use crate::providers::cloudflare::clients::IpAddressManagementAddressMapsListAddressMapsArgs;
use crate::providers::cloudflare::clients::IpAddressManagementAddressMapsRemoveAZoneMembershipFromAnAddressMapArgs;
use crate::providers::cloudflare::clients::IpAddressManagementAddressMapsRemoveAnAccountMembershipFromAnAddressMapArgs;
use crate::providers::cloudflare::clients::IpAddressManagementAddressMapsRemoveAnIpFromAnAddressMapArgs;
use crate::providers::cloudflare::clients::IpAddressManagementAddressMapsUpdateAddressMapArgs;
use crate::providers::cloudflare::clients::IpAddressManagementDynamicAdvertisementGetAdvertisementStatusArgs;
use crate::providers::cloudflare::clients::IpAddressManagementDynamicAdvertisementUpdatePrefixDynamicAdvertisementStatusArgs;
use crate::providers::cloudflare::clients::IpAddressManagementListLeasesArgs;
use crate::providers::cloudflare::clients::IpAddressManagementPrefixDelegationCreatePrefixDelegationArgs;
use crate::providers::cloudflare::clients::IpAddressManagementPrefixDelegationDeletePrefixDelegationArgs;
use crate::providers::cloudflare::clients::IpAddressManagementPrefixDelegationListPrefixDelegationsArgs;
use crate::providers::cloudflare::clients::IpAddressManagementPrefixesAddPrefixArgs;
use crate::providers::cloudflare::clients::IpAddressManagementPrefixesCreateBgpPrefixArgs;
use crate::providers::cloudflare::clients::IpAddressManagementPrefixesDeleteBgpPrefixArgs;
use crate::providers::cloudflare::clients::IpAddressManagementPrefixesDeletePrefixArgs;
use crate::providers::cloudflare::clients::IpAddressManagementPrefixesDownloadLoaDocumentArgs;
use crate::providers::cloudflare::clients::IpAddressManagementPrefixesFetchBgpPrefixArgs;
use crate::providers::cloudflare::clients::IpAddressManagementPrefixesListBgpPrefixesArgs;
use crate::providers::cloudflare::clients::IpAddressManagementPrefixesListPrefixesArgs;
use crate::providers::cloudflare::clients::IpAddressManagementPrefixesPrefixDetailsArgs;
use crate::providers::cloudflare::clients::IpAddressManagementPrefixesUpdateBgpPrefixArgs;
use crate::providers::cloudflare::clients::IpAddressManagementPrefixesUpdatePrefixDescriptionArgs;
use crate::providers::cloudflare::clients::IpAddressManagementPrefixesUploadLoaDocumentArgs;
use crate::providers::cloudflare::clients::IpAddressManagementPrefixesValidatePrefixArgs;
use crate::providers::cloudflare::clients::IpAddressManagementServiceBindingsCreateServiceBindingArgs;
use crate::providers::cloudflare::clients::IpAddressManagementServiceBindingsDeleteServiceBindingArgs;
use crate::providers::cloudflare::clients::IpAddressManagementServiceBindingsGetServiceBindingArgs;
use crate::providers::cloudflare::clients::IpAddressManagementServiceBindingsListServiceBindingsArgs;
use crate::providers::cloudflare::clients::IpAddressManagementServiceBindingsListServicesArgs;
use crate::providers::cloudflare::clients::IpIntelligenceGetIpOverviewArgs;
use crate::providers::cloudflare::clients::IpListGetIpListsArgs;
use crate::providers::cloudflare::clients::KeylessSslForAZoneCreateKeylessSslConfigurationArgs;
use crate::providers::cloudflare::clients::KeylessSslForAZoneDeleteKeylessSslConfigurationArgs;
use crate::providers::cloudflare::clients::KeylessSslForAZoneEditKeylessSslConfigurationArgs;
use crate::providers::cloudflare::clients::KeylessSslForAZoneGetKeylessSslConfigurationArgs;
use crate::providers::cloudflare::clients::KeylessSslForAZoneListKeylessSslConfigurationsArgs;
use crate::providers::cloudflare::clients::KickAllParticipantsArgs;
use crate::providers::cloudflare::clients::KickPartcipantsArgs;
use crate::providers::cloudflare::clients::ListAbuseReportsArgs;
use crate::providers::cloudflare::clients::ListAccountEntrypointRulesetVersionsArgs;
use crate::providers::cloudflare::clients::ListAccountRulesetVersionRulesByTagArgs;
use crate::providers::cloudflare::clients::ListAccountRulesetVersionsArgs;
use crate::providers::cloudflare::clients::ListAccountRulesetsArgs;
use crate::providers::cloudflare::clients::ListAllowlistPrefixesForAccountArgs;
use crate::providers::cloudflare::clients::ListAudioTracksArgs;
use crate::providers::cloudflare::clients::ListBuildTokensArgs;
use crate::providers::cloudflare::clients::ListBuildsByScriptArgs;
use crate::providers::cloudflare::clients::ListCatalogsArgs;
use crate::providers::cloudflare::clients::ListCnisArgs;
use crate::providers::cloudflare::clients::ListDevicesArgs;
use crate::providers::cloudflare::clients::ListDexRulesArgs;
use crate::providers::cloudflare::clients::ListDnsProtectionRulesForAccountArgs;
use crate::providers::cloudflare::clients::ListEmailsArgs;
use crate::providers::cloudflare::clients::ListEnvironmentVariablesArgs;
use crate::providers::cloudflare::clients::ListHyperdriveArgs;
use crate::providers::cloudflare::clients::ListInterconnectsArgs;
use crate::providers::cloudflare::clients::ListIpProfilesArgs;
use crate::providers::cloudflare::clients::ListManagedTransformsArgs;
use crate::providers::cloudflare::clients::ListMitigationsArgs;
use crate::providers::cloudflare::clients::ListNamespacesArgs;
use crate::providers::cloudflare::clients::ListPrefixesForAccountArgs;
use crate::providers::cloudflare::clients::ListRegistrationsArgs;
use crate::providers::cloudflare::clients::ListSlotsArgs;
use crate::providers::cloudflare::clients::ListSynProtectionFiltersForAccountArgs;
use crate::providers::cloudflare::clients::ListSynProtectionRulesForAccountArgs;
use crate::providers::cloudflare::clients::ListTablesArgs;
use crate::providers::cloudflare::clients::ListTcpFlowProtectionFiltersForAccountArgs;
use crate::providers::cloudflare::clients::ListTcpFlowProtectionRulesForAccountArgs;
use crate::providers::cloudflare::clients::ListTriggersByScriptArgs;
use crate::providers::cloudflare::clients::ListWarpChangeEventsArgs;
use crate::providers::cloudflare::clients::ListWorkerVersionsArgs;
use crate::providers::cloudflare::clients::ListWorkersArgs;
use crate::providers::cloudflare::clients::ListZoneEntrypointRulesetVersionsArgs;
use crate::providers::cloudflare::clients::ListZoneRulesetVersionRulesByTagArgs;
use crate::providers::cloudflare::clients::ListZoneRulesetVersionsArgs;
use crate::providers::cloudflare::clients::ListZoneRulesetsArgs;
use crate::providers::cloudflare::clients::ListZoneSnippetRulesArgs;
use crate::providers::cloudflare::clients::ListZoneSnippetsArgs;
use crate::providers::cloudflare::clients::ListsCreateAListArgs;
use crate::providers::cloudflare::clients::ListsCreateListItemsArgs;
use crate::providers::cloudflare::clients::ListsDeleteAListArgs;
use crate::providers::cloudflare::clients::ListsDeleteListItemsArgs;
use crate::providers::cloudflare::clients::ListsGetAListArgs;
use crate::providers::cloudflare::clients::ListsGetAListItemArgs;
use crate::providers::cloudflare::clients::ListsGetBulkOperationStatusArgs;
use crate::providers::cloudflare::clients::ListsGetListItemsArgs;
use crate::providers::cloudflare::clients::ListsGetListsArgs;
use crate::providers::cloudflare::clients::ListsUpdateAListArgs;
use crate::providers::cloudflare::clients::ListsUpdateAllListItemsArgs;
use crate::providers::cloudflare::clients::LivestreamSessionDetailsArgs;
use crate::providers::cloudflare::clients::LoadBalancerHealthcheckEventsListHealthcheckEventsArgs;
use crate::providers::cloudflare::clients::LoadBalancerMonitorsDeleteMonitorArgs;
use crate::providers::cloudflare::clients::LoadBalancerMonitorsListMonitorReferencesArgs;
use crate::providers::cloudflare::clients::LoadBalancerMonitorsMonitorDetailsArgs;
use crate::providers::cloudflare::clients::LoadBalancerMonitorsPatchMonitorArgs;
use crate::providers::cloudflare::clients::LoadBalancerMonitorsPreviewMonitorArgs;
use crate::providers::cloudflare::clients::LoadBalancerMonitorsPreviewResultArgs;
use crate::providers::cloudflare::clients::LoadBalancerMonitorsUpdateMonitorArgs;
use crate::providers::cloudflare::clients::LoadBalancerPoolsDeletePoolArgs;
use crate::providers::cloudflare::clients::LoadBalancerPoolsListPoolReferencesArgs;
use crate::providers::cloudflare::clients::LoadBalancerPoolsListPoolsArgs;
use crate::providers::cloudflare::clients::LoadBalancerPoolsPatchPoolArgs;
use crate::providers::cloudflare::clients::LoadBalancerPoolsPoolDetailsArgs;
use crate::providers::cloudflare::clients::LoadBalancerPoolsPoolHealthDetailsArgs;
use crate::providers::cloudflare::clients::LoadBalancerPoolsPreviewPoolArgs;
use crate::providers::cloudflare::clients::LoadBalancerPoolsUpdatePoolArgs;
use crate::providers::cloudflare::clients::LoadBalancerRegionsGetRegionArgs;
use crate::providers::cloudflare::clients::LoadBalancerRegionsListRegionsArgs;
use crate::providers::cloudflare::clients::LoadBalancersCreateLoadBalancerArgs;
use crate::providers::cloudflare::clients::LoadBalancersDeleteLoadBalancerArgs;
use crate::providers::cloudflare::clients::LoadBalancersListLoadBalancersArgs;
use crate::providers::cloudflare::clients::LoadBalancersLoadBalancerDetailsArgs;
use crate::providers::cloudflare::clients::LoadBalancersPatchLoadBalancerArgs;
use crate::providers::cloudflare::clients::LoadBalancersUpdateLoadBalancerArgs;
use crate::providers::cloudflare::clients::MTlsCertificateManagementDeleteMTlsCertificateArgs;
use crate::providers::cloudflare::clients::MTlsCertificateManagementGetMTlsCertificateArgs;
use crate::providers::cloudflare::clients::MTlsCertificateManagementListMTlsCertificateAssociationsArgs;
use crate::providers::cloudflare::clients::MTlsCertificateManagementListMTlsCertificatesArgs;
use crate::providers::cloudflare::clients::MTlsCertificateManagementUploadMTlsCertificateArgs;
use crate::providers::cloudflare::clients::MagicAccountAppsAddAppArgs;
use crate::providers::cloudflare::clients::MagicAccountAppsDeleteAppArgs;
use crate::providers::cloudflare::clients::MagicAccountAppsListAppsArgs;
use crate::providers::cloudflare::clients::MagicAccountAppsPatchAppArgs;
use crate::providers::cloudflare::clients::MagicAccountAppsUpdateAppArgs;
use crate::providers::cloudflare::clients::MagicGreTunnelsCreateGreTunnelsArgs;
use crate::providers::cloudflare::clients::MagicGreTunnelsDeleteGreTunnelArgs;
use crate::providers::cloudflare::clients::MagicGreTunnelsListGreTunnelDetailsArgs;
use crate::providers::cloudflare::clients::MagicGreTunnelsListGreTunnelsArgs;
use crate::providers::cloudflare::clients::MagicGreTunnelsUpdateGreTunnelArgs;
use crate::providers::cloudflare::clients::MagicGreTunnelsUpdateMultipleGreTunnelsArgs;
use crate::providers::cloudflare::clients::MagicInterconnectsListInterconnectDetailsArgs;
use crate::providers::cloudflare::clients::MagicInterconnectsListInterconnectsArgs;
use crate::providers::cloudflare::clients::MagicInterconnectsUpdateInterconnectArgs;
use crate::providers::cloudflare::clients::MagicInterconnectsUpdateMultipleInterconnectsArgs;
use crate::providers::cloudflare::clients::MagicIpsecTunnelsCreateIpsecTunnelsArgs;
use crate::providers::cloudflare::clients::MagicIpsecTunnelsDeleteIpsecTunnelArgs;
use crate::providers::cloudflare::clients::MagicIpsecTunnelsGeneratePreSharedKeyPskForIpsecTunnelsArgs;
use crate::providers::cloudflare::clients::MagicIpsecTunnelsListIpsecTunnelDetailsArgs;
use crate::providers::cloudflare::clients::MagicIpsecTunnelsListIpsecTunnelsArgs;
use crate::providers::cloudflare::clients::MagicIpsecTunnelsUpdateIpsecTunnelArgs;
use crate::providers::cloudflare::clients::MagicIpsecTunnelsUpdateMultipleIpsecTunnelsArgs;
use crate::providers::cloudflare::clients::MagicNetworkMonitoringConfigurationCreateAccountConfigurationArgs;
use crate::providers::cloudflare::clients::MagicNetworkMonitoringConfigurationDeleteAccountConfigurationArgs;
use crate::providers::cloudflare::clients::MagicNetworkMonitoringConfigurationListAccountConfigurationArgs;
use crate::providers::cloudflare::clients::MagicNetworkMonitoringConfigurationListRulesAndAccountConfigurationArgs;
use crate::providers::cloudflare::clients::MagicNetworkMonitoringConfigurationUpdateAccountConfigurationFieldsArgs;
use crate::providers::cloudflare::clients::MagicNetworkMonitoringConfigurationUpdateAnEntireAccountConfigurationArgs;
use crate::providers::cloudflare::clients::MagicNetworkMonitoringRulesCreateRulesArgs;
use crate::providers::cloudflare::clients::MagicNetworkMonitoringRulesDeleteRuleArgs;
use crate::providers::cloudflare::clients::MagicNetworkMonitoringRulesGetRuleArgs;
use crate::providers::cloudflare::clients::MagicNetworkMonitoringRulesListRulesArgs;
use crate::providers::cloudflare::clients::MagicNetworkMonitoringRulesUpdateAdvertisementForRuleArgs;
use crate::providers::cloudflare::clients::MagicNetworkMonitoringRulesUpdateRuleArgs;
use crate::providers::cloudflare::clients::MagicNetworkMonitoringRulesUpdateRulesArgs;
use crate::providers::cloudflare::clients::MagicNetworkMonitoringVpcFlowsGenerateAuthenticationTokenArgs;
use crate::providers::cloudflare::clients::MagicPcapCollectionAddBucketsForFullPacketCapturesArgs;
use crate::providers::cloudflare::clients::MagicPcapCollectionCreatePcapRequestArgs;
use crate::providers::cloudflare::clients::MagicPcapCollectionDeleteBucketsForFullPacketCapturesArgs;
use crate::providers::cloudflare::clients::MagicPcapCollectionDownloadSimplePcapArgs;
use crate::providers::cloudflare::clients::MagicPcapCollectionGetPcapRequestArgs;
use crate::providers::cloudflare::clients::MagicPcapCollectionListPacketCaptureRequestsArgs;
use crate::providers::cloudflare::clients::MagicPcapCollectionListPcaPsBucketOwnershipArgs;
use crate::providers::cloudflare::clients::MagicPcapCollectionStopFullPcapArgs;
use crate::providers::cloudflare::clients::MagicPcapCollectionValidateBucketsForFullPacketCapturesArgs;
use crate::providers::cloudflare::clients::MagicSiteAclsAclDetailsArgs;
use crate::providers::cloudflare::clients::MagicSiteAclsCreateAclArgs;
use crate::providers::cloudflare::clients::MagicSiteAclsDeleteAclArgs;
use crate::providers::cloudflare::clients::MagicSiteAclsListAclsArgs;
use crate::providers::cloudflare::clients::MagicSiteAclsPatchAclArgs;
use crate::providers::cloudflare::clients::MagicSiteAclsUpdateAclArgs;
use crate::providers::cloudflare::clients::MagicSiteAppConfigsAddAppConfigArgs;
use crate::providers::cloudflare::clients::MagicSiteAppConfigsDeleteAppConfigArgs;
use crate::providers::cloudflare::clients::MagicSiteAppConfigsListAppConfigsArgs;
use crate::providers::cloudflare::clients::MagicSiteAppConfigsPatchAppConfigArgs;
use crate::providers::cloudflare::clients::MagicSiteAppConfigsUpdateAppConfigArgs;
use crate::providers::cloudflare::clients::MagicSiteLansCreateLanArgs;
use crate::providers::cloudflare::clients::MagicSiteLansDeleteLanArgs;
use crate::providers::cloudflare::clients::MagicSiteLansLanDetailsArgs;
use crate::providers::cloudflare::clients::MagicSiteLansListLansArgs;
use crate::providers::cloudflare::clients::MagicSiteLansPatchLanArgs;
use crate::providers::cloudflare::clients::MagicSiteLansUpdateLanArgs;
use crate::providers::cloudflare::clients::MagicSiteNetflowConfigCreateNetflowConfigArgs;
use crate::providers::cloudflare::clients::MagicSiteNetflowConfigDeleteNetflowConfigArgs;
use crate::providers::cloudflare::clients::MagicSiteNetflowConfigDetailsArgs;
use crate::providers::cloudflare::clients::MagicSiteNetflowConfigPatchNetflowConfigArgs;
use crate::providers::cloudflare::clients::MagicSiteNetflowConfigUpdateNetflowConfigArgs;
use crate::providers::cloudflare::clients::MagicSiteWansCreateWanArgs;
use crate::providers::cloudflare::clients::MagicSiteWansDeleteWanArgs;
use crate::providers::cloudflare::clients::MagicSiteWansListWansArgs;
use crate::providers::cloudflare::clients::MagicSiteWansPatchWanArgs;
use crate::providers::cloudflare::clients::MagicSiteWansUpdateWanArgs;
use crate::providers::cloudflare::clients::MagicSiteWansWanDetailsArgs;
use crate::providers::cloudflare::clients::MagicSitesCreateSiteArgs;
use crate::providers::cloudflare::clients::MagicSitesDeleteSiteArgs;
use crate::providers::cloudflare::clients::MagicSitesListSitesArgs;
use crate::providers::cloudflare::clients::MagicSitesPatchSiteArgs;
use crate::providers::cloudflare::clients::MagicSitesSiteDetailsArgs;
use crate::providers::cloudflare::clients::MagicSitesUpdateSiteArgs;
use crate::providers::cloudflare::clients::MagicStaticRoutesCreateRoutesArgs;
use crate::providers::cloudflare::clients::MagicStaticRoutesDeleteManyRoutesArgs;
use crate::providers::cloudflare::clients::MagicStaticRoutesDeleteRouteArgs;
use crate::providers::cloudflare::clients::MagicStaticRoutesListRoutesArgs;
use crate::providers::cloudflare::clients::MagicStaticRoutesRouteDetailsArgs;
use crate::providers::cloudflare::clients::MagicStaticRoutesUpdateManyRoutesArgs;
use crate::providers::cloudflare::clients::MagicStaticRoutesUpdateRouteArgs;
use crate::providers::cloudflare::clients::MconnConnectorCreateArgs;
use crate::providers::cloudflare::clients::MconnConnectorDeleteArgs;
use crate::providers::cloudflare::clients::MconnConnectorFetchArgs;
use crate::providers::cloudflare::clients::MconnConnectorListArgs;
use crate::providers::cloudflare::clients::MconnConnectorReplaceArgs;
use crate::providers::cloudflare::clients::MconnConnectorTelemetryEventsGetArgs;
use crate::providers::cloudflare::clients::MconnConnectorTelemetryEventsListArgs;
use crate::providers::cloudflare::clients::MconnConnectorTelemetryEventsListLatestArgs;
use crate::providers::cloudflare::clients::MconnConnectorTelemetrySnapshotsGetArgs;
use crate::providers::cloudflare::clients::MconnConnectorTelemetrySnapshotsListArgs;
use crate::providers::cloudflare::clients::MconnConnectorTelemetrySnapshotsListLatestArgs;
use crate::providers::cloudflare::clients::MconnConnectorUpdateArgs;
use crate::providers::cloudflare::clients::McpPortalsApiCreatePortalsArgs;
use crate::providers::cloudflare::clients::McpPortalsApiCreateServersArgs;
use crate::providers::cloudflare::clients::McpPortalsApiDeletePortalsArgs;
use crate::providers::cloudflare::clients::McpPortalsApiDeleteServersArgs;
use crate::providers::cloudflare::clients::McpPortalsApiFetchGatewaysArgs;
use crate::providers::cloudflare::clients::McpPortalsApiFetchServersArgs;
use crate::providers::cloudflare::clients::McpPortalsApiListPortalsArgs;
use crate::providers::cloudflare::clients::McpPortalsApiListServersArgs;
use crate::providers::cloudflare::clients::McpPortalsApiSyncServerArgs;
use crate::providers::cloudflare::clients::McpPortalsApiUpdatePortalsArgs;
use crate::providers::cloudflare::clients::McpPortalsApiUpdateServersArgs;
use crate::providers::cloudflare::clients::MembersBatchCreateArgs;
use crate::providers::cloudflare::clients::MembersCreateArgs;
use crate::providers::cloudflare::clients::MembersDeleteArgs;
use crate::providers::cloudflare::clients::MembersListArgs;
use crate::providers::cloudflare::clients::MembersRetrieveArgs;
use crate::providers::cloudflare::clients::MiscategorizationCreateMiscategorizationArgs;
use crate::providers::cloudflare::clients::MuteAllParticipantsArgs;
use crate::providers::cloudflare::clients::MuteParticipantsArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerCreateArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerDeleteNamespaceArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerDeleteScriptSecretArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerDeleteScriptTagArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerDeleteScriptsArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerGetNamespaceArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerGetScriptBindingsArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerGetScriptContentArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerGetScriptSecretsArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerGetScriptSettingsArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerGetScriptTagsArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerListArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerListScriptSecretsArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerListScriptsArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerPatchNamespaceArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerPatchScriptSettingsArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerPutNamespaceArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerPutScriptContentArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerPutScriptSecretsArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerPutScriptTagArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerPutScriptTagsArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerScriptDeleteWorkerArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerScriptUpdateCreateAssetsUploadSessionArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerScriptUploadWorkerModuleArgs;
use crate::providers::cloudflare::clients::NamespaceWorkerScriptWorkerDetailsArgs;
use crate::providers::cloudflare::clients::NotificationAlertTypesGetAlertTypesArgs;
use crate::providers::cloudflare::clients::NotificationDestinationsWithPagerDutyConnectPagerDutyArgs;
use crate::providers::cloudflare::clients::NotificationDestinationsWithPagerDutyConnectPagerDutyTokenArgs;
use crate::providers::cloudflare::clients::NotificationDestinationsWithPagerDutyDeletePagerDutyServicesArgs;
use crate::providers::cloudflare::clients::NotificationDestinationsWithPagerDutyListPagerDutyServicesArgs;
use crate::providers::cloudflare::clients::NotificationHistoryListHistoryArgs;
use crate::providers::cloudflare::clients::NotificationMechanismEligibilityGetDeliveryMechanismEligibilityArgs;
use crate::providers::cloudflare::clients::NotificationPoliciesCreateANotificationPolicyArgs;
use crate::providers::cloudflare::clients::NotificationPoliciesDeleteANotificationPolicyArgs;
use crate::providers::cloudflare::clients::NotificationPoliciesGetANotificationPolicyArgs;
use crate::providers::cloudflare::clients::NotificationPoliciesListNotificationPoliciesArgs;
use crate::providers::cloudflare::clients::NotificationPoliciesUpdateANotificationPolicyArgs;
use crate::providers::cloudflare::clients::NotificationSilencesCreateSilencesArgs;
use crate::providers::cloudflare::clients::NotificationSilencesDeleteSilencesArgs;
use crate::providers::cloudflare::clients::NotificationSilencesGetSilenceArgs;
use crate::providers::cloudflare::clients::NotificationSilencesListSilencesArgs;
use crate::providers::cloudflare::clients::NotificationSilencesUpdateSilencesArgs;
use crate::providers::cloudflare::clients::NotificationWebhooksCreateAWebhookArgs;
use crate::providers::cloudflare::clients::NotificationWebhooksDeleteAWebhookArgs;
use crate::providers::cloudflare::clients::NotificationWebhooksGetAWebhookArgs;
use crate::providers::cloudflare::clients::NotificationWebhooksListWebhooksArgs;
use crate::providers::cloudflare::clients::NotificationWebhooksUpdateAWebhookArgs;
use crate::providers::cloudflare::clients::OnrampsApplyArgs;
use crate::providers::cloudflare::clients::OnrampsCreateArgs;
use crate::providers::cloudflare::clients::OnrampsDeleteArgs;
use crate::providers::cloudflare::clients::OnrampsExportArgs;
use crate::providers::cloudflare::clients::OnrampsListArgs;
use crate::providers::cloudflare::clients::OnrampsMwanAddrSpacePatchArgs;
use crate::providers::cloudflare::clients::OnrampsMwanAddrSpaceReadArgs;
use crate::providers::cloudflare::clients::OnrampsMwanAddrSpaceUpdateArgs;
use crate::providers::cloudflare::clients::OnrampsPatchArgs;
use crate::providers::cloudflare::clients::OnrampsPlanArgs;
use crate::providers::cloudflare::clients::OnrampsReadArgs;
use crate::providers::cloudflare::clients::OnrampsUpdateArgs;
use crate::providers::cloudflare::clients::OrganizationSharesListArgs;
use crate::providers::cloudflare::clients::OrganizationsCreateUserOrganizationArgs;
use crate::providers::cloudflare::clients::OrganizationsDeleteArgs;
use crate::providers::cloudflare::clients::OrganizationsGetAccountsArgs;
use crate::providers::cloudflare::clients::OrganizationsGetProfileArgs;
use crate::providers::cloudflare::clients::OrganizationsModifyArgs;
use crate::providers::cloudflare::clients::OrganizationsModifyProfileArgs;
use crate::providers::cloudflare::clients::OrganizationsRetrieveArgs;
use crate::providers::cloudflare::clients::OriginCaGetCertificateArgs;
use crate::providers::cloudflare::clients::OriginCaListCertificatesArgs;
use crate::providers::cloudflare::clients::OriginCaRevokeCertificateArgs;
use crate::providers::cloudflare::clients::PageRulesCreateAPageRuleArgs;
use crate::providers::cloudflare::clients::PageRulesDeleteAPageRuleArgs;
use crate::providers::cloudflare::clients::PageRulesEditAPageRuleArgs;
use crate::providers::cloudflare::clients::PageRulesGetAPageRuleArgs;
use crate::providers::cloudflare::clients::PageRulesListPageRulesArgs;
use crate::providers::cloudflare::clients::PageRulesUpdateAPageRuleArgs;
use crate::providers::cloudflare::clients::PageShieldCreatePolicyArgs;
use crate::providers::cloudflare::clients::PageShieldDeletePolicyArgs;
use crate::providers::cloudflare::clients::PageShieldGetConnectionArgs;
use crate::providers::cloudflare::clients::PageShieldGetCookieArgs;
use crate::providers::cloudflare::clients::PageShieldGetPolicyArgs;
use crate::providers::cloudflare::clients::PageShieldGetScriptArgs;
use crate::providers::cloudflare::clients::PageShieldGetSettingsArgs;
use crate::providers::cloudflare::clients::PageShieldListConnectionsArgs;
use crate::providers::cloudflare::clients::PageShieldListCookiesArgs;
use crate::providers::cloudflare::clients::PageShieldListPoliciesArgs;
use crate::providers::cloudflare::clients::PageShieldListScriptsArgs;
use crate::providers::cloudflare::clients::PageShieldUpdatePolicyArgs;
use crate::providers::cloudflare::clients::PageShieldUpdateSettingsArgs;
use crate::providers::cloudflare::clients::PagesDeploymentCreateDeploymentArgs;
use crate::providers::cloudflare::clients::PagesDeploymentDeleteDeploymentArgs;
use crate::providers::cloudflare::clients::PagesDeploymentGetDeploymentInfoArgs;
use crate::providers::cloudflare::clients::PagesDeploymentGetDeploymentLogsArgs;
use crate::providers::cloudflare::clients::PagesDeploymentGetDeploymentsArgs;
use crate::providers::cloudflare::clients::PagesDeploymentRetryDeploymentArgs;
use crate::providers::cloudflare::clients::PagesDeploymentRollbackDeploymentArgs;
use crate::providers::cloudflare::clients::PagesDomainsAddDomainArgs;
use crate::providers::cloudflare::clients::PagesDomainsDeleteDomainArgs;
use crate::providers::cloudflare::clients::PagesDomainsGetDomainArgs;
use crate::providers::cloudflare::clients::PagesDomainsGetDomainsArgs;
use crate::providers::cloudflare::clients::PagesDomainsPatchDomainArgs;
use crate::providers::cloudflare::clients::PagesProjectCreateProjectArgs;
use crate::providers::cloudflare::clients::PagesProjectDeleteProjectArgs;
use crate::providers::cloudflare::clients::PagesProjectGetProjectArgs;
use crate::providers::cloudflare::clients::PagesProjectGetProjectsArgs;
use crate::providers::cloudflare::clients::PagesProjectUpdateProjectArgs;
use crate::providers::cloudflare::clients::PagesPurgeBuildCacheArgs;
use crate::providers::cloudflare::clients::PassiveDnsByIpGetPassiveDnsByIpArgs;
use crate::providers::cloudflare::clients::PatchCategoryUpdateArgs;
use crate::providers::cloudflare::clients::PatchDatasetUpdateArgs;
use crate::providers::cloudflare::clients::PatchEventQueryAlertUpdateArgs;
use crate::providers::cloudflare::clients::PatchEventQueryUpdateArgs;
use crate::providers::cloudflare::clients::PatchEventRawUpdateArgs;
use crate::providers::cloudflare::clients::PatchEventUpdateArgs;
use crate::providers::cloudflare::clients::PatchEventUpdateBulkArgs;
use crate::providers::cloudflare::clients::PatchHyperdriveArgs;
use crate::providers::cloudflare::clients::PatchIndicatorUpdateArgs;
use crate::providers::cloudflare::clients::PatchPresetsPresetIdArgs;
use crate::providers::cloudflare::clients::PatchTagCategoryUpdateArgs;
use crate::providers::cloudflare::clients::PatchTagUpdateArgs;
use crate::providers::cloudflare::clients::PatchV4AccountsByAccountIdPipelinesV1StreamsByStreamIdArgs;
use crate::providers::cloudflare::clients::PauseResumeStopRecordingArgs;
use crate::providers::cloudflare::clients::PayPerCrawlCrawlerCreateStripeConfigArgs;
use crate::providers::cloudflare::clients::PayPerCrawlCrawlerDeleteStripeConfigArgs;
use crate::providers::cloudflare::clients::PayPerCrawlCrawlerGetStripeConfigArgs;
use crate::providers::cloudflare::clients::PayPerCrawlCreateConfigArgs;
use crate::providers::cloudflare::clients::PayPerCrawlGetConfigArgs;
use crate::providers::cloudflare::clients::PayPerCrawlPatchConfigArgs;
use crate::providers::cloudflare::clients::PayPerCrawlPublisherCreateStripeConfigArgs;
use crate::providers::cloudflare::clients::PayPerCrawlPublisherDeleteStripeConfigArgs;
use crate::providers::cloudflare::clients::PayPerCrawlPublisherGetStripeConfigArgs;
use crate::providers::cloudflare::clients::PayPerCrawlQueryZonesCanBeEnabledArgs;
use crate::providers::cloudflare::clients::PayPerCrawlSetZonesCanBeEnabledArgs;
use crate::providers::cloudflare::clients::PerHostnameAuthenticatedOriginPullDeleteHostnameClientCertificateArgs;
use crate::providers::cloudflare::clients::PerHostnameAuthenticatedOriginPullEnableOrDisableAHostnameForClientAuthenticationArgs;
use crate::providers::cloudflare::clients::PerHostnameAuthenticatedOriginPullGetTheHostnameClientCertificateArgs;
use crate::providers::cloudflare::clients::PerHostnameAuthenticatedOriginPullGetTheHostnameStatusForClientAuthenticationArgs;
use crate::providers::cloudflare::clients::PerHostnameAuthenticatedOriginPullListCertificatesArgs;
use crate::providers::cloudflare::clients::PerHostnameAuthenticatedOriginPullListHostnameAssociationsArgs;
use crate::providers::cloudflare::clients::PerHostnameAuthenticatedOriginPullUploadAHostnameClientCertificateArgs;
use crate::providers::cloudflare::clients::PerHostnameTlsSettingsDeleteArgs;
use crate::providers::cloudflare::clients::PerHostnameTlsSettingsGetArgs;
use crate::providers::cloudflare::clients::PerHostnameTlsSettingsListArgs;
use crate::providers::cloudflare::clients::PerHostnameTlsSettingsPutArgs;
use crate::providers::cloudflare::clients::PermissionGroupsListPermissionGroupsArgs;
use crate::providers::cloudflare::clients::PostAccountsAccountIdLogpushJobsArgs;
use crate::providers::cloudflare::clients::PostAccountsAccountIdLogpushOwnershipArgs;
use crate::providers::cloudflare::clients::PostAccountsAccountIdLogpushOwnershipValidateArgs;
use crate::providers::cloudflare::clients::PostAccountsAccountIdLogpushValidateOriginArgs;
use crate::providers::cloudflare::clients::PostAccountsAccountIdLogsControlCmbConfigArgs;
use crate::providers::cloudflare::clients::PostBinDbPostArgs;
use crate::providers::cloudflare::clients::PostCategoryCreateArgs;
use crate::providers::cloudflare::clients::PostCategoryUpdateArgs;
use crate::providers::cloudflare::clients::PostCommandsArgs;
use crate::providers::cloudflare::clients::PostConfigCreateArgs;
use crate::providers::cloudflare::clients::PostConfigUpdateArgs;
use crate::providers::cloudflare::clients::PostCreateEventRelationshipArgs;
use crate::providers::cloudflare::clients::PostDatasetCreateArgs;
use crate::providers::cloudflare::clients::PostDatasetPopulateArgs;
use crate::providers::cloudflare::clients::PostDatasetUpdateArgs;
use crate::providers::cloudflare::clients::PostDosEventCreateBulkWithRelationshipsArgs;
use crate::providers::cloudflare::clients::PostEventCopyToNewDsArgs;
use crate::providers::cloudflare::clients::PostEventCreateArgs;
use crate::providers::cloudflare::clients::PostEventCreateBulkArgs;
use crate::providers::cloudflare::clients::PostEventDoRevertArgs;
use crate::providers::cloudflare::clients::PostEventGraphQlArgs;
use crate::providers::cloudflare::clients::PostEventGraphQlv2Args;
use crate::providers::cloudflare::clients::PostEventMoveToNewDsArgs;
use crate::providers::cloudflare::clients::PostEventQueryAlertCreateArgs;
use crate::providers::cloudflare::clients::PostEventQueryAlertUpdateArgs;
use crate::providers::cloudflare::clients::PostEventQueryCreateArgs;
use crate::providers::cloudflare::clients::PostEventQueryUpdateArgs;
use crate::providers::cloudflare::clients::PostEventRawUpdateArgs;
use crate::providers::cloudflare::clients::PostEventReferenceCreateArgs;
use crate::providers::cloudflare::clients::PostEventTagCreateArgs;
use crate::providers::cloudflare::clients::PostEventUpdateArgs;
use crate::providers::cloudflare::clients::PostGroupCreateArgs;
use crate::providers::cloudflare::clients::PostGroupMemberCreateArgs;
use crate::providers::cloudflare::clients::PostIndicatorCreateArgs;
use crate::providers::cloudflare::clients::PostIndicatorCreateBulkArgs;
use crate::providers::cloudflare::clients::PostIndicatorTypeCreateArgs;
use crate::providers::cloudflare::clients::PostInsertLogoQueryArgs;
use crate::providers::cloudflare::clients::PostPermissionCreateArgs;
use crate::providers::cloudflare::clients::PostPresetsArgs;
use crate::providers::cloudflare::clients::PostSearchLogoSimilarityArgs;
use crate::providers::cloudflare::clients::PostSessionsSessionIdSummaryArgs;
use crate::providers::cloudflare::clients::PostTagCategoryCreateArgs;
use crate::providers::cloudflare::clients::PostTagCreateArgs;
use crate::providers::cloudflare::clients::PostV4AccountsByAccountIdPipelinesDeprecatedArgs;
use crate::providers::cloudflare::clients::PostV4AccountsByAccountIdPipelinesV1PipelinesArgs;
use crate::providers::cloudflare::clients::PostV4AccountsByAccountIdPipelinesV1SinksArgs;
use crate::providers::cloudflare::clients::PostV4AccountsByAccountIdPipelinesV1StreamsArgs;
use crate::providers::cloudflare::clients::PostV4AccountsByAccountIdPipelinesV1ValidateSqlArgs;
use crate::providers::cloudflare::clients::PostZonesZoneIdLogpushEdgeJobsArgs;
use crate::providers::cloudflare::clients::PostZonesZoneIdLogpushJobsArgs;
use crate::providers::cloudflare::clients::PostZonesZoneIdLogpushOwnershipArgs;
use crate::providers::cloudflare::clients::PostZonesZoneIdLogpushOwnershipValidateArgs;
use crate::providers::cloudflare::clients::PostZonesZoneIdLogpushValidateDestinationArgs;
use crate::providers::cloudflare::clients::PostZonesZoneIdLogpushValidateDestinationExistsArgs;
use crate::providers::cloudflare::clients::PostZonesZoneIdLogpushValidateOriginArgs;
use crate::providers::cloudflare::clients::PostZonesZoneIdLogsControlRetentionFlagArgs;
use crate::providers::cloudflare::clients::PostZonesZoneIdentifierZarazPublishArgs;
use crate::providers::cloudflare::clients::ProvidersCreateArgs;
use crate::providers::cloudflare::clients::ProvidersDeleteArgs;
use crate::providers::cloudflare::clients::ProvidersDiscoverAllArgs;
use crate::providers::cloudflare::clients::ProvidersDiscoverArgs;
use crate::providers::cloudflare::clients::ProvidersInitialSetupArgs;
use crate::providers::cloudflare::clients::ProvidersListArgs;
use crate::providers::cloudflare::clients::ProvidersPatchArgs;
use crate::providers::cloudflare::clients::ProvidersReadArgs;
use crate::providers::cloudflare::clients::ProvidersUpdateArgs;
use crate::providers::cloudflare::clients::PublicListApplicationsArgs;
use crate::providers::cloudflare::clients::PurgeBuildCacheArgs;
use crate::providers::cloudflare::clients::PutAccountsAccountIdLogpushJobsJobIdArgs;
use crate::providers::cloudflare::clients::PutGroupUpdateArgs;
use crate::providers::cloudflare::clients::PutPermissionUpdateArgs;
use crate::providers::cloudflare::clients::PutV4AccountsByAccountIdPipelinesByPipelineNameDeprecatedArgs;
use crate::providers::cloudflare::clients::PutZonesZoneIdActivationCheckArgs;
use crate::providers::cloudflare::clients::PutZonesZoneIdLogpushJobsJobIdArgs;
use crate::providers::cloudflare::clients::PutZonesZoneIdentifierZarazConfigArgs;
use crate::providers::cloudflare::clients::PutZonesZoneIdentifierZarazHistoryArgs;
use crate::providers::cloudflare::clients::PutZonesZoneIdentifierZarazWorkflowArgs;
use crate::providers::cloudflare::clients::QueriesDeleteArgs;
use crate::providers::cloudflare::clients::QueriesGetArgs;
use crate::providers::cloudflare::clients::QueriesListArgs;
use crate::providers::cloudflare::clients::QueriesPatchArgs;
use crate::providers::cloudflare::clients::QueriesPostArgs;
use crate::providers::cloudflare::clients::QueuesAckMessagesArgs;
use crate::providers::cloudflare::clients::QueuesCreateArgs;
use crate::providers::cloudflare::clients::QueuesCreateConsumerArgs;
use crate::providers::cloudflare::clients::QueuesDeleteArgs;
use crate::providers::cloudflare::clients::QueuesDeleteConsumerArgs;
use crate::providers::cloudflare::clients::QueuesGetArgs;
use crate::providers::cloudflare::clients::QueuesGetConsumerArgs;
use crate::providers::cloudflare::clients::QueuesListArgs;
use crate::providers::cloudflare::clients::QueuesListConsumersArgs;
use crate::providers::cloudflare::clients::QueuesPullMessagesArgs;
use crate::providers::cloudflare::clients::QueuesPurgeArgs;
use crate::providers::cloudflare::clients::QueuesPurgeGetArgs;
use crate::providers::cloudflare::clients::QueuesPushMessageArgs;
use crate::providers::cloudflare::clients::QueuesPushMessagesArgs;
use crate::providers::cloudflare::clients::QueuesUpdateArgs;
use crate::providers::cloudflare::clients::QueuesUpdateConsumerArgs;
use crate::providers::cloudflare::clients::QueuesUpdatePartialArgs;
use crate::providers::cloudflare::clients::R2AddCustomDomainArgs;
use crate::providers::cloudflare::clients::R2CreateBucketArgs;
use crate::providers::cloudflare::clients::R2CreateTempAccessCredentialsArgs;
use crate::providers::cloudflare::clients::R2DeleteBucketArgs;
use crate::providers::cloudflare::clients::R2DeleteBucketCorsPolicyArgs;
use crate::providers::cloudflare::clients::R2DeleteBucketSippyConfigArgs;
use crate::providers::cloudflare::clients::R2DeleteCustomDomainArgs;
use crate::providers::cloudflare::clients::R2EditCustomDomainSettingsArgs;
use crate::providers::cloudflare::clients::R2EventNotificationDeleteConfigArgs;
use crate::providers::cloudflare::clients::R2GetAccountLevelMetricsArgs;
use crate::providers::cloudflare::clients::R2GetBucketArgs;
use crate::providers::cloudflare::clients::R2GetBucketCorsPolicyArgs;
use crate::providers::cloudflare::clients::R2GetBucketLifecycleConfigurationArgs;
use crate::providers::cloudflare::clients::R2GetBucketLocalUploadsConfigurationArgs;
use crate::providers::cloudflare::clients::R2GetBucketLockConfigurationArgs;
use crate::providers::cloudflare::clients::R2GetBucketPublicPolicyArgs;
use crate::providers::cloudflare::clients::R2GetBucketSippyConfigArgs;
use crate::providers::cloudflare::clients::R2GetCustomDomainSettingsArgs;
use crate::providers::cloudflare::clients::R2GetEventNotificationConfigArgs;
use crate::providers::cloudflare::clients::R2GetEventNotificationConfigsArgs;
use crate::providers::cloudflare::clients::R2ListBucketsArgs;
use crate::providers::cloudflare::clients::R2ListCustomDomainsArgs;
use crate::providers::cloudflare::clients::R2PatchBucketArgs;
use crate::providers::cloudflare::clients::R2PutBucketCorsPolicyArgs;
use crate::providers::cloudflare::clients::R2PutBucketLifecycleConfigurationArgs;
use crate::providers::cloudflare::clients::R2PutBucketLocalUploadsConfigurationArgs;
use crate::providers::cloudflare::clients::R2PutBucketLockConfigurationArgs;
use crate::providers::cloudflare::clients::R2PutBucketPublicPolicyArgs;
use crate::providers::cloudflare::clients::R2PutBucketSippyConfigArgs;
use crate::providers::cloudflare::clients::R2PutEventNotificationConfigArgs;
use crate::providers::cloudflare::clients::RadarGetAiBotsSummaryArgs;
use crate::providers::cloudflare::clients::RadarGetAiBotsSummaryByUserAgentArgs;
use crate::providers::cloudflare::clients::RadarGetAiBotsTimeseriesArgs;
use crate::providers::cloudflare::clients::RadarGetAiBotsTimeseriesGroupArgs;
use crate::providers::cloudflare::clients::RadarGetAiBotsTimeseriesGroupByUserAgentArgs;
use crate::providers::cloudflare::clients::RadarGetAiInferenceSummaryArgs;
use crate::providers::cloudflare::clients::RadarGetAiInferenceSummaryByModelArgs;
use crate::providers::cloudflare::clients::RadarGetAiInferenceSummaryByTaskArgs;
use crate::providers::cloudflare::clients::RadarGetAiInferenceTimeseriesGroupArgs;
use crate::providers::cloudflare::clients::RadarGetAiInferenceTimeseriesGroupByModelArgs;
use crate::providers::cloudflare::clients::RadarGetAiInferenceTimeseriesGroupByTaskArgs;
use crate::providers::cloudflare::clients::RadarGetAnnotationsArgs;
use crate::providers::cloudflare::clients::RadarGetAnnotationsOutagesArgs;
use crate::providers::cloudflare::clients::RadarGetAnnotationsOutagesTopArgs;
use crate::providers::cloudflare::clients::RadarGetAsBotnetThreatFeedArgs;
use crate::providers::cloudflare::clients::RadarGetAsnsAsSetArgs;
use crate::providers::cloudflare::clients::RadarGetAsnsRelArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3SummaryArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3SummaryByBitrateArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3SummaryByDurationArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3SummaryByIndustryArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3SummaryByIpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3SummaryByProtocolArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3SummaryByVectorArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3SummaryByVerticalArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3TimeseriesByBytesArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3TimeseriesGroupArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3TimeseriesGroupByBitrateArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3TimeseriesGroupByDurationArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3TimeseriesGroupByIndustryArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3TimeseriesGroupByIpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3TimeseriesGroupByProtocolArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3TimeseriesGroupByVectorArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3TimeseriesGroupByVerticalArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3TopAttacksArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3TopIndustriesArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3TopOriginLocationsArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3TopTargetLocationsArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer3TopVerticalsArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7SummaryArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7SummaryByHttpMethodArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7SummaryByHttpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7SummaryByIndustryArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7SummaryByIpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7SummaryByManagedRulesArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7SummaryByMitigationProductArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7SummaryByVerticalArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7TimeseriesArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7TimeseriesGroupArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7TimeseriesGroupByHttpMethodArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7TimeseriesGroupByHttpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7TimeseriesGroupByIndustryArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7TimeseriesGroupByIpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7TimeseriesGroupByManagedRulesArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7TimeseriesGroupByMitigationProductArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7TimeseriesGroupByVerticalArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7TopAttacksArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7TopIndustriesArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7TopOriginAsArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7TopOriginLocationArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7TopTargetLocationArgs;
use crate::providers::cloudflare::clients::RadarGetAttacksLayer7TopVerticalsArgs;
use crate::providers::cloudflare::clients::RadarGetBgpHijacksEventsArgs;
use crate::providers::cloudflare::clients::RadarGetBgpIpsTimeseriesArgs;
use crate::providers::cloudflare::clients::RadarGetBgpPfx2AsArgs;
use crate::providers::cloudflare::clients::RadarGetBgpPfx2AsMoasArgs;
use crate::providers::cloudflare::clients::RadarGetBgpRouteLeakEventsArgs;
use crate::providers::cloudflare::clients::RadarGetBgpRoutesAsnsArgs;
use crate::providers::cloudflare::clients::RadarGetBgpRoutesRealtimeArgs;
use crate::providers::cloudflare::clients::RadarGetBgpRoutesStatsArgs;
use crate::providers::cloudflare::clients::RadarGetBgpRpkiAspaChangesArgs;
use crate::providers::cloudflare::clients::RadarGetBgpRpkiAspaSnapshotArgs;
use crate::providers::cloudflare::clients::RadarGetBgpRpkiAspaTimeseriesArgs;
use crate::providers::cloudflare::clients::RadarGetBgpTimeseriesArgs;
use crate::providers::cloudflare::clients::RadarGetBgpTopAsesArgs;
use crate::providers::cloudflare::clients::RadarGetBgpTopAsnsByPrefixesArgs;
use crate::providers::cloudflare::clients::RadarGetBgpTopPrefixesArgs;
use crate::providers::cloudflare::clients::RadarGetBotDetailsArgs;
use crate::providers::cloudflare::clients::RadarGetBotsArgs;
use crate::providers::cloudflare::clients::RadarGetBotsSummaryArgs;
use crate::providers::cloudflare::clients::RadarGetBotsTimeseriesArgs;
use crate::providers::cloudflare::clients::RadarGetBotsTimeseriesGroupArgs;
use crate::providers::cloudflare::clients::RadarGetCertificateAuthoritiesArgs;
use crate::providers::cloudflare::clients::RadarGetCertificateAuthorityDetailsArgs;
use crate::providers::cloudflare::clients::RadarGetCertificateLogDetailsArgs;
use crate::providers::cloudflare::clients::RadarGetCertificateLogsArgs;
use crate::providers::cloudflare::clients::RadarGetCrawlersSummaryArgs;
use crate::providers::cloudflare::clients::RadarGetCrawlersTimeseriesGroupArgs;
use crate::providers::cloudflare::clients::RadarGetCtSummaryArgs;
use crate::providers::cloudflare::clients::RadarGetCtTimeseriesArgs;
use crate::providers::cloudflare::clients::RadarGetCtTimeseriesGroupArgs;
use crate::providers::cloudflare::clients::RadarGetDnsAs112SummaryArgs;
use crate::providers::cloudflare::clients::RadarGetDnsAs112TimeseriesArgs;
use crate::providers::cloudflare::clients::RadarGetDnsAs112TimeseriesByDnssecArgs;
use crate::providers::cloudflare::clients::RadarGetDnsAs112TimeseriesByEdnsArgs;
use crate::providers::cloudflare::clients::RadarGetDnsAs112TimeseriesByIpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetDnsAs112TimeseriesByProtocolArgs;
use crate::providers::cloudflare::clients::RadarGetDnsAs112TimeseriesByQueryTypeArgs;
use crate::providers::cloudflare::clients::RadarGetDnsAs112TimeseriesByResponseCodesArgs;
use crate::providers::cloudflare::clients::RadarGetDnsAs112TimeseriesGroupArgs;
use crate::providers::cloudflare::clients::RadarGetDnsAs112TimeseriesGroupByDnssecArgs;
use crate::providers::cloudflare::clients::RadarGetDnsAs112TimeseriesGroupByEdnsArgs;
use crate::providers::cloudflare::clients::RadarGetDnsAs112TimeseriesGroupByIpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetDnsAs112TimeseriesGroupByProtocolArgs;
use crate::providers::cloudflare::clients::RadarGetDnsAs112TimeseriesGroupByQueryTypeArgs;
use crate::providers::cloudflare::clients::RadarGetDnsAs112TimeseriesGroupByResponseCodesArgs;
use crate::providers::cloudflare::clients::RadarGetDnsAs112TopLocationsArgs;
use crate::providers::cloudflare::clients::RadarGetDnsAs112TopLocationsByDnssecArgs;
use crate::providers::cloudflare::clients::RadarGetDnsAs112TopLocationsByEdnsArgs;
use crate::providers::cloudflare::clients::RadarGetDnsAs112TopLocationsByIpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetDnsSummaryArgs;
use crate::providers::cloudflare::clients::RadarGetDnsSummaryByCacheHitStatusArgs;
use crate::providers::cloudflare::clients::RadarGetDnsSummaryByDnssecArgs;
use crate::providers::cloudflare::clients::RadarGetDnsSummaryByDnssecAwarenessArgs;
use crate::providers::cloudflare::clients::RadarGetDnsSummaryByDnssecE2EVersionArgs;
use crate::providers::cloudflare::clients::RadarGetDnsSummaryByIpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetDnsSummaryByMatchingAnswerStatusArgs;
use crate::providers::cloudflare::clients::RadarGetDnsSummaryByProtocolArgs;
use crate::providers::cloudflare::clients::RadarGetDnsSummaryByQueryTypeArgs;
use crate::providers::cloudflare::clients::RadarGetDnsSummaryByResponseCodeArgs;
use crate::providers::cloudflare::clients::RadarGetDnsSummaryByResponseTtlArgs;
use crate::providers::cloudflare::clients::RadarGetDnsTimeseriesArgs;
use crate::providers::cloudflare::clients::RadarGetDnsTimeseriesGroupArgs;
use crate::providers::cloudflare::clients::RadarGetDnsTimeseriesGroupByCacheHitStatusArgs;
use crate::providers::cloudflare::clients::RadarGetDnsTimeseriesGroupByDnssecArgs;
use crate::providers::cloudflare::clients::RadarGetDnsTimeseriesGroupByDnssecAwarenessArgs;
use crate::providers::cloudflare::clients::RadarGetDnsTimeseriesGroupByDnssecE2EVersionArgs;
use crate::providers::cloudflare::clients::RadarGetDnsTimeseriesGroupByIpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetDnsTimeseriesGroupByMatchingAnswerStatusArgs;
use crate::providers::cloudflare::clients::RadarGetDnsTimeseriesGroupByProtocolArgs;
use crate::providers::cloudflare::clients::RadarGetDnsTimeseriesGroupByQueryTypeArgs;
use crate::providers::cloudflare::clients::RadarGetDnsTimeseriesGroupByResponseCodeArgs;
use crate::providers::cloudflare::clients::RadarGetDnsTimeseriesGroupByResponseTtlArgs;
use crate::providers::cloudflare::clients::RadarGetDnsTopAsesArgs;
use crate::providers::cloudflare::clients::RadarGetDnsTopLocationsArgs;
use crate::providers::cloudflare::clients::RadarGetEmailRoutingSummaryArgs;
use crate::providers::cloudflare::clients::RadarGetEmailRoutingSummaryByArcArgs;
use crate::providers::cloudflare::clients::RadarGetEmailRoutingSummaryByDkimArgs;
use crate::providers::cloudflare::clients::RadarGetEmailRoutingSummaryByDmarcArgs;
use crate::providers::cloudflare::clients::RadarGetEmailRoutingSummaryByEncryptedArgs;
use crate::providers::cloudflare::clients::RadarGetEmailRoutingSummaryByIpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetEmailRoutingSummaryBySpfArgs;
use crate::providers::cloudflare::clients::RadarGetEmailRoutingTimeseriesGroupArgs;
use crate::providers::cloudflare::clients::RadarGetEmailRoutingTimeseriesGroupByArcArgs;
use crate::providers::cloudflare::clients::RadarGetEmailRoutingTimeseriesGroupByDkimArgs;
use crate::providers::cloudflare::clients::RadarGetEmailRoutingTimeseriesGroupByDmarcArgs;
use crate::providers::cloudflare::clients::RadarGetEmailRoutingTimeseriesGroupByEncryptedArgs;
use crate::providers::cloudflare::clients::RadarGetEmailRoutingTimeseriesGroupByIpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetEmailRoutingTimeseriesGroupBySpfArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecuritySummaryArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecuritySummaryByArcArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecuritySummaryByDkimArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecuritySummaryByDmarcArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecuritySummaryByMaliciousArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecuritySummaryBySpamArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecuritySummaryBySpfArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecuritySummaryBySpoofArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecuritySummaryByThreatCategoryArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecuritySummaryByTlsVersionArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecurityTimeseriesGroupArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecurityTimeseriesGroupByArcArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecurityTimeseriesGroupByDkimArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecurityTimeseriesGroupByDmarcArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecurityTimeseriesGroupByMaliciousArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecurityTimeseriesGroupBySpamArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecurityTimeseriesGroupBySpfArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecurityTimeseriesGroupBySpoofArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecurityTimeseriesGroupByThreatCategoryArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecurityTimeseriesGroupByTlsVersionArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecurityTopTldsByMaliciousArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecurityTopTldsByMessagesArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecurityTopTldsBySpamArgs;
use crate::providers::cloudflare::clients::RadarGetEmailSecurityTopTldsBySpoofArgs;
use crate::providers::cloudflare::clients::RadarGetEntitiesAsnByIdArgs;
use crate::providers::cloudflare::clients::RadarGetEntitiesAsnByIpArgs;
use crate::providers::cloudflare::clients::RadarGetEntitiesAsnListArgs;
use crate::providers::cloudflare::clients::RadarGetEntitiesIpArgs;
use crate::providers::cloudflare::clients::RadarGetEntitiesLocationByAlpha2Args;
use crate::providers::cloudflare::clients::RadarGetEntitiesLocationsArgs;
use crate::providers::cloudflare::clients::RadarGetGeolocationDetailsArgs;
use crate::providers::cloudflare::clients::RadarGetGeolocationsArgs;
use crate::providers::cloudflare::clients::RadarGetHttpSummaryArgs;
use crate::providers::cloudflare::clients::RadarGetHttpSummaryByBotClassArgs;
use crate::providers::cloudflare::clients::RadarGetHttpSummaryByDeviceTypeArgs;
use crate::providers::cloudflare::clients::RadarGetHttpSummaryByHttpProtocolArgs;
use crate::providers::cloudflare::clients::RadarGetHttpSummaryByHttpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetHttpSummaryByIpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetHttpSummaryByOperatingSystemArgs;
use crate::providers::cloudflare::clients::RadarGetHttpSummaryByPostQuantumArgs;
use crate::providers::cloudflare::clients::RadarGetHttpSummaryByTlsVersionArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTimeseriesArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTimeseriesGroupArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTimeseriesGroupByBotClassArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTimeseriesGroupByBrowserFamiliesArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTimeseriesGroupByBrowsersArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTimeseriesGroupByDeviceTypeArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTimeseriesGroupByHttpProtocolArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTimeseriesGroupByHttpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTimeseriesGroupByIpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTimeseriesGroupByOperatingSystemArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTimeseriesGroupByPostQuantumArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTimeseriesGroupByTlsVersionArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopAsesByBotClassArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopAsesByBrowserFamilyArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopAsesByDeviceTypeArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopAsesByHttpProtocolArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopAsesByHttpRequestsArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopAsesByHttpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopAsesByIpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopAsesByOperatingSystemArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopAsesByTlsVersionArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopBrowserFamiliesArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopBrowsersArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopLocationsByBotClassArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopLocationsByBrowserFamilyArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopLocationsByDeviceTypeArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopLocationsByHttpProtocolArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopLocationsByHttpRequestsArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopLocationsByHttpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopLocationsByIpVersionArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopLocationsByOperatingSystemArgs;
use crate::providers::cloudflare::clients::RadarGetHttpTopLocationsByTlsVersionArgs;
use crate::providers::cloudflare::clients::RadarGetLeakedCredentialChecksSummaryArgs;
use crate::providers::cloudflare::clients::RadarGetLeakedCredentialChecksSummaryByBotClassArgs;
use crate::providers::cloudflare::clients::RadarGetLeakedCredentialChecksSummaryByCompromisedArgs;
use crate::providers::cloudflare::clients::RadarGetLeakedCredentialChecksTimeseriesGroupArgs;
use crate::providers::cloudflare::clients::RadarGetLeakedCredentialChecksTimeseriesGroupByBotClassArgs;
use crate::providers::cloudflare::clients::RadarGetLeakedCredentialChecksTimeseriesGroupByCompromisedArgs;
use crate::providers::cloudflare::clients::RadarGetNetflowsSummaryArgs;
use crate::providers::cloudflare::clients::RadarGetNetflowsSummaryDeprecatedArgs;
use crate::providers::cloudflare::clients::RadarGetNetflowsTimeseriesArgs;
use crate::providers::cloudflare::clients::RadarGetNetflowsTimeseriesGroupArgs;
use crate::providers::cloudflare::clients::RadarGetNetflowsTopAsesArgs;
use crate::providers::cloudflare::clients::RadarGetNetflowsTopLocationsArgs;
use crate::providers::cloudflare::clients::RadarGetOriginDetailsArgs;
use crate::providers::cloudflare::clients::RadarGetOriginPostQuantumSummaryArgs;
use crate::providers::cloudflare::clients::RadarGetOriginPostQuantumTimeseriesGroupsArgs;
use crate::providers::cloudflare::clients::RadarGetOriginsArgs;
use crate::providers::cloudflare::clients::RadarGetOriginsSummaryArgs;
use crate::providers::cloudflare::clients::RadarGetOriginsTimeseriesArgs;
use crate::providers::cloudflare::clients::RadarGetOriginsTimeseriesGroupArgs;
use crate::providers::cloudflare::clients::RadarGetPostQuantumTlsSupportArgs;
use crate::providers::cloudflare::clients::RadarGetQualityIndexSummaryArgs;
use crate::providers::cloudflare::clients::RadarGetQualityIndexTimeseriesGroupArgs;
use crate::providers::cloudflare::clients::RadarGetQualitySpeedHistogramArgs;
use crate::providers::cloudflare::clients::RadarGetQualitySpeedSummaryArgs;
use crate::providers::cloudflare::clients::RadarGetQualitySpeedTopAsesArgs;
use crate::providers::cloudflare::clients::RadarGetQualitySpeedTopLocationsArgs;
use crate::providers::cloudflare::clients::RadarGetRankingDomainDetailsArgs;
use crate::providers::cloudflare::clients::RadarGetRankingDomainTimeseriesArgs;
use crate::providers::cloudflare::clients::RadarGetRankingInternetServicesCategoriesArgs;
use crate::providers::cloudflare::clients::RadarGetRankingInternetServicesTimeseriesArgs;
use crate::providers::cloudflare::clients::RadarGetRankingTopDomainsArgs;
use crate::providers::cloudflare::clients::RadarGetRankingTopInternetServicesArgs;
use crate::providers::cloudflare::clients::RadarGetReportsDatasetDownloadArgs;
use crate::providers::cloudflare::clients::RadarGetReportsDatasetsArgs;
use crate::providers::cloudflare::clients::RadarGetRobotsTxtTopDomainCategoriesByFilesParsedArgs;
use crate::providers::cloudflare::clients::RadarGetRobotsTxtTopUserAgentsByDirectiveArgs;
use crate::providers::cloudflare::clients::RadarGetSearchGlobalArgs;
use crate::providers::cloudflare::clients::RadarGetTcpResetsTimeoutsSummaryArgs;
use crate::providers::cloudflare::clients::RadarGetTcpResetsTimeoutsTimeseriesGroupArgs;
use crate::providers::cloudflare::clients::RadarGetTldDetailsArgs;
use crate::providers::cloudflare::clients::RadarGetTldsArgs;
use crate::providers::cloudflare::clients::RadarGetTrafficAnomaliesArgs;
use crate::providers::cloudflare::clients::RadarGetTrafficAnomaliesTopArgs;
use crate::providers::cloudflare::clients::RadarGetVerifiedBotsTopByHttpRequestsArgs;
use crate::providers::cloudflare::clients::RadarGetVerifiedBotsTopCategoriesByHttpRequestsArgs;
use crate::providers::cloudflare::clients::RadarPostReportsDatasetDownloadUrlArgs;
use crate::providers::cloudflare::clients::RateLimitsForAZoneCreateARateLimitArgs;
use crate::providers::cloudflare::clients::RateLimitsForAZoneDeleteARateLimitArgs;
use crate::providers::cloudflare::clients::RateLimitsForAZoneGetARateLimitArgs;
use crate::providers::cloudflare::clients::RateLimitsForAZoneListRateLimitsArgs;
use crate::providers::cloudflare::clients::RateLimitsForAZoneUpdateARateLimitArgs;
use crate::providers::cloudflare::clients::RegenerateTokenArgs;
use crate::providers::cloudflare::clients::RegistrarDomainsGetDomainArgs;
use crate::providers::cloudflare::clients::RegistrarDomainsListDomainsArgs;
use crate::providers::cloudflare::clients::RegistrarDomainsUpdateDomainArgs;
use crate::providers::cloudflare::clients::ReplaceMeetingArgs;
use crate::providers::cloudflare::clients::ReplaceWebhookArgs;
use crate::providers::cloudflare::clients::RequestReviewArgs;
use crate::providers::cloudflare::clients::ResourcesCatalogExportArgs;
use crate::providers::cloudflare::clients::ResourcesCatalogListArgs;
use crate::providers::cloudflare::clients::ResourcesCatalogPolicyPreviewArgs;
use crate::providers::cloudflare::clients::ResourcesCatalogReadArgs;
use crate::providers::cloudflare::clients::RevokeDeviceArgs;
use crate::providers::cloudflare::clients::RevokeRegistrationsArgs;
use crate::providers::cloudflare::clients::SchemaValidationBulkEditPerOperationSettingsArgs;
use crate::providers::cloudflare::clients::SchemaValidationCreateSchemaArgs;
use crate::providers::cloudflare::clients::SchemaValidationDeletePerOperationSettingArgs;
use crate::providers::cloudflare::clients::SchemaValidationDeleteSchemaArgs;
use crate::providers::cloudflare::clients::SchemaValidationEditSchemaArgs;
use crate::providers::cloudflare::clients::SchemaValidationEditSettingsArgs;
use crate::providers::cloudflare::clients::SchemaValidationExtractOperationsFromSchemaArgs;
use crate::providers::cloudflare::clients::SchemaValidationGetPerOperationSettingArgs;
use crate::providers::cloudflare::clients::SchemaValidationGetSchemaArgs;
use crate::providers::cloudflare::clients::SchemaValidationGetSettingsArgs;
use crate::providers::cloudflare::clients::SchemaValidationListPerOperationSettingsArgs;
use crate::providers::cloudflare::clients::SchemaValidationListSchemaHostsArgs;
use crate::providers::cloudflare::clients::SchemaValidationListSchemasPaginatedArgs;
use crate::providers::cloudflare::clients::SchemaValidationUpdatePerOperationSettingArgs;
use crate::providers::cloudflare::clients::SchemaValidationUpdateSettingsArgs;
use crate::providers::cloudflare::clients::ScimGroupsCreateArgs;
use crate::providers::cloudflare::clients::ScimGroupsDeleteArgs;
use crate::providers::cloudflare::clients::ScimGroupsGetArgs;
use crate::providers::cloudflare::clients::ScimGroupsListArgs;
use crate::providers::cloudflare::clients::ScimGroupsPatchArgs;
use crate::providers::cloudflare::clients::ScimResourceTypesGetArgs;
use crate::providers::cloudflare::clients::ScimResourceTypesListArgs;
use crate::providers::cloudflare::clients::ScimSchemasGetArgs;
use crate::providers::cloudflare::clients::ScimSchemasListArgs;
use crate::providers::cloudflare::clients::ScimServiceProviderConfigGetArgs;
use crate::providers::cloudflare::clients::ScimUsersCreateArgs;
use crate::providers::cloudflare::clients::ScimUsersGetArgs;
use crate::providers::cloudflare::clients::ScimUsersListArgs;
use crate::providers::cloudflare::clients::ScimUsersPatchArgs;
use crate::providers::cloudflare::clients::ScimUsersPutArgs;
use crate::providers::cloudflare::clients::SecondaryDnsAclAclDetailsArgs;
use crate::providers::cloudflare::clients::SecondaryDnsAclCreateAclArgs;
use crate::providers::cloudflare::clients::SecondaryDnsAclDeleteAclArgs;
use crate::providers::cloudflare::clients::SecondaryDnsAclListAcLsArgs;
use crate::providers::cloudflare::clients::SecondaryDnsAclUpdateAclArgs;
use crate::providers::cloudflare::clients::SecondaryDnsPeerCreatePeerArgs;
use crate::providers::cloudflare::clients::SecondaryDnsPeerDeletePeerArgs;
use crate::providers::cloudflare::clients::SecondaryDnsPeerListPeersArgs;
use crate::providers::cloudflare::clients::SecondaryDnsPeerPeerDetailsArgs;
use crate::providers::cloudflare::clients::SecondaryDnsPeerUpdatePeerArgs;
use crate::providers::cloudflare::clients::SecondaryDnsPrimaryZoneCreatePrimaryZoneConfigurationArgs;
use crate::providers::cloudflare::clients::SecondaryDnsPrimaryZoneDeletePrimaryZoneConfigurationArgs;
use crate::providers::cloudflare::clients::SecondaryDnsPrimaryZoneDisableOutgoingZoneTransfersArgs;
use crate::providers::cloudflare::clients::SecondaryDnsPrimaryZoneEnableOutgoingZoneTransfersArgs;
use crate::providers::cloudflare::clients::SecondaryDnsPrimaryZoneForceDnsNotifyArgs;
use crate::providers::cloudflare::clients::SecondaryDnsPrimaryZoneGetOutgoingZoneTransferStatusArgs;
use crate::providers::cloudflare::clients::SecondaryDnsPrimaryZonePrimaryZoneConfigurationDetailsArgs;
use crate::providers::cloudflare::clients::SecondaryDnsPrimaryZoneUpdatePrimaryZoneConfigurationArgs;
use crate::providers::cloudflare::clients::SecondaryDnsSecondaryZoneCreateSecondaryZoneConfigurationArgs;
use crate::providers::cloudflare::clients::SecondaryDnsSecondaryZoneDeleteSecondaryZoneConfigurationArgs;
use crate::providers::cloudflare::clients::SecondaryDnsSecondaryZoneForceAxfrArgs;
use crate::providers::cloudflare::clients::SecondaryDnsSecondaryZoneSecondaryZoneConfigurationDetailsArgs;
use crate::providers::cloudflare::clients::SecondaryDnsSecondaryZoneUpdateSecondaryZoneConfigurationArgs;
use crate::providers::cloudflare::clients::SecondaryDnsTsigCreateTsigArgs;
use crate::providers::cloudflare::clients::SecondaryDnsTsigDeleteTsigArgs;
use crate::providers::cloudflare::clients::SecondaryDnsTsigListTsiGsArgs;
use crate::providers::cloudflare::clients::SecondaryDnsTsigTsigDetailsArgs;
use crate::providers::cloudflare::clients::SecondaryDnsTsigUpdateTsigArgs;
use crate::providers::cloudflare::clients::SecretsStoreCreateArgs;
use crate::providers::cloudflare::clients::SecretsStoreDeleteBulkArgs;
use crate::providers::cloudflare::clients::SecretsStoreDeleteByIdArgs;
use crate::providers::cloudflare::clients::SecretsStoreDuplicateByIdArgs;
use crate::providers::cloudflare::clients::SecretsStoreGetByIdArgs;
use crate::providers::cloudflare::clients::SecretsStoreGetStoreByIdArgs;
use crate::providers::cloudflare::clients::SecretsStoreListArgs;
use crate::providers::cloudflare::clients::SecretsStorePatchByIdArgs;
use crate::providers::cloudflare::clients::SecretsStoreQuotaArgs;
use crate::providers::cloudflare::clients::SecretsStoreSecretCreateArgs;
use crate::providers::cloudflare::clients::SecretsStoreSecretDeleteByIdArgs;
use crate::providers::cloudflare::clients::SecretsStoreSecretsListArgs;
use crate::providers::cloudflare::clients::SecretsStoreSystemCreateArgs;
use crate::providers::cloudflare::clients::SecretsStoreSystemDeleteBulkArgs;
use crate::providers::cloudflare::clients::SecretsStoreSystemDeleteByIdArgs;
use crate::providers::cloudflare::clients::SecretsStoreSystemDuplicateByIdArgs;
use crate::providers::cloudflare::clients::SecretsStoreSystemGetByIdArgs;
use crate::providers::cloudflare::clients::SecretsStoreSystemGetStoreByIdArgs;
use crate::providers::cloudflare::clients::SecretsStoreSystemListArgs;
use crate::providers::cloudflare::clients::SecretsStoreSystemPatchByIdArgs;
use crate::providers::cloudflare::clients::SecretsStoreSystemSecretCreateArgs;
use crate::providers::cloudflare::clients::SecretsStoreSystemSecretDeleteByIdArgs;
use crate::providers::cloudflare::clients::SecretsStoreSystemSecretsListArgs;
use crate::providers::cloudflare::clients::ShareCreateArgs;
use crate::providers::cloudflare::clients::ShareDeleteArgs;
use crate::providers::cloudflare::clients::ShareRecipientCreateArgs;
use crate::providers::cloudflare::clients::ShareRecipientDeleteArgs;
use crate::providers::cloudflare::clients::ShareRecipientsGetByIdArgs;
use crate::providers::cloudflare::clients::ShareRecipientsListArgs;
use crate::providers::cloudflare::clients::ShareRecipientsUpdateArgs;
use crate::providers::cloudflare::clients::ShareResourceCreateArgs;
use crate::providers::cloudflare::clients::ShareResourceDeleteArgs;
use crate::providers::cloudflare::clients::ShareResourceUpdateArgs;
use crate::providers::cloudflare::clients::ShareResourcesGetByIdArgs;
use crate::providers::cloudflare::clients::ShareResourcesListArgs;
use crate::providers::cloudflare::clients::ShareUpdateArgs;
use crate::providers::cloudflare::clients::SharesGetByIdArgs;
use crate::providers::cloudflare::clients::SharesListArgs;
use crate::providers::cloudflare::clients::SinkholeConfigGetSinkholesArgs;
use crate::providers::cloudflare::clients::SlurperAbortAllJobsArgs;
use crate::providers::cloudflare::clients::SlurperAbortJobArgs;
use crate::providers::cloudflare::clients::SlurperCheckSourceConnectivityArgs;
use crate::providers::cloudflare::clients::SlurperCheckTargetConnectivityArgs;
use crate::providers::cloudflare::clients::SlurperCreateJobArgs;
use crate::providers::cloudflare::clients::SlurperGetJobArgs;
use crate::providers::cloudflare::clients::SlurperGetJobLogsArgs;
use crate::providers::cloudflare::clients::SlurperGetJobProgressArgs;
use crate::providers::cloudflare::clients::SlurperListJobsArgs;
use crate::providers::cloudflare::clients::SlurperPauseJobArgs;
use crate::providers::cloudflare::clients::SlurperResumeJobArgs;
use crate::providers::cloudflare::clients::SmartShieldCreateHealthCheckArgs;
use crate::providers::cloudflare::clients::SmartShieldDeleteHealthCheckArgs;
use crate::providers::cloudflare::clients::SmartShieldGetSettingsArgs;
use crate::providers::cloudflare::clients::SmartShieldHealthCheckDetailsArgs;
use crate::providers::cloudflare::clients::SmartShieldListHealthChecksArgs;
use crate::providers::cloudflare::clients::SmartShieldPatchHealthCheckArgs;
use crate::providers::cloudflare::clients::SmartShieldPatchSettingsArgs;
use crate::providers::cloudflare::clients::SmartShieldSettingsGetCacheReserveClearArgs;
use crate::providers::cloudflare::clients::SmartShieldSettingsStartCacheReserveClearArgs;
use crate::providers::cloudflare::clients::SmartShieldUpdateHealthCheckArgs;
use crate::providers::cloudflare::clients::SmartTieredCacheDeleteSmartTieredCacheSettingArgs;
use crate::providers::cloudflare::clients::SmartTieredCacheGetSmartTieredCacheSettingArgs;
use crate::providers::cloudflare::clients::SmartTieredCachePatchSmartTieredCacheSettingArgs;
use crate::providers::cloudflare::clients::SpectrumAggregateAnalyticsGetCurrentAggregatedAnalyticsArgs;
use crate::providers::cloudflare::clients::SpectrumAnalyticsByTimeGetAnalyticsByTimeArgs;
use crate::providers::cloudflare::clients::SpectrumAnalyticsSummaryGetAnalyticsSummaryArgs;
use crate::providers::cloudflare::clients::SpectrumApplicationsCreateSpectrumApplicationUsingANameForTheOriginArgs;
use crate::providers::cloudflare::clients::SpectrumApplicationsDeleteSpectrumApplicationArgs;
use crate::providers::cloudflare::clients::SpectrumApplicationsGetSpectrumApplicationConfigurationArgs;
use crate::providers::cloudflare::clients::SpectrumApplicationsListSpectrumApplicationsArgs;
use crate::providers::cloudflare::clients::SpectrumApplicationsUpdateSpectrumApplicationConfigurationUsingANameForTheOriginArgs;
use crate::providers::cloudflare::clients::SpeedCreateScheduledTestArgs;
use crate::providers::cloudflare::clients::SpeedCreateTestArgs;
use crate::providers::cloudflare::clients::SpeedDeleteTestScheduleArgs;
use crate::providers::cloudflare::clients::SpeedDeleteTestsArgs;
use crate::providers::cloudflare::clients::SpeedGetAvailabilitiesArgs;
use crate::providers::cloudflare::clients::SpeedGetScheduledTestArgs;
use crate::providers::cloudflare::clients::SpeedGetTestArgs;
use crate::providers::cloudflare::clients::SpeedListPageTrendArgs;
use crate::providers::cloudflare::clients::SpeedListPagesArgs;
use crate::providers::cloudflare::clients::SpeedListTestHistoryArgs;
use crate::providers::cloudflare::clients::SslDetectorAutomaticModeGetEnrollmentArgs;
use crate::providers::cloudflare::clients::SslDetectorAutomaticModePatchEnrollmentArgs;
use crate::providers::cloudflare::clients::SslTlsModeRecommendationSslTlsRecommendationArgs;
use crate::providers::cloudflare::clients::SslVerificationEditSslCertificatePackValidationMethodArgs;
use crate::providers::cloudflare::clients::SslVerificationSslVerificationDetailsArgs;
use crate::providers::cloudflare::clients::StartLivestreamingArgs;
use crate::providers::cloudflare::clients::StartRecordingArgs;
use crate::providers::cloudflare::clients::StartTrackRecordingForAMeetingArgs;
use crate::providers::cloudflare::clients::StopLivestreamingArgs;
use crate::providers::cloudflare::clients::StoreCredentialsArgs;
use crate::providers::cloudflare::clients::StreamDownloadsCreateTypeSpecificDownloadsArgs;
use crate::providers::cloudflare::clients::StreamDownloadsDeleteTypeSpecificDownloadsArgs;
use crate::providers::cloudflare::clients::StreamLiveInputsCreateALiveInputArgs;
use crate::providers::cloudflare::clients::StreamLiveInputsCreateANewOutputConnectedToALiveInputArgs;
use crate::providers::cloudflare::clients::StreamLiveInputsDeleteALiveInputArgs;
use crate::providers::cloudflare::clients::StreamLiveInputsDeleteAnOutputArgs;
use crate::providers::cloudflare::clients::StreamLiveInputsDisableALiveInputArgs;
use crate::providers::cloudflare::clients::StreamLiveInputsEnableALiveInputArgs;
use crate::providers::cloudflare::clients::StreamLiveInputsListAllOutputsAssociatedWithASpecifiedLiveInputArgs;
use crate::providers::cloudflare::clients::StreamLiveInputsListLiveInputsArgs;
use crate::providers::cloudflare::clients::StreamLiveInputsRetrieveALiveInputArgs;
use crate::providers::cloudflare::clients::StreamLiveInputsUpdateALiveInputArgs;
use crate::providers::cloudflare::clients::StreamLiveInputsUpdateAnOutputArgs;
use crate::providers::cloudflare::clients::StreamMP4DownloadsCreateDownloadsArgs;
use crate::providers::cloudflare::clients::StreamMP4DownloadsDeleteDownloadsArgs;
use crate::providers::cloudflare::clients::StreamMP4DownloadsListDownloadsArgs;
use crate::providers::cloudflare::clients::StreamSigningKeysCreateSigningKeysArgs;
use crate::providers::cloudflare::clients::StreamSigningKeysDeleteSigningKeysArgs;
use crate::providers::cloudflare::clients::StreamSigningKeysListSigningKeysArgs;
use crate::providers::cloudflare::clients::StreamSubtitlesCaptionsDeleteCaptionsOrSubtitlesArgs;
use crate::providers::cloudflare::clients::StreamSubtitlesCaptionsGenerateCaptionOrSubtitleForLanguageArgs;
use crate::providers::cloudflare::clients::StreamSubtitlesCaptionsGetCaptionOrSubtitleForLanguageArgs;
use crate::providers::cloudflare::clients::StreamSubtitlesCaptionsGetVttCaptionOrSubtitleArgs;
use crate::providers::cloudflare::clients::StreamSubtitlesCaptionsListCaptionsOrSubtitlesArgs;
use crate::providers::cloudflare::clients::StreamSubtitlesCaptionsUploadCaptionsOrSubtitlesArgs;
use crate::providers::cloudflare::clients::StreamVideoClippingClipVideosGivenAStartAndEndTimeArgs;
use crate::providers::cloudflare::clients::StreamVideosCreateSignedUrlTokensForVideosArgs;
use crate::providers::cloudflare::clients::StreamVideosDeleteVideoArgs;
use crate::providers::cloudflare::clients::StreamVideosInitiateVideoUploadsUsingTusArgs;
use crate::providers::cloudflare::clients::StreamVideosListVideosArgs;
use crate::providers::cloudflare::clients::StreamVideosRetreieveEmbedCodeHtmlArgs;
use crate::providers::cloudflare::clients::StreamVideosRetrieveVideoDetailsArgs;
use crate::providers::cloudflare::clients::StreamVideosStorageUsageArgs;
use crate::providers::cloudflare::clients::StreamVideosUpdateVideoDetailsArgs;
use crate::providers::cloudflare::clients::StreamVideosUploadVideosFromAUrlArgs;
use crate::providers::cloudflare::clients::StreamVideosUploadVideosViaDirectUploadUrLsArgs;
use crate::providers::cloudflare::clients::StreamWatermarkProfileCreateWatermarkProfilesViaBasicUploadArgs;
use crate::providers::cloudflare::clients::StreamWatermarkProfileDeleteWatermarkProfilesArgs;
use crate::providers::cloudflare::clients::StreamWatermarkProfileListWatermarkProfilesArgs;
use crate::providers::cloudflare::clients::StreamWatermarkProfileWatermarkProfileDetailsArgs;
use crate::providers::cloudflare::clients::StreamWebhookCreateWebhooksArgs;
use crate::providers::cloudflare::clients::StreamWebhookDeleteWebhooksArgs;
use crate::providers::cloudflare::clients::StreamWebhookViewWebhooksArgs;
use crate::providers::cloudflare::clients::SubmitAbuseReportArgs;
use crate::providers::cloudflare::clients::SubscriptionsCreateArgs;
use crate::providers::cloudflare::clients::SubscriptionsDeleteArgs;
use crate::providers::cloudflare::clients::SubscriptionsGetArgs;
use crate::providers::cloudflare::clients::SubscriptionsListArgs;
use crate::providers::cloudflare::clients::SubscriptionsPatchArgs;
use crate::providers::cloudflare::clients::TagsDeleteArgs;
use crate::providers::cloudflare::clients::TagsGetArgs;
use crate::providers::cloudflare::clients::TagsListArgs;
use crate::providers::cloudflare::clients::TagsListKeysArgs;
use crate::providers::cloudflare::clients::TagsListValuesArgs;
use crate::providers::cloudflare::clients::TagsSetArgs;
use crate::providers::cloudflare::clients::TagsZoneDeleteArgs;
use crate::providers::cloudflare::clients::TagsZoneGetArgs;
use crate::providers::cloudflare::clients::TagsZoneSetArgs;
use crate::providers::cloudflare::clients::TelemetryKeysListArgs;
use crate::providers::cloudflare::clients::TelemetryQueryArgs;
use crate::providers::cloudflare::clients::TelemetryValuesListArgs;
use crate::providers::cloudflare::clients::TenantsListAccountsArgs;
use crate::providers::cloudflare::clients::TenantsListEntitlementsArgs;
use crate::providers::cloudflare::clients::TenantsListMembershipsArgs;
use crate::providers::cloudflare::clients::TenantsRetrieveTenantArgs;
use crate::providers::cloudflare::clients::TenantsValidAccountTypesArgs;
use crate::providers::cloudflare::clients::TieredCachingGetTieredCachingSettingArgs;
use crate::providers::cloudflare::clients::TieredCachingPatchTieredCachingSettingArgs;
use crate::providers::cloudflare::clients::TokenValidationConfigCreateArgs;
use crate::providers::cloudflare::clients::TokenValidationConfigCredentialsUpdateArgs;
use crate::providers::cloudflare::clients::TokenValidationConfigDeleteArgs;
use crate::providers::cloudflare::clients::TokenValidationConfigEditArgs;
use crate::providers::cloudflare::clients::TokenValidationConfigGetArgs;
use crate::providers::cloudflare::clients::TokenValidationConfigListArgs;
use crate::providers::cloudflare::clients::TokenValidationRulesBulkCreateArgs;
use crate::providers::cloudflare::clients::TokenValidationRulesBulkEditArgs;
use crate::providers::cloudflare::clients::TokenValidationRulesCreateArgs;
use crate::providers::cloudflare::clients::TokenValidationRulesDeleteArgs;
use crate::providers::cloudflare::clients::TokenValidationRulesEditArgs;
use crate::providers::cloudflare::clients::TokenValidationRulesGetArgs;
use crate::providers::cloudflare::clients::TokenValidationRulesListArgs;
use crate::providers::cloudflare::clients::TokenValidationRulesPreviewArgs;
use crate::providers::cloudflare::clients::TotalTlsEnableOrDisableTotalTlsArgs;
use crate::providers::cloudflare::clients::TotalTlsTotalTlsSettingsDetailsArgs;
use crate::providers::cloudflare::clients::TunnelRouteCreateATunnelRouteArgs;
use crate::providers::cloudflare::clients::TunnelRouteCreateATunnelRouteWithCidrArgs;
use crate::providers::cloudflare::clients::TunnelRouteDeleteATunnelRouteArgs;
use crate::providers::cloudflare::clients::TunnelRouteDeleteATunnelRouteWithCidrArgs;
use crate::providers::cloudflare::clients::TunnelRouteGetTunnelRouteArgs;
use crate::providers::cloudflare::clients::TunnelRouteGetTunnelRouteByIpArgs;
use crate::providers::cloudflare::clients::TunnelRouteListTunnelRoutesArgs;
use crate::providers::cloudflare::clients::TunnelRouteUpdateATunnelRouteArgs;
use crate::providers::cloudflare::clients::TunnelRouteUpdateATunnelRouteWithCidrArgs;
use crate::providers::cloudflare::clients::TunnelVirtualNetworkCreateAVirtualNetworkArgs;
use crate::providers::cloudflare::clients::TunnelVirtualNetworkDeleteArgs;
use crate::providers::cloudflare::clients::TunnelVirtualNetworkGetArgs;
use crate::providers::cloudflare::clients::TunnelVirtualNetworkListVirtualNetworksArgs;
use crate::providers::cloudflare::clients::TunnelVirtualNetworkUpdateArgs;
use crate::providers::cloudflare::clients::UniversalSslSettingsForAZoneEditUniversalSslSettingsArgs;
use crate::providers::cloudflare::clients::UniversalSslSettingsForAZoneUniversalSslSettingsDetailsArgs;
use crate::providers::cloudflare::clients::UnrevokeRegistrationsArgs;
use crate::providers::cloudflare::clients::UpdateAccountEntrypointRulesetArgs;
use crate::providers::cloudflare::clients::UpdateAccountRulesetArgs;
use crate::providers::cloudflare::clients::UpdateAccountRulesetRuleArgs;
use crate::providers::cloudflare::clients::UpdateAllowlistPrefixArgs;
use crate::providers::cloudflare::clients::UpdateApplicationVersionArgs;
use crate::providers::cloudflare::clients::UpdateCniArgs;
use crate::providers::cloudflare::clients::UpdateDexRuleArgs;
use crate::providers::cloudflare::clients::UpdateDnsProtectionRuleArgs;
use crate::providers::cloudflare::clients::UpdateHyperdriveArgs;
use crate::providers::cloudflare::clients::UpdateIpProfileArgs;
use crate::providers::cloudflare::clients::UpdateMaintenanceConfigArgs;
use crate::providers::cloudflare::clients::UpdateManagedTransformsArgs;
use crate::providers::cloudflare::clients::UpdateMeetingArgs;
use crate::providers::cloudflare::clients::UpdatePrefixArgs;
use crate::providers::cloudflare::clients::UpdateProtectionStatusArgs;
use crate::providers::cloudflare::clients::UpdateSecurityTxtArgs;
use crate::providers::cloudflare::clients::UpdateSettingsArgs;
use crate::providers::cloudflare::clients::UpdateSsoConnectorStateArgs;
use crate::providers::cloudflare::clients::UpdateSynProtectionFilterArgs;
use crate::providers::cloudflare::clients::UpdateSynProtectionRuleArgs;
use crate::providers::cloudflare::clients::UpdateTableMaintenanceConfigArgs;
use crate::providers::cloudflare::clients::UpdateTcpFlowProtectionFilterArgs;
use crate::providers::cloudflare::clients::UpdateTcpFlowProtectionRuleArgs;
use crate::providers::cloudflare::clients::UpdateTriggerArgs;
use crate::providers::cloudflare::clients::UpdateUrlNormalizationArgs;
use crate::providers::cloudflare::clients::UpdateWorkerArgs;
use crate::providers::cloudflare::clients::UpdateZoneEntrypointRulesetArgs;
use crate::providers::cloudflare::clients::UpdateZoneRulesetArgs;
use crate::providers::cloudflare::clients::UpdateZoneRulesetRuleArgs;
use crate::providers::cloudflare::clients::UpdateZoneSnippetArgs;
use crate::providers::cloudflare::clients::UpdateZoneSnippetRulesArgs;
use crate::providers::cloudflare::clients::UpsertEnvironmentVariablesArgs;
use crate::providers::cloudflare::clients::UpsertRepoConnectionArgs;
use crate::providers::cloudflare::clients::UrlscannerCreateScanArgs;
use crate::providers::cloudflare::clients::UrlscannerCreateScanBulkV2Args;
use crate::providers::cloudflare::clients::UrlscannerCreateScanV2Args;
use crate::providers::cloudflare::clients::UrlscannerGetResponseTextArgs;
use crate::providers::cloudflare::clients::UrlscannerGetResponseV2Args;
use crate::providers::cloudflare::clients::UrlscannerGetScanArgs;
use crate::providers::cloudflare::clients::UrlscannerGetScanDomV2Args;
use crate::providers::cloudflare::clients::UrlscannerGetScanHarArgs;
use crate::providers::cloudflare::clients::UrlscannerGetScanHarV2Args;
use crate::providers::cloudflare::clients::UrlscannerGetScanScreenshotArgs;
use crate::providers::cloudflare::clients::UrlscannerGetScanScreenshotV2Args;
use crate::providers::cloudflare::clients::UrlscannerGetScanV2Args;
use crate::providers::cloudflare::clients::UrlscannerSearchScansArgs;
use crate::providers::cloudflare::clients::UrlscannerSearchScansV2Args;
use crate::providers::cloudflare::clients::UserAgentBlockingRulesCreateAUserAgentBlockingRuleArgs;
use crate::providers::cloudflare::clients::UserAgentBlockingRulesDeleteAUserAgentBlockingRuleArgs;
use crate::providers::cloudflare::clients::UserAgentBlockingRulesGetAUserAgentBlockingRuleArgs;
use crate::providers::cloudflare::clients::UserAgentBlockingRulesListUserAgentBlockingRulesArgs;
use crate::providers::cloudflare::clients::UserAgentBlockingRulesUpdateAUserAgentBlockingRuleArgs;
use crate::providers::cloudflare::clients::UserApiTokensCreateTokenArgs;
use crate::providers::cloudflare::clients::UserApiTokensDeleteTokenArgs;
use crate::providers::cloudflare::clients::UserApiTokensListTokensArgs;
use crate::providers::cloudflare::clients::UserApiTokensRollTokenArgs;
use crate::providers::cloudflare::clients::UserApiTokensTokenDetailsArgs;
use crate::providers::cloudflare::clients::UserApiTokensUpdateTokenArgs;
use crate::providers::cloudflare::clients::UserBillingHistoryDeprecatedBillingHistoryDetailsArgs;
use crate::providers::cloudflare::clients::UserSAccountMembershipsDeleteMembershipArgs;
use crate::providers::cloudflare::clients::UserSAccountMembershipsListMembershipsArgs;
use crate::providers::cloudflare::clients::UserSAccountMembershipsMembershipDetailsArgs;
use crate::providers::cloudflare::clients::UserSAccountMembershipsUpdateMembershipArgs;
use crate::providers::cloudflare::clients::UserSInvitesInvitationDetailsArgs;
use crate::providers::cloudflare::clients::UserSInvitesRespondToInvitationArgs;
use crate::providers::cloudflare::clients::UserSOrganizationsLeaveOrganizationArgs;
use crate::providers::cloudflare::clients::UserSOrganizationsListOrganizationsArgs;
use crate::providers::cloudflare::clients::UserSOrganizationsOrganizationDetailsArgs;
use crate::providers::cloudflare::clients::UserSubscriptionDeleteUserSubscriptionArgs;
use crate::providers::cloudflare::clients::UserSubscriptionUpdateUserSubscriptionArgs;
use crate::providers::cloudflare::clients::VectorizeCreateMetadataIndexArgs;
use crate::providers::cloudflare::clients::VectorizeCreateVectorizeIndexArgs;
use crate::providers::cloudflare::clients::VectorizeDeleteMetadataIndexArgs;
use crate::providers::cloudflare::clients::VectorizeDeleteVectorizeIndexArgs;
use crate::providers::cloudflare::clients::VectorizeDeleteVectorsByIdArgs;
use crate::providers::cloudflare::clients::VectorizeDeprecatedCreateVectorizeIndexArgs;
use crate::providers::cloudflare::clients::VectorizeDeprecatedDeleteVectorizeIndexArgs;
use crate::providers::cloudflare::clients::VectorizeDeprecatedDeleteVectorsByIdArgs;
use crate::providers::cloudflare::clients::VectorizeDeprecatedGetVectorizeIndexArgs;
use crate::providers::cloudflare::clients::VectorizeDeprecatedGetVectorsByIdArgs;
use crate::providers::cloudflare::clients::VectorizeDeprecatedInsertVectorArgs;
use crate::providers::cloudflare::clients::VectorizeDeprecatedListVectorizeIndexesArgs;
use crate::providers::cloudflare::clients::VectorizeDeprecatedQueryVectorArgs;
use crate::providers::cloudflare::clients::VectorizeDeprecatedUpdateVectorizeIndexArgs;
use crate::providers::cloudflare::clients::VectorizeDeprecatedUpsertVectorArgs;
use crate::providers::cloudflare::clients::VectorizeGetVectorizeIndexArgs;
use crate::providers::cloudflare::clients::VectorizeGetVectorsByIdArgs;
use crate::providers::cloudflare::clients::VectorizeIndexInfoArgs;
use crate::providers::cloudflare::clients::VectorizeInsertVectorArgs;
use crate::providers::cloudflare::clients::VectorizeListMetadataIndexesArgs;
use crate::providers::cloudflare::clients::VectorizeListVectorizeIndexesArgs;
use crate::providers::cloudflare::clients::VectorizeListVectorsArgs;
use crate::providers::cloudflare::clients::VectorizeQueryVectorArgs;
use crate::providers::cloudflare::clients::VectorizeUpsertVectorArgs;
use crate::providers::cloudflare::clients::WafContentScanningAddCustomScanExpressionsArgs;
use crate::providers::cloudflare::clients::WafContentScanningDeleteCustomScanExpressionsArgs;
use crate::providers::cloudflare::clients::WafContentScanningDisableArgs;
use crate::providers::cloudflare::clients::WafContentScanningEnableArgs;
use crate::providers::cloudflare::clients::WafContentScanningGetStatusArgs;
use crate::providers::cloudflare::clients::WafContentScanningListCustomScanExpressionsArgs;
use crate::providers::cloudflare::clients::WafContentScanningUpdateSettingsArgs;
use crate::providers::cloudflare::clients::WafOverridesCreateAWafOverrideArgs;
use crate::providers::cloudflare::clients::WafOverridesDeleteAWafOverrideArgs;
use crate::providers::cloudflare::clients::WafOverridesGetAWafOverrideArgs;
use crate::providers::cloudflare::clients::WafOverridesListWafOverridesArgs;
use crate::providers::cloudflare::clients::WafOverridesUpdateWafOverrideArgs;
use crate::providers::cloudflare::clients::WafPackagesGetAWafPackageArgs;
use crate::providers::cloudflare::clients::WafPackagesListWafPackagesArgs;
use crate::providers::cloudflare::clients::WafPackagesUpdateAWafPackageArgs;
use crate::providers::cloudflare::clients::WafProductApiLeakedCredentialsCreateDetectionArgs;
use crate::providers::cloudflare::clients::WafProductApiLeakedCredentialsDeleteDetectionArgs;
use crate::providers::cloudflare::clients::WafProductApiLeakedCredentialsGetDetectionArgs;
use crate::providers::cloudflare::clients::WafProductApiLeakedCredentialsGetStatusArgs;
use crate::providers::cloudflare::clients::WafProductApiLeakedCredentialsListDetectionsArgs;
use crate::providers::cloudflare::clients::WafProductApiLeakedCredentialsSetStatusArgs;
use crate::providers::cloudflare::clients::WafProductApiLeakedCredentialsUpdateDetectionArgs;
use crate::providers::cloudflare::clients::WafRuleGroupsGetAWafRuleGroupArgs;
use crate::providers::cloudflare::clients::WafRuleGroupsListWafRuleGroupsArgs;
use crate::providers::cloudflare::clients::WafRuleGroupsUpdateAWafRuleGroupArgs;
use crate::providers::cloudflare::clients::WafRulesGetAWafRuleArgs;
use crate::providers::cloudflare::clients::WafRulesListWafRulesArgs;
use crate::providers::cloudflare::clients::WafRulesUpdateAWafRuleArgs;
use crate::providers::cloudflare::clients::WaitingRoomCreateACustomWaitingRoomPagePreviewArgs;
use crate::providers::cloudflare::clients::WaitingRoomCreateEventArgs;
use crate::providers::cloudflare::clients::WaitingRoomCreateWaitingRoomArgs;
use crate::providers::cloudflare::clients::WaitingRoomCreateWaitingRoomRuleArgs;
use crate::providers::cloudflare::clients::WaitingRoomDeleteEventArgs;
use crate::providers::cloudflare::clients::WaitingRoomDeleteWaitingRoomArgs;
use crate::providers::cloudflare::clients::WaitingRoomDeleteWaitingRoomRuleArgs;
use crate::providers::cloudflare::clients::WaitingRoomEventDetailsArgs;
use crate::providers::cloudflare::clients::WaitingRoomGetWaitingRoomStatusArgs;
use crate::providers::cloudflare::clients::WaitingRoomGetZoneSettingsArgs;
use crate::providers::cloudflare::clients::WaitingRoomListEventsArgs;
use crate::providers::cloudflare::clients::WaitingRoomListWaitingRoomRulesArgs;
use crate::providers::cloudflare::clients::WaitingRoomListWaitingRoomsAccountArgs;
use crate::providers::cloudflare::clients::WaitingRoomListWaitingRoomsArgs;
use crate::providers::cloudflare::clients::WaitingRoomPatchEventArgs;
use crate::providers::cloudflare::clients::WaitingRoomPatchWaitingRoomArgs;
use crate::providers::cloudflare::clients::WaitingRoomPatchWaitingRoomRuleArgs;
use crate::providers::cloudflare::clients::WaitingRoomPatchZoneSettingsArgs;
use crate::providers::cloudflare::clients::WaitingRoomPreviewActiveEventDetailsArgs;
use crate::providers::cloudflare::clients::WaitingRoomReplaceWaitingRoomRulesArgs;
use crate::providers::cloudflare::clients::WaitingRoomUpdateEventArgs;
use crate::providers::cloudflare::clients::WaitingRoomUpdateWaitingRoomArgs;
use crate::providers::cloudflare::clients::WaitingRoomUpdateZoneSettingsArgs;
use crate::providers::cloudflare::clients::WaitingRoomWaitingRoomDetailsArgs;
use crate::providers::cloudflare::clients::Web3HostnameCreateIpfsUniversalPathGatewayContentListEntryArgs;
use crate::providers::cloudflare::clients::Web3HostnameCreateWeb3HostnameArgs;
use crate::providers::cloudflare::clients::Web3HostnameDeleteIpfsUniversalPathGatewayContentListEntryArgs;
use crate::providers::cloudflare::clients::Web3HostnameDeleteWeb3HostnameArgs;
use crate::providers::cloudflare::clients::Web3HostnameEditIpfsUniversalPathGatewayContentListEntryArgs;
use crate::providers::cloudflare::clients::Web3HostnameEditWeb3HostnameArgs;
use crate::providers::cloudflare::clients::Web3HostnameIpfsUniversalPathGatewayContentListDetailsArgs;
use crate::providers::cloudflare::clients::Web3HostnameIpfsUniversalPathGatewayContentListEntryDetailsArgs;
use crate::providers::cloudflare::clients::Web3HostnameListIpfsUniversalPathGatewayContentListEntriesArgs;
use crate::providers::cloudflare::clients::Web3HostnameListWeb3HostnamesArgs;
use crate::providers::cloudflare::clients::Web3HostnameUpdateIpfsUniversalPathGatewayContentListArgs;
use crate::providers::cloudflare::clients::Web3HostnameWeb3HostnameDetailsArgs;
use crate::providers::cloudflare::clients::WebAnalyticsCreateRuleArgs;
use crate::providers::cloudflare::clients::WebAnalyticsCreateSiteArgs;
use crate::providers::cloudflare::clients::WebAnalyticsDeleteRuleArgs;
use crate::providers::cloudflare::clients::WebAnalyticsDeleteSiteArgs;
use crate::providers::cloudflare::clients::WebAnalyticsGetRumStatusArgs;
use crate::providers::cloudflare::clients::WebAnalyticsGetSiteArgs;
use crate::providers::cloudflare::clients::WebAnalyticsListRulesArgs;
use crate::providers::cloudflare::clients::WebAnalyticsListSitesArgs;
use crate::providers::cloudflare::clients::WebAnalyticsModifyRulesArgs;
use crate::providers::cloudflare::clients::WebAnalyticsToggleRumArgs;
use crate::providers::cloudflare::clients::WebAnalyticsUpdateRuleArgs;
use crate::providers::cloudflare::clients::WebAnalyticsUpdateSiteArgs;
use crate::providers::cloudflare::clients::WhoisRecordGetWhoisRecordArgs;
use crate::providers::cloudflare::clients::WorBatchCreateWorkflowInstanceArgs;
use crate::providers::cloudflare::clients::WorBatchTerminateWorkflowInstancesArgs;
use crate::providers::cloudflare::clients::WorChangeStatusWorkflowInstanceArgs;
use crate::providers::cloudflare::clients::WorCreateNewWorkflowInstanceArgs;
use crate::providers::cloudflare::clients::WorCreateOrModifyWorkflowArgs;
use crate::providers::cloudflare::clients::WorDeleteWorkflowArgs;
use crate::providers::cloudflare::clients::WorDescribeWorkflowInstanceArgs;
use crate::providers::cloudflare::clients::WorDescribeWorkflowVersionsArgs;
use crate::providers::cloudflare::clients::WorDescribeWorkflowVersionsDagArgs;
use crate::providers::cloudflare::clients::WorDescribeWorkflowVersionsGraphArgs;
use crate::providers::cloudflare::clients::WorGetWorkflowDetailsArgs;
use crate::providers::cloudflare::clients::WorListWorkflowInstancesArgs;
use crate::providers::cloudflare::clients::WorListWorkflowVersionsArgs;
use crate::providers::cloudflare::clients::WorListWorkflowsArgs;
use crate::providers::cloudflare::clients::WorSendEventWorkflowInstanceArgs;
use crate::providers::cloudflare::clients::WorStatusTerminateWorkflowInstancesArgs;
use crate::providers::cloudflare::clients::WorkerAccountSettingsCreateWorkerAccountSettingsArgs;
use crate::providers::cloudflare::clients::WorkerAccountSettingsFetchWorkerAccountSettingsArgs;
use crate::providers::cloudflare::clients::WorkerAssetsUploadArgs;
use crate::providers::cloudflare::clients::WorkerCronTriggerGetCronTriggersArgs;
use crate::providers::cloudflare::clients::WorkerCronTriggerUpdateCronTriggersArgs;
use crate::providers::cloudflare::clients::WorkerDeleteScriptSecretArgs;
use crate::providers::cloudflare::clients::WorkerDeploymentsCreateDeploymentArgs;
use crate::providers::cloudflare::clients::WorkerDeploymentsDeleteDeploymentArgs;
use crate::providers::cloudflare::clients::WorkerDeploymentsGetDeploymentArgs;
use crate::providers::cloudflare::clients::WorkerDeploymentsListDeploymentsArgs;
use crate::providers::cloudflare::clients::WorkerEnvironmentGetScriptContentArgs;
use crate::providers::cloudflare::clients::WorkerEnvironmentPutScriptContentArgs;
use crate::providers::cloudflare::clients::WorkerGetScriptSecretArgs;
use crate::providers::cloudflare::clients::WorkerListScriptSecretsArgs;
use crate::providers::cloudflare::clients::WorkerPlacementListRegionsArgs;
use crate::providers::cloudflare::clients::WorkerPutScriptSecretArgs;
use crate::providers::cloudflare::clients::WorkerRoutesCreateRouteArgs;
use crate::providers::cloudflare::clients::WorkerRoutesDeleteRouteArgs;
use crate::providers::cloudflare::clients::WorkerRoutesGetRouteArgs;
use crate::providers::cloudflare::clients::WorkerRoutesListRoutesArgs;
use crate::providers::cloudflare::clients::WorkerRoutesUpdateRouteArgs;
use crate::providers::cloudflare::clients::WorkerScriptDeleteSubdomainArgs;
use crate::providers::cloudflare::clients::WorkerScriptDeleteWorkerArgs;
use crate::providers::cloudflare::clients::WorkerScriptDownloadWorkerArgs;
use crate::providers::cloudflare::clients::WorkerScriptEnvironmentGetSettingsArgs;
use crate::providers::cloudflare::clients::WorkerScriptEnvironmentPatchSettingsArgs;
use crate::providers::cloudflare::clients::WorkerScriptFetchUsageModelArgs;
use crate::providers::cloudflare::clients::WorkerScriptGetContentArgs;
use crate::providers::cloudflare::clients::WorkerScriptGetSettingsArgs;
use crate::providers::cloudflare::clients::WorkerScriptGetSubdomainArgs;
use crate::providers::cloudflare::clients::WorkerScriptListWorkersArgs;
use crate::providers::cloudflare::clients::WorkerScriptPatchSettingsArgs;
use crate::providers::cloudflare::clients::WorkerScriptPostSubdomainArgs;
use crate::providers::cloudflare::clients::WorkerScriptPutContentArgs;
use crate::providers::cloudflare::clients::WorkerScriptSearchWorkersArgs;
use crate::providers::cloudflare::clients::WorkerScriptSettingsGetSettingsArgs;
use crate::providers::cloudflare::clients::WorkerScriptSettingsPatchSettingsArgs;
use crate::providers::cloudflare::clients::WorkerScriptUpdateCreateAssetsUploadSessionArgs;
use crate::providers::cloudflare::clients::WorkerScriptUpdateUsageModelArgs;
use crate::providers::cloudflare::clients::WorkerScriptUploadWorkerModuleArgs;
use crate::providers::cloudflare::clients::WorkerSubdomainCreateSubdomainArgs;
use crate::providers::cloudflare::clients::WorkerSubdomainDeleteSubdomainArgs;
use crate::providers::cloudflare::clients::WorkerSubdomainGetSubdomainArgs;
use crate::providers::cloudflare::clients::WorkerTailLogsDeleteTailArgs;
use crate::providers::cloudflare::clients::WorkerTailLogsStartTailArgs;
use crate::providers::cloudflare::clients::WorkerVersionsGetVersionDetailArgs;
use crate::providers::cloudflare::clients::WorkerVersionsListVersionsArgs;
use crate::providers::cloudflare::clients::WorkerVersionsUploadVersionArgs;
use crate::providers::cloudflare::clients::WorkersAiCreateFinetuneArgs;
use crate::providers::cloudflare::clients::WorkersAiGetModelSchemaArgs;
use crate::providers::cloudflare::clients::WorkersAiGetToMarkdownSupportedArgs;
use crate::providers::cloudflare::clients::WorkersAiListFinetunesArgs;
use crate::providers::cloudflare::clients::WorkersAiListPublicFinetunesArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfAi4BharatIndictrans2EnIndic1BArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfAi4BharatNonomniIndictrans2EnIndic1BArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfAisingaporeGemmaSeaLionV427BItArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfBaaiBgeBaseEnV15Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfBaaiBgeLargeEnV15Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfBaaiBgeM3Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfBaaiBgeRerankerBaseArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfBaaiBgeSmallEnV15Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfBaaiNonomniBgeBaseEnV15Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfBaaiNonomniBgeLargeEnV15Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfBaaiNonomniBgeM3Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfBaaiNonomniBgeSmallEnV15Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfBlackForestLabsFlux1SchnellArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfBlackForestLabsFlux2DevArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfBlackForestLabsFlux2Klein4BArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfBlackForestLabsFlux2Klein9BArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfBytedanceStableDiffusionXlLightningArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfDeepgramAura1Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfDeepgramAura2EnArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfDeepgramAura2EsArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfDeepgramFluxArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfDeepgramNova3Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfDeepseekAiDeepseekMath7BInstructArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfDeepseekAiDeepseekR1DistillQwen32BArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfDefogSqlcoder7B2Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfFacebookBartLargeCnnArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfFacebookNonomniBartLargeCnnArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfFacebookNonomniDetrResnet50Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfFblgitUnaCybertron7BV2Bf16Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfGoogleEmbeddinggemma300MArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfGoogleGemma2BItLoraArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfGoogleGemma312BItArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfGoogleGemma7BItLoraArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfGoogleNonomniEmbeddinggemma300MArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfHuggingfaceDistilbertSst2Int8Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfHuggingfaceNonomniDistilbertSst2Int8Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfIbmGraniteGranite40HMicroArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfLeonardoLucidOriginArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfLeonardoPhoenix10Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfLykonDreamshaper8LcmArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMetaLlama27BChatFp16Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMetaLlama27BChatInt8Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMetaLlama3170BInstructFp8FastArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMetaLlama318BInstructAwqArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMetaLlama318BInstructFp8Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMetaLlama318BInstructFp8FastArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMetaLlama3211BVisionInstructArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMetaLlama321BInstructArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMetaLlama323BInstructArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMetaLlama3370BInstructFp8FastArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMetaLlama38BInstructArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMetaLlama38BInstructAwqArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMetaLlama4Scout17B16EInstructArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMetaLlamaGuard38BArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMetaLlamaLlama27BChatHfLoraArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMetaM2M10012BArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMicrosoftNonomniResnet50Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMicrosoftPhi2Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMicrosoftResnet50Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMistralMistral7BInstructV01Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMistralMistral7BInstructV02LoraArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMistralaiMistralSmall3124BInstructArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMoonshotaiKimiK25Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfMyshellAiMelottsArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfNvidiaNemotron3120BA12BArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfOpenaiGptOss120BArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfOpenaiGptOss20BArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfOpenaiWhisperArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfOpenaiWhisperLargeV3TurboArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfOpenaiWhisperTinyEnArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfOpenchatOpenchat350106Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfPfnetPlamoEmbedding1BArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfQwenQwen1505BChatArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfQwenQwen1514BChatAwqArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfQwenQwen1518BChatArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfQwenQwen157BChatAwqArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfQwenQwen25Coder32BInstructArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfQwenQwen330BA3BFp8Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfQwenQwen3Embedding06BArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfQwenQwq32BArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfRunwaymlStableDiffusionV15Img2ImgArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfRunwaymlStableDiffusionV15InpaintingArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfStabilityaiStableDiffusionXlBase10Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfTheblokeDiscolmGerman7BV1AwqArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfTiiuaeFalcon7BInstructArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfTinyllamaTinyllama11BChatV10Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunCfZaiOrgGlm47FlashArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunHfGoogleGemma7BItArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunHfMistralMistral7BInstructV02Args;
use crate::providers::cloudflare::clients::WorkersAiPostRunHfNexusflowStarlingLm7BBetaArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunHfNousresearchHermes2ProMistral7BArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunHfTheblokeDeepseekCoder67BBaseAwqArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunHfTheblokeDeepseekCoder67BInstructAwqArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunHfTheblokeLlama213BChatAwqArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunHfTheblokeMistral7BInstructV01AwqArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunHfTheblokeNeuralChat7BV31AwqArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunHfTheblokeOpenhermes25Mistral7BAwqArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunHfTheblokeZephyr7BBetaAwqArgs;
use crate::providers::cloudflare::clients::WorkersAiPostRunModelArgs;
use crate::providers::cloudflare::clients::WorkersAiPostToMarkdownArgs;
use crate::providers::cloudflare::clients::WorkersAiPostWebsocketRunCfDeepgramAura1Args;
use crate::providers::cloudflare::clients::WorkersAiPostWebsocketRunCfDeepgramAura1InternalArgs;
use crate::providers::cloudflare::clients::WorkersAiPostWebsocketRunCfDeepgramAura2Args;
use crate::providers::cloudflare::clients::WorkersAiPostWebsocketRunCfDeepgramAura2EnArgs;
use crate::providers::cloudflare::clients::WorkersAiPostWebsocketRunCfDeepgramAura2EsArgs;
use crate::providers::cloudflare::clients::WorkersAiPostWebsocketRunCfDeepgramAuraArgs;
use crate::providers::cloudflare::clients::WorkersAiPostWebsocketRunCfDeepgramFluxArgs;
use crate::providers::cloudflare::clients::WorkersAiPostWebsocketRunCfDeepgramNova3Args;
use crate::providers::cloudflare::clients::WorkersAiPostWebsocketRunCfDeepgramNova3InternalArgs;
use crate::providers::cloudflare::clients::WorkersAiPostWebsocketRunCfDeepgramNova3WsArgs;
use crate::providers::cloudflare::clients::WorkersAiPostWebsocketRunCfPipecatAiSmartTurnV2Args;
use crate::providers::cloudflare::clients::WorkersAiPostWebsocketRunCfPipecatAiSmartTurnV3Args;
use crate::providers::cloudflare::clients::WorkersAiPostWebsocketRunCfSvenTestPipeHttpArgs;
use crate::providers::cloudflare::clients::WorkersAiPostWebsocketRunCfTestHelloWorldCogArgs;
use crate::providers::cloudflare::clients::WorkersAiSearchAuthorArgs;
use crate::providers::cloudflare::clients::WorkersAiSearchModelArgs;
use crate::providers::cloudflare::clients::WorkersAiSearchTaskArgs;
use crate::providers::cloudflare::clients::WorkersAiUploadFinetuneAssetArgs;
use crate::providers::cloudflare::clients::WorkersDomainsDeleteArgs;
use crate::providers::cloudflare::clients::WorkersDomainsGetArgs;
use crate::providers::cloudflare::clients::WorkersDomainsListArgs;
use crate::providers::cloudflare::clients::WorkersDomainsUpdateArgs;
use crate::providers::cloudflare::clients::WorkersKvNamespaceCreateANamespaceArgs;
use crate::providers::cloudflare::clients::WorkersKvNamespaceDeleteKeyValuePairArgs;
use crate::providers::cloudflare::clients::WorkersKvNamespaceDeleteMultipleKeyValuePairsArgs;
use crate::providers::cloudflare::clients::WorkersKvNamespaceDeleteMultipleKeyValuePairsDeprecatedArgs;
use crate::providers::cloudflare::clients::WorkersKvNamespaceGetANamespaceArgs;
use crate::providers::cloudflare::clients::WorkersKvNamespaceGetMultipleKeyValuePairsArgs;
use crate::providers::cloudflare::clients::WorkersKvNamespaceListANamespaceSKeysArgs;
use crate::providers::cloudflare::clients::WorkersKvNamespaceListNamespacesArgs;
use crate::providers::cloudflare::clients::WorkersKvNamespaceReadKeyValuePairArgs;
use crate::providers::cloudflare::clients::WorkersKvNamespaceReadTheMetadataForAKeyArgs;
use crate::providers::cloudflare::clients::WorkersKvNamespaceRemoveANamespaceArgs;
use crate::providers::cloudflare::clients::WorkersKvNamespaceRenameANamespaceArgs;
use crate::providers::cloudflare::clients::WorkersKvNamespaceWriteKeyValuePairWithMetadataArgs;
use crate::providers::cloudflare::clients::WorkersKvNamespaceWriteMultipleKeyValuePairsArgs;
use crate::providers::cloudflare::clients::ZeroTrustAccountsCreateZeroTrustAccountArgs;
use crate::providers::cloudflare::clients::ZeroTrustAccountsDeleteDeviceSettingsForZeroTrustAccountArgs;
use crate::providers::cloudflare::clients::ZeroTrustAccountsGetConnectivitySettingsArgs;
use crate::providers::cloudflare::clients::ZeroTrustAccountsGetDeviceSettingsForZeroTrustAccountArgs;
use crate::providers::cloudflare::clients::ZeroTrustAccountsGetLoggingSettingsForTheZeroTrustAccountArgs;
use crate::providers::cloudflare::clients::ZeroTrustAccountsGetZeroTrustAccountConfigurationArgs;
use crate::providers::cloudflare::clients::ZeroTrustAccountsGetZeroTrustAccountInformationArgs;
use crate::providers::cloudflare::clients::ZeroTrustAccountsGetZeroTrustCertificateConfigurationArgs;
use crate::providers::cloudflare::clients::ZeroTrustAccountsPatchConnectivitySettingsArgs;
use crate::providers::cloudflare::clients::ZeroTrustAccountsPatchDeviceSettingsForTheZeroTrustAccountArgs;
use crate::providers::cloudflare::clients::ZeroTrustAccountsPatchZeroTrustAccountConfigurationArgs;
use crate::providers::cloudflare::clients::ZeroTrustAccountsUpdateDeviceSettingsForTheZeroTrustAccountArgs;
use crate::providers::cloudflare::clients::ZeroTrustAccountsUpdateLoggingSettingsForTheZeroTrustAccountArgs;
use crate::providers::cloudflare::clients::ZeroTrustAccountsUpdateZeroTrustAccountConfigurationArgs;
use crate::providers::cloudflare::clients::ZeroTrustApplicationsReviewStatusListArgs;
use crate::providers::cloudflare::clients::ZeroTrustApplicationsReviewStatusUpdateArgs;
use crate::providers::cloudflare::clients::ZeroTrustCertificatesActivateZeroTrustCertificateArgs;
use crate::providers::cloudflare::clients::ZeroTrustCertificatesCreateZeroTrustCertificateArgs;
use crate::providers::cloudflare::clients::ZeroTrustCertificatesDeactivateZeroTrustCertificateArgs;
use crate::providers::cloudflare::clients::ZeroTrustCertificatesDeleteZeroTrustCertificateArgs;
use crate::providers::cloudflare::clients::ZeroTrustCertificatesListZeroTrustCertificatesArgs;
use crate::providers::cloudflare::clients::ZeroTrustCertificatesZeroTrustCertificateDetailsArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayApplicationAndApplicationTypeMappingsListApplicationAndApplicationTypeMappingsArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayCategoriesListCategoriesArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayLocationsCreateZeroTrustGatewayLocationArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayLocationsDeleteZeroTrustGatewayLocationArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayLocationsListZeroTrustGatewayLocationsArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayLocationsUpdateZeroTrustGatewayLocationArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayLocationsZeroTrustGatewayLocationDetailsArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayPacfilesCreatePacfileArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayPacfilesDeleteArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayPacfilesDetailsArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayPacfilesListArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayPacfilesUpdateArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayProxyEndpointsCreateProxyEndpointArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayProxyEndpointsDeleteProxyEndpointArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayProxyEndpointsListProxyEndpointsArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayProxyEndpointsProxyEndpointDetailsArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayProxyEndpointsUpdateProxyEndpointArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayRulesCreateZeroTrustGatewayRuleArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayRulesDeleteZeroTrustGatewayRuleArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayRulesListZeroTrustGatewayRulesArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayRulesListZeroTrustGatewayRulesTenantArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayRulesResetExpirationZeroTrustGatewayRuleArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayRulesUpdateZeroTrustGatewayRuleArgs;
use crate::providers::cloudflare::clients::ZeroTrustGatewayRulesZeroTrustGatewayRuleDetailsArgs;
use crate::providers::cloudflare::clients::ZeroTrustGetAuditSshSettingsArgs;
use crate::providers::cloudflare::clients::ZeroTrustListsCreateZeroTrustListArgs;
use crate::providers::cloudflare::clients::ZeroTrustListsDeleteZeroTrustListArgs;
use crate::providers::cloudflare::clients::ZeroTrustListsListZeroTrustListsArgs;
use crate::providers::cloudflare::clients::ZeroTrustListsPatchZeroTrustListArgs;
use crate::providers::cloudflare::clients::ZeroTrustListsUpdateZeroTrustListArgs;
use crate::providers::cloudflare::clients::ZeroTrustListsZeroTrustListDetailsArgs;
use crate::providers::cloudflare::clients::ZeroTrustListsZeroTrustListItemsArgs;
use crate::providers::cloudflare::clients::ZeroTrustNetworksRouteHostnameCreateArgs;
use crate::providers::cloudflare::clients::ZeroTrustNetworksRouteHostnameDeleteArgs;
use crate::providers::cloudflare::clients::ZeroTrustNetworksRouteHostnameGetArgs;
use crate::providers::cloudflare::clients::ZeroTrustNetworksRouteHostnameListArgs;
use crate::providers::cloudflare::clients::ZeroTrustNetworksRouteHostnameUpdateArgs;
use crate::providers::cloudflare::clients::ZeroTrustNetworksSubnetCreateWarpArgs;
use crate::providers::cloudflare::clients::ZeroTrustNetworksSubnetDeleteWarpArgs;
use crate::providers::cloudflare::clients::ZeroTrustNetworksSubnetGetWarpArgs;
use crate::providers::cloudflare::clients::ZeroTrustNetworksSubnetUpdateCloudflareSourceArgs;
use crate::providers::cloudflare::clients::ZeroTrustNetworksSubnetUpdateWarpArgs;
use crate::providers::cloudflare::clients::ZeroTrustNetworksSubnetsListArgs;
use crate::providers::cloudflare::clients::ZeroTrustOrganizationCreateYourZeroTrustOrganizationArgs;
use crate::providers::cloudflare::clients::ZeroTrustOrganizationGetYourZeroTrustOrganizationArgs;
use crate::providers::cloudflare::clients::ZeroTrustOrganizationGetYourZeroTrustOrganizationDohSettingsArgs;
use crate::providers::cloudflare::clients::ZeroTrustOrganizationRevokeAllAccessTokensForAUserArgs;
use crate::providers::cloudflare::clients::ZeroTrustOrganizationUpdateYourZeroTrustOrganizationArgs;
use crate::providers::cloudflare::clients::ZeroTrustOrganizationUpdateYourZeroTrustOrganizationDohSettingsArgs;
use crate::providers::cloudflare::clients::ZeroTrustRotateSshAccountSeedArgs;
use crate::providers::cloudflare::clients::ZeroTrustSeatsUpdateAUserSeatArgs;
use crate::providers::cloudflare::clients::ZeroTrustUpdateAuditSshSettingsArgs;
use crate::providers::cloudflare::clients::ZeroTrustUsersCreateUserArgs;
use crate::providers::cloudflare::clients::ZeroTrustUsersDeleteMfaAuthenticatorArgs;
use crate::providers::cloudflare::clients::ZeroTrustUsersDeleteUserArgs;
use crate::providers::cloudflare::clients::ZeroTrustUsersGetActiveSessionArgs;
use crate::providers::cloudflare::clients::ZeroTrustUsersGetActiveSessionsArgs;
use crate::providers::cloudflare::clients::ZeroTrustUsersGetFailedLoginsArgs;
use crate::providers::cloudflare::clients::ZeroTrustUsersGetLastSeenIdentityArgs;
use crate::providers::cloudflare::clients::ZeroTrustUsersGetUserArgs;
use crate::providers::cloudflare::clients::ZeroTrustUsersGetUsersArgs;
use crate::providers::cloudflare::clients::ZeroTrustUsersUpdateUserArgs;
use crate::providers::cloudflare::clients::ZoneAnalyticsDeprecatedGetAnalyticsByCoLocationsArgs;
use crate::providers::cloudflare::clients::ZoneAnalyticsDeprecatedGetDashboardArgs;
use crate::providers::cloudflare::clients::ZoneCacheSettingsChangeAegisSettingArgs;
use crate::providers::cloudflare::clients::ZoneCacheSettingsChangeCacheReserveSettingArgs;
use crate::providers::cloudflare::clients::ZoneCacheSettingsChangeOriginH2MaxStreamsSettingArgs;
use crate::providers::cloudflare::clients::ZoneCacheSettingsChangeOriginMaxHttpVersionSettingArgs;
use crate::providers::cloudflare::clients::ZoneCacheSettingsChangeOriginPostQuantumEncryptionSettingArgs;
use crate::providers::cloudflare::clients::ZoneCacheSettingsChangeRegionalTieredCacheSettingArgs;
use crate::providers::cloudflare::clients::ZoneCacheSettingsChangeVariantsSettingArgs;
use crate::providers::cloudflare::clients::ZoneCacheSettingsDeleteVariantsSettingArgs;
use crate::providers::cloudflare::clients::ZoneCacheSettingsGetAegisSettingArgs;
use crate::providers::cloudflare::clients::ZoneCacheSettingsGetCacheReserveClearArgs;
use crate::providers::cloudflare::clients::ZoneCacheSettingsGetCacheReserveSettingArgs;
use crate::providers::cloudflare::clients::ZoneCacheSettingsGetOriginH2MaxStreamsSettingArgs;
use crate::providers::cloudflare::clients::ZoneCacheSettingsGetOriginMaxHttpVersionSettingArgs;
use crate::providers::cloudflare::clients::ZoneCacheSettingsGetOriginPostQuantumEncryptionSettingArgs;
use crate::providers::cloudflare::clients::ZoneCacheSettingsGetRegionalTieredCacheSettingArgs;
use crate::providers::cloudflare::clients::ZoneCacheSettingsGetVariantsSettingArgs;
use crate::providers::cloudflare::clients::ZoneCacheSettingsStartCacheReserveClearArgs;
use crate::providers::cloudflare::clients::ZoneCloudConenctorRulesPutArgs;
use crate::providers::cloudflare::clients::ZoneCloudConnectorRulesArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessApplicationsAddABookmarkApplicationArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessApplicationsDeleteAnAccessApplicationArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessApplicationsGetAnAccessApplicationArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessApplicationsListAccessApplicationsArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessApplicationsPatchUpdateAccessApplicationSettingsArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessApplicationsPutUpdateAccessApplicationSettingsArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessApplicationsRevokeServiceTokensArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessApplicationsTestAccessPoliciesArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessApplicationsUpdateABookmarkApplicationArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessGroupsCreateAnAccessGroupArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessGroupsDeleteAnAccessGroupArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessGroupsGetAnAccessGroupArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessGroupsListAccessGroupsArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessGroupsUpdateAnAccessGroupArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessIdentityProvidersAddAnAccessIdentityProviderArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessIdentityProvidersDeleteAnAccessIdentityProviderArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessIdentityProvidersGetAnAccessIdentityProviderArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessIdentityProvidersListAccessIdentityProvidersArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessIdentityProvidersUpdateAnAccessIdentityProviderArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessMtlsAuthenticationAddAnMtlsCertificateArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessMtlsAuthenticationDeleteAnMtlsCertificateArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessMtlsAuthenticationGetAnMtlsCertificateArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessMtlsAuthenticationListMtlsCertificatesArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessMtlsAuthenticationListMtlsCertificatesHostnameSettingsArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessMtlsAuthenticationUpdateAnMtlsCertificateArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessMtlsAuthenticationUpdateAnMtlsCertificateSettingsArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessPoliciesCreateAnAccessPolicyArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessPoliciesDeleteAnAccessPolicyArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessPoliciesGetAnAccessPolicyArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessPoliciesListAccessPoliciesArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessPoliciesUpdateAnAccessPolicyArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessServiceTokensCreateAServiceTokenArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessServiceTokensDeleteAServiceTokenArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessServiceTokensGetAServiceTokenArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessServiceTokensListServiceTokensArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessServiceTokensUpdateAServiceTokenArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessShortLivedCertificateCAsCreateAShortLivedCertificateCaArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessShortLivedCertificateCAsDeleteAShortLivedCertificateCaArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessShortLivedCertificateCAsGetAShortLivedCertificateCaArgs;
use crate::providers::cloudflare::clients::ZoneLevelAccessShortLivedCertificateCAsListShortLivedCertificateCAsArgs;
use crate::providers::cloudflare::clients::ZoneLevelAuthenticatedOriginPullsDeleteCertificateArgs;
use crate::providers::cloudflare::clients::ZoneLevelAuthenticatedOriginPullsGetCertificateDetailsArgs;
use crate::providers::cloudflare::clients::ZoneLevelAuthenticatedOriginPullsGetEnablementSettingForZoneArgs;
use crate::providers::cloudflare::clients::ZoneLevelAuthenticatedOriginPullsListCertificatesArgs;
use crate::providers::cloudflare::clients::ZoneLevelAuthenticatedOriginPullsSetEnablementForZoneArgs;
use crate::providers::cloudflare::clients::ZoneLevelAuthenticatedOriginPullsUploadCertificateArgs;
use crate::providers::cloudflare::clients::ZoneLevelZeroTrustOrganizationCreateYourZeroTrustOrganizationArgs;
use crate::providers::cloudflare::clients::ZoneLevelZeroTrustOrganizationGetYourZeroTrustOrganizationArgs;
use crate::providers::cloudflare::clients::ZoneLevelZeroTrustOrganizationRevokeAllAccessTokensForAUserArgs;
use crate::providers::cloudflare::clients::ZoneLevelZeroTrustOrganizationUpdateYourZeroTrustOrganizationArgs;
use crate::providers::cloudflare::clients::ZoneLockdownCreateAZoneLockdownRuleArgs;
use crate::providers::cloudflare::clients::ZoneLockdownDeleteAZoneLockdownRuleArgs;
use crate::providers::cloudflare::clients::ZoneLockdownGetAZoneLockdownRuleArgs;
use crate::providers::cloudflare::clients::ZoneLockdownListZoneLockdownRulesArgs;
use crate::providers::cloudflare::clients::ZoneLockdownUpdateAZoneLockdownRuleArgs;
use crate::providers::cloudflare::clients::ZonePurgeArgs;
use crate::providers::cloudflare::clients::ZoneRatePlanAvailablePlanDetailsArgs;
use crate::providers::cloudflare::clients::ZoneRatePlanListAvailablePlansArgs;
use crate::providers::cloudflare::clients::ZoneRatePlanListAvailableRatePlansArgs;
use crate::providers::cloudflare::clients::ZoneSettingsChangeFontsSettingArgs;
use crate::providers::cloudflare::clients::ZoneSettingsChangeGoogleTagGatewayConfigArgs;
use crate::providers::cloudflare::clients::ZoneSettingsChangeSpeedBrainSettingArgs;
use crate::providers::cloudflare::clients::ZoneSettingsEditSingleSettingArgs;
use crate::providers::cloudflare::clients::ZoneSettingsEditZoneSettingsInfoArgs;
use crate::providers::cloudflare::clients::ZoneSettingsGetAllZoneSettingsArgs;
use crate::providers::cloudflare::clients::ZoneSettingsGetFontsSettingArgs;
use crate::providers::cloudflare::clients::ZoneSettingsGetGoogleTagGatewayConfigArgs;
use crate::providers::cloudflare::clients::ZoneSettingsGetSingleSettingArgs;
use crate::providers::cloudflare::clients::ZoneSettingsGetSpeedBrainSettingArgs;
use crate::providers::cloudflare::clients::ZoneSubscriptionCreateZoneSubscriptionArgs;
use crate::providers::cloudflare::clients::ZoneSubscriptionUpdateZoneSubscriptionArgs;
use crate::providers::cloudflare::clients::ZoneSubscriptionZoneSubscriptionDetailsArgs;
use crate::providers::cloudflare::clients::Zones0DeleteArgs;
use crate::providers::cloudflare::clients::Zones0GetArgs;
use crate::providers::cloudflare::clients::Zones0HoldDeleteArgs;
use crate::providers::cloudflare::clients::Zones0HoldGetArgs;
use crate::providers::cloudflare::clients::Zones0HoldPatchArgs;
use crate::providers::cloudflare::clients::Zones0HoldPostArgs;
use crate::providers::cloudflare::clients::Zones0PatchArgs;
use crate::providers::cloudflare::clients::ZonesEnvironmentsCreateArgs;
use crate::providers::cloudflare::clients::ZonesEnvironmentsDeleteArgs;
use crate::providers::cloudflare::clients::ZonesEnvironmentsEditArgs;
use crate::providers::cloudflare::clients::ZonesEnvironmentsListArgs;
use crate::providers::cloudflare::clients::ZonesEnvironmentsRollbackArgs;
use crate::providers::cloudflare::clients::ZonesEnvironmentsUpdateArgs;
use crate::providers::cloudflare::clients::ZonesGetArgs;
use crate::provider_client::{ProviderClient, ProviderError};
use foundation_core::valtron::{execute, StreamIterator};
use foundation_core::wire::simple_http::client::{SimpleHttpClient, DnsResolver};
use foundation_db::state::store_state_task::StoreStateIdentifierTask;
use std::sync::Arc;

/// CloudflareProvider with automatic state tracking.
///
/// # Type Parameters
///
/// * `S` - StateStore implementation (FileStateStore, SqliteStateStore, etc.)
/// * `R` - DNS resolver type for HTTP client
///
/// # Example
///
/// ```rust
/// let state_store = FileStateStore::new("/path", "my-project", "dev");
/// let http_client = SimpleHttpClient::with_resolver(StaticSocketAddr::new(addr));
/// let client = ProviderClient::new("my-project", "dev", state_store, http_client);
/// let provider = CloudflareProvider::from_provider_client(client);
/// ```
#[derive(Clone)]
pub struct CloudflareProvider<S, R>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + 'static,
{
    client: ProviderClient<S, R>,
    http_client: Arc<SimpleHttpClient<R>>,
}

impl<S, R> CloudflareProvider<S, R>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + 'static,
{
    /// Create new CloudflareProvider.
    pub fn new(client: ProviderClient<S, R>, http_client: Arc<SimpleHttpClient<R>>) -> Self {
        Self {
            client,
            http_client,
        }
    }

    /// Create new CloudflareProvider from ProviderClient, extracting the HTTP client.
    ///
    /// This is a convenience method that calls `Self::new()` with `client.http_client()`.
    pub fn from_provider_client(client: ProviderClient<S, R>) -> Self {
        Self::new(client, client.http_client.clone())
    }

    /// Accounts list accounts.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamResponseCollectionAccounts result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn accounts_list_accounts(
        &self,
        args: &AccountsListAccountsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamResponseCollectionAccounts, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = accounts_list_accounts_builder(
            &self.http_client,
            &args.name,
            &args.page,
            &args.per_page,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = accounts_list_accounts_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account creation.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamResponseSingleAccount result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_creation(
        &self,
        args: &AccountCreationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamResponseSingleAccount, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_creation_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = account_creation_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Accounts batch move accounts.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn accounts_batch_move_accounts(
        &self,
        args: &AccountsBatchMoveAccountsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = accounts_batch_move_accounts_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = accounts_batch_move_accounts_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get applications.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AlexandriaGetApplicationsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_applications(
        &self,
        args: &GetApplicationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AlexandriaGetApplicationsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_applications_builder(
            &self.http_client,
            &args.accountId,
            &args.filter,
            &args.limit,
            &args.offset,
            &args.order_by,
        )
        .map_err(ProviderError::Api)?;

        let task = get_applications_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create application.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AlexandriaGetApplicationResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_application(
        &self,
        args: &CreateApplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AlexandriaGetApplicationResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_application_builder(
            &self.http_client,
            &args.accountId,
        )
        .map_err(ProviderError::Api)?;

        let task = create_application_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get application by id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AlexandriaGetApplicationResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_application_by_id(
        &self,
        args: &GetApplicationByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AlexandriaGetApplicationResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_application_by_id_builder(
            &self.http_client,
            &args.accountId,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_application_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update application version.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AlexandriaGetApplicationResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_application_version(
        &self,
        args: &UpdateApplicationVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AlexandriaGetApplicationResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_application_version_builder(
            &self.http_client,
            &args.accountId,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_application_version_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get categories.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AlexandriaGetCategoriesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_categories(
        &self,
        args: &GetCategoriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AlexandriaGetCategoriesResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_categories_builder(
            &self.http_client,
            &args.accountId,
        )
        .map_err(ProviderError::Api)?;

        let task = get_categories_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get category by id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AlexandriaGetCategoryResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_category_by_id(
        &self,
        args: &GetCategoryByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AlexandriaGetCategoryResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_category_by_id_builder(
            &self.http_client,
            &args.accountId,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_category_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom pages for an account list custom pages.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomPagesCustomPageResultList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_pages_for_an_account_list_custom_pages(
        &self,
        args: &CustomPagesForAnAccountListCustomPagesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomPagesCustomPageResultList, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_pages_for_an_account_list_custom_pages_builder(
            &self.http_client,
            &args.account_identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_pages_for_an_account_list_custom_pages_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom assets for an account list custom assets.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomPagesCustomAssetResultList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_assets_for_an_account_list_custom_assets(
        &self,
        args: &CustomAssetsForAnAccountListCustomAssetsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomPagesCustomAssetResultList, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_assets_for_an_account_list_custom_assets_builder(
            &self.http_client,
            &args.account_identifier,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_assets_for_an_account_list_custom_assets_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom assets for an account create a custom asset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomPagesCustomAssetResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_assets_for_an_account_create_a_custom_asset(
        &self,
        args: &CustomAssetsForAnAccountCreateACustomAssetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomPagesCustomAssetResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_assets_for_an_account_create_a_custom_asset_builder(
            &self.http_client,
            &args.account_identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_assets_for_an_account_create_a_custom_asset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom assets for an account get a custom asset.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomPagesCustomAssetResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_assets_for_an_account_get_a_custom_asset(
        &self,
        args: &CustomAssetsForAnAccountGetACustomAssetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomPagesCustomAssetResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_assets_for_an_account_get_a_custom_asset_builder(
            &self.http_client,
            &args.asset_name,
            &args.account_identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_assets_for_an_account_get_a_custom_asset_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom assets for an account update a custom asset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomPagesCustomAssetResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_assets_for_an_account_update_a_custom_asset(
        &self,
        args: &CustomAssetsForAnAccountUpdateACustomAssetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomPagesCustomAssetResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_assets_for_an_account_update_a_custom_asset_builder(
            &self.http_client,
            &args.asset_name,
            &args.account_identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_assets_for_an_account_update_a_custom_asset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom assets for an account delete a custom asset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_assets_for_an_account_delete_a_custom_asset(
        &self,
        args: &CustomAssetsForAnAccountDeleteACustomAssetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_assets_for_an_account_delete_a_custom_asset_builder(
            &self.http_client,
            &args.asset_name,
            &args.account_identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_assets_for_an_account_delete_a_custom_asset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom pages for an account get a custom page.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomPagesCustomPageResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_pages_for_an_account_get_a_custom_page(
        &self,
        args: &CustomPagesForAnAccountGetACustomPageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomPagesCustomPageResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_pages_for_an_account_get_a_custom_page_builder(
            &self.http_client,
            &args.identifier,
            &args.account_identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_pages_for_an_account_get_a_custom_page_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom pages for an account update a custom page.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomPagesCustomPageResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_pages_for_an_account_update_a_custom_page(
        &self,
        args: &CustomPagesForAnAccountUpdateACustomPageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomPagesCustomPageResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_pages_for_an_account_update_a_custom_page_builder(
            &self.http_client,
            &args.identifier,
            &args.account_identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_pages_for_an_account_update_a_custom_page_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Accounts account details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamResponseSingleAccount result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn accounts_account_details(
        &self,
        args: &AccountsAccountDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamResponseSingleAccount, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = accounts_account_details_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = accounts_account_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Accounts update account.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamResponseSingleAccount result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn accounts_update_account(
        &self,
        args: &AccountsUpdateAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamResponseSingleAccount, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = accounts_update_account_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = accounts_update_account_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account deletion.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamApiResponseSingleId result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_deletion(
        &self,
        args: &AccountDeletionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamApiResponseSingleId, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_deletion_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_deletion_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List abuse reports.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_abuse_reports(
        &self,
        args: &ListAbuseReportsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_abuse_reports_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.sort,
            &args.domain,
            &args.created_before,
            &args.created_after,
            &args.status,
            &args.type_rs,
            &args.mitigation_status,
        )
        .map_err(ProviderError::Api)?;

        let task = list_abuse_reports_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List emails.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_emails(
        &self,
        args: &ListEmailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_emails_builder(
            &self.http_client,
            &args.account_id,
            &args.report_id,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = list_emails_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List mitigations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_mitigations(
        &self,
        args: &ListMitigationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_mitigations_builder(
            &self.http_client,
            &args.account_id,
            &args.report_id,
            &args.page,
            &args.per_page,
            &args.sort,
            &args.type_rs,
            &args.effective_before,
            &args.effective_after,
            &args.status,
            &args.entity_type,
        )
        .map_err(ProviderError::Api)?;

        let task = list_mitigations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Request review.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AbuseReportsMitigationAppealResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn request_review(
        &self,
        args: &RequestReviewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AbuseReportsMitigationAppealResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = request_review_builder(
            &self.http_client,
            &args.account_id,
            &args.report_id,
        )
        .map_err(ProviderError::Api)?;

        let task = request_review_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get abuse report.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_abuse_report(
        &self,
        args: &GetAbuseReportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_abuse_report_builder(
            &self.http_client,
            &args.account_id,
            &args.report_param,
        )
        .map_err(ProviderError::Api)?;

        let task = get_abuse_report_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Submit abuse report.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AbuseReportsSubmitReportResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn submit_abuse_report(
        &self,
        args: &SubmitAbuseReportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AbuseReportsSubmitReportResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = submit_abuse_report_builder(
            &self.http_client,
            &args.account_id,
            &args.report_param,
        )
        .map_err(ProviderError::Api)?;

        let task = submit_abuse_report_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mcp portals api list portals.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn mcp_portals_api_list_portals(
        &self,
        args: &McpPortalsApiListPortalsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mcp_portals_api_list_portals_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.search,
        )
        .map_err(ProviderError::Api)?;

        let task = mcp_portals_api_list_portals_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mcp portals api create portals.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn mcp_portals_api_create_portals(
        &self,
        args: &McpPortalsApiCreatePortalsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mcp_portals_api_create_portals_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = mcp_portals_api_create_portals_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mcp portals api fetch gateways.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn mcp_portals_api_fetch_gateways(
        &self,
        args: &McpPortalsApiFetchGatewaysArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mcp_portals_api_fetch_gateways_builder(
            &self.http_client,
            &args.id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = mcp_portals_api_fetch_gateways_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mcp portals api update portals.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn mcp_portals_api_update_portals(
        &self,
        args: &McpPortalsApiUpdatePortalsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mcp_portals_api_update_portals_builder(
            &self.http_client,
            &args.id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = mcp_portals_api_update_portals_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mcp portals api delete portals.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn mcp_portals_api_delete_portals(
        &self,
        args: &McpPortalsApiDeletePortalsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mcp_portals_api_delete_portals_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = mcp_portals_api_delete_portals_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mcp portals api list servers.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn mcp_portals_api_list_servers(
        &self,
        args: &McpPortalsApiListServersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mcp_portals_api_list_servers_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.search,
        )
        .map_err(ProviderError::Api)?;

        let task = mcp_portals_api_list_servers_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mcp portals api create servers.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn mcp_portals_api_create_servers(
        &self,
        args: &McpPortalsApiCreateServersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mcp_portals_api_create_servers_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = mcp_portals_api_create_servers_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mcp portals api fetch servers.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn mcp_portals_api_fetch_servers(
        &self,
        args: &McpPortalsApiFetchServersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mcp_portals_api_fetch_servers_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = mcp_portals_api_fetch_servers_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mcp portals api update servers.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn mcp_portals_api_update_servers(
        &self,
        args: &McpPortalsApiUpdateServersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mcp_portals_api_update_servers_builder(
            &self.http_client,
            &args.id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = mcp_portals_api_update_servers_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mcp portals api delete servers.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn mcp_portals_api_delete_servers(
        &self,
        args: &McpPortalsApiDeleteServersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mcp_portals_api_delete_servers_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = mcp_portals_api_delete_servers_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mcp portals api sync server.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn mcp_portals_api_sync_server(
        &self,
        args: &McpPortalsApiSyncServerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mcp_portals_api_sync_server_builder(
            &self.http_client,
            &args.id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = mcp_portals_api_sync_server_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access applications list access applications.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessAppsComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_applications_list_access_applications(
        &self,
        args: &AccessApplicationsListAccessApplicationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessAppsComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_applications_list_access_applications_builder(
            &self.http_client,
            &args.account_id,
            &args.name,
            &args.domain,
            &args.aud,
            &args.target_attributes,
            &args.exact,
            &args.search,
        )
        .map_err(ProviderError::Api)?;

        let task = access_applications_list_access_applications_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access applications add an application.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_applications_add_an_application(
        &self,
        args: &AccessApplicationsAddAnApplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_applications_add_an_application_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_applications_add_an_application_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access short lived certificate c as list short lived certificate c as.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessCaComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_short_lived_certificate_c_as_list_short_lived_certificate_c_as(
        &self,
        args: &AccessShortLivedCertificateCAsListShortLivedCertificateCAsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessCaComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_short_lived_certificate_c_as_list_short_lived_certificate_c_as_builder(
            &self.http_client,
            &args.account_id,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = access_short_lived_certificate_c_as_list_short_lived_certificate_c_as_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access applications get an access application.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessAppsComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_applications_get_an_access_application(
        &self,
        args: &AccessApplicationsGetAnAccessApplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessAppsComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_applications_get_an_access_application_builder(
            &self.http_client,
            &args.app_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_applications_get_an_access_application_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access applications update an access application.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_applications_update_an_access_application(
        &self,
        args: &AccessApplicationsUpdateAnAccessApplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_applications_update_an_access_application_builder(
            &self.http_client,
            &args.app_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_applications_update_an_access_application_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access applications delete an access application.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_applications_delete_an_access_application(
        &self,
        args: &AccessApplicationsDeleteAnAccessApplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_applications_delete_an_access_application_builder(
            &self.http_client,
            &args.app_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_applications_delete_an_access_application_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access short lived certificate c as get a short lived certificate ca.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessCaComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_short_lived_certificate_c_as_get_a_short_lived_certificate_ca(
        &self,
        args: &AccessShortLivedCertificateCAsGetAShortLivedCertificateCaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessCaComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_short_lived_certificate_c_as_get_a_short_lived_certificate_ca_builder(
            &self.http_client,
            &args.app_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_short_lived_certificate_c_as_get_a_short_lived_certificate_ca_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access short lived certificate c as create a short lived certificate ca.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessCaComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_short_lived_certificate_c_as_create_a_short_lived_certificate_ca(
        &self,
        args: &AccessShortLivedCertificateCAsCreateAShortLivedCertificateCaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessCaComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_short_lived_certificate_c_as_create_a_short_lived_certificate_ca_builder(
            &self.http_client,
            &args.app_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_short_lived_certificate_c_as_create_a_short_lived_certificate_ca_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access short lived certificate c as delete a short lived certificate ca.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSchemasIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_short_lived_certificate_c_as_delete_a_short_lived_certificate_ca(
        &self,
        args: &AccessShortLivedCertificateCAsDeleteAShortLivedCertificateCaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSchemasIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_short_lived_certificate_c_as_delete_a_short_lived_certificate_ca_builder(
            &self.http_client,
            &args.app_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_short_lived_certificate_c_as_delete_a_short_lived_certificate_ca_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access policies list access app policies.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessAppPoliciesComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_policies_list_access_app_policies(
        &self,
        args: &AccessPoliciesListAccessAppPoliciesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessAppPoliciesComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_policies_list_access_app_policies_builder(
            &self.http_client,
            &args.app_id,
            &args.account_id,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = access_policies_list_access_app_policies_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access policies create an access policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessAppPoliciesComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_policies_create_an_access_policy(
        &self,
        args: &AccessPoliciesCreateAnAccessPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessAppPoliciesComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_policies_create_an_access_policy_builder(
            &self.http_client,
            &args.app_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_policies_create_an_access_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access policies get an access policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessAppPoliciesComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_policies_get_an_access_policy(
        &self,
        args: &AccessPoliciesGetAnAccessPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessAppPoliciesComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_policies_get_an_access_policy_builder(
            &self.http_client,
            &args.app_id,
            &args.policy_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_policies_get_an_access_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access policies update an access policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessAppPoliciesComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_policies_update_an_access_policy(
        &self,
        args: &AccessPoliciesUpdateAnAccessPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessAppPoliciesComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_policies_update_an_access_policy_builder(
            &self.http_client,
            &args.app_id,
            &args.policy_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_policies_update_an_access_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access policies delete an access policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessAppPoliciesComponentsSchemasIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_policies_delete_an_access_policy(
        &self,
        args: &AccessPoliciesDeleteAnAccessPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessAppPoliciesComponentsSchemasIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_policies_delete_an_access_policy_builder(
            &self.http_client,
            &args.app_id,
            &args.policy_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_policies_delete_an_access_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access policies convert reusable.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessAppPoliciesComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_policies_convert_reusable(
        &self,
        args: &AccessPoliciesConvertReusableArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessAppPoliciesComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_policies_convert_reusable_builder(
            &self.http_client,
            &args.app_id,
            &args.policy_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_policies_convert_reusable_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access applications revoke service tokens.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSchemasEmptyResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_applications_revoke_service_tokens(
        &self,
        args: &AccessApplicationsRevokeServiceTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSchemasEmptyResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_applications_revoke_service_tokens_builder(
            &self.http_client,
            &args.app_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_applications_revoke_service_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access applications put update access application settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_applications_put_update_access_application_settings(
        &self,
        args: &AccessApplicationsPutUpdateAccessApplicationSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_applications_put_update_access_application_settings_builder(
            &self.http_client,
            &args.app_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_applications_put_update_access_application_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access applications patch update access application settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_applications_patch_update_access_application_settings(
        &self,
        args: &AccessApplicationsPatchUpdateAccessApplicationSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_applications_patch_update_access_application_settings_builder(
            &self.http_client,
            &args.app_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_applications_patch_update_access_application_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access applications test access policies.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessPolicyCheckResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_applications_test_access_policies(
        &self,
        args: &AccessApplicationsTestAccessPoliciesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessPolicyCheckResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_applications_test_access_policies_builder(
            &self.http_client,
            &args.app_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_applications_test_access_policies_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access bookmark applications deprecated list bookmark applications.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessBookmarksComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_bookmark_applications_deprecated_list_bookmark_applications(
        &self,
        args: &AccessBookmarkApplicationsDeprecatedListBookmarkApplicationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessBookmarksComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_bookmark_applications_deprecated_list_bookmark_applications_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_bookmark_applications_deprecated_list_bookmark_applications_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access bookmark applications deprecated get a bookmark application.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessBookmarksComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_bookmark_applications_deprecated_get_a_bookmark_application(
        &self,
        args: &AccessBookmarkApplicationsDeprecatedGetABookmarkApplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessBookmarksComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_bookmark_applications_deprecated_get_a_bookmark_application_builder(
            &self.http_client,
            &args.bookmark_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_bookmark_applications_deprecated_get_a_bookmark_application_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access bookmark applications deprecated create a bookmark application.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessBookmarksComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_bookmark_applications_deprecated_create_a_bookmark_application(
        &self,
        args: &AccessBookmarkApplicationsDeprecatedCreateABookmarkApplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessBookmarksComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_bookmark_applications_deprecated_create_a_bookmark_application_builder(
            &self.http_client,
            &args.bookmark_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_bookmark_applications_deprecated_create_a_bookmark_application_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access bookmark applications deprecated update a bookmark application.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessBookmarksComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_bookmark_applications_deprecated_update_a_bookmark_application(
        &self,
        args: &AccessBookmarkApplicationsDeprecatedUpdateABookmarkApplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessBookmarksComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_bookmark_applications_deprecated_update_a_bookmark_application_builder(
            &self.http_client,
            &args.bookmark_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_bookmark_applications_deprecated_update_a_bookmark_application_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access bookmark applications deprecated delete a bookmark application.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_bookmark_applications_deprecated_delete_a_bookmark_application(
        &self,
        args: &AccessBookmarkApplicationsDeprecatedDeleteABookmarkApplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_bookmark_applications_deprecated_delete_a_bookmark_application_builder(
            &self.http_client,
            &args.bookmark_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_bookmark_applications_deprecated_delete_a_bookmark_application_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access mtls authentication list mtls certificates.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessCertificatesComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_mtls_authentication_list_mtls_certificates(
        &self,
        args: &AccessMtlsAuthenticationListMtlsCertificatesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessCertificatesComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_mtls_authentication_list_mtls_certificates_builder(
            &self.http_client,
            &args.account_id,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = access_mtls_authentication_list_mtls_certificates_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access mtls authentication add an mtls certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessCertificatesComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_mtls_authentication_add_an_mtls_certificate(
        &self,
        args: &AccessMtlsAuthenticationAddAnMtlsCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessCertificatesComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_mtls_authentication_add_an_mtls_certificate_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_mtls_authentication_add_an_mtls_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access mtls authentication list mtls certificates hostname settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessResponseCollectionHostnames result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_mtls_authentication_list_mtls_certificates_hostname_settings(
        &self,
        args: &AccessMtlsAuthenticationListMtlsCertificatesHostnameSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessResponseCollectionHostnames, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_mtls_authentication_list_mtls_certificates_hostname_settings_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_mtls_authentication_list_mtls_certificates_hostname_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access mtls authentication update an mtls certificate settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessResponseCollectionHostnames result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_mtls_authentication_update_an_mtls_certificate_settings(
        &self,
        args: &AccessMtlsAuthenticationUpdateAnMtlsCertificateSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessResponseCollectionHostnames, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_mtls_authentication_update_an_mtls_certificate_settings_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_mtls_authentication_update_an_mtls_certificate_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access mtls authentication get an mtls certificate.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessCertificatesComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_mtls_authentication_get_an_mtls_certificate(
        &self,
        args: &AccessMtlsAuthenticationGetAnMtlsCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessCertificatesComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_mtls_authentication_get_an_mtls_certificate_builder(
            &self.http_client,
            &args.certificate_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_mtls_authentication_get_an_mtls_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access mtls authentication update an mtls certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessCertificatesComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_mtls_authentication_update_an_mtls_certificate(
        &self,
        args: &AccessMtlsAuthenticationUpdateAnMtlsCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessCertificatesComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_mtls_authentication_update_an_mtls_certificate_builder(
            &self.http_client,
            &args.certificate_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_mtls_authentication_update_an_mtls_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access mtls authentication delete an mtls certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessComponentsSchemasIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_mtls_authentication_delete_an_mtls_certificate(
        &self,
        args: &AccessMtlsAuthenticationDeleteAnMtlsCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessComponentsSchemasIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_mtls_authentication_delete_an_mtls_certificate_builder(
            &self.http_client,
            &args.certificate_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_mtls_authentication_delete_an_mtls_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access custom pages list custom pages.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessCustomPagesComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_custom_pages_list_custom_pages(
        &self,
        args: &AccessCustomPagesListCustomPagesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessCustomPagesComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_custom_pages_list_custom_pages_builder(
            &self.http_client,
            &args.account_id,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = access_custom_pages_list_custom_pages_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access custom pages create a custom page.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSingleResponseWithoutHtml result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_custom_pages_create_a_custom_page(
        &self,
        args: &AccessCustomPagesCreateACustomPageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSingleResponseWithoutHtml, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_custom_pages_create_a_custom_page_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_custom_pages_create_a_custom_page_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access custom pages get a custom page.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessCustomPagesComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_custom_pages_get_a_custom_page(
        &self,
        args: &AccessCustomPagesGetACustomPageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessCustomPagesComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_custom_pages_get_a_custom_page_builder(
            &self.http_client,
            &args.custom_page_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_custom_pages_get_a_custom_page_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access custom pages update a custom page.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSingleResponseWithoutHtml result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_custom_pages_update_a_custom_page(
        &self,
        args: &AccessCustomPagesUpdateACustomPageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSingleResponseWithoutHtml, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_custom_pages_update_a_custom_page_builder(
            &self.http_client,
            &args.custom_page_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_custom_pages_update_a_custom_page_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access custom pages delete a custom page.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessComponentsSchemasIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_custom_pages_delete_a_custom_page(
        &self,
        args: &AccessCustomPagesDeleteACustomPageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessComponentsSchemasIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_custom_pages_delete_a_custom_page_builder(
            &self.http_client,
            &args.custom_page_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_custom_pages_delete_a_custom_page_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access gateway ca list ssh ca.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessGatewayCaComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_gateway_ca_list_ssh_ca(
        &self,
        args: &AccessGatewayCaListSshCaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessGatewayCaComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_gateway_ca_list_ssh_ca_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_gateway_ca_list_ssh_ca_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access gateway ca add an ssh ca.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessGatewayCaComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_gateway_ca_add_an_ssh_ca(
        &self,
        args: &AccessGatewayCaAddAnSshCaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessGatewayCaComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_gateway_ca_add_an_ssh_ca_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_gateway_ca_add_an_ssh_ca_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access gateway ca delete an ssh ca.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_gateway_ca_delete_an_ssh_ca(
        &self,
        args: &AccessGatewayCaDeleteAnSshCaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_gateway_ca_delete_an_ssh_ca_builder(
            &self.http_client,
            &args.certificate_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_gateway_ca_delete_an_ssh_ca_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access groups list access groups.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_groups_list_access_groups(
        &self,
        args: &AccessGroupsListAccessGroupsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_groups_list_access_groups_builder(
            &self.http_client,
            &args.account_id,
            &args.name,
            &args.search,
        )
        .map_err(ProviderError::Api)?;

        let task = access_groups_list_access_groups_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access groups create an access group.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessGroupsComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_groups_create_an_access_group(
        &self,
        args: &AccessGroupsCreateAnAccessGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessGroupsComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_groups_create_an_access_group_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_groups_create_an_access_group_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access groups get an access group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessGroupsComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_groups_get_an_access_group(
        &self,
        args: &AccessGroupsGetAnAccessGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessGroupsComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_groups_get_an_access_group_builder(
            &self.http_client,
            &args.group_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_groups_get_an_access_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access groups update an access group.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessGroupsComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_groups_update_an_access_group(
        &self,
        args: &AccessGroupsUpdateAnAccessGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessGroupsComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_groups_update_an_access_group_builder(
            &self.http_client,
            &args.group_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_groups_update_an_access_group_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access groups delete an access group.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_groups_delete_an_access_group(
        &self,
        args: &AccessGroupsDeleteAnAccessGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_groups_delete_an_access_group_builder(
            &self.http_client,
            &args.group_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_groups_delete_an_access_group_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access identity providers list access identity providers.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_identity_providers_list_access_identity_providers(
        &self,
        args: &AccessIdentityProvidersListAccessIdentityProvidersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_identity_providers_list_access_identity_providers_builder(
            &self.http_client,
            &args.account_id,
            &args.scim_enabled,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = access_identity_providers_list_access_identity_providers_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access identity providers add an access identity provider.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_identity_providers_add_an_access_identity_provider(
        &self,
        args: &AccessIdentityProvidersAddAnAccessIdentityProviderArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_identity_providers_add_an_access_identity_provider_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_identity_providers_add_an_access_identity_provider_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access identity providers get an access identity provider.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_identity_providers_get_an_access_identity_provider(
        &self,
        args: &AccessIdentityProvidersGetAnAccessIdentityProviderArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_identity_providers_get_an_access_identity_provider_builder(
            &self.http_client,
            &args.identity_provider_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_identity_providers_get_an_access_identity_provider_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access identity providers update an access identity provider.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_identity_providers_update_an_access_identity_provider(
        &self,
        args: &AccessIdentityProvidersUpdateAnAccessIdentityProviderArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_identity_providers_update_an_access_identity_provider_builder(
            &self.http_client,
            &args.identity_provider_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_identity_providers_update_an_access_identity_provider_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access identity providers delete an access identity provider.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_identity_providers_delete_an_access_identity_provider(
        &self,
        args: &AccessIdentityProvidersDeleteAnAccessIdentityProviderArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_identity_providers_delete_an_access_identity_provider_builder(
            &self.http_client,
            &args.identity_provider_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_identity_providers_delete_an_access_identity_provider_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access identity providers list scim group resources.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessScimGroupsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_identity_providers_list_scim_group_resources(
        &self,
        args: &AccessIdentityProvidersListScimGroupResourcesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessScimGroupsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_identity_providers_list_scim_group_resources_builder(
            &self.http_client,
            &args.identity_provider_id,
            &args.account_id,
            &args.cf_resource_id,
            &args.idp_resource_id,
            &args.name,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = access_identity_providers_list_scim_group_resources_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access identity providers list scim user resources.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessScimUsersResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_identity_providers_list_scim_user_resources(
        &self,
        args: &AccessIdentityProvidersListScimUserResourcesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessScimUsersResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_identity_providers_list_scim_user_resources_builder(
            &self.http_client,
            &args.identity_provider_id,
            &args.account_id,
            &args.cf_resource_id,
            &args.idp_resource_id,
            &args.username,
            &args.email,
            &args.name,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = access_identity_providers_list_scim_user_resources_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access key configuration get the access key configuration.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessKeysComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_key_configuration_get_the_access_key_configuration(
        &self,
        args: &AccessKeyConfigurationGetTheAccessKeyConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessKeysComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_key_configuration_get_the_access_key_configuration_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_key_configuration_get_the_access_key_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access key configuration update the access key configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessKeysComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_key_configuration_update_the_access_key_configuration(
        &self,
        args: &AccessKeyConfigurationUpdateTheAccessKeyConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessKeysComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_key_configuration_update_the_access_key_configuration_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_key_configuration_update_the_access_key_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access key configuration rotate access keys.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessKeysComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_key_configuration_rotate_access_keys(
        &self,
        args: &AccessKeyConfigurationRotateAccessKeysArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessKeysComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_key_configuration_rotate_access_keys_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_key_configuration_rotate_access_keys_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access authentication logs get access authentication logs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessAccessRequestsComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_authentication_logs_get_access_authentication_logs(
        &self,
        args: &AccessAuthenticationLogsGetAccessAuthenticationLogsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessAccessRequestsComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_authentication_logs_get_access_authentication_logs_builder(
            &self.http_client,
            &args.account_id,
            &args.limit,
            &args.direction,
            &args.since,
            &args.until,
            &args.per_page,
            &args.email,
            &args.email_exact,
            &args.user_id,
            &args.allowedOp,
            &args.country_codeOp,
            &args.app_typeOp,
            &args.app_uidOp,
            &args.ray_idOp,
            &args.emailOp,
            &args.idpOp,
            &args.non_identityOp,
            &args.user_idOp,
            &args.fields,
        )
        .map_err(ProviderError::Api)?;

        let task = access_authentication_logs_get_access_authentication_logs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access scim update logs list access scim update logs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessScimUpdateLogsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_scim_update_logs_list_access_scim_update_logs(
        &self,
        args: &AccessScimUpdateLogsListAccessScimUpdateLogsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessScimUpdateLogsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_scim_update_logs_list_access_scim_update_logs_builder(
            &self.http_client,
            &args.account_id,
            &args.limit,
            &args.direction,
            &args.since,
            &args.until,
            &args.idp_id,
            &args.status,
            &args.resource_type,
            &args.request_method,
            &args.resource_user_email,
            &args.resource_group_name,
            &args.cf_resource_id,
            &args.idp_resource_id,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = access_scim_update_logs_list_access_scim_update_logs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust organization get your zero trust organization.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_organization_get_your_zero_trust_organization(
        &self,
        args: &ZeroTrustOrganizationGetYourZeroTrustOrganizationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_organization_get_your_zero_trust_organization_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_organization_get_your_zero_trust_organization_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust organization create your zero trust organization.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_organization_create_your_zero_trust_organization(
        &self,
        args: &ZeroTrustOrganizationCreateYourZeroTrustOrganizationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_organization_create_your_zero_trust_organization_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_organization_create_your_zero_trust_organization_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust organization update your zero trust organization.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_organization_update_your_zero_trust_organization(
        &self,
        args: &ZeroTrustOrganizationUpdateYourZeroTrustOrganizationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_organization_update_your_zero_trust_organization_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_organization_update_your_zero_trust_organization_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust organization get your zero trust organization doh settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_organization_get_your_zero_trust_organization_doh_settings(
        &self,
        args: &ZeroTrustOrganizationGetYourZeroTrustOrganizationDohSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_organization_get_your_zero_trust_organization_doh_settings_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_organization_get_your_zero_trust_organization_doh_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust organization update your zero trust organization doh settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_organization_update_your_zero_trust_organization_doh_settings(
        &self,
        args: &ZeroTrustOrganizationUpdateYourZeroTrustOrganizationDohSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_organization_update_your_zero_trust_organization_doh_settings_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_organization_update_your_zero_trust_organization_doh_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust organization revoke all access tokens for a user.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessEmptyResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_organization_revoke_all_access_tokens_for_a_user(
        &self,
        args: &ZeroTrustOrganizationRevokeAllAccessTokensForAUserArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessEmptyResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_organization_revoke_all_access_tokens_for_a_user_builder(
            &self.http_client,
            &args.account_id,
            &args.devices,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_organization_revoke_all_access_tokens_for_a_user_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access policies list access reusable policies.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessReusablePoliciesComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_policies_list_access_reusable_policies(
        &self,
        args: &AccessPoliciesListAccessReusablePoliciesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessReusablePoliciesComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_policies_list_access_reusable_policies_builder(
            &self.http_client,
            &args.account_id,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = access_policies_list_access_reusable_policies_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access policies create an access reusable policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessReusablePoliciesComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_policies_create_an_access_reusable_policy(
        &self,
        args: &AccessPoliciesCreateAnAccessReusablePolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessReusablePoliciesComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_policies_create_an_access_reusable_policy_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_policies_create_an_access_reusable_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access policies get an access reusable policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessReusablePoliciesComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_policies_get_an_access_reusable_policy(
        &self,
        args: &AccessPoliciesGetAnAccessReusablePolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessReusablePoliciesComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_policies_get_an_access_reusable_policy_builder(
            &self.http_client,
            &args.account_id,
            &args.policy_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_policies_get_an_access_reusable_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access policies update an access reusable policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessReusablePoliciesComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_policies_update_an_access_reusable_policy(
        &self,
        args: &AccessPoliciesUpdateAnAccessReusablePolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessReusablePoliciesComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_policies_update_an_access_reusable_policy_builder(
            &self.http_client,
            &args.account_id,
            &args.policy_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_policies_update_an_access_reusable_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access policies delete an access reusable policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessReusablePoliciesComponentsSchemasIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_policies_delete_an_access_reusable_policy(
        &self,
        args: &AccessPoliciesDeleteAnAccessReusablePolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessReusablePoliciesComponentsSchemasIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_policies_delete_an_access_reusable_policy_builder(
            &self.http_client,
            &args.account_id,
            &args.policy_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_policies_delete_an_access_reusable_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access policy tests.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessPolicyInitResp result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_policy_tests(
        &self,
        args: &AccessPolicyTestsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessPolicyInitResp, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_policy_tests_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_policy_tests_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access policy tests get an update.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessPolicyUpdateResp result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_policy_tests_get_an_update(
        &self,
        args: &AccessPolicyTestsGetAnUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessPolicyUpdateResp, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_policy_tests_get_an_update_builder(
            &self.http_client,
            &args.account_id,
            &args.policy_test_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_policy_tests_get_an_update_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access policy tests get a user page.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessPolicyUsersResp result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_policy_tests_get_a_user_page(
        &self,
        args: &AccessPolicyTestsGetAUserPageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessPolicyUsersResp, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_policy_tests_get_a_user_page_builder(
            &self.http_client,
            &args.account_id,
            &args.policy_test_id,
            &args.per_page,
            &args.status,
        )
        .map_err(ProviderError::Api)?;

        let task = access_policy_tests_get_a_user_page_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust seats update a user seat.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSeatsComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_seats_update_a_user_seat(
        &self,
        args: &ZeroTrustSeatsUpdateAUserSeatArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSeatsComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_seats_update_a_user_seat_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_seats_update_a_user_seat_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access service tokens list service tokens.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_service_tokens_list_service_tokens(
        &self,
        args: &AccessServiceTokensListServiceTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_service_tokens_list_service_tokens_builder(
            &self.http_client,
            &args.account_id,
            &args.name,
            &args.search,
        )
        .map_err(ProviderError::Api)?;

        let task = access_service_tokens_list_service_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access service tokens create a service token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessCreateResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_service_tokens_create_a_service_token(
        &self,
        args: &AccessServiceTokensCreateAServiceTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessCreateResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_service_tokens_create_a_service_token_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_service_tokens_create_a_service_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access service tokens get a service token.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_service_tokens_get_a_service_token(
        &self,
        args: &AccessServiceTokensGetAServiceTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_service_tokens_get_a_service_token_builder(
            &self.http_client,
            &args.service_token_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_service_tokens_get_a_service_token_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access service tokens update a service token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_service_tokens_update_a_service_token(
        &self,
        args: &AccessServiceTokensUpdateAServiceTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_service_tokens_update_a_service_token_builder(
            &self.http_client,
            &args.service_token_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_service_tokens_update_a_service_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access service tokens delete a service token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_service_tokens_delete_a_service_token(
        &self,
        args: &AccessServiceTokensDeleteAServiceTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_service_tokens_delete_a_service_token_builder(
            &self.http_client,
            &args.service_token_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_service_tokens_delete_a_service_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access service tokens refresh a service token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_service_tokens_refresh_a_service_token(
        &self,
        args: &AccessServiceTokensRefreshAServiceTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_service_tokens_refresh_a_service_token_builder(
            &self.http_client,
            &args.service_token_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_service_tokens_refresh_a_service_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access service tokens rotate a service token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessCreateResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_service_tokens_rotate_a_service_token(
        &self,
        args: &AccessServiceTokensRotateAServiceTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessCreateResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_service_tokens_rotate_a_service_token_builder(
            &self.http_client,
            &args.service_token_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_service_tokens_rotate_a_service_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access tags list tags.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessTagsComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_tags_list_tags(
        &self,
        args: &AccessTagsListTagsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessTagsComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_tags_list_tags_builder(
            &self.http_client,
            &args.account_id,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = access_tags_list_tags_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access tags create tag.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessTagsComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_tags_create_tag(
        &self,
        args: &AccessTagsCreateTagArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessTagsComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_tags_create_tag_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = access_tags_create_tag_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access tags get a tag.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessTagsComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn access_tags_get_a_tag(
        &self,
        args: &AccessTagsGetATagArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessTagsComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_tags_get_a_tag_builder(
            &self.http_client,
            &args.account_id,
            &args.tag_name,
        )
        .map_err(ProviderError::Api)?;

        let task = access_tags_get_a_tag_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access tags update a tag.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessTagsComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_tags_update_a_tag(
        &self,
        args: &AccessTagsUpdateATagArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessTagsComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_tags_update_a_tag_builder(
            &self.http_client,
            &args.account_id,
            &args.tag_name,
        )
        .map_err(ProviderError::Api)?;

        let task = access_tags_update_a_tag_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Access tags delete a tag.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessNameResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn access_tags_delete_a_tag(
        &self,
        args: &AccessTagsDeleteATagArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessNameResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = access_tags_delete_a_tag_builder(
            &self.http_client,
            &args.account_id,
            &args.tag_name,
        )
        .map_err(ProviderError::Api)?;

        let task = access_tags_delete_a_tag_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust users get users.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessUsersComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_users_get_users(
        &self,
        args: &ZeroTrustUsersGetUsersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessUsersComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_users_get_users_builder(
            &self.http_client,
            &args.account_id,
            &args.name,
            &args.email,
            &args.search,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_users_get_users_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust users create user.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSingleUserResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_users_create_user(
        &self,
        args: &ZeroTrustUsersCreateUserArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSingleUserResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_users_create_user_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_users_create_user_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust users get user.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSingleUserResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_users_get_user(
        &self,
        args: &ZeroTrustUsersGetUserArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSingleUserResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_users_get_user_builder(
            &self.http_client,
            &args.user_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_users_get_user_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust users update user.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSingleUserResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_users_update_user(
        &self,
        args: &ZeroTrustUsersUpdateUserArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSingleUserResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_users_update_user_builder(
            &self.http_client,
            &args.user_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_users_update_user_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust users delete user.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessDeleteUserResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_users_delete_user(
        &self,
        args: &ZeroTrustUsersDeleteUserArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessDeleteUserResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_users_delete_user_builder(
            &self.http_client,
            &args.user_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_users_delete_user_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust users get active sessions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessActiveSessionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_users_get_active_sessions(
        &self,
        args: &ZeroTrustUsersGetActiveSessionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessActiveSessionsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_users_get_active_sessions_builder(
            &self.http_client,
            &args.user_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_users_get_active_sessions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust users get active session.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessActiveSessionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_users_get_active_session(
        &self,
        args: &ZeroTrustUsersGetActiveSessionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessActiveSessionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_users_get_active_session_builder(
            &self.http_client,
            &args.user_id,
            &args.account_id,
            &args.nonce,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_users_get_active_session_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust users get failed logins.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessFailedLoginResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_users_get_failed_logins(
        &self,
        args: &ZeroTrustUsersGetFailedLoginsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessFailedLoginResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_users_get_failed_logins_builder(
            &self.http_client,
            &args.user_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_users_get_failed_logins_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust users get last seen identity.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessLastSeenIdentityResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_users_get_last_seen_identity(
        &self,
        args: &ZeroTrustUsersGetLastSeenIdentityArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessLastSeenIdentityResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_users_get_last_seen_identity_builder(
            &self.http_client,
            &args.user_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_users_get_last_seen_identity_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust users delete mfa authenticator.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessDeleteAuthenticatorResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_users_delete_mfa_authenticator(
        &self,
        args: &ZeroTrustUsersDeleteMfaAuthenticatorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessDeleteAuthenticatorResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_users_delete_mfa_authenticator_builder(
            &self.http_client,
            &args.user_id,
            &args.account_id,
            &args.authenticator_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_users_delete_mfa_authenticator_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management address maps list address maps.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_address_maps_list_address_maps(
        &self,
        args: &IpAddressManagementAddressMapsListAddressMapsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_address_maps_list_address_maps_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_address_maps_list_address_maps_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management address maps create address map.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingFullResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_address_maps_create_address_map(
        &self,
        args: &IpAddressManagementAddressMapsCreateAddressMapArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingFullResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_address_maps_create_address_map_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_address_maps_create_address_map_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management address maps address map details.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingFullResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_address_maps_address_map_details(
        &self,
        args: &IpAddressManagementAddressMapsAddressMapDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingFullResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_address_maps_address_map_details_builder(
            &self.http_client,
            &args.address_map_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_address_maps_address_map_details_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management address maps update address map.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_address_maps_update_address_map(
        &self,
        args: &IpAddressManagementAddressMapsUpdateAddressMapArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_address_maps_update_address_map_builder(
            &self.http_client,
            &args.address_map_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_address_maps_update_address_map_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management address maps delete address map.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingApiResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_address_maps_delete_address_map(
        &self,
        args: &IpAddressManagementAddressMapsDeleteAddressMapArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingApiResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_address_maps_delete_address_map_builder(
            &self.http_client,
            &args.address_map_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_address_maps_delete_address_map_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management address maps add an account membership to an address map.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingApiResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_address_maps_add_an_account_membership_to_an_address_map(
        &self,
        args: &IpAddressManagementAddressMapsAddAnAccountMembershipToAnAddressMapArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingApiResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_address_maps_add_an_account_membership_to_an_address_map_builder(
            &self.http_client,
            &args.account_id,
            &args.address_map_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_address_maps_add_an_account_membership_to_an_address_map_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management address maps remove an account membership from an address map.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingApiResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_address_maps_remove_an_account_membership_from_an_address_map(
        &self,
        args: &IpAddressManagementAddressMapsRemoveAnAccountMembershipFromAnAddressMapArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingApiResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_address_maps_remove_an_account_membership_from_an_address_map_builder(
            &self.http_client,
            &args.account_id,
            &args.address_map_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_address_maps_remove_an_account_membership_from_an_address_map_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management address maps add an ip to an address map.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingApiResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_address_maps_add_an_ip_to_an_address_map(
        &self,
        args: &IpAddressManagementAddressMapsAddAnIpToAnAddressMapArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingApiResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_address_maps_add_an_ip_to_an_address_map_builder(
            &self.http_client,
            &args.ip_address,
            &args.address_map_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_address_maps_add_an_ip_to_an_address_map_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management address maps remove an ip from an address map.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingApiResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_address_maps_remove_an_ip_from_an_address_map(
        &self,
        args: &IpAddressManagementAddressMapsRemoveAnIpFromAnAddressMapArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingApiResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_address_maps_remove_an_ip_from_an_address_map_builder(
            &self.http_client,
            &args.ip_address,
            &args.address_map_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_address_maps_remove_an_ip_from_an_address_map_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management address maps add a zone membership to an address map.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingApiResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_address_maps_add_a_zone_membership_to_an_address_map(
        &self,
        args: &IpAddressManagementAddressMapsAddAZoneMembershipToAnAddressMapArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingApiResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_address_maps_add_a_zone_membership_to_an_address_map_builder(
            &self.http_client,
            &args.zone_id,
            &args.address_map_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_address_maps_add_a_zone_membership_to_an_address_map_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management address maps remove a zone membership from an address map.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingApiResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_address_maps_remove_a_zone_membership_from_an_address_map(
        &self,
        args: &IpAddressManagementAddressMapsRemoveAZoneMembershipFromAnAddressMapArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingApiResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_address_maps_remove_a_zone_membership_from_an_address_map_builder(
            &self.http_client,
            &args.zone_id,
            &args.address_map_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_address_maps_remove_a_zone_membership_from_an_address_map_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management list leases.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingLeasesComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_list_leases(
        &self,
        args: &IpAddressManagementListLeasesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingLeasesComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_list_leases_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_list_leases_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management prefixes upload loa document.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingLoaUploadResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_prefixes_upload_loa_document(
        &self,
        args: &IpAddressManagementPrefixesUploadLoaDocumentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingLoaUploadResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_prefixes_upload_loa_document_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_prefixes_upload_loa_document_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management prefixes download loa document.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_prefixes_download_loa_document(
        &self,
        args: &IpAddressManagementPrefixesDownloadLoaDocumentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_prefixes_download_loa_document_builder(
            &self.http_client,
            &args.loa_document_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_prefixes_download_loa_document_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management prefixes list prefixes.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_prefixes_list_prefixes(
        &self,
        args: &IpAddressManagementPrefixesListPrefixesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_prefixes_list_prefixes_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_prefixes_list_prefixes_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management prefixes add prefix.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_prefixes_add_prefix(
        &self,
        args: &IpAddressManagementPrefixesAddPrefixArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_prefixes_add_prefix_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_prefixes_add_prefix_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management prefixes prefix details.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_prefixes_prefix_details(
        &self,
        args: &IpAddressManagementPrefixesPrefixDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_prefixes_prefix_details_builder(
            &self.http_client,
            &args.prefix_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_prefixes_prefix_details_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management prefixes update prefix description.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_prefixes_update_prefix_description(
        &self,
        args: &IpAddressManagementPrefixesUpdatePrefixDescriptionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_prefixes_update_prefix_description_builder(
            &self.http_client,
            &args.prefix_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_prefixes_update_prefix_description_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management prefixes delete prefix.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingApiResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_prefixes_delete_prefix(
        &self,
        args: &IpAddressManagementPrefixesDeletePrefixArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingApiResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_prefixes_delete_prefix_builder(
            &self.http_client,
            &args.prefix_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_prefixes_delete_prefix_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management prefixes list bgp prefixes.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingResponseCollectionBgp result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_prefixes_list_bgp_prefixes(
        &self,
        args: &IpAddressManagementPrefixesListBgpPrefixesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingResponseCollectionBgp, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_prefixes_list_bgp_prefixes_builder(
            &self.http_client,
            &args.account_id,
            &args.prefix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_prefixes_list_bgp_prefixes_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management prefixes create bgp prefix.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingSingleResponseBgp result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_prefixes_create_bgp_prefix(
        &self,
        args: &IpAddressManagementPrefixesCreateBgpPrefixArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingSingleResponseBgp, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_prefixes_create_bgp_prefix_builder(
            &self.http_client,
            &args.account_id,
            &args.prefix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_prefixes_create_bgp_prefix_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management prefixes fetch bgp prefix.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingSingleResponseBgp result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_prefixes_fetch_bgp_prefix(
        &self,
        args: &IpAddressManagementPrefixesFetchBgpPrefixArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingSingleResponseBgp, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_prefixes_fetch_bgp_prefix_builder(
            &self.http_client,
            &args.account_id,
            &args.prefix_id,
            &args.bgp_prefix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_prefixes_fetch_bgp_prefix_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management prefixes update bgp prefix.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingSingleResponseBgp result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_prefixes_update_bgp_prefix(
        &self,
        args: &IpAddressManagementPrefixesUpdateBgpPrefixArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingSingleResponseBgp, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_prefixes_update_bgp_prefix_builder(
            &self.http_client,
            &args.account_id,
            &args.prefix_id,
            &args.bgp_prefix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_prefixes_update_bgp_prefix_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management prefixes delete bgp prefix.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_prefixes_delete_bgp_prefix(
        &self,
        args: &IpAddressManagementPrefixesDeleteBgpPrefixArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_prefixes_delete_bgp_prefix_builder(
            &self.http_client,
            &args.account_id,
            &args.prefix_id,
            &args.bgp_prefix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_prefixes_delete_bgp_prefix_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management dynamic advertisement get advertisement status.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingAdvertisedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_dynamic_advertisement_get_advertisement_status(
        &self,
        args: &IpAddressManagementDynamicAdvertisementGetAdvertisementStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingAdvertisedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_dynamic_advertisement_get_advertisement_status_builder(
            &self.http_client,
            &args.prefix_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_dynamic_advertisement_get_advertisement_status_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management dynamic advertisement update prefix dynamic advertisement status.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingAdvertisedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_dynamic_advertisement_update_prefix_dynamic_advertisement_status(
        &self,
        args: &IpAddressManagementDynamicAdvertisementUpdatePrefixDynamicAdvertisementStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingAdvertisedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_dynamic_advertisement_update_prefix_dynamic_advertisement_status_builder(
            &self.http_client,
            &args.prefix_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_dynamic_advertisement_update_prefix_dynamic_advertisement_status_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management service bindings list service bindings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_service_bindings_list_service_bindings(
        &self,
        args: &IpAddressManagementServiceBindingsListServiceBindingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_service_bindings_list_service_bindings_builder(
            &self.http_client,
            &args.account_id,
            &args.prefix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_service_bindings_list_service_bindings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management service bindings create service binding.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_service_bindings_create_service_binding(
        &self,
        args: &IpAddressManagementServiceBindingsCreateServiceBindingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_service_bindings_create_service_binding_builder(
            &self.http_client,
            &args.account_id,
            &args.prefix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_service_bindings_create_service_binding_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management service bindings get service binding.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_service_bindings_get_service_binding(
        &self,
        args: &IpAddressManagementServiceBindingsGetServiceBindingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_service_bindings_get_service_binding_builder(
            &self.http_client,
            &args.account_id,
            &args.prefix_id,
            &args.binding_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_service_bindings_get_service_binding_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management service bindings delete service binding.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_service_bindings_delete_service_binding(
        &self,
        args: &IpAddressManagementServiceBindingsDeleteServiceBindingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_service_bindings_delete_service_binding_builder(
            &self.http_client,
            &args.account_id,
            &args.prefix_id,
            &args.binding_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_service_bindings_delete_service_binding_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management prefix delegation list prefix delegations.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_prefix_delegation_list_prefix_delegations(
        &self,
        args: &IpAddressManagementPrefixDelegationListPrefixDelegationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_prefix_delegation_list_prefix_delegations_builder(
            &self.http_client,
            &args.prefix_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_prefix_delegation_list_prefix_delegations_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management prefix delegation create prefix delegation.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_prefix_delegation_create_prefix_delegation(
        &self,
        args: &IpAddressManagementPrefixDelegationCreatePrefixDelegationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_prefix_delegation_create_prefix_delegation_builder(
            &self.http_client,
            &args.prefix_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_prefix_delegation_create_prefix_delegation_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management prefix delegation delete prefix delegation.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_prefix_delegation_delete_prefix_delegation(
        &self,
        args: &IpAddressManagementPrefixDelegationDeletePrefixDelegationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_prefix_delegation_delete_prefix_delegation_builder(
            &self.http_client,
            &args.delegation_id,
            &args.prefix_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_prefix_delegation_delete_prefix_delegation_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management prefixes validate prefix.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressingSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_prefixes_validate_prefix(
        &self,
        args: &IpAddressManagementPrefixesValidatePrefixArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressingSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_prefixes_validate_prefix_builder(
            &self.http_client,
            &args.prefix_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_prefixes_validate_prefix_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dls account regional hostnames account list regions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dls_account_regional_hostnames_account_list_regions(
        &self,
        args: &DlsAccountRegionalHostnamesAccountListRegionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dls_account_regional_hostnames_account_list_regions_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dls_account_regional_hostnames_account_list_regions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip address management service bindings list services.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_address_management_service_bindings_list_services(
        &self,
        args: &IpAddressManagementServiceBindingsListServicesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_address_management_service_bindings_list_services_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_address_management_service_bindings_list_services_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config list evaluators.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn aig_config_list_evaluators(
        &self,
        args: &AigConfigListEvaluatorsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_list_evaluators_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.order_by,
            &args.order_by_direction,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_list_evaluators_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config list gateway.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn aig_config_list_gateway(
        &self,
        args: &AigConfigListGatewayArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_list_gateway_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.search,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_list_gateway_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config create gateway.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aig_config_create_gateway(
        &self,
        args: &AigConfigCreateGatewayArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_create_gateway_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_create_gateway_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config list dataset.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn aig_config_list_dataset(
        &self,
        args: &AigConfigListDatasetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_list_dataset_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.page,
            &args.per_page,
            &args.name,
            &args.enable,
            &args.search,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_list_dataset_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config create dataset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aig_config_create_dataset(
        &self,
        args: &AigConfigCreateDatasetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_create_dataset_builder(
            &self.http_client,
            &args.gateway_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_create_dataset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config fetch dataset.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn aig_config_fetch_dataset(
        &self,
        args: &AigConfigFetchDatasetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_fetch_dataset_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_fetch_dataset_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config update dataset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aig_config_update_dataset(
        &self,
        args: &AigConfigUpdateDatasetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_update_dataset_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_update_dataset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config delete dataset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aig_config_delete_dataset(
        &self,
        args: &AigConfigDeleteDatasetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_delete_dataset_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_delete_dataset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config list evaluations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn aig_config_list_evaluations(
        &self,
        args: &AigConfigListEvaluationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_list_evaluations_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.page,
            &args.per_page,
            &args.name,
            &args.processed,
            &args.search,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_list_evaluations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config create evaluations.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aig_config_create_evaluations(
        &self,
        args: &AigConfigCreateEvaluationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_create_evaluations_builder(
            &self.http_client,
            &args.gateway_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_create_evaluations_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config fetch evaluations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn aig_config_fetch_evaluations(
        &self,
        args: &AigConfigFetchEvaluationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_fetch_evaluations_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_fetch_evaluations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config delete evaluations.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aig_config_delete_evaluations(
        &self,
        args: &AigConfigDeleteEvaluationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_delete_evaluations_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_delete_evaluations_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config list gateway logs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn aig_config_list_gateway_logs(
        &self,
        args: &AigConfigListGatewayLogsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_list_gateway_logs_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.search,
            &args.page,
            &args.per_page,
            &args.order_by,
            &args.order_by_direction,
            &args.filters,
            &args.meta_info,
            &args.direction,
            &args.start_date,
            &args.end_date,
            &args.min_cost,
            &args.max_cost,
            &args.min_tokens_in,
            &args.max_tokens_in,
            &args.min_tokens_out,
            &args.max_tokens_out,
            &args.min_total_tokens,
            &args.max_total_tokens,
            &args.min_duration,
            &args.max_duration,
            &args.feedback,
            &args.success,
            &args.cached,
            &args.model,
            &args.model_type,
            &args.provider,
            &args.request_content_type,
            &args.response_content_type,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_list_gateway_logs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config delete gateway logs.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aig_config_delete_gateway_logs(
        &self,
        args: &AigConfigDeleteGatewayLogsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_delete_gateway_logs_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.order_by,
            &args.order_by_direction,
            &args.filters,
            &args.limit,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_delete_gateway_logs_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config get gateway log detail.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn aig_config_get_gateway_log_detail(
        &self,
        args: &AigConfigGetGatewayLogDetailArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_get_gateway_log_detail_builder(
            &self.http_client,
            &args.id,
            &args.gateway_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_get_gateway_log_detail_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config patch gateway log.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aig_config_patch_gateway_log(
        &self,
        args: &AigConfigPatchGatewayLogArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_patch_gateway_log_builder(
            &self.http_client,
            &args.id,
            &args.gateway_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_patch_gateway_log_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config get gateway log request.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn aig_config_get_gateway_log_request(
        &self,
        args: &AigConfigGetGatewayLogRequestArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_get_gateway_log_request_builder(
            &self.http_client,
            &args.id,
            &args.gateway_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_get_gateway_log_request_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config get gateway log response.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn aig_config_get_gateway_log_response(
        &self,
        args: &AigConfigGetGatewayLogResponseArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_get_gateway_log_response_builder(
            &self.http_client,
            &args.id,
            &args.gateway_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_get_gateway_log_response_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config list providers.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn aig_config_list_providers(
        &self,
        args: &AigConfigListProvidersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_list_providers_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_list_providers_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config create providers.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aig_config_create_providers(
        &self,
        args: &AigConfigCreateProvidersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_create_providers_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_create_providers_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config update providers.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aig_config_update_providers(
        &self,
        args: &AigConfigUpdateProvidersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_update_providers_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_update_providers_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config delete providers.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aig_config_delete_providers(
        &self,
        args: &AigConfigDeleteProvidersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_delete_providers_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_delete_providers_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config list gateway dynamic routes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn aig_config_list_gateway_dynamic_routes(
        &self,
        args: &AigConfigListGatewayDynamicRoutesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_list_gateway_dynamic_routes_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_list_gateway_dynamic_routes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config post gateway dynamic route.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aig_config_post_gateway_dynamic_route(
        &self,
        args: &AigConfigPostGatewayDynamicRouteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_post_gateway_dynamic_route_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_post_gateway_dynamic_route_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config get gateway dynamic route.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn aig_config_get_gateway_dynamic_route(
        &self,
        args: &AigConfigGetGatewayDynamicRouteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_get_gateway_dynamic_route_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_get_gateway_dynamic_route_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config update gateway dynamic route.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aig_config_update_gateway_dynamic_route(
        &self,
        args: &AigConfigUpdateGatewayDynamicRouteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_update_gateway_dynamic_route_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_update_gateway_dynamic_route_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config delete gateway dynamic route.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aig_config_delete_gateway_dynamic_route(
        &self,
        args: &AigConfigDeleteGatewayDynamicRouteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_delete_gateway_dynamic_route_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_delete_gateway_dynamic_route_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config list gateway dynamic route deployments.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn aig_config_list_gateway_dynamic_route_deployments(
        &self,
        args: &AigConfigListGatewayDynamicRouteDeploymentsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_list_gateway_dynamic_route_deployments_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_list_gateway_dynamic_route_deployments_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config post gateway dynamic route deployment.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aig_config_post_gateway_dynamic_route_deployment(
        &self,
        args: &AigConfigPostGatewayDynamicRouteDeploymentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_post_gateway_dynamic_route_deployment_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_post_gateway_dynamic_route_deployment_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config list gateway dynamic route versions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn aig_config_list_gateway_dynamic_route_versions(
        &self,
        args: &AigConfigListGatewayDynamicRouteVersionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_list_gateway_dynamic_route_versions_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_list_gateway_dynamic_route_versions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config post gateway dynamic route version.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aig_config_post_gateway_dynamic_route_version(
        &self,
        args: &AigConfigPostGatewayDynamicRouteVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_post_gateway_dynamic_route_version_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_post_gateway_dynamic_route_version_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config get gateway dynamic route version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn aig_config_get_gateway_dynamic_route_version(
        &self,
        args: &AigConfigGetGatewayDynamicRouteVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_get_gateway_dynamic_route_version_builder(
            &self.http_client,
            &args.account_id,
            &args.gateway_id,
            &args.id,
            &args.version_id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_get_gateway_dynamic_route_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config get gateway url.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn aig_config_get_gateway_url(
        &self,
        args: &AigConfigGetGatewayUrlArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_get_gateway_url_builder(
            &self.http_client,
            &args.gateway_id,
            &args.account_id,
            &args.provider,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_get_gateway_url_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config fetch gateway.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn aig_config_fetch_gateway(
        &self,
        args: &AigConfigFetchGatewayArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_fetch_gateway_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_fetch_gateway_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config update gateway.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aig_config_update_gateway(
        &self,
        args: &AigConfigUpdateGatewayArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_update_gateway_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_update_gateway_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aig config delete gateway.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aig_config_delete_gateway(
        &self,
        args: &AigConfigDeleteGatewayArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aig_config_delete_gateway_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = aig_config_delete_gateway_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ai search list instances.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ai_search_list_instances(
        &self,
        args: &AiSearchListInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ai_search_list_instances_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.namespace,
            &args.search,
            &args.order_by,
            &args.order_by_direction,
        )
        .map_err(ProviderError::Api)?;

        let task = ai_search_list_instances_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ai search create instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ai_search_create_instances(
        &self,
        args: &AiSearchCreateInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ai_search_create_instances_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ai_search_create_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ai search fetch instances.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ai_search_fetch_instances(
        &self,
        args: &AiSearchFetchInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ai_search_fetch_instances_builder(
            &self.http_client,
            &args.id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ai_search_fetch_instances_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ai search update instances.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ai_search_update_instances(
        &self,
        args: &AiSearchUpdateInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ai_search_update_instances_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = ai_search_update_instances_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ai search delete instances.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ai_search_delete_instances(
        &self,
        args: &AiSearchDeleteInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ai_search_delete_instances_builder(
            &self.http_client,
            &args.id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ai_search_delete_instances_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ai search instance chat completion.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ai_search_instance_chat_completion(
        &self,
        args: &AiSearchInstanceChatCompletionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ai_search_instance_chat_completion_builder(
            &self.http_client,
            &args.id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ai_search_instance_chat_completion_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ai search instance list jobs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ai_search_instance_list_jobs(
        &self,
        args: &AiSearchInstanceListJobsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ai_search_instance_list_jobs_builder(
            &self.http_client,
            &args.id,
            &args.account_id,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = ai_search_instance_list_jobs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ai search instance create job.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ai_search_instance_create_job(
        &self,
        args: &AiSearchInstanceCreateJobArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ai_search_instance_create_job_builder(
            &self.http_client,
            &args.id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ai_search_instance_create_job_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ai search instance get job.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ai_search_instance_get_job(
        &self,
        args: &AiSearchInstanceGetJobArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ai_search_instance_get_job_builder(
            &self.http_client,
            &args.id,
            &args.job_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ai_search_instance_get_job_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ai search instance change job status.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ai_search_instance_change_job_status(
        &self,
        args: &AiSearchInstanceChangeJobStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ai_search_instance_change_job_status_builder(
            &self.http_client,
            &args.id,
            &args.job_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ai_search_instance_change_job_status_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ai search instance list job logs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ai_search_instance_list_job_logs(
        &self,
        args: &AiSearchInstanceListJobLogsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ai_search_instance_list_job_logs_builder(
            &self.http_client,
            &args.id,
            &args.job_id,
            &args.account_id,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = ai_search_instance_list_job_logs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ai search instance search.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ai_search_instance_search(
        &self,
        args: &AiSearchInstanceSearchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ai_search_instance_search_builder(
            &self.http_client,
            &args.id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ai_search_instance_search_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ai search stats.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ai_search_stats(
        &self,
        args: &AiSearchStatsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ai_search_stats_builder(
            &self.http_client,
            &args.id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ai_search_stats_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ai search list tokens.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ai_search_list_tokens(
        &self,
        args: &AiSearchListTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ai_search_list_tokens_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.order_by,
            &args.order_by_direction,
        )
        .map_err(ProviderError::Api)?;

        let task = ai_search_list_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ai search create tokens.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ai_search_create_tokens(
        &self,
        args: &AiSearchCreateTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ai_search_create_tokens_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ai_search_create_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ai search fetch tokens.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ai_search_fetch_tokens(
        &self,
        args: &AiSearchFetchTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ai_search_fetch_tokens_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = ai_search_fetch_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ai search update tokens.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ai_search_update_tokens(
        &self,
        args: &AiSearchUpdateTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ai_search_update_tokens_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = ai_search_update_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ai search delete tokens.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ai_search_delete_tokens(
        &self,
        args: &AiSearchDeleteTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ai_search_delete_tokens_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = ai_search_delete_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai search author.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn workers_ai_search_author(
        &self,
        args: &WorkersAiSearchAuthorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_search_author_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_search_author_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai list finetunes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn workers_ai_list_finetunes(
        &self,
        args: &WorkersAiListFinetunesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_list_finetunes_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_list_finetunes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai create finetune.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_create_finetune(
        &self,
        args: &WorkersAiCreateFinetuneArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_create_finetune_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_create_finetune_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai list public finetunes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn workers_ai_list_public_finetunes(
        &self,
        args: &WorkersAiListPublicFinetunesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_list_public_finetunes_builder(
            &self.http_client,
            &args.account_id,
            &args.limit,
            &args.offset,
            &args.orderBy,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_list_public_finetunes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai upload finetune asset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_upload_finetune_asset(
        &self,
        args: &WorkersAiUploadFinetuneAssetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_upload_finetune_asset_builder(
            &self.http_client,
            &args.account_id,
            &args.finetune_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_upload_finetune_asset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai get model schema.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn workers_ai_get_model_schema(
        &self,
        args: &WorkersAiGetModelSchemaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_get_model_schema_builder(
            &self.http_client,
            &args.account_id,
            &args.model,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_get_model_schema_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai search model.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn workers_ai_search_model(
        &self,
        args: &WorkersAiSearchModelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_search_model_builder(
            &self.http_client,
            &args.account_id,
            &args.per_page,
            &args.page,
            &args.task,
            &args.author,
            &args.source,
            &args.hide_experimental,
            &args.search,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_search_model_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf ai4bharat indictrans2 en indic 1 b.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_ai4bharat_indictrans2_en_indic_1_b(
        &self,
        args: &WorkersAiPostRunCfAi4BharatIndictrans2EnIndic1BArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_ai4bharat_indictrans2_en_indic_1_b_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_ai4bharat_indictrans2_en_indic_1_b_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf ai4bharat nonomni indictrans2 en indic 1b.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_ai4bharat_nonomni_indictrans2_en_indic_1b(
        &self,
        args: &WorkersAiPostRunCfAi4BharatNonomniIndictrans2EnIndic1BArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_ai4bharat_nonomni_indictrans2_en_indic_1b_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_ai4bharat_nonomni_indictrans2_en_indic_1b_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf aisingapore gemma sea lion v4 27b it.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_aisingapore_gemma_sea_lion_v4_27b_it(
        &self,
        args: &WorkersAiPostRunCfAisingaporeGemmaSeaLionV427BItArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_aisingapore_gemma_sea_lion_v4_27b_it_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_aisingapore_gemma_sea_lion_v4_27b_it_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf baai bge base en v1 5.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_baai_bge_base_en_v1_5(
        &self,
        args: &WorkersAiPostRunCfBaaiBgeBaseEnV15Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_baai_bge_base_en_v1_5_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_baai_bge_base_en_v1_5_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf baai bge large en v1 5.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_baai_bge_large_en_v1_5(
        &self,
        args: &WorkersAiPostRunCfBaaiBgeLargeEnV15Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_baai_bge_large_en_v1_5_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_baai_bge_large_en_v1_5_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf baai bge m3.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_baai_bge_m3(
        &self,
        args: &WorkersAiPostRunCfBaaiBgeM3Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_baai_bge_m3_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_baai_bge_m3_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf baai bge reranker base.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_baai_bge_reranker_base(
        &self,
        args: &WorkersAiPostRunCfBaaiBgeRerankerBaseArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_baai_bge_reranker_base_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_baai_bge_reranker_base_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf baai bge small en v1 5.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_baai_bge_small_en_v1_5(
        &self,
        args: &WorkersAiPostRunCfBaaiBgeSmallEnV15Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_baai_bge_small_en_v1_5_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_baai_bge_small_en_v1_5_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf baai nonomni bge base en v1 5.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_baai_nonomni_bge_base_en_v1_5(
        &self,
        args: &WorkersAiPostRunCfBaaiNonomniBgeBaseEnV15Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_baai_nonomni_bge_base_en_v1_5_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_baai_nonomni_bge_base_en_v1_5_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf baai nonomni bge large en v1 5.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_baai_nonomni_bge_large_en_v1_5(
        &self,
        args: &WorkersAiPostRunCfBaaiNonomniBgeLargeEnV15Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_baai_nonomni_bge_large_en_v1_5_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_baai_nonomni_bge_large_en_v1_5_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf baai nonomni bge m3.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_baai_nonomni_bge_m3(
        &self,
        args: &WorkersAiPostRunCfBaaiNonomniBgeM3Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_baai_nonomni_bge_m3_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_baai_nonomni_bge_m3_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf baai nonomni bge small en v1 5.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_baai_nonomni_bge_small_en_v1_5(
        &self,
        args: &WorkersAiPostRunCfBaaiNonomniBgeSmallEnV15Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_baai_nonomni_bge_small_en_v1_5_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_baai_nonomni_bge_small_en_v1_5_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf black forest labs flux 1 schnell.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_black_forest_labs_flux_1_schnell(
        &self,
        args: &WorkersAiPostRunCfBlackForestLabsFlux1SchnellArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_black_forest_labs_flux_1_schnell_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_black_forest_labs_flux_1_schnell_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf black forest labs flux 2 dev.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_black_forest_labs_flux_2_dev(
        &self,
        args: &WorkersAiPostRunCfBlackForestLabsFlux2DevArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_black_forest_labs_flux_2_dev_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_black_forest_labs_flux_2_dev_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf black forest labs flux 2 klein 4b.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_black_forest_labs_flux_2_klein_4b(
        &self,
        args: &WorkersAiPostRunCfBlackForestLabsFlux2Klein4BArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_black_forest_labs_flux_2_klein_4b_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_black_forest_labs_flux_2_klein_4b_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf black forest labs flux 2 klein 9b.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_black_forest_labs_flux_2_klein_9b(
        &self,
        args: &WorkersAiPostRunCfBlackForestLabsFlux2Klein9BArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_black_forest_labs_flux_2_klein_9b_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_black_forest_labs_flux_2_klein_9b_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf bytedance stable diffusion xl lightning.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_bytedance_stable_diffusion_xl_lightning(
        &self,
        args: &WorkersAiPostRunCfBytedanceStableDiffusionXlLightningArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_bytedance_stable_diffusion_xl_lightning_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_bytedance_stable_diffusion_xl_lightning_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post websocket run cf deepgram aura.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_websocket_run_cf_deepgram_aura(
        &self,
        args: &WorkersAiPostWebsocketRunCfDeepgramAuraArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_websocket_run_cf_deepgram_aura_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_websocket_run_cf_deepgram_aura_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post websocket run cf deepgram aura 1.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_websocket_run_cf_deepgram_aura_1(
        &self,
        args: &WorkersAiPostWebsocketRunCfDeepgramAura1Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_websocket_run_cf_deepgram_aura_1_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_websocket_run_cf_deepgram_aura_1_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf deepgram aura 1.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_deepgram_aura_1(
        &self,
        args: &WorkersAiPostRunCfDeepgramAura1Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_deepgram_aura_1_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_deepgram_aura_1_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post websocket run cf deepgram aura 1 internal.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_websocket_run_cf_deepgram_aura_1_internal(
        &self,
        args: &WorkersAiPostWebsocketRunCfDeepgramAura1InternalArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_websocket_run_cf_deepgram_aura_1_internal_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_websocket_run_cf_deepgram_aura_1_internal_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post websocket run cf deepgram aura 2.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_websocket_run_cf_deepgram_aura_2(
        &self,
        args: &WorkersAiPostWebsocketRunCfDeepgramAura2Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_websocket_run_cf_deepgram_aura_2_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_websocket_run_cf_deepgram_aura_2_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post websocket run cf deepgram aura 2 en.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_websocket_run_cf_deepgram_aura_2_en(
        &self,
        args: &WorkersAiPostWebsocketRunCfDeepgramAura2EnArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_websocket_run_cf_deepgram_aura_2_en_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_websocket_run_cf_deepgram_aura_2_en_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf deepgram aura 2 en.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_deepgram_aura_2_en(
        &self,
        args: &WorkersAiPostRunCfDeepgramAura2EnArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_deepgram_aura_2_en_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_deepgram_aura_2_en_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post websocket run cf deepgram aura 2 es.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_websocket_run_cf_deepgram_aura_2_es(
        &self,
        args: &WorkersAiPostWebsocketRunCfDeepgramAura2EsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_websocket_run_cf_deepgram_aura_2_es_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_websocket_run_cf_deepgram_aura_2_es_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf deepgram aura 2 es.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_deepgram_aura_2_es(
        &self,
        args: &WorkersAiPostRunCfDeepgramAura2EsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_deepgram_aura_2_es_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_deepgram_aura_2_es_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post websocket run cf deepgram flux.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_websocket_run_cf_deepgram_flux(
        &self,
        args: &WorkersAiPostWebsocketRunCfDeepgramFluxArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_websocket_run_cf_deepgram_flux_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_websocket_run_cf_deepgram_flux_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf deepgram flux.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_deepgram_flux(
        &self,
        args: &WorkersAiPostRunCfDeepgramFluxArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_deepgram_flux_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_deepgram_flux_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post websocket run cf deepgram nova 3.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_websocket_run_cf_deepgram_nova_3(
        &self,
        args: &WorkersAiPostWebsocketRunCfDeepgramNova3Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_websocket_run_cf_deepgram_nova_3_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_websocket_run_cf_deepgram_nova_3_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf deepgram nova 3.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_deepgram_nova_3(
        &self,
        args: &WorkersAiPostRunCfDeepgramNova3Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_deepgram_nova_3_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_deepgram_nova_3_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post websocket run cf deepgram nova 3 internal.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_websocket_run_cf_deepgram_nova_3_internal(
        &self,
        args: &WorkersAiPostWebsocketRunCfDeepgramNova3InternalArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_websocket_run_cf_deepgram_nova_3_internal_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_websocket_run_cf_deepgram_nova_3_internal_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post websocket run cf deepgram nova 3 ws.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_websocket_run_cf_deepgram_nova_3_ws(
        &self,
        args: &WorkersAiPostWebsocketRunCfDeepgramNova3WsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_websocket_run_cf_deepgram_nova_3_ws_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_websocket_run_cf_deepgram_nova_3_ws_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf deepseek ai deepseek math 7b instruct.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_deepseek_ai_deepseek_math_7b_instruct(
        &self,
        args: &WorkersAiPostRunCfDeepseekAiDeepseekMath7BInstructArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_deepseek_ai_deepseek_math_7b_instruct_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_deepseek_ai_deepseek_math_7b_instruct_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf deepseek ai deepseek r1 distill qwen 32b.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_deepseek_ai_deepseek_r1_distill_qwen_32b(
        &self,
        args: &WorkersAiPostRunCfDeepseekAiDeepseekR1DistillQwen32BArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_deepseek_ai_deepseek_r1_distill_qwen_32b_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_deepseek_ai_deepseek_r1_distill_qwen_32b_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf defog sqlcoder 7b 2.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_defog_sqlcoder_7b_2(
        &self,
        args: &WorkersAiPostRunCfDefogSqlcoder7B2Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_defog_sqlcoder_7b_2_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_defog_sqlcoder_7b_2_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf facebook bart large cnn.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_facebook_bart_large_cnn(
        &self,
        args: &WorkersAiPostRunCfFacebookBartLargeCnnArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_facebook_bart_large_cnn_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_facebook_bart_large_cnn_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf facebook nonomni bart large cnn.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_facebook_nonomni_bart_large_cnn(
        &self,
        args: &WorkersAiPostRunCfFacebookNonomniBartLargeCnnArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_facebook_nonomni_bart_large_cnn_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_facebook_nonomni_bart_large_cnn_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf facebook nonomni detr resnet 50.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_facebook_nonomni_detr_resnet_50(
        &self,
        args: &WorkersAiPostRunCfFacebookNonomniDetrResnet50Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_facebook_nonomni_detr_resnet_50_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_facebook_nonomni_detr_resnet_50_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf fblgit una cybertron 7b v2 bf16.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_fblgit_una_cybertron_7b_v2_bf16(
        &self,
        args: &WorkersAiPostRunCfFblgitUnaCybertron7BV2Bf16Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_fblgit_una_cybertron_7b_v2_bf16_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_fblgit_una_cybertron_7b_v2_bf16_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf google embeddinggemma 300m.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_google_embeddinggemma_300m(
        &self,
        args: &WorkersAiPostRunCfGoogleEmbeddinggemma300MArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_google_embeddinggemma_300m_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_google_embeddinggemma_300m_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf google gemma 2b it lora.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_google_gemma_2b_it_lora(
        &self,
        args: &WorkersAiPostRunCfGoogleGemma2BItLoraArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_google_gemma_2b_it_lora_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_google_gemma_2b_it_lora_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf google gemma 3 12b it.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_google_gemma_3_12b_it(
        &self,
        args: &WorkersAiPostRunCfGoogleGemma312BItArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_google_gemma_3_12b_it_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_google_gemma_3_12b_it_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf google gemma 7b it lora.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_google_gemma_7b_it_lora(
        &self,
        args: &WorkersAiPostRunCfGoogleGemma7BItLoraArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_google_gemma_7b_it_lora_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_google_gemma_7b_it_lora_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf google nonomni embeddinggemma 300m.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_google_nonomni_embeddinggemma_300m(
        &self,
        args: &WorkersAiPostRunCfGoogleNonomniEmbeddinggemma300MArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_google_nonomni_embeddinggemma_300m_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_google_nonomni_embeddinggemma_300m_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf huggingface distilbert sst 2 int8.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_huggingface_distilbert_sst_2_int8(
        &self,
        args: &WorkersAiPostRunCfHuggingfaceDistilbertSst2Int8Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_huggingface_distilbert_sst_2_int8_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_huggingface_distilbert_sst_2_int8_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf huggingface nonomni distilbert sst 2 int8.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_huggingface_nonomni_distilbert_sst_2_int8(
        &self,
        args: &WorkersAiPostRunCfHuggingfaceNonomniDistilbertSst2Int8Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_huggingface_nonomni_distilbert_sst_2_int8_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_huggingface_nonomni_distilbert_sst_2_int8_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf ibm granite granite 4 0 h micro.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_ibm_granite_granite_4_0_h_micro(
        &self,
        args: &WorkersAiPostRunCfIbmGraniteGranite40HMicroArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_ibm_granite_granite_4_0_h_micro_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_ibm_granite_granite_4_0_h_micro_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf leonardo lucid origin.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_leonardo_lucid_origin(
        &self,
        args: &WorkersAiPostRunCfLeonardoLucidOriginArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_leonardo_lucid_origin_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_leonardo_lucid_origin_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf leonardo phoenix 1 0.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_leonardo_phoenix_1_0(
        &self,
        args: &WorkersAiPostRunCfLeonardoPhoenix10Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_leonardo_phoenix_1_0_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_leonardo_phoenix_1_0_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf lykon dreamshaper 8 lcm.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_lykon_dreamshaper_8_lcm(
        &self,
        args: &WorkersAiPostRunCfLykonDreamshaper8LcmArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_lykon_dreamshaper_8_lcm_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_lykon_dreamshaper_8_lcm_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf meta llama llama 2 7b chat hf lora.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_meta_llama_llama_2_7b_chat_hf_lora(
        &self,
        args: &WorkersAiPostRunCfMetaLlamaLlama27BChatHfLoraArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_meta_llama_llama_2_7b_chat_hf_lora_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_meta_llama_llama_2_7b_chat_hf_lora_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf meta llama 2 7b chat fp16.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_meta_llama_2_7b_chat_fp16(
        &self,
        args: &WorkersAiPostRunCfMetaLlama27BChatFp16Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_meta_llama_2_7b_chat_fp16_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_meta_llama_2_7b_chat_fp16_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf meta llama 2 7b chat int8.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_meta_llama_2_7b_chat_int8(
        &self,
        args: &WorkersAiPostRunCfMetaLlama27BChatInt8Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_meta_llama_2_7b_chat_int8_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_meta_llama_2_7b_chat_int8_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf meta llama 3 8b instruct.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_meta_llama_3_8b_instruct(
        &self,
        args: &WorkersAiPostRunCfMetaLlama38BInstructArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_meta_llama_3_8b_instruct_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_meta_llama_3_8b_instruct_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf meta llama 3 8b instruct awq.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_meta_llama_3_8b_instruct_awq(
        &self,
        args: &WorkersAiPostRunCfMetaLlama38BInstructAwqArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_meta_llama_3_8b_instruct_awq_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_meta_llama_3_8b_instruct_awq_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf meta llama 3 1 70b instruct fp8 fast.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_meta_llama_3_1_70b_instruct_fp8_fast(
        &self,
        args: &WorkersAiPostRunCfMetaLlama3170BInstructFp8FastArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_meta_llama_3_1_70b_instruct_fp8_fast_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_meta_llama_3_1_70b_instruct_fp8_fast_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf meta llama 3 1 8b instruct awq.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_awq(
        &self,
        args: &WorkersAiPostRunCfMetaLlama318BInstructAwqArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_awq_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_awq_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf meta llama 3 1 8b instruct fp8.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_fp8(
        &self,
        args: &WorkersAiPostRunCfMetaLlama318BInstructFp8Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_fp8_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_fp8_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf meta llama 3 1 8b instruct fp8 fast.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_fp8_fast(
        &self,
        args: &WorkersAiPostRunCfMetaLlama318BInstructFp8FastArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_fp8_fast_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_fp8_fast_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf meta llama 3 2 11b vision instruct.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_meta_llama_3_2_11b_vision_instruct(
        &self,
        args: &WorkersAiPostRunCfMetaLlama3211BVisionInstructArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_meta_llama_3_2_11b_vision_instruct_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_meta_llama_3_2_11b_vision_instruct_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf meta llama 3 2 1b instruct.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_meta_llama_3_2_1b_instruct(
        &self,
        args: &WorkersAiPostRunCfMetaLlama321BInstructArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_meta_llama_3_2_1b_instruct_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_meta_llama_3_2_1b_instruct_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf meta llama 3 2 3b instruct.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_meta_llama_3_2_3b_instruct(
        &self,
        args: &WorkersAiPostRunCfMetaLlama323BInstructArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_meta_llama_3_2_3b_instruct_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_meta_llama_3_2_3b_instruct_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf meta llama 3 3 70b instruct fp8 fast.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_meta_llama_3_3_70b_instruct_fp8_fast(
        &self,
        args: &WorkersAiPostRunCfMetaLlama3370BInstructFp8FastArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_meta_llama_3_3_70b_instruct_fp8_fast_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_meta_llama_3_3_70b_instruct_fp8_fast_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf meta llama 4 scout 17b 16e instruct.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_meta_llama_4_scout_17b_16e_instruct(
        &self,
        args: &WorkersAiPostRunCfMetaLlama4Scout17B16EInstructArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_meta_llama_4_scout_17b_16e_instruct_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_meta_llama_4_scout_17b_16e_instruct_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf meta llama guard 3 8b.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_meta_llama_guard_3_8b(
        &self,
        args: &WorkersAiPostRunCfMetaLlamaGuard38BArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_meta_llama_guard_3_8b_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_meta_llama_guard_3_8b_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf meta m2m100 1 2b.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_meta_m2m100_1_2b(
        &self,
        args: &WorkersAiPostRunCfMetaM2M10012BArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_meta_m2m100_1_2b_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_meta_m2m100_1_2b_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf microsoft nonomni resnet 50.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_microsoft_nonomni_resnet_50(
        &self,
        args: &WorkersAiPostRunCfMicrosoftNonomniResnet50Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_microsoft_nonomni_resnet_50_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_microsoft_nonomni_resnet_50_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf microsoft phi 2.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_microsoft_phi_2(
        &self,
        args: &WorkersAiPostRunCfMicrosoftPhi2Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_microsoft_phi_2_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_microsoft_phi_2_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf microsoft resnet 50.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_microsoft_resnet_50(
        &self,
        args: &WorkersAiPostRunCfMicrosoftResnet50Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_microsoft_resnet_50_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_microsoft_resnet_50_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf mistral mistral 7b instruct v0 1.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_mistral_mistral_7b_instruct_v0_1(
        &self,
        args: &WorkersAiPostRunCfMistralMistral7BInstructV01Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_mistral_mistral_7b_instruct_v0_1_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_mistral_mistral_7b_instruct_v0_1_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf mistral mistral 7b instruct v0 2 lora.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_mistral_mistral_7b_instruct_v0_2_lora(
        &self,
        args: &WorkersAiPostRunCfMistralMistral7BInstructV02LoraArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_mistral_mistral_7b_instruct_v0_2_lora_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_mistral_mistral_7b_instruct_v0_2_lora_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf mistralai mistral small 3 1 24b instruct.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_mistralai_mistral_small_3_1_24b_instruct(
        &self,
        args: &WorkersAiPostRunCfMistralaiMistralSmall3124BInstructArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_mistralai_mistral_small_3_1_24b_instruct_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_mistralai_mistral_small_3_1_24b_instruct_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf moonshotai kimi k2 5.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_moonshotai_kimi_k2_5(
        &self,
        args: &WorkersAiPostRunCfMoonshotaiKimiK25Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_moonshotai_kimi_k2_5_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_moonshotai_kimi_k2_5_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf myshell ai melotts.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_myshell_ai_melotts(
        &self,
        args: &WorkersAiPostRunCfMyshellAiMelottsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_myshell_ai_melotts_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_myshell_ai_melotts_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf nvidia nemotron 3 120b a12b.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_nvidia_nemotron_3_120b_a12b(
        &self,
        args: &WorkersAiPostRunCfNvidiaNemotron3120BA12BArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_nvidia_nemotron_3_120b_a12b_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_nvidia_nemotron_3_120b_a12b_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf openai gpt oss 120b.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_openai_gpt_oss_120b(
        &self,
        args: &WorkersAiPostRunCfOpenaiGptOss120BArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_openai_gpt_oss_120b_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_openai_gpt_oss_120b_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf openai gpt oss 20b.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_openai_gpt_oss_20b(
        &self,
        args: &WorkersAiPostRunCfOpenaiGptOss20BArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_openai_gpt_oss_20b_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_openai_gpt_oss_20b_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf openai whisper.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_openai_whisper(
        &self,
        args: &WorkersAiPostRunCfOpenaiWhisperArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_openai_whisper_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_openai_whisper_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf openai whisper large v3 turbo.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_openai_whisper_large_v3_turbo(
        &self,
        args: &WorkersAiPostRunCfOpenaiWhisperLargeV3TurboArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_openai_whisper_large_v3_turbo_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_openai_whisper_large_v3_turbo_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf openai whisper tiny en.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_openai_whisper_tiny_en(
        &self,
        args: &WorkersAiPostRunCfOpenaiWhisperTinyEnArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_openai_whisper_tiny_en_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_openai_whisper_tiny_en_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf openchat openchat 3 5 0106.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_openchat_openchat_3_5_0106(
        &self,
        args: &WorkersAiPostRunCfOpenchatOpenchat350106Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_openchat_openchat_3_5_0106_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_openchat_openchat_3_5_0106_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf pfnet plamo embedding 1b.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_pfnet_plamo_embedding_1b(
        &self,
        args: &WorkersAiPostRunCfPfnetPlamoEmbedding1BArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_pfnet_plamo_embedding_1b_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_pfnet_plamo_embedding_1b_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post websocket run cf pipecat ai smart turn v2.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_v2(
        &self,
        args: &WorkersAiPostWebsocketRunCfPipecatAiSmartTurnV2Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_v2_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_v2_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post websocket run cf pipecat ai smart turn v3.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_v3(
        &self,
        args: &WorkersAiPostWebsocketRunCfPipecatAiSmartTurnV3Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_v3_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_v3_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf qwen qwen1 5 0 5b chat.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_qwen_qwen1_5_0_5b_chat(
        &self,
        args: &WorkersAiPostRunCfQwenQwen1505BChatArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_qwen_qwen1_5_0_5b_chat_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_qwen_qwen1_5_0_5b_chat_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf qwen qwen1 5 1 8b chat.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_qwen_qwen1_5_1_8b_chat(
        &self,
        args: &WorkersAiPostRunCfQwenQwen1518BChatArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_qwen_qwen1_5_1_8b_chat_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_qwen_qwen1_5_1_8b_chat_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf qwen qwen1 5 14b chat awq.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_qwen_qwen1_5_14b_chat_awq(
        &self,
        args: &WorkersAiPostRunCfQwenQwen1514BChatAwqArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_qwen_qwen1_5_14b_chat_awq_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_qwen_qwen1_5_14b_chat_awq_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf qwen qwen1 5 7b chat awq.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_qwen_qwen1_5_7b_chat_awq(
        &self,
        args: &WorkersAiPostRunCfQwenQwen157BChatAwqArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_qwen_qwen1_5_7b_chat_awq_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_qwen_qwen1_5_7b_chat_awq_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf qwen qwen2 5 coder 32b instruct.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_qwen_qwen2_5_coder_32b_instruct(
        &self,
        args: &WorkersAiPostRunCfQwenQwen25Coder32BInstructArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_qwen_qwen2_5_coder_32b_instruct_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_qwen_qwen2_5_coder_32b_instruct_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf qwen qwen3 30b a3b fp8.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_qwen_qwen3_30b_a3b_fp8(
        &self,
        args: &WorkersAiPostRunCfQwenQwen330BA3BFp8Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_qwen_qwen3_30b_a3b_fp8_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_qwen_qwen3_30b_a3b_fp8_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf qwen qwen3 embedding 0 6b.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_qwen_qwen3_embedding_0_6b(
        &self,
        args: &WorkersAiPostRunCfQwenQwen3Embedding06BArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_qwen_qwen3_embedding_0_6b_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_qwen_qwen3_embedding_0_6b_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf qwen qwq 32b.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_qwen_qwq_32b(
        &self,
        args: &WorkersAiPostRunCfQwenQwq32BArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_qwen_qwq_32b_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_qwen_qwq_32b_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf runwayml stable diffusion v1 5 img2img.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_runwayml_stable_diffusion_v1_5_img2img(
        &self,
        args: &WorkersAiPostRunCfRunwaymlStableDiffusionV15Img2ImgArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_runwayml_stable_diffusion_v1_5_img2img_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_runwayml_stable_diffusion_v1_5_img2img_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf runwayml stable diffusion v1 5 inpainting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_runwayml_stable_diffusion_v1_5_inpainting(
        &self,
        args: &WorkersAiPostRunCfRunwaymlStableDiffusionV15InpaintingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_runwayml_stable_diffusion_v1_5_inpainting_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_runwayml_stable_diffusion_v1_5_inpainting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf stabilityai stable diffusion xl base 1 0.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_stabilityai_stable_diffusion_xl_base_1_0(
        &self,
        args: &WorkersAiPostRunCfStabilityaiStableDiffusionXlBase10Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_stabilityai_stable_diffusion_xl_base_1_0_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_stabilityai_stable_diffusion_xl_base_1_0_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post websocket run cf sven test pipe http.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_websocket_run_cf_sven_test_pipe_http(
        &self,
        args: &WorkersAiPostWebsocketRunCfSvenTestPipeHttpArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_websocket_run_cf_sven_test_pipe_http_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_websocket_run_cf_sven_test_pipe_http_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post websocket run cf test hello world cog.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_websocket_run_cf_test_hello_world_cog(
        &self,
        args: &WorkersAiPostWebsocketRunCfTestHelloWorldCogArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_websocket_run_cf_test_hello_world_cog_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_websocket_run_cf_test_hello_world_cog_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf thebloke discolm german 7b v1 awq.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_thebloke_discolm_german_7b_v1_awq(
        &self,
        args: &WorkersAiPostRunCfTheblokeDiscolmGerman7BV1AwqArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_thebloke_discolm_german_7b_v1_awq_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_thebloke_discolm_german_7b_v1_awq_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf tiiuae falcon 7b instruct.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_tiiuae_falcon_7b_instruct(
        &self,
        args: &WorkersAiPostRunCfTiiuaeFalcon7BInstructArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_tiiuae_falcon_7b_instruct_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_tiiuae_falcon_7b_instruct_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf tinyllama tinyllama 1 1b chat v1 0.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_tinyllama_tinyllama_1_1b_chat_v1_0(
        &self,
        args: &WorkersAiPostRunCfTinyllamaTinyllama11BChatV10Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_tinyllama_tinyllama_1_1b_chat_v1_0_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_tinyllama_tinyllama_1_1b_chat_v1_0_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run cf zai org glm 4 7 flash.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_cf_zai_org_glm_4_7_flash(
        &self,
        args: &WorkersAiPostRunCfZaiOrgGlm47FlashArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_cf_zai_org_glm_4_7_flash_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_cf_zai_org_glm_4_7_flash_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run hf google gemma 7b it.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_hf_google_gemma_7b_it(
        &self,
        args: &WorkersAiPostRunHfGoogleGemma7BItArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_hf_google_gemma_7b_it_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_hf_google_gemma_7b_it_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run hf mistral mistral 7b instruct v0 2.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_hf_mistral_mistral_7b_instruct_v0_2(
        &self,
        args: &WorkersAiPostRunHfMistralMistral7BInstructV02Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_hf_mistral_mistral_7b_instruct_v0_2_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_hf_mistral_mistral_7b_instruct_v0_2_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run hf nexusflow starling lm 7b beta.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_hf_nexusflow_starling_lm_7b_beta(
        &self,
        args: &WorkersAiPostRunHfNexusflowStarlingLm7BBetaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_hf_nexusflow_starling_lm_7b_beta_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_hf_nexusflow_starling_lm_7b_beta_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run hf nousresearch hermes 2 pro mistral 7b.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn workers_ai_post_run_hf_nousresearch_hermes_2_pro_mistral_7b(
        &self,
        args: &WorkersAiPostRunHfNousresearchHermes2ProMistral7BArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_hf_nousresearch_hermes_2_pro_mistral_7b_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_hf_nousresearch_hermes_2_pro_mistral_7b_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run hf thebloke deepseek coder 6 7b base awq.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_hf_thebloke_deepseek_coder_6_7b_base_awq(
        &self,
        args: &WorkersAiPostRunHfTheblokeDeepseekCoder67BBaseAwqArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_hf_thebloke_deepseek_coder_6_7b_base_awq_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_hf_thebloke_deepseek_coder_6_7b_base_awq_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run hf thebloke deepseek coder 6 7b instruct awq.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_hf_thebloke_deepseek_coder_6_7b_instruct_awq(
        &self,
        args: &WorkersAiPostRunHfTheblokeDeepseekCoder67BInstructAwqArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_hf_thebloke_deepseek_coder_6_7b_instruct_awq_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_hf_thebloke_deepseek_coder_6_7b_instruct_awq_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run hf thebloke llama 2 13b chat awq.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_hf_thebloke_llama_2_13b_chat_awq(
        &self,
        args: &WorkersAiPostRunHfTheblokeLlama213BChatAwqArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_hf_thebloke_llama_2_13b_chat_awq_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_hf_thebloke_llama_2_13b_chat_awq_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run hf thebloke mistral 7b instruct v0 1 awq.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_hf_thebloke_mistral_7b_instruct_v0_1_awq(
        &self,
        args: &WorkersAiPostRunHfTheblokeMistral7BInstructV01AwqArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_hf_thebloke_mistral_7b_instruct_v0_1_awq_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_hf_thebloke_mistral_7b_instruct_v0_1_awq_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run hf thebloke neural chat 7b v3 1 awq.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_hf_thebloke_neural_chat_7b_v3_1_awq(
        &self,
        args: &WorkersAiPostRunHfTheblokeNeuralChat7BV31AwqArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_hf_thebloke_neural_chat_7b_v3_1_awq_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_hf_thebloke_neural_chat_7b_v3_1_awq_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run hf thebloke openhermes 2 5 mistral 7b awq.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_hf_thebloke_openhermes_2_5_mistral_7b_awq(
        &self,
        args: &WorkersAiPostRunHfTheblokeOpenhermes25Mistral7BAwqArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_hf_thebloke_openhermes_2_5_mistral_7b_awq_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_hf_thebloke_openhermes_2_5_mistral_7b_awq_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run hf thebloke zephyr 7b beta awq.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_hf_thebloke_zephyr_7b_beta_awq(
        &self,
        args: &WorkersAiPostRunHfTheblokeZephyr7BBetaAwqArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_hf_thebloke_zephyr_7b_beta_awq_builder(
            &self.http_client,
            &args.account_id,
            &args.queueRequest,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_hf_thebloke_zephyr_7b_beta_awq_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post run model.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_run_model(
        &self,
        args: &WorkersAiPostRunModelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_run_model_builder(
            &self.http_client,
            &args.account_id,
            &args.model_name,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_run_model_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai search task.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn workers_ai_search_task(
        &self,
        args: &WorkersAiSearchTaskArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_search_task_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_search_task_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai post to markdown.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_ai_post_to_markdown(
        &self,
        args: &WorkersAiPostToMarkdownArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_post_to_markdown_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_post_to_markdown_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers ai get to markdown supported.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn workers_ai_get_to_markdown_supported(
        &self,
        args: &WorkersAiGetToMarkdownSupportedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_ai_get_to_markdown_supported_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_ai_get_to_markdown_supported_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification alert types get alert types.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaAlertsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn notification_alert_types_get_alert_types(
        &self,
        args: &NotificationAlertTypesGetAlertTypesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaAlertsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_alert_types_get_alert_types_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_alert_types_get_alert_types_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification mechanism eligibility get delivery mechanism eligibility.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn notification_mechanism_eligibility_get_delivery_mechanism_eligibility(
        &self,
        args: &NotificationMechanismEligibilityGetDeliveryMechanismEligibilityArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_mechanism_eligibility_get_delivery_mechanism_eligibility_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_mechanism_eligibility_get_delivery_mechanism_eligibility_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification destinations with pager duty list pager duty services.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn notification_destinations_with_pager_duty_list_pager_duty_services(
        &self,
        args: &NotificationDestinationsWithPagerDutyListPagerDutyServicesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_destinations_with_pager_duty_list_pager_duty_services_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_destinations_with_pager_duty_list_pager_duty_services_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification destinations with pager duty delete pager duty services.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaSchemasApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn notification_destinations_with_pager_duty_delete_pager_duty_services(
        &self,
        args: &NotificationDestinationsWithPagerDutyDeletePagerDutyServicesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaSchemasApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_destinations_with_pager_duty_delete_pager_duty_services_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_destinations_with_pager_duty_delete_pager_duty_services_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification destinations with pager duty connect pager duty.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaSensitiveIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn notification_destinations_with_pager_duty_connect_pager_duty(
        &self,
        args: &NotificationDestinationsWithPagerDutyConnectPagerDutyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaSensitiveIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_destinations_with_pager_duty_connect_pager_duty_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_destinations_with_pager_duty_connect_pager_duty_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification destinations with pager duty connect pager duty token.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn notification_destinations_with_pager_duty_connect_pager_duty_token(
        &self,
        args: &NotificationDestinationsWithPagerDutyConnectPagerDutyTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_destinations_with_pager_duty_connect_pager_duty_token_builder(
            &self.http_client,
            &args.account_id,
            &args.token_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_destinations_with_pager_duty_connect_pager_duty_token_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification webhooks list webhooks.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaWebhooksComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn notification_webhooks_list_webhooks(
        &self,
        args: &NotificationWebhooksListWebhooksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaWebhooksComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_webhooks_list_webhooks_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_webhooks_list_webhooks_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification webhooks create a webhook.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn notification_webhooks_create_a_webhook(
        &self,
        args: &NotificationWebhooksCreateAWebhookArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_webhooks_create_a_webhook_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_webhooks_create_a_webhook_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification webhooks get a webhook.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn notification_webhooks_get_a_webhook(
        &self,
        args: &NotificationWebhooksGetAWebhookArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_webhooks_get_a_webhook_builder(
            &self.http_client,
            &args.account_id,
            &args.webhook_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_webhooks_get_a_webhook_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification webhooks update a webhook.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn notification_webhooks_update_a_webhook(
        &self,
        args: &NotificationWebhooksUpdateAWebhookArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_webhooks_update_a_webhook_builder(
            &self.http_client,
            &args.webhook_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_webhooks_update_a_webhook_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification webhooks delete a webhook.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaSchemasApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn notification_webhooks_delete_a_webhook(
        &self,
        args: &NotificationWebhooksDeleteAWebhookArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaSchemasApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_webhooks_delete_a_webhook_builder(
            &self.http_client,
            &args.webhook_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_webhooks_delete_a_webhook_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification history list history.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaHistoryComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn notification_history_list_history(
        &self,
        args: &NotificationHistoryListHistoryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaHistoryComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_history_list_history_builder(
            &self.http_client,
            &args.account_id,
            &args.per_page,
            &args.before,
            &args.page,
            &args.since,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_history_list_history_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification policies list notification policies.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaPoliciesComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn notification_policies_list_notification_policies(
        &self,
        args: &NotificationPoliciesListNotificationPoliciesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaPoliciesComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_policies_list_notification_policies_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_policies_list_notification_policies_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification policies create a notification policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn notification_policies_create_a_notification_policy(
        &self,
        args: &NotificationPoliciesCreateANotificationPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_policies_create_a_notification_policy_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_policies_create_a_notification_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification policies get a notification policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn notification_policies_get_a_notification_policy(
        &self,
        args: &NotificationPoliciesGetANotificationPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_policies_get_a_notification_policy_builder(
            &self.http_client,
            &args.account_id,
            &args.policy_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_policies_get_a_notification_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification policies update a notification policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn notification_policies_update_a_notification_policy(
        &self,
        args: &NotificationPoliciesUpdateANotificationPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_policies_update_a_notification_policy_builder(
            &self.http_client,
            &args.account_id,
            &args.policy_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_policies_update_a_notification_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification policies delete a notification policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaApiResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn notification_policies_delete_a_notification_policy(
        &self,
        args: &NotificationPoliciesDeleteANotificationPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaApiResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_policies_delete_a_notification_policy_builder(
            &self.http_client,
            &args.account_id,
            &args.policy_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_policies_delete_a_notification_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification silences list silences.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaSilencesComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn notification_silences_list_silences(
        &self,
        args: &NotificationSilencesListSilencesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaSilencesComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_silences_list_silences_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_silences_list_silences_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification silences create silences.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaSchemasApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn notification_silences_create_silences(
        &self,
        args: &NotificationSilencesCreateSilencesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaSchemasApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_silences_create_silences_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_silences_create_silences_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification silences update silences.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaSilencesComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn notification_silences_update_silences(
        &self,
        args: &NotificationSilencesUpdateSilencesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaSilencesComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_silences_update_silences_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_silences_update_silences_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification silences get silence.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaSilenceComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn notification_silences_get_silence(
        &self,
        args: &NotificationSilencesGetSilenceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaSilenceComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_silences_get_silence_builder(
            &self.http_client,
            &args.account_id,
            &args.silence_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_silences_get_silence_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Notification silences delete silences.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaSchemasApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn notification_silences_delete_silences(
        &self,
        args: &NotificationSilencesDeleteSilencesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaSchemasApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = notification_silences_delete_silences_builder(
            &self.http_client,
            &args.account_id,
            &args.silence_id,
        )
        .map_err(ProviderError::Api)?;

        let task = notification_silences_delete_silences_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Audit logs get account audit logs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaAuditLogsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn audit_logs_get_account_audit_logs(
        &self,
        args: &AuditLogsGetAccountAuditLogsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaAuditLogsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = audit_logs_get_account_audit_logs_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
            &args.export,
            &args.action_type,
            &args.actor_ip,
            &args.actor_email,
            &args.since,
            &args.before,
            &args.zone_name,
            &args.direction,
            &args.per_page,
            &args.page,
            &args.hide_user_logs,
        )
        .map_err(ProviderError::Api)?;

        let task = audit_logs_get_account_audit_logs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Autorag config ai search.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn autorag_config_ai_search(
        &self,
        args: &AutoragConfigAiSearchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = autorag_config_ai_search_builder(
            &self.http_client,
            &args.id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = autorag_config_ai_search_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Autorag config files.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn autorag_config_files(
        &self,
        args: &AutoragConfigFilesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = autorag_config_files_builder(
            &self.http_client,
            &args.id,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.search,
            &args.status,
        )
        .map_err(ProviderError::Api)?;

        let task = autorag_config_files_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Autorag config list jobs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn autorag_config_list_jobs(
        &self,
        args: &AutoragConfigListJobsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = autorag_config_list_jobs_builder(
            &self.http_client,
            &args.id,
            &args.account_id,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = autorag_config_list_jobs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Autorag config get job.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn autorag_config_get_job(
        &self,
        args: &AutoragConfigGetJobArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = autorag_config_get_job_builder(
            &self.http_client,
            &args.id,
            &args.job_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = autorag_config_get_job_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Autorag config list job logs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn autorag_config_list_job_logs(
        &self,
        args: &AutoragConfigListJobLogsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = autorag_config_list_job_logs_builder(
            &self.http_client,
            &args.id,
            &args.job_id,
            &args.account_id,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = autorag_config_list_job_logs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Autorag config search.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn autorag_config_search(
        &self,
        args: &AutoragConfigSearchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = autorag_config_search_builder(
            &self.http_client,
            &args.id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = autorag_config_search_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Autorag config sync.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn autorag_config_sync(
        &self,
        args: &AutoragConfigSyncArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = autorag_config_sync_builder(
            &self.http_client,
            &args.id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = autorag_config_sync_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account billing profile deprecated billing profile details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BillSubsApiBillingResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_billing_profile_deprecated_billing_profile_details(
        &self,
        args: &AccountBillingProfileDeprecatedBillingProfileDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BillSubsApiBillingResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_billing_profile_deprecated_billing_profile_details_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_billing_profile_deprecated_billing_profile_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Billable usage get paygo account usage.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BillableUsageApiUsageResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn billable_usage_get_paygo_account_usage(
        &self,
        args: &BillableUsageGetPaygoAccountUsageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BillableUsageApiUsageResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = billable_usage_get_paygo_account_usage_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = billable_usage_get_paygo_account_usage_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Botnet threat feed get day report.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn botnet_threat_feed_get_day_report(
        &self,
        args: &BotnetThreatFeedGetDayReportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = botnet_threat_feed_get_day_report_builder(
            &self.http_client,
            &args.account_id,
            &args.asn_id,
            &args.date,
        )
        .map_err(ProviderError::Api)?;

        let task = botnet_threat_feed_get_day_report_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Botnet threat feed get full report.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn botnet_threat_feed_get_full_report(
        &self,
        args: &BotnetThreatFeedGetFullReportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = botnet_threat_feed_get_full_report_builder(
            &self.http_client,
            &args.account_id,
            &args.asn_id,
        )
        .map_err(ProviderError::Api)?;

        let task = botnet_threat_feed_get_full_report_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Botnet threat feed list asn.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn botnet_threat_feed_list_asn(
        &self,
        args: &BotnetThreatFeedListAsnArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = botnet_threat_feed_list_asn_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = botnet_threat_feed_list_asn_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Botnet threat feed delete asn.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn botnet_threat_feed_delete_asn(
        &self,
        args: &BotnetThreatFeedDeleteAsnArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = botnet_threat_feed_delete_asn_builder(
            &self.http_client,
            &args.account_id,
            &args.asn_id,
        )
        .map_err(ProviderError::Api)?;

        let task = botnet_threat_feed_delete_asn_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Brapi post content.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn brapi_post_content(
        &self,
        args: &BrapiPostContentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = brapi_post_content_builder(
            &self.http_client,
            &args.account_id,
            &args.cacheTTL,
        )
        .map_err(ProviderError::Api)?;

        let task = brapi_post_content_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Brapi post crawl.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn brapi_post_crawl(
        &self,
        args: &BrapiPostCrawlArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = brapi_post_crawl_builder(
            &self.http_client,
            &args.account_id,
            &args.cacheTTL,
        )
        .map_err(ProviderError::Api)?;

        let task = brapi_post_crawl_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Brapi get crawl result.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn brapi_get_crawl_result(
        &self,
        args: &BrapiGetCrawlResultArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = brapi_get_crawl_result_builder(
            &self.http_client,
            &args.account_id,
            &args.job_id,
            &args.cacheTTL,
            &args.status,
            &args.cursor,
            &args.limit,
        )
        .map_err(ProviderError::Api)?;

        let task = brapi_get_crawl_result_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Brapi delete cancel crawl.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn brapi_delete_cancel_crawl(
        &self,
        args: &BrapiDeleteCancelCrawlArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = brapi_delete_cancel_crawl_builder(
            &self.http_client,
            &args.account_id,
            &args.job_id,
        )
        .map_err(ProviderError::Api)?;

        let task = brapi_delete_cancel_crawl_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Brapi post json.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn brapi_post_json(
        &self,
        args: &BrapiPostJsonArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = brapi_post_json_builder(
            &self.http_client,
            &args.account_id,
            &args.cacheTTL,
        )
        .map_err(ProviderError::Api)?;

        let task = brapi_post_json_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Brapi post links.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn brapi_post_links(
        &self,
        args: &BrapiPostLinksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = brapi_post_links_builder(
            &self.http_client,
            &args.account_id,
            &args.cacheTTL,
        )
        .map_err(ProviderError::Api)?;

        let task = brapi_post_links_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Brapi post markdown.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn brapi_post_markdown(
        &self,
        args: &BrapiPostMarkdownArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = brapi_post_markdown_builder(
            &self.http_client,
            &args.account_id,
            &args.cacheTTL,
        )
        .map_err(ProviderError::Api)?;

        let task = brapi_post_markdown_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Brapi post pdf.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn brapi_post_pdf(
        &self,
        args: &BrapiPostPdfArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = brapi_post_pdf_builder(
            &self.http_client,
            &args.account_id,
            &args.cacheTTL,
        )
        .map_err(ProviderError::Api)?;

        let task = brapi_post_pdf_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Brapi post scrape.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn brapi_post_scrape(
        &self,
        args: &BrapiPostScrapeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = brapi_post_scrape_builder(
            &self.http_client,
            &args.account_id,
            &args.cacheTTL,
        )
        .map_err(ProviderError::Api)?;

        let task = brapi_post_scrape_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Brapi post screenshot.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn brapi_post_screenshot(
        &self,
        args: &BrapiPostScreenshotArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = brapi_post_screenshot_builder(
            &self.http_client,
            &args.account_id,
            &args.cacheTTL,
        )
        .map_err(ProviderError::Api)?;

        let task = brapi_post_screenshot_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Brapi post snapshot.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn brapi_post_snapshot(
        &self,
        args: &BrapiPostSnapshotArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = brapi_post_snapshot_builder(
            &self.http_client,
            &args.account_id,
            &args.cacheTTL,
        )
        .map_err(ProviderError::Api)?;

        let task = brapi_post_snapshot_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get account limits.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_account_limits(
        &self,
        args: &GetAccountLimitsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_account_limits_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_account_limits_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get builds by version ids.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_builds_by_version_ids(
        &self,
        args: &GetBuildsByVersionIdsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_builds_by_version_ids_builder(
            &self.http_client,
            &args.account_id,
            &args.version_ids,
        )
        .map_err(ProviderError::Api)?;

        let task = get_builds_by_version_ids_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get latest builds by scripts.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_latest_builds_by_scripts(
        &self,
        args: &GetLatestBuildsByScriptsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_latest_builds_by_scripts_builder(
            &self.http_client,
            &args.account_id,
            &args.external_script_ids,
        )
        .map_err(ProviderError::Api)?;

        let task = get_latest_builds_by_scripts_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get build by uuid.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_build_by_uuid(
        &self,
        args: &GetBuildByUuidArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_build_by_uuid_builder(
            &self.http_client,
            &args.account_id,
            &args.build_uuid,
        )
        .map_err(ProviderError::Api)?;

        let task = get_build_by_uuid_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cancel build by uuid.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cancel_build_by_uuid(
        &self,
        args: &CancelBuildByUuidArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cancel_build_by_uuid_builder(
            &self.http_client,
            &args.account_id,
            &args.build_uuid,
        )
        .map_err(ProviderError::Api)?;

        let task = cancel_build_by_uuid_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get build logs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_build_logs(
        &self,
        args: &GetBuildLogsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_build_logs_builder(
            &self.http_client,
            &args.account_id,
            &args.build_uuid,
            &args.cursor,
        )
        .map_err(ProviderError::Api)?;

        let task = get_build_logs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Upsert repo connection.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn upsert_repo_connection(
        &self,
        args: &UpsertRepoConnectionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = upsert_repo_connection_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = upsert_repo_connection_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete repo connection.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_repo_connection(
        &self,
        args: &DeleteRepoConnectionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_repo_connection_builder(
            &self.http_client,
            &args.account_id,
            &args.repo_connection_uuid,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_repo_connection_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get worker config autofill.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_worker_config_autofill(
        &self,
        args: &GetWorkerConfigAutofillArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_worker_config_autofill_builder(
            &self.http_client,
            &args.provider_type,
            &args.provider_account_id,
            &args.repo_id,
            &args.branch,
            &args.root_directory,
        )
        .map_err(ProviderError::Api)?;

        let task = get_worker_config_autofill_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List build tokens.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_build_tokens(
        &self,
        args: &ListBuildTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_build_tokens_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = list_build_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create build token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_build_token(
        &self,
        args: &CreateBuildTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_build_token_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_build_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete build token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_build_token(
        &self,
        args: &DeleteBuildTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_build_token_builder(
            &self.http_client,
            &args.build_token_uuid,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_build_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create trigger.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_trigger(
        &self,
        args: &CreateTriggerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_trigger_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_trigger_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update trigger.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_trigger(
        &self,
        args: &UpdateTriggerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_trigger_builder(
            &self.http_client,
            &args.account_id,
            &args.trigger_uuid,
        )
        .map_err(ProviderError::Api)?;

        let task = update_trigger_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete trigger.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_trigger(
        &self,
        args: &DeleteTriggerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_trigger_builder(
            &self.http_client,
            &args.account_id,
            &args.trigger_uuid,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_trigger_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create manual build.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_manual_build(
        &self,
        args: &CreateManualBuildArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_manual_build_builder(
            &self.http_client,
            &args.account_id,
            &args.trigger_uuid,
        )
        .map_err(ProviderError::Api)?;

        let task = create_manual_build_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List environment variables.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_environment_variables(
        &self,
        args: &ListEnvironmentVariablesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_environment_variables_builder(
            &self.http_client,
            &args.account_id,
            &args.trigger_uuid,
        )
        .map_err(ProviderError::Api)?;

        let task = list_environment_variables_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Upsert environment variables.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn upsert_environment_variables(
        &self,
        args: &UpsertEnvironmentVariablesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = upsert_environment_variables_builder(
            &self.http_client,
            &args.account_id,
            &args.trigger_uuid,
        )
        .map_err(ProviderError::Api)?;

        let task = upsert_environment_variables_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete environment variable.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_environment_variable(
        &self,
        args: &DeleteEnvironmentVariableArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_environment_variable_builder(
            &self.http_client,
            &args.environment_variable_key,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_environment_variable_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Purge build cache.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn purge_build_cache(
        &self,
        args: &PurgeBuildCacheArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = purge_build_cache_builder(
            &self.http_client,
            &args.account_id,
            &args.trigger_uuid,
        )
        .map_err(ProviderError::Api)?;

        let task = purge_build_cache_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List builds by script.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_builds_by_script(
        &self,
        args: &ListBuildsByScriptArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_builds_by_script_builder(
            &self.http_client,
            &args.external_script_id,
        )
        .map_err(ProviderError::Api)?;

        let task = list_builds_by_script_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List triggers by script.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_triggers_by_script(
        &self,
        args: &ListTriggersByScriptArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_triggers_by_script_builder(
            &self.http_client,
            &args.external_script_id,
        )
        .map_err(ProviderError::Api)?;

        let task = list_triggers_by_script_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Calls apps list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CallsAppResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn calls_apps_list(
        &self,
        args: &CallsAppsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CallsAppResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = calls_apps_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = calls_apps_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Calls apps create a new app.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CallsAppResponseSingleWithSecret result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn calls_apps_create_a_new_app(
        &self,
        args: &CallsAppsCreateANewAppArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CallsAppResponseSingleWithSecret, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = calls_apps_create_a_new_app_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = calls_apps_create_a_new_app_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Calls apps retrieve app details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CallsAppResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn calls_apps_retrieve_app_details(
        &self,
        args: &CallsAppsRetrieveAppDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CallsAppResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = calls_apps_retrieve_app_details_builder(
            &self.http_client,
            &args.app_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = calls_apps_retrieve_app_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Calls apps update app details.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CallsAppResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn calls_apps_update_app_details(
        &self,
        args: &CallsAppsUpdateAppDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CallsAppResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = calls_apps_update_app_details_builder(
            &self.http_client,
            &args.app_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = calls_apps_update_app_details_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Calls apps delete app.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CallsAppResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn calls_apps_delete_app(
        &self,
        args: &CallsAppsDeleteAppArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CallsAppResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = calls_apps_delete_app_builder(
            &self.http_client,
            &args.app_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = calls_apps_delete_app_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Calls turn key list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CallsTurnKeyCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn calls_turn_key_list(
        &self,
        args: &CallsTurnKeyListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CallsTurnKeyCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = calls_turn_key_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = calls_turn_key_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Calls turn key create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CallsTurnKeySingleWithSecret result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn calls_turn_key_create(
        &self,
        args: &CallsTurnKeyCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CallsTurnKeySingleWithSecret, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = calls_turn_key_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = calls_turn_key_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Calls retrieve turn key details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CallsTurnKeyResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn calls_retrieve_turn_key_details(
        &self,
        args: &CallsRetrieveTurnKeyDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CallsTurnKeyResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = calls_retrieve_turn_key_details_builder(
            &self.http_client,
            &args.key_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = calls_retrieve_turn_key_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Calls update turn key.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CallsTurnKeyResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn calls_update_turn_key(
        &self,
        args: &CallsUpdateTurnKeyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CallsTurnKeyResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = calls_update_turn_key_builder(
            &self.http_client,
            &args.key_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = calls_update_turn_key_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Calls delete turn key.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CallsTurnKeyResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn calls_delete_turn_key(
        &self,
        args: &CallsDeleteTurnKeyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CallsTurnKeyResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = calls_delete_turn_key_builder(
            &self.http_client,
            &args.key_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = calls_delete_turn_key_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel list cloudflare tunnels.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelCfdTunnelResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_tunnel_list_cloudflare_tunnels(
        &self,
        args: &CloudflareTunnelListCloudflareTunnelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelCfdTunnelResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_list_cloudflare_tunnels_builder(
            &self.http_client,
            &args.account_id,
            &args.name,
            &args.is_deleted,
            &args.existed_at,
            &args.uuid,
            &args.was_active_at,
            &args.was_inactive_at,
            &args.include_prefix,
            &args.exclude_prefix,
            &args.status,
            &args.per_page,
            &args.page,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_list_cloudflare_tunnels_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel create a cloudflare tunnel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelCfdTunnelResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudflare_tunnel_create_a_cloudflare_tunnel(
        &self,
        args: &CloudflareTunnelCreateACloudflareTunnelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelCfdTunnelResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_create_a_cloudflare_tunnel_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_create_a_cloudflare_tunnel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel get a cloudflare tunnel.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelCfdTunnelResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_tunnel_get_a_cloudflare_tunnel(
        &self,
        args: &CloudflareTunnelGetACloudflareTunnelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelCfdTunnelResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_get_a_cloudflare_tunnel_builder(
            &self.http_client,
            &args.account_id,
            &args.tunnel_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_get_a_cloudflare_tunnel_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel update a cloudflare tunnel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelCfdTunnelResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudflare_tunnel_update_a_cloudflare_tunnel(
        &self,
        args: &CloudflareTunnelUpdateACloudflareTunnelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelCfdTunnelResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_update_a_cloudflare_tunnel_builder(
            &self.http_client,
            &args.tunnel_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_update_a_cloudflare_tunnel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel delete a cloudflare tunnel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelCfdTunnelResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudflare_tunnel_delete_a_cloudflare_tunnel(
        &self,
        args: &CloudflareTunnelDeleteACloudflareTunnelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelCfdTunnelResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_delete_a_cloudflare_tunnel_builder(
            &self.http_client,
            &args.account_id,
            &args.tunnel_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_delete_a_cloudflare_tunnel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel configuration get configuration.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelConfigurationResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_tunnel_configuration_get_configuration(
        &self,
        args: &CloudflareTunnelConfigurationGetConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelConfigurationResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_configuration_get_configuration_builder(
            &self.http_client,
            &args.account_id,
            &args.tunnel_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_configuration_get_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel configuration put configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelConfigurationResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudflare_tunnel_configuration_put_configuration(
        &self,
        args: &CloudflareTunnelConfigurationPutConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelConfigurationResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_configuration_put_configuration_builder(
            &self.http_client,
            &args.account_id,
            &args.tunnel_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_configuration_put_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel list cloudflare tunnel connections.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelTunnelConnectionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_tunnel_list_cloudflare_tunnel_connections(
        &self,
        args: &CloudflareTunnelListCloudflareTunnelConnectionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelTunnelConnectionsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_list_cloudflare_tunnel_connections_builder(
            &self.http_client,
            &args.account_id,
            &args.tunnel_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_list_cloudflare_tunnel_connections_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel clean up cloudflare tunnel connections.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelEmptyResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudflare_tunnel_clean_up_cloudflare_tunnel_connections(
        &self,
        args: &CloudflareTunnelCleanUpCloudflareTunnelConnectionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelEmptyResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_clean_up_cloudflare_tunnel_connections_builder(
            &self.http_client,
            &args.account_id,
            &args.tunnel_id,
            &args.client_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_clean_up_cloudflare_tunnel_connections_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel get cloudflare tunnel connector.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelTunnelClientResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_tunnel_get_cloudflare_tunnel_connector(
        &self,
        args: &CloudflareTunnelGetCloudflareTunnelConnectorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelTunnelClientResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_get_cloudflare_tunnel_connector_builder(
            &self.http_client,
            &args.account_id,
            &args.tunnel_id,
            &args.connector_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_get_cloudflare_tunnel_connector_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel get a cloudflare tunnel management token.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelTunnelResponseToken result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_tunnel_get_a_cloudflare_tunnel_management_token(
        &self,
        args: &CloudflareTunnelGetACloudflareTunnelManagementTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelTunnelResponseToken, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_get_a_cloudflare_tunnel_management_token_builder(
            &self.http_client,
            &args.account_id,
            &args.tunnel_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_get_a_cloudflare_tunnel_management_token_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel get a cloudflare tunnel token.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelTunnelResponseToken result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_tunnel_get_a_cloudflare_tunnel_token(
        &self,
        args: &CloudflareTunnelGetACloudflareTunnelTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelTunnelResponseToken, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_get_a_cloudflare_tunnel_token_builder(
            &self.http_client,
            &args.account_id,
            &args.tunnel_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_get_a_cloudflare_tunnel_token_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Accounts turnstile widgets list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn accounts_turnstile_widgets_list(
        &self,
        args: &AccountsTurnstileWidgetsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = accounts_turnstile_widgets_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = accounts_turnstile_widgets_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Accounts turnstile widget create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn accounts_turnstile_widget_create(
        &self,
        args: &AccountsTurnstileWidgetCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = accounts_turnstile_widget_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = accounts_turnstile_widget_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Accounts turnstile widget get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn accounts_turnstile_widget_get(
        &self,
        args: &AccountsTurnstileWidgetGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = accounts_turnstile_widget_get_builder(
            &self.http_client,
            &args.account_id,
            &args.sitekey,
        )
        .map_err(ProviderError::Api)?;

        let task = accounts_turnstile_widget_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Accounts turnstile widget update.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn accounts_turnstile_widget_update(
        &self,
        args: &AccountsTurnstileWidgetUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = accounts_turnstile_widget_update_builder(
            &self.http_client,
            &args.account_id,
            &args.sitekey,
        )
        .map_err(ProviderError::Api)?;

        let task = accounts_turnstile_widget_update_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Accounts turnstile widget delete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn accounts_turnstile_widget_delete(
        &self,
        args: &AccountsTurnstileWidgetDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = accounts_turnstile_widget_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.sitekey,
        )
        .map_err(ProviderError::Api)?;

        let task = accounts_turnstile_widget_delete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Accounts turnstile widget rotate secret.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn accounts_turnstile_widget_rotate_secret(
        &self,
        args: &AccountsTurnstileWidgetRotateSecretArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = accounts_turnstile_widget_rotate_secret_builder(
            &self.http_client,
            &args.account_id,
            &args.sitekey,
        )
        .map_err(ProviderError::Api)?;

        let task = accounts_turnstile_widget_rotate_secret_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post bin db post.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_bin_db_post(
        &self,
        args: &PostBinDbPostArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_bin_db_post_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_bin_db_post_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get bin db get binary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_bin_db_get_binary(
        &self,
        args: &GetBinDbGetBinaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_bin_db_get_binary_builder(
            &self.http_client,
            &args.account_id,
            &args.hash,
        )
        .map_err(ProviderError::Api)?;

        let task = get_bin_db_get_binary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get event list get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_event_list_get(
        &self,
        args: &GetEventListGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_event_list_get_builder(
            &self.http_client,
            &args.account_id,
            &args.cursor,
            &args.search,
            &args.page,
            &args.pageSize,
            &args.orderBy,
            &args.order,
            &args.datasetId,
            &args.forceRefresh,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = get_event_list_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get event aggregate.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_event_aggregate(
        &self,
        args: &GetEventAggregateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_event_aggregate_builder(
            &self.http_client,
            &args.account_id,
            &args.aggregateBy,
            &args.datasetId,
            &args.startDate,
            &args.endDate,
            &args.groupByDate,
            &args.limit,
        )
        .map_err(ProviderError::Api)?;

        let task = get_event_aggregate_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get attacker list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_attacker_list(
        &self,
        args: &GetAttackerListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_attacker_list_builder(
            &self.http_client,
            &args.account_id,
            &args.datasetIds,
        )
        .map_err(ProviderError::Api)?;

        let task = get_attacker_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get category list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_category_list(
        &self,
        args: &GetCategoryListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_category_list_builder(
            &self.http_client,
            &args.account_id,
            &args.datasetIds,
        )
        .map_err(ProviderError::Api)?;

        let task = get_category_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get category list complete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_category_list_complete(
        &self,
        args: &GetCategoryListCompleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_category_list_complete_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_category_list_complete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post category create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_category_create(
        &self,
        args: &PostCategoryCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_category_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_category_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get category read.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_category_read(
        &self,
        args: &GetCategoryReadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_category_read_builder(
            &self.http_client,
            &args.account_id,
            &args.category_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_category_read_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post category update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_category_update(
        &self,
        args: &PostCategoryUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_category_update_builder(
            &self.http_client,
            &args.account_id,
            &args.category_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_category_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Patch category update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn patch_category_update(
        &self,
        args: &PatchCategoryUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = patch_category_update_builder(
            &self.http_client,
            &args.account_id,
            &args.category_id,
        )
        .map_err(ProviderError::Api)?;

        let task = patch_category_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete category delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_category_delete(
        &self,
        args: &DeleteCategoryDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_category_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.category_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_category_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get country read.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_country_read(
        &self,
        args: &GetCountryReadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_country_read_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_country_read_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post event create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_event_create(
        &self,
        args: &PostEventCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_event_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_event_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post event create bulk.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_event_create_bulk(
        &self,
        args: &PostEventCreateBulkArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_event_create_bulk_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_event_create_bulk_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post dos event create bulk with relationships.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_dos_event_create_bulk_with_relationships(
        &self,
        args: &PostDosEventCreateBulkWithRelationshipsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_dos_event_create_bulk_with_relationships_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_dos_event_create_bulk_with_relationships_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get dataset list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_dataset_list(
        &self,
        args: &GetDatasetListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_dataset_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_dataset_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get group list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_group_list(
        &self,
        args: &GetGroupListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_group_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_group_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post group create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_group_create(
        &self,
        args: &PostGroupCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_group_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_group_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get group read.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_group_read(
        &self,
        args: &GetGroupReadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_group_read_builder(
            &self.http_client,
            &args.account_id,
            &args.group_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_group_read_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Put group update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn put_group_update(
        &self,
        args: &PutGroupUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = put_group_update_builder(
            &self.http_client,
            &args.account_id,
            &args.group_id,
        )
        .map_err(ProviderError::Api)?;

        let task = put_group_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete group delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_group_delete(
        &self,
        args: &DeleteGroupDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_group_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.group_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_group_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get group member list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_group_member_list(
        &self,
        args: &GetGroupMemberListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_group_member_list_builder(
            &self.http_client,
            &args.account_id,
            &args.group_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_group_member_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post group member create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_group_member_create(
        &self,
        args: &PostGroupMemberCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_group_member_create_builder(
            &self.http_client,
            &args.account_id,
            &args.group_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_group_member_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete group member delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_group_member_delete(
        &self,
        args: &DeleteGroupMemberDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_group_member_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.group_id,
            &args.member_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_group_member_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post dataset create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_dataset_create(
        &self,
        args: &PostDatasetCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_dataset_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_dataset_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get dataset read.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_dataset_read(
        &self,
        args: &GetDatasetReadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_dataset_read_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_dataset_read_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post dataset update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_dataset_update(
        &self,
        args: &PostDatasetUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_dataset_update_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_dataset_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Patch dataset update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn patch_dataset_update(
        &self,
        args: &PatchDatasetUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = patch_dataset_update_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = patch_dataset_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete dataset delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_dataset_delete(
        &self,
        args: &DeleteDatasetDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_dataset_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_dataset_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post event copy to new ds.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_event_copy_to_new_ds(
        &self,
        args: &PostEventCopyToNewDsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_event_copy_to_new_ds_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
            &args.keepRawData,
        )
        .map_err(ProviderError::Api)?;

        let task = post_event_copy_to_new_ds_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get event read.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_event_read(
        &self,
        args: &GetEventReadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_event_read_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
            &args.event_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_event_read_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post indicator type create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_indicator_type_create(
        &self,
        args: &PostIndicatorTypeCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_indicator_type_create_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_indicator_type_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get indicator list legacy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_indicator_list_legacy(
        &self,
        args: &GetIndicatorListLegacyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_indicator_list_legacy_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
            &args.page,
            &args.pageSize,
            &args.name,
            &args.indicatorType,
            &args.relatedEvent,
        )
        .map_err(ProviderError::Api)?;

        let task = get_indicator_list_legacy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post indicator create bulk.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_indicator_create_bulk(
        &self,
        args: &PostIndicatorCreateBulkArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_indicator_create_bulk_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_indicator_create_bulk_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post indicator create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_indicator_create(
        &self,
        args: &PostIndicatorCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_indicator_create_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_indicator_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get indicator tags list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_indicator_tags_list(
        &self,
        args: &GetIndicatorTagsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_indicator_tags_list_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_indicator_tags_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get indicator read.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_indicator_read(
        &self,
        args: &GetIndicatorReadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_indicator_read_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
            &args.indicator_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_indicator_read_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Patch indicator update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn patch_indicator_update(
        &self,
        args: &PatchIndicatorUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = patch_indicator_update_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
            &args.indicator_id,
        )
        .map_err(ProviderError::Api)?;

        let task = patch_indicator_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete indicator delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_indicator_delete(
        &self,
        args: &DeleteIndicatorDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_indicator_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
            &args.indicator_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_indicator_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post event move to new ds.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_event_move_to_new_ds(
        &self,
        args: &PostEventMoveToNewDsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_event_move_to_new_ds_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
            &args.keepRawData,
        )
        .map_err(ProviderError::Api)?;

        let task = post_event_move_to_new_ds_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get permission list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_permission_list(
        &self,
        args: &GetPermissionListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_permission_list_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_permission_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post permission create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_permission_create(
        &self,
        args: &PostPermissionCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_permission_create_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_permission_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Put permission update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn put_permission_update(
        &self,
        args: &PutPermissionUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = put_permission_update_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
            &args.grant_id,
        )
        .map_err(ProviderError::Api)?;

        let task = put_permission_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete permission delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_permission_delete(
        &self,
        args: &DeletePermissionDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_permission_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
            &args.grant_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_permission_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get tag indicators list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_tag_indicators_list(
        &self,
        args: &GetTagIndicatorsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_tag_indicators_list_builder(
            &self.http_client,
            &args.account_id,
            &args.tag_uuid,
            &args.dataset_id,
            &args.page,
            &args.pageSize,
            &args.indicatorType,
            &args.relatedEvent,
        )
        .map_err(ProviderError::Api)?;

        let task = get_tag_indicators_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get target industry list by dataset.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_target_industry_list_by_dataset(
        &self,
        args: &GetTargetIndustryListByDatasetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_target_industry_list_by_dataset_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_target_industry_list_by_dataset_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post dataset populate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_dataset_populate(
        &self,
        args: &PostDatasetPopulateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_dataset_populate_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_dataset_populate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete event tag delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_event_tag_delete(
        &self,
        args: &DeleteEventTagDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_event_tag_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.event_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_event_tag_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post event tag create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_event_tag_create(
        &self,
        args: &PostEventTagCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_event_tag_create_builder(
            &self.http_client,
            &args.account_id,
            &args.event_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_event_tag_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post event graph ql.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_event_graph_ql(
        &self,
        args: &PostEventGraphQlArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_event_graph_ql_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_event_graph_ql_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get indicator types list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_indicator_types_list(
        &self,
        args: &GetIndicatorTypesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_indicator_types_list_builder(
            &self.http_client,
            &args.account_id,
            &args.datasetIds,
        )
        .map_err(ProviderError::Api)?;

        let task = get_indicator_types_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get legacy indicator types list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_legacy_indicator_types_list(
        &self,
        args: &GetLegacyIndicatorTypesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_legacy_indicator_types_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_legacy_indicator_types_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get indicator list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_indicator_list(
        &self,
        args: &GetIndicatorListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_indicator_list_builder(
            &self.http_client,
            &args.account_id,
            &args.datasetIds,
            &args.page,
            &args.pageSize,
            &args.search,
            &args.name,
            &args.indicatorType,
            &args.relatedEvents,
            &args.tags,
            &args.createdAfter,
            &args.createdBefore,
            &args.relatedEventsLimit,
            &args.includeTags,
            &args.includeTotalCount,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = get_indicator_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get event query list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_event_query_list(
        &self,
        args: &GetEventQueryListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_event_query_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_event_query_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get event query alert list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_event_query_alert_list(
        &self,
        args: &GetEventQueryAlertListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_event_query_alert_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_event_query_alert_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post event query alert create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_event_query_alert_create(
        &self,
        args: &PostEventQueryAlertCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_event_query_alert_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_event_query_alert_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get event query alert read.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_event_query_alert_read(
        &self,
        args: &GetEventQueryAlertReadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_event_query_alert_read_builder(
            &self.http_client,
            &args.account_id,
            &args.alert_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_event_query_alert_read_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post event query alert update.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn post_event_query_alert_update(
        &self,
        args: &PostEventQueryAlertUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_event_query_alert_update_builder(
            &self.http_client,
            &args.account_id,
            &args.alert_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_event_query_alert_update_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Patch event query alert update.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn patch_event_query_alert_update(
        &self,
        args: &PatchEventQueryAlertUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = patch_event_query_alert_update_builder(
            &self.http_client,
            &args.account_id,
            &args.alert_id,
        )
        .map_err(ProviderError::Api)?;

        let task = patch_event_query_alert_update_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete event query alert delete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn delete_event_query_alert_delete(
        &self,
        args: &DeleteEventQueryAlertDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_event_query_alert_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.alert_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_event_query_alert_delete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post event query create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_event_query_create(
        &self,
        args: &PostEventQueryCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_event_query_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_event_query_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get event query read.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_event_query_read(
        &self,
        args: &GetEventQueryReadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_event_query_read_builder(
            &self.http_client,
            &args.account_id,
            &args.query_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_event_query_read_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post event query update.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn post_event_query_update(
        &self,
        args: &PostEventQueryUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_event_query_update_builder(
            &self.http_client,
            &args.account_id,
            &args.query_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_event_query_update_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Patch event query update.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn patch_event_query_update(
        &self,
        args: &PatchEventQueryUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = patch_event_query_update_builder(
            &self.http_client,
            &args.account_id,
            &args.query_id,
        )
        .map_err(ProviderError::Api)?;

        let task = patch_event_query_update_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete event query delete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn delete_event_query_delete(
        &self,
        args: &DeleteEventQueryDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_event_query_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.query_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_event_query_delete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get event raw read ds.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn get_event_raw_read_ds(
        &self,
        args: &GetEventRawReadDsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_event_raw_read_ds_builder(
            &self.http_client,
            &args.account_id,
            &args.event_id,
            &args.dataset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_event_raw_read_ds_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete event reference delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_event_reference_delete(
        &self,
        args: &DeleteEventReferenceDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_event_reference_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.event_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_event_reference_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post event reference create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_event_reference_create(
        &self,
        args: &PostEventReferenceCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_event_reference_create_builder(
            &self.http_client,
            &args.account_id,
            &args.event_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_event_reference_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post create event relationship.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_create_event_relationship(
        &self,
        args: &PostCreateEventRelationshipArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_create_event_relationship_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_create_event_relationship_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get tag list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_tag_list(
        &self,
        args: &GetTagListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_tag_list_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.pageSize,
            &args.search,
            &args.categoryUuid,
        )
        .map_err(ProviderError::Api)?;

        let task = get_tag_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get tag category list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_tag_category_list(
        &self,
        args: &GetTagCategoryListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_tag_category_list_builder(
            &self.http_client,
            &args.account_id,
            &args.search,
        )
        .map_err(ProviderError::Api)?;

        let task = get_tag_category_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post tag category create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_tag_category_create(
        &self,
        args: &PostTagCategoryCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_tag_category_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_tag_category_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Patch tag category update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn patch_tag_category_update(
        &self,
        args: &PatchTagCategoryUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = patch_tag_category_update_builder(
            &self.http_client,
            &args.account_id,
            &args.category_uuid,
        )
        .map_err(ProviderError::Api)?;

        let task = patch_tag_category_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete tag category delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_tag_category_delete(
        &self,
        args: &DeleteTagCategoryDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_tag_category_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.category_uuid,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_tag_category_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post tag create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_tag_create(
        &self,
        args: &PostTagCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_tag_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_tag_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Patch tag update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn patch_tag_update(
        &self,
        args: &PatchTagUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = patch_tag_update_builder(
            &self.http_client,
            &args.account_id,
            &args.tag_uuid,
        )
        .map_err(ProviderError::Api)?;

        let task = patch_tag_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete tag delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_tag_delete(
        &self,
        args: &DeleteTagDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_tag_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.tag_uuid,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_tag_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get target industry list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_target_industry_list(
        &self,
        args: &GetTargetIndustryListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_target_industry_list_builder(
            &self.http_client,
            &args.account_id,
            &args.datasetIds,
        )
        .map_err(ProviderError::Api)?;

        let task = get_target_industry_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get target industry list complete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_target_industry_list_complete(
        &self,
        args: &GetTargetIndustryListCompleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_target_industry_list_complete_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_target_industry_list_complete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Patch event update bulk.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn patch_event_update_bulk(
        &self,
        args: &PatchEventUpdateBulkArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = patch_event_update_bulk_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = patch_event_update_bulk_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete event delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_event_delete(
        &self,
        args: &DeleteEventDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_event_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
            &args.eventIds,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_event_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post event do revert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_event_do_revert(
        &self,
        args: &PostEventDoRevertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_event_do_revert_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_event_do_revert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get event read deprecated.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn get_event_read_deprecated(
        &self,
        args: &GetEventReadDeprecatedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_event_read_deprecated_builder(
            &self.http_client,
            &args.account_id,
            &args.event_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_event_read_deprecated_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post event update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_event_update(
        &self,
        args: &PostEventUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_event_update_builder(
            &self.http_client,
            &args.account_id,
            &args.event_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_event_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Patch event update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn patch_event_update(
        &self,
        args: &PatchEventUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = patch_event_update_builder(
            &self.http_client,
            &args.account_id,
            &args.event_id,
        )
        .map_err(ProviderError::Api)?;

        let task = patch_event_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get event raw read.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_event_raw_read(
        &self,
        args: &GetEventRawReadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_event_raw_read_builder(
            &self.http_client,
            &args.account_id,
            &args.event_id,
            &args.raw_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_event_raw_read_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post event raw update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_event_raw_update(
        &self,
        args: &PostEventRawUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_event_raw_update_builder(
            &self.http_client,
            &args.account_id,
            &args.event_id,
            &args.raw_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_event_raw_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Patch event raw update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn patch_event_raw_update(
        &self,
        args: &PatchEventRawUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = patch_event_raw_update_builder(
            &self.http_client,
            &args.account_id,
            &args.event_id,
            &args.raw_id,
        )
        .map_err(ProviderError::Api)?;

        let task = patch_event_raw_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get event relationships.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_event_relationships(
        &self,
        args: &GetEventRelationshipsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_event_relationships_builder(
            &self.http_client,
            &args.account_id,
            &args.event_id,
            &args.direction,
            &args.maxDepth,
            &args.relationshipTypes,
            &args.indicatorTypeIds,
            &args.datasetId,
            &args.includeParent,
            &args.page,
            &args.pageSize,
        )
        .map_err(ProviderError::Api)?;

        let task = get_event_relationships_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one request list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudforce_one_request_list(
        &self,
        args: &CloudforceOneRequestListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_request_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_request_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one request constants.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudforce_one_request_constants(
        &self,
        args: &CloudforceOneRequestConstantsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_request_constants_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_request_constants_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one request new.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudforce_one_request_new(
        &self,
        args: &CloudforceOneRequestNewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_request_new_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_request_new_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one priority list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudforce_one_priority_list(
        &self,
        args: &CloudforceOnePriorityListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_priority_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_priority_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one priority new.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudforce_one_priority_new(
        &self,
        args: &CloudforceOnePriorityNewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_priority_new_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_priority_new_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one priority quota.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudforce_one_priority_quota(
        &self,
        args: &CloudforceOnePriorityQuotaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_priority_quota_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_priority_quota_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one priority get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudforce_one_priority_get(
        &self,
        args: &CloudforceOnePriorityGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_priority_get_builder(
            &self.http_client,
            &args.account_id,
            &args.priority_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_priority_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one priority update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudforce_one_priority_update(
        &self,
        args: &CloudforceOnePriorityUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_priority_update_builder(
            &self.http_client,
            &args.account_id,
            &args.priority_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_priority_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one priority delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CloudforceOneRequestsApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudforce_one_priority_delete(
        &self,
        args: &CloudforceOnePriorityDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CloudforceOneRequestsApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_priority_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.priority_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_priority_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one request quota.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudforce_one_request_quota(
        &self,
        args: &CloudforceOneRequestQuotaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_request_quota_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_request_quota_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one request types.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudforce_one_request_types(
        &self,
        args: &CloudforceOneRequestTypesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_request_types_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_request_types_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one request get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudforce_one_request_get(
        &self,
        args: &CloudforceOneRequestGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_request_get_builder(
            &self.http_client,
            &args.account_id,
            &args.request_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_request_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one request update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudforce_one_request_update(
        &self,
        args: &CloudforceOneRequestUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_request_update_builder(
            &self.http_client,
            &args.account_id,
            &args.request_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_request_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one request delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CloudforceOneRequestsApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudforce_one_request_delete(
        &self,
        args: &CloudforceOneRequestDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CloudforceOneRequestsApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_request_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.request_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_request_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one request asset list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudforce_one_request_asset_list(
        &self,
        args: &CloudforceOneRequestAssetListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_request_asset_list_builder(
            &self.http_client,
            &args.account_id,
            &args.request_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_request_asset_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one request asset new.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudforce_one_request_asset_new(
        &self,
        args: &CloudforceOneRequestAssetNewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_request_asset_new_builder(
            &self.http_client,
            &args.account_id,
            &args.request_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_request_asset_new_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one request asset get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudforce_one_request_asset_get(
        &self,
        args: &CloudforceOneRequestAssetGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_request_asset_get_builder(
            &self.http_client,
            &args.account_id,
            &args.request_id,
            &args.asset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_request_asset_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one request asset update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudforce_one_request_asset_update(
        &self,
        args: &CloudforceOneRequestAssetUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_request_asset_update_builder(
            &self.http_client,
            &args.account_id,
            &args.request_id,
            &args.asset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_request_asset_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one request asset delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CloudforceOneRequestsApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudforce_one_request_asset_delete(
        &self,
        args: &CloudforceOneRequestAssetDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CloudforceOneRequestsApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_request_asset_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.request_id,
            &args.asset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_request_asset_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one request message list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudforce_one_request_message_list(
        &self,
        args: &CloudforceOneRequestMessageListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_request_message_list_builder(
            &self.http_client,
            &args.account_id,
            &args.request_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_request_message_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one request message new.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudforce_one_request_message_new(
        &self,
        args: &CloudforceOneRequestMessageNewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_request_message_new_builder(
            &self.http_client,
            &args.account_id,
            &args.request_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_request_message_new_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one request message update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudforce_one_request_message_update(
        &self,
        args: &CloudforceOneRequestMessageUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_request_message_update_builder(
            &self.http_client,
            &args.account_id,
            &args.request_id,
            &args.message_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_request_message_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one request message delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CloudforceOneRequestsApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudforce_one_request_message_delete(
        &self,
        args: &CloudforceOneRequestMessageDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CloudforceOneRequestsApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_request_message_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.request_id,
            &args.message_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_request_message_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one list rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CloudforceOneRulesListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudforce_one_list_rules(
        &self,
        args: &CloudforceOneListRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CloudforceOneRulesListResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_list_rules_builder(
            &self.http_client,
            &args.account_id,
            &args.namespace,
            &args.recursive,
            &args.search,
            &args.is_public,
            &args.limit,
            &args.offset,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_list_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one create rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CloudforceOneRule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudforce_one_create_rule(
        &self,
        args: &CloudforceOneCreateRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CloudforceOneRule, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_create_rule_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_create_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one delete all rules.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CloudforceOneDeleteAllResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudforce_one_delete_all_rules(
        &self,
        args: &CloudforceOneDeleteAllRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CloudforceOneDeleteAllResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_delete_all_rules_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_delete_all_rules_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one get rule stats.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CloudforceOneStatsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudforce_one_get_rule_stats(
        &self,
        args: &CloudforceOneGetRuleStatsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CloudforceOneStatsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_get_rule_stats_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_get_rule_stats_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one get rule tree.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CloudforceOneTreeResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudforce_one_get_rule_tree(
        &self,
        args: &CloudforceOneGetRuleTreeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CloudforceOneTreeResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_get_rule_tree_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_get_rule_tree_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one validate rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CloudforceOneValidationResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudforce_one_validate_rule(
        &self,
        args: &CloudforceOneValidateRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CloudforceOneValidationResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_validate_rule_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_validate_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one get rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CloudforceOneRule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudforce_one_get_rule(
        &self,
        args: &CloudforceOneGetRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CloudforceOneRule, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_get_rule_builder(
            &self.http_client,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_get_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one update rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CloudforceOneRule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudforce_one_update_rule(
        &self,
        args: &CloudforceOneUpdateRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CloudforceOneRule, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_update_rule_builder(
            &self.http_client,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_update_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudforce one delete rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CloudforceOneSuccessResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudforce_one_delete_rule(
        &self,
        args: &CloudforceOneDeleteRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CloudforceOneSuccessResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudforce_one_delete_rule_builder(
            &self.http_client,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudforce_one_delete_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get config fetch.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_config_fetch(
        &self,
        args: &GetConfigFetchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_config_fetch_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_config_fetch_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post config create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_config_create(
        &self,
        args: &PostConfigCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_config_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_config_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post config update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_config_update(
        &self,
        args: &PostConfigUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_config_update_builder(
            &self.http_client,
            &args.account_id,
            &args.config_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_config_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete delete scans.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_delete_scans(
        &self,
        args: &DeleteDeleteScansArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_delete_scans_builder(
            &self.http_client,
            &args.account_id,
            &args.config_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_delete_scans_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get get open ports.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_get_open_ports(
        &self,
        args: &GetGetOpenPortsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_get_open_ports_builder(
            &self.http_client,
            &args.account_id,
            &args.config_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_get_open_ports_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get domain match list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_domain_match_list(
        &self,
        args: &GetDomainMatchListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_domain_match_list_builder(
            &self.http_client,
            &args.account_id,
            &args.offset,
            &args.limit,
            &args.query_id,
            &args.include_domain_id,
            &args.include_dismissed,
            &args.domain_search,
            &args.orderBy,
            &args.order,
        )
        .map_err(ProviderError::Api)?;

        let task = get_domain_match_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get get domain queries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_get_domain_queries(
        &self,
        args: &GetGetDomainQueriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_get_domain_queries_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_get_domain_queries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get logo match list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_logo_match_list(
        &self,
        args: &GetLogoMatchListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_logo_match_list_builder(
            &self.http_client,
            &args.account_id,
            &args.offset,
            &args.limit,
            &args.query_id,
            &args.download,
            &args.orderBy,
            &args.order,
        )
        .map_err(ProviderError::Api)?;

        let task = get_logo_match_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get get logo queries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_get_logo_queries(
        &self,
        args: &GetGetLogoQueriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_get_logo_queries_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
            &args.download,
        )
        .map_err(ProviderError::Api)?;

        let task = get_get_logo_queries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post insert logo query.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_insert_logo_query(
        &self,
        args: &PostInsertLogoQueryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_insert_logo_query_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_insert_logo_query_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete delete logo query.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn delete_delete_logo_query(
        &self,
        args: &DeleteDeleteLogoQueryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_delete_logo_query_builder(
            &self.http_client,
            &args.account_id,
            &args.query_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_delete_logo_query_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post search logo similarity.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn post_search_logo_similarity(
        &self,
        args: &PostSearchLogoSimilarityArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_search_logo_similarity_builder(
            &self.http_client,
            &args.account_id,
            &args.showHistoric,
            &args.download,
        )
        .map_err(ProviderError::Api)?;

        let task = post_search_logo_similarity_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get total queries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_total_queries(
        &self,
        args: &GetTotalQueriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_total_queries_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_total_queries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post event graph qlv2.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_event_graph_qlv2(
        &self,
        args: &PostEventGraphQlv2Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_event_graph_qlv2_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_event_graph_qlv2_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List cnis.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NscCniList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_cnis(
        &self,
        args: &ListCnisArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NscCniList, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_cnis_builder(
            &self.http_client,
            &args.account_id,
            &args.slot,
            &args.tunnel_id,
            &args.cursor,
            &args.limit,
        )
        .map_err(ProviderError::Api)?;

        let task = list_cnis_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create cni.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NscCni result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_cni(
        &self,
        args: &CreateCniArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NscCni, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_cni_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_cni_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get cni.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NscCni result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_cni(
        &self,
        args: &GetCniArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NscCni, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_cni_builder(
            &self.http_client,
            &args.cni,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_cni_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update cni.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NscCni result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_cni(
        &self,
        args: &UpdateCniArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NscCni, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_cni_builder(
            &self.http_client,
            &args.cni,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_cni_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete cni.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_cni(
        &self,
        args: &DeleteCniArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_cni_builder(
            &self.http_client,
            &args.cni,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_cni_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List interconnects.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NscInterconnectList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_interconnects(
        &self,
        args: &ListInterconnectsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NscInterconnectList, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_interconnects_builder(
            &self.http_client,
            &args.account_id,
            &args.site,
            &args.type_rs,
            &args.cursor,
            &args.limit,
        )
        .map_err(ProviderError::Api)?;

        let task = list_interconnects_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create interconnect.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NscInterconnect result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_interconnect(
        &self,
        args: &CreateInterconnectArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NscInterconnect, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_interconnect_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_interconnect_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get interconnect.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NscInterconnect result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_interconnect(
        &self,
        args: &GetInterconnectArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NscInterconnect, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_interconnect_builder(
            &self.http_client,
            &args.icon,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_interconnect_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete interconnect.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_interconnect(
        &self,
        args: &DeleteInterconnectArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_interconnect_builder(
            &self.http_client,
            &args.icon,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_interconnect_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get interconnect loa.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_interconnect_loa(
        &self,
        args: &GetInterconnectLoaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_interconnect_loa_builder(
            &self.http_client,
            &args.icon,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_interconnect_loa_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get interconnect status.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NscStatusInfo result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_interconnect_status(
        &self,
        args: &GetInterconnectStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NscStatusInfo, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_interconnect_status_builder(
            &self.http_client,
            &args.icon,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_interconnect_status_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NscSettings result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_settings(
        &self,
        args: &GetSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NscSettings, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_settings_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NscSettings result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_settings(
        &self,
        args: &UpdateSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NscSettings, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_settings_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List slots.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NscSlotList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_slots(
        &self,
        args: &ListSlotsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NscSlotList, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_slots_builder(
            &self.http_client,
            &args.account_id,
            &args.address_contains,
            &args.site,
            &args.speed,
            &args.occupied,
            &args.cursor,
            &args.limit,
        )
        .map_err(ProviderError::Api)?;

        let task = list_slots_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get slot.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NscSlotInfo result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_slot(
        &self,
        args: &GetSlotArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NscSlotInfo, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_slot_builder(
            &self.http_client,
            &args.slot,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_slot_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Connectivity services list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn connectivity_services_list(
        &self,
        args: &ConnectivityServicesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = connectivity_services_list_builder(
            &self.http_client,
            &args.account_id,
            &args.type_rs,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = connectivity_services_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Connectivity services post.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn connectivity_services_post(
        &self,
        args: &ConnectivityServicesPostArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = connectivity_services_post_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = connectivity_services_post_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Connectivity services get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn connectivity_services_get(
        &self,
        args: &ConnectivityServicesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = connectivity_services_get_builder(
            &self.http_client,
            &args.account_id,
            &args.service_id,
        )
        .map_err(ProviderError::Api)?;

        let task = connectivity_services_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Connectivity services put.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn connectivity_services_put(
        &self,
        args: &ConnectivityServicesPutArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = connectivity_services_put_builder(
            &self.http_client,
            &args.account_id,
            &args.service_id,
        )
        .map_err(ProviderError::Api)?;

        let task = connectivity_services_put_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Connectivity services delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn connectivity_services_delete(
        &self,
        args: &ConnectivityServicesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = connectivity_services_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.service_id,
        )
        .map_err(ProviderError::Api)?;

        let task = connectivity_services_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Public list applications.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn public_list_applications(
        &self,
        args: &PublicListApplicationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = public_list_applications_builder(
            &self.http_client,
            &args.account_id,
            &args.name,
            &args.image,
        )
        .map_err(ProviderError::Api)?;

        let task = public_list_applications_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Generate image registry credentials.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn generate_image_registry_credentials(
        &self,
        args: &GenerateImageRegistryCredentialsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = generate_image_registry_credentials_builder(
            &self.http_client,
            &args.domain,
        )
        .map_err(ProviderError::Api)?;

        let task = generate_image_registry_credentials_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account level custom nameservers list account custom nameservers.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsCustomNameserversAcnsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_level_custom_nameservers_list_account_custom_nameservers(
        &self,
        args: &AccountLevelCustomNameserversListAccountCustomNameserversArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsCustomNameserversAcnsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_level_custom_nameservers_list_account_custom_nameservers_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_level_custom_nameservers_list_account_custom_nameservers_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account level custom nameservers add account custom nameserver.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsCustomNameserversAcnsResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_level_custom_nameservers_add_account_custom_nameserver(
        &self,
        args: &AccountLevelCustomNameserversAddAccountCustomNameserverArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsCustomNameserversAcnsResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_level_custom_nameservers_add_account_custom_nameserver_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_level_custom_nameservers_add_account_custom_nameserver_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account level custom nameservers delete account custom nameserver.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsCustomNameserversEmptyResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_level_custom_nameservers_delete_account_custom_nameserver(
        &self,
        args: &AccountLevelCustomNameserversDeleteAccountCustomNameserverArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsCustomNameserversEmptyResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_level_custom_nameservers_delete_account_custom_nameserver_builder(
            &self.http_client,
            &args.custom_ns_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_level_custom_nameservers_delete_account_custom_nameserver_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// D1 list databases.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn d1_list_databases(
        &self,
        args: &D1ListDatabasesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = d1_list_databases_builder(
            &self.http_client,
            &args.account_id,
            &args.name,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = d1_list_databases_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// D1 create database.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn d1_create_database(
        &self,
        args: &D1CreateDatabaseArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = d1_create_database_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = d1_create_database_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// D1 get database.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn d1_get_database(
        &self,
        args: &D1GetDatabaseArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = d1_get_database_builder(
            &self.http_client,
            &args.account_id,
            &args.database_id,
        )
        .map_err(ProviderError::Api)?;

        let task = d1_get_database_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// D1 update database.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn d1_update_database(
        &self,
        args: &D1UpdateDatabaseArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = d1_update_database_builder(
            &self.http_client,
            &args.account_id,
            &args.database_id,
        )
        .map_err(ProviderError::Api)?;

        let task = d1_update_database_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// D1 update partial database.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn d1_update_partial_database(
        &self,
        args: &D1UpdatePartialDatabaseArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = d1_update_partial_database_builder(
            &self.http_client,
            &args.account_id,
            &args.database_id,
        )
        .map_err(ProviderError::Api)?;

        let task = d1_update_partial_database_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// D1 delete database.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn d1_delete_database(
        &self,
        args: &D1DeleteDatabaseArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = d1_delete_database_builder(
            &self.http_client,
            &args.account_id,
            &args.database_id,
        )
        .map_err(ProviderError::Api)?;

        let task = d1_delete_database_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// D1 export database.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn d1_export_database(
        &self,
        args: &D1ExportDatabaseArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = d1_export_database_builder(
            &self.http_client,
            &args.account_id,
            &args.database_id,
        )
        .map_err(ProviderError::Api)?;

        let task = d1_export_database_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// D1 import database.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn d1_import_database(
        &self,
        args: &D1ImportDatabaseArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = d1_import_database_builder(
            &self.http_client,
            &args.account_id,
            &args.database_id,
        )
        .map_err(ProviderError::Api)?;

        let task = d1_import_database_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// D1 query database.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn d1_query_database(
        &self,
        args: &D1QueryDatabaseArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = d1_query_database_builder(
            &self.http_client,
            &args.account_id,
            &args.database_id,
        )
        .map_err(ProviderError::Api)?;

        let task = d1_query_database_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// D1 raw database query.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn d1_raw_database_query(
        &self,
        args: &D1RawDatabaseQueryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = d1_raw_database_query_builder(
            &self.http_client,
            &args.account_id,
            &args.database_id,
        )
        .map_err(ProviderError::Api)?;

        let task = d1_raw_database_query_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// D1 time travel get bookmark.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn d1_time_travel_get_bookmark(
        &self,
        args: &D1TimeTravelGetBookmarkArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = d1_time_travel_get_bookmark_builder(
            &self.http_client,
            &args.account_id,
            &args.database_id,
            &args.timestamp,
        )
        .map_err(ProviderError::Api)?;

        let task = d1_time_travel_get_bookmark_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// D1 time travel restore.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn d1_time_travel_restore(
        &self,
        args: &D1TimeTravelRestoreArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = d1_time_travel_restore_builder(
            &self.http_client,
            &args.account_id,
            &args.database_id,
            &args.bookmark,
            &args.timestamp,
        )
        .map_err(ProviderError::Api)?;

        let task = d1_time_travel_restore_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices list devices.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesDevicesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_list_devices(
        &self,
        args: &DevicesListDevicesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesDevicesResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_list_devices_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_list_devices_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List ip profiles.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_ip_profiles(
        &self,
        args: &ListIpProfilesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_ip_profiles_builder(
            &self.http_client,
            &args.account_id,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = list_ip_profiles_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create ip profile.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_ip_profile(
        &self,
        args: &CreateIpProfileArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_ip_profile_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_ip_profile_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get ip profile.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_ip_profile(
        &self,
        args: &GetIpProfileArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_ip_profile_builder(
            &self.http_client,
            &args.account_id,
            &args.profile_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_ip_profile_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update ip profile.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_ip_profile(
        &self,
        args: &UpdateIpProfileArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_ip_profile_builder(
            &self.http_client,
            &args.account_id,
            &args.profile_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_ip_profile_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete ip profile.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_ip_profile(
        &self,
        args: &DeleteIpProfileArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_ip_profile_builder(
            &self.http_client,
            &args.account_id,
            &args.profile_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_ip_profile_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device managed networks list device managed networks.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn device_managed_networks_list_device_managed_networks(
        &self,
        args: &DeviceManagedNetworksListDeviceManagedNetworksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_managed_networks_list_device_managed_networks_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = device_managed_networks_list_device_managed_networks_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device managed networks create device managed network.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn device_managed_networks_create_device_managed_network(
        &self,
        args: &DeviceManagedNetworksCreateDeviceManagedNetworkArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_managed_networks_create_device_managed_network_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = device_managed_networks_create_device_managed_network_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device managed networks device managed network details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn device_managed_networks_device_managed_network_details(
        &self,
        args: &DeviceManagedNetworksDeviceManagedNetworkDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_managed_networks_device_managed_network_details_builder(
            &self.http_client,
            &args.network_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = device_managed_networks_device_managed_network_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device managed networks update device managed network.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn device_managed_networks_update_device_managed_network(
        &self,
        args: &DeviceManagedNetworksUpdateDeviceManagedNetworkArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_managed_networks_update_device_managed_network_builder(
            &self.http_client,
            &args.network_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = device_managed_networks_update_device_managed_network_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device managed networks delete device managed network.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn device_managed_networks_delete_device_managed_network(
        &self,
        args: &DeviceManagedNetworksDeleteDeviceManagedNetworkArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_managed_networks_delete_device_managed_network_builder(
            &self.http_client,
            &args.network_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = device_managed_networks_delete_device_managed_network_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List devices.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_devices(
        &self,
        args: &ListDevicesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_devices_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = list_devices_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get device.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_device(
        &self,
        args: &GetDeviceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_device_builder(
            &self.http_client,
            &args.device_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_device_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete device.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_device(
        &self,
        args: &DeleteDeviceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_device_builder(
            &self.http_client,
            &args.device_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_device_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Revoke device.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn revoke_device(
        &self,
        args: &RevokeDeviceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = revoke_device_builder(
            &self.http_client,
            &args.account_id,
            &args.device_id,
        )
        .map_err(ProviderError::Api)?;

        let task = revoke_device_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices list device settings policies.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesDeviceSettingsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_list_device_settings_policies(
        &self,
        args: &DevicesListDeviceSettingsPoliciesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesDeviceSettingsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_list_device_settings_policies_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_list_device_settings_policies_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices get default device settings policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesDefaultDeviceSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_get_default_device_settings_policy(
        &self,
        args: &DevicesGetDefaultDeviceSettingsPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesDefaultDeviceSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_get_default_device_settings_policy_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_get_default_device_settings_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices create device settings policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesDeviceSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn devices_create_device_settings_policy(
        &self,
        args: &DevicesCreateDeviceSettingsPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesDeviceSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_create_device_settings_policy_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_create_device_settings_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices update default device settings policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesDefaultDeviceSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn devices_update_default_device_settings_policy(
        &self,
        args: &DevicesUpdateDefaultDeviceSettingsPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesDefaultDeviceSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_update_default_device_settings_policy_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_update_default_device_settings_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices get split tunnel exclude list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesSplitTunnelResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_get_split_tunnel_exclude_list(
        &self,
        args: &DevicesGetSplitTunnelExcludeListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesSplitTunnelResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_get_split_tunnel_exclude_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_get_split_tunnel_exclude_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices set split tunnel exclude list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesSplitTunnelResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_set_split_tunnel_exclude_list(
        &self,
        args: &DevicesSetSplitTunnelExcludeListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesSplitTunnelResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_set_split_tunnel_exclude_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_set_split_tunnel_exclude_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices get local domain fallback list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesFallbackDomainResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_get_local_domain_fallback_list(
        &self,
        args: &DevicesGetLocalDomainFallbackListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesFallbackDomainResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_get_local_domain_fallback_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_get_local_domain_fallback_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices set local domain fallback list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesFallbackDomainResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_set_local_domain_fallback_list(
        &self,
        args: &DevicesSetLocalDomainFallbackListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesFallbackDomainResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_set_local_domain_fallback_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_set_local_domain_fallback_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices get split tunnel include list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesSplitTunnelIncludeResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_get_split_tunnel_include_list(
        &self,
        args: &DevicesGetSplitTunnelIncludeListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesSplitTunnelIncludeResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_get_split_tunnel_include_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_get_split_tunnel_include_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices set split tunnel include list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesSplitTunnelIncludeResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_set_split_tunnel_include_list(
        &self,
        args: &DevicesSetSplitTunnelIncludeListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesSplitTunnelIncludeResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_set_split_tunnel_include_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_set_split_tunnel_include_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices get device settings policy by id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesDeviceSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_get_device_settings_policy_by_id(
        &self,
        args: &DevicesGetDeviceSettingsPolicyByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesDeviceSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_get_device_settings_policy_by_id_builder(
            &self.http_client,
            &args.policy_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_get_device_settings_policy_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices update device settings policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesDeviceSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn devices_update_device_settings_policy(
        &self,
        args: &DevicesUpdateDeviceSettingsPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesDeviceSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_update_device_settings_policy_builder(
            &self.http_client,
            &args.policy_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_update_device_settings_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices delete device settings policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesDeviceSettingsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn devices_delete_device_settings_policy(
        &self,
        args: &DevicesDeleteDeviceSettingsPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesDeviceSettingsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_delete_device_settings_policy_builder(
            &self.http_client,
            &args.policy_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_delete_device_settings_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices get split tunnel exclude list for a device settings policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesSplitTunnelResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_get_split_tunnel_exclude_list_for_a_device_settings_policy(
        &self,
        args: &DevicesGetSplitTunnelExcludeListForADeviceSettingsPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesSplitTunnelResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_get_split_tunnel_exclude_list_for_a_device_settings_policy_builder(
            &self.http_client,
            &args.policy_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_get_split_tunnel_exclude_list_for_a_device_settings_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices set split tunnel exclude list for a device settings policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesSplitTunnelResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_set_split_tunnel_exclude_list_for_a_device_settings_policy(
        &self,
        args: &DevicesSetSplitTunnelExcludeListForADeviceSettingsPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesSplitTunnelResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_set_split_tunnel_exclude_list_for_a_device_settings_policy_builder(
            &self.http_client,
            &args.policy_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_set_split_tunnel_exclude_list_for_a_device_settings_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices get local domain fallback list for a device settings policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesFallbackDomainResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_get_local_domain_fallback_list_for_a_device_settings_policy(
        &self,
        args: &DevicesGetLocalDomainFallbackListForADeviceSettingsPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesFallbackDomainResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_get_local_domain_fallback_list_for_a_device_settings_policy_builder(
            &self.http_client,
            &args.policy_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_get_local_domain_fallback_list_for_a_device_settings_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices set local domain fallback list for a device settings policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesFallbackDomainResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_set_local_domain_fallback_list_for_a_device_settings_policy(
        &self,
        args: &DevicesSetLocalDomainFallbackListForADeviceSettingsPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesFallbackDomainResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_set_local_domain_fallback_list_for_a_device_settings_policy_builder(
            &self.http_client,
            &args.policy_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_set_local_domain_fallback_list_for_a_device_settings_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices get split tunnel include list for a device settings policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesSplitTunnelIncludeResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_get_split_tunnel_include_list_for_a_device_settings_policy(
        &self,
        args: &DevicesGetSplitTunnelIncludeListForADeviceSettingsPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesSplitTunnelIncludeResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_get_split_tunnel_include_list_for_a_device_settings_policy_builder(
            &self.http_client,
            &args.policy_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_get_split_tunnel_include_list_for_a_device_settings_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices set split tunnel include list for a device settings policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesSplitTunnelIncludeResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_set_split_tunnel_include_list_for_a_device_settings_policy(
        &self,
        args: &DevicesSetSplitTunnelIncludeListForADeviceSettingsPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesSplitTunnelIncludeResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_set_split_tunnel_include_list_for_a_device_settings_policy_builder(
            &self.http_client,
            &args.policy_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_set_split_tunnel_include_list_for_a_device_settings_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device posture rules list device posture rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn device_posture_rules_list_device_posture_rules(
        &self,
        args: &DevicePostureRulesListDevicePostureRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_posture_rules_list_device_posture_rules_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = device_posture_rules_list_device_posture_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device posture rules create device posture rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn device_posture_rules_create_device_posture_rule(
        &self,
        args: &DevicePostureRulesCreateDevicePostureRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_posture_rules_create_device_posture_rule_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = device_posture_rules_create_device_posture_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device posture integrations list device posture integrations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn device_posture_integrations_list_device_posture_integrations(
        &self,
        args: &DevicePostureIntegrationsListDevicePostureIntegrationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_posture_integrations_list_device_posture_integrations_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = device_posture_integrations_list_device_posture_integrations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device posture integrations create device posture integration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn device_posture_integrations_create_device_posture_integration(
        &self,
        args: &DevicePostureIntegrationsCreateDevicePostureIntegrationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_posture_integrations_create_device_posture_integration_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = device_posture_integrations_create_device_posture_integration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device posture integrations device posture integration details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn device_posture_integrations_device_posture_integration_details(
        &self,
        args: &DevicePostureIntegrationsDevicePostureIntegrationDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_posture_integrations_device_posture_integration_details_builder(
            &self.http_client,
            &args.integration_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = device_posture_integrations_device_posture_integration_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device posture integrations update device posture integration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn device_posture_integrations_update_device_posture_integration(
        &self,
        args: &DevicePostureIntegrationsUpdateDevicePostureIntegrationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_posture_integrations_update_device_posture_integration_builder(
            &self.http_client,
            &args.integration_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = device_posture_integrations_update_device_posture_integration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device posture integrations delete device posture integration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesSchemasIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn device_posture_integrations_delete_device_posture_integration(
        &self,
        args: &DevicePostureIntegrationsDeleteDevicePostureIntegrationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesSchemasIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_posture_integrations_delete_device_posture_integration_builder(
            &self.http_client,
            &args.integration_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = device_posture_integrations_delete_device_posture_integration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device posture rules device posture rules details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn device_posture_rules_device_posture_rules_details(
        &self,
        args: &DevicePostureRulesDevicePostureRulesDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_posture_rules_device_posture_rules_details_builder(
            &self.http_client,
            &args.rule_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = device_posture_rules_device_posture_rules_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device posture rules update device posture rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn device_posture_rules_update_device_posture_rule(
        &self,
        args: &DevicePostureRulesUpdateDevicePostureRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_posture_rules_update_device_posture_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = device_posture_rules_update_device_posture_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device posture rules delete device posture rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn device_posture_rules_delete_device_posture_rule(
        &self,
        args: &DevicePostureRulesDeleteDevicePostureRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_posture_rules_delete_device_posture_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = device_posture_rules_delete_device_posture_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List registrations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_registrations(
        &self,
        args: &ListRegistrationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_registrations_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = list_registrations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete registrations.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_registrations(
        &self,
        args: &DeleteRegistrationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_registrations_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_registrations_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Revoke registrations.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn revoke_registrations(
        &self,
        args: &RevokeRegistrationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = revoke_registrations_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = revoke_registrations_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Unrevoke registrations.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn unrevoke_registrations(
        &self,
        args: &UnrevokeRegistrationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = unrevoke_registrations_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = unrevoke_registrations_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get registration.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_registration(
        &self,
        args: &GetRegistrationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_registration_builder(
            &self.http_client,
            &args.registration_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_registration_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete registration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_registration(
        &self,
        args: &DeleteRegistrationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_registration_builder(
            &self.http_client,
            &args.registration_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_registration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get registration override codes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_registration_override_codes(
        &self,
        args: &GetRegistrationOverrideCodesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_registration_override_codes_builder(
            &self.http_client,
            &args.account_id,
            &args.registration_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_registration_override_codes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices resilience retrieve global warp override.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesGlobalWarpOverrideResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_resilience_retrieve_global_warp_override(
        &self,
        args: &DevicesResilienceRetrieveGlobalWarpOverrideArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesGlobalWarpOverrideResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_resilience_retrieve_global_warp_override_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_resilience_retrieve_global_warp_override_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices resilience set global warp override.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesGlobalWarpOverrideResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn devices_resilience_set_global_warp_override(
        &self,
        args: &DevicesResilienceSetGlobalWarpOverrideArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesGlobalWarpOverrideResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_resilience_set_global_warp_override_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_resilience_set_global_warp_override_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices revoke devices.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesApiResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn devices_revoke_devices(
        &self,
        args: &DevicesRevokeDevicesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesApiResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_revoke_devices_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_revoke_devices_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust accounts get device settings for zero trust account.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesZeroTrustAccountDeviceSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_accounts_get_device_settings_for_zero_trust_account(
        &self,
        args: &ZeroTrustAccountsGetDeviceSettingsForZeroTrustAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesZeroTrustAccountDeviceSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_accounts_get_device_settings_for_zero_trust_account_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_accounts_get_device_settings_for_zero_trust_account_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust accounts update device settings for the zero trust account.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesZeroTrustAccountDeviceSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_accounts_update_device_settings_for_the_zero_trust_account(
        &self,
        args: &ZeroTrustAccountsUpdateDeviceSettingsForTheZeroTrustAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesZeroTrustAccountDeviceSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_accounts_update_device_settings_for_the_zero_trust_account_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_accounts_update_device_settings_for_the_zero_trust_account_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust accounts patch device settings for the zero trust account.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesZeroTrustAccountDeviceSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_accounts_patch_device_settings_for_the_zero_trust_account(
        &self,
        args: &ZeroTrustAccountsPatchDeviceSettingsForTheZeroTrustAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesZeroTrustAccountDeviceSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_accounts_patch_device_settings_for_the_zero_trust_account_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_accounts_patch_device_settings_for_the_zero_trust_account_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust accounts delete device settings for zero trust account.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesZeroTrustAccountDeviceSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_accounts_delete_device_settings_for_zero_trust_account(
        &self,
        args: &ZeroTrustAccountsDeleteDeviceSettingsForZeroTrustAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesZeroTrustAccountDeviceSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_accounts_delete_device_settings_for_zero_trust_account_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_accounts_delete_device_settings_for_zero_trust_account_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices unrevoke devices.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesApiResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn devices_unrevoke_devices(
        &self,
        args: &DevicesUnrevokeDevicesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesApiResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_unrevoke_devices_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_unrevoke_devices_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices device details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesDeviceResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_device_details(
        &self,
        args: &DevicesDeviceDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesDeviceResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_device_details_builder(
            &self.http_client,
            &args.device_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_device_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices list admin override code for device.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesOverrideCodesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_list_admin_override_code_for_device(
        &self,
        args: &DevicesListAdminOverrideCodeForDeviceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesOverrideCodesResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_list_admin_override_code_for_device_builder(
            &self.http_client,
            &args.device_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_list_admin_override_code_for_device_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dex endpoints list colos.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dex_endpoints_list_colos(
        &self,
        args: &DexEndpointsListColosArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dex_endpoints_list_colos_builder(
            &self.http_client,
            &args.account_id,
            &args.from,
            &args.to,
            &args.sortBy,
        )
        .map_err(ProviderError::Api)?;

        let task = dex_endpoints_list_colos_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get commands.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_commands(
        &self,
        args: &GetCommandsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_commands_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.from,
            &args.to,
            &args.device_id,
            &args.user_email,
            &args.command_type,
            &args.status,
        )
        .map_err(ProviderError::Api)?;

        let task = get_commands_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post commands.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_commands(
        &self,
        args: &PostCommandsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_commands_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_commands_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get commands eligible devices.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_commands_eligible_devices(
        &self,
        args: &GetCommandsEligibleDevicesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_commands_eligible_devices_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.search,
        )
        .map_err(ProviderError::Api)?;

        let task = get_commands_eligible_devices_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get commands quota.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_commands_quota(
        &self,
        args: &GetCommandsQuotaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_commands_quota_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_commands_quota_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get commands command id downloads filename.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_commands_command_id_downloads_filename(
        &self,
        args: &GetCommandsCommandIdDownloadsFilenameArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_commands_command_id_downloads_filename_builder(
            &self.http_client,
            &args.account_id,
            &args.command_id,
            &args.filename,
        )
        .map_err(ProviderError::Api)?;

        let task = get_commands_command_id_downloads_filename_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device dex test details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DigitalExperienceMonitoringDexResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn device_dex_test_details(
        &self,
        args: &DeviceDexTestDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DigitalExperienceMonitoringDexResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_dex_test_details_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.testName,
            &args.kind,
        )
        .map_err(ProviderError::Api)?;

        let task = device_dex_test_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device dex test create device dex test.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DigitalExperienceMonitoringDexSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn device_dex_test_create_device_dex_test(
        &self,
        args: &DeviceDexTestCreateDeviceDexTestArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DigitalExperienceMonitoringDexSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_dex_test_create_device_dex_test_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = device_dex_test_create_device_dex_test_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device dex test get device dex test.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DigitalExperienceMonitoringDexSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn device_dex_test_get_device_dex_test(
        &self,
        args: &DeviceDexTestGetDeviceDexTestArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DigitalExperienceMonitoringDexSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_dex_test_get_device_dex_test_builder(
            &self.http_client,
            &args.account_id,
            &args.dex_test_id,
        )
        .map_err(ProviderError::Api)?;

        let task = device_dex_test_get_device_dex_test_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device dex test update device dex test.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DigitalExperienceMonitoringDexSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn device_dex_test_update_device_dex_test(
        &self,
        args: &DeviceDexTestUpdateDeviceDexTestArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DigitalExperienceMonitoringDexSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_dex_test_update_device_dex_test_builder(
            &self.http_client,
            &args.account_id,
            &args.dex_test_id,
        )
        .map_err(ProviderError::Api)?;

        let task = device_dex_test_update_device_dex_test_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Device dex test delete device dex test.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DigitalExperienceMonitoringDexDeleteResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn device_dex_test_delete_device_dex_test(
        &self,
        args: &DeviceDexTestDeleteDeviceDexTestArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DigitalExperienceMonitoringDexDeleteResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = device_dex_test_delete_device_dex_test_builder(
            &self.http_client,
            &args.account_id,
            &args.dex_test_id,
        )
        .map_err(ProviderError::Api)?;

        let task = device_dex_test_delete_device_dex_test_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices live status.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DigitalExperienceMonitoringDevice result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_live_status(
        &self,
        args: &DevicesLiveStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DigitalExperienceMonitoringDevice, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_live_status_builder(
            &self.http_client,
            &args.account_id,
            &args.device_id,
            &args.since_minutes,
            &args.time_now,
            &args.colo,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_live_status_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dex fleet status devices.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DigitalExperienceMonitoringFleetStatusDevicesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dex_fleet_status_devices(
        &self,
        args: &DexFleetStatusDevicesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DigitalExperienceMonitoringFleetStatusDevicesResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dex_fleet_status_devices_builder(
            &self.http_client,
            &args.account_id,
            &args.to,
            &args.from,
            &args.page,
            &args.per_page,
            &args.sort_by,
            &args.colo,
            &args.device_id,
            &args.mode,
            &args.status,
            &args.platform,
            &args.version,
            &args.source,
        )
        .map_err(ProviderError::Api)?;

        let task = dex_fleet_status_devices_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dex fleet status live.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DigitalExperienceMonitoringFleetStatusLiveResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dex_fleet_status_live(
        &self,
        args: &DexFleetStatusLiveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DigitalExperienceMonitoringFleetStatusLiveResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dex_fleet_status_live_builder(
            &self.http_client,
            &args.account_id,
            &args.since_minutes,
        )
        .map_err(ProviderError::Api)?;

        let task = dex_fleet_status_live_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dex fleet status over time.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DigitalExperienceMonitoringFleetStatusOverTimeResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dex_fleet_status_over_time(
        &self,
        args: &DexFleetStatusOverTimeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DigitalExperienceMonitoringFleetStatusOverTimeResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dex_fleet_status_over_time_builder(
            &self.http_client,
            &args.account_id,
            &args.to,
            &args.from,
            &args.colo,
            &args.device_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dex_fleet_status_over_time_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dex endpoints http test details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dex_endpoints_http_test_details(
        &self,
        args: &DexEndpointsHttpTestDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dex_endpoints_http_test_details_builder(
            &self.http_client,
            &args.account_id,
            &args.test_id,
            &args.deviceId,
            &args.from,
            &args.to,
            &args.interval,
            &args.colo,
        )
        .map_err(ProviderError::Api)?;

        let task = dex_endpoints_http_test_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dex endpoints http test percentiles.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dex_endpoints_http_test_percentiles(
        &self,
        args: &DexEndpointsHttpTestPercentilesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dex_endpoints_http_test_percentiles_builder(
            &self.http_client,
            &args.account_id,
            &args.test_id,
            &args.deviceId,
            &args.from,
            &args.to,
            &args.colo,
        )
        .map_err(ProviderError::Api)?;

        let task = dex_endpoints_http_test_percentiles_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List dex rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_dex_rules(
        &self,
        args: &ListDexRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_dex_rules_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.sort_order,
            &args.sort_by,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = list_dex_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create dex rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_dex_rule(
        &self,
        args: &CreateDexRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_dex_rule_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_dex_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get dex rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_dex_rule(
        &self,
        args: &GetDexRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_dex_rule_builder(
            &self.http_client,
            &args.account_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_dex_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update dex rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_dex_rule(
        &self,
        args: &UpdateDexRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_dex_rule_builder(
            &self.http_client,
            &args.account_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_dex_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete dex rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_dex_rule(
        &self,
        args: &DeleteDexRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_dex_rule_builder(
            &self.http_client,
            &args.account_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_dex_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dex endpoints list tests overview.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dex_endpoints_list_tests_overview(
        &self,
        args: &DexEndpointsListTestsOverviewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dex_endpoints_list_tests_overview_builder(
            &self.http_client,
            &args.account_id,
            &args.colo,
            &args.testName,
            &args.deviceId,
            &args.page,
            &args.per_page,
            &args.kind,
        )
        .map_err(ProviderError::Api)?;

        let task = dex_endpoints_list_tests_overview_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dex endpoints tests unique devices.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dex_endpoints_tests_unique_devices(
        &self,
        args: &DexEndpointsTestsUniqueDevicesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dex_endpoints_tests_unique_devices_builder(
            &self.http_client,
            &args.account_id,
            &args.testName,
            &args.deviceId,
        )
        .map_err(ProviderError::Api)?;

        let task = dex_endpoints_tests_unique_devices_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dex endpoints traceroute test result network path.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dex_endpoints_traceroute_test_result_network_path(
        &self,
        args: &DexEndpointsTracerouteTestResultNetworkPathArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dex_endpoints_traceroute_test_result_network_path_builder(
            &self.http_client,
            &args.account_id,
            &args.test_result_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dex_endpoints_traceroute_test_result_network_path_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dex endpoints traceroute test details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dex_endpoints_traceroute_test_details(
        &self,
        args: &DexEndpointsTracerouteTestDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dex_endpoints_traceroute_test_details_builder(
            &self.http_client,
            &args.account_id,
            &args.test_id,
            &args.deviceId,
            &args.from,
            &args.to,
            &args.interval,
            &args.colo,
        )
        .map_err(ProviderError::Api)?;

        let task = dex_endpoints_traceroute_test_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dex endpoints traceroute test network path.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dex_endpoints_traceroute_test_network_path(
        &self,
        args: &DexEndpointsTracerouteTestNetworkPathArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dex_endpoints_traceroute_test_network_path_builder(
            &self.http_client,
            &args.account_id,
            &args.test_id,
            &args.deviceId,
            &args.from,
            &args.to,
            &args.interval,
        )
        .map_err(ProviderError::Api)?;

        let task = dex_endpoints_traceroute_test_network_path_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dex endpoints traceroute test percentiles.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dex_endpoints_traceroute_test_percentiles(
        &self,
        args: &DexEndpointsTracerouteTestPercentilesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dex_endpoints_traceroute_test_percentiles_builder(
            &self.http_client,
            &args.account_id,
            &args.test_id,
            &args.deviceId,
            &args.from,
            &args.to,
            &args.colo,
        )
        .map_err(ProviderError::Api)?;

        let task = dex_endpoints_traceroute_test_percentiles_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List warp change events.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_warp_change_events(
        &self,
        args: &ListWarpChangeEventsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_warp_change_events_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.from,
            &args.to,
            &args.type_rs,
            &args.toggle,
            &args.config_name,
            &args.account_name,
            &args.sort_order,
        )
        .map_err(ProviderError::Api)?;

        let task = list_warp_change_events_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Diagnostics endpoint healthcheck list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicTransitEndpointHealthCheckResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn diagnostics_endpoint_healthcheck_list(
        &self,
        args: &DiagnosticsEndpointHealthcheckListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicTransitEndpointHealthCheckResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = diagnostics_endpoint_healthcheck_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = diagnostics_endpoint_healthcheck_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Diagnostics endpoint healthcheck create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicTransitEndpointHealthCheckResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn diagnostics_endpoint_healthcheck_create(
        &self,
        args: &DiagnosticsEndpointHealthcheckCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicTransitEndpointHealthCheckResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = diagnostics_endpoint_healthcheck_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = diagnostics_endpoint_healthcheck_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Diagnostics endpoint healthcheck get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicTransitEndpointHealthCheckResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn diagnostics_endpoint_healthcheck_get(
        &self,
        args: &DiagnosticsEndpointHealthcheckGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicTransitEndpointHealthCheckResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = diagnostics_endpoint_healthcheck_get_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = diagnostics_endpoint_healthcheck_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Diagnostics endpoint healthcheck update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicTransitEndpointHealthCheckResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn diagnostics_endpoint_healthcheck_update(
        &self,
        args: &DiagnosticsEndpointHealthcheckUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicTransitEndpointHealthCheckResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = diagnostics_endpoint_healthcheck_update_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = diagnostics_endpoint_healthcheck_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Diagnostics endpoint healthcheck delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicTransitApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn diagnostics_endpoint_healthcheck_delete(
        &self,
        args: &DiagnosticsEndpointHealthcheckDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicTransitApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = diagnostics_endpoint_healthcheck_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = diagnostics_endpoint_healthcheck_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Diagnostics traceroute.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicTransitTracerouteResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn diagnostics_traceroute(
        &self,
        args: &DiagnosticsTracerouteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicTransitTracerouteResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = diagnostics_traceroute_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = diagnostics_traceroute_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp datasets read all.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_datasets_read_all(
        &self,
        args: &DlpDatasetsReadAllArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_datasets_read_all_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_datasets_read_all_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp datasets create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_datasets_create(
        &self,
        args: &DlpDatasetsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_datasets_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_datasets_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp datasets read.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_datasets_read(
        &self,
        args: &DlpDatasetsReadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_datasets_read_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_datasets_read_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp datasets update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_datasets_update(
        &self,
        args: &DlpDatasetsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_datasets_update_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_datasets_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp datasets delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_datasets_delete(
        &self,
        args: &DlpDatasetsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_datasets_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_datasets_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp datasets create version.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_datasets_create_version(
        &self,
        args: &DlpDatasetsCreateVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_datasets_create_version_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_datasets_create_version_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp datasets upload version.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_datasets_upload_version(
        &self,
        args: &DlpDatasetsUploadVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_datasets_upload_version_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
            &args.version,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_datasets_upload_version_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp datasets define columns.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_datasets_define_columns(
        &self,
        args: &DlpDatasetsDefineColumnsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_datasets_define_columns_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
            &args.version,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_datasets_define_columns_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp datasets upload dataset column.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_datasets_upload_dataset_column(
        &self,
        args: &DlpDatasetsUploadDatasetColumnArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_datasets_upload_dataset_column_builder(
            &self.http_client,
            &args.account_id,
            &args.dataset_id,
            &args.version,
            &args.entry_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_datasets_upload_dataset_column_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp document fingerprints read all.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_document_fingerprints_read_all(
        &self,
        args: &DlpDocumentFingerprintsReadAllArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_document_fingerprints_read_all_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_document_fingerprints_read_all_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp document fingerprints create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_document_fingerprints_create(
        &self,
        args: &DlpDocumentFingerprintsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_document_fingerprints_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_document_fingerprints_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp document fingerprints read.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_document_fingerprints_read(
        &self,
        args: &DlpDocumentFingerprintsReadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_document_fingerprints_read_builder(
            &self.http_client,
            &args.account_id,
            &args.document_fingerprint_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_document_fingerprints_read_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp document fingerprints update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_document_fingerprints_update(
        &self,
        args: &DlpDocumentFingerprintsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_document_fingerprints_update_builder(
            &self.http_client,
            &args.account_id,
            &args.document_fingerprint_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_document_fingerprints_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp document fingerprints upload.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_document_fingerprints_upload(
        &self,
        args: &DlpDocumentFingerprintsUploadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_document_fingerprints_upload_builder(
            &self.http_client,
            &args.account_id,
            &args.document_fingerprint_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_document_fingerprints_upload_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp document fingerprints delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_document_fingerprints_delete(
        &self,
        args: &DlpDocumentFingerprintsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_document_fingerprints_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.document_fingerprint_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_document_fingerprints_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp email scanner get account mapping.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_email_scanner_get_account_mapping(
        &self,
        args: &DlpEmailScannerGetAccountMappingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_email_scanner_get_account_mapping_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_email_scanner_get_account_mapping_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp email scanner create account mapping.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_email_scanner_create_account_mapping(
        &self,
        args: &DlpEmailScannerCreateAccountMappingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_email_scanner_create_account_mapping_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_email_scanner_create_account_mapping_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp email scanner list all rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_email_scanner_list_all_rules(
        &self,
        args: &DlpEmailScannerListAllRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_email_scanner_list_all_rules_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_email_scanner_list_all_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp email scanner create rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_email_scanner_create_rule(
        &self,
        args: &DlpEmailScannerCreateRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_email_scanner_create_rule_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_email_scanner_create_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp email scanner update rule priorities.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_email_scanner_update_rule_priorities(
        &self,
        args: &DlpEmailScannerUpdateRulePrioritiesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_email_scanner_update_rule_priorities_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_email_scanner_update_rule_priorities_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp email scanner get rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_email_scanner_get_rule(
        &self,
        args: &DlpEmailScannerGetRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_email_scanner_get_rule_builder(
            &self.http_client,
            &args.account_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_email_scanner_get_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp email scanner update rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_email_scanner_update_rule(
        &self,
        args: &DlpEmailScannerUpdateRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_email_scanner_update_rule_builder(
            &self.http_client,
            &args.account_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_email_scanner_update_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp email scanner delete rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_email_scanner_delete_rule(
        &self,
        args: &DlpEmailScannerDeleteRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_email_scanner_delete_rule_builder(
            &self.http_client,
            &args.account_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_email_scanner_delete_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp entries list all entries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_entries_list_all_entries(
        &self,
        args: &DlpEntriesListAllEntriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_entries_list_all_entries_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_entries_list_all_entries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp entries create entry.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_entries_create_entry(
        &self,
        args: &DlpEntriesCreateEntryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_entries_create_entry_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_entries_create_entry_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp entries update custom entry.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_entries_update_custom_entry(
        &self,
        args: &DlpEntriesUpdateCustomEntryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_entries_update_custom_entry_builder(
            &self.http_client,
            &args.account_id,
            &args.entry_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_entries_update_custom_entry_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp entries create integration entry.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_entries_create_integration_entry(
        &self,
        args: &DlpEntriesCreateIntegrationEntryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_entries_create_integration_entry_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_entries_create_integration_entry_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp entries update integration entry.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_entries_update_integration_entry(
        &self,
        args: &DlpEntriesUpdateIntegrationEntryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_entries_update_integration_entry_builder(
            &self.http_client,
            &args.account_id,
            &args.entry_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_entries_update_integration_entry_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp entries delete integration entry.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_entries_delete_integration_entry(
        &self,
        args: &DlpEntriesDeleteIntegrationEntryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_entries_delete_integration_entry_builder(
            &self.http_client,
            &args.account_id,
            &args.entry_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_entries_delete_integration_entry_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp entries create predefined entry.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_entries_create_predefined_entry(
        &self,
        args: &DlpEntriesCreatePredefinedEntryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_entries_create_predefined_entry_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_entries_create_predefined_entry_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp entries update predefined entry.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_entries_update_predefined_entry(
        &self,
        args: &DlpEntriesUpdatePredefinedEntryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_entries_update_predefined_entry_builder(
            &self.http_client,
            &args.account_id,
            &args.entry_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_entries_update_predefined_entry_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp entries delete predefined entry.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_entries_delete_predefined_entry(
        &self,
        args: &DlpEntriesDeletePredefinedEntryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_entries_delete_predefined_entry_builder(
            &self.http_client,
            &args.account_id,
            &args.entry_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_entries_delete_predefined_entry_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp entries get dlp entry.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_entries_get_dlp_entry(
        &self,
        args: &DlpEntriesGetDlpEntryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_entries_get_dlp_entry_builder(
            &self.http_client,
            &args.account_id,
            &args.entry_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_entries_get_dlp_entry_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp entries update entry.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_entries_update_entry(
        &self,
        args: &DlpEntriesUpdateEntryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_entries_update_entry_builder(
            &self.http_client,
            &args.account_id,
            &args.entry_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_entries_update_entry_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp entries delete entry.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_entries_delete_entry(
        &self,
        args: &DlpEntriesDeleteEntryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_entries_delete_entry_builder(
            &self.http_client,
            &args.account_id,
            &args.entry_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_entries_delete_entry_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp limits get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_limits_get(
        &self,
        args: &DlpLimitsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_limits_get_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_limits_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp pattern validate.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_pattern_validate(
        &self,
        args: &DlpPatternValidateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_pattern_validate_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_pattern_validate_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp payload log get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_payload_log_get(
        &self,
        args: &DlpPayloadLogGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_payload_log_get_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_payload_log_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp payload log put.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_payload_log_put(
        &self,
        args: &DlpPayloadLogPutArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_payload_log_put_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_payload_log_put_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp profiles list all profiles.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_profiles_list_all_profiles(
        &self,
        args: &DlpProfilesListAllProfilesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_profiles_list_all_profiles_builder(
            &self.http_client,
            &args.account_id,
            &args.all,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_profiles_list_all_profiles_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp profiles list all custom profiles.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_profiles_list_all_custom_profiles(
        &self,
        args: &DlpProfilesListAllCustomProfilesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_profiles_list_all_custom_profiles_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_profiles_list_all_custom_profiles_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp profiles create custom profiles.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_profiles_create_custom_profiles(
        &self,
        args: &DlpProfilesCreateCustomProfilesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_profiles_create_custom_profiles_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_profiles_create_custom_profiles_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp profiles get custom profile.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_profiles_get_custom_profile(
        &self,
        args: &DlpProfilesGetCustomProfileArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_profiles_get_custom_profile_builder(
            &self.http_client,
            &args.account_id,
            &args.profile_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_profiles_get_custom_profile_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp profiles update custom profile.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_profiles_update_custom_profile(
        &self,
        args: &DlpProfilesUpdateCustomProfileArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_profiles_update_custom_profile_builder(
            &self.http_client,
            &args.account_id,
            &args.profile_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_profiles_update_custom_profile_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp profiles delete custom profile.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_profiles_delete_custom_profile(
        &self,
        args: &DlpProfilesDeleteCustomProfileArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_profiles_delete_custom_profile_builder(
            &self.http_client,
            &args.account_id,
            &args.profile_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_profiles_delete_custom_profile_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp profiles create predefined profile.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_profiles_create_predefined_profile(
        &self,
        args: &DlpProfilesCreatePredefinedProfileArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_profiles_create_predefined_profile_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_profiles_create_predefined_profile_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp profiles get predefined profile.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_profiles_get_predefined_profile(
        &self,
        args: &DlpProfilesGetPredefinedProfileArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_profiles_get_predefined_profile_builder(
            &self.http_client,
            &args.account_id,
            &args.profile_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_profiles_get_predefined_profile_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp profiles update predefined profile.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_profiles_update_predefined_profile(
        &self,
        args: &DlpProfilesUpdatePredefinedProfileArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_profiles_update_predefined_profile_builder(
            &self.http_client,
            &args.account_id,
            &args.profile_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_profiles_update_predefined_profile_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp profiles delete predefined profile.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_profiles_delete_predefined_profile(
        &self,
        args: &DlpProfilesDeletePredefinedProfileArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_profiles_delete_predefined_profile_builder(
            &self.http_client,
            &args.account_id,
            &args.profile_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_profiles_delete_predefined_profile_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp profiles get predefined profile config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_profiles_get_predefined_profile_config(
        &self,
        args: &DlpProfilesGetPredefinedProfileConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_profiles_get_predefined_profile_config_builder(
            &self.http_client,
            &args.account_id,
            &args.profile_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_profiles_get_predefined_profile_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp profiles create predefined profile config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_profiles_create_predefined_profile_config(
        &self,
        args: &DlpProfilesCreatePredefinedProfileConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_profiles_create_predefined_profile_config_builder(
            &self.http_client,
            &args.account_id,
            &args.profile_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_profiles_create_predefined_profile_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp profiles update predefined profile config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_profiles_update_predefined_profile_config(
        &self,
        args: &DlpProfilesUpdatePredefinedProfileConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_profiles_update_predefined_profile_config_builder(
            &self.http_client,
            &args.account_id,
            &args.profile_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_profiles_update_predefined_profile_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp profiles get dlp profile.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_profiles_get_dlp_profile(
        &self,
        args: &DlpProfilesGetDlpProfileArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_profiles_get_dlp_profile_builder(
            &self.http_client,
            &args.account_id,
            &args.profile_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_profiles_get_dlp_profile_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp settings get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_settings_get(
        &self,
        args: &DlpSettingsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_settings_get_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_settings_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp settings update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_settings_update(
        &self,
        args: &DlpSettingsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_settings_update_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_settings_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp settings edit.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_settings_edit(
        &self,
        args: &DlpSettingsEditArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_settings_edit_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_settings_edit_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp settings delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_settings_delete(
        &self,
        args: &DlpSettingsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_settings_delete_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_settings_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns firewall list dns firewall clusters.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsFirewallDnsFirewallResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dns_firewall_list_dns_firewall_clusters(
        &self,
        args: &DnsFirewallListDnsFirewallClustersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsFirewallDnsFirewallResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_firewall_list_dns_firewall_clusters_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_firewall_list_dns_firewall_clusters_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns firewall create dns firewall cluster.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsFirewallDnsFirewallSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dns_firewall_create_dns_firewall_cluster(
        &self,
        args: &DnsFirewallCreateDnsFirewallClusterArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsFirewallDnsFirewallSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_firewall_create_dns_firewall_cluster_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_firewall_create_dns_firewall_cluster_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns firewall dns firewall cluster details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsFirewallDnsFirewallSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dns_firewall_dns_firewall_cluster_details(
        &self,
        args: &DnsFirewallDnsFirewallClusterDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsFirewallDnsFirewallSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_firewall_dns_firewall_cluster_details_builder(
            &self.http_client,
            &args.dns_firewall_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_firewall_dns_firewall_cluster_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns firewall update dns firewall cluster.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsFirewallDnsFirewallSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dns_firewall_update_dns_firewall_cluster(
        &self,
        args: &DnsFirewallUpdateDnsFirewallClusterArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsFirewallDnsFirewallSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_firewall_update_dns_firewall_cluster_builder(
            &self.http_client,
            &args.dns_firewall_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_firewall_update_dns_firewall_cluster_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns firewall delete dns firewall cluster.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dns_firewall_delete_dns_firewall_cluster(
        &self,
        args: &DnsFirewallDeleteDnsFirewallClusterArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_firewall_delete_dns_firewall_cluster_builder(
            &self.http_client,
            &args.dns_firewall_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_firewall_delete_dns_firewall_cluster_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns firewall analytics table.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dns_firewall_analytics_table(
        &self,
        args: &DnsFirewallAnalyticsTableArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_firewall_analytics_table_builder(
            &self.http_client,
            &args.dns_firewall_id,
            &args.account_id,
            &args.metrics,
            &args.dimensions,
            &args.since,
            &args.until,
            &args.limit,
            &args.sort,
            &args.filters,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_firewall_analytics_table_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns firewall analytics by time.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dns_firewall_analytics_by_time(
        &self,
        args: &DnsFirewallAnalyticsByTimeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_firewall_analytics_by_time_builder(
            &self.http_client,
            &args.dns_firewall_id,
            &args.account_id,
            &args.metrics,
            &args.dimensions,
            &args.since,
            &args.until,
            &args.limit,
            &args.sort,
            &args.filters,
            &args.time_delta,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_firewall_analytics_by_time_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns firewall show dns firewall cluster reverse dns.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsFirewallDnsFirewallReverseDnsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dns_firewall_show_dns_firewall_cluster_reverse_dns(
        &self,
        args: &DnsFirewallShowDnsFirewallClusterReverseDnsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsFirewallDnsFirewallReverseDnsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_firewall_show_dns_firewall_cluster_reverse_dns_builder(
            &self.http_client,
            &args.dns_firewall_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_firewall_show_dns_firewall_cluster_reverse_dns_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns firewall update dns firewall cluster reverse dns.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsFirewallDnsFirewallReverseDnsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dns_firewall_update_dns_firewall_cluster_reverse_dns(
        &self,
        args: &DnsFirewallUpdateDnsFirewallClusterReverseDnsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsFirewallDnsFirewallReverseDnsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_firewall_update_dns_firewall_cluster_reverse_dns_builder(
            &self.http_client,
            &args.dns_firewall_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_firewall_update_dns_firewall_cluster_reverse_dns_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns records for an account get usage.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsRecordsDnsResponseAccountUsage result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dns_records_for_an_account_get_usage(
        &self,
        args: &DnsRecordsForAnAccountGetUsageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsRecordsDnsResponseAccountUsage, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_records_for_an_account_get_usage_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_records_for_an_account_get_usage_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns settings for an account list dns settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsSettingsDnsResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dns_settings_for_an_account_list_dns_settings(
        &self,
        args: &DnsSettingsForAnAccountListDnsSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsSettingsDnsResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_settings_for_an_account_list_dns_settings_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_settings_for_an_account_list_dns_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns settings for an account update dns settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsSettingsDnsResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dns_settings_for_an_account_update_dns_settings(
        &self,
        args: &DnsSettingsForAnAccountUpdateDnsSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsSettingsDnsResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_settings_for_an_account_update_dns_settings_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_settings_for_an_account_update_dns_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns views for an account list internal dns views.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsSettingsDnsViewResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dns_views_for_an_account_list_internal_dns_views(
        &self,
        args: &DnsViewsForAnAccountListInternalDnsViewsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsSettingsDnsViewResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_views_for_an_account_list_internal_dns_views_builder(
            &self.http_client,
            &args.account_id,
            &args.name,
            &args.name_exact,
            &args.name_contains,
            &args.name_startswith,
            &args.name_endswith,
            &args.zone_id,
            &args.zone_name,
            &args.match_rs,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_views_for_an_account_list_internal_dns_views_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns views for an account create internal dns views.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsSettingsDnsViewResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dns_views_for_an_account_create_internal_dns_views(
        &self,
        args: &DnsViewsForAnAccountCreateInternalDnsViewsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsSettingsDnsViewResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_views_for_an_account_create_internal_dns_views_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_views_for_an_account_create_internal_dns_views_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns views for an account get internal dns view.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsSettingsDnsViewResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dns_views_for_an_account_get_internal_dns_view(
        &self,
        args: &DnsViewsForAnAccountGetInternalDnsViewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsSettingsDnsViewResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_views_for_an_account_get_internal_dns_view_builder(
            &self.http_client,
            &args.account_id,
            &args.view_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_views_for_an_account_get_internal_dns_view_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns views for an account update internal dns view.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsSettingsDnsViewResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dns_views_for_an_account_update_internal_dns_view(
        &self,
        args: &DnsViewsForAnAccountUpdateInternalDnsViewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsSettingsDnsViewResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_views_for_an_account_update_internal_dns_view_builder(
            &self.http_client,
            &args.account_id,
            &args.view_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_views_for_an_account_update_internal_dns_view_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns views for an account delete internal dns view.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dns_views_for_an_account_delete_internal_dns_view(
        &self,
        args: &DnsViewsForAnAccountDeleteInternalDnsViewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_views_for_an_account_delete_internal_dns_view_builder(
            &self.http_client,
            &args.account_id,
            &args.view_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_views_for_an_account_delete_internal_dns_view_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security investigate.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_security_investigate(
        &self,
        args: &EmailSecurityInvestigateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_investigate_builder(
            &self.http_client,
            &args.account_id,
            &args.start,
            &args.end,
            &args.query,
            &args.detections_only,
            &args.action_log,
            &args.final_disposition,
            &args.metric,
            &args.message_action,
            &args.recipient,
            &args.sender,
            &args.alert_id,
            &args.domain,
            &args.message_id,
            &args.subject,
            &args.exact_subject,
            &args.submissions,
            &args.cursor,
            &args.per_page,
            &args.page,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_investigate_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security post bulk message move.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_post_bulk_message_move(
        &self,
        args: &EmailSecurityPostBulkMessageMoveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_post_bulk_message_move_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_post_bulk_message_move_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security post preview.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_post_preview(
        &self,
        args: &EmailSecurityPostPreviewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_post_preview_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_post_preview_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security post release.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_post_release(
        &self,
        args: &EmailSecurityPostReleaseArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_post_release_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_post_release_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security get message.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_security_get_message(
        &self,
        args: &EmailSecurityGetMessageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_get_message_builder(
            &self.http_client,
            &args.account_id,
            &args.postfix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_get_message_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security get message detections.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_security_get_message_detections(
        &self,
        args: &EmailSecurityGetMessageDetectionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_get_message_detections_builder(
            &self.http_client,
            &args.account_id,
            &args.postfix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_get_message_detections_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security post message move.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_post_message_move(
        &self,
        args: &EmailSecurityPostMessageMoveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_post_message_move_builder(
            &self.http_client,
            &args.account_id,
            &args.postfix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_post_message_move_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security get message preview.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_security_get_message_preview(
        &self,
        args: &EmailSecurityGetMessagePreviewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_get_message_preview_builder(
            &self.http_client,
            &args.account_id,
            &args.postfix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_get_message_preview_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security get message raw.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_security_get_message_raw(
        &self,
        args: &EmailSecurityGetMessageRawArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_get_message_raw_builder(
            &self.http_client,
            &args.account_id,
            &args.postfix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_get_message_raw_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security post reclassify.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_post_reclassify(
        &self,
        args: &EmailSecurityPostReclassifyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_post_reclassify_builder(
            &self.http_client,
            &args.account_id,
            &args.postfix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_post_reclassify_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security get message trace.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_security_get_message_trace(
        &self,
        args: &EmailSecurityGetMessageTraceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_get_message_trace_builder(
            &self.http_client,
            &args.account_id,
            &args.postfix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_get_message_trace_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security get phishguard reports.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_security_get_phishguard_reports(
        &self,
        args: &EmailSecurityGetPhishguardReportsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_get_phishguard_reports_builder(
            &self.http_client,
            &args.account_id,
            &args.from_date,
            &args.to_date,
            &args.start,
            &args.end,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_get_phishguard_reports_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security list allow policies.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_security_list_allow_policies(
        &self,
        args: &EmailSecurityListAllowPoliciesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_list_allow_policies_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
            &args.search,
            &args.is_sender,
            &args.is_trusted_sender,
            &args.is_recipient,
            &args.is_exempt_recipient,
            &args.is_spoof,
            &args.is_acceptable_sender,
            &args.verify_sender,
            &args.pattern_type,
            &args.pattern,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_list_allow_policies_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security create allow policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_create_allow_policy(
        &self,
        args: &EmailSecurityCreateAllowPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_create_allow_policy_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_create_allow_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security batch allow policies.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_batch_allow_policies(
        &self,
        args: &EmailSecurityBatchAllowPoliciesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_batch_allow_policies_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_batch_allow_policies_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security get allow policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_security_get_allow_policy(
        &self,
        args: &EmailSecurityGetAllowPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_get_allow_policy_builder(
            &self.http_client,
            &args.account_id,
            &args.policy_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_get_allow_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security update allow policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_update_allow_policy(
        &self,
        args: &EmailSecurityUpdateAllowPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_update_allow_policy_builder(
            &self.http_client,
            &args.account_id,
            &args.policy_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_update_allow_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security delete allow policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_delete_allow_policy(
        &self,
        args: &EmailSecurityDeleteAllowPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_delete_allow_policy_builder(
            &self.http_client,
            &args.account_id,
            &args.policy_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_delete_allow_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security list blocked senders.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_security_list_blocked_senders(
        &self,
        args: &EmailSecurityListBlockedSendersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_list_blocked_senders_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
            &args.search,
            &args.pattern_type,
            &args.pattern,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_list_blocked_senders_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security create blocked sender.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_create_blocked_sender(
        &self,
        args: &EmailSecurityCreateBlockedSenderArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_create_blocked_sender_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_create_blocked_sender_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security batch blocked senders.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_batch_blocked_senders(
        &self,
        args: &EmailSecurityBatchBlockedSendersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_batch_blocked_senders_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_batch_blocked_senders_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security get blocked sender.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_security_get_blocked_sender(
        &self,
        args: &EmailSecurityGetBlockedSenderArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_get_blocked_sender_builder(
            &self.http_client,
            &args.account_id,
            &args.pattern_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_get_blocked_sender_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security update blocked sender.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_update_blocked_sender(
        &self,
        args: &EmailSecurityUpdateBlockedSenderArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_update_blocked_sender_builder(
            &self.http_client,
            &args.account_id,
            &args.pattern_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_update_blocked_sender_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security delete blocked sender.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_delete_blocked_sender(
        &self,
        args: &EmailSecurityDeleteBlockedSenderArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_delete_blocked_sender_builder(
            &self.http_client,
            &args.account_id,
            &args.pattern_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_delete_blocked_sender_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security list domains.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_security_list_domains(
        &self,
        args: &EmailSecurityListDomainsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_list_domains_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
            &args.search,
            &args.allowed_delivery_mode,
            &args.domain,
            &args.active_delivery_mode,
            &args.integration_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_list_domains_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security delete domains.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_delete_domains(
        &self,
        args: &EmailSecurityDeleteDomainsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_delete_domains_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_delete_domains_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security get domain.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_security_get_domain(
        &self,
        args: &EmailSecurityGetDomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_get_domain_builder(
            &self.http_client,
            &args.account_id,
            &args.domain_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_get_domain_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security update domain.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_update_domain(
        &self,
        args: &EmailSecurityUpdateDomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_update_domain_builder(
            &self.http_client,
            &args.account_id,
            &args.domain_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_update_domain_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security delete domain.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_delete_domain(
        &self,
        args: &EmailSecurityDeleteDomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_delete_domain_builder(
            &self.http_client,
            &args.account_id,
            &args.domain_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_delete_domain_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security list display names.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_security_list_display_names(
        &self,
        args: &EmailSecurityListDisplayNamesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_list_display_names_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
            &args.search,
            &args.provenance,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_list_display_names_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security create display name.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_create_display_name(
        &self,
        args: &EmailSecurityCreateDisplayNameArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_create_display_name_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_create_display_name_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security get display name.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_security_get_display_name(
        &self,
        args: &EmailSecurityGetDisplayNameArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_get_display_name_builder(
            &self.http_client,
            &args.account_id,
            &args.display_name_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_get_display_name_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security update display name.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_update_display_name(
        &self,
        args: &EmailSecurityUpdateDisplayNameArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_update_display_name_builder(
            &self.http_client,
            &args.account_id,
            &args.display_name_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_update_display_name_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security delete display name.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_delete_display_name(
        &self,
        args: &EmailSecurityDeleteDisplayNameArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_delete_display_name_builder(
            &self.http_client,
            &args.account_id,
            &args.display_name_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_delete_display_name_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security batch sending domain restrictions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_batch_sending_domain_restrictions(
        &self,
        args: &EmailSecurityBatchSendingDomainRestrictionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_batch_sending_domain_restrictions_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_batch_sending_domain_restrictions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security list trusted domains.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_security_list_trusted_domains(
        &self,
        args: &EmailSecurityListTrustedDomainsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_list_trusted_domains_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
            &args.search,
            &args.is_recent,
            &args.is_similarity,
            &args.pattern,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_list_trusted_domains_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security create trusted domain.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_create_trusted_domain(
        &self,
        args: &EmailSecurityCreateTrustedDomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_create_trusted_domain_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_create_trusted_domain_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security batch trusted domains.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_batch_trusted_domains(
        &self,
        args: &EmailSecurityBatchTrustedDomainsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_batch_trusted_domains_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_batch_trusted_domains_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security get trusted domain.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_security_get_trusted_domain(
        &self,
        args: &EmailSecurityGetTrustedDomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_get_trusted_domain_builder(
            &self.http_client,
            &args.account_id,
            &args.trusted_domain_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_get_trusted_domain_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security update trusted domain.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_update_trusted_domain(
        &self,
        args: &EmailSecurityUpdateTrustedDomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_update_trusted_domain_builder(
            &self.http_client,
            &args.account_id,
            &args.trusted_domain_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_update_trusted_domain_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security delete trusted domain.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_security_delete_trusted_domain(
        &self,
        args: &EmailSecurityDeleteTrustedDomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_delete_trusted_domain_builder(
            &self.http_client,
            &args.account_id,
            &args.trusted_domain_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_delete_trusted_domain_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email security submissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_security_submissions(
        &self,
        args: &EmailSecuritySubmissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_security_submissions_builder(
            &self.http_client,
            &args.account_id,
            &args.start,
            &args.end,
            &args.type_rs,
            &args.submission_id,
            &args.original_disposition,
            &args.requested_disposition,
            &args.outcome_disposition,
            &args.status,
            &args.query,
            &args.customer_status,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = email_security_submissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email routing destination addresses list destination addresses.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailDestinationAddressesResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_routing_destination_addresses_list_destination_addresses(
        &self,
        args: &EmailRoutingDestinationAddressesListDestinationAddressesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailDestinationAddressesResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_routing_destination_addresses_list_destination_addresses_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.direction,
            &args.verified,
        )
        .map_err(ProviderError::Api)?;

        let task = email_routing_destination_addresses_list_destination_addresses_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email routing destination addresses create a destination address.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailDestinationAddressResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_routing_destination_addresses_create_a_destination_address(
        &self,
        args: &EmailRoutingDestinationAddressesCreateADestinationAddressArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailDestinationAddressResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_routing_destination_addresses_create_a_destination_address_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_routing_destination_addresses_create_a_destination_address_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email routing destination addresses get a destination address.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailDestinationAddressResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_routing_destination_addresses_get_a_destination_address(
        &self,
        args: &EmailRoutingDestinationAddressesGetADestinationAddressArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailDestinationAddressResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_routing_destination_addresses_get_a_destination_address_builder(
            &self.http_client,
            &args.destination_address_identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_routing_destination_addresses_get_a_destination_address_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email routing destination addresses delete destination address.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailDestinationAddressResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_routing_destination_addresses_delete_destination_address(
        &self,
        args: &EmailRoutingDestinationAddressesDeleteDestinationAddressArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailDestinationAddressResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_routing_destination_addresses_delete_destination_address_builder(
            &self.http_client,
            &args.destination_address_identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_routing_destination_addresses_delete_destination_address_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email sending account send builder.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_sending_account_send_builder(
        &self,
        args: &EmailSendingAccountSendBuilderArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_sending_account_send_builder_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_sending_account_send_builder_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email sending account send raw message.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_sending_account_send_raw_message(
        &self,
        args: &EmailSendingAccountSendRawMessageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_sending_account_send_raw_message_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_sending_account_send_raw_message_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 get event notification configs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn r2_get_event_notification_configs(
        &self,
        args: &R2GetEventNotificationConfigsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_get_event_notification_configs_builder(
            &self.http_client,
            &args.bucket_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_get_event_notification_configs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 get event notification config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn r2_get_event_notification_config(
        &self,
        args: &R2GetEventNotificationConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_get_event_notification_config_builder(
            &self.http_client,
            &args.queue_id,
            &args.bucket_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_get_event_notification_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 put event notification config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn r2_put_event_notification_config(
        &self,
        args: &R2PutEventNotificationConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_put_event_notification_config_builder(
            &self.http_client,
            &args.queue_id,
            &args.bucket_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_put_event_notification_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 event notification delete config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn r2_event_notification_delete_config(
        &self,
        args: &R2EventNotificationDeleteConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_event_notification_delete_config_builder(
            &self.http_client,
            &args.queue_id,
            &args.bucket_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_event_notification_delete_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Subscriptions list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn subscriptions_list(
        &self,
        args: &SubscriptionsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = subscriptions_list_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = subscriptions_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Subscriptions create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn subscriptions_create(
        &self,
        args: &SubscriptionsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = subscriptions_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = subscriptions_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Subscriptions get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn subscriptions_get(
        &self,
        args: &SubscriptionsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = subscriptions_get_builder(
            &self.http_client,
            &args.account_id,
            &args.subscription_id,
        )
        .map_err(ProviderError::Api)?;

        let task = subscriptions_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Subscriptions patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn subscriptions_patch(
        &self,
        args: &SubscriptionsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = subscriptions_patch_builder(
            &self.http_client,
            &args.account_id,
            &args.subscription_id,
        )
        .map_err(ProviderError::Api)?;

        let task = subscriptions_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Subscriptions delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn subscriptions_delete(
        &self,
        args: &SubscriptionsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = subscriptions_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.subscription_id,
        )
        .map_err(ProviderError::Api)?;

        let task = subscriptions_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip access rules for an account list ip access rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ip_access_rules_for_an_account_list_ip_access_rules(
        &self,
        args: &IpAccessRulesForAnAccountListIpAccessRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_access_rules_for_an_account_list_ip_access_rules_builder(
            &self.http_client,
            &args.account_id,
            &args.mode,
            &args.configuration_target,
            &args.configuration_value,
            &args.notes,
            &args.match_rs,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_access_rules_for_an_account_list_ip_access_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip access rules for an account create an ip access rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_access_rules_for_an_account_create_an_ip_access_rule(
        &self,
        args: &IpAccessRulesForAnAccountCreateAnIpAccessRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_access_rules_for_an_account_create_an_ip_access_rule_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_access_rules_for_an_account_create_an_ip_access_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip access rules for an account get an ip access rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ip_access_rules_for_an_account_get_an_ip_access_rule(
        &self,
        args: &IpAccessRulesForAnAccountGetAnIpAccessRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_access_rules_for_an_account_get_an_ip_access_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_access_rules_for_an_account_get_an_ip_access_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip access rules for an account update an ip access rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_access_rules_for_an_account_update_an_ip_access_rule(
        &self,
        args: &IpAccessRulesForAnAccountUpdateAnIpAccessRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_access_rules_for_an_account_update_an_ip_access_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_access_rules_for_an_account_update_an_ip_access_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip access rules for an account delete an ip access rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallApiResponseSingleId result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_access_rules_for_an_account_delete_an_ip_access_rule(
        &self,
        args: &IpAccessRulesForAnAccountDeleteAnIpAccessRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallApiResponseSingleId, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_access_rules_for_an_account_delete_an_ip_access_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_access_rules_for_an_account_delete_an_ip_access_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust accounts get zero trust account information.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayGatewayAccount result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_accounts_get_zero_trust_account_information(
        &self,
        args: &ZeroTrustAccountsGetZeroTrustAccountInformationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayGatewayAccount, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_accounts_get_zero_trust_account_information_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_accounts_get_zero_trust_account_information_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust accounts create zero trust account.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayGatewayAccount result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_accounts_create_zero_trust_account(
        &self,
        args: &ZeroTrustAccountsCreateZeroTrustAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayGatewayAccount, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_accounts_create_zero_trust_account_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_accounts_create_zero_trust_account_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway application and application type mappings list application and application type mappings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayAppTypesComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_gateway_application_and_application_type_mappings_list_application_and_application_type_mappings(
        &self,
        args: &ZeroTrustGatewayApplicationAndApplicationTypeMappingsListApplicationAndApplicationTypeMappingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayAppTypesComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_application_and_application_type_mappings_list_application_and_application_type_mappings_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_application_and_application_type_mappings_list_application_and_application_type_mappings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust applications review status list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayApplicationsReviewStatusResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_applications_review_status_list(
        &self,
        args: &ZeroTrustApplicationsReviewStatusListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayApplicationsReviewStatusResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_applications_review_status_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_applications_review_status_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust applications review status update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayApplicationsReviewStatusResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_applications_review_status_update(
        &self,
        args: &ZeroTrustApplicationsReviewStatusUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayApplicationsReviewStatusResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_applications_review_status_update_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_applications_review_status_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust get audit ssh settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayAuditSshSettingsComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_get_audit_ssh_settings(
        &self,
        args: &ZeroTrustGetAuditSshSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayAuditSshSettingsComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_get_audit_ssh_settings_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_get_audit_ssh_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust update audit ssh settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayAuditSshSettingsComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_update_audit_ssh_settings(
        &self,
        args: &ZeroTrustUpdateAuditSshSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayAuditSshSettingsComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_update_audit_ssh_settings_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_update_audit_ssh_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust rotate ssh account seed.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayAuditSshSettingsComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_rotate_ssh_account_seed(
        &self,
        args: &ZeroTrustRotateSshAccountSeedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayAuditSshSettingsComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_rotate_ssh_account_seed_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_rotate_ssh_account_seed_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway categories list categories.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayCategoriesComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_gateway_categories_list_categories(
        &self,
        args: &ZeroTrustGatewayCategoriesListCategoriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayCategoriesComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_categories_list_categories_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_categories_list_categories_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust certificates list zero trust certificates.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_certificates_list_zero_trust_certificates(
        &self,
        args: &ZeroTrustCertificatesListZeroTrustCertificatesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_certificates_list_zero_trust_certificates_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_certificates_list_zero_trust_certificates_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust certificates create zero trust certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewaySingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_certificates_create_zero_trust_certificate(
        &self,
        args: &ZeroTrustCertificatesCreateZeroTrustCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewaySingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_certificates_create_zero_trust_certificate_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_certificates_create_zero_trust_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust certificates zero trust certificate details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewaySingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_certificates_zero_trust_certificate_details(
        &self,
        args: &ZeroTrustCertificatesZeroTrustCertificateDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewaySingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_certificates_zero_trust_certificate_details_builder(
            &self.http_client,
            &args.certificate_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_certificates_zero_trust_certificate_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust certificates delete zero trust certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewaySingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_certificates_delete_zero_trust_certificate(
        &self,
        args: &ZeroTrustCertificatesDeleteZeroTrustCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewaySingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_certificates_delete_zero_trust_certificate_builder(
            &self.http_client,
            &args.certificate_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_certificates_delete_zero_trust_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust certificates activate zero trust certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewaySingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_certificates_activate_zero_trust_certificate(
        &self,
        args: &ZeroTrustCertificatesActivateZeroTrustCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewaySingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_certificates_activate_zero_trust_certificate_builder(
            &self.http_client,
            &args.certificate_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_certificates_activate_zero_trust_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust certificates deactivate zero trust certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewaySingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_certificates_deactivate_zero_trust_certificate(
        &self,
        args: &ZeroTrustCertificatesDeactivateZeroTrustCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewaySingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_certificates_deactivate_zero_trust_certificate_builder(
            &self.http_client,
            &args.certificate_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_certificates_deactivate_zero_trust_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust accounts get zero trust account configuration.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayGatewayAccountConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_accounts_get_zero_trust_account_configuration(
        &self,
        args: &ZeroTrustAccountsGetZeroTrustAccountConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayGatewayAccountConfig, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_accounts_get_zero_trust_account_configuration_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_accounts_get_zero_trust_account_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust accounts update zero trust account configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayGatewayAccountConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_accounts_update_zero_trust_account_configuration(
        &self,
        args: &ZeroTrustAccountsUpdateZeroTrustAccountConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayGatewayAccountConfig, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_accounts_update_zero_trust_account_configuration_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_accounts_update_zero_trust_account_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust accounts patch zero trust account configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayGatewayAccountConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_accounts_patch_zero_trust_account_configuration(
        &self,
        args: &ZeroTrustAccountsPatchZeroTrustAccountConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayGatewayAccountConfig, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_accounts_patch_zero_trust_account_configuration_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_accounts_patch_zero_trust_account_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust accounts get zero trust certificate configuration.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayCustomCertificateSettings result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_accounts_get_zero_trust_certificate_configuration(
        &self,
        args: &ZeroTrustAccountsGetZeroTrustCertificateConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayCustomCertificateSettings, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_accounts_get_zero_trust_certificate_configuration_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_accounts_get_zero_trust_certificate_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust lists list zero trust lists.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewaySchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_lists_list_zero_trust_lists(
        &self,
        args: &ZeroTrustListsListZeroTrustListsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewaySchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_lists_list_zero_trust_lists_builder(
            &self.http_client,
            &args.account_id,
            &args.type_rs,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_lists_list_zero_trust_lists_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust lists create zero trust list.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewaySingleResponseWithListItems result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_lists_create_zero_trust_list(
        &self,
        args: &ZeroTrustListsCreateZeroTrustListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewaySingleResponseWithListItems, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_lists_create_zero_trust_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_lists_create_zero_trust_list_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust lists zero trust list details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayListSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_lists_zero_trust_list_details(
        &self,
        args: &ZeroTrustListsZeroTrustListDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayListSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_lists_zero_trust_list_details_builder(
            &self.http_client,
            &args.list_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_lists_zero_trust_list_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust lists update zero trust list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayListSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_lists_update_zero_trust_list(
        &self,
        args: &ZeroTrustListsUpdateZeroTrustListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayListSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_lists_update_zero_trust_list_builder(
            &self.http_client,
            &args.list_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_lists_update_zero_trust_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust lists patch zero trust list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayListSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_lists_patch_zero_trust_list(
        &self,
        args: &ZeroTrustListsPatchZeroTrustListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayListSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_lists_patch_zero_trust_list_builder(
            &self.http_client,
            &args.list_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_lists_patch_zero_trust_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust lists delete zero trust list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayEmptyResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_lists_delete_zero_trust_list(
        &self,
        args: &ZeroTrustListsDeleteZeroTrustListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayEmptyResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_lists_delete_zero_trust_list_builder(
            &self.http_client,
            &args.list_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_lists_delete_zero_trust_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust lists zero trust list items.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayListItemResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_lists_zero_trust_list_items(
        &self,
        args: &ZeroTrustListsZeroTrustListItemsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayListItemResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_lists_zero_trust_list_items_builder(
            &self.http_client,
            &args.list_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_lists_zero_trust_list_items_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway locations list zero trust gateway locations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_gateway_locations_list_zero_trust_gateway_locations(
        &self,
        args: &ZeroTrustGatewayLocationsListZeroTrustGatewayLocationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_locations_list_zero_trust_gateway_locations_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_locations_list_zero_trust_gateway_locations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway locations create zero trust gateway location.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewaySchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_gateway_locations_create_zero_trust_gateway_location(
        &self,
        args: &ZeroTrustGatewayLocationsCreateZeroTrustGatewayLocationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewaySchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_locations_create_zero_trust_gateway_location_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_locations_create_zero_trust_gateway_location_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway locations zero trust gateway location details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewaySchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_gateway_locations_zero_trust_gateway_location_details(
        &self,
        args: &ZeroTrustGatewayLocationsZeroTrustGatewayLocationDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewaySchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_locations_zero_trust_gateway_location_details_builder(
            &self.http_client,
            &args.location_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_locations_zero_trust_gateway_location_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway locations update zero trust gateway location.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewaySchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_gateway_locations_update_zero_trust_gateway_location(
        &self,
        args: &ZeroTrustGatewayLocationsUpdateZeroTrustGatewayLocationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewaySchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_locations_update_zero_trust_gateway_location_builder(
            &self.http_client,
            &args.location_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_locations_update_zero_trust_gateway_location_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway locations delete zero trust gateway location.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayEmptyResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_gateway_locations_delete_zero_trust_gateway_location(
        &self,
        args: &ZeroTrustGatewayLocationsDeleteZeroTrustGatewayLocationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayEmptyResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_locations_delete_zero_trust_gateway_location_builder(
            &self.http_client,
            &args.location_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_locations_delete_zero_trust_gateway_location_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust accounts get logging settings for the zero trust account.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayGatewayAccountLoggingSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_accounts_get_logging_settings_for_the_zero_trust_account(
        &self,
        args: &ZeroTrustAccountsGetLoggingSettingsForTheZeroTrustAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayGatewayAccountLoggingSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_accounts_get_logging_settings_for_the_zero_trust_account_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_accounts_get_logging_settings_for_the_zero_trust_account_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust accounts update logging settings for the zero trust account.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayGatewayAccountLoggingSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_accounts_update_logging_settings_for_the_zero_trust_account(
        &self,
        args: &ZeroTrustAccountsUpdateLoggingSettingsForTheZeroTrustAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayGatewayAccountLoggingSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_accounts_update_logging_settings_for_the_zero_trust_account_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_accounts_update_logging_settings_for_the_zero_trust_account_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway pacfiles list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayPacfilesComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_gateway_pacfiles_list(
        &self,
        args: &ZeroTrustGatewayPacfilesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayPacfilesComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_pacfiles_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_pacfiles_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway pacfiles create pacfile.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayPacfilesComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_gateway_pacfiles_create_pacfile(
        &self,
        args: &ZeroTrustGatewayPacfilesCreatePacfileArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayPacfilesComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_pacfiles_create_pacfile_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_pacfiles_create_pacfile_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway pacfiles details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayPacfilesComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_gateway_pacfiles_details(
        &self,
        args: &ZeroTrustGatewayPacfilesDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayPacfilesComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_pacfiles_details_builder(
            &self.http_client,
            &args.pacfile_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_pacfiles_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway pacfiles update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayPacfilesComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_gateway_pacfiles_update(
        &self,
        args: &ZeroTrustGatewayPacfilesUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayPacfilesComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_pacfiles_update_builder(
            &self.http_client,
            &args.pacfile_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_pacfiles_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway pacfiles delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayEmptyResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_gateway_pacfiles_delete(
        &self,
        args: &ZeroTrustGatewayPacfilesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayEmptyResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_pacfiles_delete_builder(
            &self.http_client,
            &args.pacfile_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_pacfiles_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway proxy endpoints list proxy endpoints.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayProxyEndpointsComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_gateway_proxy_endpoints_list_proxy_endpoints(
        &self,
        args: &ZeroTrustGatewayProxyEndpointsListProxyEndpointsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayProxyEndpointsComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_proxy_endpoints_list_proxy_endpoints_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_proxy_endpoints_list_proxy_endpoints_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway proxy endpoints create proxy endpoint.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayProxyEndpointsComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_gateway_proxy_endpoints_create_proxy_endpoint(
        &self,
        args: &ZeroTrustGatewayProxyEndpointsCreateProxyEndpointArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayProxyEndpointsComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_proxy_endpoints_create_proxy_endpoint_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_proxy_endpoints_create_proxy_endpoint_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway proxy endpoints proxy endpoint details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayProxyEndpointsComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_gateway_proxy_endpoints_proxy_endpoint_details(
        &self,
        args: &ZeroTrustGatewayProxyEndpointsProxyEndpointDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayProxyEndpointsComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_proxy_endpoints_proxy_endpoint_details_builder(
            &self.http_client,
            &args.proxy_endpoint_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_proxy_endpoints_proxy_endpoint_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway proxy endpoints update proxy endpoint.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayProxyEndpointsComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_gateway_proxy_endpoints_update_proxy_endpoint(
        &self,
        args: &ZeroTrustGatewayProxyEndpointsUpdateProxyEndpointArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayProxyEndpointsComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_proxy_endpoints_update_proxy_endpoint_builder(
            &self.http_client,
            &args.proxy_endpoint_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_proxy_endpoints_update_proxy_endpoint_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway proxy endpoints delete proxy endpoint.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayEmptyResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_gateway_proxy_endpoints_delete_proxy_endpoint(
        &self,
        args: &ZeroTrustGatewayProxyEndpointsDeleteProxyEndpointArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayEmptyResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_proxy_endpoints_delete_proxy_endpoint_builder(
            &self.http_client,
            &args.proxy_endpoint_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_proxy_endpoints_delete_proxy_endpoint_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway rules list zero trust gateway rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayRulesComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_gateway_rules_list_zero_trust_gateway_rules(
        &self,
        args: &ZeroTrustGatewayRulesListZeroTrustGatewayRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayRulesComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_rules_list_zero_trust_gateway_rules_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_rules_list_zero_trust_gateway_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway rules create zero trust gateway rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_gateway_rules_create_zero_trust_gateway_rule(
        &self,
        args: &ZeroTrustGatewayRulesCreateZeroTrustGatewayRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_rules_create_zero_trust_gateway_rule_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_rules_create_zero_trust_gateway_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway rules list zero trust gateway rules tenant.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayRulesComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_gateway_rules_list_zero_trust_gateway_rules_tenant(
        &self,
        args: &ZeroTrustGatewayRulesListZeroTrustGatewayRulesTenantArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayRulesComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_rules_list_zero_trust_gateway_rules_tenant_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_rules_list_zero_trust_gateway_rules_tenant_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway rules zero trust gateway rule details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_gateway_rules_zero_trust_gateway_rule_details(
        &self,
        args: &ZeroTrustGatewayRulesZeroTrustGatewayRuleDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_rules_zero_trust_gateway_rule_details_builder(
            &self.http_client,
            &args.rule_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_rules_zero_trust_gateway_rule_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway rules update zero trust gateway rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_gateway_rules_update_zero_trust_gateway_rule(
        &self,
        args: &ZeroTrustGatewayRulesUpdateZeroTrustGatewayRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_rules_update_zero_trust_gateway_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_rules_update_zero_trust_gateway_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway rules delete zero trust gateway rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayEmptyResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_gateway_rules_delete_zero_trust_gateway_rule(
        &self,
        args: &ZeroTrustGatewayRulesDeleteZeroTrustGatewayRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayEmptyResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_rules_delete_zero_trust_gateway_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_rules_delete_zero_trust_gateway_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust gateway rules reset expiration zero trust gateway rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZeroTrustGatewayComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_gateway_rules_reset_expiration_zero_trust_gateway_rule(
        &self,
        args: &ZeroTrustGatewayRulesResetExpirationZeroTrustGatewayRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZeroTrustGatewayComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_gateway_rules_reset_expiration_zero_trust_gateway_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_gateway_rules_reset_expiration_zero_trust_gateway_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List hyperdrive.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_hyperdrive(
        &self,
        args: &ListHyperdriveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_hyperdrive_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = list_hyperdrive_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create hyperdrive.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_hyperdrive(
        &self,
        args: &CreateHyperdriveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_hyperdrive_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_hyperdrive_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get hyperdrive.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_hyperdrive(
        &self,
        args: &GetHyperdriveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_hyperdrive_builder(
            &self.http_client,
            &args.account_id,
            &args.hyperdrive_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_hyperdrive_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update hyperdrive.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_hyperdrive(
        &self,
        args: &UpdateHyperdriveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_hyperdrive_builder(
            &self.http_client,
            &args.account_id,
            &args.hyperdrive_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_hyperdrive_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Patch hyperdrive.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn patch_hyperdrive(
        &self,
        args: &PatchHyperdriveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = patch_hyperdrive_builder(
            &self.http_client,
            &args.account_id,
            &args.hyperdrive_id,
        )
        .map_err(ProviderError::Api)?;

        let task = patch_hyperdrive_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete hyperdrive.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_hyperdrive(
        &self,
        args: &DeleteHyperdriveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_hyperdrive_builder(
            &self.http_client,
            &args.account_id,
            &args.hyperdrive_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_hyperdrive_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account permission group list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamCollectionPermissionGroupsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_permission_group_list(
        &self,
        args: &AccountPermissionGroupListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamCollectionPermissionGroupsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_permission_group_list_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
            &args.name,
            &args.label,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = account_permission_group_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account permission group details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSinglePermissionGroupsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_permission_group_details(
        &self,
        args: &AccountPermissionGroupDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSinglePermissionGroupsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_permission_group_details_builder(
            &self.http_client,
            &args.account_id,
            &args.permission_group_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_permission_group_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account resource group list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_resource_group_list(
        &self,
        args: &AccountResourceGroupListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_resource_group_list_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = account_resource_group_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account resource group create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_resource_group_create(
        &self,
        args: &AccountResourceGroupCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_resource_group_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_resource_group_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account resource group details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_resource_group_details(
        &self,
        args: &AccountResourceGroupDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_resource_group_details_builder(
            &self.http_client,
            &args.account_id,
            &args.resource_group_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_resource_group_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account resource group update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_resource_group_update(
        &self,
        args: &AccountResourceGroupUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_resource_group_update_builder(
            &self.http_client,
            &args.account_id,
            &args.resource_group_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_resource_group_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account resource group delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamApiResponseSingleId result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_resource_group_delete(
        &self,
        args: &AccountResourceGroupDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamApiResponseSingleId, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_resource_group_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.resource_group_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_resource_group_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account user group list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_user_group_list(
        &self,
        args: &AccountUserGroupListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_user_group_list_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
            &args.name,
            &args.fuzzyName,
            &args.page,
            &args.per_page,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = account_user_group_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account user group create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_user_group_create(
        &self,
        args: &AccountUserGroupCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_user_group_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_user_group_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account user group details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_user_group_details(
        &self,
        args: &AccountUserGroupDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_user_group_details_builder(
            &self.http_client,
            &args.account_id,
            &args.user_group_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_user_group_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account user group update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_user_group_update(
        &self,
        args: &AccountUserGroupUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_user_group_update_builder(
            &self.http_client,
            &args.account_id,
            &args.user_group_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_user_group_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account user group delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamApiResponseSingleId result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_user_group_delete(
        &self,
        args: &AccountUserGroupDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamApiResponseSingleId, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_user_group_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.user_group_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_user_group_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account user group member list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_user_group_member_list(
        &self,
        args: &AccountUserGroupMemberListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_user_group_member_list_builder(
            &self.http_client,
            &args.account_id,
            &args.user_group_id,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = account_user_group_member_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account user group member create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_user_group_member_create(
        &self,
        args: &AccountUserGroupMemberCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_user_group_member_create_builder(
            &self.http_client,
            &args.account_id,
            &args.user_group_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_user_group_member_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account user group members update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_user_group_members_update(
        &self,
        args: &AccountUserGroupMembersUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_user_group_members_update_builder(
            &self.http_client,
            &args.account_id,
            &args.user_group_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_user_group_members_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account user group member delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_user_group_member_delete(
        &self,
        args: &AccountUserGroupMemberDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_user_group_member_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.user_group_id,
            &args.member_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_user_group_member_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare images list images.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ImagesImagesListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_images_list_images(
        &self,
        args: &CloudflareImagesListImagesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ImagesImagesListResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_images_list_images_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.creator,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_images_list_images_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare images upload an image via url.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ImagesImageResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudflare_images_upload_an_image_via_url(
        &self,
        args: &CloudflareImagesUploadAnImageViaUrlArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ImagesImageResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_images_upload_an_image_via_url_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_images_upload_an_image_via_url_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare images keys list signing keys.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ImagesImageKeyResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_images_keys_list_signing_keys(
        &self,
        args: &CloudflareImagesKeysListSigningKeysArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ImagesImageKeyResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_images_keys_list_signing_keys_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_images_keys_list_signing_keys_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare images keys add signing key.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ImagesImageKeyResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudflare_images_keys_add_signing_key(
        &self,
        args: &CloudflareImagesKeysAddSigningKeyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ImagesImageKeyResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_images_keys_add_signing_key_builder(
            &self.http_client,
            &args.signing_key_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_images_keys_add_signing_key_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare images keys delete signing key.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ImagesImageKeyResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudflare_images_keys_delete_signing_key(
        &self,
        args: &CloudflareImagesKeysDeleteSigningKeyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ImagesImageKeyResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_images_keys_delete_signing_key_builder(
            &self.http_client,
            &args.signing_key_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_images_keys_delete_signing_key_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare images images usage statistics.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ImagesImagesStatsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_images_images_usage_statistics(
        &self,
        args: &CloudflareImagesImagesUsageStatisticsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ImagesImagesStatsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_images_images_usage_statistics_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_images_images_usage_statistics_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare images variants list variants.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ImagesImageVariantListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_images_variants_list_variants(
        &self,
        args: &CloudflareImagesVariantsListVariantsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ImagesImageVariantListResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_images_variants_list_variants_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_images_variants_list_variants_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare images variants create a variant.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ImagesImageVariantSimpleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudflare_images_variants_create_a_variant(
        &self,
        args: &CloudflareImagesVariantsCreateAVariantArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ImagesImageVariantSimpleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_images_variants_create_a_variant_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_images_variants_create_a_variant_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare images variants variant details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ImagesImageVariantSimpleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_images_variants_variant_details(
        &self,
        args: &CloudflareImagesVariantsVariantDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ImagesImageVariantSimpleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_images_variants_variant_details_builder(
            &self.http_client,
            &args.variant_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_images_variants_variant_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare images variants update a variant.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ImagesImageVariantSimpleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudflare_images_variants_update_a_variant(
        &self,
        args: &CloudflareImagesVariantsUpdateAVariantArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ImagesImageVariantSimpleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_images_variants_update_a_variant_builder(
            &self.http_client,
            &args.variant_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_images_variants_update_a_variant_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare images variants delete a variant.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ImagesDeletedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudflare_images_variants_delete_a_variant(
        &self,
        args: &CloudflareImagesVariantsDeleteAVariantArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ImagesDeletedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_images_variants_delete_a_variant_builder(
            &self.http_client,
            &args.variant_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_images_variants_delete_a_variant_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare images variants variant details flat.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ImagesImageVariantFlatResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_images_variants_variant_details_flat(
        &self,
        args: &CloudflareImagesVariantsVariantDetailsFlatArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ImagesImageVariantFlatResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_images_variants_variant_details_flat_builder(
            &self.http_client,
            &args.variant_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_images_variants_variant_details_flat_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare images image details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ImagesImageResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_images_image_details(
        &self,
        args: &CloudflareImagesImageDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ImagesImageResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_images_image_details_builder(
            &self.http_client,
            &args.image_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_images_image_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare images update image.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ImagesImageResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudflare_images_update_image(
        &self,
        args: &CloudflareImagesUpdateImageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ImagesImageResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_images_update_image_builder(
            &self.http_client,
            &args.image_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_images_update_image_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare images delete image.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ImagesDeletedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudflare_images_delete_image(
        &self,
        args: &CloudflareImagesDeleteImageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ImagesDeletedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_images_delete_image_builder(
            &self.http_client,
            &args.image_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_images_delete_image_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare images base image.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_images_base_image(
        &self,
        args: &CloudflareImagesBaseImageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_images_base_image_builder(
            &self.http_client,
            &args.image_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_images_base_image_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare images list images v2.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ImagesImagesListResponseV2 result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_images_list_images_v2(
        &self,
        args: &CloudflareImagesListImagesV2Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<ImagesImagesListResponseV2, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_images_list_images_v2_builder(
            &self.http_client,
            &args.account_id,
            &args.continuation_token,
            &args.per_page,
            &args.sort_order,
            &args.creator,
            &args.meta__field___operator__,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_images_list_images_v2_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare images create authenticated direct upload url v 2.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ImagesImageDirectUploadResponseV2 result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudflare_images_create_authenticated_direct_upload_url_v_2(
        &self,
        args: &CloudflareImagesCreateAuthenticatedDirectUploadUrlV2Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<ImagesImageDirectUploadResponseV2, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_images_create_authenticated_direct_upload_url_v_2_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_images_create_authenticated_direct_upload_url_v_2_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Infra targets list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn infra_targets_list(
        &self,
        args: &InfraTargetsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = infra_targets_list_builder(
            &self.http_client,
            &args.account_id,
            &args.hostname,
            &args.hostname_contains,
            &args.virtual_network_id,
            &args.ip_v4,
            &args.ip_v6,
            &args.created_before,
            &args.created_after,
            &args.modified_before,
            &args.modified_after,
            &args.ips,
            &args.target_ids,
            &args.ip_like,
            &args.ipv4_start,
            &args.ipv4_end,
            &args.ipv6_start,
            &args.ipv6_end,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = infra_targets_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Infra targets post.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn infra_targets_post(
        &self,
        args: &InfraTargetsPostArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = infra_targets_post_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = infra_targets_post_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Infra targets put batch.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn infra_targets_put_batch(
        &self,
        args: &InfraTargetsPutBatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = infra_targets_put_batch_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = infra_targets_put_batch_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Infra targets delete batch.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn infra_targets_delete_batch(
        &self,
        args: &InfraTargetsDeleteBatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = infra_targets_delete_batch_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = infra_targets_delete_batch_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Infra targets delete batch post.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn infra_targets_delete_batch_post(
        &self,
        args: &InfraTargetsDeleteBatchPostArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = infra_targets_delete_batch_post_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = infra_targets_delete_batch_post_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Infra targets get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn infra_targets_get(
        &self,
        args: &InfraTargetsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = infra_targets_get_builder(
            &self.http_client,
            &args.account_id,
            &args.target_id,
        )
        .map_err(ProviderError::Api)?;

        let task = infra_targets_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Infra targets put.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn infra_targets_put(
        &self,
        args: &InfraTargetsPutArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = infra_targets_put_builder(
            &self.http_client,
            &args.account_id,
            &args.target_id,
        )
        .map_err(ProviderError::Api)?;

        let task = infra_targets_put_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Infra targets delete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn infra_targets_delete(
        &self,
        args: &InfraTargetsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = infra_targets_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.target_id,
        )
        .map_err(ProviderError::Api)?;

        let task = infra_targets_delete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Asn intelligence get asn overview.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IntelAsnComponentsSchemasResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn asn_intelligence_get_asn_overview(
        &self,
        args: &AsnIntelligenceGetAsnOverviewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IntelAsnComponentsSchemasResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = asn_intelligence_get_asn_overview_builder(
            &self.http_client,
            &args.asn,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = asn_intelligence_get_asn_overview_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Asn intelligence get asn subnets.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn asn_intelligence_get_asn_subnets(
        &self,
        args: &AsnIntelligenceGetAsnSubnetsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = asn_intelligence_get_asn_subnets_builder(
            &self.http_client,
            &args.asn,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = asn_intelligence_get_asn_subnets_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get security center issue types.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_security_center_issue_types(
        &self,
        args: &GetSecurityCenterIssueTypesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_security_center_issue_types_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_security_center_issue_types_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get security center issues.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_security_center_issues(
        &self,
        args: &GetSecurityCenterIssuesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_security_center_issues_builder(
            &self.http_client,
            &args.account_id,
            &args.dismissed,
            &args.issue_class,
            &args.issue_type,
            &args.product,
            &args.severity,
            &args.subject,
            &args.issue_class_neq,
            &args.issue_type_neq,
            &args.product_neq,
            &args.severity_neq,
            &args.subject_neq,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = get_security_center_issues_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get security center issue counts by class.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityCenterValueCountsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_security_center_issue_counts_by_class(
        &self,
        args: &GetSecurityCenterIssueCountsByClassArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityCenterValueCountsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_security_center_issue_counts_by_class_builder(
            &self.http_client,
            &args.account_id,
            &args.dismissed,
            &args.issue_class,
            &args.issue_type,
            &args.product,
            &args.severity,
            &args.subject,
            &args.issue_class_neq,
            &args.issue_type_neq,
            &args.product_neq,
            &args.severity_neq,
            &args.subject_neq,
        )
        .map_err(ProviderError::Api)?;

        let task = get_security_center_issue_counts_by_class_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get security center issue counts by severity.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityCenterValueCountsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_security_center_issue_counts_by_severity(
        &self,
        args: &GetSecurityCenterIssueCountsBySeverityArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityCenterValueCountsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_security_center_issue_counts_by_severity_builder(
            &self.http_client,
            &args.account_id,
            &args.dismissed,
            &args.issue_class,
            &args.issue_type,
            &args.product,
            &args.severity,
            &args.subject,
            &args.issue_class_neq,
            &args.issue_type_neq,
            &args.product_neq,
            &args.severity_neq,
            &args.subject_neq,
        )
        .map_err(ProviderError::Api)?;

        let task = get_security_center_issue_counts_by_severity_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get security center issue counts by type.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityCenterValueCountsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_security_center_issue_counts_by_type(
        &self,
        args: &GetSecurityCenterIssueCountsByTypeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityCenterValueCountsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_security_center_issue_counts_by_type_builder(
            &self.http_client,
            &args.account_id,
            &args.dismissed,
            &args.issue_class,
            &args.issue_type,
            &args.product,
            &args.severity,
            &args.subject,
            &args.issue_class_neq,
            &args.issue_type_neq,
            &args.product_neq,
            &args.severity_neq,
            &args.subject_neq,
        )
        .map_err(ProviderError::Api)?;

        let task = get_security_center_issue_counts_by_type_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Archive security center insight deprecated.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn archive_security_center_insight_deprecated(
        &self,
        args: &ArchiveSecurityCenterInsightDeprecatedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = archive_security_center_insight_deprecated_builder(
            &self.http_client,
            &args.account_id,
            &args.issue_id,
        )
        .map_err(ProviderError::Api)?;

        let task = archive_security_center_insight_deprecated_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Passive dns by ip get passive dns by ip.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IntelComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn passive_dns_by_ip_get_passive_dns_by_ip(
        &self,
        args: &PassiveDnsByIpGetPassiveDnsByIpArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IntelComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = passive_dns_by_ip_get_passive_dns_by_ip_builder(
            &self.http_client,
            &args.account_id,
            &args.start_end_params,
            &args.ipv4,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = passive_dns_by_ip_get_passive_dns_by_ip_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Domain intelligence get domain details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IntelSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn domain_intelligence_get_domain_details(
        &self,
        args: &DomainIntelligenceGetDomainDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IntelSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = domain_intelligence_get_domain_details_builder(
            &self.http_client,
            &args.account_id,
            &args.domain,
        )
        .map_err(ProviderError::Api)?;

        let task = domain_intelligence_get_domain_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Domain history get domain history.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IntelResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn domain_history_get_domain_history(
        &self,
        args: &DomainHistoryGetDomainHistoryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IntelResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = domain_history_get_domain_history_builder(
            &self.http_client,
            &args.account_id,
            &args.domain,
        )
        .map_err(ProviderError::Api)?;

        let task = domain_history_get_domain_history_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Domain intelligence get multiple domain details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IntelCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn domain_intelligence_get_multiple_domain_details(
        &self,
        args: &DomainIntelligenceGetMultipleDomainDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IntelCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = domain_intelligence_get_multiple_domain_details_builder(
            &self.http_client,
            &args.account_id,
            &args.domain,
        )
        .map_err(ProviderError::Api)?;

        let task = domain_intelligence_get_multiple_domain_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom indicator feeds get indicator feeds.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomIndicatorFeedsIndicatorFeedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_indicator_feeds_get_indicator_feeds(
        &self,
        args: &CustomIndicatorFeedsGetIndicatorFeedsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomIndicatorFeedsIndicatorFeedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_indicator_feeds_get_indicator_feeds_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_indicator_feeds_get_indicator_feeds_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom indicator feeds create indicator feeds.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomIndicatorFeedsCreateFeedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_indicator_feeds_create_indicator_feeds(
        &self,
        args: &CustomIndicatorFeedsCreateIndicatorFeedsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomIndicatorFeedsCreateFeedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_indicator_feeds_create_indicator_feeds_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_indicator_feeds_create_indicator_feeds_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom indicator feeds add permission.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomIndicatorFeedsPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_indicator_feeds_add_permission(
        &self,
        args: &CustomIndicatorFeedsAddPermissionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomIndicatorFeedsPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_indicator_feeds_add_permission_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_indicator_feeds_add_permission_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom indicator feeds remove permission.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomIndicatorFeedsPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_indicator_feeds_remove_permission(
        &self,
        args: &CustomIndicatorFeedsRemovePermissionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomIndicatorFeedsPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_indicator_feeds_remove_permission_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_indicator_feeds_remove_permission_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom indicator feeds view permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomIndicatorFeedsPermissionListItemResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_indicator_feeds_view_permissions(
        &self,
        args: &CustomIndicatorFeedsViewPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomIndicatorFeedsPermissionListItemResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_indicator_feeds_view_permissions_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_indicator_feeds_view_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom indicator feeds get indicator feed metadata.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomIndicatorFeedsIndicatorFeedMetadataResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_indicator_feeds_get_indicator_feed_metadata(
        &self,
        args: &CustomIndicatorFeedsGetIndicatorFeedMetadataArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomIndicatorFeedsIndicatorFeedMetadataResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_indicator_feeds_get_indicator_feed_metadata_builder(
            &self.http_client,
            &args.account_id,
            &args.feed_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_indicator_feeds_get_indicator_feed_metadata_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom indicator feeds update indicator feed metadata.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomIndicatorFeedsUpdatePublicFieldResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_indicator_feeds_update_indicator_feed_metadata(
        &self,
        args: &CustomIndicatorFeedsUpdateIndicatorFeedMetadataArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomIndicatorFeedsUpdatePublicFieldResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_indicator_feeds_update_indicator_feed_metadata_builder(
            &self.http_client,
            &args.account_id,
            &args.feed_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_indicator_feeds_update_indicator_feed_metadata_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom indicator feeds get indicator feed data.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_indicator_feeds_get_indicator_feed_data(
        &self,
        args: &CustomIndicatorFeedsGetIndicatorFeedDataArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_indicator_feeds_get_indicator_feed_data_builder(
            &self.http_client,
            &args.account_id,
            &args.feed_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_indicator_feeds_get_indicator_feed_data_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom indicator feeds download indicator feed data.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomIndicatorFeedsUpdateFeedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_indicator_feeds_download_indicator_feed_data(
        &self,
        args: &CustomIndicatorFeedsDownloadIndicatorFeedDataArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomIndicatorFeedsUpdateFeedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_indicator_feeds_download_indicator_feed_data_builder(
            &self.http_client,
            &args.account_id,
            &args.feed_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_indicator_feeds_download_indicator_feed_data_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom indicator feeds update indicator feed data.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomIndicatorFeedsUpdateFeedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_indicator_feeds_update_indicator_feed_data(
        &self,
        args: &CustomIndicatorFeedsUpdateIndicatorFeedDataArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomIndicatorFeedsUpdateFeedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_indicator_feeds_update_indicator_feed_data_builder(
            &self.http_client,
            &args.account_id,
            &args.feed_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_indicator_feeds_update_indicator_feed_data_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip intelligence get ip overview.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IntelSchemasResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ip_intelligence_get_ip_overview(
        &self,
        args: &IpIntelligenceGetIpOverviewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IntelSchemasResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_intelligence_get_ip_overview_builder(
            &self.http_client,
            &args.account_id,
            &args.ipv4,
            &args.ipv6,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_intelligence_get_ip_overview_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip list get ip lists.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IntelComponentsSchemasResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ip_list_get_ip_lists(
        &self,
        args: &IpListGetIpListsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IntelComponentsSchemasResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_list_get_ip_lists_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_list_get_ip_lists_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Miscategorization create miscategorization.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IntelApiResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn miscategorization_create_miscategorization(
        &self,
        args: &MiscategorizationCreateMiscategorizationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IntelApiResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = miscategorization_create_miscategorization_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = miscategorization_create_miscategorization_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Sinkhole config get sinkholes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IntelSinkholesGetSinkholesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn sinkhole_config_get_sinkholes(
        &self,
        args: &SinkholeConfigGetSinkholesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IntelSinkholesGetSinkholesResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = sinkhole_config_get_sinkholes_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = sinkhole_config_get_sinkholes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Whois record get whois record.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CloudforceOneWhoisSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn whois_record_get_whois_record(
        &self,
        args: &WhoisRecordGetWhoisRecordArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CloudforceOneWhoisSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = whois_record_get_whois_record_builder(
            &self.http_client,
            &args.account_id,
            &args.domain,
        )
        .map_err(ProviderError::Api)?;

        let task = whois_record_get_whois_record_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer monitor groups list monitor groups.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingMonitorGroupResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_load_balancer_monitor_groups_list_monitor_groups(
        &self,
        args: &AccountLoadBalancerMonitorGroupsListMonitorGroupsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingMonitorGroupResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_monitor_groups_list_monitor_groups_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_monitor_groups_list_monitor_groups_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer monitor groups create monitor group.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingMonitorGroupSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_load_balancer_monitor_groups_create_monitor_group(
        &self,
        args: &AccountLoadBalancerMonitorGroupsCreateMonitorGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingMonitorGroupSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_monitor_groups_create_monitor_group_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_monitor_groups_create_monitor_group_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer monitor groups monitor group details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingMonitorGroupSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_load_balancer_monitor_groups_monitor_group_details(
        &self,
        args: &AccountLoadBalancerMonitorGroupsMonitorGroupDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingMonitorGroupSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_monitor_groups_monitor_group_details_builder(
            &self.http_client,
            &args.monitor_group_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_monitor_groups_monitor_group_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer monitor groups update monitor group.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingMonitorGroupSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_load_balancer_monitor_groups_update_monitor_group(
        &self,
        args: &AccountLoadBalancerMonitorGroupsUpdateMonitorGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingMonitorGroupSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_monitor_groups_update_monitor_group_builder(
            &self.http_client,
            &args.monitor_group_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_monitor_groups_update_monitor_group_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer monitor groups patch monitor group.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingMonitorGroupSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_load_balancer_monitor_groups_patch_monitor_group(
        &self,
        args: &AccountLoadBalancerMonitorGroupsPatchMonitorGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingMonitorGroupSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_monitor_groups_patch_monitor_group_builder(
            &self.http_client,
            &args.monitor_group_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_monitor_groups_patch_monitor_group_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer monitor groups delete monitor group.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingMonitorGroupSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_load_balancer_monitor_groups_delete_monitor_group(
        &self,
        args: &AccountLoadBalancerMonitorGroupsDeleteMonitorGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingMonitorGroupSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_monitor_groups_delete_monitor_group_builder(
            &self.http_client,
            &args.monitor_group_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_monitor_groups_delete_monitor_group_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer monitor groups list monitor group references.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingMonitorGroupReferencesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_load_balancer_monitor_groups_list_monitor_group_references(
        &self,
        args: &AccountLoadBalancerMonitorGroupsListMonitorGroupReferencesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingMonitorGroupReferencesResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_monitor_groups_list_monitor_group_references_builder(
            &self.http_client,
            &args.monitor_group_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_monitor_groups_list_monitor_group_references_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer monitors list monitors.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingMonitorResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_load_balancer_monitors_list_monitors(
        &self,
        args: &AccountLoadBalancerMonitorsListMonitorsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingMonitorResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_monitors_list_monitors_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_monitors_list_monitors_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer monitors create monitor.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingMonitorResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_load_balancer_monitors_create_monitor(
        &self,
        args: &AccountLoadBalancerMonitorsCreateMonitorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingMonitorResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_monitors_create_monitor_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_monitors_create_monitor_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer monitors monitor details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingMonitorResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_load_balancer_monitors_monitor_details(
        &self,
        args: &AccountLoadBalancerMonitorsMonitorDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingMonitorResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_monitors_monitor_details_builder(
            &self.http_client,
            &args.monitor_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_monitors_monitor_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer monitors update monitor.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingMonitorResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_load_balancer_monitors_update_monitor(
        &self,
        args: &AccountLoadBalancerMonitorsUpdateMonitorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingMonitorResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_monitors_update_monitor_builder(
            &self.http_client,
            &args.monitor_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_monitors_update_monitor_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer monitors patch monitor.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingMonitorResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_load_balancer_monitors_patch_monitor(
        &self,
        args: &AccountLoadBalancerMonitorsPatchMonitorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingMonitorResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_monitors_patch_monitor_builder(
            &self.http_client,
            &args.monitor_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_monitors_patch_monitor_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer monitors delete monitor.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_load_balancer_monitors_delete_monitor(
        &self,
        args: &AccountLoadBalancerMonitorsDeleteMonitorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_monitors_delete_monitor_builder(
            &self.http_client,
            &args.monitor_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_monitors_delete_monitor_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer monitors preview monitor.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingPreviewResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_load_balancer_monitors_preview_monitor(
        &self,
        args: &AccountLoadBalancerMonitorsPreviewMonitorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingPreviewResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_monitors_preview_monitor_builder(
            &self.http_client,
            &args.monitor_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_monitors_preview_monitor_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer monitors list monitor references.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingMonitorReferencesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_load_balancer_monitors_list_monitor_references(
        &self,
        args: &AccountLoadBalancerMonitorsListMonitorReferencesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingMonitorReferencesResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_monitors_list_monitor_references_builder(
            &self.http_client,
            &args.monitor_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_monitors_list_monitor_references_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer pools list pools.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_load_balancer_pools_list_pools(
        &self,
        args: &AccountLoadBalancerPoolsListPoolsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_pools_list_pools_builder(
            &self.http_client,
            &args.account_id,
            &args.monitor,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_pools_list_pools_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer pools create pool.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_load_balancer_pools_create_pool(
        &self,
        args: &AccountLoadBalancerPoolsCreatePoolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_pools_create_pool_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_pools_create_pool_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer pools patch pools.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_load_balancer_pools_patch_pools(
        &self,
        args: &AccountLoadBalancerPoolsPatchPoolsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_pools_patch_pools_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_pools_patch_pools_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer pools pool details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_load_balancer_pools_pool_details(
        &self,
        args: &AccountLoadBalancerPoolsPoolDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_pools_pool_details_builder(
            &self.http_client,
            &args.pool_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_pools_pool_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer pools update pool.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_load_balancer_pools_update_pool(
        &self,
        args: &AccountLoadBalancerPoolsUpdatePoolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_pools_update_pool_builder(
            &self.http_client,
            &args.pool_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_pools_update_pool_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer pools patch pool.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_load_balancer_pools_patch_pool(
        &self,
        args: &AccountLoadBalancerPoolsPatchPoolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_pools_patch_pool_builder(
            &self.http_client,
            &args.pool_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_pools_patch_pool_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer pools delete pool.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingSchemasIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_load_balancer_pools_delete_pool(
        &self,
        args: &AccountLoadBalancerPoolsDeletePoolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingSchemasIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_pools_delete_pool_builder(
            &self.http_client,
            &args.pool_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_pools_delete_pool_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer pools pool health details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingHealthDetails result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_load_balancer_pools_pool_health_details(
        &self,
        args: &AccountLoadBalancerPoolsPoolHealthDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingHealthDetails, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_pools_pool_health_details_builder(
            &self.http_client,
            &args.pool_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_pools_pool_health_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer pools preview pool.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingPreviewResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_load_balancer_pools_preview_pool(
        &self,
        args: &AccountLoadBalancerPoolsPreviewPoolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingPreviewResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_pools_preview_pool_builder(
            &self.http_client,
            &args.pool_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_pools_preview_pool_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer pools list pool references.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingPoolsReferencesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_load_balancer_pools_list_pool_references(
        &self,
        args: &AccountLoadBalancerPoolsListPoolReferencesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingPoolsReferencesResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_pools_list_pool_references_builder(
            &self.http_client,
            &args.pool_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_pools_list_pool_references_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer monitors preview result.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingPreviewResultResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_load_balancer_monitors_preview_result(
        &self,
        args: &AccountLoadBalancerMonitorsPreviewResultArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingPreviewResultResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_monitors_preview_result_builder(
            &self.http_client,
            &args.preview_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_monitors_preview_result_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer regions list regions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingRegionComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn load_balancer_regions_list_regions(
        &self,
        args: &LoadBalancerRegionsListRegionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingRegionComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_regions_list_regions_builder(
            &self.http_client,
            &args.account_id,
            &args.subdivision_code,
            &args.subdivision_code_a2,
            &args.country_code_a2,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_regions_list_regions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer regions get region.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn load_balancer_regions_get_region(
        &self,
        args: &LoadBalancerRegionsGetRegionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_regions_get_region_builder(
            &self.http_client,
            &args.region_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_regions_get_region_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account load balancer search search resources.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_load_balancer_search_search_resources(
        &self,
        args: &AccountLoadBalancerSearchSearchResourcesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_load_balancer_search_search_resources_builder(
            &self.http_client,
            &args.account_id,
            &args.query,
            &args.references,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = account_load_balancer_search_search_resources_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get accounts account id logpush datasets dataset id fields.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushLogpushFieldResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_accounts_account_id_logpush_datasets_dataset_id_fields(
        &self,
        args: &GetAccountsAccountIdLogpushDatasetsDatasetIdFieldsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushLogpushFieldResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_accounts_account_id_logpush_datasets_dataset_id_fields_builder(
            &self.http_client,
            &args.dataset_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_accounts_account_id_logpush_datasets_dataset_id_fields_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get accounts account id logpush datasets dataset id jobs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushLogpushJobResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_accounts_account_id_logpush_datasets_dataset_id_jobs(
        &self,
        args: &GetAccountsAccountIdLogpushDatasetsDatasetIdJobsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushLogpushJobResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_accounts_account_id_logpush_datasets_dataset_id_jobs_builder(
            &self.http_client,
            &args.dataset_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_accounts_account_id_logpush_datasets_dataset_id_jobs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get accounts account id logpush jobs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushLogpushJobResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_accounts_account_id_logpush_jobs(
        &self,
        args: &GetAccountsAccountIdLogpushJobsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushLogpushJobResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_accounts_account_id_logpush_jobs_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_accounts_account_id_logpush_jobs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post accounts account id logpush jobs.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushLogpushJobResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_accounts_account_id_logpush_jobs(
        &self,
        args: &PostAccountsAccountIdLogpushJobsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushLogpushJobResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_accounts_account_id_logpush_jobs_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_accounts_account_id_logpush_jobs_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get accounts account id logpush jobs job id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushLogpushJobResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_accounts_account_id_logpush_jobs_job_id(
        &self,
        args: &GetAccountsAccountIdLogpushJobsJobIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushLogpushJobResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_accounts_account_id_logpush_jobs_job_id_builder(
            &self.http_client,
            &args.job_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_accounts_account_id_logpush_jobs_job_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Put accounts account id logpush jobs job id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushLogpushJobResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn put_accounts_account_id_logpush_jobs_job_id(
        &self,
        args: &PutAccountsAccountIdLogpushJobsJobIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushLogpushJobResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = put_accounts_account_id_logpush_jobs_job_id_builder(
            &self.http_client,
            &args.job_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = put_accounts_account_id_logpush_jobs_job_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete accounts account id logpush jobs job id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_accounts_account_id_logpush_jobs_job_id(
        &self,
        args: &DeleteAccountsAccountIdLogpushJobsJobIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_accounts_account_id_logpush_jobs_job_id_builder(
            &self.http_client,
            &args.job_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_accounts_account_id_logpush_jobs_job_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post accounts account id logpush ownership.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushGetOwnershipResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_accounts_account_id_logpush_ownership(
        &self,
        args: &PostAccountsAccountIdLogpushOwnershipArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushGetOwnershipResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_accounts_account_id_logpush_ownership_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_accounts_account_id_logpush_ownership_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post accounts account id logpush ownership validate.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushValidateOwnershipResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn post_accounts_account_id_logpush_ownership_validate(
        &self,
        args: &PostAccountsAccountIdLogpushOwnershipValidateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushValidateOwnershipResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_accounts_account_id_logpush_ownership_validate_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_accounts_account_id_logpush_ownership_validate_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete accounts account id logpush validate destination.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushValidateResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_accounts_account_id_logpush_validate_destination(
        &self,
        args: &DeleteAccountsAccountIdLogpushValidateDestinationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushValidateResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_accounts_account_id_logpush_validate_destination_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_accounts_account_id_logpush_validate_destination_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete accounts account id logpush validate destination exists.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushDestinationExistsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_accounts_account_id_logpush_validate_destination_exists(
        &self,
        args: &DeleteAccountsAccountIdLogpushValidateDestinationExistsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushDestinationExistsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_accounts_account_id_logpush_validate_destination_exists_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_accounts_account_id_logpush_validate_destination_exists_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post accounts account id logpush validate origin.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushValidateResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn post_accounts_account_id_logpush_validate_origin(
        &self,
        args: &PostAccountsAccountIdLogpushValidateOriginArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushValidateResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_accounts_account_id_logpush_validate_origin_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_accounts_account_id_logpush_validate_origin_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Audit logs v2 get account audit logs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaAuditLogsV2ResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn audit_logs_v2_get_account_audit_logs(
        &self,
        args: &AuditLogsV2GetAccountAuditLogsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaAuditLogsV2ResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = audit_logs_v2_get_account_audit_logs_builder(
            &self.http_client,
            &args.account_id,
            &args.account_name,
            &args.action_result,
            &args.action_type,
            &args.actor_context,
            &args.actor_email,
            &args.actor_id,
            &args.actor_ip_address,
            &args.actor_token_id,
            &args.actor_token_name,
            &args.actor_type,
            &args.audit_log_id,
            &args.id,
            &args.raw_cf_ray_id,
            &args.raw_method,
            &args.raw_status_code,
            &args.raw_uri,
            &args.resource_id,
            &args.resource_product,
            &args.resource_type,
            &args.resource_scope,
            &args.zone_id,
            &args.zone_name,
            &args.account_name_not,
            &args.action_result_not,
            &args.action_type_not,
            &args.actor_context_not,
            &args.actor_email_not,
            &args.actor_id_not,
            &args.actor_ip_address_not,
            &args.actor_token_id_not,
            &args.actor_token_name_not,
            &args.actor_type_not,
            &args.audit_log_id_not,
            &args.id_not,
            &args.raw_cf_ray_id_not,
            &args.raw_method_not,
            &args.raw_status_code_not,
            &args.raw_uri_not,
            &args.resource_id_not,
            &args.resource_product_not,
            &args.resource_type_not,
            &args.resource_scope_not,
            &args.zone_id_not,
            &args.zone_name_not,
            &args.since,
            &args.before,
            &args.direction,
            &args.limit,
            &args.cursor,
        )
        .map_err(ProviderError::Api)?;

        let task = audit_logs_v2_get_account_audit_logs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get accounts account id logs control cmb config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogcontrolCmbConfigResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_accounts_account_id_logs_control_cmb_config(
        &self,
        args: &GetAccountsAccountIdLogsControlCmbConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogcontrolCmbConfigResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_accounts_account_id_logs_control_cmb_config_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_accounts_account_id_logs_control_cmb_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post accounts account id logs control cmb config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogcontrolCmbConfigResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_accounts_account_id_logs_control_cmb_config(
        &self,
        args: &PostAccountsAccountIdLogsControlCmbConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogcontrolCmbConfigResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_accounts_account_id_logs_control_cmb_config_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_accounts_account_id_logs_control_cmb_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete accounts account id logs control cmb config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_accounts_account_id_logs_control_cmb_config(
        &self,
        args: &DeleteAccountsAccountIdLogsControlCmbConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_accounts_account_id_logs_control_cmb_config_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_accounts_account_id_logs_control_cmb_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List dns protection rules for account.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosDnsProtectionRuleListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_dns_protection_rules_for_account(
        &self,
        args: &ListDnsProtectionRulesForAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosDnsProtectionRuleListResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_dns_protection_rules_for_account_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = list_dns_protection_rules_for_account_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create dns protection rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosDnsProtectionRuleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_dns_protection_rule(
        &self,
        args: &CreateDnsProtectionRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosDnsProtectionRuleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_dns_protection_rule_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_dns_protection_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete dns protection rules for account.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_dns_protection_rules_for_account(
        &self,
        args: &DeleteDnsProtectionRulesForAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_dns_protection_rules_for_account_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_dns_protection_rules_for_account_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get dns protection rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosDnsProtectionRuleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_dns_protection_rule(
        &self,
        args: &GetDnsProtectionRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosDnsProtectionRuleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_dns_protection_rule_builder(
            &self.http_client,
            &args.account_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_dns_protection_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update dns protection rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosDnsProtectionRuleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_dns_protection_rule(
        &self,
        args: &UpdateDnsProtectionRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosDnsProtectionRuleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_dns_protection_rule_builder(
            &self.http_client,
            &args.account_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_dns_protection_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete dns protection rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_dns_protection_rule(
        &self,
        args: &DeleteDnsProtectionRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_dns_protection_rule_builder(
            &self.http_client,
            &args.account_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_dns_protection_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List allowlist prefixes for account.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosInfraPrefixListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_allowlist_prefixes_for_account(
        &self,
        args: &ListAllowlistPrefixesForAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosInfraPrefixListResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_allowlist_prefixes_for_account_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = list_allowlist_prefixes_for_account_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create allowlisted prefix.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosInfraPrefixResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_allowlisted_prefix(
        &self,
        args: &CreateAllowlistedPrefixArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosInfraPrefixResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_allowlisted_prefix_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_allowlisted_prefix_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete allowlist prefixes for account.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn delete_allowlist_prefixes_for_account(
        &self,
        args: &DeleteAllowlistPrefixesForAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_allowlist_prefixes_for_account_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_allowlist_prefixes_for_account_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get allowlist prefix.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosInfraPrefixResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_allowlist_prefix(
        &self,
        args: &GetAllowlistPrefixArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosInfraPrefixResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_allowlist_prefix_builder(
            &self.http_client,
            &args.account_id,
            &args.prefix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_allowlist_prefix_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update allowlist prefix.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosInfraPrefixResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn update_allowlist_prefix(
        &self,
        args: &UpdateAllowlistPrefixArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosInfraPrefixResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_allowlist_prefix_builder(
            &self.http_client,
            &args.account_id,
            &args.prefix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_allowlist_prefix_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete allowlist prefix.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn delete_allowlist_prefix(
        &self,
        args: &DeleteAllowlistPrefixArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_allowlist_prefix_builder(
            &self.http_client,
            &args.account_id,
            &args.prefix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_allowlist_prefix_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List prefixes for account.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosPrefixListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_prefixes_for_account(
        &self,
        args: &ListPrefixesForAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosPrefixListResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_prefixes_for_account_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = list_prefixes_for_account_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create prefix.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosPrefixResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_prefix(
        &self,
        args: &CreatePrefixArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosPrefixResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_prefix_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_prefix_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete prefixes for account.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_prefixes_for_account(
        &self,
        args: &DeletePrefixesForAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_prefixes_for_account_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_prefixes_for_account_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Bulk create prefixes.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosPrefixListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn bulk_create_prefixes(
        &self,
        args: &BulkCreatePrefixesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosPrefixListResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = bulk_create_prefixes_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = bulk_create_prefixes_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get prefix.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosPrefixResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_prefix(
        &self,
        args: &GetPrefixArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosPrefixResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_prefix_builder(
            &self.http_client,
            &args.account_id,
            &args.prefix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_prefix_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update prefix.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosPrefixResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_prefix(
        &self,
        args: &UpdatePrefixArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosPrefixResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_prefix_builder(
            &self.http_client,
            &args.account_id,
            &args.prefix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_prefix_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete prefix.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_prefix(
        &self,
        args: &DeletePrefixArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_prefix_builder(
            &self.http_client,
            &args.account_id,
            &args.prefix_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_prefix_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List syn protection filters for account.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosExpressionFilterListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_syn_protection_filters_for_account(
        &self,
        args: &ListSynProtectionFiltersForAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosExpressionFilterListResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_syn_protection_filters_for_account_builder(
            &self.http_client,
            &args.account_id,
            &args.mode,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = list_syn_protection_filters_for_account_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create syn protection filter.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosExpressionFilterResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_syn_protection_filter(
        &self,
        args: &CreateSynProtectionFilterArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosExpressionFilterResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_syn_protection_filter_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_syn_protection_filter_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete syn protection filters for account.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_syn_protection_filters_for_account(
        &self,
        args: &DeleteSynProtectionFiltersForAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_syn_protection_filters_for_account_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_syn_protection_filters_for_account_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get syn protection filter.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosExpressionFilterResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_syn_protection_filter(
        &self,
        args: &GetSynProtectionFilterArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosExpressionFilterResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_syn_protection_filter_builder(
            &self.http_client,
            &args.account_id,
            &args.filter_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_syn_protection_filter_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update syn protection filter.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosExpressionFilterResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_syn_protection_filter(
        &self,
        args: &UpdateSynProtectionFilterArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosExpressionFilterResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_syn_protection_filter_builder(
            &self.http_client,
            &args.account_id,
            &args.filter_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_syn_protection_filter_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete syn protection filter.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_syn_protection_filter(
        &self,
        args: &DeleteSynProtectionFilterArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_syn_protection_filter_builder(
            &self.http_client,
            &args.account_id,
            &args.filter_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_syn_protection_filter_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List syn protection rules for account.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosSynProtectionRuleListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_syn_protection_rules_for_account(
        &self,
        args: &ListSynProtectionRulesForAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosSynProtectionRuleListResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_syn_protection_rules_for_account_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = list_syn_protection_rules_for_account_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create syn protection rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosSynProtectionRuleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_syn_protection_rule(
        &self,
        args: &CreateSynProtectionRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosSynProtectionRuleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_syn_protection_rule_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_syn_protection_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete syn protection rules for account.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_syn_protection_rules_for_account(
        &self,
        args: &DeleteSynProtectionRulesForAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_syn_protection_rules_for_account_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_syn_protection_rules_for_account_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get syn protection rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosSynProtectionRuleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_syn_protection_rule(
        &self,
        args: &GetSynProtectionRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosSynProtectionRuleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_syn_protection_rule_builder(
            &self.http_client,
            &args.account_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_syn_protection_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update syn protection rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosSynProtectionRuleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_syn_protection_rule(
        &self,
        args: &UpdateSynProtectionRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosSynProtectionRuleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_syn_protection_rule_builder(
            &self.http_client,
            &args.account_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_syn_protection_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete syn protection rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_syn_protection_rule(
        &self,
        args: &DeleteSynProtectionRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_syn_protection_rule_builder(
            &self.http_client,
            &args.account_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_syn_protection_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List tcp flow protection filters for account.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosExpressionFilterListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_tcp_flow_protection_filters_for_account(
        &self,
        args: &ListTcpFlowProtectionFiltersForAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosExpressionFilterListResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_tcp_flow_protection_filters_for_account_builder(
            &self.http_client,
            &args.account_id,
            &args.mode,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = list_tcp_flow_protection_filters_for_account_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create tcp flow protection filter.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosExpressionFilterResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_tcp_flow_protection_filter(
        &self,
        args: &CreateTcpFlowProtectionFilterArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosExpressionFilterResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_tcp_flow_protection_filter_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_tcp_flow_protection_filter_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete tcp flow protection filters for account.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_tcp_flow_protection_filters_for_account(
        &self,
        args: &DeleteTcpFlowProtectionFiltersForAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_tcp_flow_protection_filters_for_account_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_tcp_flow_protection_filters_for_account_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get tcp flow protection filter.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosExpressionFilterResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_tcp_flow_protection_filter(
        &self,
        args: &GetTcpFlowProtectionFilterArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosExpressionFilterResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_tcp_flow_protection_filter_builder(
            &self.http_client,
            &args.account_id,
            &args.filter_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_tcp_flow_protection_filter_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update tcp flow protection filter.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosExpressionFilterResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_tcp_flow_protection_filter(
        &self,
        args: &UpdateTcpFlowProtectionFilterArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosExpressionFilterResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_tcp_flow_protection_filter_builder(
            &self.http_client,
            &args.account_id,
            &args.filter_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_tcp_flow_protection_filter_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete tcp flow protection filter.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_tcp_flow_protection_filter(
        &self,
        args: &DeleteTcpFlowProtectionFilterArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_tcp_flow_protection_filter_builder(
            &self.http_client,
            &args.account_id,
            &args.filter_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_tcp_flow_protection_filter_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List tcp flow protection rules for account.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosTcpFlowProtectionRuleListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_tcp_flow_protection_rules_for_account(
        &self,
        args: &ListTcpFlowProtectionRulesForAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosTcpFlowProtectionRuleListResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_tcp_flow_protection_rules_for_account_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = list_tcp_flow_protection_rules_for_account_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create tcp flow protection rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosTcpFlowProtectionRuleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_tcp_flow_protection_rule(
        &self,
        args: &CreateTcpFlowProtectionRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosTcpFlowProtectionRuleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_tcp_flow_protection_rule_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_tcp_flow_protection_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete tcp flow protection rules for account.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_tcp_flow_protection_rules_for_account(
        &self,
        args: &DeleteTcpFlowProtectionRulesForAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_tcp_flow_protection_rules_for_account_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_tcp_flow_protection_rules_for_account_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get tcp flow protection rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosTcpFlowProtectionRuleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_tcp_flow_protection_rule(
        &self,
        args: &GetTcpFlowProtectionRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosTcpFlowProtectionRuleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_tcp_flow_protection_rule_builder(
            &self.http_client,
            &args.account_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_tcp_flow_protection_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update tcp flow protection rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosTcpFlowProtectionRuleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_tcp_flow_protection_rule(
        &self,
        args: &UpdateTcpFlowProtectionRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosTcpFlowProtectionRuleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_tcp_flow_protection_rule_builder(
            &self.http_client,
            &args.account_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_tcp_flow_protection_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete tcp flow protection rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_tcp_flow_protection_rule(
        &self,
        args: &DeleteTcpFlowProtectionRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_tcp_flow_protection_rule_builder(
            &self.http_client,
            &args.account_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_tcp_flow_protection_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get protection status.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosProtectionStatusResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_protection_status(
        &self,
        args: &GetProtectionStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosProtectionStatusResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_protection_status_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_protection_status_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update protection status.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DosProtectionStatusResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_protection_status(
        &self,
        args: &UpdateProtectionStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DosProtectionStatusResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_protection_status_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_protection_status_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic account apps list apps.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicAppsCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_account_apps_list_apps(
        &self,
        args: &MagicAccountAppsListAppsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicAppsCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_account_apps_list_apps_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_account_apps_list_apps_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic account apps add app.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicAppSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_account_apps_add_app(
        &self,
        args: &MagicAccountAppsAddAppArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicAppSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_account_apps_add_app_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_account_apps_add_app_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic account apps update app.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicAppSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_account_apps_update_app(
        &self,
        args: &MagicAccountAppsUpdateAppArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicAppSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_account_apps_update_app_builder(
            &self.http_client,
            &args.account_id,
            &args.account_app_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_account_apps_update_app_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic account apps patch app.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicAppSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_account_apps_patch_app(
        &self,
        args: &MagicAccountAppsPatchAppArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicAppSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_account_apps_patch_app_builder(
            &self.http_client,
            &args.account_id,
            &args.account_app_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_account_apps_patch_app_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic account apps delete app.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicAppSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_account_apps_delete_app(
        &self,
        args: &MagicAccountAppsDeleteAppArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicAppSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_account_apps_delete_app_builder(
            &self.http_client,
            &args.account_id,
            &args.account_app_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_account_apps_delete_app_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic interconnects list interconnects.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicComponentsSchemasTunnelsCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_interconnects_list_interconnects(
        &self,
        args: &MagicInterconnectsListInterconnectsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicComponentsSchemasTunnelsCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_interconnects_list_interconnects_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_interconnects_list_interconnects_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic interconnects update multiple interconnects.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicComponentsSchemasModifiedTunnelsCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_interconnects_update_multiple_interconnects(
        &self,
        args: &MagicInterconnectsUpdateMultipleInterconnectsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicComponentsSchemasModifiedTunnelsCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_interconnects_update_multiple_interconnects_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_interconnects_update_multiple_interconnects_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic interconnects list interconnect details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicComponentsSchemasTunnelSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_interconnects_list_interconnect_details(
        &self,
        args: &MagicInterconnectsListInterconnectDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicComponentsSchemasTunnelSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_interconnects_list_interconnect_details_builder(
            &self.http_client,
            &args.cf_interconnect_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_interconnects_list_interconnect_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic interconnects update interconnect.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicComponentsSchemasTunnelModifiedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_interconnects_update_interconnect(
        &self,
        args: &MagicInterconnectsUpdateInterconnectArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicComponentsSchemasTunnelModifiedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_interconnects_update_interconnect_builder(
            &self.http_client,
            &args.cf_interconnect_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_interconnects_update_interconnect_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Catalog syncs list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnReadAccountCatalogSyncsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn catalog_syncs_list(
        &self,
        args: &CatalogSyncsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnReadAccountCatalogSyncsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = catalog_syncs_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = catalog_syncs_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Catalog syncs create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnCreateCatalogSyncResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn catalog_syncs_create(
        &self,
        args: &CatalogSyncsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnCreateCatalogSyncResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = catalog_syncs_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = catalog_syncs_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Catalog syncs prebuilt policies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnCatalogSyncsPrebuiltPoliciesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn catalog_syncs_prebuilt_policies_list(
        &self,
        args: &CatalogSyncsPrebuiltPoliciesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnCatalogSyncsPrebuiltPoliciesResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = catalog_syncs_prebuilt_policies_list_builder(
            &self.http_client,
            &args.account_id,
            &args.destination_type,
        )
        .map_err(ProviderError::Api)?;

        let task = catalog_syncs_prebuilt_policies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Catalog syncs read.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnReadAccountCatalogSyncResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn catalog_syncs_read(
        &self,
        args: &CatalogSyncsReadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnReadAccountCatalogSyncResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = catalog_syncs_read_builder(
            &self.http_client,
            &args.account_id,
            &args.sync_id,
        )
        .map_err(ProviderError::Api)?;

        let task = catalog_syncs_read_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Catalog syncs update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnUpdateCatalogSyncResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn catalog_syncs_update(
        &self,
        args: &CatalogSyncsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnUpdateCatalogSyncResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = catalog_syncs_update_builder(
            &self.http_client,
            &args.account_id,
            &args.sync_id,
        )
        .map_err(ProviderError::Api)?;

        let task = catalog_syncs_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Catalog syncs patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnUpdateCatalogSyncResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn catalog_syncs_patch(
        &self,
        args: &CatalogSyncsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnUpdateCatalogSyncResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = catalog_syncs_patch_builder(
            &self.http_client,
            &args.account_id,
            &args.sync_id,
        )
        .map_err(ProviderError::Api)?;

        let task = catalog_syncs_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Catalog syncs delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnDeleteCatalogSyncResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn catalog_syncs_delete(
        &self,
        args: &CatalogSyncsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnDeleteCatalogSyncResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = catalog_syncs_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.sync_id,
            &args.delete_destination,
        )
        .map_err(ProviderError::Api)?;

        let task = catalog_syncs_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Catalog syncs refresh.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnRefreshCatalogSyncResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn catalog_syncs_refresh(
        &self,
        args: &CatalogSyncsRefreshArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnRefreshCatalogSyncResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = catalog_syncs_refresh_builder(
            &self.http_client,
            &args.account_id,
            &args.sync_id,
        )
        .map_err(ProviderError::Api)?;

        let task = catalog_syncs_refresh_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Onramps list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnListOnrampsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn onramps_list(
        &self,
        args: &OnrampsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnListOnrampsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = onramps_list_builder(
            &self.http_client,
            &args.account_id,
            &args.order_by,
            &args.desc,
            &args.status,
            &args.vpcs,
        )
        .map_err(ProviderError::Api)?;

        let task = onramps_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Onramps create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnCreateOnrampResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn onramps_create(
        &self,
        args: &OnrampsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnCreateOnrampResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = onramps_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = onramps_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Onramps mwan addr space read.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnGetMagicWanAddressSpaceResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn onramps_mwan_addr_space_read(
        &self,
        args: &OnrampsMwanAddrSpaceReadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnGetMagicWanAddressSpaceResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = onramps_mwan_addr_space_read_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = onramps_mwan_addr_space_read_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Onramps mwan addr space update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnUpdateMagicWanAddressSpaceResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn onramps_mwan_addr_space_update(
        &self,
        args: &OnrampsMwanAddrSpaceUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnUpdateMagicWanAddressSpaceResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = onramps_mwan_addr_space_update_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = onramps_mwan_addr_space_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Onramps mwan addr space patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnUpdateMagicWanAddressSpaceResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn onramps_mwan_addr_space_patch(
        &self,
        args: &OnrampsMwanAddrSpacePatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnUpdateMagicWanAddressSpaceResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = onramps_mwan_addr_space_patch_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = onramps_mwan_addr_space_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Onramps read.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnGetOnrampResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn onramps_read(
        &self,
        args: &OnrampsReadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnGetOnrampResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = onramps_read_builder(
            &self.http_client,
            &args.account_id,
            &args.onramp_id,
            &args.status,
            &args.vpcs,
            &args.post_apply_resources,
            &args.planned_resources,
        )
        .map_err(ProviderError::Api)?;

        let task = onramps_read_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Onramps update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnUpdateOnrampResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn onramps_update(
        &self,
        args: &OnrampsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnUpdateOnrampResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = onramps_update_builder(
            &self.http_client,
            &args.account_id,
            &args.onramp_id,
        )
        .map_err(ProviderError::Api)?;

        let task = onramps_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Onramps patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnUpdateOnrampResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn onramps_patch(
        &self,
        args: &OnrampsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnUpdateOnrampResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = onramps_patch_builder(
            &self.http_client,
            &args.account_id,
            &args.onramp_id,
        )
        .map_err(ProviderError::Api)?;

        let task = onramps_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Onramps delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnDeleteOnrampResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn onramps_delete(
        &self,
        args: &OnrampsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnDeleteOnrampResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = onramps_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.onramp_id,
            &args.destroy,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = onramps_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Onramps apply.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnGoodResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn onramps_apply(
        &self,
        args: &OnrampsApplyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnGoodResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = onramps_apply_builder(
            &self.http_client,
            &args.account_id,
            &args.onramp_id,
        )
        .map_err(ProviderError::Api)?;

        let task = onramps_apply_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Onramps export.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn onramps_export(
        &self,
        args: &OnrampsExportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = onramps_export_builder(
            &self.http_client,
            &args.account_id,
            &args.onramp_id,
        )
        .map_err(ProviderError::Api)?;

        let task = onramps_export_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Onramps plan.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnGoodResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn onramps_plan(
        &self,
        args: &OnrampsPlanArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnGoodResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = onramps_plan_builder(
            &self.http_client,
            &args.account_id,
            &args.onramp_id,
        )
        .map_err(ProviderError::Api)?;

        let task = onramps_plan_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Providers list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnReadAccountProvidersResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn providers_list(
        &self,
        args: &ProvidersListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnReadAccountProvidersResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = providers_list_builder(
            &self.http_client,
            &args.account_id,
            &args.status,
            &args.order_by,
            &args.desc,
            &args.cloudflare,
        )
        .map_err(ProviderError::Api)?;

        let task = providers_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Providers create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnCreateProviderResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn providers_create(
        &self,
        args: &ProvidersCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnCreateProviderResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = providers_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = providers_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Providers discover all.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnGoodResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn providers_discover_all(
        &self,
        args: &ProvidersDiscoverAllArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnGoodResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = providers_discover_all_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = providers_discover_all_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Providers read.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnReadAccountProviderResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn providers_read(
        &self,
        args: &ProvidersReadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnReadAccountProviderResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = providers_read_builder(
            &self.http_client,
            &args.account_id,
            &args.provider_id,
            &args.status,
        )
        .map_err(ProviderError::Api)?;

        let task = providers_read_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Providers update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnUpdateProviderResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn providers_update(
        &self,
        args: &ProvidersUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnUpdateProviderResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = providers_update_builder(
            &self.http_client,
            &args.account_id,
            &args.provider_id,
        )
        .map_err(ProviderError::Api)?;

        let task = providers_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Providers patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnUpdateProviderResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn providers_patch(
        &self,
        args: &ProvidersPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnUpdateProviderResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = providers_patch_builder(
            &self.http_client,
            &args.account_id,
            &args.provider_id,
        )
        .map_err(ProviderError::Api)?;

        let task = providers_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Providers delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnDeleteProviderResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn providers_delete(
        &self,
        args: &ProvidersDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnDeleteProviderResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = providers_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.provider_id,
        )
        .map_err(ProviderError::Api)?;

        let task = providers_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Providers discover.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnGoodResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn providers_discover(
        &self,
        args: &ProvidersDiscoverArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnGoodResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = providers_discover_builder(
            &self.http_client,
            &args.account_id,
            &args.provider_id,
            &args.v2,
        )
        .map_err(ProviderError::Api)?;

        let task = providers_discover_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Providers initial setup.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnProviderInitialSetupResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn providers_initial_setup(
        &self,
        args: &ProvidersInitialSetupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnProviderInitialSetupResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = providers_initial_setup_builder(
            &self.http_client,
            &args.account_id,
            &args.provider_id,
        )
        .map_err(ProviderError::Api)?;

        let task = providers_initial_setup_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Resources catalog list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnReadAccountResourcesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn resources_catalog_list(
        &self,
        args: &ResourcesCatalogListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnReadAccountResourcesResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = resources_catalog_list_builder(
            &self.http_client,
            &args.account_id,
            &args.provider_id,
            &args.resource_type,
            &args.resource_id,
            &args.region,
            &args.resource_group,
            &args.managed,
            &args.search,
            &args.order_by,
            &args.desc,
            &args.per_page,
            &args.page,
            &args.cloudflare,
            &args.v2,
        )
        .map_err(ProviderError::Api)?;

        let task = resources_catalog_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Resources catalog export.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn resources_catalog_export(
        &self,
        args: &ResourcesCatalogExportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = resources_catalog_export_builder(
            &self.http_client,
            &args.account_id,
            &args.provider_id,
            &args.resource_type,
            &args.resource_id,
            &args.region,
            &args.resource_group,
            &args.search,
            &args.order_by,
            &args.desc,
            &args.v2,
        )
        .map_err(ProviderError::Api)?;

        let task = resources_catalog_export_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Resources catalog policy preview.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnResourcesCatalogPolicyPreviewResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn resources_catalog_policy_preview(
        &self,
        args: &ResourcesCatalogPolicyPreviewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnResourcesCatalogPolicyPreviewResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = resources_catalog_policy_preview_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = resources_catalog_policy_preview_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Resources catalog read.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the McnReadAccountResourceResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn resources_catalog_read(
        &self,
        args: &ResourcesCatalogReadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<McnReadAccountResourceResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = resources_catalog_read_builder(
            &self.http_client,
            &args.account_id,
            &args.resource_id,
            &args.v2,
        )
        .map_err(ProviderError::Api)?;

        let task = resources_catalog_read_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mconn connector list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MconnCustomerConnectorListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn mconn_connector_list(
        &self,
        args: &MconnConnectorListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MconnCustomerConnectorListResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mconn_connector_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = mconn_connector_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mconn connector create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MconnCustomerConnectorCreateResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn mconn_connector_create(
        &self,
        args: &MconnConnectorCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MconnCustomerConnectorCreateResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mconn_connector_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = mconn_connector_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mconn connector fetch.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MconnCustomerConnectorFetchResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn mconn_connector_fetch(
        &self,
        args: &MconnConnectorFetchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MconnCustomerConnectorFetchResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mconn_connector_fetch_builder(
            &self.http_client,
            &args.account_id,
            &args.connector_id,
        )
        .map_err(ProviderError::Api)?;

        let task = mconn_connector_fetch_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mconn connector replace.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MconnCustomerConnectorUpdateResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn mconn_connector_replace(
        &self,
        args: &MconnConnectorReplaceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MconnCustomerConnectorUpdateResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mconn_connector_replace_builder(
            &self.http_client,
            &args.account_id,
            &args.connector_id,
        )
        .map_err(ProviderError::Api)?;

        let task = mconn_connector_replace_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mconn connector update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MconnCustomerConnectorUpdateResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn mconn_connector_update(
        &self,
        args: &MconnConnectorUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MconnCustomerConnectorUpdateResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mconn_connector_update_builder(
            &self.http_client,
            &args.account_id,
            &args.connector_id,
        )
        .map_err(ProviderError::Api)?;

        let task = mconn_connector_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mconn connector delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MconnCustomerConnectorDeleteResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn mconn_connector_delete(
        &self,
        args: &MconnConnectorDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MconnCustomerConnectorDeleteResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mconn_connector_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.connector_id,
        )
        .map_err(ProviderError::Api)?;

        let task = mconn_connector_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mconn connector telemetry events list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MconnCustomerEventsGetSuccess result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn mconn_connector_telemetry_events_list(
        &self,
        args: &MconnConnectorTelemetryEventsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MconnCustomerEventsGetSuccess, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mconn_connector_telemetry_events_list_builder(
            &self.http_client,
            &args.account_id,
            &args.connector_id,
            &args.from,
            &args.to,
            &args.limit,
            &args.cursor,
            &args.k,
        )
        .map_err(ProviderError::Api)?;

        let task = mconn_connector_telemetry_events_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mconn connector telemetry events list latest.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MconnCustomerEventsGetLatestSuccess result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn mconn_connector_telemetry_events_list_latest(
        &self,
        args: &MconnConnectorTelemetryEventsListLatestArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MconnCustomerEventsGetLatestSuccess, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mconn_connector_telemetry_events_list_latest_builder(
            &self.http_client,
            &args.account_id,
            &args.connector_id,
        )
        .map_err(ProviderError::Api)?;

        let task = mconn_connector_telemetry_events_list_latest_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mconn connector telemetry events get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MconnCustomerEventGetSuccess result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn mconn_connector_telemetry_events_get(
        &self,
        args: &MconnConnectorTelemetryEventsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MconnCustomerEventGetSuccess, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mconn_connector_telemetry_events_get_builder(
            &self.http_client,
            &args.account_id,
            &args.connector_id,
            &args.event_t,
            &args.event_n,
        )
        .map_err(ProviderError::Api)?;

        let task = mconn_connector_telemetry_events_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mconn connector telemetry snapshots list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MconnCustomerSnapshotsGetSuccess result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn mconn_connector_telemetry_snapshots_list(
        &self,
        args: &MconnConnectorTelemetrySnapshotsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MconnCustomerSnapshotsGetSuccess, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mconn_connector_telemetry_snapshots_list_builder(
            &self.http_client,
            &args.account_id,
            &args.connector_id,
            &args.from,
            &args.to,
            &args.limit,
            &args.cursor,
        )
        .map_err(ProviderError::Api)?;

        let task = mconn_connector_telemetry_snapshots_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mconn connector telemetry snapshots list latest.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MconnCustomerSnapshotsGetLatestSuccess result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn mconn_connector_telemetry_snapshots_list_latest(
        &self,
        args: &MconnConnectorTelemetrySnapshotsListLatestArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MconnCustomerSnapshotsGetLatestSuccess, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mconn_connector_telemetry_snapshots_list_latest_builder(
            &self.http_client,
            &args.account_id,
            &args.connector_id,
        )
        .map_err(ProviderError::Api)?;

        let task = mconn_connector_telemetry_snapshots_list_latest_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mconn connector telemetry snapshots get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MconnCustomerSnapshotGetSuccess result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn mconn_connector_telemetry_snapshots_get(
        &self,
        args: &MconnConnectorTelemetrySnapshotsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MconnCustomerSnapshotGetSuccess, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mconn_connector_telemetry_snapshots_get_builder(
            &self.http_client,
            &args.account_id,
            &args.connector_id,
            &args.snapshot_t,
        )
        .map_err(ProviderError::Api)?;

        let task = mconn_connector_telemetry_snapshots_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic gre tunnels list gre tunnels.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicTunnelsCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_gre_tunnels_list_gre_tunnels(
        &self,
        args: &MagicGreTunnelsListGreTunnelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicTunnelsCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_gre_tunnels_list_gre_tunnels_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_gre_tunnels_list_gre_tunnels_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic gre tunnels create gre tunnels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicCreateGreTunnelResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_gre_tunnels_create_gre_tunnels(
        &self,
        args: &MagicGreTunnelsCreateGreTunnelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicCreateGreTunnelResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_gre_tunnels_create_gre_tunnels_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_gre_tunnels_create_gre_tunnels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic gre tunnels update multiple gre tunnels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicModifiedTunnelsCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_gre_tunnels_update_multiple_gre_tunnels(
        &self,
        args: &MagicGreTunnelsUpdateMultipleGreTunnelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicModifiedTunnelsCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_gre_tunnels_update_multiple_gre_tunnels_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_gre_tunnels_update_multiple_gre_tunnels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic gre tunnels list gre tunnel details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicTunnelSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_gre_tunnels_list_gre_tunnel_details(
        &self,
        args: &MagicGreTunnelsListGreTunnelDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicTunnelSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_gre_tunnels_list_gre_tunnel_details_builder(
            &self.http_client,
            &args.gre_tunnel_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_gre_tunnels_list_gre_tunnel_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic gre tunnels update gre tunnel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicTunnelModifiedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_gre_tunnels_update_gre_tunnel(
        &self,
        args: &MagicGreTunnelsUpdateGreTunnelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicTunnelModifiedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_gre_tunnels_update_gre_tunnel_builder(
            &self.http_client,
            &args.gre_tunnel_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_gre_tunnels_update_gre_tunnel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic gre tunnels delete gre tunnel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicTunnelDeletedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_gre_tunnels_delete_gre_tunnel(
        &self,
        args: &MagicGreTunnelsDeleteGreTunnelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicTunnelDeletedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_gre_tunnels_delete_gre_tunnel_builder(
            &self.http_client,
            &args.gre_tunnel_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_gre_tunnels_delete_gre_tunnel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic ipsec tunnels list ipsec tunnels.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicSchemasTunnelsCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_ipsec_tunnels_list_ipsec_tunnels(
        &self,
        args: &MagicIpsecTunnelsListIpsecTunnelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicSchemasTunnelsCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_ipsec_tunnels_list_ipsec_tunnels_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_ipsec_tunnels_list_ipsec_tunnels_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic ipsec tunnels create ipsec tunnels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicSchemasCreateIpsecTunnelResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_ipsec_tunnels_create_ipsec_tunnels(
        &self,
        args: &MagicIpsecTunnelsCreateIpsecTunnelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicSchemasCreateIpsecTunnelResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_ipsec_tunnels_create_ipsec_tunnels_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_ipsec_tunnels_create_ipsec_tunnels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic ipsec tunnels update multiple ipsec tunnels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicSchemasModifiedTunnelsCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_ipsec_tunnels_update_multiple_ipsec_tunnels(
        &self,
        args: &MagicIpsecTunnelsUpdateMultipleIpsecTunnelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicSchemasModifiedTunnelsCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_ipsec_tunnels_update_multiple_ipsec_tunnels_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_ipsec_tunnels_update_multiple_ipsec_tunnels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic ipsec tunnels list ipsec tunnel details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicSchemasTunnelSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_ipsec_tunnels_list_ipsec_tunnel_details(
        &self,
        args: &MagicIpsecTunnelsListIpsecTunnelDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicSchemasTunnelSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_ipsec_tunnels_list_ipsec_tunnel_details_builder(
            &self.http_client,
            &args.ipsec_tunnel_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_ipsec_tunnels_list_ipsec_tunnel_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic ipsec tunnels update ipsec tunnel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicSchemasTunnelModifiedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_ipsec_tunnels_update_ipsec_tunnel(
        &self,
        args: &MagicIpsecTunnelsUpdateIpsecTunnelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicSchemasTunnelModifiedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_ipsec_tunnels_update_ipsec_tunnel_builder(
            &self.http_client,
            &args.ipsec_tunnel_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_ipsec_tunnels_update_ipsec_tunnel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic ipsec tunnels delete ipsec tunnel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicSchemasTunnelDeletedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_ipsec_tunnels_delete_ipsec_tunnel(
        &self,
        args: &MagicIpsecTunnelsDeleteIpsecTunnelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicSchemasTunnelDeletedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_ipsec_tunnels_delete_ipsec_tunnel_builder(
            &self.http_client,
            &args.ipsec_tunnel_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_ipsec_tunnels_delete_ipsec_tunnel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic ipsec tunnels generate pre shared key psk for ipsec tunnels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicPskGenerationResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_ipsec_tunnels_generate_pre_shared_key_psk_for_ipsec_tunnels(
        &self,
        args: &MagicIpsecTunnelsGeneratePreSharedKeyPskForIpsecTunnelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicPskGenerationResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_ipsec_tunnels_generate_pre_shared_key_psk_for_ipsec_tunnels_builder(
            &self.http_client,
            &args.ipsec_tunnel_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_ipsec_tunnels_generate_pre_shared_key_psk_for_ipsec_tunnels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic static routes list routes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicRoutesCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_static_routes_list_routes(
        &self,
        args: &MagicStaticRoutesListRoutesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicRoutesCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_static_routes_list_routes_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_static_routes_list_routes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic static routes create routes.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicCreateRouteResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_static_routes_create_routes(
        &self,
        args: &MagicStaticRoutesCreateRoutesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicCreateRouteResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_static_routes_create_routes_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_static_routes_create_routes_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic static routes update many routes.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicMultipleRouteModifiedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_static_routes_update_many_routes(
        &self,
        args: &MagicStaticRoutesUpdateManyRoutesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicMultipleRouteModifiedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_static_routes_update_many_routes_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_static_routes_update_many_routes_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic static routes delete many routes.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicMultipleRouteDeleteResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_static_routes_delete_many_routes(
        &self,
        args: &MagicStaticRoutesDeleteManyRoutesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicMultipleRouteDeleteResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_static_routes_delete_many_routes_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_static_routes_delete_many_routes_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic static routes route details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicRouteSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_static_routes_route_details(
        &self,
        args: &MagicStaticRoutesRouteDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicRouteSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_static_routes_route_details_builder(
            &self.http_client,
            &args.route_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_static_routes_route_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic static routes update route.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicRouteModifiedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_static_routes_update_route(
        &self,
        args: &MagicStaticRoutesUpdateRouteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicRouteModifiedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_static_routes_update_route_builder(
            &self.http_client,
            &args.route_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_static_routes_update_route_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic static routes delete route.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicRouteDeletedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_static_routes_delete_route(
        &self,
        args: &MagicStaticRoutesDeleteRouteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicRouteDeletedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_static_routes_delete_route_builder(
            &self.http_client,
            &args.route_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_static_routes_delete_route_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic sites list sites.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicSitesCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_sites_list_sites(
        &self,
        args: &MagicSitesListSitesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicSitesCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_sites_list_sites_builder(
            &self.http_client,
            &args.account_id,
            &args.connectorid,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_sites_list_sites_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic sites create site.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicSiteSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_sites_create_site(
        &self,
        args: &MagicSitesCreateSiteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicSiteSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_sites_create_site_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_sites_create_site_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic sites site details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicSiteSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_sites_site_details(
        &self,
        args: &MagicSitesSiteDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicSiteSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_sites_site_details_builder(
            &self.http_client,
            &args.site_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_sites_site_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic sites update site.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicSiteModifiedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_sites_update_site(
        &self,
        args: &MagicSitesUpdateSiteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicSiteModifiedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_sites_update_site_builder(
            &self.http_client,
            &args.site_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_sites_update_site_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic sites patch site.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicSiteModifiedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_sites_patch_site(
        &self,
        args: &MagicSitesPatchSiteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicSiteModifiedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_sites_patch_site_builder(
            &self.http_client,
            &args.site_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_sites_patch_site_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic sites delete site.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicSiteDeletedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_sites_delete_site(
        &self,
        args: &MagicSitesDeleteSiteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicSiteDeletedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_sites_delete_site_builder(
            &self.http_client,
            &args.site_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_sites_delete_site_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site acls list acls.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicAclsCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_site_acls_list_acls(
        &self,
        args: &MagicSiteAclsListAclsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicAclsCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_acls_list_acls_builder(
            &self.http_client,
            &args.account_id,
            &args.site_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_acls_list_acls_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site acls create acl.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicAclSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_acls_create_acl(
        &self,
        args: &MagicSiteAclsCreateAclArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicAclSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_acls_create_acl_builder(
            &self.http_client,
            &args.account_id,
            &args.site_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_acls_create_acl_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site acls acl details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicAclSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_site_acls_acl_details(
        &self,
        args: &MagicSiteAclsAclDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicAclSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_acls_acl_details_builder(
            &self.http_client,
            &args.site_id,
            &args.account_id,
            &args.acl_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_acls_acl_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site acls update acl.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicAclModifiedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_acls_update_acl(
        &self,
        args: &MagicSiteAclsUpdateAclArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicAclModifiedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_acls_update_acl_builder(
            &self.http_client,
            &args.site_id,
            &args.account_id,
            &args.acl_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_acls_update_acl_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site acls patch acl.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicAclModifiedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_acls_patch_acl(
        &self,
        args: &MagicSiteAclsPatchAclArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicAclModifiedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_acls_patch_acl_builder(
            &self.http_client,
            &args.site_id,
            &args.account_id,
            &args.acl_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_acls_patch_acl_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site acls delete acl.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicAclDeletedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_acls_delete_acl(
        &self,
        args: &MagicSiteAclsDeleteAclArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicAclDeletedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_acls_delete_acl_builder(
            &self.http_client,
            &args.site_id,
            &args.account_id,
            &args.acl_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_acls_delete_acl_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site app configs list app configs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicAppConfigsCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_site_app_configs_list_app_configs(
        &self,
        args: &MagicSiteAppConfigsListAppConfigsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicAppConfigsCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_app_configs_list_app_configs_builder(
            &self.http_client,
            &args.account_id,
            &args.site_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_app_configs_list_app_configs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site app configs add app config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicAppConfigSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_app_configs_add_app_config(
        &self,
        args: &MagicSiteAppConfigsAddAppConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicAppConfigSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_app_configs_add_app_config_builder(
            &self.http_client,
            &args.account_id,
            &args.site_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_app_configs_add_app_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site app configs update app config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicAppConfigSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_app_configs_update_app_config(
        &self,
        args: &MagicSiteAppConfigsUpdateAppConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicAppConfigSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_app_configs_update_app_config_builder(
            &self.http_client,
            &args.account_id,
            &args.site_id,
            &args.app_config_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_app_configs_update_app_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site app configs patch app config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicAppConfigSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_app_configs_patch_app_config(
        &self,
        args: &MagicSiteAppConfigsPatchAppConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicAppConfigSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_app_configs_patch_app_config_builder(
            &self.http_client,
            &args.account_id,
            &args.site_id,
            &args.app_config_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_app_configs_patch_app_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site app configs delete app config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicAppConfigSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_app_configs_delete_app_config(
        &self,
        args: &MagicSiteAppConfigsDeleteAppConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicAppConfigSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_app_configs_delete_app_config_builder(
            &self.http_client,
            &args.account_id,
            &args.site_id,
            &args.app_config_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_app_configs_delete_app_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site lans list lans.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicLansCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_site_lans_list_lans(
        &self,
        args: &MagicSiteLansListLansArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicLansCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_lans_list_lans_builder(
            &self.http_client,
            &args.account_id,
            &args.site_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_lans_list_lans_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site lans create lan.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicLansCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_lans_create_lan(
        &self,
        args: &MagicSiteLansCreateLanArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicLansCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_lans_create_lan_builder(
            &self.http_client,
            &args.account_id,
            &args.site_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_lans_create_lan_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site lans lan details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicLanSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_site_lans_lan_details(
        &self,
        args: &MagicSiteLansLanDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicLanSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_lans_lan_details_builder(
            &self.http_client,
            &args.site_id,
            &args.account_id,
            &args.lan_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_lans_lan_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site lans update lan.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicLanModifiedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_lans_update_lan(
        &self,
        args: &MagicSiteLansUpdateLanArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicLanModifiedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_lans_update_lan_builder(
            &self.http_client,
            &args.site_id,
            &args.account_id,
            &args.lan_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_lans_update_lan_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site lans patch lan.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicLanModifiedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_lans_patch_lan(
        &self,
        args: &MagicSiteLansPatchLanArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicLanModifiedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_lans_patch_lan_builder(
            &self.http_client,
            &args.site_id,
            &args.account_id,
            &args.lan_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_lans_patch_lan_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site lans delete lan.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicLanDeletedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_lans_delete_lan(
        &self,
        args: &MagicSiteLansDeleteLanArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicLanDeletedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_lans_delete_lan_builder(
            &self.http_client,
            &args.site_id,
            &args.account_id,
            &args.lan_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_lans_delete_lan_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site netflow config details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicNetflowConfigSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_site_netflow_config_details(
        &self,
        args: &MagicSiteNetflowConfigDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicNetflowConfigSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_netflow_config_details_builder(
            &self.http_client,
            &args.account_id,
            &args.site_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_netflow_config_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site netflow config create netflow config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicNetflowConfigSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_netflow_config_create_netflow_config(
        &self,
        args: &MagicSiteNetflowConfigCreateNetflowConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicNetflowConfigSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_netflow_config_create_netflow_config_builder(
            &self.http_client,
            &args.account_id,
            &args.site_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_netflow_config_create_netflow_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site netflow config update netflow config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicNetflowConfigSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_netflow_config_update_netflow_config(
        &self,
        args: &MagicSiteNetflowConfigUpdateNetflowConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicNetflowConfigSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_netflow_config_update_netflow_config_builder(
            &self.http_client,
            &args.account_id,
            &args.site_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_netflow_config_update_netflow_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site netflow config patch netflow config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicNetflowConfigSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_netflow_config_patch_netflow_config(
        &self,
        args: &MagicSiteNetflowConfigPatchNetflowConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicNetflowConfigSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_netflow_config_patch_netflow_config_builder(
            &self.http_client,
            &args.account_id,
            &args.site_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_netflow_config_patch_netflow_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site netflow config delete netflow config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicNetflowConfigSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_netflow_config_delete_netflow_config(
        &self,
        args: &MagicSiteNetflowConfigDeleteNetflowConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicNetflowConfigSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_netflow_config_delete_netflow_config_builder(
            &self.http_client,
            &args.account_id,
            &args.site_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_netflow_config_delete_netflow_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site wans list wans.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicWansCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_site_wans_list_wans(
        &self,
        args: &MagicSiteWansListWansArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicWansCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_wans_list_wans_builder(
            &self.http_client,
            &args.account_id,
            &args.site_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_wans_list_wans_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site wans create wan.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicWansCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_wans_create_wan(
        &self,
        args: &MagicSiteWansCreateWanArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicWansCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_wans_create_wan_builder(
            &self.http_client,
            &args.account_id,
            &args.site_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_wans_create_wan_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site wans wan details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicWanSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_site_wans_wan_details(
        &self,
        args: &MagicSiteWansWanDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicWanSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_wans_wan_details_builder(
            &self.http_client,
            &args.site_id,
            &args.account_id,
            &args.wan_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_wans_wan_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site wans update wan.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicWanModifiedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_wans_update_wan(
        &self,
        args: &MagicSiteWansUpdateWanArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicWanModifiedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_wans_update_wan_builder(
            &self.http_client,
            &args.site_id,
            &args.account_id,
            &args.wan_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_wans_update_wan_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site wans patch wan.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicWanModifiedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_wans_patch_wan(
        &self,
        args: &MagicSiteWansPatchWanArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicWanModifiedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_wans_patch_wan_builder(
            &self.http_client,
            &args.site_id,
            &args.account_id,
            &args.wan_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_wans_patch_wan_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic site wans delete wan.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicWanDeletedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_site_wans_delete_wan(
        &self,
        args: &MagicSiteWansDeleteWanArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicWanDeletedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_site_wans_delete_wan_builder(
            &self.http_client,
            &args.site_id,
            &args.account_id,
            &args.wan_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_site_wans_delete_wan_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account members list members.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamCollectionMemberResponseWithPolicies result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_members_list_members(
        &self,
        args: &AccountMembersListMembersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamCollectionMemberResponseWithPolicies, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_members_list_members_builder(
            &self.http_client,
            &args.account_id,
            &args.order,
            &args.status,
            &args.page,
            &args.per_page,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = account_members_list_members_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account members add member.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSingleMemberResponseWithPolicies result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_members_add_member(
        &self,
        args: &AccountMembersAddMemberArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSingleMemberResponseWithPolicies, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_members_add_member_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_members_add_member_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account members member details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSingleMemberResponseWithPolicies result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_members_member_details(
        &self,
        args: &AccountMembersMemberDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSingleMemberResponseWithPolicies, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_members_member_details_builder(
            &self.http_client,
            &args.member_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_members_member_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account members update member.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSingleMemberResponseWithPolicies result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_members_update_member(
        &self,
        args: &AccountMembersUpdateMemberArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSingleMemberResponseWithPolicies, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_members_update_member_builder(
            &self.http_client,
            &args.member_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_members_update_member_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account members remove member.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamApiResponseSingleId result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_members_remove_member(
        &self,
        args: &AccountMembersRemoveMemberArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamApiResponseSingleId, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_members_remove_member_builder(
            &self.http_client,
            &args.member_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_members_remove_member_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic network monitoring configuration list account configuration.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityMnmMnmConfigSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_network_monitoring_configuration_list_account_configuration(
        &self,
        args: &MagicNetworkMonitoringConfigurationListAccountConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityMnmMnmConfigSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_network_monitoring_configuration_list_account_configuration_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_network_monitoring_configuration_list_account_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic network monitoring configuration create account configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityMnmMnmConfigSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_network_monitoring_configuration_create_account_configuration(
        &self,
        args: &MagicNetworkMonitoringConfigurationCreateAccountConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityMnmMnmConfigSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_network_monitoring_configuration_create_account_configuration_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_network_monitoring_configuration_create_account_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic network monitoring configuration update an entire account configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityMnmMnmConfigSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_network_monitoring_configuration_update_an_entire_account_configuration(
        &self,
        args: &MagicNetworkMonitoringConfigurationUpdateAnEntireAccountConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityMnmMnmConfigSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_network_monitoring_configuration_update_an_entire_account_configuration_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_network_monitoring_configuration_update_an_entire_account_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic network monitoring configuration update account configuration fields.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityMnmMnmConfigSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_network_monitoring_configuration_update_account_configuration_fields(
        &self,
        args: &MagicNetworkMonitoringConfigurationUpdateAccountConfigurationFieldsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityMnmMnmConfigSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_network_monitoring_configuration_update_account_configuration_fields_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_network_monitoring_configuration_update_account_configuration_fields_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic network monitoring configuration delete account configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityMnmMnmConfigSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_network_monitoring_configuration_delete_account_configuration(
        &self,
        args: &MagicNetworkMonitoringConfigurationDeleteAccountConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityMnmMnmConfigSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_network_monitoring_configuration_delete_account_configuration_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_network_monitoring_configuration_delete_account_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic network monitoring configuration list rules and account configuration.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityMnmMnmConfigSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_network_monitoring_configuration_list_rules_and_account_configuration(
        &self,
        args: &MagicNetworkMonitoringConfigurationListRulesAndAccountConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityMnmMnmConfigSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_network_monitoring_configuration_list_rules_and_account_configuration_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_network_monitoring_configuration_list_rules_and_account_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic network monitoring rules list rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityMnmMnmRulesCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_network_monitoring_rules_list_rules(
        &self,
        args: &MagicNetworkMonitoringRulesListRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityMnmMnmRulesCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_network_monitoring_rules_list_rules_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_network_monitoring_rules_list_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic network monitoring rules create rules.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityMnmMnmRulesSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_network_monitoring_rules_create_rules(
        &self,
        args: &MagicNetworkMonitoringRulesCreateRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityMnmMnmRulesSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_network_monitoring_rules_create_rules_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_network_monitoring_rules_create_rules_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic network monitoring rules update rules.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityMnmMnmRulesSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_network_monitoring_rules_update_rules(
        &self,
        args: &MagicNetworkMonitoringRulesUpdateRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityMnmMnmRulesSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_network_monitoring_rules_update_rules_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_network_monitoring_rules_update_rules_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic network monitoring rules get rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityMnmMnmRulesSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_network_monitoring_rules_get_rule(
        &self,
        args: &MagicNetworkMonitoringRulesGetRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityMnmMnmRulesSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_network_monitoring_rules_get_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_network_monitoring_rules_get_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic network monitoring rules update rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityMnmMnmRulesSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_network_monitoring_rules_update_rule(
        &self,
        args: &MagicNetworkMonitoringRulesUpdateRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityMnmMnmRulesSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_network_monitoring_rules_update_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_network_monitoring_rules_update_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic network monitoring rules delete rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityMnmMnmRulesSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_network_monitoring_rules_delete_rule(
        &self,
        args: &MagicNetworkMonitoringRulesDeleteRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityMnmMnmRulesSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_network_monitoring_rules_delete_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_network_monitoring_rules_delete_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic network monitoring rules update advertisement for rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityMnmMnmRuleAdvertisementSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_network_monitoring_rules_update_advertisement_for_rule(
        &self,
        args: &MagicNetworkMonitoringRulesUpdateAdvertisementForRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityMnmMnmRuleAdvertisementSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_network_monitoring_rules_update_advertisement_for_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_network_monitoring_rules_update_advertisement_for_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic network monitoring vpc flows generate authentication token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityMnmMnmVpcFlowsSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_network_monitoring_vpc_flows_generate_authentication_token(
        &self,
        args: &MagicNetworkMonitoringVpcFlowsGenerateAuthenticationTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityMnmMnmVpcFlowsSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_network_monitoring_vpc_flows_generate_authentication_token_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_network_monitoring_vpc_flows_generate_authentication_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Accounts move accounts.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn accounts_move_accounts(
        &self,
        args: &AccountsMoveAccountsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = accounts_move_accounts_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = accounts_move_accounts_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// M tls certificate management list m tls certificates.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesMtlsManagementComponentsSchemasCertificateResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn m_tls_certificate_management_list_m_tls_certificates(
        &self,
        args: &MTlsCertificateManagementListMTlsCertificatesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesMtlsManagementComponentsSchemasCertificateResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = m_tls_certificate_management_list_m_tls_certificates_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = m_tls_certificate_management_list_m_tls_certificates_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// M tls certificate management upload m tls certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCertificateResponseSinglePost result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn m_tls_certificate_management_upload_m_tls_certificate(
        &self,
        args: &MTlsCertificateManagementUploadMTlsCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCertificateResponseSinglePost, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = m_tls_certificate_management_upload_m_tls_certificate_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = m_tls_certificate_management_upload_m_tls_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// M tls certificate management get m tls certificate.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesMtlsManagementComponentsSchemasCertificateResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn m_tls_certificate_management_get_m_tls_certificate(
        &self,
        args: &MTlsCertificateManagementGetMTlsCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesMtlsManagementComponentsSchemasCertificateResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = m_tls_certificate_management_get_m_tls_certificate_builder(
            &self.http_client,
            &args.mtls_certificate_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = m_tls_certificate_management_get_m_tls_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// M tls certificate management delete m tls certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesMtlsManagementComponentsSchemasCertificateResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn m_tls_certificate_management_delete_m_tls_certificate(
        &self,
        args: &MTlsCertificateManagementDeleteMTlsCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesMtlsManagementComponentsSchemasCertificateResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = m_tls_certificate_management_delete_m_tls_certificate_builder(
            &self.http_client,
            &args.mtls_certificate_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = m_tls_certificate_management_delete_m_tls_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// M tls certificate management list m tls certificate associations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesAssociationResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn m_tls_certificate_management_list_m_tls_certificate_associations(
        &self,
        args: &MTlsCertificateManagementListMTlsCertificateAssociationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesAssociationResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = m_tls_certificate_management_list_m_tls_certificate_associations_builder(
            &self.http_client,
            &args.mtls_certificate_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = m_tls_certificate_management_list_m_tls_certificate_associations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Accounts list account organizations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn accounts_list_account_organizations(
        &self,
        args: &AccountsListAccountOrganizationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = accounts_list_account_organizations_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = accounts_list_account_organizations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pages project get projects.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn pages_project_get_projects(
        &self,
        args: &PagesProjectGetProjectsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pages_project_get_projects_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = pages_project_get_projects_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pages project create project.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn pages_project_create_project(
        &self,
        args: &PagesProjectCreateProjectArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pages_project_create_project_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pages_project_create_project_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pages project get project.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn pages_project_get_project(
        &self,
        args: &PagesProjectGetProjectArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pages_project_get_project_builder(
            &self.http_client,
            &args.project_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pages_project_get_project_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pages project update project.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn pages_project_update_project(
        &self,
        args: &PagesProjectUpdateProjectArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pages_project_update_project_builder(
            &self.http_client,
            &args.project_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pages_project_update_project_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pages project delete project.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn pages_project_delete_project(
        &self,
        args: &PagesProjectDeleteProjectArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pages_project_delete_project_builder(
            &self.http_client,
            &args.project_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pages_project_delete_project_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pages deployment get deployments.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn pages_deployment_get_deployments(
        &self,
        args: &PagesDeploymentGetDeploymentsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pages_deployment_get_deployments_builder(
            &self.http_client,
            &args.project_name,
            &args.account_id,
            &args.env,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = pages_deployment_get_deployments_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pages deployment create deployment.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn pages_deployment_create_deployment(
        &self,
        args: &PagesDeploymentCreateDeploymentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pages_deployment_create_deployment_builder(
            &self.http_client,
            &args.project_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pages_deployment_create_deployment_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pages deployment get deployment info.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn pages_deployment_get_deployment_info(
        &self,
        args: &PagesDeploymentGetDeploymentInfoArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pages_deployment_get_deployment_info_builder(
            &self.http_client,
            &args.deployment_id,
            &args.project_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pages_deployment_get_deployment_info_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pages deployment delete deployment.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn pages_deployment_delete_deployment(
        &self,
        args: &PagesDeploymentDeleteDeploymentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pages_deployment_delete_deployment_builder(
            &self.http_client,
            &args.deployment_id,
            &args.project_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pages_deployment_delete_deployment_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pages deployment get deployment logs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn pages_deployment_get_deployment_logs(
        &self,
        args: &PagesDeploymentGetDeploymentLogsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pages_deployment_get_deployment_logs_builder(
            &self.http_client,
            &args.deployment_id,
            &args.project_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pages_deployment_get_deployment_logs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pages deployment retry deployment.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn pages_deployment_retry_deployment(
        &self,
        args: &PagesDeploymentRetryDeploymentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pages_deployment_retry_deployment_builder(
            &self.http_client,
            &args.deployment_id,
            &args.project_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pages_deployment_retry_deployment_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pages deployment rollback deployment.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn pages_deployment_rollback_deployment(
        &self,
        args: &PagesDeploymentRollbackDeploymentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pages_deployment_rollback_deployment_builder(
            &self.http_client,
            &args.deployment_id,
            &args.project_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pages_deployment_rollback_deployment_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pages domains get domains.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn pages_domains_get_domains(
        &self,
        args: &PagesDomainsGetDomainsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pages_domains_get_domains_builder(
            &self.http_client,
            &args.project_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pages_domains_get_domains_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pages domains add domain.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn pages_domains_add_domain(
        &self,
        args: &PagesDomainsAddDomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pages_domains_add_domain_builder(
            &self.http_client,
            &args.project_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pages_domains_add_domain_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pages domains get domain.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn pages_domains_get_domain(
        &self,
        args: &PagesDomainsGetDomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pages_domains_get_domain_builder(
            &self.http_client,
            &args.domain_name,
            &args.project_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pages_domains_get_domain_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pages domains patch domain.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn pages_domains_patch_domain(
        &self,
        args: &PagesDomainsPatchDomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pages_domains_patch_domain_builder(
            &self.http_client,
            &args.domain_name,
            &args.project_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pages_domains_patch_domain_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pages domains delete domain.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn pages_domains_delete_domain(
        &self,
        args: &PagesDomainsDeleteDomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pages_domains_delete_domain_builder(
            &self.http_client,
            &args.domain_name,
            &args.project_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pages_domains_delete_domain_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pages purge build cache.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn pages_purge_build_cache(
        &self,
        args: &PagesPurgeBuildCacheArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pages_purge_build_cache_builder(
            &self.http_client,
            &args.project_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pages_purge_build_cache_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pay per crawl crawler get stripe config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PayPerCrawlGetStripeConfigResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn pay_per_crawl_crawler_get_stripe_config(
        &self,
        args: &PayPerCrawlCrawlerGetStripeConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PayPerCrawlGetStripeConfigResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pay_per_crawl_crawler_get_stripe_config_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pay_per_crawl_crawler_get_stripe_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pay per crawl crawler create stripe config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PayPerCrawlCreateStripeConfigResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn pay_per_crawl_crawler_create_stripe_config(
        &self,
        args: &PayPerCrawlCrawlerCreateStripeConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PayPerCrawlCreateStripeConfigResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pay_per_crawl_crawler_create_stripe_config_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pay_per_crawl_crawler_create_stripe_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pay per crawl crawler delete stripe config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PayPerCrawlApiNoResultResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn pay_per_crawl_crawler_delete_stripe_config(
        &self,
        args: &PayPerCrawlCrawlerDeleteStripeConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PayPerCrawlApiNoResultResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pay_per_crawl_crawler_delete_stripe_config_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pay_per_crawl_crawler_delete_stripe_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pay per crawl publisher get stripe config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PayPerCrawlGetStripeConfigResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn pay_per_crawl_publisher_get_stripe_config(
        &self,
        args: &PayPerCrawlPublisherGetStripeConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PayPerCrawlGetStripeConfigResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pay_per_crawl_publisher_get_stripe_config_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pay_per_crawl_publisher_get_stripe_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pay per crawl publisher create stripe config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PayPerCrawlCreateStripeConfigResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn pay_per_crawl_publisher_create_stripe_config(
        &self,
        args: &PayPerCrawlPublisherCreateStripeConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PayPerCrawlCreateStripeConfigResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pay_per_crawl_publisher_create_stripe_config_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pay_per_crawl_publisher_create_stripe_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pay per crawl publisher delete stripe config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PayPerCrawlApiNoResultResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn pay_per_crawl_publisher_delete_stripe_config(
        &self,
        args: &PayPerCrawlPublisherDeleteStripeConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PayPerCrawlApiNoResultResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pay_per_crawl_publisher_delete_stripe_config_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pay_per_crawl_publisher_delete_stripe_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pay per crawl set zones can be enabled.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PayPerCrawlApiNoResultResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn pay_per_crawl_set_zones_can_be_enabled(
        &self,
        args: &PayPerCrawlSetZonesCanBeEnabledArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PayPerCrawlApiNoResultResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pay_per_crawl_set_zones_can_be_enabled_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pay_per_crawl_set_zones_can_be_enabled_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pay per crawl query zones can be enabled.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PayPerCrawlQueryZonesCanBeEnabledResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn pay_per_crawl_query_zones_can_be_enabled(
        &self,
        args: &PayPerCrawlQueryZonesCanBeEnabledArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PayPerCrawlQueryZonesCanBeEnabledResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pay_per_crawl_query_zones_can_be_enabled_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pay_per_crawl_query_zones_can_be_enabled_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic pcap collection list packet capture requests.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityPcapsPcapsCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_pcap_collection_list_packet_capture_requests(
        &self,
        args: &MagicPcapCollectionListPacketCaptureRequestsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityPcapsPcapsCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_pcap_collection_list_packet_capture_requests_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_pcap_collection_list_packet_capture_requests_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic pcap collection create pcap request.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityPcapsPcapsSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_pcap_collection_create_pcap_request(
        &self,
        args: &MagicPcapCollectionCreatePcapRequestArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityPcapsPcapsSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_pcap_collection_create_pcap_request_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_pcap_collection_create_pcap_request_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic pcap collection list pca ps bucket ownership.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityPcapsPcapsOwnershipCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_pcap_collection_list_pca_ps_bucket_ownership(
        &self,
        args: &MagicPcapCollectionListPcaPsBucketOwnershipArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityPcapsPcapsOwnershipCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_pcap_collection_list_pca_ps_bucket_ownership_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_pcap_collection_list_pca_ps_bucket_ownership_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic pcap collection add buckets for full packet captures.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityPcapsPcapsOwnershipSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_pcap_collection_add_buckets_for_full_packet_captures(
        &self,
        args: &MagicPcapCollectionAddBucketsForFullPacketCapturesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityPcapsPcapsOwnershipSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_pcap_collection_add_buckets_for_full_packet_captures_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_pcap_collection_add_buckets_for_full_packet_captures_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic pcap collection validate buckets for full packet captures.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityPcapsPcapsOwnershipSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_pcap_collection_validate_buckets_for_full_packet_captures(
        &self,
        args: &MagicPcapCollectionValidateBucketsForFullPacketCapturesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityPcapsPcapsOwnershipSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_pcap_collection_validate_buckets_for_full_packet_captures_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_pcap_collection_validate_buckets_for_full_packet_captures_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic pcap collection delete buckets for full packet captures.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_pcap_collection_delete_buckets_for_full_packet_captures(
        &self,
        args: &MagicPcapCollectionDeleteBucketsForFullPacketCapturesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_pcap_collection_delete_buckets_for_full_packet_captures_builder(
            &self.http_client,
            &args.ownership_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_pcap_collection_delete_buckets_for_full_packet_captures_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic pcap collection get pcap request.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MagicVisibilityPcapsPcapsSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_pcap_collection_get_pcap_request(
        &self,
        args: &MagicPcapCollectionGetPcapRequestArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MagicVisibilityPcapsPcapsSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_pcap_collection_get_pcap_request_builder(
            &self.http_client,
            &args.pcap_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_pcap_collection_get_pcap_request_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic pcap collection download simple pcap.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn magic_pcap_collection_download_simple_pcap(
        &self,
        args: &MagicPcapCollectionDownloadSimplePcapArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_pcap_collection_download_simple_pcap_builder(
            &self.http_client,
            &args.pcap_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_pcap_collection_download_simple_pcap_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Magic pcap collection stop full pcap.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn magic_pcap_collection_stop_full_pcap(
        &self,
        args: &MagicPcapCollectionStopFullPcapArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = magic_pcap_collection_stop_full_pcap_builder(
            &self.http_client,
            &args.pcap_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = magic_pcap_collection_stop_full_pcap_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get v4 accounts by account id pipelines deprecated.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_v4_accounts_by_account_id_pipelines_deprecated(
        &self,
        args: &GetV4AccountsByAccountIdPipelinesDeprecatedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_v4_accounts_by_account_id_pipelines_deprecated_builder(
            &self.http_client,
            &args.account_id,
            &args.search,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = get_v4_accounts_by_account_id_pipelines_deprecated_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post v4 accounts by account id pipelines deprecated.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_v4_accounts_by_account_id_pipelines_deprecated(
        &self,
        args: &PostV4AccountsByAccountIdPipelinesDeprecatedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_v4_accounts_by_account_id_pipelines_deprecated_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_v4_accounts_by_account_id_pipelines_deprecated_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get v4 accounts by account id pipelines v1 pipelines.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_v4_accounts_by_account_id_pipelines_v1_pipelines(
        &self,
        args: &GetV4AccountsByAccountIdPipelinesV1PipelinesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_v4_accounts_by_account_id_pipelines_v1_pipelines_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = get_v4_accounts_by_account_id_pipelines_v1_pipelines_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post v4 accounts by account id pipelines v1 pipelines.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_v4_accounts_by_account_id_pipelines_v1_pipelines(
        &self,
        args: &PostV4AccountsByAccountIdPipelinesV1PipelinesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_v4_accounts_by_account_id_pipelines_v1_pipelines_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_v4_accounts_by_account_id_pipelines_v1_pipelines_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get v4 accounts by account id pipelines v1 pipelines by pipeline id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_v4_accounts_by_account_id_pipelines_v1_pipelines_by_pipeline_id(
        &self,
        args: &GetV4AccountsByAccountIdPipelinesV1PipelinesByPipelineIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_v4_accounts_by_account_id_pipelines_v1_pipelines_by_pipeline_id_builder(
            &self.http_client,
            &args.account_id,
            &args.pipeline_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_v4_accounts_by_account_id_pipelines_v1_pipelines_by_pipeline_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete v4 accounts by account id pipelines v1 pipelines by pipeline id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_v4_accounts_by_account_id_pipelines_v1_pipelines_by_pipeline_id(
        &self,
        args: &DeleteV4AccountsByAccountIdPipelinesV1PipelinesByPipelineIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_v4_accounts_by_account_id_pipelines_v1_pipelines_by_pipeline_id_builder(
            &self.http_client,
            &args.account_id,
            &args.pipeline_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_v4_accounts_by_account_id_pipelines_v1_pipelines_by_pipeline_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get v4 accounts by account id pipelines v1 sinks.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_v4_accounts_by_account_id_pipelines_v1_sinks(
        &self,
        args: &GetV4AccountsByAccountIdPipelinesV1SinksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_v4_accounts_by_account_id_pipelines_v1_sinks_builder(
            &self.http_client,
            &args.account_id,
            &args.pipeline_id,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = get_v4_accounts_by_account_id_pipelines_v1_sinks_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post v4 accounts by account id pipelines v1 sinks.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_v4_accounts_by_account_id_pipelines_v1_sinks(
        &self,
        args: &PostV4AccountsByAccountIdPipelinesV1SinksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_v4_accounts_by_account_id_pipelines_v1_sinks_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_v4_accounts_by_account_id_pipelines_v1_sinks_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get v4 accounts by account id pipelines v1 sinks by sink id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_v4_accounts_by_account_id_pipelines_v1_sinks_by_sink_id(
        &self,
        args: &GetV4AccountsByAccountIdPipelinesV1SinksBySinkIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_v4_accounts_by_account_id_pipelines_v1_sinks_by_sink_id_builder(
            &self.http_client,
            &args.account_id,
            &args.sink_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_v4_accounts_by_account_id_pipelines_v1_sinks_by_sink_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete v4 accounts by account id pipelines v1 sinks by sink id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_v4_accounts_by_account_id_pipelines_v1_sinks_by_sink_id(
        &self,
        args: &DeleteV4AccountsByAccountIdPipelinesV1SinksBySinkIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_v4_accounts_by_account_id_pipelines_v1_sinks_by_sink_id_builder(
            &self.http_client,
            &args.account_id,
            &args.sink_id,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_v4_accounts_by_account_id_pipelines_v1_sinks_by_sink_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get v4 accounts by account id pipelines v1 streams.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_v4_accounts_by_account_id_pipelines_v1_streams(
        &self,
        args: &GetV4AccountsByAccountIdPipelinesV1StreamsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_v4_accounts_by_account_id_pipelines_v1_streams_builder(
            &self.http_client,
            &args.account_id,
            &args.pipeline_id,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = get_v4_accounts_by_account_id_pipelines_v1_streams_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post v4 accounts by account id pipelines v1 streams.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_v4_accounts_by_account_id_pipelines_v1_streams(
        &self,
        args: &PostV4AccountsByAccountIdPipelinesV1StreamsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_v4_accounts_by_account_id_pipelines_v1_streams_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_v4_accounts_by_account_id_pipelines_v1_streams_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get v4 accounts by account id pipelines v1 streams by stream id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_v4_accounts_by_account_id_pipelines_v1_streams_by_stream_id(
        &self,
        args: &GetV4AccountsByAccountIdPipelinesV1StreamsByStreamIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_v4_accounts_by_account_id_pipelines_v1_streams_by_stream_id_builder(
            &self.http_client,
            &args.account_id,
            &args.stream_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_v4_accounts_by_account_id_pipelines_v1_streams_by_stream_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Patch v4 accounts by account id pipelines v1 streams by stream id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn patch_v4_accounts_by_account_id_pipelines_v1_streams_by_stream_id(
        &self,
        args: &PatchV4AccountsByAccountIdPipelinesV1StreamsByStreamIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = patch_v4_accounts_by_account_id_pipelines_v1_streams_by_stream_id_builder(
            &self.http_client,
            &args.account_id,
            &args.stream_id,
        )
        .map_err(ProviderError::Api)?;

        let task = patch_v4_accounts_by_account_id_pipelines_v1_streams_by_stream_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete v4 accounts by account id pipelines v1 streams by stream id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_v4_accounts_by_account_id_pipelines_v1_streams_by_stream_id(
        &self,
        args: &DeleteV4AccountsByAccountIdPipelinesV1StreamsByStreamIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_v4_accounts_by_account_id_pipelines_v1_streams_by_stream_id_builder(
            &self.http_client,
            &args.account_id,
            &args.stream_id,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_v4_accounts_by_account_id_pipelines_v1_streams_by_stream_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post v4 accounts by account id pipelines v1 validate sql.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn post_v4_accounts_by_account_id_pipelines_v1_validate_sql(
        &self,
        args: &PostV4AccountsByAccountIdPipelinesV1ValidateSqlArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_v4_accounts_by_account_id_pipelines_v1_validate_sql_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_v4_accounts_by_account_id_pipelines_v1_validate_sql_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get v4 accounts by account id pipelines by pipeline name deprecated.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_v4_accounts_by_account_id_pipelines_by_pipeline_name_deprecated(
        &self,
        args: &GetV4AccountsByAccountIdPipelinesByPipelineNameDeprecatedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_v4_accounts_by_account_id_pipelines_by_pipeline_name_deprecated_builder(
            &self.http_client,
            &args.account_id,
            &args.pipeline_name,
        )
        .map_err(ProviderError::Api)?;

        let task = get_v4_accounts_by_account_id_pipelines_by_pipeline_name_deprecated_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Put v4 accounts by account id pipelines by pipeline name deprecated.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn put_v4_accounts_by_account_id_pipelines_by_pipeline_name_deprecated(
        &self,
        args: &PutV4AccountsByAccountIdPipelinesByPipelineNameDeprecatedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = put_v4_accounts_by_account_id_pipelines_by_pipeline_name_deprecated_builder(
            &self.http_client,
            &args.account_id,
            &args.pipeline_name,
        )
        .map_err(ProviderError::Api)?;

        let task = put_v4_accounts_by_account_id_pipelines_by_pipeline_name_deprecated_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete v4 accounts by account id pipelines by pipeline name deprecated.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_v4_accounts_by_account_id_pipelines_by_pipeline_name_deprecated(
        &self,
        args: &DeleteV4AccountsByAccountIdPipelinesByPipelineNameDeprecatedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_v4_accounts_by_account_id_pipelines_by_pipeline_name_deprecated_builder(
            &self.http_client,
            &args.account_id,
            &args.pipeline_name,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_v4_accounts_by_account_id_pipelines_by_pipeline_name_deprecated_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Accounts get account profile.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn accounts_get_account_profile(
        &self,
        args: &AccountsGetAccountProfileArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = accounts_get_account_profile_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = accounts_get_account_profile_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Accounts modify account profile.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn accounts_modify_account_profile(
        &self,
        args: &AccountsModifyAccountProfileArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = accounts_modify_account_profile_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = accounts_modify_account_profile_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queues list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn queues_list(
        &self,
        args: &QueuesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queues_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = queues_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queues create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn queues_create(
        &self,
        args: &QueuesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queues_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = queues_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queues get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn queues_get(
        &self,
        args: &QueuesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queues_get_builder(
            &self.http_client,
            &args.queue_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = queues_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queues update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn queues_update(
        &self,
        args: &QueuesUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queues_update_builder(
            &self.http_client,
            &args.queue_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = queues_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queues update partial.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn queues_update_partial(
        &self,
        args: &QueuesUpdatePartialArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queues_update_partial_builder(
            &self.http_client,
            &args.queue_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = queues_update_partial_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queues delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MqApiV4Success result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn queues_delete(
        &self,
        args: &QueuesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MqApiV4Success, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queues_delete_builder(
            &self.http_client,
            &args.queue_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = queues_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queues list consumers.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn queues_list_consumers(
        &self,
        args: &QueuesListConsumersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queues_list_consumers_builder(
            &self.http_client,
            &args.queue_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = queues_list_consumers_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queues create consumer.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn queues_create_consumer(
        &self,
        args: &QueuesCreateConsumerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queues_create_consumer_builder(
            &self.http_client,
            &args.queue_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = queues_create_consumer_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queues get consumer.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn queues_get_consumer(
        &self,
        args: &QueuesGetConsumerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queues_get_consumer_builder(
            &self.http_client,
            &args.consumer_id,
            &args.queue_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = queues_get_consumer_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queues update consumer.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn queues_update_consumer(
        &self,
        args: &QueuesUpdateConsumerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queues_update_consumer_builder(
            &self.http_client,
            &args.consumer_id,
            &args.queue_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = queues_update_consumer_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queues delete consumer.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MqApiV4Success result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn queues_delete_consumer(
        &self,
        args: &QueuesDeleteConsumerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MqApiV4Success, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queues_delete_consumer_builder(
            &self.http_client,
            &args.consumer_id,
            &args.queue_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = queues_delete_consumer_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queues push message.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MqApiV4Success result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn queues_push_message(
        &self,
        args: &QueuesPushMessageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MqApiV4Success, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queues_push_message_builder(
            &self.http_client,
            &args.queue_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = queues_push_message_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queues ack messages.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn queues_ack_messages(
        &self,
        args: &QueuesAckMessagesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queues_ack_messages_builder(
            &self.http_client,
            &args.queue_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = queues_ack_messages_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queues push messages.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MqApiV4Success result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn queues_push_messages(
        &self,
        args: &QueuesPushMessagesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MqApiV4Success, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queues_push_messages_builder(
            &self.http_client,
            &args.queue_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = queues_push_messages_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queues pull messages.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn queues_pull_messages(
        &self,
        args: &QueuesPullMessagesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queues_pull_messages_builder(
            &self.http_client,
            &args.queue_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = queues_pull_messages_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queues purge get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn queues_purge_get(
        &self,
        args: &QueuesPurgeGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queues_purge_get_builder(
            &self.http_client,
            &args.queue_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = queues_purge_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queues purge.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn queues_purge(
        &self,
        args: &QueuesPurgeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queues_purge_builder(
            &self.http_client,
            &args.queue_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = queues_purge_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List catalogs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_catalogs(
        &self,
        args: &ListCatalogsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_catalogs_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = list_catalogs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get catalog details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_catalog_details(
        &self,
        args: &GetCatalogDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_catalog_details_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
        )
        .map_err(ProviderError::Api)?;

        let task = get_catalog_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Store credentials.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn store_credentials(
        &self,
        args: &StoreCredentialsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = store_credentials_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
        )
        .map_err(ProviderError::Api)?;

        let task = store_credentials_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Disable catalog.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn disable_catalog(
        &self,
        args: &DisableCatalogArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = disable_catalog_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
        )
        .map_err(ProviderError::Api)?;

        let task = disable_catalog_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Enable catalog.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn enable_catalog(
        &self,
        args: &EnableCatalogArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = enable_catalog_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
        )
        .map_err(ProviderError::Api)?;

        let task = enable_catalog_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get maintenance config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_maintenance_config(
        &self,
        args: &GetMaintenanceConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_maintenance_config_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
        )
        .map_err(ProviderError::Api)?;

        let task = get_maintenance_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update maintenance config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_maintenance_config(
        &self,
        args: &UpdateMaintenanceConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_maintenance_config_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
        )
        .map_err(ProviderError::Api)?;

        let task = update_maintenance_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List namespaces.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_namespaces(
        &self,
        args: &ListNamespacesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_namespaces_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
            &args.page_token,
            &args.page_size,
            &args.parent,
            &args.return_uuids,
            &args.return_details,
        )
        .map_err(ProviderError::Api)?;

        let task = list_namespaces_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List tables.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_tables(
        &self,
        args: &ListTablesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_tables_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
            &args.namespace,
            &args.page_token,
            &args.page_size,
            &args.return_uuids,
            &args.return_details,
        )
        .map_err(ProviderError::Api)?;

        let task = list_tables_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get table maintenance config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_table_maintenance_config(
        &self,
        args: &GetTableMaintenanceConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_table_maintenance_config_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
            &args.namespace,
            &args.table_name,
        )
        .map_err(ProviderError::Api)?;

        let task = get_table_maintenance_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update table maintenance config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_table_maintenance_config(
        &self,
        args: &UpdateTableMaintenanceConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_table_maintenance_config_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
            &args.namespace,
            &args.table_name,
        )
        .map_err(ProviderError::Api)?;

        let task = update_table_maintenance_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 list buckets.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn r2_list_buckets(
        &self,
        args: &R2ListBucketsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_list_buckets_builder(
            &self.http_client,
            &args.account_id,
            &args.name_contains,
            &args.start_after,
            &args.per_page,
            &args.order,
            &args.direction,
            &args.cursor,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_list_buckets_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 create bucket.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn r2_create_bucket(
        &self,
        args: &R2CreateBucketArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_create_bucket_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_create_bucket_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 get bucket.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn r2_get_bucket(
        &self,
        args: &R2GetBucketArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_get_bucket_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_get_bucket_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 patch bucket.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn r2_patch_bucket(
        &self,
        args: &R2PatchBucketArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_patch_bucket_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_patch_bucket_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 delete bucket.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the R2V4Response result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn r2_delete_bucket(
        &self,
        args: &R2DeleteBucketArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<R2V4Response, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_delete_bucket_builder(
            &self.http_client,
            &args.bucket_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_delete_bucket_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 get bucket cors policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn r2_get_bucket_cors_policy(
        &self,
        args: &R2GetBucketCorsPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_get_bucket_cors_policy_builder(
            &self.http_client,
            &args.bucket_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_get_bucket_cors_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 put bucket cors policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn r2_put_bucket_cors_policy(
        &self,
        args: &R2PutBucketCorsPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_put_bucket_cors_policy_builder(
            &self.http_client,
            &args.bucket_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_put_bucket_cors_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 delete bucket cors policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn r2_delete_bucket_cors_policy(
        &self,
        args: &R2DeleteBucketCorsPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_delete_bucket_cors_policy_builder(
            &self.http_client,
            &args.bucket_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_delete_bucket_cors_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 list custom domains.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn r2_list_custom_domains(
        &self,
        args: &R2ListCustomDomainsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_list_custom_domains_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_list_custom_domains_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 add custom domain.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn r2_add_custom_domain(
        &self,
        args: &R2AddCustomDomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_add_custom_domain_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_add_custom_domain_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 get custom domain settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn r2_get_custom_domain_settings(
        &self,
        args: &R2GetCustomDomainSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_get_custom_domain_settings_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
            &args.domain,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_get_custom_domain_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 edit custom domain settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn r2_edit_custom_domain_settings(
        &self,
        args: &R2EditCustomDomainSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_edit_custom_domain_settings_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
            &args.domain,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_edit_custom_domain_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 delete custom domain.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn r2_delete_custom_domain(
        &self,
        args: &R2DeleteCustomDomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_delete_custom_domain_builder(
            &self.http_client,
            &args.bucket_name,
            &args.account_id,
            &args.domain,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_delete_custom_domain_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 get bucket public policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn r2_get_bucket_public_policy(
        &self,
        args: &R2GetBucketPublicPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_get_bucket_public_policy_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_get_bucket_public_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 put bucket public policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn r2_put_bucket_public_policy(
        &self,
        args: &R2PutBucketPublicPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_put_bucket_public_policy_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_put_bucket_public_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 get bucket lifecycle configuration.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn r2_get_bucket_lifecycle_configuration(
        &self,
        args: &R2GetBucketLifecycleConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_get_bucket_lifecycle_configuration_builder(
            &self.http_client,
            &args.bucket_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_get_bucket_lifecycle_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 put bucket lifecycle configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn r2_put_bucket_lifecycle_configuration(
        &self,
        args: &R2PutBucketLifecycleConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_put_bucket_lifecycle_configuration_builder(
            &self.http_client,
            &args.bucket_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_put_bucket_lifecycle_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 get bucket local uploads configuration.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn r2_get_bucket_local_uploads_configuration(
        &self,
        args: &R2GetBucketLocalUploadsConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_get_bucket_local_uploads_configuration_builder(
            &self.http_client,
            &args.bucket_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_get_bucket_local_uploads_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 put bucket local uploads configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn r2_put_bucket_local_uploads_configuration(
        &self,
        args: &R2PutBucketLocalUploadsConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_put_bucket_local_uploads_configuration_builder(
            &self.http_client,
            &args.bucket_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_put_bucket_local_uploads_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 get bucket lock configuration.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn r2_get_bucket_lock_configuration(
        &self,
        args: &R2GetBucketLockConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_get_bucket_lock_configuration_builder(
            &self.http_client,
            &args.bucket_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_get_bucket_lock_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 put bucket lock configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn r2_put_bucket_lock_configuration(
        &self,
        args: &R2PutBucketLockConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_put_bucket_lock_configuration_builder(
            &self.http_client,
            &args.bucket_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_put_bucket_lock_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 get bucket sippy config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn r2_get_bucket_sippy_config(
        &self,
        args: &R2GetBucketSippyConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_get_bucket_sippy_config_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_get_bucket_sippy_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 put bucket sippy config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn r2_put_bucket_sippy_config(
        &self,
        args: &R2PutBucketSippyConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_put_bucket_sippy_config_builder(
            &self.http_client,
            &args.account_id,
            &args.bucket_name,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_put_bucket_sippy_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 delete bucket sippy config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn r2_delete_bucket_sippy_config(
        &self,
        args: &R2DeleteBucketSippyConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_delete_bucket_sippy_config_builder(
            &self.http_client,
            &args.bucket_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_delete_bucket_sippy_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 get account level metrics.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn r2_get_account_level_metrics(
        &self,
        args: &R2GetAccountLevelMetricsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_get_account_level_metrics_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_get_account_level_metrics_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// R2 create temp access credentials.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn r2_create_temp_access_credentials(
        &self,
        args: &R2CreateTempAccessCredentialsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = r2_create_temp_access_credentials_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = r2_create_temp_access_credentials_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get apps.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_apps(
        &self,
        args: &GetAppsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_apps_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_apps_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create app.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_app(
        &self,
        args: &CreateAppArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_app_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_app_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get org analytics.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_org_analytics(
        &self,
        args: &GetOrgAnalyticsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_org_analytics_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_org_analytics_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get livestream analytics complete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_livestream_analytics_complete(
        &self,
        args: &GetLivestreamAnalyticsCompleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_livestream_analytics_complete_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.start_time,
            &args.end_time,
        )
        .map_err(ProviderError::Api)?;

        let task = get_livestream_analytics_complete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Fetch all livestreams.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn fetch_all_livestreams(
        &self,
        args: &FetchAllLivestreamsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = fetch_all_livestreams_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.exclude_meetings,
            &args.per_page,
            &args.page_no,
            &args.status,
            &args.start_time,
            &args.end_time,
            &args.sort_order,
        )
        .map_err(ProviderError::Api)?;

        let task = fetch_all_livestreams_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get v2 livestreams livestream session id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_v2_livestreams_livestream_session_id(
        &self,
        args: &GetV2LivestreamsLivestreamSessionIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_v2_livestreams_livestream_session_id_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.livestream_session_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_v2_livestreams_livestream_session_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get v2 livestream session livestream id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_v2_livestream_session_livestream_id(
        &self,
        args: &GetV2LivestreamSessionLivestreamIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_v2_livestream_session_livestream_id_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.livestream_id,
            &args.page_no,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = get_v2_livestream_session_livestream_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get v2 active livestream session details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_v2_active_livestream_session_details(
        &self,
        args: &GetV2ActiveLivestreamSessionDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_v2_active_livestream_session_details_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.livestream_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_v2_active_livestream_session_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get all meetings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_all_meetings(
        &self,
        args: &GetAllMeetingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_all_meetings_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_all_meetings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create meeting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_meeting(
        &self,
        args: &CreateMeetingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_meeting_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_meeting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get meeting.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_meeting(
        &self,
        args: &GetMeetingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_meeting_builder(
            &self.http_client,
            &args.meeting_id,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = get_meeting_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Replace meeting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn replace_meeting(
        &self,
        args: &ReplaceMeetingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = replace_meeting_builder(
            &self.http_client,
            &args.meeting_id,
        )
        .map_err(ProviderError::Api)?;

        let task = replace_meeting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update meeting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_meeting(
        &self,
        args: &UpdateMeetingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_meeting_builder(
            &self.http_client,
            &args.meeting_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_meeting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get v2 meetings meeting id active livestream.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_v2_meetings_meeting_id_active_livestream(
        &self,
        args: &GetV2MeetingsMeetingIdActiveLivestreamArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_v2_meetings_meeting_id_active_livestream_builder(
            &self.http_client,
            &args.meeting_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_v2_meetings_meeting_id_active_livestream_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stop livestreaming.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stop_livestreaming(
        &self,
        args: &StopLivestreamingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stop_livestreaming_builder(
            &self.http_client,
            &args.meeting_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stop_livestreaming_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get active session.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_active_session(
        &self,
        args: &GetActiveSessionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_active_session_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.meeting_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_active_session_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Kick partcipants.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn kick_partcipants(
        &self,
        args: &KickPartcipantsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = kick_partcipants_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
        )
        .map_err(ProviderError::Api)?;

        let task = kick_partcipants_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Kick all participants.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn kick_all_participants(
        &self,
        args: &KickAllParticipantsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = kick_all_participants_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
        )
        .map_err(ProviderError::Api)?;

        let task = kick_all_participants_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mute participants.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn mute_participants(
        &self,
        args: &MuteParticipantsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mute_participants_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
        )
        .map_err(ProviderError::Api)?;

        let task = mute_participants_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Mute all participants.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn mute_all_participants(
        &self,
        args: &MuteAllParticipantsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = mute_all_participants_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
        )
        .map_err(ProviderError::Api)?;

        let task = mute_all_participants_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create poll.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_poll(
        &self,
        args: &CreatePollArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_poll_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_poll_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Livestream session details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn livestream_session_details(
        &self,
        args: &LivestreamSessionDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = livestream_session_details_builder(
            &self.http_client,
            &args.meeting_id,
            &args.page_no,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = livestream_session_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Start livestreaming.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn start_livestreaming(
        &self,
        args: &StartLivestreamingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = start_livestreaming_builder(
            &self.http_client,
            &args.meeting_id,
        )
        .map_err(ProviderError::Api)?;

        let task = start_livestreaming_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get meeting participants.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_meeting_participants(
        &self,
        args: &GetMeetingParticipantsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_meeting_participants_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.meeting_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_meeting_participants_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Add participant.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn add_participant(
        &self,
        args: &AddParticipantArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = add_participant_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.meeting_id,
        )
        .map_err(ProviderError::Api)?;

        let task = add_participant_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get meeting participant.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_meeting_participant(
        &self,
        args: &GetMeetingParticipantArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_meeting_participant_builder(
            &self.http_client,
            &args.meeting_id,
            &args.participant_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_meeting_participant_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Edit participant.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn edit_participant(
        &self,
        args: &EditParticipantArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = edit_participant_builder(
            &self.http_client,
            &args.meeting_id,
            &args.participant_id,
        )
        .map_err(ProviderError::Api)?;

        let task = edit_participant_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete meeting participant.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_meeting_participant(
        &self,
        args: &DeleteMeetingParticipantArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_meeting_participant_builder(
            &self.http_client,
            &args.meeting_id,
            &args.participant_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_meeting_participant_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Regenerate token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn regenerate_token(
        &self,
        args: &RegenerateTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = regenerate_token_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.meeting_id,
            &args.participant_id,
        )
        .map_err(ProviderError::Api)?;

        let task = regenerate_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get presets.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_presets(
        &self,
        args: &GetPresetsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_presets_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_presets_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post presets.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_presets(
        &self,
        args: &PostPresetsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_presets_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_presets_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get presets preset id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_presets_preset_id(
        &self,
        args: &GetPresetsPresetIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_presets_preset_id_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.preset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_presets_preset_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Patch presets preset id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn patch_presets_preset_id(
        &self,
        args: &PatchPresetsPresetIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = patch_presets_preset_id_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.preset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = patch_presets_preset_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete presets preset id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_presets_preset_id(
        &self,
        args: &DeletePresetsPresetIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_presets_preset_id_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.preset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_presets_preset_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get all recordings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_all_recordings(
        &self,
        args: &GetAllRecordingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_all_recordings_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.meeting_id,
            &args.expired,
        )
        .map_err(ProviderError::Api)?;

        let task = get_all_recordings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Start recording.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn start_recording(
        &self,
        args: &StartRecordingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = start_recording_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
        )
        .map_err(ProviderError::Api)?;

        let task = start_recording_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get active recording.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_active_recording(
        &self,
        args: &GetActiveRecordingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_active_recording_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.meeting_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_active_recording_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Start track recording for a meeting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn start_track_recording_for_a_meeting(
        &self,
        args: &StartTrackRecordingForAMeetingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = start_track_recording_for_a_meeting_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
        )
        .map_err(ProviderError::Api)?;

        let task = start_track_recording_for_a_meeting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get one recording.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_one_recording(
        &self,
        args: &GetOneRecordingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_one_recording_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.recording_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_one_recording_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pause resume stop recording.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn pause_resume_stop_recording(
        &self,
        args: &PauseResumeStopRecordingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pause_resume_stop_recording_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.recording_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pause_resume_stop_recording_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get sessions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_sessions(
        &self,
        args: &GetSessionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_sessions_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.search,
            &args.associated_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_sessions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get participant data from peer id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_participant_data_from_peer_id(
        &self,
        args: &GetParticipantDataFromPeerIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_participant_data_from_peer_id_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.peer_id,
            &args.filters,
        )
        .map_err(ProviderError::Api)?;

        let task = get_participant_data_from_peer_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get session details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_session_details(
        &self,
        args: &GetSessionDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_session_details_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.session_id,
            &args.include_breakout_rooms,
        )
        .map_err(ProviderError::Api)?;

        let task = get_session_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get session chat.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_session_chat(
        &self,
        args: &GetSessionChatArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_session_chat_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.session_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_session_chat_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get v2 livestreamsession session meeting id active livestream.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_v2_livestreamsession_session_meeting_id_active_livestream(
        &self,
        args: &GetV2LivestreamsessionSessionMeetingIdActiveLivestreamArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_v2_livestreamsession_session_meeting_id_active_livestream_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.session_id,
            &args.per_page,
            &args.page_no,
        )
        .map_err(ProviderError::Api)?;

        let task = get_v2_livestreamsession_session_meeting_id_active_livestream_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get session participants.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_session_participants(
        &self,
        args: &GetSessionParticipantsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_session_participants_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.session_id,
            &args.include_peer_events,
            &args.view,
        )
        .map_err(ProviderError::Api)?;

        let task = get_session_participants_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get participant details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_participant_details(
        &self,
        args: &GetParticipantDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_participant_details_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.participant_id,
            &args.session_id,
            &args.filters,
            &args.include_peer_events,
        )
        .map_err(ProviderError::Api)?;

        let task = get_participant_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get session summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_session_summary(
        &self,
        args: &GetSessionSummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_session_summary_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.session_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_session_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post sessions session id summary.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_sessions_session_id_summary(
        &self,
        args: &PostSessionsSessionIdSummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_sessions_session_id_summary_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.session_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_sessions_session_id_summary_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get session transcript.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_session_transcript(
        &self,
        args: &GetSessionTranscriptArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_session_transcript_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.session_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_session_transcript_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get all webhooks.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RealtimekitWebhooksListSuccessResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_all_webhooks(
        &self,
        args: &GetAllWebhooksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RealtimekitWebhooksListSuccessResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_all_webhooks_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_all_webhooks_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Add webhook.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RealtimekitWebhookSuccessResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn add_webhook(
        &self,
        args: &AddWebhookArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RealtimekitWebhookSuccessResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = add_webhook_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
        )
        .map_err(ProviderError::Api)?;

        let task = add_webhook_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get webhook.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RealtimekitWebhookSuccessResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_webhook(
        &self,
        args: &GetWebhookArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RealtimekitWebhookSuccessResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_webhook_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.webhook_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_webhook_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Replace webhook.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RealtimekitWebhookSuccessResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn replace_webhook(
        &self,
        args: &ReplaceWebhookArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RealtimekitWebhookSuccessResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = replace_webhook_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.webhook_id,
        )
        .map_err(ProviderError::Api)?;

        let task = replace_webhook_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Edit webhook.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RealtimekitWebhookSuccessResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn edit_webhook(
        &self,
        args: &EditWebhookArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RealtimekitWebhookSuccessResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = edit_webhook_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.webhook_id,
        )
        .map_err(ProviderError::Api)?;

        let task = edit_webhook_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete webhook.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RealtimekitWebhookSuccessResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_webhook(
        &self,
        args: &DeleteWebhookArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RealtimekitWebhookSuccessResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_webhook_builder(
            &self.http_client,
            &args.account_id,
            &args.app_id,
            &args.webhook_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_webhook_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Registrar domains list domains.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RegistrarApiDomainResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn registrar_domains_list_domains(
        &self,
        args: &RegistrarDomainsListDomainsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RegistrarApiDomainResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = registrar_domains_list_domains_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = registrar_domains_list_domains_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Registrar domains get domain.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RegistrarApiDomainResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn registrar_domains_get_domain(
        &self,
        args: &RegistrarDomainsGetDomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RegistrarApiDomainResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = registrar_domains_get_domain_builder(
            &self.http_client,
            &args.domain_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = registrar_domains_get_domain_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Registrar domains update domain.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RegistrarApiDomainResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn registrar_domains_update_domain(
        &self,
        args: &RegistrarDomainsUpdateDomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RegistrarApiDomainResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = registrar_domains_update_domain_builder(
            &self.http_client,
            &args.domain_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = registrar_domains_update_domain_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account request tracer request trace.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_request_tracer_request_trace(
        &self,
        args: &AccountRequestTracerRequestTraceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_request_tracer_request_trace_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_request_tracer_request_trace_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account roles list roles.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamCollectionRoleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_roles_list_roles(
        &self,
        args: &AccountRolesListRolesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamCollectionRoleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_roles_list_roles_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = account_roles_list_roles_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account roles role details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSingleRoleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_roles_role_details(
        &self,
        args: &AccountRolesRoleDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSingleRoleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_roles_role_details_builder(
            &self.http_client,
            &args.role_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_roles_role_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Lists get lists.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListsListsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn lists_get_lists(
        &self,
        args: &ListsGetListsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListsListsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = lists_get_lists_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = lists_get_lists_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Lists create a list.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListsListResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn lists_create_a_list(
        &self,
        args: &ListsCreateAListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListsListResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = lists_create_a_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = lists_create_a_list_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Lists get bulk operation status.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListsBulkOperationResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn lists_get_bulk_operation_status(
        &self,
        args: &ListsGetBulkOperationStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListsBulkOperationResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = lists_get_bulk_operation_status_builder(
            &self.http_client,
            &args.operation_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = lists_get_bulk_operation_status_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Lists get a list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListsListResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn lists_get_a_list(
        &self,
        args: &ListsGetAListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListsListResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = lists_get_a_list_builder(
            &self.http_client,
            &args.list_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = lists_get_a_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Lists update a list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListsListResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn lists_update_a_list(
        &self,
        args: &ListsUpdateAListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListsListResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = lists_update_a_list_builder(
            &self.http_client,
            &args.list_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = lists_update_a_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Lists delete a list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListsListDeleteResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn lists_delete_a_list(
        &self,
        args: &ListsDeleteAListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListsListDeleteResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = lists_delete_a_list_builder(
            &self.http_client,
            &args.list_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = lists_delete_a_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Lists get list items.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListsItemsListResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn lists_get_list_items(
        &self,
        args: &ListsGetListItemsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListsItemsListResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = lists_get_list_items_builder(
            &self.http_client,
            &args.list_id,
            &args.account_id,
            &args.cursor,
            &args.per_page,
            &args.search,
        )
        .map_err(ProviderError::Api)?;

        let task = lists_get_list_items_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Lists create list items.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListsListsAsyncResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn lists_create_list_items(
        &self,
        args: &ListsCreateListItemsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListsListsAsyncResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = lists_create_list_items_builder(
            &self.http_client,
            &args.list_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = lists_create_list_items_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Lists update all list items.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListsListsAsyncResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn lists_update_all_list_items(
        &self,
        args: &ListsUpdateAllListItemsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListsListsAsyncResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = lists_update_all_list_items_builder(
            &self.http_client,
            &args.list_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = lists_update_all_list_items_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Lists delete list items.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListsListsAsyncResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn lists_delete_list_items(
        &self,
        args: &ListsDeleteListItemsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListsListsAsyncResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = lists_delete_list_items_builder(
            &self.http_client,
            &args.list_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = lists_delete_list_items_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Lists get a list item.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListsItemResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn lists_get_a_list_item(
        &self,
        args: &ListsGetAListItemArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListsItemResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = lists_get_a_list_item_builder(
            &self.http_client,
            &args.item_id,
            &args.list_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = lists_get_a_list_item_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List account rulesets.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_account_rulesets(
        &self,
        args: &ListAccountRulesetsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_account_rulesets_builder(
            &self.http_client,
            &args.account_id,
            &args.cursor,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = list_account_rulesets_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create account ruleset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_account_ruleset(
        &self,
        args: &CreateAccountRulesetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_account_ruleset_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_account_ruleset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get account entrypoint ruleset.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_account_entrypoint_ruleset(
        &self,
        args: &GetAccountEntrypointRulesetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_account_entrypoint_ruleset_builder(
            &self.http_client,
            &args.ruleset_phase,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_account_entrypoint_ruleset_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update account entrypoint ruleset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_account_entrypoint_ruleset(
        &self,
        args: &UpdateAccountEntrypointRulesetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_account_entrypoint_ruleset_builder(
            &self.http_client,
            &args.ruleset_phase,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_account_entrypoint_ruleset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List account entrypoint ruleset versions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_account_entrypoint_ruleset_versions(
        &self,
        args: &ListAccountEntrypointRulesetVersionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_account_entrypoint_ruleset_versions_builder(
            &self.http_client,
            &args.ruleset_phase,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = list_account_entrypoint_ruleset_versions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get account entrypoint ruleset version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_account_entrypoint_ruleset_version(
        &self,
        args: &GetAccountEntrypointRulesetVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_account_entrypoint_ruleset_version_builder(
            &self.http_client,
            &args.ruleset_version,
            &args.ruleset_phase,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_account_entrypoint_ruleset_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get account ruleset.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_account_ruleset(
        &self,
        args: &GetAccountRulesetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_account_ruleset_builder(
            &self.http_client,
            &args.ruleset_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_account_ruleset_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update account ruleset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_account_ruleset(
        &self,
        args: &UpdateAccountRulesetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_account_ruleset_builder(
            &self.http_client,
            &args.ruleset_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_account_ruleset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete account ruleset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_account_ruleset(
        &self,
        args: &DeleteAccountRulesetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_account_ruleset_builder(
            &self.http_client,
            &args.ruleset_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_account_ruleset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create account ruleset rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_account_ruleset_rule(
        &self,
        args: &CreateAccountRulesetRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_account_ruleset_rule_builder(
            &self.http_client,
            &args.ruleset_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_account_ruleset_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update account ruleset rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_account_ruleset_rule(
        &self,
        args: &UpdateAccountRulesetRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_account_ruleset_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.ruleset_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_account_ruleset_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete account ruleset rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_account_ruleset_rule(
        &self,
        args: &DeleteAccountRulesetRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_account_ruleset_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.ruleset_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_account_ruleset_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List account ruleset versions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_account_ruleset_versions(
        &self,
        args: &ListAccountRulesetVersionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_account_ruleset_versions_builder(
            &self.http_client,
            &args.ruleset_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = list_account_ruleset_versions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get account ruleset version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_account_ruleset_version(
        &self,
        args: &GetAccountRulesetVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_account_ruleset_version_builder(
            &self.http_client,
            &args.ruleset_version,
            &args.ruleset_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_account_ruleset_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete account ruleset version.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_account_ruleset_version(
        &self,
        args: &DeleteAccountRulesetVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_account_ruleset_version_builder(
            &self.http_client,
            &args.ruleset_version,
            &args.ruleset_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_account_ruleset_version_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List account ruleset version rules by tag.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_account_ruleset_version_rules_by_tag(
        &self,
        args: &ListAccountRulesetVersionRulesByTagArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_account_ruleset_version_rules_by_tag_builder(
            &self.http_client,
            &args.rule_tag,
            &args.ruleset_version,
            &args.ruleset_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = list_account_ruleset_version_rules_by_tag_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web analytics create site.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RumSiteResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn web_analytics_create_site(
        &self,
        args: &WebAnalyticsCreateSiteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RumSiteResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web_analytics_create_site_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web_analytics_create_site_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web analytics list sites.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RumSitesResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn web_analytics_list_sites(
        &self,
        args: &WebAnalyticsListSitesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RumSitesResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web_analytics_list_sites_builder(
            &self.http_client,
            &args.account_id,
            &args.per_page,
            &args.page,
            &args.order_by,
        )
        .map_err(ProviderError::Api)?;

        let task = web_analytics_list_sites_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web analytics get site.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RumSiteResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn web_analytics_get_site(
        &self,
        args: &WebAnalyticsGetSiteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RumSiteResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web_analytics_get_site_builder(
            &self.http_client,
            &args.account_id,
            &args.site_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web_analytics_get_site_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web analytics update site.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RumSiteResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn web_analytics_update_site(
        &self,
        args: &WebAnalyticsUpdateSiteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RumSiteResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web_analytics_update_site_builder(
            &self.http_client,
            &args.account_id,
            &args.site_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web_analytics_update_site_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web analytics delete site.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RumSiteTagResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn web_analytics_delete_site(
        &self,
        args: &WebAnalyticsDeleteSiteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RumSiteTagResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web_analytics_delete_site_builder(
            &self.http_client,
            &args.account_id,
            &args.site_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web_analytics_delete_site_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web analytics create rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RumRuleResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn web_analytics_create_rule(
        &self,
        args: &WebAnalyticsCreateRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RumRuleResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web_analytics_create_rule_builder(
            &self.http_client,
            &args.account_id,
            &args.ruleset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web_analytics_create_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web analytics update rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RumRuleResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn web_analytics_update_rule(
        &self,
        args: &WebAnalyticsUpdateRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RumRuleResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web_analytics_update_rule_builder(
            &self.http_client,
            &args.account_id,
            &args.ruleset_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web_analytics_update_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web analytics delete rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RumRuleIdResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn web_analytics_delete_rule(
        &self,
        args: &WebAnalyticsDeleteRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RumRuleIdResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web_analytics_delete_rule_builder(
            &self.http_client,
            &args.account_id,
            &args.ruleset_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web_analytics_delete_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web analytics list rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RumRulesResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn web_analytics_list_rules(
        &self,
        args: &WebAnalyticsListRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RumRulesResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web_analytics_list_rules_builder(
            &self.http_client,
            &args.account_id,
            &args.ruleset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web_analytics_list_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web analytics modify rules.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RumRulesResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn web_analytics_modify_rules(
        &self,
        args: &WebAnalyticsModifyRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RumRulesResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web_analytics_modify_rules_builder(
            &self.http_client,
            &args.account_id,
            &args.ruleset_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web_analytics_modify_rules_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Scim groups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn scim_groups_list(
        &self,
        args: &ScimGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = scim_groups_list_builder(
            &self.http_client,
            &args.account_id,
            &args.startIndex,
            &args.count,
            &args.filter,
        )
        .map_err(ProviderError::Api)?;

        let task = scim_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Scim groups create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn scim_groups_create(
        &self,
        args: &ScimGroupsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = scim_groups_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = scim_groups_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Scim groups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn scim_groups_get(
        &self,
        args: &ScimGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = scim_groups_get_builder(
            &self.http_client,
            &args.account_id,
            &args.group_id,
        )
        .map_err(ProviderError::Api)?;

        let task = scim_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Scim groups patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn scim_groups_patch(
        &self,
        args: &ScimGroupsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = scim_groups_patch_builder(
            &self.http_client,
            &args.account_id,
            &args.group_id,
        )
        .map_err(ProviderError::Api)?;

        let task = scim_groups_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Scim groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn scim_groups_delete(
        &self,
        args: &ScimGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = scim_groups_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.group_id,
        )
        .map_err(ProviderError::Api)?;

        let task = scim_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Scim resource types list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn scim_resource_types_list(
        &self,
        args: &ScimResourceTypesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = scim_resource_types_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = scim_resource_types_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Scim resource types get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn scim_resource_types_get(
        &self,
        args: &ScimResourceTypesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = scim_resource_types_get_builder(
            &self.http_client,
            &args.account_id,
            &args.resource_type_id,
        )
        .map_err(ProviderError::Api)?;

        let task = scim_resource_types_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Scim schemas list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn scim_schemas_list(
        &self,
        args: &ScimSchemasListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = scim_schemas_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = scim_schemas_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Scim schemas get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn scim_schemas_get(
        &self,
        args: &ScimSchemasGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = scim_schemas_get_builder(
            &self.http_client,
            &args.account_id,
            &args.schema_id,
        )
        .map_err(ProviderError::Api)?;

        let task = scim_schemas_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Scim service provider config get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn scim_service_provider_config_get(
        &self,
        args: &ScimServiceProviderConfigGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = scim_service_provider_config_get_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = scim_service_provider_config_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Scim users list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn scim_users_list(
        &self,
        args: &ScimUsersListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = scim_users_list_builder(
            &self.http_client,
            &args.account_id,
            &args.startIndex,
            &args.count,
            &args.filter,
        )
        .map_err(ProviderError::Api)?;

        let task = scim_users_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Scim users create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn scim_users_create(
        &self,
        args: &ScimUsersCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = scim_users_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = scim_users_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Scim users get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn scim_users_get(
        &self,
        args: &ScimUsersGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = scim_users_get_builder(
            &self.http_client,
            &args.account_id,
            &args.user_id,
        )
        .map_err(ProviderError::Api)?;

        let task = scim_users_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Scim users put.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn scim_users_put(
        &self,
        args: &ScimUsersPutArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = scim_users_put_builder(
            &self.http_client,
            &args.account_id,
            &args.user_id,
        )
        .map_err(ProviderError::Api)?;

        let task = scim_users_put_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Scim users patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn scim_users_patch(
        &self,
        args: &ScimUsersPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = scim_users_patch_builder(
            &self.http_client,
            &args.account_id,
            &args.user_id,
        )
        .map_err(ProviderError::Api)?;

        let task = scim_users_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns acl list ac ls.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn secondary_dns_acl_list_ac_ls(
        &self,
        args: &SecondaryDnsAclListAcLsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_acl_list_ac_ls_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_acl_list_ac_ls_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns acl create acl.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secondary_dns_acl_create_acl(
        &self,
        args: &SecondaryDnsAclCreateAclArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_acl_create_acl_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_acl_create_acl_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns acl acl details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn secondary_dns_acl_acl_details(
        &self,
        args: &SecondaryDnsAclAclDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_acl_acl_details_builder(
            &self.http_client,
            &args.acl_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_acl_acl_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns acl update acl.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secondary_dns_acl_update_acl(
        &self,
        args: &SecondaryDnsAclUpdateAclArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_acl_update_acl_builder(
            &self.http_client,
            &args.acl_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_acl_update_acl_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns acl delete acl.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsComponentsSchemasIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secondary_dns_acl_delete_acl(
        &self,
        args: &SecondaryDnsAclDeleteAclArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsComponentsSchemasIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_acl_delete_acl_builder(
            &self.http_client,
            &args.acl_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_acl_delete_acl_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns peer list peers.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn secondary_dns_peer_list_peers(
        &self,
        args: &SecondaryDnsPeerListPeersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_peer_list_peers_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_peer_list_peers_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns peer create peer.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secondary_dns_peer_create_peer(
        &self,
        args: &SecondaryDnsPeerCreatePeerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_peer_create_peer_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_peer_create_peer_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns peer peer details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn secondary_dns_peer_peer_details(
        &self,
        args: &SecondaryDnsPeerPeerDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_peer_peer_details_builder(
            &self.http_client,
            &args.peer_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_peer_peer_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns peer update peer.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secondary_dns_peer_update_peer(
        &self,
        args: &SecondaryDnsPeerUpdatePeerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_peer_update_peer_builder(
            &self.http_client,
            &args.peer_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_peer_update_peer_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns peer delete peer.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsComponentsSchemasIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secondary_dns_peer_delete_peer(
        &self,
        args: &SecondaryDnsPeerDeletePeerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsComponentsSchemasIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_peer_delete_peer_builder(
            &self.http_client,
            &args.peer_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_peer_delete_peer_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns tsig list tsi gs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn secondary_dns_tsig_list_tsi_gs(
        &self,
        args: &SecondaryDnsTsigListTsiGsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_tsig_list_tsi_gs_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_tsig_list_tsi_gs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns tsig create tsig.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secondary_dns_tsig_create_tsig(
        &self,
        args: &SecondaryDnsTsigCreateTsigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_tsig_create_tsig_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_tsig_create_tsig_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns tsig tsig details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn secondary_dns_tsig_tsig_details(
        &self,
        args: &SecondaryDnsTsigTsigDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_tsig_tsig_details_builder(
            &self.http_client,
            &args.tsig_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_tsig_tsig_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns tsig update tsig.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secondary_dns_tsig_update_tsig(
        &self,
        args: &SecondaryDnsTsigUpdateTsigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_tsig_update_tsig_builder(
            &self.http_client,
            &args.tsig_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_tsig_update_tsig_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns tsig delete tsig.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsSchemasIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secondary_dns_tsig_delete_tsig(
        &self,
        args: &SecondaryDnsTsigDeleteTsigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsSchemasIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_tsig_delete_tsig_builder(
            &self.http_client,
            &args.tsig_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_tsig_delete_tsig_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store quota.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreQuotaResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn secrets_store_quota(
        &self,
        args: &SecretsStoreQuotaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreQuotaResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_quota_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_quota_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreStoresResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn secrets_store_list(
        &self,
        args: &SecretsStoreListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreStoresResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreStoresResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secrets_store_create(
        &self,
        args: &SecretsStoreCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreStoresResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store get store by id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreStoreResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn secrets_store_get_store_by_id(
        &self,
        args: &SecretsStoreGetStoreByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreStoreResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_get_store_by_id_builder(
            &self.http_client,
            &args.account_id,
            &args.store_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_get_store_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store delete by id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreStoreResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secrets_store_delete_by_id(
        &self,
        args: &SecretsStoreDeleteByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreStoreResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_delete_by_id_builder(
            &self.http_client,
            &args.account_id,
            &args.store_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_delete_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store secrets list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreSecretsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn secrets_store_secrets_list(
        &self,
        args: &SecretsStoreSecretsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreSecretsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_secrets_list_builder(
            &self.http_client,
            &args.account_id,
            &args.store_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_secrets_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store secret create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreSecretsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secrets_store_secret_create(
        &self,
        args: &SecretsStoreSecretCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreSecretsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_secret_create_builder(
            &self.http_client,
            &args.account_id,
            &args.store_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_secret_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store delete bulk.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreSecretsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secrets_store_delete_bulk(
        &self,
        args: &SecretsStoreDeleteBulkArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreSecretsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_delete_bulk_builder(
            &self.http_client,
            &args.account_id,
            &args.store_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_delete_bulk_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store get by id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreSecretResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn secrets_store_get_by_id(
        &self,
        args: &SecretsStoreGetByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreSecretResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_get_by_id_builder(
            &self.http_client,
            &args.account_id,
            &args.store_id,
            &args.secret_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_get_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store patch by id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreSecretResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secrets_store_patch_by_id(
        &self,
        args: &SecretsStorePatchByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreSecretResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_patch_by_id_builder(
            &self.http_client,
            &args.account_id,
            &args.store_id,
            &args.secret_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_patch_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store secret delete by id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreSecretResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secrets_store_secret_delete_by_id(
        &self,
        args: &SecretsStoreSecretDeleteByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreSecretResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_secret_delete_by_id_builder(
            &self.http_client,
            &args.account_id,
            &args.store_id,
            &args.secret_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_secret_delete_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store duplicate by id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreSecretResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secrets_store_duplicate_by_id(
        &self,
        args: &SecretsStoreDuplicateByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreSecretResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_duplicate_by_id_builder(
            &self.http_client,
            &args.account_id,
            &args.store_id,
            &args.secret_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_duplicate_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get security center insights.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_security_center_insights(
        &self,
        args: &GetSecurityCenterInsightsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_security_center_insights_builder(
            &self.http_client,
            &args.account_id,
            &args.dismissed,
            &args.issue_class,
            &args.issue_type,
            &args.product,
            &args.severity,
            &args.subject,
            &args.issue_class_neq,
            &args.issue_type_neq,
            &args.product_neq,
            &args.severity_neq,
            &args.subject_neq,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = get_security_center_insights_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get security center insight counts by class.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityCenterValueCountsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_security_center_insight_counts_by_class(
        &self,
        args: &GetSecurityCenterInsightCountsByClassArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityCenterValueCountsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_security_center_insight_counts_by_class_builder(
            &self.http_client,
            &args.account_id,
            &args.dismissed,
            &args.issue_class,
            &args.issue_type,
            &args.product,
            &args.severity,
            &args.subject,
            &args.issue_class_neq,
            &args.issue_type_neq,
            &args.product_neq,
            &args.severity_neq,
            &args.subject_neq,
        )
        .map_err(ProviderError::Api)?;

        let task = get_security_center_insight_counts_by_class_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get security center insight counts by severity.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityCenterValueCountsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_security_center_insight_counts_by_severity(
        &self,
        args: &GetSecurityCenterInsightCountsBySeverityArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityCenterValueCountsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_security_center_insight_counts_by_severity_builder(
            &self.http_client,
            &args.account_id,
            &args.dismissed,
            &args.issue_class,
            &args.issue_type,
            &args.product,
            &args.severity,
            &args.subject,
            &args.issue_class_neq,
            &args.issue_type_neq,
            &args.product_neq,
            &args.severity_neq,
            &args.subject_neq,
        )
        .map_err(ProviderError::Api)?;

        let task = get_security_center_insight_counts_by_severity_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get security center insight counts by type.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityCenterValueCountsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_security_center_insight_counts_by_type(
        &self,
        args: &GetSecurityCenterInsightCountsByTypeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityCenterValueCountsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_security_center_insight_counts_by_type_builder(
            &self.http_client,
            &args.account_id,
            &args.dismissed,
            &args.issue_class,
            &args.issue_type,
            &args.product,
            &args.severity,
            &args.subject,
            &args.issue_class_neq,
            &args.issue_type_neq,
            &args.product_neq,
            &args.severity_neq,
            &args.subject_neq,
        )
        .map_err(ProviderError::Api)?;

        let task = get_security_center_insight_counts_by_type_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get security center insight context.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_security_center_insight_context(
        &self,
        args: &GetSecurityCenterInsightContextArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_security_center_insight_context_builder(
            &self.http_client,
            &args.account_id,
            &args.issue_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_security_center_insight_context_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Archive security center insight.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn archive_security_center_insight(
        &self,
        args: &ArchiveSecurityCenterInsightArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = archive_security_center_insight_builder(
            &self.http_client,
            &args.account_id,
            &args.issue_id,
        )
        .map_err(ProviderError::Api)?;

        let task = archive_security_center_insight_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Shares list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceSharingShareResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn shares_list(
        &self,
        args: &SharesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceSharingShareResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = shares_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = shares_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Share create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceSharingShareResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn share_create(
        &self,
        args: &ShareCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceSharingShareResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = share_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = share_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Shares get by id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceSharingShareResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn shares_get_by_id(
        &self,
        args: &SharesGetByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceSharingShareResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = shares_get_by_id_builder(
            &self.http_client,
            &args.account_id,
            &args.share_id,
        )
        .map_err(ProviderError::Api)?;

        let task = shares_get_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Share update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceSharingShareResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn share_update(
        &self,
        args: &ShareUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceSharingShareResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = share_update_builder(
            &self.http_client,
            &args.account_id,
            &args.share_id,
        )
        .map_err(ProviderError::Api)?;

        let task = share_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Share delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceSharingShareResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn share_delete(
        &self,
        args: &ShareDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceSharingShareResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = share_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.share_id,
        )
        .map_err(ProviderError::Api)?;

        let task = share_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Share recipients list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceSharingShareRecipientResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn share_recipients_list(
        &self,
        args: &ShareRecipientsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceSharingShareRecipientResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = share_recipients_list_builder(
            &self.http_client,
            &args.account_id,
            &args.share_id,
        )
        .map_err(ProviderError::Api)?;

        let task = share_recipients_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Share recipient create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceSharingShareRecipientResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn share_recipient_create(
        &self,
        args: &ShareRecipientCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceSharingShareRecipientResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = share_recipient_create_builder(
            &self.http_client,
            &args.account_id,
            &args.share_id,
        )
        .map_err(ProviderError::Api)?;

        let task = share_recipient_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Share recipients update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn share_recipients_update(
        &self,
        args: &ShareRecipientsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = share_recipients_update_builder(
            &self.http_client,
            &args.account_id,
            &args.share_id,
        )
        .map_err(ProviderError::Api)?;

        let task = share_recipients_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Share recipients get by id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceSharingShareRecipientResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn share_recipients_get_by_id(
        &self,
        args: &ShareRecipientsGetByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceSharingShareRecipientResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = share_recipients_get_by_id_builder(
            &self.http_client,
            &args.account_id,
            &args.share_id,
            &args.recipient_id,
        )
        .map_err(ProviderError::Api)?;

        let task = share_recipients_get_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Share recipient delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceSharingShareRecipientResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn share_recipient_delete(
        &self,
        args: &ShareRecipientDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceSharingShareRecipientResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = share_recipient_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.share_id,
            &args.recipient_id,
        )
        .map_err(ProviderError::Api)?;

        let task = share_recipient_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Share resources list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceSharingShareResourceResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn share_resources_list(
        &self,
        args: &ShareResourcesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceSharingShareResourceResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = share_resources_list_builder(
            &self.http_client,
            &args.account_id,
            &args.share_id,
        )
        .map_err(ProviderError::Api)?;

        let task = share_resources_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Share resource create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceSharingShareResourceResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn share_resource_create(
        &self,
        args: &ShareResourceCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceSharingShareResourceResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = share_resource_create_builder(
            &self.http_client,
            &args.account_id,
            &args.share_id,
        )
        .map_err(ProviderError::Api)?;

        let task = share_resource_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Share resources get by id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceSharingShareResourceResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn share_resources_get_by_id(
        &self,
        args: &ShareResourcesGetByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceSharingShareResourceResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = share_resources_get_by_id_builder(
            &self.http_client,
            &args.account_id,
            &args.share_id,
            &args.resource_id,
        )
        .map_err(ProviderError::Api)?;

        let task = share_resources_get_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Share resource update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceSharingShareResourceResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn share_resource_update(
        &self,
        args: &ShareResourceUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceSharingShareResourceResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = share_resource_update_builder(
            &self.http_client,
            &args.account_id,
            &args.share_id,
            &args.resource_id,
        )
        .map_err(ProviderError::Api)?;

        let task = share_resource_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Share resource delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceSharingShareResourceResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn share_resource_delete(
        &self,
        args: &ShareResourceDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceSharingShareResourceResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = share_resource_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.share_id,
            &args.resource_id,
        )
        .map_err(ProviderError::Api)?;

        let task = share_resource_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Slurper list jobs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn slurper_list_jobs(
        &self,
        args: &SlurperListJobsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = slurper_list_jobs_builder(
            &self.http_client,
            &args.account_id,
            &args.limit,
            &args.offset,
        )
        .map_err(ProviderError::Api)?;

        let task = slurper_list_jobs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Slurper create job.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn slurper_create_job(
        &self,
        args: &SlurperCreateJobArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = slurper_create_job_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = slurper_create_job_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Slurper abort all jobs.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn slurper_abort_all_jobs(
        &self,
        args: &SlurperAbortAllJobsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = slurper_abort_all_jobs_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = slurper_abort_all_jobs_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Slurper get job.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn slurper_get_job(
        &self,
        args: &SlurperGetJobArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = slurper_get_job_builder(
            &self.http_client,
            &args.account_id,
            &args.job_id,
        )
        .map_err(ProviderError::Api)?;

        let task = slurper_get_job_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Slurper abort job.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn slurper_abort_job(
        &self,
        args: &SlurperAbortJobArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = slurper_abort_job_builder(
            &self.http_client,
            &args.account_id,
            &args.job_id,
        )
        .map_err(ProviderError::Api)?;

        let task = slurper_abort_job_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Slurper get job logs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn slurper_get_job_logs(
        &self,
        args: &SlurperGetJobLogsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = slurper_get_job_logs_builder(
            &self.http_client,
            &args.account_id,
            &args.job_id,
            &args.limit,
            &args.offset,
        )
        .map_err(ProviderError::Api)?;

        let task = slurper_get_job_logs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Slurper pause job.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn slurper_pause_job(
        &self,
        args: &SlurperPauseJobArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = slurper_pause_job_builder(
            &self.http_client,
            &args.account_id,
            &args.job_id,
        )
        .map_err(ProviderError::Api)?;

        let task = slurper_pause_job_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Slurper get job progress.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn slurper_get_job_progress(
        &self,
        args: &SlurperGetJobProgressArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = slurper_get_job_progress_builder(
            &self.http_client,
            &args.account_id,
            &args.job_id,
        )
        .map_err(ProviderError::Api)?;

        let task = slurper_get_job_progress_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Slurper resume job.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn slurper_resume_job(
        &self,
        args: &SlurperResumeJobArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = slurper_resume_job_builder(
            &self.http_client,
            &args.account_id,
            &args.job_id,
        )
        .map_err(ProviderError::Api)?;

        let task = slurper_resume_job_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Slurper check source connectivity.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn slurper_check_source_connectivity(
        &self,
        args: &SlurperCheckSourceConnectivityArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = slurper_check_source_connectivity_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = slurper_check_source_connectivity_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Slurper check target connectivity.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn slurper_check_target_connectivity(
        &self,
        args: &SlurperCheckTargetConnectivityArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = slurper_check_target_connectivity_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = slurper_check_target_connectivity_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get all sso connectors.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSsoConnectorCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_all_sso_connectors(
        &self,
        args: &GetAllSsoConnectorsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSsoConnectorCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_all_sso_connectors_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_all_sso_connectors_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Init new sso connector.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSsoConnectorResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn init_new_sso_connector(
        &self,
        args: &InitNewSsoConnectorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSsoConnectorResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = init_new_sso_connector_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = init_new_sso_connector_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get sso connector.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSsoConnectorResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_sso_connector(
        &self,
        args: &GetSsoConnectorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSsoConnectorResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_sso_connector_builder(
            &self.http_client,
            &args.account_id,
            &args.sso_connector_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_sso_connector_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update sso connector state.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSsoConnectorResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_sso_connector_state(
        &self,
        args: &UpdateSsoConnectorStateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSsoConnectorResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_sso_connector_state_builder(
            &self.http_client,
            &args.account_id,
            &args.sso_connector_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_sso_connector_state_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete sso connector.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamApiResponseSingleId result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_sso_connector(
        &self,
        args: &DeleteSsoConnectorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamApiResponseSingleId, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_sso_connector_builder(
            &self.http_client,
            &args.account_id,
            &args.sso_connector_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_sso_connector_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Begin sso connector verification.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamApiResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn begin_sso_connector_verification(
        &self,
        args: &BeginSsoConnectorVerificationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamApiResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = begin_sso_connector_verification_builder(
            &self.http_client,
            &args.account_id,
            &args.sso_connector_id,
        )
        .map_err(ProviderError::Api)?;

        let task = begin_sso_connector_verification_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers kv namespace list namespaces.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn workers_kv_namespace_list_namespaces(
        &self,
        args: &WorkersKvNamespaceListNamespacesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_kv_namespace_list_namespaces_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_kv_namespace_list_namespaces_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers kv namespace create a namespace.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_kv_namespace_create_a_namespace(
        &self,
        args: &WorkersKvNamespaceCreateANamespaceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_kv_namespace_create_a_namespace_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_kv_namespace_create_a_namespace_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers kv namespace get a namespace.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn workers_kv_namespace_get_a_namespace(
        &self,
        args: &WorkersKvNamespaceGetANamespaceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_kv_namespace_get_a_namespace_builder(
            &self.http_client,
            &args.namespace_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_kv_namespace_get_a_namespace_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers kv namespace rename a namespace.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_kv_namespace_rename_a_namespace(
        &self,
        args: &WorkersKvNamespaceRenameANamespaceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_kv_namespace_rename_a_namespace_builder(
            &self.http_client,
            &args.namespace_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_kv_namespace_rename_a_namespace_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers kv namespace remove a namespace.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersKvApiResponseCommonNoResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_kv_namespace_remove_a_namespace(
        &self,
        args: &WorkersKvNamespaceRemoveANamespaceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersKvApiResponseCommonNoResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_kv_namespace_remove_a_namespace_builder(
            &self.http_client,
            &args.namespace_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_kv_namespace_remove_a_namespace_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers kv namespace write multiple key value pairs.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_kv_namespace_write_multiple_key_value_pairs(
        &self,
        args: &WorkersKvNamespaceWriteMultipleKeyValuePairsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_kv_namespace_write_multiple_key_value_pairs_builder(
            &self.http_client,
            &args.namespace_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_kv_namespace_write_multiple_key_value_pairs_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers kv namespace delete multiple key value pairs deprecated.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_kv_namespace_delete_multiple_key_value_pairs_deprecated(
        &self,
        args: &WorkersKvNamespaceDeleteMultipleKeyValuePairsDeprecatedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_kv_namespace_delete_multiple_key_value_pairs_deprecated_builder(
            &self.http_client,
            &args.namespace_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_kv_namespace_delete_multiple_key_value_pairs_deprecated_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers kv namespace delete multiple key value pairs.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_kv_namespace_delete_multiple_key_value_pairs(
        &self,
        args: &WorkersKvNamespaceDeleteMultipleKeyValuePairsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_kv_namespace_delete_multiple_key_value_pairs_builder(
            &self.http_client,
            &args.namespace_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_kv_namespace_delete_multiple_key_value_pairs_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers kv namespace get multiple key value pairs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn workers_kv_namespace_get_multiple_key_value_pairs(
        &self,
        args: &WorkersKvNamespaceGetMultipleKeyValuePairsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_kv_namespace_get_multiple_key_value_pairs_builder(
            &self.http_client,
            &args.namespace_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_kv_namespace_get_multiple_key_value_pairs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers kv namespace list a namespace s keys.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn workers_kv_namespace_list_a_namespace_s_keys(
        &self,
        args: &WorkersKvNamespaceListANamespaceSKeysArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_kv_namespace_list_a_namespace_s_keys_builder(
            &self.http_client,
            &args.namespace_id,
            &args.account_id,
            &args.limit,
            &args.prefix,
            &args.cursor,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_kv_namespace_list_a_namespace_s_keys_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers kv namespace read the metadata for a key.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn workers_kv_namespace_read_the_metadata_for_a_key(
        &self,
        args: &WorkersKvNamespaceReadTheMetadataForAKeyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_kv_namespace_read_the_metadata_for_a_key_builder(
            &self.http_client,
            &args.key_name,
            &args.namespace_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_kv_namespace_read_the_metadata_for_a_key_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers kv namespace read key value pair.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn workers_kv_namespace_read_key_value_pair(
        &self,
        args: &WorkersKvNamespaceReadKeyValuePairArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_kv_namespace_read_key_value_pair_builder(
            &self.http_client,
            &args.key_name,
            &args.namespace_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_kv_namespace_read_key_value_pair_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers kv namespace write key value pair with metadata.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersKvApiResponseCommonNoResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_kv_namespace_write_key_value_pair_with_metadata(
        &self,
        args: &WorkersKvNamespaceWriteKeyValuePairWithMetadataArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersKvApiResponseCommonNoResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_kv_namespace_write_key_value_pair_with_metadata_builder(
            &self.http_client,
            &args.key_name,
            &args.namespace_id,
            &args.account_id,
            &args.expiration,
            &args.expiration_ttl,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_kv_namespace_write_key_value_pair_with_metadata_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers kv namespace delete key value pair.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersKvApiResponseCommonNoResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_kv_namespace_delete_key_value_pair(
        &self,
        args: &WorkersKvNamespaceDeleteKeyValuePairArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersKvApiResponseCommonNoResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_kv_namespace_delete_key_value_pair_builder(
            &self.http_client,
            &args.key_name,
            &args.namespace_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_kv_namespace_delete_key_value_pair_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream videos list videos.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamVideoResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn stream_videos_list_videos(
        &self,
        args: &StreamVideosListVideosArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamVideoResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_videos_list_videos_builder(
            &self.http_client,
            &args.account_id,
            &args.status,
            &args.creator,
            &args.type_rs,
            &args.asc,
            &args.video_name,
            &args.search,
            &args.start,
            &args.end,
            &args.include_counts,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_videos_list_videos_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream videos initiate video uploads using tus.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_videos_initiate_video_uploads_using_tus(
        &self,
        args: &StreamVideosInitiateVideoUploadsUsingTusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_videos_initiate_video_uploads_using_tus_builder(
            &self.http_client,
            &args.account_id,
            &args.direct_user,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_videos_initiate_video_uploads_using_tus_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream video clipping clip videos given a start and end time.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamClipResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_video_clipping_clip_videos_given_a_start_and_end_time(
        &self,
        args: &StreamVideoClippingClipVideosGivenAStartAndEndTimeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamClipResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_video_clipping_clip_videos_given_a_start_and_end_time_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_video_clipping_clip_videos_given_a_start_and_end_time_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream videos upload videos from a url.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamVideoResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_videos_upload_videos_from_a_url(
        &self,
        args: &StreamVideosUploadVideosFromAUrlArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamVideoResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_videos_upload_videos_from_a_url_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_videos_upload_videos_from_a_url_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream videos upload videos via direct upload ur ls.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamDirectUploadResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_videos_upload_videos_via_direct_upload_ur_ls(
        &self,
        args: &StreamVideosUploadVideosViaDirectUploadUrLsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamDirectUploadResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_videos_upload_videos_via_direct_upload_ur_ls_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_videos_upload_videos_via_direct_upload_ur_ls_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream signing keys list signing keys.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamKeyResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn stream_signing_keys_list_signing_keys(
        &self,
        args: &StreamSigningKeysListSigningKeysArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamKeyResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_signing_keys_list_signing_keys_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_signing_keys_list_signing_keys_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream signing keys create signing keys.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamKeyGenerationResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_signing_keys_create_signing_keys(
        &self,
        args: &StreamSigningKeysCreateSigningKeysArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamKeyGenerationResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_signing_keys_create_signing_keys_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_signing_keys_create_signing_keys_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream signing keys delete signing keys.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamDeletedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_signing_keys_delete_signing_keys(
        &self,
        args: &StreamSigningKeysDeleteSigningKeysArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamDeletedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_signing_keys_delete_signing_keys_builder(
            &self.http_client,
            &args.identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_signing_keys_delete_signing_keys_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream live inputs list live inputs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamLiveInputResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn stream_live_inputs_list_live_inputs(
        &self,
        args: &StreamLiveInputsListLiveInputsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamLiveInputResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_live_inputs_list_live_inputs_builder(
            &self.http_client,
            &args.account_id,
            &args.include_counts,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_live_inputs_list_live_inputs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream live inputs create a live input.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamLiveInputResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_live_inputs_create_a_live_input(
        &self,
        args: &StreamLiveInputsCreateALiveInputArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamLiveInputResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_live_inputs_create_a_live_input_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_live_inputs_create_a_live_input_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream live inputs retrieve a live input.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamLiveInputResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn stream_live_inputs_retrieve_a_live_input(
        &self,
        args: &StreamLiveInputsRetrieveALiveInputArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamLiveInputResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_live_inputs_retrieve_a_live_input_builder(
            &self.http_client,
            &args.live_input_identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_live_inputs_retrieve_a_live_input_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream live inputs update a live input.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamLiveInputResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_live_inputs_update_a_live_input(
        &self,
        args: &StreamLiveInputsUpdateALiveInputArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamLiveInputResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_live_inputs_update_a_live_input_builder(
            &self.http_client,
            &args.live_input_identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_live_inputs_update_a_live_input_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream live inputs delete a live input.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_live_inputs_delete_a_live_input(
        &self,
        args: &StreamLiveInputsDeleteALiveInputArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_live_inputs_delete_a_live_input_builder(
            &self.http_client,
            &args.live_input_identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_live_inputs_delete_a_live_input_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream live inputs disable a live input.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamLiveInputResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_live_inputs_disable_a_live_input(
        &self,
        args: &StreamLiveInputsDisableALiveInputArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamLiveInputResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_live_inputs_disable_a_live_input_builder(
            &self.http_client,
            &args.live_input_identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_live_inputs_disable_a_live_input_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream live inputs enable a live input.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamLiveInputResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_live_inputs_enable_a_live_input(
        &self,
        args: &StreamLiveInputsEnableALiveInputArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamLiveInputResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_live_inputs_enable_a_live_input_builder(
            &self.http_client,
            &args.live_input_identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_live_inputs_enable_a_live_input_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream live inputs list all outputs associated with a specified live input.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamOutputResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn stream_live_inputs_list_all_outputs_associated_with_a_specified_live_input(
        &self,
        args: &StreamLiveInputsListAllOutputsAssociatedWithASpecifiedLiveInputArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamOutputResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_live_inputs_list_all_outputs_associated_with_a_specified_live_input_builder(
            &self.http_client,
            &args.live_input_identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_live_inputs_list_all_outputs_associated_with_a_specified_live_input_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream live inputs create a new output connected to a live input.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamOutputResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_live_inputs_create_a_new_output_connected_to_a_live_input(
        &self,
        args: &StreamLiveInputsCreateANewOutputConnectedToALiveInputArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamOutputResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_live_inputs_create_a_new_output_connected_to_a_live_input_builder(
            &self.http_client,
            &args.live_input_identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_live_inputs_create_a_new_output_connected_to_a_live_input_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream live inputs update an output.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamOutputResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_live_inputs_update_an_output(
        &self,
        args: &StreamLiveInputsUpdateAnOutputArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamOutputResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_live_inputs_update_an_output_builder(
            &self.http_client,
            &args.output_identifier,
            &args.live_input_identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_live_inputs_update_an_output_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream live inputs delete an output.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_live_inputs_delete_an_output(
        &self,
        args: &StreamLiveInputsDeleteAnOutputArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_live_inputs_delete_an_output_builder(
            &self.http_client,
            &args.output_identifier,
            &args.live_input_identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_live_inputs_delete_an_output_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream videos storage usage.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamStorageUseResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn stream_videos_storage_usage(
        &self,
        args: &StreamVideosStorageUsageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamStorageUseResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_videos_storage_usage_builder(
            &self.http_client,
            &args.account_id,
            &args.creator,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_videos_storage_usage_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream watermark profile list watermark profiles.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamWatermarkResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn stream_watermark_profile_list_watermark_profiles(
        &self,
        args: &StreamWatermarkProfileListWatermarkProfilesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamWatermarkResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_watermark_profile_list_watermark_profiles_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_watermark_profile_list_watermark_profiles_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream watermark profile create watermark profiles via basic upload.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamWatermarkResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_watermark_profile_create_watermark_profiles_via_basic_upload(
        &self,
        args: &StreamWatermarkProfileCreateWatermarkProfilesViaBasicUploadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamWatermarkResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_watermark_profile_create_watermark_profiles_via_basic_upload_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_watermark_profile_create_watermark_profiles_via_basic_upload_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream watermark profile watermark profile details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamWatermarkResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn stream_watermark_profile_watermark_profile_details(
        &self,
        args: &StreamWatermarkProfileWatermarkProfileDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamWatermarkResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_watermark_profile_watermark_profile_details_builder(
            &self.http_client,
            &args.identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_watermark_profile_watermark_profile_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream watermark profile delete watermark profiles.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_watermark_profile_delete_watermark_profiles(
        &self,
        args: &StreamWatermarkProfileDeleteWatermarkProfilesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_watermark_profile_delete_watermark_profiles_builder(
            &self.http_client,
            &args.identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_watermark_profile_delete_watermark_profiles_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream webhook view webhooks.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamWebhookResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn stream_webhook_view_webhooks(
        &self,
        args: &StreamWebhookViewWebhooksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamWebhookResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_webhook_view_webhooks_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_webhook_view_webhooks_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream webhook create webhooks.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamWebhookResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_webhook_create_webhooks(
        &self,
        args: &StreamWebhookCreateWebhooksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamWebhookResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_webhook_create_webhooks_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_webhook_create_webhooks_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream webhook delete webhooks.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamDeletedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_webhook_delete_webhooks(
        &self,
        args: &StreamWebhookDeleteWebhooksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamDeletedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_webhook_delete_webhooks_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_webhook_delete_webhooks_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream videos retrieve video details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamVideoResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn stream_videos_retrieve_video_details(
        &self,
        args: &StreamVideosRetrieveVideoDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamVideoResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_videos_retrieve_video_details_builder(
            &self.http_client,
            &args.identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_videos_retrieve_video_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream videos update video details.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamVideoResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_videos_update_video_details(
        &self,
        args: &StreamVideosUpdateVideoDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamVideoResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_videos_update_video_details_builder(
            &self.http_client,
            &args.identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_videos_update_video_details_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream videos delete video.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_videos_delete_video(
        &self,
        args: &StreamVideosDeleteVideoArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_videos_delete_video_builder(
            &self.http_client,
            &args.identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_videos_delete_video_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List audio tracks.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamListAudioTrackResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_audio_tracks(
        &self,
        args: &ListAudioTracksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamListAudioTrackResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_audio_tracks_builder(
            &self.http_client,
            &args.account_id,
            &args.identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = list_audio_tracks_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Add audio track.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamAddAudioTrackResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn add_audio_track(
        &self,
        args: &AddAudioTrackArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamAddAudioTrackResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = add_audio_track_builder(
            &self.http_client,
            &args.account_id,
            &args.identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = add_audio_track_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Edit audio tracks.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamAddAudioTrackResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn edit_audio_tracks(
        &self,
        args: &EditAudioTracksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamAddAudioTrackResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = edit_audio_tracks_builder(
            &self.http_client,
            &args.account_id,
            &args.identifier,
            &args.audio_identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = edit_audio_tracks_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete audio tracks.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamDeletedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_audio_tracks(
        &self,
        args: &DeleteAudioTracksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamDeletedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_audio_tracks_builder(
            &self.http_client,
            &args.account_id,
            &args.identifier,
            &args.audio_identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_audio_tracks_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream subtitles captions list captions or subtitles.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamLanguageResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn stream_subtitles_captions_list_captions_or_subtitles(
        &self,
        args: &StreamSubtitlesCaptionsListCaptionsOrSubtitlesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamLanguageResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_subtitles_captions_list_captions_or_subtitles_builder(
            &self.http_client,
            &args.identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_subtitles_captions_list_captions_or_subtitles_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream subtitles captions get caption or subtitle for language.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamLanguageResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn stream_subtitles_captions_get_caption_or_subtitle_for_language(
        &self,
        args: &StreamSubtitlesCaptionsGetCaptionOrSubtitleForLanguageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamLanguageResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_subtitles_captions_get_caption_or_subtitle_for_language_builder(
            &self.http_client,
            &args.language,
            &args.identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_subtitles_captions_get_caption_or_subtitle_for_language_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream subtitles captions upload captions or subtitles.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamLanguageResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_subtitles_captions_upload_captions_or_subtitles(
        &self,
        args: &StreamSubtitlesCaptionsUploadCaptionsOrSubtitlesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamLanguageResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_subtitles_captions_upload_captions_or_subtitles_builder(
            &self.http_client,
            &args.language,
            &args.identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_subtitles_captions_upload_captions_or_subtitles_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream subtitles captions delete captions or subtitles.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_subtitles_captions_delete_captions_or_subtitles(
        &self,
        args: &StreamSubtitlesCaptionsDeleteCaptionsOrSubtitlesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_subtitles_captions_delete_captions_or_subtitles_builder(
            &self.http_client,
            &args.language,
            &args.identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_subtitles_captions_delete_captions_or_subtitles_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream subtitles captions generate caption or subtitle for language.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamLanguageResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_subtitles_captions_generate_caption_or_subtitle_for_language(
        &self,
        args: &StreamSubtitlesCaptionsGenerateCaptionOrSubtitleForLanguageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamLanguageResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_subtitles_captions_generate_caption_or_subtitle_for_language_builder(
            &self.http_client,
            &args.language,
            &args.identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_subtitles_captions_generate_caption_or_subtitle_for_language_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream subtitles captions get vtt caption or subtitle.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn stream_subtitles_captions_get_vtt_caption_or_subtitle(
        &self,
        args: &StreamSubtitlesCaptionsGetVttCaptionOrSubtitleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_subtitles_captions_get_vtt_caption_or_subtitle_builder(
            &self.http_client,
            &args.language,
            &args.identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_subtitles_captions_get_vtt_caption_or_subtitle_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream m p 4 downloads list downloads.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamDownloadsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn stream_m_p_4_downloads_list_downloads(
        &self,
        args: &StreamMP4DownloadsListDownloadsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamDownloadsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_m_p_4_downloads_list_downloads_builder(
            &self.http_client,
            &args.identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_m_p_4_downloads_list_downloads_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream m p 4 downloads create downloads.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamDownloadsResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_m_p_4_downloads_create_downloads(
        &self,
        args: &StreamMP4DownloadsCreateDownloadsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamDownloadsResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_m_p_4_downloads_create_downloads_builder(
            &self.http_client,
            &args.identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_m_p_4_downloads_create_downloads_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream m p 4 downloads delete downloads.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamDeletedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_m_p_4_downloads_delete_downloads(
        &self,
        args: &StreamMP4DownloadsDeleteDownloadsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamDeletedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_m_p_4_downloads_delete_downloads_builder(
            &self.http_client,
            &args.identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_m_p_4_downloads_delete_downloads_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream downloads create type specific downloads.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamDownloadsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_downloads_create_type_specific_downloads(
        &self,
        args: &StreamDownloadsCreateTypeSpecificDownloadsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamDownloadsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_downloads_create_type_specific_downloads_builder(
            &self.http_client,
            &args.identifier,
            &args.account_id,
            &args.download_type,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_downloads_create_type_specific_downloads_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream downloads delete type specific downloads.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamDeletedResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_downloads_delete_type_specific_downloads(
        &self,
        args: &StreamDownloadsDeleteTypeSpecificDownloadsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamDeletedResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_downloads_delete_type_specific_downloads_builder(
            &self.http_client,
            &args.identifier,
            &args.account_id,
            &args.download_type,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_downloads_delete_type_specific_downloads_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream videos retreieve embed code html.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn stream_videos_retreieve_embed_code_html(
        &self,
        args: &StreamVideosRetreieveEmbedCodeHtmlArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_videos_retreieve_embed_code_html_builder(
            &self.http_client,
            &args.identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_videos_retreieve_embed_code_html_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Stream videos create signed url tokens for videos.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StreamSignedTokenResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn stream_videos_create_signed_url_tokens_for_videos(
        &self,
        args: &StreamVideosCreateSignedUrlTokensForVideosArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StreamSignedTokenResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = stream_videos_create_signed_url_tokens_for_videos_builder(
            &self.http_client,
            &args.identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = stream_videos_create_signed_url_tokens_for_videos_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account subscriptions list subscriptions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BillSubsApiAccountSubscriptionResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_subscriptions_list_subscriptions(
        &self,
        args: &AccountSubscriptionsListSubscriptionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BillSubsApiAccountSubscriptionResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_subscriptions_list_subscriptions_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_subscriptions_list_subscriptions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account subscriptions create subscription.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BillSubsApiAccountSubscriptionResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_subscriptions_create_subscription(
        &self,
        args: &AccountSubscriptionsCreateSubscriptionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BillSubsApiAccountSubscriptionResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_subscriptions_create_subscription_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_subscriptions_create_subscription_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account subscriptions update subscription.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BillSubsApiAccountSubscriptionResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_subscriptions_update_subscription(
        &self,
        args: &AccountSubscriptionsUpdateSubscriptionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BillSubsApiAccountSubscriptionResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_subscriptions_update_subscription_builder(
            &self.http_client,
            &args.subscription_identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_subscriptions_update_subscription_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account subscriptions delete subscription.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_subscriptions_delete_subscription(
        &self,
        args: &AccountSubscriptionsDeleteSubscriptionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_subscriptions_delete_subscription_builder(
            &self.http_client,
            &args.subscription_identifier,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_subscriptions_delete_subscription_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tags get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceTaggingTaggedResourceResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn tags_get(
        &self,
        args: &TagsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceTaggingTaggedResourceResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tags_get_builder(
            &self.http_client,
            &args.account_id,
            &args.resource_id,
            &args.resource_type,
            &args.worker_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tags_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tags set.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceTaggingTaggedResourceResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn tags_set(
        &self,
        args: &TagsSetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceTaggingTaggedResourceResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tags_set_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tags_set_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tags delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn tags_delete(
        &self,
        args: &TagsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tags_delete_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tags_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tags list keys.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceTaggingTagKeysResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn tags_list_keys(
        &self,
        args: &TagsListKeysArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceTaggingTagKeysResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tags_list_keys_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tags_list_keys_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tags list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceTaggingTaggedResourceResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn tags_list(
        &self,
        args: &TagsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceTaggingTaggedResourceResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tags_list_builder(
            &self.http_client,
            &args.account_id,
            &args.type_rs,
        )
        .map_err(ProviderError::Api)?;

        let task = tags_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tags list values.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceTaggingTagValuesResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn tags_list_values(
        &self,
        args: &TagsListValuesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceTaggingTagValuesResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tags_list_values_builder(
            &self.http_client,
            &args.account_id,
            &args.tag_key,
        )
        .map_err(ProviderError::Api)?;

        let task = tags_list_values_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tunnel route list tunnel routes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelTeamnetResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn tunnel_route_list_tunnel_routes(
        &self,
        args: &TunnelRouteListTunnelRoutesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelTeamnetResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tunnel_route_list_tunnel_routes_builder(
            &self.http_client,
            &args.account_id,
            &args.comment,
            &args.is_deleted,
            &args.network_subset,
            &args.network_superset,
            &args.existed_at,
            &args.tunnel_id,
            &args.route_id,
            &args.tun_types,
            &args.virtual_network_id,
            &args.per_page,
            &args.page,
        )
        .map_err(ProviderError::Api)?;

        let task = tunnel_route_list_tunnel_routes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tunnel route create a tunnel route.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelRouteResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn tunnel_route_create_a_tunnel_route(
        &self,
        args: &TunnelRouteCreateATunnelRouteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelRouteResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tunnel_route_create_a_tunnel_route_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tunnel_route_create_a_tunnel_route_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tunnel route get tunnel route by ip.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelTeamnetResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn tunnel_route_get_tunnel_route_by_ip(
        &self,
        args: &TunnelRouteGetTunnelRouteByIpArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelTeamnetResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tunnel_route_get_tunnel_route_by_ip_builder(
            &self.http_client,
            &args.ip,
            &args.account_id,
            &args.virtual_network_id,
            &args.default_virtual_network_fallback,
        )
        .map_err(ProviderError::Api)?;

        let task = tunnel_route_get_tunnel_route_by_ip_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tunnel route create a tunnel route with cidr.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelRouteResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn tunnel_route_create_a_tunnel_route_with_cidr(
        &self,
        args: &TunnelRouteCreateATunnelRouteWithCidrArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelRouteResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tunnel_route_create_a_tunnel_route_with_cidr_builder(
            &self.http_client,
            &args.ip_network_encoded,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tunnel_route_create_a_tunnel_route_with_cidr_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tunnel route update a tunnel route with cidr.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelRouteResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn tunnel_route_update_a_tunnel_route_with_cidr(
        &self,
        args: &TunnelRouteUpdateATunnelRouteWithCidrArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelRouteResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tunnel_route_update_a_tunnel_route_with_cidr_builder(
            &self.http_client,
            &args.ip_network_encoded,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tunnel_route_update_a_tunnel_route_with_cidr_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tunnel route delete a tunnel route with cidr.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelRouteResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn tunnel_route_delete_a_tunnel_route_with_cidr(
        &self,
        args: &TunnelRouteDeleteATunnelRouteWithCidrArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelRouteResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tunnel_route_delete_a_tunnel_route_with_cidr_builder(
            &self.http_client,
            &args.ip_network_encoded,
            &args.account_id,
            &args.virtual_network_id,
            &args.tun_type,
            &args.tunnel_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tunnel_route_delete_a_tunnel_route_with_cidr_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tunnel route get tunnel route.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelRouteResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn tunnel_route_get_tunnel_route(
        &self,
        args: &TunnelRouteGetTunnelRouteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelRouteResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tunnel_route_get_tunnel_route_builder(
            &self.http_client,
            &args.account_id,
            &args.route_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tunnel_route_get_tunnel_route_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tunnel route update a tunnel route.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelRouteResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn tunnel_route_update_a_tunnel_route(
        &self,
        args: &TunnelRouteUpdateATunnelRouteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelRouteResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tunnel_route_update_a_tunnel_route_builder(
            &self.http_client,
            &args.route_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tunnel_route_update_a_tunnel_route_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tunnel route delete a tunnel route.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelRouteResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn tunnel_route_delete_a_tunnel_route(
        &self,
        args: &TunnelRouteDeleteATunnelRouteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelRouteResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tunnel_route_delete_a_tunnel_route_builder(
            &self.http_client,
            &args.route_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tunnel_route_delete_a_tunnel_route_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tunnel virtual network list virtual networks.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelVnetResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn tunnel_virtual_network_list_virtual_networks(
        &self,
        args: &TunnelVirtualNetworkListVirtualNetworksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelVnetResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tunnel_virtual_network_list_virtual_networks_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
            &args.name,
            &args.is_default,
            &args.is_default_network,
            &args.is_deleted,
        )
        .map_err(ProviderError::Api)?;

        let task = tunnel_virtual_network_list_virtual_networks_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tunnel virtual network create a virtual network.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelVnetResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn tunnel_virtual_network_create_a_virtual_network(
        &self,
        args: &TunnelVirtualNetworkCreateAVirtualNetworkArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelVnetResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tunnel_virtual_network_create_a_virtual_network_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tunnel_virtual_network_create_a_virtual_network_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tunnel virtual network get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelVnetResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn tunnel_virtual_network_get(
        &self,
        args: &TunnelVirtualNetworkGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelVnetResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tunnel_virtual_network_get_builder(
            &self.http_client,
            &args.account_id,
            &args.virtual_network_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tunnel_virtual_network_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tunnel virtual network update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelVnetResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn tunnel_virtual_network_update(
        &self,
        args: &TunnelVirtualNetworkUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelVnetResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tunnel_virtual_network_update_builder(
            &self.http_client,
            &args.account_id,
            &args.virtual_network_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tunnel_virtual_network_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tunnel virtual network delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelVnetResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn tunnel_virtual_network_delete(
        &self,
        args: &TunnelVirtualNetworkDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelVnetResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tunnel_virtual_network_delete_builder(
            &self.http_client,
            &args.virtual_network_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tunnel_virtual_network_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account api tokens list tokens.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamCollectionTokensResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_api_tokens_list_tokens(
        &self,
        args: &AccountApiTokensListTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamCollectionTokensResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_api_tokens_list_tokens_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = account_api_tokens_list_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account api tokens create token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSingleTokenCreateResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_api_tokens_create_token(
        &self,
        args: &AccountApiTokensCreateTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSingleTokenCreateResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_api_tokens_create_token_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_api_tokens_create_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account api tokens list permission groups.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamPermissionsGroupResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_api_tokens_list_permission_groups(
        &self,
        args: &AccountApiTokensListPermissionGroupsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamPermissionsGroupResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_api_tokens_list_permission_groups_builder(
            &self.http_client,
            &args.account_id,
            &args.name,
            &args.scope,
        )
        .map_err(ProviderError::Api)?;

        let task = account_api_tokens_list_permission_groups_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account api tokens verify token.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamTokenVerifyResponseSingleSegment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_api_tokens_verify_token(
        &self,
        args: &AccountApiTokensVerifyTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamTokenVerifyResponseSingleSegment, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_api_tokens_verify_token_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_api_tokens_verify_token_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account api tokens token details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSingleTokenResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_api_tokens_token_details(
        &self,
        args: &AccountApiTokensTokenDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSingleTokenResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_api_tokens_token_details_builder(
            &self.http_client,
            &args.account_id,
            &args.token_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_api_tokens_token_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account api tokens update token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSingleTokenResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_api_tokens_update_token(
        &self,
        args: &AccountApiTokensUpdateTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSingleTokenResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_api_tokens_update_token_builder(
            &self.http_client,
            &args.account_id,
            &args.token_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_api_tokens_update_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account api tokens delete token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamApiResponseSingleId result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_api_tokens_delete_token(
        &self,
        args: &AccountApiTokensDeleteTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamApiResponseSingleId, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_api_tokens_delete_token_builder(
            &self.http_client,
            &args.account_id,
            &args.token_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_api_tokens_delete_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account api tokens roll token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamResponseSingleValue result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_api_tokens_roll_token(
        &self,
        args: &AccountApiTokensRollTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamResponseSingleValue, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_api_tokens_roll_token_builder(
            &self.http_client,
            &args.account_id,
            &args.token_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_api_tokens_roll_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel list all tunnels.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelTunnelResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_tunnel_list_all_tunnels(
        &self,
        args: &CloudflareTunnelListAllTunnelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelTunnelResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_list_all_tunnels_builder(
            &self.http_client,
            &args.account_id,
            &args.name,
            &args.is_deleted,
            &args.existed_at,
            &args.uuid,
            &args.was_active_at,
            &args.was_inactive_at,
            &args.include_prefix,
            &args.exclude_prefix,
            &args.tun_types,
            &args.status,
            &args.per_page,
            &args.page,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_list_all_tunnels_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Urlscanner get response text.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn urlscanner_get_response_text(
        &self,
        args: &UrlscannerGetResponseTextArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = urlscanner_get_response_text_builder(
            &self.http_client,
            &args.response_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = urlscanner_get_response_text_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Urlscanner search scans.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn urlscanner_search_scans(
        &self,
        args: &UrlscannerSearchScansArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = urlscanner_search_scans_builder(
            &self.http_client,
            &args.account_id,
            &args.scan_id,
            &args.limit,
            &args.next_cursor,
            &args.date_start,
            &args.date_end,
            &args.url,
            &args.hostname,
            &args.path,
            &args.ip,
            &args.hash,
            &args.page_url,
            &args.page_hostname,
            &args.page_path,
            &args.page_asn,
            &args.page_ip,
            &args.account_scans,
            &args.is_malicious,
        )
        .map_err(ProviderError::Api)?;

        let task = urlscanner_search_scans_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Urlscanner create scan.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn urlscanner_create_scan(
        &self,
        args: &UrlscannerCreateScanArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = urlscanner_create_scan_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = urlscanner_create_scan_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Urlscanner get scan.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn urlscanner_get_scan(
        &self,
        args: &UrlscannerGetScanArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = urlscanner_get_scan_builder(
            &self.http_client,
            &args.scan_id,
            &args.account_id,
            &args.full,
        )
        .map_err(ProviderError::Api)?;

        let task = urlscanner_get_scan_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Urlscanner get scan har.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn urlscanner_get_scan_har(
        &self,
        args: &UrlscannerGetScanHarArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = urlscanner_get_scan_har_builder(
            &self.http_client,
            &args.scan_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = urlscanner_get_scan_har_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Urlscanner get scan screenshot.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn urlscanner_get_scan_screenshot(
        &self,
        args: &UrlscannerGetScanScreenshotArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = urlscanner_get_scan_screenshot_builder(
            &self.http_client,
            &args.scan_id,
            &args.account_id,
            &args.resolution,
        )
        .map_err(ProviderError::Api)?;

        let task = urlscanner_get_scan_screenshot_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Urlscanner create scan bulk v2.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn urlscanner_create_scan_bulk_v2(
        &self,
        args: &UrlscannerCreateScanBulkV2Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = urlscanner_create_scan_bulk_v2_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = urlscanner_create_scan_bulk_v2_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Urlscanner get scan dom v2.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn urlscanner_get_scan_dom_v2(
        &self,
        args: &UrlscannerGetScanDomV2Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = urlscanner_get_scan_dom_v2_builder(
            &self.http_client,
            &args.scan_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = urlscanner_get_scan_dom_v2_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Urlscanner get scan har v2.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn urlscanner_get_scan_har_v2(
        &self,
        args: &UrlscannerGetScanHarV2Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = urlscanner_get_scan_har_v2_builder(
            &self.http_client,
            &args.scan_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = urlscanner_get_scan_har_v2_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Urlscanner get response v2.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn urlscanner_get_response_v2(
        &self,
        args: &UrlscannerGetResponseV2Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = urlscanner_get_response_v2_builder(
            &self.http_client,
            &args.response_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = urlscanner_get_response_v2_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Urlscanner get scan v2.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn urlscanner_get_scan_v2(
        &self,
        args: &UrlscannerGetScanV2Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = urlscanner_get_scan_v2_builder(
            &self.http_client,
            &args.scan_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = urlscanner_get_scan_v2_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Urlscanner create scan v2.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn urlscanner_create_scan_v2(
        &self,
        args: &UrlscannerCreateScanV2Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = urlscanner_create_scan_v2_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = urlscanner_create_scan_v2_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Urlscanner get scan screenshot v2.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn urlscanner_get_scan_screenshot_v2(
        &self,
        args: &UrlscannerGetScanScreenshotV2Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = urlscanner_get_scan_screenshot_v2_builder(
            &self.http_client,
            &args.scan_id,
            &args.account_id,
            &args.resolution,
        )
        .map_err(ProviderError::Api)?;

        let task = urlscanner_get_scan_screenshot_v2_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Urlscanner search scans v2.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn urlscanner_search_scans_v2(
        &self,
        args: &UrlscannerSearchScansV2Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = urlscanner_search_scans_v2_builder(
            &self.http_client,
            &args.account_id,
            &args.size,
            &args.q,
        )
        .map_err(ProviderError::Api)?;

        let task = urlscanner_search_scans_v2_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize deprecated list vectorize indexes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn vectorize_deprecated_list_vectorize_indexes(
        &self,
        args: &VectorizeDeprecatedListVectorizeIndexesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_deprecated_list_vectorize_indexes_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_deprecated_list_vectorize_indexes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize deprecated create vectorize index.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn vectorize_deprecated_create_vectorize_index(
        &self,
        args: &VectorizeDeprecatedCreateVectorizeIndexArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_deprecated_create_vectorize_index_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_deprecated_create_vectorize_index_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize deprecated get vectorize index.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn vectorize_deprecated_get_vectorize_index(
        &self,
        args: &VectorizeDeprecatedGetVectorizeIndexArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_deprecated_get_vectorize_index_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_deprecated_get_vectorize_index_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize deprecated update vectorize index.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn vectorize_deprecated_update_vectorize_index(
        &self,
        args: &VectorizeDeprecatedUpdateVectorizeIndexArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_deprecated_update_vectorize_index_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_deprecated_update_vectorize_index_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize deprecated delete vectorize index.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn vectorize_deprecated_delete_vectorize_index(
        &self,
        args: &VectorizeDeprecatedDeleteVectorizeIndexArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_deprecated_delete_vectorize_index_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_deprecated_delete_vectorize_index_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize deprecated delete vectors by id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn vectorize_deprecated_delete_vectors_by_id(
        &self,
        args: &VectorizeDeprecatedDeleteVectorsByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_deprecated_delete_vectors_by_id_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_deprecated_delete_vectors_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize deprecated get vectors by id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn vectorize_deprecated_get_vectors_by_id(
        &self,
        args: &VectorizeDeprecatedGetVectorsByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_deprecated_get_vectors_by_id_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_deprecated_get_vectors_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize deprecated insert vector.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn vectorize_deprecated_insert_vector(
        &self,
        args: &VectorizeDeprecatedInsertVectorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_deprecated_insert_vector_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_deprecated_insert_vector_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize deprecated query vector.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn vectorize_deprecated_query_vector(
        &self,
        args: &VectorizeDeprecatedQueryVectorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_deprecated_query_vector_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_deprecated_query_vector_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize deprecated upsert vector.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn vectorize_deprecated_upsert_vector(
        &self,
        args: &VectorizeDeprecatedUpsertVectorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_deprecated_upsert_vector_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_deprecated_upsert_vector_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize list vectorize indexes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn vectorize_list_vectorize_indexes(
        &self,
        args: &VectorizeListVectorizeIndexesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_list_vectorize_indexes_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_list_vectorize_indexes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize create vectorize index.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn vectorize_create_vectorize_index(
        &self,
        args: &VectorizeCreateVectorizeIndexArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_create_vectorize_index_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_create_vectorize_index_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize get vectorize index.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn vectorize_get_vectorize_index(
        &self,
        args: &VectorizeGetVectorizeIndexArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_get_vectorize_index_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_get_vectorize_index_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize delete vectorize index.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn vectorize_delete_vectorize_index(
        &self,
        args: &VectorizeDeleteVectorizeIndexArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_delete_vectorize_index_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_delete_vectorize_index_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize delete vectors by id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn vectorize_delete_vectors_by_id(
        &self,
        args: &VectorizeDeleteVectorsByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_delete_vectors_by_id_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_delete_vectors_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize get vectors by id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn vectorize_get_vectors_by_id(
        &self,
        args: &VectorizeGetVectorsByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_get_vectors_by_id_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_get_vectors_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize index info.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn vectorize_index_info(
        &self,
        args: &VectorizeIndexInfoArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_index_info_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_index_info_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize insert vector.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn vectorize_insert_vector(
        &self,
        args: &VectorizeInsertVectorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_insert_vector_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
            &args.unparsable_behavior,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_insert_vector_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize list vectors.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn vectorize_list_vectors(
        &self,
        args: &VectorizeListVectorsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_list_vectors_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
            &args.count,
            &args.cursor,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_list_vectors_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize create metadata index.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn vectorize_create_metadata_index(
        &self,
        args: &VectorizeCreateMetadataIndexArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_create_metadata_index_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_create_metadata_index_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize delete metadata index.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn vectorize_delete_metadata_index(
        &self,
        args: &VectorizeDeleteMetadataIndexArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_delete_metadata_index_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_delete_metadata_index_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize list metadata indexes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn vectorize_list_metadata_indexes(
        &self,
        args: &VectorizeListMetadataIndexesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_list_metadata_indexes_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_list_metadata_indexes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize query vector.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn vectorize_query_vector(
        &self,
        args: &VectorizeQueryVectorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_query_vector_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_query_vector_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Vectorize upsert vector.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn vectorize_upsert_vector(
        &self,
        args: &VectorizeUpsertVectorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = vectorize_upsert_vector_builder(
            &self.http_client,
            &args.account_id,
            &args.index_name,
            &args.unparsable_behavior,
        )
        .map_err(ProviderError::Api)?;

        let task = vectorize_upsert_vector_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room list waiting rooms account.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waiting_room_list_waiting_rooms_account(
        &self,
        args: &WaitingRoomListWaitingRoomsAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_list_waiting_rooms_account_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_list_waiting_rooms_account_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel list warp connector tunnels.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelWarpConnectorResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_tunnel_list_warp_connector_tunnels(
        &self,
        args: &CloudflareTunnelListWarpConnectorTunnelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelWarpConnectorResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_list_warp_connector_tunnels_builder(
            &self.http_client,
            &args.account_id,
            &args.name,
            &args.is_deleted,
            &args.existed_at,
            &args.uuid,
            &args.was_active_at,
            &args.was_inactive_at,
            &args.include_prefix,
            &args.exclude_prefix,
            &args.status,
            &args.per_page,
            &args.page,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_list_warp_connector_tunnels_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel create a warp connector tunnel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelWarpConnectorResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudflare_tunnel_create_a_warp_connector_tunnel(
        &self,
        args: &CloudflareTunnelCreateAWarpConnectorTunnelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelWarpConnectorResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_create_a_warp_connector_tunnel_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_create_a_warp_connector_tunnel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel get a warp connector tunnel.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelWarpConnectorResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_tunnel_get_a_warp_connector_tunnel(
        &self,
        args: &CloudflareTunnelGetAWarpConnectorTunnelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelWarpConnectorResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_get_a_warp_connector_tunnel_builder(
            &self.http_client,
            &args.account_id,
            &args.tunnel_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_get_a_warp_connector_tunnel_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel update a warp connector tunnel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelWarpConnectorResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudflare_tunnel_update_a_warp_connector_tunnel(
        &self,
        args: &CloudflareTunnelUpdateAWarpConnectorTunnelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelWarpConnectorResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_update_a_warp_connector_tunnel_builder(
            &self.http_client,
            &args.account_id,
            &args.tunnel_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_update_a_warp_connector_tunnel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel delete a warp connector tunnel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelWarpConnectorResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudflare_tunnel_delete_a_warp_connector_tunnel(
        &self,
        args: &CloudflareTunnelDeleteAWarpConnectorTunnelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelWarpConnectorResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_delete_a_warp_connector_tunnel_builder(
            &self.http_client,
            &args.account_id,
            &args.tunnel_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_delete_a_warp_connector_tunnel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel list warp connector tunnel connections.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelTunnelWarpConnectorConnectionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_tunnel_list_warp_connector_tunnel_connections(
        &self,
        args: &CloudflareTunnelListWarpConnectorTunnelConnectionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelTunnelWarpConnectorConnectionsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_list_warp_connector_tunnel_connections_builder(
            &self.http_client,
            &args.account_id,
            &args.tunnel_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_list_warp_connector_tunnel_connections_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel get warp connector tunnel connector.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelTunnelWarpConnectorClientResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_tunnel_get_warp_connector_tunnel_connector(
        &self,
        args: &CloudflareTunnelGetWarpConnectorTunnelConnectorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelTunnelWarpConnectorClientResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_get_warp_connector_tunnel_connector_builder(
            &self.http_client,
            &args.account_id,
            &args.tunnel_id,
            &args.connector_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_get_warp_connector_tunnel_connector_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel manual failover warp connector tunnel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelEmptyResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn cloudflare_tunnel_manual_failover_warp_connector_tunnel(
        &self,
        args: &CloudflareTunnelManualFailoverWarpConnectorTunnelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelEmptyResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_manual_failover_warp_connector_tunnel_builder(
            &self.http_client,
            &args.account_id,
            &args.tunnel_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_manual_failover_warp_connector_tunnel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare tunnel get a warp connector tunnel token.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelTunnelResponseToken result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_tunnel_get_a_warp_connector_tunnel_token(
        &self,
        args: &CloudflareTunnelGetAWarpConnectorTunnelTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelTunnelResponseToken, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_tunnel_get_a_warp_connector_tunnel_token_builder(
            &self.http_client,
            &args.account_id,
            &args.tunnel_id,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_tunnel_get_a_warp_connector_tunnel_token_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker account settings fetch worker account settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_account_settings_fetch_worker_account_settings(
        &self,
        args: &WorkerAccountSettingsFetchWorkerAccountSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_account_settings_fetch_worker_account_settings_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_account_settings_fetch_worker_account_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker account settings create worker account settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_account_settings_create_worker_account_settings(
        &self,
        args: &WorkerAccountSettingsCreateWorkerAccountSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_account_settings_create_worker_account_settings_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_account_settings_create_worker_account_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker assets upload.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersCompletedUploadAssetsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_assets_upload(
        &self,
        args: &WorkerAssetsUploadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersCompletedUploadAssetsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_assets_upload_builder(
            &self.http_client,
            &args.account_id,
            &args.base64,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_assets_upload_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersNamespaceListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn namespace_worker_list(
        &self,
        args: &NamespaceWorkerListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersNamespaceListResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersNamespaceSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn namespace_worker_create(
        &self,
        args: &NamespaceWorkerCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersNamespaceSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker get namespace.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersNamespaceSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn namespace_worker_get_namespace(
        &self,
        args: &NamespaceWorkerGetNamespaceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersNamespaceSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_get_namespace_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_get_namespace_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker put namespace.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersNamespaceSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn namespace_worker_put_namespace(
        &self,
        args: &NamespaceWorkerPutNamespaceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersNamespaceSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_put_namespace_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_put_namespace_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker patch namespace.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersNamespaceSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn namespace_worker_patch_namespace(
        &self,
        args: &NamespaceWorkerPatchNamespaceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersNamespaceSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_patch_namespace_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_patch_namespace_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker delete namespace.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersApiResponseNullResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn namespace_worker_delete_namespace(
        &self,
        args: &NamespaceWorkerDeleteNamespaceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersApiResponseNullResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_delete_namespace_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_delete_namespace_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker list scripts.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn namespace_worker_list_scripts(
        &self,
        args: &NamespaceWorkerListScriptsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_list_scripts_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_list_scripts_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker delete scripts.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersNamespaceScriptDeleteBulkResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn namespace_worker_delete_scripts(
        &self,
        args: &NamespaceWorkerDeleteScriptsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersNamespaceScriptDeleteBulkResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_delete_scripts_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
            &args.tags,
            &args.limit,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_delete_scripts_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker script worker details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersNamespaceScriptResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn namespace_worker_script_worker_details(
        &self,
        args: &NamespaceWorkerScriptWorkerDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersNamespaceScriptResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_script_worker_details_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_script_worker_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker script upload worker module.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn namespace_worker_script_upload_worker_module(
        &self,
        args: &NamespaceWorkerScriptUploadWorkerModuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_script_upload_worker_module_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
            &args.script_name,
            &args.bindings_inherit,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_script_upload_worker_module_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker script delete worker.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersApiResponseNullResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn namespace_worker_script_delete_worker(
        &self,
        args: &NamespaceWorkerScriptDeleteWorkerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersApiResponseNullResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_script_delete_worker_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
            &args.script_name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_script_delete_worker_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker script update create assets upload session.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersCreateAssetsUploadSessionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn namespace_worker_script_update_create_assets_upload_session(
        &self,
        args: &NamespaceWorkerScriptUpdateCreateAssetsUploadSessionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersCreateAssetsUploadSessionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_script_update_create_assets_upload_session_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_script_update_create_assets_upload_session_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker get script bindings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn namespace_worker_get_script_bindings(
        &self,
        args: &NamespaceWorkerGetScriptBindingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_get_script_bindings_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_get_script_bindings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker get script content.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn namespace_worker_get_script_content(
        &self,
        args: &NamespaceWorkerGetScriptContentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_get_script_content_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_get_script_content_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker put script content.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersScriptResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn namespace_worker_put_script_content(
        &self,
        args: &NamespaceWorkerPutScriptContentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersScriptResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_put_script_content_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_put_script_content_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker list script secrets.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn namespace_worker_list_script_secrets(
        &self,
        args: &NamespaceWorkerListScriptSecretsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_list_script_secrets_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_list_script_secrets_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker put script secrets.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn namespace_worker_put_script_secrets(
        &self,
        args: &NamespaceWorkerPutScriptSecretsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_put_script_secrets_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_put_script_secrets_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker get script secrets.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn namespace_worker_get_script_secrets(
        &self,
        args: &NamespaceWorkerGetScriptSecretsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_get_script_secrets_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
            &args.script_name,
            &args.secret_name,
            &args.url_encoded,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_get_script_secrets_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker delete script secret.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersApiResponseNullResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn namespace_worker_delete_script_secret(
        &self,
        args: &NamespaceWorkerDeleteScriptSecretArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersApiResponseNullResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_delete_script_secret_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
            &args.script_name,
            &args.secret_name,
            &args.url_encoded,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_delete_script_secret_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker get script settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn namespace_worker_get_script_settings(
        &self,
        args: &NamespaceWorkerGetScriptSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_get_script_settings_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_get_script_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker patch script settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn namespace_worker_patch_script_settings(
        &self,
        args: &NamespaceWorkerPatchScriptSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_patch_script_settings_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_patch_script_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker get script tags.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn namespace_worker_get_script_tags(
        &self,
        args: &NamespaceWorkerGetScriptTagsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_get_script_tags_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_get_script_tags_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker put script tags.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn namespace_worker_put_script_tags(
        &self,
        args: &NamespaceWorkerPutScriptTagsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_put_script_tags_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_put_script_tags_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker put script tag.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersApiResponseNullResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn namespace_worker_put_script_tag(
        &self,
        args: &NamespaceWorkerPutScriptTagArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersApiResponseNullResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_put_script_tag_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
            &args.script_name,
            &args.tag,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_put_script_tag_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Namespace worker delete script tag.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersApiResponseNullResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn namespace_worker_delete_script_tag(
        &self,
        args: &NamespaceWorkerDeleteScriptTagArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersApiResponseNullResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = namespace_worker_delete_script_tag_builder(
            &self.http_client,
            &args.account_id,
            &args.dispatch_namespace,
            &args.script_name,
            &args.tag,
        )
        .map_err(ProviderError::Api)?;

        let task = namespace_worker_delete_script_tag_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers domains list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn workers_domains_list(
        &self,
        args: &WorkersDomainsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_domains_list_builder(
            &self.http_client,
            &args.account_id,
            &args.zone_id,
            &args.zone_name,
            &args.service,
            &args.hostname,
            &args.environment,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_domains_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers domains update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_domains_update(
        &self,
        args: &WorkersDomainsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_domains_update_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_domains_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers domains get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn workers_domains_get(
        &self,
        args: &WorkersDomainsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_domains_get_builder(
            &self.http_client,
            &args.account_id,
            &args.domain_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_domains_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Workers domains delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn workers_domains_delete(
        &self,
        args: &WorkersDomainsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = workers_domains_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.domain_id,
        )
        .map_err(ProviderError::Api)?;

        let task = workers_domains_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Durable objects namespace list namespaces.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn durable_objects_namespace_list_namespaces(
        &self,
        args: &DurableObjectsNamespaceListNamespacesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = durable_objects_namespace_list_namespaces_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = durable_objects_namespace_list_namespaces_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Durable objects namespace list objects.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn durable_objects_namespace_list_objects(
        &self,
        args: &DurableObjectsNamespaceListObjectsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = durable_objects_namespace_list_objects_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
            &args.limit,
            &args.cursor,
        )
        .map_err(ProviderError::Api)?;

        let task = durable_objects_namespace_list_objects_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Destination list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn destination_list(
        &self,
        args: &DestinationListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = destination_list_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.perPage,
            &args.order,
            &args.orderBy,
        )
        .map_err(ProviderError::Api)?;

        let task = destination_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Destination create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn destination_create(
        &self,
        args: &DestinationCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = destination_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = destination_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Destination update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn destination_update(
        &self,
        args: &DestinationUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = destination_update_builder(
            &self.http_client,
            &args.slug,
        )
        .map_err(ProviderError::Api)?;

        let task = destination_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Destinations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn destinations_delete(
        &self,
        args: &DestinationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = destinations_delete_builder(
            &self.http_client,
            &args.slug,
        )
        .map_err(ProviderError::Api)?;

        let task = destinations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queries list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn queries_list(
        &self,
        args: &QueriesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queries_list_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.perPage,
            &args.order,
            &args.orderBy,
        )
        .map_err(ProviderError::Api)?;

        let task = queries_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queries post.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn queries_post(
        &self,
        args: &QueriesPostArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queries_post_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = queries_post_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queries get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn queries_get(
        &self,
        args: &QueriesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queries_get_builder(
            &self.http_client,
            &args.queryId,
        )
        .map_err(ProviderError::Api)?;

        let task = queries_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queries patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn queries_patch(
        &self,
        args: &QueriesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queries_patch_builder(
            &self.http_client,
            &args.queryId,
        )
        .map_err(ProviderError::Api)?;

        let task = queries_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Queries delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn queries_delete(
        &self,
        args: &QueriesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = queries_delete_builder(
            &self.http_client,
            &args.queryId,
        )
        .map_err(ProviderError::Api)?;

        let task = queries_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Telemetry keys list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn telemetry_keys_list(
        &self,
        args: &TelemetryKeysListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = telemetry_keys_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = telemetry_keys_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Telemetry query.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn telemetry_query(
        &self,
        args: &TelemetryQueryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = telemetry_query_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = telemetry_query_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Telemetry values list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn telemetry_values_list(
        &self,
        args: &TelemetryValuesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = telemetry_values_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = telemetry_values_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker placement list regions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_placement_list_regions(
        &self,
        args: &WorkerPlacementListRegionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_placement_list_regions_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_placement_list_regions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker script list workers.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersScriptResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_script_list_workers(
        &self,
        args: &WorkerScriptListWorkersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersScriptResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_script_list_workers_builder(
            &self.http_client,
            &args.account_id,
            &args.tags,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_script_list_workers_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker script search workers.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_script_search_workers(
        &self,
        args: &WorkerScriptSearchWorkersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_script_search_workers_builder(
            &self.http_client,
            &args.account_id,
            &args.name,
            &args.id,
            &args.order_by,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_script_search_workers_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker script download worker.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_script_download_worker(
        &self,
        args: &WorkerScriptDownloadWorkerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_script_download_worker_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_script_download_worker_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker script upload worker module.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_script_upload_worker_module(
        &self,
        args: &WorkerScriptUploadWorkerModuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_script_upload_worker_module_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
            &args.bindings_inherit,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_script_upload_worker_module_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker script delete worker.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersApiResponseNullResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_script_delete_worker(
        &self,
        args: &WorkerScriptDeleteWorkerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersApiResponseNullResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_script_delete_worker_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_script_delete_worker_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker script update create assets upload session.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersCreateAssetsUploadSessionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_script_update_create_assets_upload_session(
        &self,
        args: &WorkerScriptUpdateCreateAssetsUploadSessionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersCreateAssetsUploadSessionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_script_update_create_assets_upload_session_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_script_update_create_assets_upload_session_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker script put content.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersScriptResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_script_put_content(
        &self,
        args: &WorkerScriptPutContentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersScriptResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_script_put_content_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_script_put_content_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker script get content.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_script_get_content(
        &self,
        args: &WorkerScriptGetContentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_script_get_content_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_script_get_content_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker deployments list deployments.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_deployments_list_deployments(
        &self,
        args: &WorkerDeploymentsListDeploymentsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_deployments_list_deployments_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_deployments_list_deployments_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker deployments create deployment.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_deployments_create_deployment(
        &self,
        args: &WorkerDeploymentsCreateDeploymentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_deployments_create_deployment_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_deployments_create_deployment_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker deployments get deployment.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_deployments_get_deployment(
        &self,
        args: &WorkerDeploymentsGetDeploymentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_deployments_get_deployment_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
            &args.deployment_id,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_deployments_get_deployment_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker deployments delete deployment.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_deployments_delete_deployment(
        &self,
        args: &WorkerDeploymentsDeleteDeploymentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_deployments_delete_deployment_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
            &args.deployment_id,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_deployments_delete_deployment_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker cron trigger get cron triggers.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_cron_trigger_get_cron_triggers(
        &self,
        args: &WorkerCronTriggerGetCronTriggersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_cron_trigger_get_cron_triggers_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_cron_trigger_get_cron_triggers_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker cron trigger update cron triggers.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_cron_trigger_update_cron_triggers(
        &self,
        args: &WorkerCronTriggerUpdateCronTriggersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_cron_trigger_update_cron_triggers_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_cron_trigger_update_cron_triggers_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker script settings get settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersScriptSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_script_settings_get_settings(
        &self,
        args: &WorkerScriptSettingsGetSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersScriptSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_script_settings_get_settings_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_script_settings_get_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker script settings patch settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersScriptSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_script_settings_patch_settings(
        &self,
        args: &WorkerScriptSettingsPatchSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersScriptSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_script_settings_patch_settings_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_script_settings_patch_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker list script secrets.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_list_script_secrets(
        &self,
        args: &WorkerListScriptSecretsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_list_script_secrets_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_list_script_secrets_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker put script secret.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_put_script_secret(
        &self,
        args: &WorkerPutScriptSecretArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_put_script_secret_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_put_script_secret_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker get script secret.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_get_script_secret(
        &self,
        args: &WorkerGetScriptSecretArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_get_script_secret_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
            &args.secret_name,
            &args.url_encoded,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_get_script_secret_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker delete script secret.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersApiResponseNullResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_delete_script_secret(
        &self,
        args: &WorkerDeleteScriptSecretArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersApiResponseNullResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_delete_script_secret_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
            &args.secret_name,
            &args.url_encoded,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_delete_script_secret_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker script get settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersScriptAndVersionSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_script_get_settings(
        &self,
        args: &WorkerScriptGetSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersScriptAndVersionSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_script_get_settings_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_script_get_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker script patch settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersScriptAndVersionSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_script_patch_settings(
        &self,
        args: &WorkerScriptPatchSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersScriptAndVersionSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_script_patch_settings_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_script_patch_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker script get subdomain.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_script_get_subdomain(
        &self,
        args: &WorkerScriptGetSubdomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_script_get_subdomain_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_script_get_subdomain_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker script post subdomain.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_script_post_subdomain(
        &self,
        args: &WorkerScriptPostSubdomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_script_post_subdomain_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_script_post_subdomain_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker script delete subdomain.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_script_delete_subdomain(
        &self,
        args: &WorkerScriptDeleteSubdomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_script_delete_subdomain_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_script_delete_subdomain_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker tail logs start tail.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_tail_logs_start_tail(
        &self,
        args: &WorkerTailLogsStartTailArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_tail_logs_start_tail_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_tail_logs_start_tail_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker tail logs delete tail.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_tail_logs_delete_tail(
        &self,
        args: &WorkerTailLogsDeleteTailArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_tail_logs_delete_tail_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_tail_logs_delete_tail_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker script fetch usage model.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersUsageModelResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_script_fetch_usage_model(
        &self,
        args: &WorkerScriptFetchUsageModelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersUsageModelResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_script_fetch_usage_model_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_script_fetch_usage_model_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker script update usage model.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersUsageModelResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_script_update_usage_model(
        &self,
        args: &WorkerScriptUpdateUsageModelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersUsageModelResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_script_update_usage_model_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_script_update_usage_model_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker versions list versions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersVersionsListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_versions_list_versions(
        &self,
        args: &WorkerVersionsListVersionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersVersionsListResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_versions_list_versions_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
            &args.deployable,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_versions_list_versions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker versions upload version.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersVersionsUploadResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_versions_upload_version(
        &self,
        args: &WorkerVersionsUploadVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersVersionsUploadResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_versions_upload_version_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
            &args.bindings_inherit,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_versions_upload_version_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker versions get version detail.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersVersionsSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_versions_get_version_detail(
        &self,
        args: &WorkerVersionsGetVersionDetailArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersVersionsSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_versions_get_version_detail_builder(
            &self.http_client,
            &args.account_id,
            &args.script_name,
            &args.version_id,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_versions_get_version_detail_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker environment get script content.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_environment_get_script_content(
        &self,
        args: &WorkerEnvironmentGetScriptContentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_environment_get_script_content_builder(
            &self.http_client,
            &args.account_id,
            &args.service_name,
            &args.environment_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_environment_get_script_content_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker environment put script content.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersScriptResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_environment_put_script_content(
        &self,
        args: &WorkerEnvironmentPutScriptContentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersScriptResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_environment_put_script_content_builder(
            &self.http_client,
            &args.account_id,
            &args.service_name,
            &args.environment_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_environment_put_script_content_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker script environment get settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersScriptSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_script_environment_get_settings(
        &self,
        args: &WorkerScriptEnvironmentGetSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersScriptSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_script_environment_get_settings_builder(
            &self.http_client,
            &args.account_id,
            &args.service_name,
            &args.environment_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_script_environment_get_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker script environment patch settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersScriptSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_script_environment_patch_settings(
        &self,
        args: &WorkerScriptEnvironmentPatchSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersScriptSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_script_environment_patch_settings_builder(
            &self.http_client,
            &args.account_id,
            &args.service_name,
            &args.environment_name,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_script_environment_patch_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker subdomain get subdomain.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_subdomain_get_subdomain(
        &self,
        args: &WorkerSubdomainGetSubdomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_subdomain_get_subdomain_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_subdomain_get_subdomain_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker subdomain create subdomain.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_subdomain_create_subdomain(
        &self,
        args: &WorkerSubdomainCreateSubdomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_subdomain_create_subdomain_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_subdomain_create_subdomain_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker subdomain delete subdomain.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_subdomain_delete_subdomain(
        &self,
        args: &WorkerSubdomainDeleteSubdomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_subdomain_delete_subdomain_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_subdomain_delete_subdomain_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List workers.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_workers(
        &self,
        args: &ListWorkersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_workers_builder(
            &self.http_client,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.order_by,
            &args.order,
        )
        .map_err(ProviderError::Api)?;

        let task = list_workers_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create worker.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_worker(
        &self,
        args: &CreateWorkerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_worker_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_worker_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get worker.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_worker(
        &self,
        args: &GetWorkerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_worker_builder(
            &self.http_client,
            &args.account_id,
            &args.worker_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_worker_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update worker.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_worker(
        &self,
        args: &UpdateWorkerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_worker_builder(
            &self.http_client,
            &args.account_id,
            &args.worker_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_worker_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Edit worker.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn edit_worker(
        &self,
        args: &EditWorkerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = edit_worker_builder(
            &self.http_client,
            &args.account_id,
            &args.worker_id,
        )
        .map_err(ProviderError::Api)?;

        let task = edit_worker_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete worker.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_worker(
        &self,
        args: &DeleteWorkerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_worker_builder(
            &self.http_client,
            &args.account_id,
            &args.worker_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_worker_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List worker versions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_worker_versions(
        &self,
        args: &ListWorkerVersionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_worker_versions_builder(
            &self.http_client,
            &args.account_id,
            &args.worker_id,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = list_worker_versions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create worker version.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_worker_version(
        &self,
        args: &CreateWorkerVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_worker_version_builder(
            &self.http_client,
            &args.account_id,
            &args.worker_id,
            &args.deploy,
        )
        .map_err(ProviderError::Api)?;

        let task = create_worker_version_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get worker version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_worker_version(
        &self,
        args: &GetWorkerVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_worker_version_builder(
            &self.http_client,
            &args.account_id,
            &args.worker_id,
            &args.version_id,
            &args.include,
        )
        .map_err(ProviderError::Api)?;

        let task = get_worker_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete worker version.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WorkersApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_worker_version(
        &self,
        args: &DeleteWorkerVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WorkersApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_worker_version_builder(
            &self.http_client,
            &args.account_id,
            &args.worker_id,
            &args.version_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_worker_version_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Wor list workflows.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn wor_list_workflows(
        &self,
        args: &WorListWorkflowsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = wor_list_workflows_builder(
            &self.http_client,
            &args.account_id,
            &args.per_page,
            &args.page,
            &args.search,
        )
        .map_err(ProviderError::Api)?;

        let task = wor_list_workflows_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Wor get workflow details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn wor_get_workflow_details(
        &self,
        args: &WorGetWorkflowDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = wor_get_workflow_details_builder(
            &self.http_client,
            &args.workflow_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = wor_get_workflow_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Wor create or modify workflow.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn wor_create_or_modify_workflow(
        &self,
        args: &WorCreateOrModifyWorkflowArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = wor_create_or_modify_workflow_builder(
            &self.http_client,
            &args.workflow_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = wor_create_or_modify_workflow_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Wor delete workflow.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn wor_delete_workflow(
        &self,
        args: &WorDeleteWorkflowArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = wor_delete_workflow_builder(
            &self.http_client,
            &args.workflow_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = wor_delete_workflow_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Wor list workflow instances.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn wor_list_workflow_instances(
        &self,
        args: &WorListWorkflowInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = wor_list_workflow_instances_builder(
            &self.http_client,
            &args.workflow_name,
            &args.account_id,
            &args.page,
            &args.per_page,
            &args.cursor,
            &args.direction,
            &args.status,
            &args.date_start,
            &args.date_end,
        )
        .map_err(ProviderError::Api)?;

        let task = wor_list_workflow_instances_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Wor create new workflow instance.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn wor_create_new_workflow_instance(
        &self,
        args: &WorCreateNewWorkflowInstanceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = wor_create_new_workflow_instance_builder(
            &self.http_client,
            &args.workflow_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = wor_create_new_workflow_instance_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Wor batch create workflow instance.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn wor_batch_create_workflow_instance(
        &self,
        args: &WorBatchCreateWorkflowInstanceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = wor_batch_create_workflow_instance_builder(
            &self.http_client,
            &args.workflow_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = wor_batch_create_workflow_instance_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Wor batch terminate workflow instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn wor_batch_terminate_workflow_instances(
        &self,
        args: &WorBatchTerminateWorkflowInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = wor_batch_terminate_workflow_instances_builder(
            &self.http_client,
            &args.workflow_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = wor_batch_terminate_workflow_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Wor status terminate workflow instances.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn wor_status_terminate_workflow_instances(
        &self,
        args: &WorStatusTerminateWorkflowInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = wor_status_terminate_workflow_instances_builder(
            &self.http_client,
            &args.workflow_name,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = wor_status_terminate_workflow_instances_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Wor describe workflow instance.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn wor_describe_workflow_instance(
        &self,
        args: &WorDescribeWorkflowInstanceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = wor_describe_workflow_instance_builder(
            &self.http_client,
            &args.workflow_name,
            &args.instance_id,
            &args.account_id,
            &args.simple,
            &args.order,
        )
        .map_err(ProviderError::Api)?;

        let task = wor_describe_workflow_instance_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Wor send event workflow instance.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn wor_send_event_workflow_instance(
        &self,
        args: &WorSendEventWorkflowInstanceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = wor_send_event_workflow_instance_builder(
            &self.http_client,
            &args.workflow_name,
            &args.instance_id,
            &args.event_type,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = wor_send_event_workflow_instance_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Wor change status workflow instance.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn wor_change_status_workflow_instance(
        &self,
        args: &WorChangeStatusWorkflowInstanceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = wor_change_status_workflow_instance_builder(
            &self.http_client,
            &args.workflow_name,
            &args.instance_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = wor_change_status_workflow_instance_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Wor list workflow versions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn wor_list_workflow_versions(
        &self,
        args: &WorListWorkflowVersionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = wor_list_workflow_versions_builder(
            &self.http_client,
            &args.workflow_name,
            &args.account_id,
            &args.per_page,
            &args.page,
        )
        .map_err(ProviderError::Api)?;

        let task = wor_list_workflow_versions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Wor describe workflow versions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn wor_describe_workflow_versions(
        &self,
        args: &WorDescribeWorkflowVersionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = wor_describe_workflow_versions_builder(
            &self.http_client,
            &args.workflow_name,
            &args.version_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = wor_describe_workflow_versions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Wor describe workflow versions dag.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn wor_describe_workflow_versions_dag(
        &self,
        args: &WorDescribeWorkflowVersionsDagArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = wor_describe_workflow_versions_dag_builder(
            &self.http_client,
            &args.workflow_name,
            &args.version_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = wor_describe_workflow_versions_dag_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Wor describe workflow versions graph.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn wor_describe_workflow_versions_graph(
        &self,
        args: &WorDescribeWorkflowVersionsGraphArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = wor_describe_workflow_versions_graph_builder(
            &self.http_client,
            &args.workflow_name,
            &args.version_id,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = wor_describe_workflow_versions_graph_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust accounts get connectivity settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelZeroTrustConnectivitySettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_accounts_get_connectivity_settings(
        &self,
        args: &ZeroTrustAccountsGetConnectivitySettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelZeroTrustConnectivitySettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_accounts_get_connectivity_settings_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_accounts_get_connectivity_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust accounts patch connectivity settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelZeroTrustConnectivitySettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_accounts_patch_connectivity_settings(
        &self,
        args: &ZeroTrustAccountsPatchConnectivitySettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelZeroTrustConnectivitySettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_accounts_patch_connectivity_settings_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_accounts_patch_connectivity_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust networks route hostname list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelHostnameRouteResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_networks_route_hostname_list(
        &self,
        args: &ZeroTrustNetworksRouteHostnameListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelHostnameRouteResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_networks_route_hostname_list_builder(
            &self.http_client,
            &args.account_id,
            &args.id,
            &args.hostname,
            &args.tunnel_id,
            &args.comment,
            &args.existed_at,
            &args.is_deleted,
            &args.per_page,
            &args.page,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_networks_route_hostname_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust networks route hostname create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelHostnameRouteResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_networks_route_hostname_create(
        &self,
        args: &ZeroTrustNetworksRouteHostnameCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelHostnameRouteResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_networks_route_hostname_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_networks_route_hostname_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust networks route hostname get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelHostnameRouteResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_networks_route_hostname_get(
        &self,
        args: &ZeroTrustNetworksRouteHostnameGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelHostnameRouteResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_networks_route_hostname_get_builder(
            &self.http_client,
            &args.account_id,
            &args.hostname_route_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_networks_route_hostname_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust networks route hostname update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelHostnameRouteResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_networks_route_hostname_update(
        &self,
        args: &ZeroTrustNetworksRouteHostnameUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelHostnameRouteResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_networks_route_hostname_update_builder(
            &self.http_client,
            &args.account_id,
            &args.hostname_route_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_networks_route_hostname_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust networks route hostname delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelHostnameRouteResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_networks_route_hostname_delete(
        &self,
        args: &ZeroTrustNetworksRouteHostnameDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelHostnameRouteResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_networks_route_hostname_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.hostname_route_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_networks_route_hostname_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust networks subnets list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelSubnetResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_networks_subnets_list(
        &self,
        args: &ZeroTrustNetworksSubnetsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelSubnetResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_networks_subnets_list_builder(
            &self.http_client,
            &args.account_id,
            &args.name,
            &args.comment,
            &args.network,
            &args.existed_at,
            &args.address_family,
            &args.is_default_network,
            &args.is_deleted,
            &args.sort_order,
            &args.subnet_types,
            &args.per_page,
            &args.page,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_networks_subnets_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust networks subnet update cloudflare source.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelSubnetResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_networks_subnet_update_cloudflare_source(
        &self,
        args: &ZeroTrustNetworksSubnetUpdateCloudflareSourceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelSubnetResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_networks_subnet_update_cloudflare_source_builder(
            &self.http_client,
            &args.account_id,
            &args.address_family,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_networks_subnet_update_cloudflare_source_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust networks subnet create warp.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelSubnetResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_networks_subnet_create_warp(
        &self,
        args: &ZeroTrustNetworksSubnetCreateWarpArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelSubnetResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_networks_subnet_create_warp_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_networks_subnet_create_warp_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust networks subnet get warp.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelSubnetResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zero_trust_networks_subnet_get_warp(
        &self,
        args: &ZeroTrustNetworksSubnetGetWarpArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelSubnetResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_networks_subnet_get_warp_builder(
            &self.http_client,
            &args.account_id,
            &args.subnet_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_networks_subnet_get_warp_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust networks subnet update warp.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelSubnetResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_networks_subnet_update_warp(
        &self,
        args: &ZeroTrustNetworksSubnetUpdateWarpArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelSubnetResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_networks_subnet_update_warp_builder(
            &self.http_client,
            &args.account_id,
            &args.subnet_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_networks_subnet_update_warp_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zero trust networks subnet delete warp.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TunnelSubnetResponseSingleNullable result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zero_trust_networks_subnet_delete_warp(
        &self,
        args: &ZeroTrustNetworksSubnetDeleteWarpArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TunnelSubnetResponseSingleNullable, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zero_trust_networks_subnet_delete_warp_builder(
            &self.http_client,
            &args.account_id,
            &args.subnet_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zero_trust_networks_subnet_delete_warp_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp risk score behaviors get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_risk_score_behaviors_get(
        &self,
        args: &DlpRiskScoreBehaviorsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_risk_score_behaviors_get_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_risk_score_behaviors_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp risk score behaviors put.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_risk_score_behaviors_put(
        &self,
        args: &DlpRiskScoreBehaviorsPutArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_risk_score_behaviors_put_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_risk_score_behaviors_put_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp zt risk score integration list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_zt_risk_score_integration_list(
        &self,
        args: &DlpZtRiskScoreIntegrationListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_zt_risk_score_integration_list_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_zt_risk_score_integration_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp zt risk score integration create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_zt_risk_score_integration_create(
        &self,
        args: &DlpZtRiskScoreIntegrationCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_zt_risk_score_integration_create_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_zt_risk_score_integration_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp zt risk score integration get by reference id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_zt_risk_score_integration_get_by_reference_id(
        &self,
        args: &DlpZtRiskScoreIntegrationGetByReferenceIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_zt_risk_score_integration_get_by_reference_id_builder(
            &self.http_client,
            &args.account_id,
            &args.reference_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_zt_risk_score_integration_get_by_reference_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp zt risk score integration get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_zt_risk_score_integration_get(
        &self,
        args: &DlpZtRiskScoreIntegrationGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_zt_risk_score_integration_get_builder(
            &self.http_client,
            &args.account_id,
            &args.integration_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_zt_risk_score_integration_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp zt risk score integration update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_zt_risk_score_integration_update(
        &self,
        args: &DlpZtRiskScoreIntegrationUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_zt_risk_score_integration_update_builder(
            &self.http_client,
            &args.account_id,
            &args.integration_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_zt_risk_score_integration_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp zt risk score integration delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_zt_risk_score_integration_delete(
        &self,
        args: &DlpZtRiskScoreIntegrationDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_zt_risk_score_integration_delete_builder(
            &self.http_client,
            &args.account_id,
            &args.integration_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_zt_risk_score_integration_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp risk score summary get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_risk_score_summary_get(
        &self,
        args: &DlpRiskScoreSummaryGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_risk_score_summary_get_builder(
            &self.http_client,
            &args.account_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_risk_score_summary_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp risk score summary get for user.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dlp_risk_score_summary_get_for_user(
        &self,
        args: &DlpRiskScoreSummaryGetForUserArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_risk_score_summary_get_for_user_builder(
            &self.http_client,
            &args.account_id,
            &args.user_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_risk_score_summary_get_for_user_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dlp risk score reset post.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dlp_risk_score_reset_post(
        &self,
        args: &DlpRiskScoreResetPostArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dlp_risk_score_reset_post_builder(
            &self.http_client,
            &args.account_id,
            &args.user_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dlp_risk_score_reset_post_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Origin ca list certificates.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesSchemasCertificateResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn origin_ca_list_certificates(
        &self,
        args: &OriginCaListCertificatesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesSchemasCertificateResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = origin_ca_list_certificates_builder(
            &self.http_client,
            &args.zone_id,
            &args.page,
            &args.per_page,
            &args.limit,
            &args.offset,
        )
        .map_err(ProviderError::Api)?;

        let task = origin_ca_list_certificates_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Origin ca create certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesSchemasCertificateResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn origin_ca_create_certificate(
        &self,
        args: &OriginCaCreateCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesSchemasCertificateResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = origin_ca_create_certificate_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = origin_ca_create_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Origin ca get certificate.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesSchemasCertificateResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn origin_ca_get_certificate(
        &self,
        args: &OriginCaGetCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesSchemasCertificateResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = origin_ca_get_certificate_builder(
            &self.http_client,
            &args.certificate_id,
        )
        .map_err(ProviderError::Api)?;

        let task = origin_ca_get_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Origin ca revoke certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCertificateRevokeResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn origin_ca_revoke_certificate(
        &self,
        args: &OriginCaRevokeCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCertificateRevokeResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = origin_ca_revoke_certificate_builder(
            &self.http_client,
            &args.certificate_id,
        )
        .map_err(ProviderError::Api)?;

        let task = origin_ca_revoke_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Cloudflare ips cloudflare ip details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn cloudflare_ips_cloudflare_ip_details(
        &self,
        args: &CloudflareIpsCloudflareIpDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = cloudflare_ips_cloudflare_ip_details_builder(
            &self.http_client,
            &args.networks,
        )
        .map_err(ProviderError::Api)?;

        let task = cloudflare_ips_cloudflare_ip_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User s account memberships list memberships.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn user_s_account_memberships_list_memberships(
        &self,
        args: &UserSAccountMembershipsListMembershipsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_s_account_memberships_list_memberships_builder(
            &self.http_client,
            &args.account_name,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
            &args.name,
            &args.status,
        )
        .map_err(ProviderError::Api)?;

        let task = user_s_account_memberships_list_memberships_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User s account memberships membership details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSingleMembershipResponseWithPolicies result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn user_s_account_memberships_membership_details(
        &self,
        args: &UserSAccountMembershipsMembershipDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSingleMembershipResponseWithPolicies, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_s_account_memberships_membership_details_builder(
            &self.http_client,
            &args.membership_id,
        )
        .map_err(ProviderError::Api)?;

        let task = user_s_account_memberships_membership_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User s account memberships update membership.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSingleMembershipResponseWithPolicies result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn user_s_account_memberships_update_membership(
        &self,
        args: &UserSAccountMembershipsUpdateMembershipArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSingleMembershipResponseWithPolicies, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_s_account_memberships_update_membership_builder(
            &self.http_client,
            &args.membership_id,
        )
        .map_err(ProviderError::Api)?;

        let task = user_s_account_memberships_update_membership_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User s account memberships delete membership.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn user_s_account_memberships_delete_membership(
        &self,
        args: &UserSAccountMembershipsDeleteMembershipArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_s_account_memberships_delete_membership_builder(
            &self.http_client,
            &args.membership_id,
        )
        .map_err(ProviderError::Api)?;

        let task = user_s_account_memberships_delete_membership_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Organization list organizations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn organization_list_organizations(
        &self,
        args: &OrganizationListOrganizationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = organization_list_organizations_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = organization_list_organizations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Organizations create user organization.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn organizations_create_user_organization(
        &self,
        args: &OrganizationsCreateUserOrganizationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = organizations_create_user_organization_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = organizations_create_user_organization_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Organizations retrieve.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn organizations_retrieve(
        &self,
        args: &OrganizationsRetrieveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = organizations_retrieve_builder(
            &self.http_client,
            &args.organization_id,
        )
        .map_err(ProviderError::Api)?;

        let task = organizations_retrieve_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Organizations modify.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn organizations_modify(
        &self,
        args: &OrganizationsModifyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = organizations_modify_builder(
            &self.http_client,
            &args.organization_id,
        )
        .map_err(ProviderError::Api)?;

        let task = organizations_modify_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Organizations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn organizations_delete(
        &self,
        args: &OrganizationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = organizations_delete_builder(
            &self.http_client,
            &args.organization_id,
        )
        .map_err(ProviderError::Api)?;

        let task = organizations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Organizations get accounts.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn organizations_get_accounts(
        &self,
        args: &OrganizationsGetAccountsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = organizations_get_accounts_builder(
            &self.http_client,
            &args.organization_id,
            &args.account_pubname,
            &args.account_pubname_startsWith,
            &args.account_pubname_endsWith,
            &args.account_pubname_contains,
            &args.name,
            &args.name_startsWith,
            &args.name_endsWith,
            &args.name_contains,
            &args.order_by,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = organizations_get_accounts_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Audit logs v2 get organization audit logs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaAuditLogsV2OrgResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn audit_logs_v2_get_organization_audit_logs(
        &self,
        args: &AuditLogsV2GetOrganizationAuditLogsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaAuditLogsV2OrgResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = audit_logs_v2_get_organization_audit_logs_builder(
            &self.http_client,
            &args.organization_id,
            &args.action_result,
            &args.action_type,
            &args.actor_context,
            &args.actor_email,
            &args.actor_id,
            &args.actor_ip_address,
            &args.actor_token_id,
            &args.actor_token_name,
            &args.actor_type,
            &args.id,
            &args.raw_cf_ray_id,
            &args.raw_method,
            &args.raw_status_code,
            &args.raw_uri,
            &args.resource_id,
            &args.resource_product,
            &args.resource_type,
            &args.resource_scope,
            &args.action_result_not,
            &args.action_type_not,
            &args.actor_context_not,
            &args.actor_email_not,
            &args.actor_id_not,
            &args.actor_ip_address_not,
            &args.actor_token_id_not,
            &args.actor_token_name_not,
            &args.actor_type_not,
            &args.id_not,
            &args.raw_cf_ray_id_not,
            &args.raw_method_not,
            &args.raw_status_code_not,
            &args.raw_uri_not,
            &args.resource_id_not,
            &args.resource_product_not,
            &args.resource_type_not,
            &args.resource_scope_not,
            &args.since,
            &args.before,
            &args.direction,
            &args.limit,
            &args.cursor,
        )
        .map_err(ProviderError::Api)?;

        let task = audit_logs_v2_get_organization_audit_logs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Members list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn members_list(
        &self,
        args: &MembersListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = members_list_builder(
            &self.http_client,
            &args.organization_id,
            &args.status,
            &args.user_email,
            &args.user_email_contains,
            &args.user_email_startsWith,
            &args.user_email_endsWith,
        )
        .map_err(ProviderError::Api)?;

        let task = members_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Members create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn members_create(
        &self,
        args: &MembersCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = members_create_builder(
            &self.http_client,
            &args.organization_id,
        )
        .map_err(ProviderError::Api)?;

        let task = members_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Members retrieve.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn members_retrieve(
        &self,
        args: &MembersRetrieveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = members_retrieve_builder(
            &self.http_client,
            &args.organization_id,
            &args.member_id,
        )
        .map_err(ProviderError::Api)?;

        let task = members_retrieve_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Members delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn members_delete(
        &self,
        args: &MembersDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = members_delete_builder(
            &self.http_client,
            &args.organization_id,
            &args.member_id,
        )
        .map_err(ProviderError::Api)?;

        let task = members_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Members batch create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn members_batch_create(
        &self,
        args: &MembersBatchCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = members_batch_create_builder(
            &self.http_client,
            &args.organization_id,
        )
        .map_err(ProviderError::Api)?;

        let task = members_batch_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Organizations get profile.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the OrganizationsApiProfileResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn organizations_get_profile(
        &self,
        args: &OrganizationsGetProfileArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<OrganizationsApiProfileResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = organizations_get_profile_builder(
            &self.http_client,
            &args.organization_id,
        )
        .map_err(ProviderError::Api)?;

        let task = organizations_get_profile_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Organizations modify profile.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn organizations_modify_profile(
        &self,
        args: &OrganizationsModifyProfileArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = organizations_modify_profile_builder(
            &self.http_client,
            &args.organization_id,
        )
        .map_err(ProviderError::Api)?;

        let task = organizations_modify_profile_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Organization shares list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceSharingShareResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn organization_shares_list(
        &self,
        args: &OrganizationSharesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceSharingShareResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = organization_shares_list_builder(
            &self.http_client,
            &args.organization_id,
        )
        .map_err(ProviderError::Api)?;

        let task = organization_shares_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ai bots summary by user agent.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ai_bots_summary_by_user_agent(
        &self,
        args: &RadarGetAiBotsSummaryByUserAgentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ai_bots_summary_by_user_agent_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ai_bots_summary_by_user_agent_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ai bots summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ai_bots_summary(
        &self,
        args: &RadarGetAiBotsSummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ai_bots_summary_builder(
            &self.http_client,
            &args.dimension,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.crawlPurpose,
            &args.userAgent,
            &args.vertical,
            &args.industry,
            &args.contentType,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ai_bots_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ai bots timeseries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ai_bots_timeseries(
        &self,
        args: &RadarGetAiBotsTimeseriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ai_bots_timeseries_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.crawlPurpose,
            &args.userAgent,
            &args.industry,
            &args.vertical,
            &args.contentType,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ai_bots_timeseries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ai bots timeseries group by user agent.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ai_bots_timeseries_group_by_user_agent(
        &self,
        args: &RadarGetAiBotsTimeseriesGroupByUserAgentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ai_bots_timeseries_group_by_user_agent_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ai_bots_timeseries_group_by_user_agent_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ai bots timeseries group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ai_bots_timeseries_group(
        &self,
        args: &RadarGetAiBotsTimeseriesGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ai_bots_timeseries_group_builder(
            &self.http_client,
            &args.dimension,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.crawlPurpose,
            &args.userAgent,
            &args.industry,
            &args.vertical,
            &args.contentType,
            &args.limitPerGroup,
            &args.normalization,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ai_bots_timeseries_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ai inference summary by model.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ai_inference_summary_by_model(
        &self,
        args: &RadarGetAiInferenceSummaryByModelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ai_inference_summary_by_model_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ai_inference_summary_by_model_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ai inference summary by task.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ai_inference_summary_by_task(
        &self,
        args: &RadarGetAiInferenceSummaryByTaskArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ai_inference_summary_by_task_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ai_inference_summary_by_task_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ai inference summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ai_inference_summary(
        &self,
        args: &RadarGetAiInferenceSummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ai_inference_summary_builder(
            &self.http_client,
            &args.dimension,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ai_inference_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ai inference timeseries group by model.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ai_inference_timeseries_group_by_model(
        &self,
        args: &RadarGetAiInferenceTimeseriesGroupByModelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ai_inference_timeseries_group_by_model_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ai_inference_timeseries_group_by_model_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ai inference timeseries group by task.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ai_inference_timeseries_group_by_task(
        &self,
        args: &RadarGetAiInferenceTimeseriesGroupByTaskArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ai_inference_timeseries_group_by_task_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ai_inference_timeseries_group_by_task_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ai inference timeseries group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ai_inference_timeseries_group(
        &self,
        args: &RadarGetAiInferenceTimeseriesGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ai_inference_timeseries_group_builder(
            &self.http_client,
            &args.dimension,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.limitPerGroup,
            &args.normalization,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ai_inference_timeseries_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get annotations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_annotations(
        &self,
        args: &RadarGetAnnotationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_annotations_builder(
            &self.http_client,
            &args.limit,
            &args.offset,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.dataSource,
            &args.eventType,
            &args.asn,
            &args.location,
            &args.origin,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_annotations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get annotations outages.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_annotations_outages(
        &self,
        args: &RadarGetAnnotationsOutagesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_annotations_outages_builder(
            &self.http_client,
            &args.limit,
            &args.offset,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.origin,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_annotations_outages_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get annotations outages top.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_annotations_outages_top(
        &self,
        args: &RadarGetAnnotationsOutagesTopArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_annotations_outages_top_builder(
            &self.http_client,
            &args.limit,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_annotations_outages_top_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns as112 timeseries by dnssec.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_as112_timeseries_by_dnssec(
        &self,
        args: &RadarGetDnsAs112TimeseriesByDnssecArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_as112_timeseries_by_dnssec_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_as112_timeseries_by_dnssec_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns as112 timeseries by edns.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_as112_timeseries_by_edns(
        &self,
        args: &RadarGetDnsAs112TimeseriesByEdnsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_as112_timeseries_by_edns_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_as112_timeseries_by_edns_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns as112 timeseries by ip version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_as112_timeseries_by_ip_version(
        &self,
        args: &RadarGetDnsAs112TimeseriesByIpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_as112_timeseries_by_ip_version_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_as112_timeseries_by_ip_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns as112 timeseries by protocol.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_as112_timeseries_by_protocol(
        &self,
        args: &RadarGetDnsAs112TimeseriesByProtocolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_as112_timeseries_by_protocol_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.queryType,
            &args.responseCode,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_as112_timeseries_by_protocol_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns as112 timeseries by query type.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_as112_timeseries_by_query_type(
        &self,
        args: &RadarGetDnsAs112TimeseriesByQueryTypeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_as112_timeseries_by_query_type_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.protocol,
            &args.responseCode,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_as112_timeseries_by_query_type_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns as112 timeseries by response codes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_as112_timeseries_by_response_codes(
        &self,
        args: &RadarGetDnsAs112TimeseriesByResponseCodesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_as112_timeseries_by_response_codes_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.queryType,
            &args.protocol,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_as112_timeseries_by_response_codes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns as112 summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_as112_summary(
        &self,
        args: &RadarGetDnsAs112SummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_as112_summary_builder(
            &self.http_client,
            &args.dimension,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_as112_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns as112 timeseries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_as112_timeseries(
        &self,
        args: &RadarGetDnsAs112TimeseriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_as112_timeseries_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_as112_timeseries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns as112 timeseries group by dnssec.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_as112_timeseries_group_by_dnssec(
        &self,
        args: &RadarGetDnsAs112TimeseriesGroupByDnssecArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_as112_timeseries_group_by_dnssec_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_as112_timeseries_group_by_dnssec_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns as112 timeseries group by edns.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_as112_timeseries_group_by_edns(
        &self,
        args: &RadarGetDnsAs112TimeseriesGroupByEdnsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_as112_timeseries_group_by_edns_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_as112_timeseries_group_by_edns_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns as112 timeseries group by ip version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_as112_timeseries_group_by_ip_version(
        &self,
        args: &RadarGetDnsAs112TimeseriesGroupByIpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_as112_timeseries_group_by_ip_version_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_as112_timeseries_group_by_ip_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns as112 timeseries group by protocol.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_as112_timeseries_group_by_protocol(
        &self,
        args: &RadarGetDnsAs112TimeseriesGroupByProtocolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_as112_timeseries_group_by_protocol_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.queryType,
            &args.responseCode,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_as112_timeseries_group_by_protocol_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns as112 timeseries group by query type.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_as112_timeseries_group_by_query_type(
        &self,
        args: &RadarGetDnsAs112TimeseriesGroupByQueryTypeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_as112_timeseries_group_by_query_type_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.protocol,
            &args.responseCode,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_as112_timeseries_group_by_query_type_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns as112 timeseries group by response codes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_as112_timeseries_group_by_response_codes(
        &self,
        args: &RadarGetDnsAs112TimeseriesGroupByResponseCodesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_as112_timeseries_group_by_response_codes_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.queryType,
            &args.protocol,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_as112_timeseries_group_by_response_codes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns as112 timeseries group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_as112_timeseries_group(
        &self,
        args: &RadarGetDnsAs112TimeseriesGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_as112_timeseries_group_builder(
            &self.http_client,
            &args.dimension,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_as112_timeseries_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns as112 top locations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_as112_top_locations(
        &self,
        args: &RadarGetDnsAs112TopLocationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_as112_top_locations_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_as112_top_locations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns as112 top locations by dnssec.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_as112_top_locations_by_dnssec(
        &self,
        args: &RadarGetDnsAs112TopLocationsByDnssecArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_as112_top_locations_by_dnssec_builder(
            &self.http_client,
            &args.dnssec,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_as112_top_locations_by_dnssec_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns as112 top locations by edns.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_as112_top_locations_by_edns(
        &self,
        args: &RadarGetDnsAs112TopLocationsByEdnsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_as112_top_locations_by_edns_builder(
            &self.http_client,
            &args.edns,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_as112_top_locations_by_edns_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns as112 top locations by ip version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_as112_top_locations_by_ip_version(
        &self,
        args: &RadarGetDnsAs112TopLocationsByIpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_as112_top_locations_by_ip_version_builder(
            &self.http_client,
            &args.ip_version,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_as112_top_locations_by_ip_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 summary by bitrate.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_summary_by_bitrate(
        &self,
        args: &RadarGetAttacksLayer3SummaryByBitrateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_summary_by_bitrate_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.protocol,
            &args.direction,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_summary_by_bitrate_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 summary by duration.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_summary_by_duration(
        &self,
        args: &RadarGetAttacksLayer3SummaryByDurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_summary_by_duration_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.protocol,
            &args.direction,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_summary_by_duration_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 summary by industry.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_summary_by_industry(
        &self,
        args: &RadarGetAttacksLayer3SummaryByIndustryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_summary_by_industry_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.protocol,
            &args.direction,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_summary_by_industry_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 summary by ip version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_summary_by_ip_version(
        &self,
        args: &RadarGetAttacksLayer3SummaryByIpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_summary_by_ip_version_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.protocol,
            &args.direction,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_summary_by_ip_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 summary by protocol.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_summary_by_protocol(
        &self,
        args: &RadarGetAttacksLayer3SummaryByProtocolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_summary_by_protocol_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.direction,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_summary_by_protocol_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 summary by vector.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_summary_by_vector(
        &self,
        args: &RadarGetAttacksLayer3SummaryByVectorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_summary_by_vector_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.protocol,
            &args.direction,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_summary_by_vector_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 summary by vertical.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_summary_by_vertical(
        &self,
        args: &RadarGetAttacksLayer3SummaryByVerticalArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_summary_by_vertical_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.protocol,
            &args.direction,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_summary_by_vertical_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_summary(
        &self,
        args: &RadarGetAttacksLayer3SummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_summary_builder(
            &self.http_client,
            &args.dimension,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.protocol,
            &args.direction,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 timeseries by bytes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_timeseries_by_bytes(
        &self,
        args: &RadarGetAttacksLayer3TimeseriesByBytesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_timeseries_by_bytes_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.protocol,
            &args.normalization,
            &args.metric,
            &args.direction,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_timeseries_by_bytes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 timeseries group by bitrate.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_timeseries_group_by_bitrate(
        &self,
        args: &RadarGetAttacksLayer3TimeseriesGroupByBitrateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_timeseries_group_by_bitrate_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.protocol,
            &args.normalization,
            &args.direction,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_timeseries_group_by_bitrate_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 timeseries group by duration.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_timeseries_group_by_duration(
        &self,
        args: &RadarGetAttacksLayer3TimeseriesGroupByDurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_timeseries_group_by_duration_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.protocol,
            &args.normalization,
            &args.direction,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_timeseries_group_by_duration_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 timeseries group by industry.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_timeseries_group_by_industry(
        &self,
        args: &RadarGetAttacksLayer3TimeseriesGroupByIndustryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_timeseries_group_by_industry_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.protocol,
            &args.normalization,
            &args.direction,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_timeseries_group_by_industry_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 timeseries group by ip version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_timeseries_group_by_ip_version(
        &self,
        args: &RadarGetAttacksLayer3TimeseriesGroupByIpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_timeseries_group_by_ip_version_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.protocol,
            &args.normalization,
            &args.direction,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_timeseries_group_by_ip_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 timeseries group by protocol.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_timeseries_group_by_protocol(
        &self,
        args: &RadarGetAttacksLayer3TimeseriesGroupByProtocolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_timeseries_group_by_protocol_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.normalization,
            &args.direction,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_timeseries_group_by_protocol_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 timeseries group by vector.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_timeseries_group_by_vector(
        &self,
        args: &RadarGetAttacksLayer3TimeseriesGroupByVectorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_timeseries_group_by_vector_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.protocol,
            &args.normalization,
            &args.direction,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_timeseries_group_by_vector_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 timeseries group by vertical.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_timeseries_group_by_vertical(
        &self,
        args: &RadarGetAttacksLayer3TimeseriesGroupByVerticalArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_timeseries_group_by_vertical_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.protocol,
            &args.normalization,
            &args.direction,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_timeseries_group_by_vertical_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 timeseries group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_timeseries_group(
        &self,
        args: &RadarGetAttacksLayer3TimeseriesGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_timeseries_group_builder(
            &self.http_client,
            &args.dimension,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.protocol,
            &args.normalization,
            &args.direction,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_timeseries_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 top attacks.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_top_attacks(
        &self,
        args: &RadarGetAttacksLayer3TopAttacksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_top_attacks_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.protocol,
            &args.limitDirection,
            &args.limitPerLocation,
            &args.magnitude,
            &args.normalization,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_top_attacks_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 top industries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_top_industries(
        &self,
        args: &RadarGetAttacksLayer3TopIndustriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_top_industries_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.protocol,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_top_industries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 top origin locations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_top_origin_locations(
        &self,
        args: &RadarGetAttacksLayer3TopOriginLocationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_top_origin_locations_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.protocol,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_top_origin_locations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 top target locations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_top_target_locations(
        &self,
        args: &RadarGetAttacksLayer3TopTargetLocationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_top_target_locations_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.protocol,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_top_target_locations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer3 top verticals.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer3_top_verticals(
        &self,
        args: &RadarGetAttacksLayer3TopVerticalsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer3_top_verticals_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.protocol,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer3_top_verticals_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 summary by http method.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_summary_by_http_method(
        &self,
        args: &RadarGetAttacksLayer7SummaryByHttpMethodArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_summary_by_http_method_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.httpVersion,
            &args.mitigationProduct,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_summary_by_http_method_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 summary by http version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_summary_by_http_version(
        &self,
        args: &RadarGetAttacksLayer7SummaryByHttpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_summary_by_http_version_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.httpMethod,
            &args.mitigationProduct,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_summary_by_http_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 summary by industry.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_summary_by_industry(
        &self,
        args: &RadarGetAttacksLayer7SummaryByIndustryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_summary_by_industry_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.httpVersion,
            &args.httpMethod,
            &args.mitigationProduct,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_summary_by_industry_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 summary by ip version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_summary_by_ip_version(
        &self,
        args: &RadarGetAttacksLayer7SummaryByIpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_summary_by_ip_version_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.httpVersion,
            &args.httpMethod,
            &args.mitigationProduct,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_summary_by_ip_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 summary by managed rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_summary_by_managed_rules(
        &self,
        args: &RadarGetAttacksLayer7SummaryByManagedRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_summary_by_managed_rules_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.httpVersion,
            &args.httpMethod,
            &args.mitigationProduct,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_summary_by_managed_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 summary by mitigation product.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_summary_by_mitigation_product(
        &self,
        args: &RadarGetAttacksLayer7SummaryByMitigationProductArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_summary_by_mitigation_product_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.httpVersion,
            &args.httpMethod,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_summary_by_mitigation_product_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 summary by vertical.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_summary_by_vertical(
        &self,
        args: &RadarGetAttacksLayer7SummaryByVerticalArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_summary_by_vertical_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.httpVersion,
            &args.httpMethod,
            &args.mitigationProduct,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_summary_by_vertical_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_summary(
        &self,
        args: &RadarGetAttacksLayer7SummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_summary_builder(
            &self.http_client,
            &args.dimension,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.httpVersion,
            &args.httpMethod,
            &args.mitigationProduct,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 timeseries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_timeseries(
        &self,
        args: &RadarGetAttacksLayer7TimeseriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_timeseries_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.normalization,
            &args.ipVersion,
            &args.httpVersion,
            &args.httpMethod,
            &args.mitigationProduct,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_timeseries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 timeseries group by http method.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_timeseries_group_by_http_method(
        &self,
        args: &RadarGetAttacksLayer7TimeseriesGroupByHttpMethodArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_timeseries_group_by_http_method_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.httpVersion,
            &args.mitigationProduct,
            &args.normalization,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_timeseries_group_by_http_method_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 timeseries group by http version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_timeseries_group_by_http_version(
        &self,
        args: &RadarGetAttacksLayer7TimeseriesGroupByHttpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_timeseries_group_by_http_version_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.httpMethod,
            &args.mitigationProduct,
            &args.normalization,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_timeseries_group_by_http_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 timeseries group by industry.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_timeseries_group_by_industry(
        &self,
        args: &RadarGetAttacksLayer7TimeseriesGroupByIndustryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_timeseries_group_by_industry_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.httpVersion,
            &args.httpMethod,
            &args.mitigationProduct,
            &args.normalization,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_timeseries_group_by_industry_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 timeseries group by ip version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_timeseries_group_by_ip_version(
        &self,
        args: &RadarGetAttacksLayer7TimeseriesGroupByIpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_timeseries_group_by_ip_version_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.httpVersion,
            &args.httpMethod,
            &args.mitigationProduct,
            &args.normalization,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_timeseries_group_by_ip_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 timeseries group by managed rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_timeseries_group_by_managed_rules(
        &self,
        args: &RadarGetAttacksLayer7TimeseriesGroupByManagedRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_timeseries_group_by_managed_rules_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.httpVersion,
            &args.httpMethod,
            &args.mitigationProduct,
            &args.normalization,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_timeseries_group_by_managed_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 timeseries group by mitigation product.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_timeseries_group_by_mitigation_product(
        &self,
        args: &RadarGetAttacksLayer7TimeseriesGroupByMitigationProductArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_timeseries_group_by_mitigation_product_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.httpVersion,
            &args.httpMethod,
            &args.normalization,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_timeseries_group_by_mitigation_product_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 timeseries group by vertical.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_timeseries_group_by_vertical(
        &self,
        args: &RadarGetAttacksLayer7TimeseriesGroupByVerticalArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_timeseries_group_by_vertical_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.httpVersion,
            &args.httpMethod,
            &args.mitigationProduct,
            &args.normalization,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_timeseries_group_by_vertical_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 timeseries group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_timeseries_group(
        &self,
        args: &RadarGetAttacksLayer7TimeseriesGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_timeseries_group_builder(
            &self.http_client,
            &args.dimension,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.httpVersion,
            &args.httpMethod,
            &args.mitigationProduct,
            &args.normalization,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_timeseries_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 top origin as.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_top_origin_as(
        &self,
        args: &RadarGetAttacksLayer7TopOriginAsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_top_origin_as_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.httpVersion,
            &args.httpMethod,
            &args.mitigationProduct,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_top_origin_as_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 top attacks.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_top_attacks(
        &self,
        args: &RadarGetAttacksLayer7TopAttacksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_top_attacks_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.mitigationProduct,
            &args.limitDirection,
            &args.limitPerLocation,
            &args.normalization,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_top_attacks_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 top industries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_top_industries(
        &self,
        args: &RadarGetAttacksLayer7TopIndustriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_top_industries_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.httpVersion,
            &args.httpMethod,
            &args.mitigationProduct,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_top_industries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 top origin location.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_top_origin_location(
        &self,
        args: &RadarGetAttacksLayer7TopOriginLocationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_top_origin_location_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.continent,
            &args.ipVersion,
            &args.httpVersion,
            &args.httpMethod,
            &args.mitigationProduct,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_top_origin_location_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 top target location.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_top_target_location(
        &self,
        args: &RadarGetAttacksLayer7TopTargetLocationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_top_target_location_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.continent,
            &args.mitigationProduct,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_top_target_location_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get attacks layer7 top verticals.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_attacks_layer7_top_verticals(
        &self,
        args: &RadarGetAttacksLayer7TopVerticalsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_attacks_layer7_top_verticals_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.ipVersion,
            &args.httpVersion,
            &args.httpMethod,
            &args.mitigationProduct,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_attacks_layer7_top_verticals_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bgp hijacks events.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bgp_hijacks_events(
        &self,
        args: &RadarGetBgpHijacksEventsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bgp_hijacks_events_builder(
            &self.http_client,
            &args.page,
            &args.per_page,
            &args.eventId,
            &args.hijackerAsn,
            &args.victimAsn,
            &args.involvedAsn,
            &args.involvedCountry,
            &args.prefix,
            &args.minConfidence,
            &args.maxConfidence,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.sortBy,
            &args.sortOrder,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bgp_hijacks_events_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bgp ips timeseries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bgp_ips_timeseries(
        &self,
        args: &RadarGetBgpIpsTimeseriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bgp_ips_timeseries_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.ipVersion,
            &args.includeDelay,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bgp_ips_timeseries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bgp route leak events.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bgp_route_leak_events(
        &self,
        args: &RadarGetBgpRouteLeakEventsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bgp_route_leak_events_builder(
            &self.http_client,
            &args.page,
            &args.per_page,
            &args.eventId,
            &args.leakAsn,
            &args.involvedAsn,
            &args.involvedCountry,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.sortBy,
            &args.sortOrder,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bgp_route_leak_events_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bgp routes asns.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bgp_routes_asns(
        &self,
        args: &RadarGetBgpRoutesAsnsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bgp_routes_asns_builder(
            &self.http_client,
            &args.location,
            &args.limit,
            &args.sortBy,
            &args.sortOrder,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bgp_routes_asns_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bgp pfx2as moas.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bgp_pfx2as_moas(
        &self,
        args: &RadarGetBgpPfx2AsMoasArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bgp_pfx2as_moas_builder(
            &self.http_client,
            &args.origin,
            &args.prefix,
            &args.invalid_only,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bgp_pfx2as_moas_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bgp pfx2as.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bgp_pfx2as(
        &self,
        args: &RadarGetBgpPfx2AsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bgp_pfx2as_builder(
            &self.http_client,
            &args.prefix,
            &args.origin,
            &args.rpkiStatus,
            &args.longestPrefixMatch,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bgp_pfx2as_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bgp routes realtime.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bgp_routes_realtime(
        &self,
        args: &RadarGetBgpRoutesRealtimeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bgp_routes_realtime_builder(
            &self.http_client,
            &args.prefix,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bgp_routes_realtime_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bgp routes stats.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bgp_routes_stats(
        &self,
        args: &RadarGetBgpRoutesStatsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bgp_routes_stats_builder(
            &self.http_client,
            &args.asn,
            &args.location,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bgp_routes_stats_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bgp rpki aspa changes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bgp_rpki_aspa_changes(
        &self,
        args: &RadarGetBgpRpkiAspaChangesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bgp_rpki_aspa_changes_builder(
            &self.http_client,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.includeAsnInfo,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bgp_rpki_aspa_changes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bgp rpki aspa snapshot.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bgp_rpki_aspa_snapshot(
        &self,
        args: &RadarGetBgpRpkiAspaSnapshotArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bgp_rpki_aspa_snapshot_builder(
            &self.http_client,
            &args.customerAsn,
            &args.providerAsn,
            &args.date,
            &args.includeAsnInfo,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bgp_rpki_aspa_snapshot_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bgp rpki aspa timeseries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bgp_rpki_aspa_timeseries(
        &self,
        args: &RadarGetBgpRpkiAspaTimeseriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bgp_rpki_aspa_timeseries_builder(
            &self.http_client,
            &args.dateStart,
            &args.dateEnd,
            &args.name,
            &args.rir,
            &args.location,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bgp_rpki_aspa_timeseries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bgp timeseries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bgp_timeseries(
        &self,
        args: &RadarGetBgpTimeseriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bgp_timeseries_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.prefix,
            &args.updateType,
            &args.asn,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bgp_timeseries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bgp top ases.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bgp_top_ases(
        &self,
        args: &RadarGetBgpTopAsesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bgp_top_ases_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.prefix,
            &args.updateType,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bgp_top_ases_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bgp top asns by prefixes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bgp_top_asns_by_prefixes(
        &self,
        args: &RadarGetBgpTopAsnsByPrefixesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bgp_top_asns_by_prefixes_builder(
            &self.http_client,
            &args.country,
            &args.limit,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bgp_top_asns_by_prefixes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bgp top prefixes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bgp_top_prefixes(
        &self,
        args: &RadarGetBgpTopPrefixesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bgp_top_prefixes_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.updateType,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bgp_top_prefixes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bots.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bots(
        &self,
        args: &RadarGetBotsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bots_builder(
            &self.http_client,
            &args.limit,
            &args.offset,
            &args.botCategory,
            &args.botOperator,
            &args.kind,
            &args.botVerificationStatus,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bots_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get crawlers summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_crawlers_summary(
        &self,
        args: &RadarGetCrawlersSummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_crawlers_summary_builder(
            &self.http_client,
            &args.dimension,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.limitPerGroup,
            &args.botOperator,
            &args.vertical,
            &args.industry,
            &args.clientType,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_crawlers_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get crawlers timeseries group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_crawlers_timeseries_group(
        &self,
        args: &RadarGetCrawlersTimeseriesGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_crawlers_timeseries_group_builder(
            &self.http_client,
            &args.dimension,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.limitPerGroup,
            &args.botOperator,
            &args.vertical,
            &args.industry,
            &args.clientType,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_crawlers_timeseries_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bots summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bots_summary(
        &self,
        args: &RadarGetBotsSummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bots_summary_builder(
            &self.http_client,
            &args.dimension,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.limitPerGroup,
            &args.bot,
            &args.botOperator,
            &args.botCategory,
            &args.botKind,
            &args.botVerificationStatus,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bots_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bots timeseries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bots_timeseries(
        &self,
        args: &RadarGetBotsTimeseriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bots_timeseries_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.bot,
            &args.botOperator,
            &args.botCategory,
            &args.botKind,
            &args.botVerificationStatus,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bots_timeseries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bots timeseries group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bots_timeseries_group(
        &self,
        args: &RadarGetBotsTimeseriesGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bots_timeseries_group_builder(
            &self.http_client,
            &args.dimension,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.limitPerGroup,
            &args.bot,
            &args.botOperator,
            &args.botCategory,
            &args.botKind,
            &args.botVerificationStatus,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bots_timeseries_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get bot details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_bot_details(
        &self,
        args: &RadarGetBotDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_bot_details_builder(
            &self.http_client,
            &args.bot_slug,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_bot_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get certificate authorities.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_certificate_authorities(
        &self,
        args: &RadarGetCertificateAuthoritiesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_certificate_authorities_builder(
            &self.http_client,
            &args.limit,
            &args.offset,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_certificate_authorities_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get certificate authority details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_certificate_authority_details(
        &self,
        args: &RadarGetCertificateAuthorityDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_certificate_authority_details_builder(
            &self.http_client,
            &args.ca_slug,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_certificate_authority_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get certificate logs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_certificate_logs(
        &self,
        args: &RadarGetCertificateLogsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_certificate_logs_builder(
            &self.http_client,
            &args.limit,
            &args.offset,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_certificate_logs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get certificate log details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_certificate_log_details(
        &self,
        args: &RadarGetCertificateLogDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_certificate_log_details_builder(
            &self.http_client,
            &args.log_slug,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_certificate_log_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ct summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ct_summary(
        &self,
        args: &RadarGetCtSummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ct_summary_builder(
            &self.http_client,
            &args.dimension,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.limitPerGroup,
            &args.ca,
            &args.caOwner,
            &args.duration,
            &args.entryType,
            &args.expirationStatus,
            &args.hasIps,
            &args.hasWildcards,
            &args.log,
            &args.logApi,
            &args.logOperator,
            &args.publicKeyAlgorithm,
            &args.signatureAlgorithm,
            &args.tld,
            &args.validationLevel,
            &args.uniqueEntries,
            &args.normalization,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ct_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ct timeseries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ct_timeseries(
        &self,
        args: &RadarGetCtTimeseriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ct_timeseries_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.ca,
            &args.caOwner,
            &args.duration,
            &args.entryType,
            &args.expirationStatus,
            &args.hasIps,
            &args.hasWildcards,
            &args.log,
            &args.logApi,
            &args.logOperator,
            &args.publicKeyAlgorithm,
            &args.signatureAlgorithm,
            &args.tld,
            &args.validationLevel,
            &args.uniqueEntries,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ct_timeseries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ct timeseries group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ct_timeseries_group(
        &self,
        args: &RadarGetCtTimeseriesGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ct_timeseries_group_builder(
            &self.http_client,
            &args.dimension,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.limitPerGroup,
            &args.ca,
            &args.caOwner,
            &args.duration,
            &args.entryType,
            &args.expirationStatus,
            &args.hasIps,
            &args.hasWildcards,
            &args.log,
            &args.logApi,
            &args.logOperator,
            &args.publicKeyAlgorithm,
            &args.signatureAlgorithm,
            &args.validationLevel,
            &args.tld,
            &args.normalization,
            &args.uniqueEntries,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ct_timeseries_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get reports datasets.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_reports_datasets(
        &self,
        args: &RadarGetReportsDatasetsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_reports_datasets_builder(
            &self.http_client,
            &args.limit,
            &args.offset,
            &args.datasetType,
            &args.date,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_reports_datasets_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar post reports dataset download url.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn radar_post_reports_dataset_download_url(
        &self,
        args: &RadarPostReportsDatasetDownloadUrlArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_post_reports_dataset_download_url_builder(
            &self.http_client,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_post_reports_dataset_download_url_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get reports dataset download.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_reports_dataset_download(
        &self,
        args: &RadarGetReportsDatasetDownloadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_reports_dataset_download_builder(
            &self.http_client,
            &args.alias,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_reports_dataset_download_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns summary by cache hit status.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_summary_by_cache_hit_status(
        &self,
        args: &RadarGetDnsSummaryByCacheHitStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_summary_by_cache_hit_status_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.nodata,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_summary_by_cache_hit_status_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns summary by dnssec.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_summary_by_dnssec(
        &self,
        args: &RadarGetDnsSummaryByDnssecArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_summary_by_dnssec_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.nodata,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_summary_by_dnssec_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns summary by dnssec awareness.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_summary_by_dnssec_awareness(
        &self,
        args: &RadarGetDnsSummaryByDnssecAwarenessArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_summary_by_dnssec_awareness_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.nodata,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_summary_by_dnssec_awareness_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns summary by dnssec e2e version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_summary_by_dnssec_e2e_version(
        &self,
        args: &RadarGetDnsSummaryByDnssecE2EVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_summary_by_dnssec_e2e_version_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.nodata,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_summary_by_dnssec_e2e_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns summary by ip version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_summary_by_ip_version(
        &self,
        args: &RadarGetDnsSummaryByIpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_summary_by_ip_version_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.nodata,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_summary_by_ip_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns summary by matching answer status.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_summary_by_matching_answer_status(
        &self,
        args: &RadarGetDnsSummaryByMatchingAnswerStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_summary_by_matching_answer_status_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.nodata,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_summary_by_matching_answer_status_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns summary by protocol.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_summary_by_protocol(
        &self,
        args: &RadarGetDnsSummaryByProtocolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_summary_by_protocol_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.queryType,
            &args.responseCode,
            &args.nodata,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_summary_by_protocol_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns summary by query type.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_summary_by_query_type(
        &self,
        args: &RadarGetDnsSummaryByQueryTypeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_summary_by_query_type_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.protocol,
            &args.responseCode,
            &args.nodata,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_summary_by_query_type_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns summary by response code.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_summary_by_response_code(
        &self,
        args: &RadarGetDnsSummaryByResponseCodeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_summary_by_response_code_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.queryType,
            &args.protocol,
            &args.nodata,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_summary_by_response_code_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns summary by response ttl.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_summary_by_response_ttl(
        &self,
        args: &RadarGetDnsSummaryByResponseTtlArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_summary_by_response_ttl_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.nodata,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_summary_by_response_ttl_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_summary(
        &self,
        args: &RadarGetDnsSummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_summary_builder(
            &self.http_client,
            &args.dimension,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.cacheHit,
            &args.nodata,
            &args.protocol,
            &args.queryType,
            &args.responseCode,
            &args.responseTtl,
            &args.dnssec,
            &args.dnssecAware,
            &args.dnssecE2e,
            &args.ipVersion,
            &args.limitPerGroup,
            &args.matchingAnswer,
            &args.tld,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns timeseries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_timeseries(
        &self,
        args: &RadarGetDnsTimeseriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_timeseries_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.cacheHit,
            &args.nodata,
            &args.protocol,
            &args.queryType,
            &args.responseCode,
            &args.responseTtl,
            &args.dnssec,
            &args.dnssecAware,
            &args.dnssecE2e,
            &args.ipVersion,
            &args.matchingAnswer,
            &args.tld,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_timeseries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns timeseries group by cache hit status.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_timeseries_group_by_cache_hit_status(
        &self,
        args: &RadarGetDnsTimeseriesGroupByCacheHitStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_timeseries_group_by_cache_hit_status_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.nodata,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_timeseries_group_by_cache_hit_status_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns timeseries group by dnssec.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_timeseries_group_by_dnssec(
        &self,
        args: &RadarGetDnsTimeseriesGroupByDnssecArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_timeseries_group_by_dnssec_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.nodata,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_timeseries_group_by_dnssec_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns timeseries group by dnssec awareness.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_timeseries_group_by_dnssec_awareness(
        &self,
        args: &RadarGetDnsTimeseriesGroupByDnssecAwarenessArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_timeseries_group_by_dnssec_awareness_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.nodata,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_timeseries_group_by_dnssec_awareness_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns timeseries group by dnssec e2e version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_timeseries_group_by_dnssec_e2e_version(
        &self,
        args: &RadarGetDnsTimeseriesGroupByDnssecE2EVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_timeseries_group_by_dnssec_e2e_version_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.nodata,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_timeseries_group_by_dnssec_e2e_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns timeseries group by ip version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_timeseries_group_by_ip_version(
        &self,
        args: &RadarGetDnsTimeseriesGroupByIpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_timeseries_group_by_ip_version_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.nodata,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_timeseries_group_by_ip_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns timeseries group by matching answer status.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_timeseries_group_by_matching_answer_status(
        &self,
        args: &RadarGetDnsTimeseriesGroupByMatchingAnswerStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_timeseries_group_by_matching_answer_status_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.nodata,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_timeseries_group_by_matching_answer_status_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns timeseries group by protocol.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_timeseries_group_by_protocol(
        &self,
        args: &RadarGetDnsTimeseriesGroupByProtocolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_timeseries_group_by_protocol_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.queryType,
            &args.responseCode,
            &args.nodata,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_timeseries_group_by_protocol_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns timeseries group by query type.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_timeseries_group_by_query_type(
        &self,
        args: &RadarGetDnsTimeseriesGroupByQueryTypeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_timeseries_group_by_query_type_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.protocol,
            &args.responseCode,
            &args.nodata,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_timeseries_group_by_query_type_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns timeseries group by response code.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_timeseries_group_by_response_code(
        &self,
        args: &RadarGetDnsTimeseriesGroupByResponseCodeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_timeseries_group_by_response_code_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.queryType,
            &args.protocol,
            &args.nodata,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_timeseries_group_by_response_code_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns timeseries group by response ttl.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_timeseries_group_by_response_ttl(
        &self,
        args: &RadarGetDnsTimeseriesGroupByResponseTtlArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_timeseries_group_by_response_ttl_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.tld,
            &args.queryType,
            &args.protocol,
            &args.responseCode,
            &args.nodata,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_timeseries_group_by_response_ttl_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns timeseries group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_timeseries_group(
        &self,
        args: &RadarGetDnsTimeseriesGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_timeseries_group_builder(
            &self.http_client,
            &args.dimension,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.cacheHit,
            &args.nodata,
            &args.protocol,
            &args.queryType,
            &args.responseCode,
            &args.responseTtl,
            &args.dnssec,
            &args.dnssecAware,
            &args.dnssecE2e,
            &args.ipVersion,
            &args.limitPerGroup,
            &args.matchingAnswer,
            &args.tld,
            &args.normalization,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_timeseries_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns top ases.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_top_ases(
        &self,
        args: &RadarGetDnsTopAsesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_top_ases_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.domain,
            &args.cacheHit,
            &args.nodata,
            &args.protocol,
            &args.queryType,
            &args.responseCode,
            &args.responseTtl,
            &args.dnssec,
            &args.dnssecAware,
            &args.dnssecE2e,
            &args.ipVersion,
            &args.matchingAnswer,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_top_ases_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get dns top locations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_dns_top_locations(
        &self,
        args: &RadarGetDnsTopLocationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_dns_top_locations_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.domain,
            &args.cacheHit,
            &args.nodata,
            &args.protocol,
            &args.queryType,
            &args.responseCode,
            &args.responseTtl,
            &args.dnssec,
            &args.dnssecAware,
            &args.dnssecE2e,
            &args.ipVersion,
            &args.matchingAnswer,
            &args.tld,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_dns_top_locations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email routing summary by arc.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_routing_summary_by_arc(
        &self,
        args: &RadarGetEmailRoutingSummaryByArcArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_routing_summary_by_arc_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.ipVersion,
            &args.encrypted,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_routing_summary_by_arc_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email routing summary by dkim.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_routing_summary_by_dkim(
        &self,
        args: &RadarGetEmailRoutingSummaryByDkimArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_routing_summary_by_dkim_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dmarc,
            &args.spf,
            &args.ipVersion,
            &args.encrypted,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_routing_summary_by_dkim_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email routing summary by dmarc.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_routing_summary_by_dmarc(
        &self,
        args: &RadarGetEmailRoutingSummaryByDmarcArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_routing_summary_by_dmarc_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.spf,
            &args.ipVersion,
            &args.encrypted,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_routing_summary_by_dmarc_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email routing summary by encrypted.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_routing_summary_by_encrypted(
        &self,
        args: &RadarGetEmailRoutingSummaryByEncryptedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_routing_summary_by_encrypted_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.ipVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_routing_summary_by_encrypted_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email routing summary by ip version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_routing_summary_by_ip_version(
        &self,
        args: &RadarGetEmailRoutingSummaryByIpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_routing_summary_by_ip_version_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.encrypted,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_routing_summary_by_ip_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email routing summary by spf.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_routing_summary_by_spf(
        &self,
        args: &RadarGetEmailRoutingSummaryBySpfArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_routing_summary_by_spf_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.ipVersion,
            &args.encrypted,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_routing_summary_by_spf_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email routing summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_routing_summary(
        &self,
        args: &RadarGetEmailRoutingSummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_routing_summary_builder(
            &self.http_client,
            &args.dimension,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.ipVersion,
            &args.encrypted,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_routing_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email routing timeseries group by arc.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_routing_timeseries_group_by_arc(
        &self,
        args: &RadarGetEmailRoutingTimeseriesGroupByArcArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_routing_timeseries_group_by_arc_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.ipVersion,
            &args.encrypted,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_routing_timeseries_group_by_arc_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email routing timeseries group by dkim.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_routing_timeseries_group_by_dkim(
        &self,
        args: &RadarGetEmailRoutingTimeseriesGroupByDkimArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_routing_timeseries_group_by_dkim_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dmarc,
            &args.spf,
            &args.ipVersion,
            &args.encrypted,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_routing_timeseries_group_by_dkim_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email routing timeseries group by dmarc.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_routing_timeseries_group_by_dmarc(
        &self,
        args: &RadarGetEmailRoutingTimeseriesGroupByDmarcArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_routing_timeseries_group_by_dmarc_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.spf,
            &args.ipVersion,
            &args.encrypted,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_routing_timeseries_group_by_dmarc_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email routing timeseries group by encrypted.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_routing_timeseries_group_by_encrypted(
        &self,
        args: &RadarGetEmailRoutingTimeseriesGroupByEncryptedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_routing_timeseries_group_by_encrypted_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.ipVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_routing_timeseries_group_by_encrypted_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email routing timeseries group by ip version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_routing_timeseries_group_by_ip_version(
        &self,
        args: &RadarGetEmailRoutingTimeseriesGroupByIpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_routing_timeseries_group_by_ip_version_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.encrypted,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_routing_timeseries_group_by_ip_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email routing timeseries group by spf.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_routing_timeseries_group_by_spf(
        &self,
        args: &RadarGetEmailRoutingTimeseriesGroupBySpfArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_routing_timeseries_group_by_spf_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.ipVersion,
            &args.encrypted,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_routing_timeseries_group_by_spf_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email routing timeseries group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_routing_timeseries_group(
        &self,
        args: &RadarGetEmailRoutingTimeseriesGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_routing_timeseries_group_builder(
            &self.http_client,
            &args.dimension,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.ipVersion,
            &args.encrypted,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_routing_timeseries_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security summary by arc.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_summary_by_arc(
        &self,
        args: &RadarGetEmailSecuritySummaryByArcArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_summary_by_arc_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_summary_by_arc_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security summary by dkim.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_summary_by_dkim(
        &self,
        args: &RadarGetEmailSecuritySummaryByDkimArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_summary_by_dkim_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dmarc,
            &args.spf,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_summary_by_dkim_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security summary by dmarc.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_summary_by_dmarc(
        &self,
        args: &RadarGetEmailSecuritySummaryByDmarcArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_summary_by_dmarc_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.spf,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_summary_by_dmarc_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security summary by malicious.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_summary_by_malicious(
        &self,
        args: &RadarGetEmailSecuritySummaryByMaliciousArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_summary_by_malicious_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_summary_by_malicious_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security summary by spam.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_summary_by_spam(
        &self,
        args: &RadarGetEmailSecuritySummaryBySpamArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_summary_by_spam_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_summary_by_spam_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security summary by spf.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_summary_by_spf(
        &self,
        args: &RadarGetEmailSecuritySummaryBySpfArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_summary_by_spf_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_summary_by_spf_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security summary by spoof.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_summary_by_spoof(
        &self,
        args: &RadarGetEmailSecuritySummaryBySpoofArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_summary_by_spoof_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_summary_by_spoof_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security summary by threat category.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_summary_by_threat_category(
        &self,
        args: &RadarGetEmailSecuritySummaryByThreatCategoryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_summary_by_threat_category_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_summary_by_threat_category_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security summary by tls version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_summary_by_tls_version(
        &self,
        args: &RadarGetEmailSecuritySummaryByTlsVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_summary_by_tls_version_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_summary_by_tls_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_summary(
        &self,
        args: &RadarGetEmailSecuritySummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_summary_builder(
            &self.http_client,
            &args.dimension,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.tlsVersion,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security timeseries group by arc.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_timeseries_group_by_arc(
        &self,
        args: &RadarGetEmailSecurityTimeseriesGroupByArcArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_timeseries_group_by_arc_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_timeseries_group_by_arc_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security timeseries group by dkim.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_timeseries_group_by_dkim(
        &self,
        args: &RadarGetEmailSecurityTimeseriesGroupByDkimArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_timeseries_group_by_dkim_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dmarc,
            &args.spf,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_timeseries_group_by_dkim_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security timeseries group by dmarc.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_timeseries_group_by_dmarc(
        &self,
        args: &RadarGetEmailSecurityTimeseriesGroupByDmarcArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_timeseries_group_by_dmarc_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.spf,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_timeseries_group_by_dmarc_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security timeseries group by malicious.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_timeseries_group_by_malicious(
        &self,
        args: &RadarGetEmailSecurityTimeseriesGroupByMaliciousArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_timeseries_group_by_malicious_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_timeseries_group_by_malicious_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security timeseries group by spam.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_timeseries_group_by_spam(
        &self,
        args: &RadarGetEmailSecurityTimeseriesGroupBySpamArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_timeseries_group_by_spam_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_timeseries_group_by_spam_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security timeseries group by spf.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_timeseries_group_by_spf(
        &self,
        args: &RadarGetEmailSecurityTimeseriesGroupBySpfArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_timeseries_group_by_spf_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_timeseries_group_by_spf_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security timeseries group by spoof.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_timeseries_group_by_spoof(
        &self,
        args: &RadarGetEmailSecurityTimeseriesGroupBySpoofArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_timeseries_group_by_spoof_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_timeseries_group_by_spoof_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security timeseries group by threat category.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_timeseries_group_by_threat_category(
        &self,
        args: &RadarGetEmailSecurityTimeseriesGroupByThreatCategoryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_timeseries_group_by_threat_category_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_timeseries_group_by_threat_category_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security timeseries group by tls version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_timeseries_group_by_tls_version(
        &self,
        args: &RadarGetEmailSecurityTimeseriesGroupByTlsVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_timeseries_group_by_tls_version_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_timeseries_group_by_tls_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security timeseries group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_timeseries_group(
        &self,
        args: &RadarGetEmailSecurityTimeseriesGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_timeseries_group_builder(
            &self.http_client,
            &args.dimension,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.tlsVersion,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_timeseries_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security top tlds by messages.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_top_tlds_by_messages(
        &self,
        args: &RadarGetEmailSecurityTopTldsByMessagesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_top_tlds_by_messages_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.tlsVersion,
            &args.tldCategory,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_top_tlds_by_messages_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security top tlds by malicious.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_top_tlds_by_malicious(
        &self,
        args: &RadarGetEmailSecurityTopTldsByMaliciousArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_top_tlds_by_malicious_builder(
            &self.http_client,
            &args.malicious,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.tlsVersion,
            &args.tldCategory,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_top_tlds_by_malicious_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security top tlds by spam.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_top_tlds_by_spam(
        &self,
        args: &RadarGetEmailSecurityTopTldsBySpamArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_top_tlds_by_spam_builder(
            &self.http_client,
            &args.spam,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.tlsVersion,
            &args.tldCategory,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_top_tlds_by_spam_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get email security top tlds by spoof.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_email_security_top_tlds_by_spoof(
        &self,
        args: &RadarGetEmailSecurityTopTldsBySpoofArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_email_security_top_tlds_by_spoof_builder(
            &self.http_client,
            &args.spoof,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.arc,
            &args.dkim,
            &args.dmarc,
            &args.spf,
            &args.tlsVersion,
            &args.tldCategory,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_email_security_top_tlds_by_spoof_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get entities asn list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_entities_asn_list(
        &self,
        args: &RadarGetEntitiesAsnListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_entities_asn_list_builder(
            &self.http_client,
            &args.limit,
            &args.offset,
            &args.asn,
            &args.location,
            &args.orderBy,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_entities_asn_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get as botnet threat feed.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_as_botnet_threat_feed(
        &self,
        args: &RadarGetAsBotnetThreatFeedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_as_botnet_threat_feed_builder(
            &self.http_client,
            &args.limit,
            &args.offset,
            &args.metric,
            &args.date,
            &args.compareDateRange,
            &args.location,
            &args.asn,
            &args.sortOrder,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_as_botnet_threat_feed_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get entities asn by ip.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_entities_asn_by_ip(
        &self,
        args: &RadarGetEntitiesAsnByIpArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_entities_asn_by_ip_builder(
            &self.http_client,
            &args.ip,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_entities_asn_by_ip_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get entities asn by id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_entities_asn_by_id(
        &self,
        args: &RadarGetEntitiesAsnByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_entities_asn_by_id_builder(
            &self.http_client,
            &args.asn,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_entities_asn_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get asns as set.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_asns_as_set(
        &self,
        args: &RadarGetAsnsAsSetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_asns_as_set_builder(
            &self.http_client,
            &args.asn,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_asns_as_set_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get asns rel.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_asns_rel(
        &self,
        args: &RadarGetAsnsRelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_asns_rel_builder(
            &self.http_client,
            &args.asn,
            &args.asn2,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_asns_rel_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get entities ip.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_entities_ip(
        &self,
        args: &RadarGetEntitiesIpArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_entities_ip_builder(
            &self.http_client,
            &args.ip,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_entities_ip_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get entities locations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_entities_locations(
        &self,
        args: &RadarGetEntitiesLocationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_entities_locations_builder(
            &self.http_client,
            &args.limit,
            &args.offset,
            &args.location,
            &args.region,
            &args.subregion,
            &args.continent,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_entities_locations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get entities location by alpha2.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_entities_location_by_alpha2(
        &self,
        args: &RadarGetEntitiesLocationByAlpha2Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_entities_location_by_alpha2_builder(
            &self.http_client,
            &args.location,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_entities_location_by_alpha2_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get geolocations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_geolocations(
        &self,
        args: &RadarGetGeolocationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_geolocations_builder(
            &self.http_client,
            &args.limit,
            &args.offset,
            &args.geoId,
            &args.location,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_geolocations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get geolocation details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_geolocation_details(
        &self,
        args: &RadarGetGeolocationDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_geolocation_details_builder(
            &self.http_client,
            &args.geo_id,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_geolocation_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http summary by bot class.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_summary_by_bot_class(
        &self,
        args: &RadarGetHttpSummaryByBotClassArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_summary_by_bot_class_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_summary_by_bot_class_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http summary by device type.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_summary_by_device_type(
        &self,
        args: &RadarGetHttpSummaryByDeviceTypeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_summary_by_device_type_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_summary_by_device_type_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http summary by http protocol.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_summary_by_http_protocol(
        &self,
        args: &RadarGetHttpSummaryByHttpProtocolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_summary_by_http_protocol_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_summary_by_http_protocol_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http summary by http version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_summary_by_http_version(
        &self,
        args: &RadarGetHttpSummaryByHttpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_summary_by_http_version_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_summary_by_http_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http summary by ip version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_summary_by_ip_version(
        &self,
        args: &RadarGetHttpSummaryByIpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_summary_by_ip_version_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_summary_by_ip_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http summary by operating system.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_summary_by_operating_system(
        &self,
        args: &RadarGetHttpSummaryByOperatingSystemArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_summary_by_operating_system_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_summary_by_operating_system_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http summary by post quantum.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_summary_by_post_quantum(
        &self,
        args: &RadarGetHttpSummaryByPostQuantumArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_summary_by_post_quantum_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_summary_by_post_quantum_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http summary by tls version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_summary_by_tls_version(
        &self,
        args: &RadarGetHttpSummaryByTlsVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_summary_by_tls_version_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_summary_by_tls_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_summary(
        &self,
        args: &RadarGetHttpSummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_summary_builder(
            &self.http_client,
            &args.dimension,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http timeseries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_timeseries(
        &self,
        args: &RadarGetHttpTimeseriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_timeseries_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.normalization,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_timeseries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http timeseries group by bot class.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_timeseries_group_by_bot_class(
        &self,
        args: &RadarGetHttpTimeseriesGroupByBotClassArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_timeseries_group_by_bot_class_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_timeseries_group_by_bot_class_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http timeseries group by browsers.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_timeseries_group_by_browsers(
        &self,
        args: &RadarGetHttpTimeseriesGroupByBrowsersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_timeseries_group_by_browsers_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_timeseries_group_by_browsers_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http timeseries group by browser families.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_timeseries_group_by_browser_families(
        &self,
        args: &RadarGetHttpTimeseriesGroupByBrowserFamiliesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_timeseries_group_by_browser_families_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_timeseries_group_by_browser_families_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http timeseries group by device type.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_timeseries_group_by_device_type(
        &self,
        args: &RadarGetHttpTimeseriesGroupByDeviceTypeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_timeseries_group_by_device_type_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_timeseries_group_by_device_type_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http timeseries group by http protocol.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_timeseries_group_by_http_protocol(
        &self,
        args: &RadarGetHttpTimeseriesGroupByHttpProtocolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_timeseries_group_by_http_protocol_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_timeseries_group_by_http_protocol_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http timeseries group by http version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_timeseries_group_by_http_version(
        &self,
        args: &RadarGetHttpTimeseriesGroupByHttpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_timeseries_group_by_http_version_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_timeseries_group_by_http_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http timeseries group by ip version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_timeseries_group_by_ip_version(
        &self,
        args: &RadarGetHttpTimeseriesGroupByIpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_timeseries_group_by_ip_version_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_timeseries_group_by_ip_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http timeseries group by operating system.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_timeseries_group_by_operating_system(
        &self,
        args: &RadarGetHttpTimeseriesGroupByOperatingSystemArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_timeseries_group_by_operating_system_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_timeseries_group_by_operating_system_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http timeseries group by post quantum.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_timeseries_group_by_post_quantum(
        &self,
        args: &RadarGetHttpTimeseriesGroupByPostQuantumArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_timeseries_group_by_post_quantum_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_timeseries_group_by_post_quantum_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http timeseries group by tls version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_timeseries_group_by_tls_version(
        &self,
        args: &RadarGetHttpTimeseriesGroupByTlsVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_timeseries_group_by_tls_version_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_timeseries_group_by_tls_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http timeseries group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_timeseries_group(
        &self,
        args: &RadarGetHttpTimeseriesGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_timeseries_group_builder(
            &self.http_client,
            &args.dimension,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.limitPerGroup,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.normalization,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_timeseries_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top ases by http requests.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_ases_by_http_requests(
        &self,
        args: &RadarGetHttpTopAsesByHttpRequestsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_ases_by_http_requests_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_ases_by_http_requests_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top ases by bot class.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_ases_by_bot_class(
        &self,
        args: &RadarGetHttpTopAsesByBotClassArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_ases_by_bot_class_builder(
            &self.http_client,
            &args.bot_class,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_ases_by_bot_class_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top ases by browser family.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_ases_by_browser_family(
        &self,
        args: &RadarGetHttpTopAsesByBrowserFamilyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_ases_by_browser_family_builder(
            &self.http_client,
            &args.browser_family,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_ases_by_browser_family_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top ases by device type.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_ases_by_device_type(
        &self,
        args: &RadarGetHttpTopAsesByDeviceTypeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_ases_by_device_type_builder(
            &self.http_client,
            &args.device_type,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_ases_by_device_type_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top ases by http protocol.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_ases_by_http_protocol(
        &self,
        args: &RadarGetHttpTopAsesByHttpProtocolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_ases_by_http_protocol_builder(
            &self.http_client,
            &args.http_protocol,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_ases_by_http_protocol_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top ases by http version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_ases_by_http_version(
        &self,
        args: &RadarGetHttpTopAsesByHttpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_ases_by_http_version_builder(
            &self.http_client,
            &args.http_version,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_ases_by_http_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top ases by ip version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_ases_by_ip_version(
        &self,
        args: &RadarGetHttpTopAsesByIpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_ases_by_ip_version_builder(
            &self.http_client,
            &args.ip_version,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_ases_by_ip_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top ases by operating system.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_ases_by_operating_system(
        &self,
        args: &RadarGetHttpTopAsesByOperatingSystemArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_ases_by_operating_system_builder(
            &self.http_client,
            &args.os,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_ases_by_operating_system_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top ases by tls version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_ases_by_tls_version(
        &self,
        args: &RadarGetHttpTopAsesByTlsVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_ases_by_tls_version_builder(
            &self.http_client,
            &args.tls_version,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_ases_by_tls_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top browsers.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_browsers(
        &self,
        args: &RadarGetHttpTopBrowsersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_browsers_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_browsers_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top browser families.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_browser_families(
        &self,
        args: &RadarGetHttpTopBrowserFamiliesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_browser_families_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_browser_families_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top locations by http requests.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_locations_by_http_requests(
        &self,
        args: &RadarGetHttpTopLocationsByHttpRequestsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_locations_by_http_requests_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_locations_by_http_requests_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top locations by bot class.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_locations_by_bot_class(
        &self,
        args: &RadarGetHttpTopLocationsByBotClassArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_locations_by_bot_class_builder(
            &self.http_client,
            &args.bot_class,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_locations_by_bot_class_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top locations by browser family.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_locations_by_browser_family(
        &self,
        args: &RadarGetHttpTopLocationsByBrowserFamilyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_locations_by_browser_family_builder(
            &self.http_client,
            &args.browser_family,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_locations_by_browser_family_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top locations by device type.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_locations_by_device_type(
        &self,
        args: &RadarGetHttpTopLocationsByDeviceTypeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_locations_by_device_type_builder(
            &self.http_client,
            &args.device_type,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_locations_by_device_type_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top locations by http protocol.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_locations_by_http_protocol(
        &self,
        args: &RadarGetHttpTopLocationsByHttpProtocolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_locations_by_http_protocol_builder(
            &self.http_client,
            &args.http_protocol,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_locations_by_http_protocol_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top locations by http version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_locations_by_http_version(
        &self,
        args: &RadarGetHttpTopLocationsByHttpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_locations_by_http_version_builder(
            &self.http_client,
            &args.http_version,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.ipVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_locations_by_http_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top locations by ip version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_locations_by_ip_version(
        &self,
        args: &RadarGetHttpTopLocationsByIpVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_locations_by_ip_version_builder(
            &self.http_client,
            &args.ip_version,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.os,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_locations_by_ip_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top locations by operating system.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_locations_by_operating_system(
        &self,
        args: &RadarGetHttpTopLocationsByOperatingSystemArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_locations_by_operating_system_builder(
            &self.http_client,
            &args.os,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.tlsVersion,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_locations_by_operating_system_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get http top locations by tls version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_http_top_locations_by_tls_version(
        &self,
        args: &RadarGetHttpTopLocationsByTlsVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_http_top_locations_by_tls_version_builder(
            &self.http_client,
            &args.tls_version,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.botClass,
            &args.deviceType,
            &args.httpProtocol,
            &args.httpVersion,
            &args.ipVersion,
            &args.os,
            &args.browserFamily,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_http_top_locations_by_tls_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get leaked credential checks summary by bot class.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_leaked_credential_checks_summary_by_bot_class(
        &self,
        args: &RadarGetLeakedCredentialChecksSummaryByBotClassArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_leaked_credential_checks_summary_by_bot_class_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.compromised,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_leaked_credential_checks_summary_by_bot_class_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get leaked credential checks summary by compromised.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_leaked_credential_checks_summary_by_compromised(
        &self,
        args: &RadarGetLeakedCredentialChecksSummaryByCompromisedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_leaked_credential_checks_summary_by_compromised_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.botClass,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_leaked_credential_checks_summary_by_compromised_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get leaked credential checks summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_leaked_credential_checks_summary(
        &self,
        args: &RadarGetLeakedCredentialChecksSummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_leaked_credential_checks_summary_builder(
            &self.http_client,
            &args.dimension,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.botClass,
            &args.compromised,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_leaked_credential_checks_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get leaked credential checks timeseries group by bot class.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_leaked_credential_checks_timeseries_group_by_bot_class(
        &self,
        args: &RadarGetLeakedCredentialChecksTimeseriesGroupByBotClassArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_leaked_credential_checks_timeseries_group_by_bot_class_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.compromised,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_leaked_credential_checks_timeseries_group_by_bot_class_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get leaked credential checks timeseries group by compromised.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_leaked_credential_checks_timeseries_group_by_compromised(
        &self,
        args: &RadarGetLeakedCredentialChecksTimeseriesGroupByCompromisedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_leaked_credential_checks_timeseries_group_by_compromised_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.botClass,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_leaked_credential_checks_timeseries_group_by_compromised_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get leaked credential checks timeseries group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_leaked_credential_checks_timeseries_group(
        &self,
        args: &RadarGetLeakedCredentialChecksTimeseriesGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_leaked_credential_checks_timeseries_group_builder(
            &self.http_client,
            &args.dimension,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.botClass,
            &args.compromised,
            &args.checkResult,
            &args.limitPerGroup,
            &args.normalization,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_leaked_credential_checks_timeseries_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get netflows summary deprecated.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_netflows_summary_deprecated(
        &self,
        args: &RadarGetNetflowsSummaryDeprecatedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_netflows_summary_deprecated_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_netflows_summary_deprecated_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get netflows summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_netflows_summary(
        &self,
        args: &RadarGetNetflowsSummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_netflows_summary_builder(
            &self.http_client,
            &args.dimension,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.product,
            &args.limitPerGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_netflows_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get netflows timeseries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_netflows_timeseries(
        &self,
        args: &RadarGetNetflowsTimeseriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_netflows_timeseries_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.product,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.normalization,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_netflows_timeseries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get netflows timeseries group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_netflows_timeseries_group(
        &self,
        args: &RadarGetNetflowsTimeseriesGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_netflows_timeseries_group_builder(
            &self.http_client,
            &args.dimension,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.limitPerGroup,
            &args.normalization,
            &args.product,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_netflows_timeseries_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get netflows top ases.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_netflows_top_ases(
        &self,
        args: &RadarGetNetflowsTopAsesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_netflows_top_ases_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_netflows_top_ases_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get netflows top locations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_netflows_top_locations(
        &self,
        args: &RadarGetNetflowsTopLocationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_netflows_top_locations_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.geoId,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_netflows_top_locations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get origins.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_origins(
        &self,
        args: &RadarGetOriginsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_origins_builder(
            &self.http_client,
            &args.limit,
            &args.offset,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_origins_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get origins summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_origins_summary(
        &self,
        args: &RadarGetOriginsSummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_origins_summary_builder(
            &self.http_client,
            &args.dimension,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.limitPerGroup,
            &args.origin,
            &args.metric,
            &args.region,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_origins_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get origins timeseries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_origins_timeseries(
        &self,
        args: &RadarGetOriginsTimeseriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_origins_timeseries_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.origin,
            &args.metric,
            &args.region,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_origins_timeseries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get origins timeseries group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_origins_timeseries_group(
        &self,
        args: &RadarGetOriginsTimeseriesGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_origins_timeseries_group_builder(
            &self.http_client,
            &args.dimension,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.limitPerGroup,
            &args.origin,
            &args.metric,
            &args.region,
            &args.normalization,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_origins_timeseries_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get origin details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_origin_details(
        &self,
        args: &RadarGetOriginDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_origin_details_builder(
            &self.http_client,
            &args.slug,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_origin_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get origin post quantum summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_origin_post_quantum_summary(
        &self,
        args: &RadarGetOriginPostQuantumSummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_origin_post_quantum_summary_builder(
            &self.http_client,
            &args.dimension,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_origin_post_quantum_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get origin post quantum timeseries groups.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_origin_post_quantum_timeseries_groups(
        &self,
        args: &RadarGetOriginPostQuantumTimeseriesGroupsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_origin_post_quantum_timeseries_groups_builder(
            &self.http_client,
            &args.dimension,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_origin_post_quantum_timeseries_groups_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get post quantum tls support.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_post_quantum_tls_support(
        &self,
        args: &RadarGetPostQuantumTlsSupportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_post_quantum_tls_support_builder(
            &self.http_client,
            &args.host,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_post_quantum_tls_support_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get quality index summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_quality_index_summary(
        &self,
        args: &RadarGetQualityIndexSummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_quality_index_summary_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.metric,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_quality_index_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get quality index timeseries group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_quality_index_timeseries_group(
        &self,
        args: &RadarGetQualityIndexTimeseriesGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_quality_index_timeseries_group_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.interpolation,
            &args.metric,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_quality_index_timeseries_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get quality speed histogram.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_quality_speed_histogram(
        &self,
        args: &RadarGetQualitySpeedHistogramArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_quality_speed_histogram_builder(
            &self.http_client,
            &args.name,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.bucketSize,
            &args.metricGroup,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_quality_speed_histogram_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get quality speed summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_quality_speed_summary(
        &self,
        args: &RadarGetQualitySpeedSummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_quality_speed_summary_builder(
            &self.http_client,
            &args.name,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_quality_speed_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get quality speed top ases.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_quality_speed_top_ases(
        &self,
        args: &RadarGetQualitySpeedTopAsesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_quality_speed_top_ases_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.orderBy,
            &args.reverse,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_quality_speed_top_ases_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get quality speed top locations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_quality_speed_top_locations(
        &self,
        args: &RadarGetQualitySpeedTopLocationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_quality_speed_top_locations_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.orderBy,
            &args.reverse,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_quality_speed_top_locations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ranking domain details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ranking_domain_details(
        &self,
        args: &RadarGetRankingDomainDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ranking_domain_details_builder(
            &self.http_client,
            &args.domain,
            &args.limit,
            &args.rankingType,
            &args.name,
            &args.includeTopLocations,
            &args.date,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ranking_domain_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ranking internet services categories.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ranking_internet_services_categories(
        &self,
        args: &RadarGetRankingInternetServicesCategoriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ranking_internet_services_categories_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.date,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ranking_internet_services_categories_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ranking internet services timeseries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ranking_internet_services_timeseries(
        &self,
        args: &RadarGetRankingInternetServicesTimeseriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ranking_internet_services_timeseries_builder(
            &self.http_client,
            &args.serviceCategory,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ranking_internet_services_timeseries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ranking top internet services.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ranking_top_internet_services(
        &self,
        args: &RadarGetRankingTopInternetServicesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ranking_top_internet_services_builder(
            &self.http_client,
            &args.serviceCategory,
            &args.limit,
            &args.name,
            &args.date,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ranking_top_internet_services_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ranking domain timeseries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ranking_domain_timeseries(
        &self,
        args: &RadarGetRankingDomainTimeseriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ranking_domain_timeseries_builder(
            &self.http_client,
            &args.limit,
            &args.rankingType,
            &args.name,
            &args.location,
            &args.domains,
            &args.domainCategory,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ranking_domain_timeseries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get ranking top domains.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_ranking_top_domains(
        &self,
        args: &RadarGetRankingTopDomainsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_ranking_top_domains_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.location,
            &args.domainCategory,
            &args.date,
            &args.rankingType,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_ranking_top_domains_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get robots txt top domain categories by files parsed.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_robots_txt_top_domain_categories_by_files_parsed(
        &self,
        args: &RadarGetRobotsTxtTopDomainCategoriesByFilesParsedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_robots_txt_top_domain_categories_by_files_parsed_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.userAgentCategory,
            &args.date,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_robots_txt_top_domain_categories_by_files_parsed_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get robots txt top user agents by directive.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_robots_txt_top_user_agents_by_directive(
        &self,
        args: &RadarGetRobotsTxtTopUserAgentsByDirectiveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_robots_txt_top_user_agents_by_directive_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.userAgentCategory,
            &args.date,
            &args.domainCategory,
            &args.directive,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_robots_txt_top_user_agents_by_directive_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get search global.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_search_global(
        &self,
        args: &RadarGetSearchGlobalArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_search_global_builder(
            &self.http_client,
            &args.limit,
            &args.limitPerGroup,
            &args.query,
            &args.include,
            &args.exclude,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_search_global_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get tcp resets timeouts summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_tcp_resets_timeouts_summary(
        &self,
        args: &RadarGetTcpResetsTimeoutsSummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_tcp_resets_timeouts_summary_builder(
            &self.http_client,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_tcp_resets_timeouts_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get tcp resets timeouts timeseries group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_tcp_resets_timeouts_timeseries_group(
        &self,
        args: &RadarGetTcpResetsTimeoutsTimeseriesGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_tcp_resets_timeouts_timeseries_group_builder(
            &self.http_client,
            &args.aggInterval,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_tcp_resets_timeouts_timeseries_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get tlds.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_tlds(
        &self,
        args: &RadarGetTldsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_tlds_builder(
            &self.http_client,
            &args.limit,
            &args.offset,
            &args.tldManager,
            &args.tldType,
            &args.tld,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_tlds_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get tld details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_tld_details(
        &self,
        args: &RadarGetTldDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_tld_details_builder(
            &self.http_client,
            &args.tld,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_tld_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get traffic anomalies.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_traffic_anomalies(
        &self,
        args: &RadarGetTrafficAnomaliesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_traffic_anomalies_builder(
            &self.http_client,
            &args.limit,
            &args.offset,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.status,
            &args.type_rs,
            &args.asn,
            &args.location,
            &args.origin,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_traffic_anomalies_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get traffic anomalies top.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_traffic_anomalies_top(
        &self,
        args: &RadarGetTrafficAnomaliesTopArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_traffic_anomalies_top_builder(
            &self.http_client,
            &args.limit,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.status,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_traffic_anomalies_top_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get verified bots top by http requests.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_verified_bots_top_by_http_requests(
        &self,
        args: &RadarGetVerifiedBotsTopByHttpRequestsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_verified_bots_top_by_http_requests_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_verified_bots_top_by_http_requests_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Radar get verified bots top categories by http requests.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn radar_get_verified_bots_top_categories_by_http_requests(
        &self,
        args: &RadarGetVerifiedBotsTopCategoriesByHttpRequestsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = radar_get_verified_bots_top_categories_by_http_requests_builder(
            &self.http_client,
            &args.limit,
            &args.name,
            &args.dateRange,
            &args.dateStart,
            &args.dateEnd,
            &args.asn,
            &args.location,
            &args.continent,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = radar_get_verified_bots_top_categories_by_http_requests_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store system list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreStoresResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn secrets_store_system_list(
        &self,
        args: &SecretsStoreSystemListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreStoresResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_system_list_builder(
            &self.http_client,
            &args.account_tag,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_system_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store system create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreStoreResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secrets_store_system_create(
        &self,
        args: &SecretsStoreSystemCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreStoreResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_system_create_builder(
            &self.http_client,
            &args.account_tag,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_system_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store system get store by id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreStoreResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn secrets_store_system_get_store_by_id(
        &self,
        args: &SecretsStoreSystemGetStoreByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreStoreResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_system_get_store_by_id_builder(
            &self.http_client,
            &args.store_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_system_get_store_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store system delete by id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreStoreResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secrets_store_system_delete_by_id(
        &self,
        args: &SecretsStoreSystemDeleteByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreStoreResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_system_delete_by_id_builder(
            &self.http_client,
            &args.store_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_system_delete_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store system secrets list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreSecretsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn secrets_store_system_secrets_list(
        &self,
        args: &SecretsStoreSystemSecretsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreSecretsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_system_secrets_list_builder(
            &self.http_client,
            &args.store_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_system_secrets_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store system secret create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreSecretsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secrets_store_system_secret_create(
        &self,
        args: &SecretsStoreSystemSecretCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreSecretsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_system_secret_create_builder(
            &self.http_client,
            &args.store_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_system_secret_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store system delete bulk.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreSecretsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secrets_store_system_delete_bulk(
        &self,
        args: &SecretsStoreSystemDeleteBulkArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreSecretsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_system_delete_bulk_builder(
            &self.http_client,
            &args.store_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_system_delete_bulk_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store system get by id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreSecretResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn secrets_store_system_get_by_id(
        &self,
        args: &SecretsStoreSystemGetByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreSecretResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_system_get_by_id_builder(
            &self.http_client,
            &args.store_id,
            &args.secret_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_system_get_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store system patch by id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreSecretResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secrets_store_system_patch_by_id(
        &self,
        args: &SecretsStoreSystemPatchByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreSecretResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_system_patch_by_id_builder(
            &self.http_client,
            &args.store_id,
            &args.secret_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_system_patch_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store system secret delete by id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreSecretResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secrets_store_system_secret_delete_by_id(
        &self,
        args: &SecretsStoreSystemSecretDeleteByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreSecretResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_system_secret_delete_by_id_builder(
            &self.http_client,
            &args.store_id,
            &args.secret_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_system_secret_delete_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secrets store system duplicate by id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecretsStoreSecretResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secrets_store_system_duplicate_by_id(
        &self,
        args: &SecretsStoreSystemDuplicateByIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecretsStoreSecretResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secrets_store_system_duplicate_by_id_builder(
            &self.http_client,
            &args.store_id,
            &args.secret_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secrets_store_system_duplicate_by_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tenants retrieve tenant.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn tenants_retrieve_tenant(
        &self,
        args: &TenantsRetrieveTenantArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tenants_retrieve_tenant_builder(
            &self.http_client,
            &args.tenant_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tenants_retrieve_tenant_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tenants valid account types.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn tenants_valid_account_types(
        &self,
        args: &TenantsValidAccountTypesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tenants_valid_account_types_builder(
            &self.http_client,
            &args.tenant_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tenants_valid_account_types_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tenants list accounts.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn tenants_list_accounts(
        &self,
        args: &TenantsListAccountsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tenants_list_accounts_builder(
            &self.http_client,
            &args.tenant_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tenants_list_accounts_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tenants list entitlements.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn tenants_list_entitlements(
        &self,
        args: &TenantsListEntitlementsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tenants_list_entitlements_builder(
            &self.http_client,
            &args.tenant_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tenants_list_entitlements_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tenants list memberships.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn tenants_list_memberships(
        &self,
        args: &TenantsListMembershipsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tenants_list_memberships_builder(
            &self.http_client,
            &args.tenant_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tenants_list_memberships_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User user details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSingleUserResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn user_user_details(
        &self,
        args: &UserUserDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSingleUserResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_user_details_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = user_user_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User edit user.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSingleUserResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn user_edit_user(
        &self,
        args: &UserEditUserArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSingleUserResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_edit_user_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = user_edit_user_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Audit logs get user audit logs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AaaAuditLogsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn audit_logs_get_user_audit_logs(
        &self,
        args: &AuditLogsGetUserAuditLogsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AaaAuditLogsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = audit_logs_get_user_audit_logs_builder(
            &self.http_client,
            &args.id,
            &args.export,
            &args.action_type,
            &args.actor_ip,
            &args.actor_email,
            &args.since,
            &args.before,
            &args.zone_name,
            &args.direction,
            &args.per_page,
            &args.page,
            &args.hide_user_logs,
        )
        .map_err(ProviderError::Api)?;

        let task = audit_logs_get_user_audit_logs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User billing history deprecated billing history details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BillSubsApiBillingHistoryCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn user_billing_history_deprecated_billing_history_details(
        &self,
        args: &UserBillingHistoryDeprecatedBillingHistoryDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BillSubsApiBillingHistoryCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_billing_history_deprecated_billing_history_details_builder(
            &self.http_client,
            &args.page,
            &args.per_page,
            &args.order,
            &args.occurred_at,
            &args.type_rs,
            &args.action,
        )
        .map_err(ProviderError::Api)?;

        let task = user_billing_history_deprecated_billing_history_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User billing profile deprecated billing profile details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BillSubsApiBillingResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn user_billing_profile_deprecated_billing_profile_details(
        &self,
        args: &UserBillingProfileDeprecatedBillingProfileDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BillSubsApiBillingResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_billing_profile_deprecated_billing_profile_details_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = user_billing_profile_deprecated_billing_profile_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip access rules for a user list ip access rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallRuleCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ip_access_rules_for_a_user_list_ip_access_rules(
        &self,
        args: &IpAccessRulesForAUserListIpAccessRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallRuleCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_access_rules_for_a_user_list_ip_access_rules_builder(
            &self.http_client,
            &args.mode,
            &args.configuration_target,
            &args.configuration_value,
            &args.notes,
            &args.match_rs,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_access_rules_for_a_user_list_ip_access_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip access rules for a user create an ip access rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallRuleSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_access_rules_for_a_user_create_an_ip_access_rule(
        &self,
        args: &IpAccessRulesForAUserCreateAnIpAccessRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallRuleSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_access_rules_for_a_user_create_an_ip_access_rule_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_access_rules_for_a_user_create_an_ip_access_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip access rules for a user update an ip access rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallRuleSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_access_rules_for_a_user_update_an_ip_access_rule(
        &self,
        args: &IpAccessRulesForAUserUpdateAnIpAccessRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallRuleSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_access_rules_for_a_user_update_an_ip_access_rule_builder(
            &self.http_client,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_access_rules_for_a_user_update_an_ip_access_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip access rules for a user delete an ip access rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallRuleSingleIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_access_rules_for_a_user_delete_an_ip_access_rule(
        &self,
        args: &IpAccessRulesForAUserDeleteAnIpAccessRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallRuleSingleIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_access_rules_for_a_user_delete_an_ip_access_rule_builder(
            &self.http_client,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_access_rules_for_a_user_delete_an_ip_access_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User s invites list invitations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSchemasCollectionInviteResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn user_s_invites_list_invitations(
        &self,
        args: &UserSInvitesListInvitationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSchemasCollectionInviteResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_s_invites_list_invitations_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = user_s_invites_list_invitations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User s invites invitation details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSingleInviteResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn user_s_invites_invitation_details(
        &self,
        args: &UserSInvitesInvitationDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSingleInviteResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_s_invites_invitation_details_builder(
            &self.http_client,
            &args.invite_id,
        )
        .map_err(ProviderError::Api)?;

        let task = user_s_invites_invitation_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User s invites respond to invitation.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSingleInviteResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn user_s_invites_respond_to_invitation(
        &self,
        args: &UserSInvitesRespondToInvitationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSingleInviteResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_s_invites_respond_to_invitation_builder(
            &self.http_client,
            &args.invite_id,
        )
        .map_err(ProviderError::Api)?;

        let task = user_s_invites_respond_to_invitation_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer monitors list monitors.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingMonitorResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn load_balancer_monitors_list_monitors(
        &self,
        args: &LoadBalancerMonitorsListMonitorsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingMonitorResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_monitors_list_monitors_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_monitors_list_monitors_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer monitors create monitor.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingMonitorResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn load_balancer_monitors_create_monitor(
        &self,
        args: &LoadBalancerMonitorsCreateMonitorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingMonitorResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_monitors_create_monitor_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_monitors_create_monitor_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer monitors monitor details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingMonitorResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn load_balancer_monitors_monitor_details(
        &self,
        args: &LoadBalancerMonitorsMonitorDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingMonitorResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_monitors_monitor_details_builder(
            &self.http_client,
            &args.monitor_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_monitors_monitor_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer monitors update monitor.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingMonitorResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn load_balancer_monitors_update_monitor(
        &self,
        args: &LoadBalancerMonitorsUpdateMonitorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingMonitorResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_monitors_update_monitor_builder(
            &self.http_client,
            &args.monitor_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_monitors_update_monitor_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer monitors patch monitor.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingMonitorResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn load_balancer_monitors_patch_monitor(
        &self,
        args: &LoadBalancerMonitorsPatchMonitorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingMonitorResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_monitors_patch_monitor_builder(
            &self.http_client,
            &args.monitor_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_monitors_patch_monitor_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer monitors delete monitor.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn load_balancer_monitors_delete_monitor(
        &self,
        args: &LoadBalancerMonitorsDeleteMonitorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_monitors_delete_monitor_builder(
            &self.http_client,
            &args.monitor_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_monitors_delete_monitor_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer monitors preview monitor.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingPreviewResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn load_balancer_monitors_preview_monitor(
        &self,
        args: &LoadBalancerMonitorsPreviewMonitorArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingPreviewResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_monitors_preview_monitor_builder(
            &self.http_client,
            &args.monitor_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_monitors_preview_monitor_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer monitors list monitor references.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingMonitorReferencesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn load_balancer_monitors_list_monitor_references(
        &self,
        args: &LoadBalancerMonitorsListMonitorReferencesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingMonitorReferencesResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_monitors_list_monitor_references_builder(
            &self.http_client,
            &args.monitor_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_monitors_list_monitor_references_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer pools list pools.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn load_balancer_pools_list_pools(
        &self,
        args: &LoadBalancerPoolsListPoolsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_pools_list_pools_builder(
            &self.http_client,
            &args.monitor,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_pools_list_pools_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer pools create pool.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn load_balancer_pools_create_pool(
        &self,
        args: &LoadBalancerPoolsCreatePoolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_pools_create_pool_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_pools_create_pool_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer pools patch pools.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn load_balancer_pools_patch_pools(
        &self,
        args: &LoadBalancerPoolsPatchPoolsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_pools_patch_pools_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_pools_patch_pools_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer pools pool details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn load_balancer_pools_pool_details(
        &self,
        args: &LoadBalancerPoolsPoolDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_pools_pool_details_builder(
            &self.http_client,
            &args.pool_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_pools_pool_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer pools update pool.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn load_balancer_pools_update_pool(
        &self,
        args: &LoadBalancerPoolsUpdatePoolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_pools_update_pool_builder(
            &self.http_client,
            &args.pool_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_pools_update_pool_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer pools patch pool.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn load_balancer_pools_patch_pool(
        &self,
        args: &LoadBalancerPoolsPatchPoolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_pools_patch_pool_builder(
            &self.http_client,
            &args.pool_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_pools_patch_pool_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer pools delete pool.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingSchemasIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn load_balancer_pools_delete_pool(
        &self,
        args: &LoadBalancerPoolsDeletePoolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingSchemasIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_pools_delete_pool_builder(
            &self.http_client,
            &args.pool_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_pools_delete_pool_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer pools pool health details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingHealthDetails result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn load_balancer_pools_pool_health_details(
        &self,
        args: &LoadBalancerPoolsPoolHealthDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingHealthDetails, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_pools_pool_health_details_builder(
            &self.http_client,
            &args.pool_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_pools_pool_health_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer pools preview pool.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingPreviewResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn load_balancer_pools_preview_pool(
        &self,
        args: &LoadBalancerPoolsPreviewPoolArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingPreviewResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_pools_preview_pool_builder(
            &self.http_client,
            &args.pool_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_pools_preview_pool_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer pools list pool references.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingPoolsReferencesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn load_balancer_pools_list_pool_references(
        &self,
        args: &LoadBalancerPoolsListPoolReferencesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingPoolsReferencesResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_pools_list_pool_references_builder(
            &self.http_client,
            &args.pool_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_pools_list_pool_references_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer monitors preview result.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingPreviewResultResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn load_balancer_monitors_preview_result(
        &self,
        args: &LoadBalancerMonitorsPreviewResultArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingPreviewResultResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_monitors_preview_result_builder(
            &self.http_client,
            &args.preview_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_monitors_preview_result_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancer healthcheck events list healthcheck events.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn load_balancer_healthcheck_events_list_healthcheck_events(
        &self,
        args: &LoadBalancerHealthcheckEventsListHealthcheckEventsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancer_healthcheck_events_list_healthcheck_events_builder(
            &self.http_client,
            &args.until,
            &args.pool_name,
            &args.origin_healthy,
            &args.pool_id,
            &args.since,
            &args.origin_name,
            &args.pool_healthy,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancer_healthcheck_events_list_healthcheck_events_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User s organizations list organizations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamCollectionOrganizationResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn user_s_organizations_list_organizations(
        &self,
        args: &UserSOrganizationsListOrganizationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamCollectionOrganizationResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_s_organizations_list_organizations_builder(
            &self.http_client,
            &args.name,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
            &args.match_rs,
            &args.status,
        )
        .map_err(ProviderError::Api)?;

        let task = user_s_organizations_list_organizations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User s organizations organization details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSingleOrganizationResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn user_s_organizations_organization_details(
        &self,
        args: &UserSOrganizationsOrganizationDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSingleOrganizationResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_s_organizations_organization_details_builder(
            &self.http_client,
            &args.organization_id,
        )
        .map_err(ProviderError::Api)?;

        let task = user_s_organizations_organization_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User s organizations leave organization.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn user_s_organizations_leave_organization(
        &self,
        args: &UserSOrganizationsLeaveOrganizationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_s_organizations_leave_organization_builder(
            &self.http_client,
            &args.organization_id,
        )
        .map_err(ProviderError::Api)?;

        let task = user_s_organizations_leave_organization_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User subscription get user subscriptions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BillSubsApiUserSubscriptionResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn user_subscription_get_user_subscriptions(
        &self,
        args: &UserSubscriptionGetUserSubscriptionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BillSubsApiUserSubscriptionResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_subscription_get_user_subscriptions_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = user_subscription_get_user_subscriptions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User subscription update user subscription.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BillSubsApiUserSubscriptionResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn user_subscription_update_user_subscription(
        &self,
        args: &UserSubscriptionUpdateUserSubscriptionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BillSubsApiUserSubscriptionResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_subscription_update_user_subscription_builder(
            &self.http_client,
            &args.identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = user_subscription_update_user_subscription_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User subscription delete user subscription.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn user_subscription_delete_user_subscription(
        &self,
        args: &UserSubscriptionDeleteUserSubscriptionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_subscription_delete_user_subscription_builder(
            &self.http_client,
            &args.identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = user_subscription_delete_user_subscription_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User api tokens list tokens.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamCollectionTokensResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn user_api_tokens_list_tokens(
        &self,
        args: &UserApiTokensListTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamCollectionTokensResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_api_tokens_list_tokens_builder(
            &self.http_client,
            &args.page,
            &args.per_page,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = user_api_tokens_list_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User api tokens create token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSingleTokenCreateResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn user_api_tokens_create_token(
        &self,
        args: &UserApiTokensCreateTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSingleTokenCreateResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_api_tokens_create_token_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = user_api_tokens_create_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Permission groups list permission groups.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamPermissionsGroupResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn permission_groups_list_permission_groups(
        &self,
        args: &PermissionGroupsListPermissionGroupsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamPermissionsGroupResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = permission_groups_list_permission_groups_builder(
            &self.http_client,
            &args.name,
            &args.scope,
        )
        .map_err(ProviderError::Api)?;

        let task = permission_groups_list_permission_groups_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User api tokens verify token.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamTokenVerifyResponseSingleSegment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn user_api_tokens_verify_token(
        &self,
        args: &UserApiTokensVerifyTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamTokenVerifyResponseSingleSegment, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_api_tokens_verify_token_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = user_api_tokens_verify_token_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User api tokens token details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSingleTokenResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn user_api_tokens_token_details(
        &self,
        args: &UserApiTokensTokenDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSingleTokenResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_api_tokens_token_details_builder(
            &self.http_client,
            &args.token_id,
        )
        .map_err(ProviderError::Api)?;

        let task = user_api_tokens_token_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User api tokens update token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamSingleTokenResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn user_api_tokens_update_token(
        &self,
        args: &UserApiTokensUpdateTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamSingleTokenResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_api_tokens_update_token_builder(
            &self.http_client,
            &args.token_id,
        )
        .map_err(ProviderError::Api)?;

        let task = user_api_tokens_update_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User api tokens delete token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamApiResponseSingleId result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn user_api_tokens_delete_token(
        &self,
        args: &UserApiTokensDeleteTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamApiResponseSingleId, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_api_tokens_delete_token_builder(
            &self.http_client,
            &args.token_id,
        )
        .map_err(ProviderError::Api)?;

        let task = user_api_tokens_delete_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User api tokens roll token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IamResponseSingleValue result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn user_api_tokens_roll_token(
        &self,
        args: &UserApiTokensRollTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IamResponseSingleValue, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_api_tokens_roll_token_builder(
            &self.http_client,
            &args.token_id,
        )
        .map_err(ProviderError::Api)?;

        let task = user_api_tokens_roll_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User list user tenants.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn user_list_user_tenants(
        &self,
        args: &UserListUserTenantsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_list_user_tenants_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = user_list_user_tenants_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zones get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zones_get(
        &self,
        args: &ZonesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zones_get_builder(
            &self.http_client,
            &args.name,
            &args.status,
            &args.account_id,
            &args.account_name,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
            &args.match_rs,
        )
        .map_err(ProviderError::Api)?;

        let task = zones_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zones post.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zones_post(
        &self,
        args: &ZonesPostArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zones_post_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = zones_post_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone analytics deprecated get analytics by co locations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZoneAnalyticsApiColoResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_analytics_deprecated_get_analytics_by_co_locations(
        &self,
        args: &ZoneAnalyticsDeprecatedGetAnalyticsByCoLocationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZoneAnalyticsApiColoResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_analytics_deprecated_get_analytics_by_co_locations_builder(
            &self.http_client,
            &args.zone_identifier,
            &args.until,
            &args.since,
            &args.continuous,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_analytics_deprecated_get_analytics_by_co_locations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone analytics deprecated get dashboard.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZoneAnalyticsApiDashboardResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_analytics_deprecated_get_dashboard(
        &self,
        args: &ZoneAnalyticsDeprecatedGetDashboardArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZoneAnalyticsApiDashboardResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_analytics_deprecated_get_dashboard_builder(
            &self.http_client,
            &args.zone_identifier,
            &args.until,
            &args.since,
            &args.continuous,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_analytics_deprecated_get_dashboard_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom pages for a zone list custom pages.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomPagesCustomPageResultList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_pages_for_a_zone_list_custom_pages(
        &self,
        args: &CustomPagesForAZoneListCustomPagesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomPagesCustomPageResultList, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_pages_for_a_zone_list_custom_pages_builder(
            &self.http_client,
            &args.zone_identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_pages_for_a_zone_list_custom_pages_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom assets for a zone list custom assets.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomPagesCustomAssetResultList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_assets_for_a_zone_list_custom_assets(
        &self,
        args: &CustomAssetsForAZoneListCustomAssetsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomPagesCustomAssetResultList, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_assets_for_a_zone_list_custom_assets_builder(
            &self.http_client,
            &args.zone_identifier,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_assets_for_a_zone_list_custom_assets_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom assets for a zone create a custom asset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomPagesCustomAssetResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_assets_for_a_zone_create_a_custom_asset(
        &self,
        args: &CustomAssetsForAZoneCreateACustomAssetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomPagesCustomAssetResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_assets_for_a_zone_create_a_custom_asset_builder(
            &self.http_client,
            &args.zone_identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_assets_for_a_zone_create_a_custom_asset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom assets for a zone get a custom asset.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomPagesCustomAssetResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_assets_for_a_zone_get_a_custom_asset(
        &self,
        args: &CustomAssetsForAZoneGetACustomAssetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomPagesCustomAssetResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_assets_for_a_zone_get_a_custom_asset_builder(
            &self.http_client,
            &args.asset_name,
            &args.zone_identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_assets_for_a_zone_get_a_custom_asset_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom assets for a zone update a custom asset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomPagesCustomAssetResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_assets_for_a_zone_update_a_custom_asset(
        &self,
        args: &CustomAssetsForAZoneUpdateACustomAssetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomPagesCustomAssetResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_assets_for_a_zone_update_a_custom_asset_builder(
            &self.http_client,
            &args.asset_name,
            &args.zone_identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_assets_for_a_zone_update_a_custom_asset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom assets for a zone delete a custom asset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_assets_for_a_zone_delete_a_custom_asset(
        &self,
        args: &CustomAssetsForAZoneDeleteACustomAssetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_assets_for_a_zone_delete_a_custom_asset_builder(
            &self.http_client,
            &args.asset_name,
            &args.zone_identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_assets_for_a_zone_delete_a_custom_asset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom pages for a zone get a custom page.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomPagesCustomPage result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_pages_for_a_zone_get_a_custom_page(
        &self,
        args: &CustomPagesForAZoneGetACustomPageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomPagesCustomPage, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_pages_for_a_zone_get_a_custom_page_builder(
            &self.http_client,
            &args.identifier,
            &args.zone_identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_pages_for_a_zone_get_a_custom_page_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom pages for a zone update a custom page.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CustomPagesCustomPageResult result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_pages_for_a_zone_update_a_custom_page(
        &self,
        args: &CustomPagesForAZoneUpdateACustomPageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CustomPagesCustomPageResult, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_pages_for_a_zone_update_a_custom_page_builder(
            &self.http_client,
            &args.identifier,
            &args.zone_identifier,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_pages_for_a_zone_update_a_custom_page_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zones 0 get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zones_0_get(
        &self,
        args: &Zones0GetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zones_0_get_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zones_0_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zones 0 patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zones_0_patch(
        &self,
        args: &Zones0PatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zones_0_patch_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zones_0_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zones 0 delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZonesApiResponseSingleId result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zones_0_delete(
        &self,
        args: &Zones0DeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZonesApiResponseSingleId, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zones_0_delete_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zones_0_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access applications list access applications.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessAppsComponentsSchemasResponseCollection2 result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_access_applications_list_access_applications(
        &self,
        args: &ZoneLevelAccessApplicationsListAccessApplicationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessAppsComponentsSchemasResponseCollection2, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_applications_list_access_applications_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_applications_list_access_applications_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access applications add a bookmark application.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_applications_add_a_bookmark_application(
        &self,
        args: &ZoneLevelAccessApplicationsAddABookmarkApplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_applications_add_a_bookmark_application_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_applications_add_a_bookmark_application_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access short lived certificate c as list short lived certificate c as.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessCaComponentsSchemasResponseCollection2 result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_access_short_lived_certificate_c_as_list_short_lived_certificate_c_as(
        &self,
        args: &ZoneLevelAccessShortLivedCertificateCAsListShortLivedCertificateCAsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessCaComponentsSchemasResponseCollection2, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_short_lived_certificate_c_as_list_short_lived_certificate_c_as_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_short_lived_certificate_c_as_list_short_lived_certificate_c_as_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access applications get an access application.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessAppsComponentsSchemasSingleResponse2 result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_access_applications_get_an_access_application(
        &self,
        args: &ZoneLevelAccessApplicationsGetAnAccessApplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessAppsComponentsSchemasSingleResponse2, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_applications_get_an_access_application_builder(
            &self.http_client,
            &args.app_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_applications_get_an_access_application_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access applications update a bookmark application.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_applications_update_a_bookmark_application(
        &self,
        args: &ZoneLevelAccessApplicationsUpdateABookmarkApplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_applications_update_a_bookmark_application_builder(
            &self.http_client,
            &args.app_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_applications_update_a_bookmark_application_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access applications delete an access application.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_applications_delete_an_access_application(
        &self,
        args: &ZoneLevelAccessApplicationsDeleteAnAccessApplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_applications_delete_an_access_application_builder(
            &self.http_client,
            &args.app_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_applications_delete_an_access_application_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access short lived certificate c as get a short lived certificate ca.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessCaComponentsSchemasSingleResponse2 result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_access_short_lived_certificate_c_as_get_a_short_lived_certificate_ca(
        &self,
        args: &ZoneLevelAccessShortLivedCertificateCAsGetAShortLivedCertificateCaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessCaComponentsSchemasSingleResponse2, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_short_lived_certificate_c_as_get_a_short_lived_certificate_ca_builder(
            &self.http_client,
            &args.app_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_short_lived_certificate_c_as_get_a_short_lived_certificate_ca_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access short lived certificate c as create a short lived certificate ca.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessCaComponentsSchemasSingleResponse2 result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_short_lived_certificate_c_as_create_a_short_lived_certificate_ca(
        &self,
        args: &ZoneLevelAccessShortLivedCertificateCAsCreateAShortLivedCertificateCaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessCaComponentsSchemasSingleResponse2, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_short_lived_certificate_c_as_create_a_short_lived_certificate_ca_builder(
            &self.http_client,
            &args.app_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_short_lived_certificate_c_as_create_a_short_lived_certificate_ca_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access short lived certificate c as delete a short lived certificate ca.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessCaComponentsSchemasIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_short_lived_certificate_c_as_delete_a_short_lived_certificate_ca(
        &self,
        args: &ZoneLevelAccessShortLivedCertificateCAsDeleteAShortLivedCertificateCaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessCaComponentsSchemasIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_short_lived_certificate_c_as_delete_a_short_lived_certificate_ca_builder(
            &self.http_client,
            &args.app_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_short_lived_certificate_c_as_delete_a_short_lived_certificate_ca_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access policies list access policies.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessPoliciesComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_access_policies_list_access_policies(
        &self,
        args: &ZoneLevelAccessPoliciesListAccessPoliciesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessPoliciesComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_policies_list_access_policies_builder(
            &self.http_client,
            &args.app_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_policies_list_access_policies_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access policies create an access policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessPoliciesComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_policies_create_an_access_policy(
        &self,
        args: &ZoneLevelAccessPoliciesCreateAnAccessPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessPoliciesComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_policies_create_an_access_policy_builder(
            &self.http_client,
            &args.app_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_policies_create_an_access_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access policies get an access policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessPoliciesComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_access_policies_get_an_access_policy(
        &self,
        args: &ZoneLevelAccessPoliciesGetAnAccessPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessPoliciesComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_policies_get_an_access_policy_builder(
            &self.http_client,
            &args.policy_id,
            &args.app_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_policies_get_an_access_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access policies update an access policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessPoliciesComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_policies_update_an_access_policy(
        &self,
        args: &ZoneLevelAccessPoliciesUpdateAnAccessPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessPoliciesComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_policies_update_an_access_policy_builder(
            &self.http_client,
            &args.policy_id,
            &args.app_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_policies_update_an_access_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access policies delete an access policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_policies_delete_an_access_policy(
        &self,
        args: &ZoneLevelAccessPoliciesDeleteAnAccessPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_policies_delete_an_access_policy_builder(
            &self.http_client,
            &args.policy_id,
            &args.app_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_policies_delete_an_access_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access applications revoke service tokens.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSchemasEmptyResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_applications_revoke_service_tokens(
        &self,
        args: &ZoneLevelAccessApplicationsRevokeServiceTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSchemasEmptyResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_applications_revoke_service_tokens_builder(
            &self.http_client,
            &args.app_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_applications_revoke_service_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access applications put update access application settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_applications_put_update_access_application_settings(
        &self,
        args: &ZoneLevelAccessApplicationsPutUpdateAccessApplicationSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_applications_put_update_access_application_settings_builder(
            &self.http_client,
            &args.app_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_applications_put_update_access_application_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access applications patch update access application settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_applications_patch_update_access_application_settings(
        &self,
        args: &ZoneLevelAccessApplicationsPatchUpdateAccessApplicationSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_applications_patch_update_access_application_settings_builder(
            &self.http_client,
            &args.app_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_applications_patch_update_access_application_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access applications test access policies.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSchemasPolicyCheckResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_access_applications_test_access_policies(
        &self,
        args: &ZoneLevelAccessApplicationsTestAccessPoliciesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSchemasPolicyCheckResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_applications_test_access_policies_builder(
            &self.http_client,
            &args.app_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_applications_test_access_policies_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access mtls authentication list mtls certificates.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessCertificatesComponentsSchemasResponseCollection2 result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_access_mtls_authentication_list_mtls_certificates(
        &self,
        args: &ZoneLevelAccessMtlsAuthenticationListMtlsCertificatesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessCertificatesComponentsSchemasResponseCollection2, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_mtls_authentication_list_mtls_certificates_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_mtls_authentication_list_mtls_certificates_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access mtls authentication add an mtls certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessCertificatesComponentsSchemasSingleResponse2 result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_mtls_authentication_add_an_mtls_certificate(
        &self,
        args: &ZoneLevelAccessMtlsAuthenticationAddAnMtlsCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessCertificatesComponentsSchemasSingleResponse2, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_mtls_authentication_add_an_mtls_certificate_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_mtls_authentication_add_an_mtls_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access mtls authentication list mtls certificates hostname settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSchemasResponseCollectionHostnames result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_access_mtls_authentication_list_mtls_certificates_hostname_settings(
        &self,
        args: &ZoneLevelAccessMtlsAuthenticationListMtlsCertificatesHostnameSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSchemasResponseCollectionHostnames, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_mtls_authentication_list_mtls_certificates_hostname_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_mtls_authentication_list_mtls_certificates_hostname_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access mtls authentication update an mtls certificate settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSchemasResponseCollectionHostnames result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_mtls_authentication_update_an_mtls_certificate_settings(
        &self,
        args: &ZoneLevelAccessMtlsAuthenticationUpdateAnMtlsCertificateSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSchemasResponseCollectionHostnames, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_mtls_authentication_update_an_mtls_certificate_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_mtls_authentication_update_an_mtls_certificate_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access mtls authentication get an mtls certificate.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessCertificatesComponentsSchemasSingleResponse2 result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_access_mtls_authentication_get_an_mtls_certificate(
        &self,
        args: &ZoneLevelAccessMtlsAuthenticationGetAnMtlsCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessCertificatesComponentsSchemasSingleResponse2, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_mtls_authentication_get_an_mtls_certificate_builder(
            &self.http_client,
            &args.certificate_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_mtls_authentication_get_an_mtls_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access mtls authentication update an mtls certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessCertificatesComponentsSchemasSingleResponse2 result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_mtls_authentication_update_an_mtls_certificate(
        &self,
        args: &ZoneLevelAccessMtlsAuthenticationUpdateAnMtlsCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessCertificatesComponentsSchemasSingleResponse2, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_mtls_authentication_update_an_mtls_certificate_builder(
            &self.http_client,
            &args.certificate_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_mtls_authentication_update_an_mtls_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access mtls authentication delete an mtls certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessComponentsSchemasIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_mtls_authentication_delete_an_mtls_certificate(
        &self,
        args: &ZoneLevelAccessMtlsAuthenticationDeleteAnMtlsCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessComponentsSchemasIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_mtls_authentication_delete_an_mtls_certificate_builder(
            &self.http_client,
            &args.certificate_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_mtls_authentication_delete_an_mtls_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access groups list access groups.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessGroupsComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_access_groups_list_access_groups(
        &self,
        args: &ZoneLevelAccessGroupsListAccessGroupsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessGroupsComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_groups_list_access_groups_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_groups_list_access_groups_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access groups create an access group.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessGroupsComponentsSchemasSingleResponse2 result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_groups_create_an_access_group(
        &self,
        args: &ZoneLevelAccessGroupsCreateAnAccessGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessGroupsComponentsSchemasSingleResponse2, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_groups_create_an_access_group_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_groups_create_an_access_group_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access groups get an access group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessGroupsComponentsSchemasSingleResponse2 result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_access_groups_get_an_access_group(
        &self,
        args: &ZoneLevelAccessGroupsGetAnAccessGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessGroupsComponentsSchemasSingleResponse2, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_groups_get_an_access_group_builder(
            &self.http_client,
            &args.group_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_groups_get_an_access_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access groups update an access group.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessGroupsComponentsSchemasSingleResponse2 result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_groups_update_an_access_group(
        &self,
        args: &ZoneLevelAccessGroupsUpdateAnAccessGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessGroupsComponentsSchemasSingleResponse2, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_groups_update_an_access_group_builder(
            &self.http_client,
            &args.group_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_groups_update_an_access_group_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access groups delete an access group.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_groups_delete_an_access_group(
        &self,
        args: &ZoneLevelAccessGroupsDeleteAnAccessGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_groups_delete_an_access_group_builder(
            &self.http_client,
            &args.group_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_groups_delete_an_access_group_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access identity providers list access identity providers.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessIdentityProvidersComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_access_identity_providers_list_access_identity_providers(
        &self,
        args: &ZoneLevelAccessIdentityProvidersListAccessIdentityProvidersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessIdentityProvidersComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_identity_providers_list_access_identity_providers_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_identity_providers_list_access_identity_providers_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access identity providers add an access identity provider.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessIdentityProvidersComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_identity_providers_add_an_access_identity_provider(
        &self,
        args: &ZoneLevelAccessIdentityProvidersAddAnAccessIdentityProviderArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessIdentityProvidersComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_identity_providers_add_an_access_identity_provider_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_identity_providers_add_an_access_identity_provider_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access identity providers get an access identity provider.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessIdentityProvidersComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_access_identity_providers_get_an_access_identity_provider(
        &self,
        args: &ZoneLevelAccessIdentityProvidersGetAnAccessIdentityProviderArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessIdentityProvidersComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_identity_providers_get_an_access_identity_provider_builder(
            &self.http_client,
            &args.identity_provider_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_identity_providers_get_an_access_identity_provider_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access identity providers update an access identity provider.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessIdentityProvidersComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_identity_providers_update_an_access_identity_provider(
        &self,
        args: &ZoneLevelAccessIdentityProvidersUpdateAnAccessIdentityProviderArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessIdentityProvidersComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_identity_providers_update_an_access_identity_provider_builder(
            &self.http_client,
            &args.identity_provider_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_identity_providers_update_an_access_identity_provider_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access identity providers delete an access identity provider.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_identity_providers_delete_an_access_identity_provider(
        &self,
        args: &ZoneLevelAccessIdentityProvidersDeleteAnAccessIdentityProviderArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_identity_providers_delete_an_access_identity_provider_builder(
            &self.http_client,
            &args.identity_provider_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_identity_providers_delete_an_access_identity_provider_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level zero trust organization get your zero trust organization.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessOrganizationsComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_zero_trust_organization_get_your_zero_trust_organization(
        &self,
        args: &ZoneLevelZeroTrustOrganizationGetYourZeroTrustOrganizationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessOrganizationsComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_zero_trust_organization_get_your_zero_trust_organization_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_zero_trust_organization_get_your_zero_trust_organization_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level zero trust organization create your zero trust organization.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessOrganizationsComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_zero_trust_organization_create_your_zero_trust_organization(
        &self,
        args: &ZoneLevelZeroTrustOrganizationCreateYourZeroTrustOrganizationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessOrganizationsComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_zero_trust_organization_create_your_zero_trust_organization_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_zero_trust_organization_create_your_zero_trust_organization_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level zero trust organization update your zero trust organization.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessOrganizationsComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_zero_trust_organization_update_your_zero_trust_organization(
        &self,
        args: &ZoneLevelZeroTrustOrganizationUpdateYourZeroTrustOrganizationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessOrganizationsComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_zero_trust_organization_update_your_zero_trust_organization_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_zero_trust_organization_update_your_zero_trust_organization_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level zero trust organization revoke all access tokens for a user.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessEmptyResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_zero_trust_organization_revoke_all_access_tokens_for_a_user(
        &self,
        args: &ZoneLevelZeroTrustOrganizationRevokeAllAccessTokensForAUserArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessEmptyResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_zero_trust_organization_revoke_all_access_tokens_for_a_user_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_zero_trust_organization_revoke_all_access_tokens_for_a_user_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access service tokens list service tokens.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessServiceTokensComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_access_service_tokens_list_service_tokens(
        &self,
        args: &ZoneLevelAccessServiceTokensListServiceTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessServiceTokensComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_service_tokens_list_service_tokens_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_service_tokens_list_service_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access service tokens create a service token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessSchemasCreateResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_service_tokens_create_a_service_token(
        &self,
        args: &ZoneLevelAccessServiceTokensCreateAServiceTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessSchemasCreateResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_service_tokens_create_a_service_token_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_service_tokens_create_a_service_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access service tokens get a service token.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessServiceTokensComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_access_service_tokens_get_a_service_token(
        &self,
        args: &ZoneLevelAccessServiceTokensGetAServiceTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessServiceTokensComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_service_tokens_get_a_service_token_builder(
            &self.http_client,
            &args.service_token_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_service_tokens_get_a_service_token_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access service tokens update a service token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessServiceTokensComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_service_tokens_update_a_service_token(
        &self,
        args: &ZoneLevelAccessServiceTokensUpdateAServiceTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessServiceTokensComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_service_tokens_update_a_service_token_builder(
            &self.http_client,
            &args.service_token_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_service_tokens_update_a_service_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level access service tokens delete a service token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AccessServiceTokensComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_access_service_tokens_delete_a_service_token(
        &self,
        args: &ZoneLevelAccessServiceTokensDeleteAServiceTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AccessServiceTokensComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_access_service_tokens_delete_a_service_token_builder(
            &self.http_client,
            &args.service_token_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_access_service_tokens_delete_a_service_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom origin trust store list details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCustomTrustStoreResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_origin_trust_store_list_details(
        &self,
        args: &CustomOriginTrustStoreListDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCustomTrustStoreResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_origin_trust_store_list_details_builder(
            &self.http_client,
            &args.zone_id,
            &args.page,
            &args.per_page,
            &args.limit,
            &args.offset,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_origin_trust_store_list_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom origin trust store create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCustomTrustStoreResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_origin_trust_store_create(
        &self,
        args: &CustomOriginTrustStoreCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCustomTrustStoreResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_origin_trust_store_create_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_origin_trust_store_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom origin trust store details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCustomTrustStoreResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_origin_trust_store_details(
        &self,
        args: &CustomOriginTrustStoreDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCustomTrustStoreResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_origin_trust_store_details_builder(
            &self.http_client,
            &args.custom_origin_trust_store_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_origin_trust_store_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom origin trust store delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCustomTrustStoreResponseIdOnly result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_origin_trust_store_delete(
        &self,
        args: &CustomOriginTrustStoreDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCustomTrustStoreResponseIdOnly, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_origin_trust_store_delete_builder(
            &self.http_client,
            &args.custom_origin_trust_store_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_origin_trust_store_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Total tls total tls settings details.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesTotalTlsSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn total_tls_total_tls_settings_details(
        &self,
        args: &TotalTlsTotalTlsSettingsDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesTotalTlsSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = total_tls_total_tls_settings_details_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = total_tls_total_tls_settings_details_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Total tls enable or disable total tls.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesTotalTlsSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn total_tls_enable_or_disable_total_tls(
        &self,
        args: &TotalTlsEnableOrDisableTotalTlsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesTotalTlsSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = total_tls_enable_or_disable_total_tls_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = total_tls_enable_or_disable_total_tls_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Put zones zone id activation check.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn put_zones_zone_id_activation_check(
        &self,
        args: &PutZonesZoneIdActivationCheckArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = put_zones_zone_id_activation_check_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = put_zones_zone_id_activation_check_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dls account regional hostnames account list hostnames.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dls_account_regional_hostnames_account_list_hostnames(
        &self,
        args: &DlsAccountRegionalHostnamesAccountListHostnamesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dls_account_regional_hostnames_account_list_hostnames_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dls_account_regional_hostnames_account_list_hostnames_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dls account regional hostnames account create hostname.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dls_account_regional_hostnames_account_create_hostname(
        &self,
        args: &DlsAccountRegionalHostnamesAccountCreateHostnameArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dls_account_regional_hostnames_account_create_hostname_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dls_account_regional_hostnames_account_create_hostname_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dls account regional hostnames account fetch hostname.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dls_account_regional_hostnames_account_fetch_hostname(
        &self,
        args: &DlsAccountRegionalHostnamesAccountFetchHostnameArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dls_account_regional_hostnames_account_fetch_hostname_builder(
            &self.http_client,
            &args.zone_id,
            &args.hostname,
        )
        .map_err(ProviderError::Api)?;

        let task = dls_account_regional_hostnames_account_fetch_hostname_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dls account regional hostnames account patch hostname.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dls_account_regional_hostnames_account_patch_hostname(
        &self,
        args: &DlsAccountRegionalHostnamesAccountPatchHostnameArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dls_account_regional_hostnames_account_patch_hostname_builder(
            &self.http_client,
            &args.zone_id,
            &args.hostname,
        )
        .map_err(ProviderError::Api)?;

        let task = dls_account_regional_hostnames_account_patch_hostname_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dls account regional hostnames account delete hostname.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dls_account_regional_hostnames_account_delete_hostname(
        &self,
        args: &DlsAccountRegionalHostnamesAccountDeleteHostnameArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dls_account_regional_hostnames_account_delete_hostname_builder(
            &self.http_client,
            &args.zone_id,
            &args.hostname,
        )
        .map_err(ProviderError::Api)?;

        let task = dls_account_regional_hostnames_account_delete_hostname_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Argo analytics for zone argo analytics for a zone.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ArgoAnalyticsResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn argo_analytics_for_zone_argo_analytics_for_a_zone(
        &self,
        args: &ArgoAnalyticsForZoneArgoAnalyticsForAZoneArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ArgoAnalyticsResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = argo_analytics_for_zone_argo_analytics_for_a_zone_builder(
            &self.http_client,
            &args.zone_id,
            &args.bins,
        )
        .map_err(ProviderError::Api)?;

        let task = argo_analytics_for_zone_argo_analytics_for_a_zone_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Argo analytics for geolocation argo analytics for a zone at different po ps.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ArgoAnalyticsResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn argo_analytics_for_geolocation_argo_analytics_for_a_zone_at_different_po_ps(
        &self,
        args: &ArgoAnalyticsForGeolocationArgoAnalyticsForAZoneAtDifferentPoPsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ArgoAnalyticsResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = argo_analytics_for_geolocation_argo_analytics_for_a_zone_at_different_po_ps_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = argo_analytics_for_geolocation_argo_analytics_for_a_zone_at_different_po_ps_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield settings retrieve information about specific configuration properties.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn api_shield_settings_retrieve_information_about_specific_configuration_properties(
        &self,
        args: &ApiShieldSettingsRetrieveInformationAboutSpecificConfigurationPropertiesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_settings_retrieve_information_about_specific_configuration_properties_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_settings_retrieve_information_about_specific_configuration_properties_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield settings set configuration properties.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_settings_set_configuration_properties(
        &self,
        args: &ApiShieldSettingsSetConfigurationPropertiesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_settings_set_configuration_properties_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_settings_set_configuration_properties_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield api discovery retrieve discovered operations on a zone as openapi.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ApiShieldSchemaResponseDiscovery result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn api_shield_api_discovery_retrieve_discovered_operations_on_a_zone_as_openapi(
        &self,
        args: &ApiShieldApiDiscoveryRetrieveDiscoveredOperationsOnAZoneAsOpenapiArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ApiShieldSchemaResponseDiscovery, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_api_discovery_retrieve_discovered_operations_on_a_zone_as_openapi_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_api_discovery_retrieve_discovered_operations_on_a_zone_as_openapi_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield api discovery retrieve discovered operations on a zone.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn api_shield_api_discovery_retrieve_discovered_operations_on_a_zone(
        &self,
        args: &ApiShieldApiDiscoveryRetrieveDiscoveredOperationsOnAZoneArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_api_discovery_retrieve_discovered_operations_on_a_zone_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_api_discovery_retrieve_discovered_operations_on_a_zone_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield api patch discovered operations.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ApiShieldPatchDiscoveriesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_api_patch_discovered_operations(
        &self,
        args: &ApiShieldApiPatchDiscoveredOperationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ApiShieldPatchDiscoveriesResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_api_patch_discovered_operations_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_api_patch_discovered_operations_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield api patch discovered operation.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ApiShieldPatchDiscoveryResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_api_patch_discovered_operation(
        &self,
        args: &ApiShieldApiPatchDiscoveredOperationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ApiShieldPatchDiscoveryResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_api_patch_discovered_operation_builder(
            &self.http_client,
            &args.zone_id,
            &args.operation_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_api_patch_discovered_operation_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield expression templates fallthrough.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_expression_templates_fallthrough(
        &self,
        args: &ApiShieldExpressionTemplatesFallthroughArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_expression_templates_fallthrough_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_expression_templates_fallthrough_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield labels get labels.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn api_shield_labels_get_labels(
        &self,
        args: &ApiShieldLabelsGetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_labels_get_labels_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_labels_get_labels_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield labels get managed label.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn api_shield_labels_get_managed_label(
        &self,
        args: &ApiShieldLabelsGetManagedLabelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_labels_get_managed_label_builder(
            &self.http_client,
            &args.zone_id,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_labels_get_managed_label_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield labels replace operations attached to managed label.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_labels_replace_operations_attached_to_managed_label(
        &self,
        args: &ApiShieldLabelsReplaceOperationsAttachedToManagedLabelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_labels_replace_operations_attached_to_managed_label_builder(
            &self.http_client,
            &args.zone_id,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_labels_replace_operations_attached_to_managed_label_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield labels create user labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_labels_create_user_labels(
        &self,
        args: &ApiShieldLabelsCreateUserLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_labels_create_user_labels_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_labels_create_user_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield labels delete user labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_labels_delete_user_labels(
        &self,
        args: &ApiShieldLabelsDeleteUserLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_labels_delete_user_labels_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_labels_delete_user_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield labels get user label.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn api_shield_labels_get_user_label(
        &self,
        args: &ApiShieldLabelsGetUserLabelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_labels_get_user_label_builder(
            &self.http_client,
            &args.zone_id,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_labels_get_user_label_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield put user label.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_put_user_label(
        &self,
        args: &ApiShieldPutUserLabelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_put_user_label_builder(
            &self.http_client,
            &args.zone_id,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_put_user_label_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield patch user label.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_patch_user_label(
        &self,
        args: &ApiShieldPatchUserLabelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_patch_user_label_builder(
            &self.http_client,
            &args.zone_id,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_patch_user_label_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield delete user label.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_delete_user_label(
        &self,
        args: &ApiShieldDeleteUserLabelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_delete_user_label_builder(
            &self.http_client,
            &args.zone_id,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_delete_user_label_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield labels replace operations attached to user label.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_labels_replace_operations_attached_to_user_label(
        &self,
        args: &ApiShieldLabelsReplaceOperationsAttachedToUserLabelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_labels_replace_operations_attached_to_user_label_builder(
            &self.http_client,
            &args.zone_id,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_labels_replace_operations_attached_to_user_label_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield endpoint management retrieve information about all operations on a zone.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ApiShieldMultipleOperationResponsePaginated result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn api_shield_endpoint_management_retrieve_information_about_all_operations_on_a_zone(
        &self,
        args: &ApiShieldEndpointManagementRetrieveInformationAboutAllOperationsOnAZoneArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ApiShieldMultipleOperationResponsePaginated, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_endpoint_management_retrieve_information_about_all_operations_on_a_zone_builder(
            &self.http_client,
            &args.zone_id,
            &args.order,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_endpoint_management_retrieve_information_about_all_operations_on_a_zone_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield endpoint management add operations to a zone.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ApiShieldMultipleOperationResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_endpoint_management_add_operations_to_a_zone(
        &self,
        args: &ApiShieldEndpointManagementAddOperationsToAZoneArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ApiShieldMultipleOperationResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_endpoint_management_add_operations_to_a_zone_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_endpoint_management_add_operations_to_a_zone_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield endpoint management delete multiple operations.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ApiShieldApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_endpoint_management_delete_multiple_operations(
        &self,
        args: &ApiShieldEndpointManagementDeleteMultipleOperationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ApiShieldApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_endpoint_management_delete_multiple_operations_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_endpoint_management_delete_multiple_operations_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield endpoint management add operation to a zone.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ApiShieldSingleOperationResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_endpoint_management_add_operation_to_a_zone(
        &self,
        args: &ApiShieldEndpointManagementAddOperationToAZoneArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ApiShieldSingleOperationResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_endpoint_management_add_operation_to_a_zone_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_endpoint_management_add_operation_to_a_zone_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield operations bulk post labels to operations.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_operations_bulk_post_labels_to_operations(
        &self,
        args: &ApiShieldOperationsBulkPostLabelsToOperationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_operations_bulk_post_labels_to_operations_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_operations_bulk_post_labels_to_operations_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield operations bulk put labels to operations.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_operations_bulk_put_labels_to_operations(
        &self,
        args: &ApiShieldOperationsBulkPutLabelsToOperationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_operations_bulk_put_labels_to_operations_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_operations_bulk_put_labels_to_operations_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield operations bulk delete labels to operations.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_operations_bulk_delete_labels_to_operations(
        &self,
        args: &ApiShieldOperationsBulkDeleteLabelsToOperationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_operations_bulk_delete_labels_to_operations_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_operations_bulk_delete_labels_to_operations_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield schema validation update multiple operation level settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_schema_validation_update_multiple_operation_level_settings(
        &self,
        args: &ApiShieldSchemaValidationUpdateMultipleOperationLevelSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_schema_validation_update_multiple_operation_level_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_schema_validation_update_multiple_operation_level_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield endpoint management retrieve information about an operation.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ApiShieldSingleOperationResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn api_shield_endpoint_management_retrieve_information_about_an_operation(
        &self,
        args: &ApiShieldEndpointManagementRetrieveInformationAboutAnOperationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ApiShieldSingleOperationResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_endpoint_management_retrieve_information_about_an_operation_builder(
            &self.http_client,
            &args.zone_id,
            &args.operation_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_endpoint_management_retrieve_information_about_an_operation_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield endpoint management delete an operation.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ApiShieldApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_endpoint_management_delete_an_operation(
        &self,
        args: &ApiShieldEndpointManagementDeleteAnOperationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ApiShieldApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_endpoint_management_delete_an_operation_builder(
            &self.http_client,
            &args.zone_id,
            &args.operation_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_endpoint_management_delete_an_operation_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield operations post labels to operation.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_operations_post_labels_to_operation(
        &self,
        args: &ApiShieldOperationsPostLabelsToOperationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_operations_post_labels_to_operation_builder(
            &self.http_client,
            &args.zone_id,
            &args.operation_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_operations_post_labels_to_operation_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield operations put labels to operation.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_operations_put_labels_to_operation(
        &self,
        args: &ApiShieldOperationsPutLabelsToOperationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_operations_put_labels_to_operation_builder(
            &self.http_client,
            &args.zone_id,
            &args.operation_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_operations_put_labels_to_operation_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield operations delete labels from operation.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_operations_delete_labels_from_operation(
        &self,
        args: &ApiShieldOperationsDeleteLabelsFromOperationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_operations_delete_labels_from_operation_builder(
            &self.http_client,
            &args.zone_id,
            &args.operation_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_operations_delete_labels_from_operation_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield schema validation retrieve operation level settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ApiShieldOldOperationSchemaValidationSettings result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn api_shield_schema_validation_retrieve_operation_level_settings(
        &self,
        args: &ApiShieldSchemaValidationRetrieveOperationLevelSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ApiShieldOldOperationSchemaValidationSettings, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_schema_validation_retrieve_operation_level_settings_builder(
            &self.http_client,
            &args.zone_id,
            &args.operation_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_schema_validation_retrieve_operation_level_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield schema validation update operation level settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ApiShieldOldOperationSchemaValidationSettings result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_schema_validation_update_operation_level_settings(
        &self,
        args: &ApiShieldSchemaValidationUpdateOperationLevelSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ApiShieldOldOperationSchemaValidationSettings, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_schema_validation_update_operation_level_settings_builder(
            &self.http_client,
            &args.zone_id,
            &args.operation_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_schema_validation_update_operation_level_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield endpoint management retrieve operations and features as open api schemas.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ApiShieldSchemaResponseWithThresholds result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn api_shield_endpoint_management_retrieve_operations_and_features_as_open_api_schemas(
        &self,
        args: &ApiShieldEndpointManagementRetrieveOperationsAndFeaturesAsOpenApiSchemasArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ApiShieldSchemaResponseWithThresholds, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_endpoint_management_retrieve_operations_and_features_as_open_api_schemas_builder(
            &self.http_client,
            &args.zone_id,
            &args.host,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_endpoint_management_retrieve_operations_and_features_as_open_api_schemas_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield schema validation retrieve zone level settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ApiShieldOldZoneSchemaValidationSettings result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn api_shield_schema_validation_retrieve_zone_level_settings(
        &self,
        args: &ApiShieldSchemaValidationRetrieveZoneLevelSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ApiShieldOldZoneSchemaValidationSettings, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_schema_validation_retrieve_zone_level_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_schema_validation_retrieve_zone_level_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield schema validation update zone level settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ApiShieldOldZoneSchemaValidationSettings result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_schema_validation_update_zone_level_settings(
        &self,
        args: &ApiShieldSchemaValidationUpdateZoneLevelSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ApiShieldOldZoneSchemaValidationSettings, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_schema_validation_update_zone_level_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_schema_validation_update_zone_level_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield schema validation patch zone level settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ApiShieldOldZoneSchemaValidationSettings result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_schema_validation_patch_zone_level_settings(
        &self,
        args: &ApiShieldSchemaValidationPatchZoneLevelSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ApiShieldOldZoneSchemaValidationSettings, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_schema_validation_patch_zone_level_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_schema_validation_patch_zone_level_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield schema validation retrieve information about all schemas.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn api_shield_schema_validation_retrieve_information_about_all_schemas(
        &self,
        args: &ApiShieldSchemaValidationRetrieveInformationAboutAllSchemasArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_schema_validation_retrieve_information_about_all_schemas_builder(
            &self.http_client,
            &args.zone_id,
            &args.validation_enabled,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_schema_validation_retrieve_information_about_all_schemas_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield schema validation post schema.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_schema_validation_post_schema(
        &self,
        args: &ApiShieldSchemaValidationPostSchemaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_schema_validation_post_schema_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_schema_validation_post_schema_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield schema validation retrieve user schema hosts.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn api_shield_schema_validation_retrieve_user_schema_hosts(
        &self,
        args: &ApiShieldSchemaValidationRetrieveUserSchemaHostsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_schema_validation_retrieve_user_schema_hosts_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_schema_validation_retrieve_user_schema_hosts_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield schema validation retrieve information about specific schema.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn api_shield_schema_validation_retrieve_information_about_specific_schema(
        &self,
        args: &ApiShieldSchemaValidationRetrieveInformationAboutSpecificSchemaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_schema_validation_retrieve_information_about_specific_schema_builder(
            &self.http_client,
            &args.zone_id,
            &args.schema_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_schema_validation_retrieve_information_about_specific_schema_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield schema validation enable validation for a schema.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_schema_validation_enable_validation_for_a_schema(
        &self,
        args: &ApiShieldSchemaValidationEnableValidationForASchemaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_schema_validation_enable_validation_for_a_schema_builder(
            &self.http_client,
            &args.zone_id,
            &args.schema_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_schema_validation_enable_validation_for_a_schema_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield schema delete a schema.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ApiShieldApiResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn api_shield_schema_delete_a_schema(
        &self,
        args: &ApiShieldSchemaDeleteASchemaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ApiShieldApiResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_schema_delete_a_schema_builder(
            &self.http_client,
            &args.zone_id,
            &args.schema_id,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_schema_delete_a_schema_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Api shield schema validation extract operations from schema.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn api_shield_schema_validation_extract_operations_from_schema(
        &self,
        args: &ApiShieldSchemaValidationExtractOperationsFromSchemaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = api_shield_schema_validation_extract_operations_from_schema_builder(
            &self.http_client,
            &args.zone_id,
            &args.schema_id,
            &args.operation_status,
        )
        .map_err(ProviderError::Api)?;

        let task = api_shield_schema_validation_extract_operations_from_schema_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Argo smart routing get argo smart routing setting.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ArgoConfigApiResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn argo_smart_routing_get_argo_smart_routing_setting(
        &self,
        args: &ArgoSmartRoutingGetArgoSmartRoutingSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ArgoConfigApiResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = argo_smart_routing_get_argo_smart_routing_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = argo_smart_routing_get_argo_smart_routing_setting_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Argo smart routing patch argo smart routing setting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ArgoConfigApiResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn argo_smart_routing_patch_argo_smart_routing_setting(
        &self,
        args: &ArgoSmartRoutingPatchArgoSmartRoutingSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ArgoConfigApiResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = argo_smart_routing_patch_argo_smart_routing_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = argo_smart_routing_patch_argo_smart_routing_setting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tiered caching get tiered caching setting.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn tiered_caching_get_tiered_caching_setting(
        &self,
        args: &TieredCachingGetTieredCachingSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tiered_caching_get_tiered_caching_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tiered_caching_get_tiered_caching_setting_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tiered caching patch tiered caching setting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn tiered_caching_patch_tiered_caching_setting(
        &self,
        args: &TieredCachingPatchTieredCachingSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tiered_caching_patch_tiered_caching_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tiered_caching_patch_tiered_caching_setting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone rate plan list available plans.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_rate_plan_list_available_plans(
        &self,
        args: &ZoneRatePlanListAvailablePlansArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_rate_plan_list_available_plans_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_rate_plan_list_available_plans_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone rate plan available plan details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_rate_plan_available_plan_details(
        &self,
        args: &ZoneRatePlanAvailablePlanDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_rate_plan_available_plan_details_builder(
            &self.http_client,
            &args.plan_identifier,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_rate_plan_available_plan_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone rate plan list available rate plans.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BillSubsApiPlanResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_rate_plan_list_available_rate_plans(
        &self,
        args: &ZoneRatePlanListAvailableRatePlansArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BillSubsApiPlanResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_rate_plan_list_available_rate_plans_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_rate_plan_list_available_rate_plans_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Bot management for a zone get config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BotManagementBotManagementResponseBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn bot_management_for_a_zone_get_config(
        &self,
        args: &BotManagementForAZoneGetConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BotManagementBotManagementResponseBody, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = bot_management_for_a_zone_get_config_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = bot_management_for_a_zone_get_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Bot management for a zone update config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BotManagementBotManagementResponseBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn bot_management_for_a_zone_update_config(
        &self,
        args: &BotManagementForAZoneUpdateConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BotManagementBotManagementResponseBody, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = bot_management_for_a_zone_update_config_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = bot_management_for_a_zone_update_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Bot management zone feedback list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn bot_management_zone_feedback_list(
        &self,
        args: &BotManagementZoneFeedbackListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = bot_management_zone_feedback_list_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = bot_management_zone_feedback_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Bot management zone feedback create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn bot_management_zone_feedback_create(
        &self,
        args: &BotManagementZoneFeedbackCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = bot_management_zone_feedback_create_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = bot_management_zone_feedback_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone cache settings get cache reserve setting.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_cache_settings_get_cache_reserve_setting(
        &self,
        args: &ZoneCacheSettingsGetCacheReserveSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_cache_settings_get_cache_reserve_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_cache_settings_get_cache_reserve_setting_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone cache settings change cache reserve setting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_cache_settings_change_cache_reserve_setting(
        &self,
        args: &ZoneCacheSettingsChangeCacheReserveSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_cache_settings_change_cache_reserve_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_cache_settings_change_cache_reserve_setting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone cache settings get cache reserve clear.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_cache_settings_get_cache_reserve_clear(
        &self,
        args: &ZoneCacheSettingsGetCacheReserveClearArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_cache_settings_get_cache_reserve_clear_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_cache_settings_get_cache_reserve_clear_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone cache settings start cache reserve clear.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_cache_settings_start_cache_reserve_clear(
        &self,
        args: &ZoneCacheSettingsStartCacheReserveClearArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_cache_settings_start_cache_reserve_clear_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_cache_settings_start_cache_reserve_clear_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone cache settings get origin post quantum encryption setting.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_cache_settings_get_origin_post_quantum_encryption_setting(
        &self,
        args: &ZoneCacheSettingsGetOriginPostQuantumEncryptionSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_cache_settings_get_origin_post_quantum_encryption_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_cache_settings_get_origin_post_quantum_encryption_setting_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone cache settings change origin post quantum encryption setting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_cache_settings_change_origin_post_quantum_encryption_setting(
        &self,
        args: &ZoneCacheSettingsChangeOriginPostQuantumEncryptionSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_cache_settings_change_origin_post_quantum_encryption_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_cache_settings_change_origin_post_quantum_encryption_setting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone cache settings get regional tiered cache setting.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_cache_settings_get_regional_tiered_cache_setting(
        &self,
        args: &ZoneCacheSettingsGetRegionalTieredCacheSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_cache_settings_get_regional_tiered_cache_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_cache_settings_get_regional_tiered_cache_setting_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone cache settings change regional tiered cache setting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_cache_settings_change_regional_tiered_cache_setting(
        &self,
        args: &ZoneCacheSettingsChangeRegionalTieredCacheSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_cache_settings_change_regional_tiered_cache_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_cache_settings_change_regional_tiered_cache_setting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Smart tiered cache get smart tiered cache setting.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn smart_tiered_cache_get_smart_tiered_cache_setting(
        &self,
        args: &SmartTieredCacheGetSmartTieredCacheSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = smart_tiered_cache_get_smart_tiered_cache_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = smart_tiered_cache_get_smart_tiered_cache_setting_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Smart tiered cache patch smart tiered cache setting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn smart_tiered_cache_patch_smart_tiered_cache_setting(
        &self,
        args: &SmartTieredCachePatchSmartTieredCacheSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = smart_tiered_cache_patch_smart_tiered_cache_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = smart_tiered_cache_patch_smart_tiered_cache_setting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Smart tiered cache delete smart tiered cache setting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn smart_tiered_cache_delete_smart_tiered_cache_setting(
        &self,
        args: &SmartTieredCacheDeleteSmartTieredCacheSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = smart_tiered_cache_delete_smart_tiered_cache_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = smart_tiered_cache_delete_smart_tiered_cache_setting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone cache settings get variants setting.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_cache_settings_get_variants_setting(
        &self,
        args: &ZoneCacheSettingsGetVariantsSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_cache_settings_get_variants_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_cache_settings_get_variants_setting_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone cache settings change variants setting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_cache_settings_change_variants_setting(
        &self,
        args: &ZoneCacheSettingsChangeVariantsSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_cache_settings_change_variants_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_cache_settings_change_variants_setting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone cache settings delete variants setting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_cache_settings_delete_variants_setting(
        &self,
        args: &ZoneCacheSettingsDeleteVariantsSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_cache_settings_delete_variants_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_cache_settings_delete_variants_setting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Client certificate for a zone list hostname associations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesHostnameAssociationsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn client_certificate_for_a_zone_list_hostname_associations(
        &self,
        args: &ClientCertificateForAZoneListHostnameAssociationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesHostnameAssociationsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = client_certificate_for_a_zone_list_hostname_associations_builder(
            &self.http_client,
            &args.zone_id,
            &args.mtls_certificate_id,
        )
        .map_err(ProviderError::Api)?;

        let task = client_certificate_for_a_zone_list_hostname_associations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Client certificate for a zone put hostname associations.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesHostnameAssociationsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn client_certificate_for_a_zone_put_hostname_associations(
        &self,
        args: &ClientCertificateForAZonePutHostnameAssociationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesHostnameAssociationsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = client_certificate_for_a_zone_put_hostname_associations_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = client_certificate_for_a_zone_put_hostname_associations_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Client certificate for a zone list client certificates.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesClientCertificateResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn client_certificate_for_a_zone_list_client_certificates(
        &self,
        args: &ClientCertificateForAZoneListClientCertificatesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesClientCertificateResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = client_certificate_for_a_zone_list_client_certificates_builder(
            &self.http_client,
            &args.zone_id,
            &args.status,
            &args.page,
            &args.per_page,
            &args.limit,
            &args.offset,
        )
        .map_err(ProviderError::Api)?;

        let task = client_certificate_for_a_zone_list_client_certificates_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Client certificate for a zone create client certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesClientCertificateResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn client_certificate_for_a_zone_create_client_certificate(
        &self,
        args: &ClientCertificateForAZoneCreateClientCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesClientCertificateResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = client_certificate_for_a_zone_create_client_certificate_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = client_certificate_for_a_zone_create_client_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Client certificate for a zone client certificate details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesClientCertificateResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn client_certificate_for_a_zone_client_certificate_details(
        &self,
        args: &ClientCertificateForAZoneClientCertificateDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesClientCertificateResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = client_certificate_for_a_zone_client_certificate_details_builder(
            &self.http_client,
            &args.zone_id,
            &args.client_certificate_id,
        )
        .map_err(ProviderError::Api)?;

        let task = client_certificate_for_a_zone_client_certificate_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Client certificate for a zone edit client certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesClientCertificateResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn client_certificate_for_a_zone_edit_client_certificate(
        &self,
        args: &ClientCertificateForAZoneEditClientCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesClientCertificateResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = client_certificate_for_a_zone_edit_client_certificate_builder(
            &self.http_client,
            &args.zone_id,
            &args.client_certificate_id,
        )
        .map_err(ProviderError::Api)?;

        let task = client_certificate_for_a_zone_edit_client_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Client certificate for a zone delete client certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesClientCertificateResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn client_certificate_for_a_zone_delete_client_certificate(
        &self,
        args: &ClientCertificateForAZoneDeleteClientCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesClientCertificateResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = client_certificate_for_a_zone_delete_client_certificate_builder(
            &self.http_client,
            &args.zone_id,
            &args.client_certificate_id,
        )
        .map_err(ProviderError::Api)?;

        let task = client_certificate_for_a_zone_delete_client_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone cloud connector rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_cloud_connector_rules(
        &self,
        args: &ZoneCloudConnectorRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_cloud_connector_rules_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_cloud_connector_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone cloud conenctor rules put.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_cloud_conenctor_rules_put(
        &self,
        args: &ZoneCloudConenctorRulesPutArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_cloud_conenctor_rules_put_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_cloud_conenctor_rules_put_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf content scanning disable.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafProductApiBundleSchemasApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waf_content_scanning_disable(
        &self,
        args: &WafContentScanningDisableArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafProductApiBundleSchemasApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_content_scanning_disable_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_content_scanning_disable_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf content scanning enable.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafProductApiBundleSchemasApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waf_content_scanning_enable(
        &self,
        args: &WafContentScanningEnableArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafProductApiBundleSchemasApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_content_scanning_enable_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_content_scanning_enable_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf content scanning list custom scan expressions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafProductApiBundleResponseCustomScanCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waf_content_scanning_list_custom_scan_expressions(
        &self,
        args: &WafContentScanningListCustomScanExpressionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafProductApiBundleResponseCustomScanCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_content_scanning_list_custom_scan_expressions_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_content_scanning_list_custom_scan_expressions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf content scanning add custom scan expressions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafProductApiBundleResponseCustomScanCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waf_content_scanning_add_custom_scan_expressions(
        &self,
        args: &WafContentScanningAddCustomScanExpressionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafProductApiBundleResponseCustomScanCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_content_scanning_add_custom_scan_expressions_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_content_scanning_add_custom_scan_expressions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf content scanning delete custom scan expressions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafProductApiBundleResponseCustomScanCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waf_content_scanning_delete_custom_scan_expressions(
        &self,
        args: &WafContentScanningDeleteCustomScanExpressionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafProductApiBundleResponseCustomScanCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_content_scanning_delete_custom_scan_expressions_builder(
            &self.http_client,
            &args.zone_id,
            &args.expression_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_content_scanning_delete_custom_scan_expressions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf content scanning get status.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafProductApiBundleSchemasResponseStatus result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waf_content_scanning_get_status(
        &self,
        args: &WafContentScanningGetStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafProductApiBundleSchemasResponseStatus, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_content_scanning_get_status_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_content_scanning_get_status_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf content scanning update settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafProductApiBundleSchemasResponseStatus result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waf_content_scanning_update_settings(
        &self,
        args: &WafContentScanningUpdateSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafProductApiBundleSchemasResponseStatus, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_content_scanning_update_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_content_scanning_update_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom ssl for a zone list ssl configurations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCertificateResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_ssl_for_a_zone_list_ssl_configurations(
        &self,
        args: &CustomSslForAZoneListSslConfigurationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCertificateResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_ssl_for_a_zone_list_ssl_configurations_builder(
            &self.http_client,
            &args.zone_id,
            &args.page,
            &args.per_page,
            &args.match_rs,
            &args.status,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_ssl_for_a_zone_list_ssl_configurations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom ssl for a zone create ssl configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCertificateResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_ssl_for_a_zone_create_ssl_configuration(
        &self,
        args: &CustomSslForAZoneCreateSslConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCertificateResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_ssl_for_a_zone_create_ssl_configuration_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_ssl_for_a_zone_create_ssl_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom ssl for a zone re prioritize ssl certificates.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCertificateResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_ssl_for_a_zone_re_prioritize_ssl_certificates(
        &self,
        args: &CustomSslForAZoneRePrioritizeSslCertificatesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCertificateResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_ssl_for_a_zone_re_prioritize_ssl_certificates_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_ssl_for_a_zone_re_prioritize_ssl_certificates_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom ssl for a zone ssl configuration details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCertificateResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_ssl_for_a_zone_ssl_configuration_details(
        &self,
        args: &CustomSslForAZoneSslConfigurationDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCertificateResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_ssl_for_a_zone_ssl_configuration_details_builder(
            &self.http_client,
            &args.custom_certificate_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_ssl_for_a_zone_ssl_configuration_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom ssl for a zone edit ssl configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCertificateResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_ssl_for_a_zone_edit_ssl_configuration(
        &self,
        args: &CustomSslForAZoneEditSslConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCertificateResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_ssl_for_a_zone_edit_ssl_configuration_builder(
            &self.http_client,
            &args.custom_certificate_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_ssl_for_a_zone_edit_ssl_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom ssl for a zone delete ssl configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCertificateResponseIdOnly result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_ssl_for_a_zone_delete_ssl_configuration(
        &self,
        args: &CustomSslForAZoneDeleteSslConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCertificateResponseIdOnly, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_ssl_for_a_zone_delete_ssl_configuration_builder(
            &self.http_client,
            &args.custom_certificate_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_ssl_for_a_zone_delete_ssl_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom hostname for a zone list custom hostnames.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCustomHostnameResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_hostname_for_a_zone_list_custom_hostnames(
        &self,
        args: &CustomHostnameForAZoneListCustomHostnamesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCustomHostnameResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_hostname_for_a_zone_list_custom_hostnames_builder(
            &self.http_client,
            &args.zone_id,
            &args.hostname,
            &args.hostname_contain,
            &args.id,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
            &args.ssl_status,
            &args.hostname_status,
            &args.certificate_authority,
            &args.wildcard,
            &args.custom_origin_server,
            &args.ssl,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_hostname_for_a_zone_list_custom_hostnames_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom hostname for a zone create custom hostname.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCustomHostnameResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_hostname_for_a_zone_create_custom_hostname(
        &self,
        args: &CustomHostnameForAZoneCreateCustomHostnameArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCustomHostnameResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_hostname_for_a_zone_create_custom_hostname_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_hostname_for_a_zone_create_custom_hostname_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom hostname fallback origin for a zone get fallback origin for custom hostnames.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesFallbackOriginResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_hostname_fallback_origin_for_a_zone_get_fallback_origin_for_custom_hostnames(
        &self,
        args: &CustomHostnameFallbackOriginForAZoneGetFallbackOriginForCustomHostnamesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesFallbackOriginResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_hostname_fallback_origin_for_a_zone_get_fallback_origin_for_custom_hostnames_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_hostname_fallback_origin_for_a_zone_get_fallback_origin_for_custom_hostnames_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom hostname fallback origin for a zone update fallback origin for custom hostnames.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesFallbackOriginResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_hostname_fallback_origin_for_a_zone_update_fallback_origin_for_custom_hostnames(
        &self,
        args: &CustomHostnameFallbackOriginForAZoneUpdateFallbackOriginForCustomHostnamesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesFallbackOriginResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_hostname_fallback_origin_for_a_zone_update_fallback_origin_for_custom_hostnames_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_hostname_fallback_origin_for_a_zone_update_fallback_origin_for_custom_hostnames_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom hostname fallback origin for a zone delete fallback origin for custom hostnames.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesFallbackOriginResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_hostname_fallback_origin_for_a_zone_delete_fallback_origin_for_custom_hostnames(
        &self,
        args: &CustomHostnameFallbackOriginForAZoneDeleteFallbackOriginForCustomHostnamesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesFallbackOriginResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_hostname_fallback_origin_for_a_zone_delete_fallback_origin_for_custom_hostnames_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_hostname_fallback_origin_for_a_zone_delete_fallback_origin_for_custom_hostnames_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom hostname for a zone custom hostname details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCustomHostnameResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn custom_hostname_for_a_zone_custom_hostname_details(
        &self,
        args: &CustomHostnameForAZoneCustomHostnameDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCustomHostnameResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_hostname_for_a_zone_custom_hostname_details_builder(
            &self.http_client,
            &args.custom_hostname_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_hostname_for_a_zone_custom_hostname_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom hostname for a zone edit custom hostname.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCustomHostnameResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_hostname_for_a_zone_edit_custom_hostname(
        &self,
        args: &CustomHostnameForAZoneEditCustomHostnameArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCustomHostnameResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_hostname_for_a_zone_edit_custom_hostname_builder(
            &self.http_client,
            &args.custom_hostname_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_hostname_for_a_zone_edit_custom_hostname_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom hostname for a zone delete custom hostname and any issued ssl certificates.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_hostname_for_a_zone_delete_custom_hostname_and_any_issued_ssl_certificates(
        &self,
        args: &CustomHostnameForAZoneDeleteCustomHostnameAndAnyIssuedSslCertificatesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_hostname_for_a_zone_delete_custom_hostname_and_any_issued_ssl_certificates_builder(
            &self.http_client,
            &args.custom_hostname_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_hostname_for_a_zone_delete_custom_hostname_and_any_issued_ssl_certificates_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom hostname for a zone edit custom certificate custom hostname.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCustomHostnameResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_hostname_for_a_zone_edit_custom_certificate_custom_hostname(
        &self,
        args: &CustomHostnameForAZoneEditCustomCertificateCustomHostnameArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCustomHostnameResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_hostname_for_a_zone_edit_custom_certificate_custom_hostname_builder(
            &self.http_client,
            &args.custom_hostname_id,
            &args.certificate_pack_id,
            &args.certificate_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_hostname_for_a_zone_edit_custom_certificate_custom_hostname_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Custom hostname for a zone delete single certificate and key in a custom hostname.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn custom_hostname_for_a_zone_delete_single_certificate_and_key_in_a_custom_hostname(
        &self,
        args: &CustomHostnameForAZoneDeleteSingleCertificateAndKeyInACustomHostnameArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = custom_hostname_for_a_zone_delete_single_certificate_and_key_in_a_custom_hostname_builder(
            &self.http_client,
            &args.custom_hostname_id,
            &args.certificate_pack_id,
            &args.certificate_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = custom_hostname_for_a_zone_delete_single_certificate_and_key_in_a_custom_hostname_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account level custom nameservers usage for a zone get account custom nameserver related zone metadata.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsCustomNameserversGetResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn account_level_custom_nameservers_usage_for_a_zone_get_account_custom_nameserver_related_zone_metadata(
        &self,
        args: &AccountLevelCustomNameserversUsageForAZoneGetAccountCustomNameserverRelatedZoneMetadataArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsCustomNameserversGetResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_level_custom_nameservers_usage_for_a_zone_get_account_custom_nameserver_related_zone_metadata_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_level_custom_nameservers_usage_for_a_zone_get_account_custom_nameserver_related_zone_metadata_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Account level custom nameservers usage for a zone set account custom nameserver related zone metadata.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsCustomNameserversSchemasEmptyResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn account_level_custom_nameservers_usage_for_a_zone_set_account_custom_nameserver_related_zone_metadata(
        &self,
        args: &AccountLevelCustomNameserversUsageForAZoneSetAccountCustomNameserverRelatedZoneMetadataArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsCustomNameserversSchemasEmptyResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = account_level_custom_nameservers_usage_for_a_zone_set_account_custom_nameserver_related_zone_metadata_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = account_level_custom_nameservers_usage_for_a_zone_set_account_custom_nameserver_related_zone_metadata_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dcv delegation uuid get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesDcvDelegationResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dcv_delegation_uuid_get(
        &self,
        args: &DcvDelegationUuidGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesDcvDelegationResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dcv_delegation_uuid_get_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dcv_delegation_uuid_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices get policy certificates.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesDevicesPolicyCertificatesSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn devices_get_policy_certificates(
        &self,
        args: &DevicesGetPolicyCertificatesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesDevicesPolicyCertificatesSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_get_policy_certificates_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_get_policy_certificates_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Devices update policy certificates.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TeamsDevicesDevicesPolicyCertificatesSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn devices_update_policy_certificates(
        &self,
        args: &DevicesUpdatePolicyCertificatesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TeamsDevicesDevicesPolicyCertificatesSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = devices_update_policy_certificates_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = devices_update_policy_certificates_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns analytics table.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dns_analytics_table(
        &self,
        args: &DnsAnalyticsTableArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_analytics_table_builder(
            &self.http_client,
            &args.zone_id,
            &args.metrics,
            &args.dimensions,
            &args.since,
            &args.until,
            &args.limit,
            &args.sort,
            &args.filters,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_analytics_table_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns analytics by time.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dns_analytics_by_time(
        &self,
        args: &DnsAnalyticsByTimeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_analytics_by_time_builder(
            &self.http_client,
            &args.zone_id,
            &args.metrics,
            &args.dimensions,
            &args.since,
            &args.until,
            &args.limit,
            &args.sort,
            &args.filters,
            &args.time_delta,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_analytics_by_time_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns records for a zone list dns records.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsRecordsDnsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dns_records_for_a_zone_list_dns_records(
        &self,
        args: &DnsRecordsForAZoneListDnsRecordsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsRecordsDnsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_records_for_a_zone_list_dns_records_builder(
            &self.http_client,
            &args.zone_id,
            &args.name,
            &args.name_exact,
            &args.name_contains,
            &args.name_startswith,
            &args.name_endswith,
            &args.type_rs,
            &args.content,
            &args.content_exact,
            &args.content_contains,
            &args.content_startswith,
            &args.content_endswith,
            &args.proxied,
            &args.match_rs,
            &args.comment,
            &args.comment_present,
            &args.comment_absent,
            &args.comment_exact,
            &args.comment_contains,
            &args.comment_startswith,
            &args.comment_endswith,
            &args.tag,
            &args.tag_present,
            &args.tag_absent,
            &args.tag_exact,
            &args.tag_contains,
            &args.tag_startswith,
            &args.tag_endswith,
            &args.search,
            &args.tag_match,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_records_for_a_zone_list_dns_records_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns records for a zone create dns record.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsRecordsDnsResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dns_records_for_a_zone_create_dns_record(
        &self,
        args: &DnsRecordsForAZoneCreateDnsRecordArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsRecordsDnsResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_records_for_a_zone_create_dns_record_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_records_for_a_zone_create_dns_record_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns records for a zone batch dns records.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsRecordsDnsResponseBatch result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dns_records_for_a_zone_batch_dns_records(
        &self,
        args: &DnsRecordsForAZoneBatchDnsRecordsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsRecordsDnsResponseBatch, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_records_for_a_zone_batch_dns_records_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_records_for_a_zone_batch_dns_records_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns records for a zone export dns records.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dns_records_for_a_zone_export_dns_records(
        &self,
        args: &DnsRecordsForAZoneExportDnsRecordsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_records_for_a_zone_export_dns_records_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_records_for_a_zone_export_dns_records_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns records for a zone import dns records.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsRecordsDnsResponseImportScan result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dns_records_for_a_zone_import_dns_records(
        &self,
        args: &DnsRecordsForAZoneImportDnsRecordsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsRecordsDnsResponseImportScan, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_records_for_a_zone_import_dns_records_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_records_for_a_zone_import_dns_records_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns records for a zone scan dns records.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsRecordsDnsResponseImportScan result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dns_records_for_a_zone_scan_dns_records(
        &self,
        args: &DnsRecordsForAZoneScanDnsRecordsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsRecordsDnsResponseImportScan, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_records_for_a_zone_scan_dns_records_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_records_for_a_zone_scan_dns_records_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns records for a zone review dns scan.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsRecordsDnsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dns_records_for_a_zone_review_dns_scan(
        &self,
        args: &DnsRecordsForAZoneReviewDnsScanArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsRecordsDnsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_records_for_a_zone_review_dns_scan_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_records_for_a_zone_review_dns_scan_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns records for a zone apply dns scan results.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsRecordsDnsResponseReviewScan result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dns_records_for_a_zone_apply_dns_scan_results(
        &self,
        args: &DnsRecordsForAZoneApplyDnsScanResultsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsRecordsDnsResponseReviewScan, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_records_for_a_zone_apply_dns_scan_results_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_records_for_a_zone_apply_dns_scan_results_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns records for a zone trigger dns scan.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsRecordsDnsResponseTriggerScan result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dns_records_for_a_zone_trigger_dns_scan(
        &self,
        args: &DnsRecordsForAZoneTriggerDnsScanArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsRecordsDnsResponseTriggerScan, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_records_for_a_zone_trigger_dns_scan_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_records_for_a_zone_trigger_dns_scan_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns records for a zone get usage.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsRecordsDnsResponseZoneUsage result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dns_records_for_a_zone_get_usage(
        &self,
        args: &DnsRecordsForAZoneGetUsageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsRecordsDnsResponseZoneUsage, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_records_for_a_zone_get_usage_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_records_for_a_zone_get_usage_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns records for a zone dns record details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsRecordsDnsResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dns_records_for_a_zone_dns_record_details(
        &self,
        args: &DnsRecordsForAZoneDnsRecordDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsRecordsDnsResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_records_for_a_zone_dns_record_details_builder(
            &self.http_client,
            &args.dns_record_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_records_for_a_zone_dns_record_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns records for a zone update dns record.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsRecordsDnsResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dns_records_for_a_zone_update_dns_record(
        &self,
        args: &DnsRecordsForAZoneUpdateDnsRecordArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsRecordsDnsResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_records_for_a_zone_update_dns_record_builder(
            &self.http_client,
            &args.dns_record_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_records_for_a_zone_update_dns_record_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns records for a zone patch dns record.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsRecordsDnsResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dns_records_for_a_zone_patch_dns_record(
        &self,
        args: &DnsRecordsForAZonePatchDnsRecordArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsRecordsDnsResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_records_for_a_zone_patch_dns_record_builder(
            &self.http_client,
            &args.dns_record_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_records_for_a_zone_patch_dns_record_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns records for a zone delete dns record.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dns_records_for_a_zone_delete_dns_record(
        &self,
        args: &DnsRecordsForAZoneDeleteDnsRecordArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_records_for_a_zone_delete_dns_record_builder(
            &self.http_client,
            &args.dns_record_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_records_for_a_zone_delete_dns_record_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns settings for a zone list dns settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsSettingsSchemasDnsResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dns_settings_for_a_zone_list_dns_settings(
        &self,
        args: &DnsSettingsForAZoneListDnsSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsSettingsSchemasDnsResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_settings_for_a_zone_list_dns_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_settings_for_a_zone_list_dns_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dns settings for a zone update dns settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsSettingsSchemasDnsResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dns_settings_for_a_zone_update_dns_settings(
        &self,
        args: &DnsSettingsForAZoneUpdateDnsSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsSettingsSchemasDnsResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dns_settings_for_a_zone_update_dns_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dns_settings_for_a_zone_update_dns_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dnssec dnssec details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnssecDnssecResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dnssec_dnssec_details(
        &self,
        args: &DnssecDnssecDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnssecDnssecResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dnssec_dnssec_details_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dnssec_dnssec_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dnssec edit dnssec status.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnssecDnssecResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dnssec_edit_dnssec_status(
        &self,
        args: &DnssecEditDnssecStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnssecDnssecResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dnssec_edit_dnssec_status_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dnssec_edit_dnssec_status_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dnssec delete dnssec records.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnssecDeleteDnssecResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dnssec_delete_dnssec_records(
        &self,
        args: &DnssecDeleteDnssecRecordsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnssecDeleteDnssecResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dnssec_delete_dnssec_records_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = dnssec_delete_dnssec_records_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email routing settings get email routing settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailEmailSettingsResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_routing_settings_get_email_routing_settings(
        &self,
        args: &EmailRoutingSettingsGetEmailRoutingSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailEmailSettingsResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_routing_settings_get_email_routing_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_routing_settings_get_email_routing_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email routing settings disable email routing.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailEmailSettingsResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_routing_settings_disable_email_routing(
        &self,
        args: &EmailRoutingSettingsDisableEmailRoutingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailEmailSettingsResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_routing_settings_disable_email_routing_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_routing_settings_disable_email_routing_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email routing settings email routing dns settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_routing_settings_email_routing_dns_settings(
        &self,
        args: &EmailRoutingSettingsEmailRoutingDnsSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_routing_settings_email_routing_dns_settings_builder(
            &self.http_client,
            &args.zone_id,
            &args.subdomain,
        )
        .map_err(ProviderError::Api)?;

        let task = email_routing_settings_email_routing_dns_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email routing settings enable email routing dns.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailEmailSettingsResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_routing_settings_enable_email_routing_dns(
        &self,
        args: &EmailRoutingSettingsEnableEmailRoutingDnsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailEmailSettingsResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_routing_settings_enable_email_routing_dns_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_routing_settings_enable_email_routing_dns_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email routing settings unlock email routing dns.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailEmailSettingsResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_routing_settings_unlock_email_routing_dns(
        &self,
        args: &EmailRoutingSettingsUnlockEmailRoutingDnsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailEmailSettingsResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_routing_settings_unlock_email_routing_dns_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_routing_settings_unlock_email_routing_dns_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email routing settings disable email routing dns.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_routing_settings_disable_email_routing_dns(
        &self,
        args: &EmailRoutingSettingsDisableEmailRoutingDnsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_routing_settings_disable_email_routing_dns_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_routing_settings_disable_email_routing_dns_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email routing settings enable email routing.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailEmailSettingsResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_routing_settings_enable_email_routing(
        &self,
        args: &EmailRoutingSettingsEnableEmailRoutingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailEmailSettingsResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_routing_settings_enable_email_routing_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_routing_settings_enable_email_routing_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email routing routing rules list routing rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailRulesResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_routing_routing_rules_list_routing_rules(
        &self,
        args: &EmailRoutingRoutingRulesListRoutingRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailRulesResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_routing_routing_rules_list_routing_rules_builder(
            &self.http_client,
            &args.zone_id,
            &args.page,
            &args.per_page,
            &args.enabled,
        )
        .map_err(ProviderError::Api)?;

        let task = email_routing_routing_rules_list_routing_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email routing routing rules create routing rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailRuleResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_routing_routing_rules_create_routing_rule(
        &self,
        args: &EmailRoutingRoutingRulesCreateRoutingRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailRuleResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_routing_routing_rules_create_routing_rule_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_routing_routing_rules_create_routing_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email routing routing rules get catch all rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailCatchAllRuleResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_routing_routing_rules_get_catch_all_rule(
        &self,
        args: &EmailRoutingRoutingRulesGetCatchAllRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailCatchAllRuleResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_routing_routing_rules_get_catch_all_rule_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_routing_routing_rules_get_catch_all_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email routing routing rules update catch all rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailCatchAllRuleResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_routing_routing_rules_update_catch_all_rule(
        &self,
        args: &EmailRoutingRoutingRulesUpdateCatchAllRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailCatchAllRuleResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_routing_routing_rules_update_catch_all_rule_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_routing_routing_rules_update_catch_all_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email routing routing rules get routing rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailRuleResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_routing_routing_rules_get_routing_rule(
        &self,
        args: &EmailRoutingRoutingRulesGetRoutingRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailRuleResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_routing_routing_rules_get_routing_rule_builder(
            &self.http_client,
            &args.rule_identifier,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_routing_routing_rules_get_routing_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email routing routing rules update routing rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailRuleResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_routing_routing_rules_update_routing_rule(
        &self,
        args: &EmailRoutingRoutingRulesUpdateRoutingRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailRuleResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_routing_routing_rules_update_routing_rule_builder(
            &self.http_client,
            &args.rule_identifier,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_routing_routing_rules_update_routing_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email routing routing rules delete routing rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailRuleResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_routing_routing_rules_delete_routing_rule(
        &self,
        args: &EmailRoutingRoutingRulesDeleteRoutingRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailRuleResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_routing_routing_rules_delete_routing_rule_builder(
            &self.http_client,
            &args.rule_identifier,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_routing_routing_rules_delete_routing_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email sending subdomains list sending subdomains.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailSendingSubdomainsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_sending_subdomains_list_sending_subdomains(
        &self,
        args: &EmailSendingSubdomainsListSendingSubdomainsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailSendingSubdomainsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_sending_subdomains_list_sending_subdomains_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_sending_subdomains_list_sending_subdomains_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email sending subdomains create sending subdomain.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailSendingSubdomainResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_sending_subdomains_create_sending_subdomain(
        &self,
        args: &EmailSendingSubdomainsCreateSendingSubdomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailSendingSubdomainResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_sending_subdomains_create_sending_subdomain_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_sending_subdomains_create_sending_subdomain_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email sending subdomains preview sending subdomain.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailSendingSubdomainPreviewResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_sending_subdomains_preview_sending_subdomain(
        &self,
        args: &EmailSendingSubdomainsPreviewSendingSubdomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailSendingSubdomainPreviewResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_sending_subdomains_preview_sending_subdomain_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_sending_subdomains_preview_sending_subdomain_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email sending subdomains get sending subdomain.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailSendingSubdomainResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_sending_subdomains_get_sending_subdomain(
        &self,
        args: &EmailSendingSubdomainsGetSendingSubdomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailSendingSubdomainResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_sending_subdomains_get_sending_subdomain_builder(
            &self.http_client,
            &args.subdomain_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_sending_subdomains_get_sending_subdomain_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email sending subdomains delete sending subdomain.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailApiResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn email_sending_subdomains_delete_sending_subdomain(
        &self,
        args: &EmailSendingSubdomainsDeleteSendingSubdomainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailApiResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_sending_subdomains_delete_sending_subdomain_builder(
            &self.http_client,
            &args.subdomain_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_sending_subdomains_delete_sending_subdomain_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Email sending subdomains get sending subdomain dns.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EmailDnsSettingsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn email_sending_subdomains_get_sending_subdomain_dns(
        &self,
        args: &EmailSendingSubdomainsGetSendingSubdomainDnsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EmailDnsSettingsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = email_sending_subdomains_get_sending_subdomain_dns_builder(
            &self.http_client,
            &args.subdomain_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = email_sending_subdomains_get_sending_subdomain_dns_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zones environments list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the KaminoEnvironmentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zones_environments_list(
        &self,
        args: &ZonesEnvironmentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<KaminoEnvironmentsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zones_environments_list_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zones_environments_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zones environments create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the KaminoEnvironmentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zones_environments_create(
        &self,
        args: &ZonesEnvironmentsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<KaminoEnvironmentsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zones_environments_create_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zones_environments_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zones environments update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the KaminoEnvironmentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zones_environments_update(
        &self,
        args: &ZonesEnvironmentsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<KaminoEnvironmentsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zones_environments_update_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zones_environments_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zones environments edit.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the KaminoEnvironmentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zones_environments_edit(
        &self,
        args: &ZonesEnvironmentsEditArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<KaminoEnvironmentsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zones_environments_edit_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zones_environments_edit_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zones environments delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the KaminoEnvironmentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zones_environments_delete(
        &self,
        args: &ZonesEnvironmentsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<KaminoEnvironmentsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zones_environments_delete_builder(
            &self.http_client,
            &args.zone_id,
            &args.environment_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zones_environments_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zones environments rollback.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the KaminoEnvironmentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zones_environments_rollback(
        &self,
        args: &ZonesEnvironmentsRollbackArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<KaminoEnvironmentsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zones_environments_rollback_builder(
            &self.http_client,
            &args.zone_id,
            &args.environment_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zones_environments_rollback_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Filters list filters.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallFilterResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn filters_list_filters(
        &self,
        args: &FiltersListFiltersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallFilterResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = filters_list_filters_builder(
            &self.http_client,
            &args.zone_id,
            &args.paused,
            &args.expression,
            &args.description,
            &args.ref_rs,
            &args.page,
            &args.per_page,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = filters_list_filters_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Filters create filters.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn filters_create_filters(
        &self,
        args: &FiltersCreateFiltersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = filters_create_filters_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = filters_create_filters_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Filters update filters.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallFilterResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn filters_update_filters(
        &self,
        args: &FiltersUpdateFiltersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallFilterResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = filters_update_filters_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = filters_update_filters_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Filters delete filters.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallFilterDeleteResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn filters_delete_filters(
        &self,
        args: &FiltersDeleteFiltersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallFilterDeleteResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = filters_delete_filters_builder(
            &self.http_client,
            &args.zone_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = filters_delete_filters_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Filters get a filter.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallFilterResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn filters_get_a_filter(
        &self,
        args: &FiltersGetAFilterArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallFilterResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = filters_get_a_filter_builder(
            &self.http_client,
            &args.filter_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = filters_get_a_filter_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Filters update a filter.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallFilterResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn filters_update_a_filter(
        &self,
        args: &FiltersUpdateAFilterArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallFilterResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = filters_update_a_filter_builder(
            &self.http_client,
            &args.filter_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = filters_update_a_filter_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Filters delete a filter.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallFilterDeleteResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn filters_delete_a_filter(
        &self,
        args: &FiltersDeleteAFilterArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallFilterDeleteResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = filters_delete_a_filter_builder(
            &self.http_client,
            &args.filter_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = filters_delete_a_filter_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Firewall for ai custom topics get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafProductApiBundleResponseCustomTopics result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn firewall_for_ai_custom_topics_get(
        &self,
        args: &FirewallForAiCustomTopicsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafProductApiBundleResponseCustomTopics, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = firewall_for_ai_custom_topics_get_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = firewall_for_ai_custom_topics_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Firewall for ai custom topics put.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafProductApiBundleResponseCustomTopics result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn firewall_for_ai_custom_topics_put(
        &self,
        args: &FirewallForAiCustomTopicsPutArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafProductApiBundleResponseCustomTopics, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = firewall_for_ai_custom_topics_put_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = firewall_for_ai_custom_topics_put_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Firewall for ai settings get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafProductApiBundleResponseSettings result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn firewall_for_ai_settings_get(
        &self,
        args: &FirewallForAiSettingsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafProductApiBundleResponseSettings, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = firewall_for_ai_settings_get_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = firewall_for_ai_settings_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Firewall for ai settings put.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafProductApiBundleResponseSettings result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn firewall_for_ai_settings_put(
        &self,
        args: &FirewallForAiSettingsPutArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafProductApiBundleResponseSettings, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = firewall_for_ai_settings_put_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = firewall_for_ai_settings_put_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip access rules for a zone list ip access rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallRuleCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ip_access_rules_for_a_zone_list_ip_access_rules(
        &self,
        args: &IpAccessRulesForAZoneListIpAccessRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallRuleCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_access_rules_for_a_zone_list_ip_access_rules_builder(
            &self.http_client,
            &args.zone_id,
            &args.mode,
            &args.configuration_target,
            &args.configuration_value,
            &args.notes,
            &args.match_rs,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_access_rules_for_a_zone_list_ip_access_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip access rules for a zone create an ip access rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallRuleSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_access_rules_for_a_zone_create_an_ip_access_rule(
        &self,
        args: &IpAccessRulesForAZoneCreateAnIpAccessRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallRuleSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_access_rules_for_a_zone_create_an_ip_access_rule_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_access_rules_for_a_zone_create_an_ip_access_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip access rules for a zone update an ip access rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallRuleSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_access_rules_for_a_zone_update_an_ip_access_rule(
        &self,
        args: &IpAccessRulesForAZoneUpdateAnIpAccessRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallRuleSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_access_rules_for_a_zone_update_an_ip_access_rule_builder(
            &self.http_client,
            &args.zone_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_access_rules_for_a_zone_update_an_ip_access_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ip access rules for a zone delete an ip access rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallRuleSingleIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ip_access_rules_for_a_zone_delete_an_ip_access_rule(
        &self,
        args: &IpAccessRulesForAZoneDeleteAnIpAccessRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallRuleSingleIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ip_access_rules_for_a_zone_delete_an_ip_access_rule_builder(
            &self.http_client,
            &args.zone_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ip_access_rules_for_a_zone_delete_an_ip_access_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone lockdown list zone lockdown rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallZonelockdownResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_lockdown_list_zone_lockdown_rules(
        &self,
        args: &ZoneLockdownListZoneLockdownRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallZonelockdownResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_lockdown_list_zone_lockdown_rules_builder(
            &self.http_client,
            &args.zone_id,
            &args.page,
            &args.description,
            &args.modified_on,
            &args.ip,
            &args.priority,
            &args.uri_search,
            &args.ip_range_search,
            &args.per_page,
            &args.created_on,
            &args.description_search,
            &args.ip_search,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_lockdown_list_zone_lockdown_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone lockdown create a zone lockdown rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallZonelockdownResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_lockdown_create_a_zone_lockdown_rule(
        &self,
        args: &ZoneLockdownCreateAZoneLockdownRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallZonelockdownResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_lockdown_create_a_zone_lockdown_rule_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_lockdown_create_a_zone_lockdown_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone lockdown get a zone lockdown rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallZonelockdownResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_lockdown_get_a_zone_lockdown_rule(
        &self,
        args: &ZoneLockdownGetAZoneLockdownRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallZonelockdownResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_lockdown_get_a_zone_lockdown_rule_builder(
            &self.http_client,
            &args.lock_downs_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_lockdown_get_a_zone_lockdown_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone lockdown update a zone lockdown rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallZonelockdownResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_lockdown_update_a_zone_lockdown_rule(
        &self,
        args: &ZoneLockdownUpdateAZoneLockdownRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallZonelockdownResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_lockdown_update_a_zone_lockdown_rule_builder(
            &self.http_client,
            &args.lock_downs_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_lockdown_update_a_zone_lockdown_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone lockdown delete a zone lockdown rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_lockdown_delete_a_zone_lockdown_rule(
        &self,
        args: &ZoneLockdownDeleteAZoneLockdownRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_lockdown_delete_a_zone_lockdown_rule_builder(
            &self.http_client,
            &args.lock_downs_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_lockdown_delete_a_zone_lockdown_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Firewall rules list firewall rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallFilterRulesResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn firewall_rules_list_firewall_rules(
        &self,
        args: &FirewallRulesListFirewallRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallFilterRulesResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = firewall_rules_list_firewall_rules_builder(
            &self.http_client,
            &args.zone_id,
            &args.description,
            &args.action,
            &args.page,
            &args.per_page,
            &args.id,
            &args.paused,
        )
        .map_err(ProviderError::Api)?;

        let task = firewall_rules_list_firewall_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Firewall rules create firewall rules.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallFilterRulesResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn firewall_rules_create_firewall_rules(
        &self,
        args: &FirewallRulesCreateFirewallRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallFilterRulesResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = firewall_rules_create_firewall_rules_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = firewall_rules_create_firewall_rules_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Firewall rules update firewall rules.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallFilterRulesResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn firewall_rules_update_firewall_rules(
        &self,
        args: &FirewallRulesUpdateFirewallRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallFilterRulesResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = firewall_rules_update_firewall_rules_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = firewall_rules_update_firewall_rules_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Firewall rules update priority of firewall rules.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallFilterRulesResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn firewall_rules_update_priority_of_firewall_rules(
        &self,
        args: &FirewallRulesUpdatePriorityOfFirewallRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallFilterRulesResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = firewall_rules_update_priority_of_firewall_rules_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = firewall_rules_update_priority_of_firewall_rules_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Firewall rules delete firewall rules.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallFilterRulesResponseCollectionDelete result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn firewall_rules_delete_firewall_rules(
        &self,
        args: &FirewallRulesDeleteFirewallRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallFilterRulesResponseCollectionDelete, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = firewall_rules_delete_firewall_rules_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = firewall_rules_delete_firewall_rules_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Firewall rules get a firewall rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallFilterRulesSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn firewall_rules_get_a_firewall_rule(
        &self,
        args: &FirewallRulesGetAFirewallRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallFilterRulesSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = firewall_rules_get_a_firewall_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.zone_id,
            &args.id,
        )
        .map_err(ProviderError::Api)?;

        let task = firewall_rules_get_a_firewall_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Firewall rules update a firewall rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallFilterRulesSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn firewall_rules_update_a_firewall_rule(
        &self,
        args: &FirewallRulesUpdateAFirewallRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallFilterRulesSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = firewall_rules_update_a_firewall_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = firewall_rules_update_a_firewall_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Firewall rules update priority of a firewall rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallFilterRulesResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn firewall_rules_update_priority_of_a_firewall_rule(
        &self,
        args: &FirewallRulesUpdatePriorityOfAFirewallRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallFilterRulesResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = firewall_rules_update_priority_of_a_firewall_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = firewall_rules_update_priority_of_a_firewall_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Firewall rules delete a firewall rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallFilterRulesSingleResponseDelete result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn firewall_rules_delete_a_firewall_rule(
        &self,
        args: &FirewallRulesDeleteAFirewallRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallFilterRulesSingleResponseDelete, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = firewall_rules_delete_a_firewall_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = firewall_rules_delete_a_firewall_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User agent blocking rules list user agent blocking rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallFirewalluablockResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn user_agent_blocking_rules_list_user_agent_blocking_rules(
        &self,
        args: &UserAgentBlockingRulesListUserAgentBlockingRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallFirewalluablockResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_agent_blocking_rules_list_user_agent_blocking_rules_builder(
            &self.http_client,
            &args.zone_id,
            &args.page,
            &args.description,
            &args.per_page,
            &args.user_agent,
            &args.paused,
        )
        .map_err(ProviderError::Api)?;

        let task = user_agent_blocking_rules_list_user_agent_blocking_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User agent blocking rules create a user agent blocking rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallFirewalluablockResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn user_agent_blocking_rules_create_a_user_agent_blocking_rule(
        &self,
        args: &UserAgentBlockingRulesCreateAUserAgentBlockingRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallFirewalluablockResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_agent_blocking_rules_create_a_user_agent_blocking_rule_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = user_agent_blocking_rules_create_a_user_agent_blocking_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User agent blocking rules get a user agent blocking rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallFirewalluablockResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn user_agent_blocking_rules_get_a_user_agent_blocking_rule(
        &self,
        args: &UserAgentBlockingRulesGetAUserAgentBlockingRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallFirewalluablockResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_agent_blocking_rules_get_a_user_agent_blocking_rule_builder(
            &self.http_client,
            &args.ua_rule_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = user_agent_blocking_rules_get_a_user_agent_blocking_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User agent blocking rules update a user agent blocking rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallFirewalluablockResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn user_agent_blocking_rules_update_a_user_agent_blocking_rule(
        &self,
        args: &UserAgentBlockingRulesUpdateAUserAgentBlockingRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallFirewalluablockResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_agent_blocking_rules_update_a_user_agent_blocking_rule_builder(
            &self.http_client,
            &args.ua_rule_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = user_agent_blocking_rules_update_a_user_agent_blocking_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// User agent blocking rules delete a user agent blocking rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn user_agent_blocking_rules_delete_a_user_agent_blocking_rule(
        &self,
        args: &UserAgentBlockingRulesDeleteAUserAgentBlockingRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = user_agent_blocking_rules_delete_a_user_agent_blocking_rule_builder(
            &self.http_client,
            &args.ua_rule_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = user_agent_blocking_rules_delete_a_user_agent_blocking_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf overrides list waf overrides.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallOverrideResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waf_overrides_list_waf_overrides(
        &self,
        args: &WafOverridesListWafOverridesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallOverrideResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_overrides_list_waf_overrides_builder(
            &self.http_client,
            &args.zone_id,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_overrides_list_waf_overrides_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf overrides create a waf override.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallOverrideResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waf_overrides_create_a_waf_override(
        &self,
        args: &WafOverridesCreateAWafOverrideArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallOverrideResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_overrides_create_a_waf_override_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_overrides_create_a_waf_override_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf overrides get a waf override.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallOverrideResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waf_overrides_get_a_waf_override(
        &self,
        args: &WafOverridesGetAWafOverrideArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallOverrideResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_overrides_get_a_waf_override_builder(
            &self.http_client,
            &args.overrides_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_overrides_get_a_waf_override_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf overrides update waf override.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallOverrideResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waf_overrides_update_waf_override(
        &self,
        args: &WafOverridesUpdateWafOverrideArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallOverrideResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_overrides_update_waf_override_builder(
            &self.http_client,
            &args.overrides_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_overrides_update_waf_override_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf overrides delete a waf override.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waf_overrides_delete_a_waf_override(
        &self,
        args: &WafOverridesDeleteAWafOverrideArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_overrides_delete_a_waf_override_builder(
            &self.http_client,
            &args.overrides_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_overrides_delete_a_waf_override_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf packages list waf packages.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallPackageResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waf_packages_list_waf_packages(
        &self,
        args: &WafPackagesListWafPackagesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallPackageResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_packages_list_waf_packages_builder(
            &self.http_client,
            &args.zone_id,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
            &args.match_rs,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_packages_list_waf_packages_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf packages get a waf package.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallPackageResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waf_packages_get_a_waf_package(
        &self,
        args: &WafPackagesGetAWafPackageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallPackageResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_packages_get_a_waf_package_builder(
            &self.http_client,
            &args.package_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_packages_get_a_waf_package_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf packages update a waf package.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waf_packages_update_a_waf_package(
        &self,
        args: &WafPackagesUpdateAWafPackageArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_packages_update_a_waf_package_builder(
            &self.http_client,
            &args.package_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_packages_update_a_waf_package_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf rule groups list waf rule groups.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafManagedRulesRuleGroupResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waf_rule_groups_list_waf_rule_groups(
        &self,
        args: &WafRuleGroupsListWafRuleGroupsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafManagedRulesRuleGroupResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_rule_groups_list_waf_rule_groups_builder(
            &self.http_client,
            &args.package_id,
            &args.zone_id,
            &args.mode,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
            &args.match_rs,
            &args.name,
            &args.rules_count,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_rule_groups_list_waf_rule_groups_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf rule groups get a waf rule group.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafManagedRulesRuleGroupResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waf_rule_groups_get_a_waf_rule_group(
        &self,
        args: &WafRuleGroupsGetAWafRuleGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafManagedRulesRuleGroupResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_rule_groups_get_a_waf_rule_group_builder(
            &self.http_client,
            &args.group_id,
            &args.package_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_rule_groups_get_a_waf_rule_group_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf rule groups update a waf rule group.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafManagedRulesRuleGroupResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waf_rule_groups_update_a_waf_rule_group(
        &self,
        args: &WafRuleGroupsUpdateAWafRuleGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafManagedRulesRuleGroupResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_rule_groups_update_a_waf_rule_group_builder(
            &self.http_client,
            &args.group_id,
            &args.package_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_rule_groups_update_a_waf_rule_group_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf rules list waf rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafManagedRulesRuleResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waf_rules_list_waf_rules(
        &self,
        args: &WafRulesListWafRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafManagedRulesRuleResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_rules_list_waf_rules_builder(
            &self.http_client,
            &args.package_id,
            &args.zone_id,
            &args.mode,
            &args.group_id,
            &args.page,
            &args.per_page,
            &args.order,
            &args.direction,
            &args.match_rs,
            &args.description,
            &args.priority,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_rules_list_waf_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf rules get a waf rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafManagedRulesRuleResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waf_rules_get_a_waf_rule(
        &self,
        args: &WafRulesGetAWafRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafManagedRulesRuleResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_rules_get_a_waf_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.package_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_rules_get_a_waf_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf rules update a waf rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waf_rules_update_a_waf_rule(
        &self,
        args: &WafRulesUpdateAWafRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_rules_update_a_waf_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.package_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_rules_update_a_waf_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Fraud detection zone get settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FraudFraudSettingsResponseBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn fraud_detection_zone_get_settings(
        &self,
        args: &FraudDetectionZoneGetSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FraudFraudSettingsResponseBody, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = fraud_detection_zone_get_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = fraud_detection_zone_get_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Fraud detection zone update settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FraudFraudSettingsResponseBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn fraud_detection_zone_update_settings(
        &self,
        args: &FraudDetectionZoneUpdateSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FraudFraudSettingsResponseBody, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = fraud_detection_zone_update_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = fraud_detection_zone_update_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Health checks list health checks.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthchecksResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn health_checks_list_health_checks(
        &self,
        args: &HealthChecksListHealthChecksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthchecksResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = health_checks_list_health_checks_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = health_checks_list_health_checks_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Health checks create health check.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthchecksSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn health_checks_create_health_check(
        &self,
        args: &HealthChecksCreateHealthCheckArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthchecksSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = health_checks_create_health_check_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = health_checks_create_health_check_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Health checks create preview health check.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthchecksSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn health_checks_create_preview_health_check(
        &self,
        args: &HealthChecksCreatePreviewHealthCheckArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthchecksSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = health_checks_create_preview_health_check_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = health_checks_create_preview_health_check_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Health checks health check preview details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthchecksSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn health_checks_health_check_preview_details(
        &self,
        args: &HealthChecksHealthCheckPreviewDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthchecksSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = health_checks_health_check_preview_details_builder(
            &self.http_client,
            &args.healthcheck_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = health_checks_health_check_preview_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Health checks delete preview health check.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthchecksIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn health_checks_delete_preview_health_check(
        &self,
        args: &HealthChecksDeletePreviewHealthCheckArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthchecksIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = health_checks_delete_preview_health_check_builder(
            &self.http_client,
            &args.healthcheck_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = health_checks_delete_preview_health_check_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Health checks health check details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthchecksSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn health_checks_health_check_details(
        &self,
        args: &HealthChecksHealthCheckDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthchecksSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = health_checks_health_check_details_builder(
            &self.http_client,
            &args.healthcheck_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = health_checks_health_check_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Health checks update health check.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthchecksSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn health_checks_update_health_check(
        &self,
        args: &HealthChecksUpdateHealthCheckArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthchecksSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = health_checks_update_health_check_builder(
            &self.http_client,
            &args.healthcheck_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = health_checks_update_health_check_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Health checks patch health check.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthchecksSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn health_checks_patch_health_check(
        &self,
        args: &HealthChecksPatchHealthCheckArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthchecksSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = health_checks_patch_health_check_builder(
            &self.http_client,
            &args.healthcheck_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = health_checks_patch_health_check_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Health checks delete health check.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthchecksIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn health_checks_delete_health_check(
        &self,
        args: &HealthChecksDeleteHealthCheckArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthchecksIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = health_checks_delete_health_check_builder(
            &self.http_client,
            &args.healthcheck_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = health_checks_delete_health_check_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zones 0 hold get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zones_0_hold_get(
        &self,
        args: &Zones0HoldGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zones_0_hold_get_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zones_0_hold_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zones 0 hold post.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zones_0_hold_post(
        &self,
        args: &Zones0HoldPostArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zones_0_hold_post_builder(
            &self.http_client,
            &args.zone_id,
            &args.include_subdomains,
        )
        .map_err(ProviderError::Api)?;

        let task = zones_0_hold_post_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zones 0 hold patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zones_0_hold_patch(
        &self,
        args: &Zones0HoldPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zones_0_hold_patch_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zones_0_hold_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zones 0 hold delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zones_0_hold_delete(
        &self,
        args: &Zones0HoldDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zones_0_hold_delete_builder(
            &self.http_client,
            &args.zone_id,
            &args.hold_after,
        )
        .map_err(ProviderError::Api)?;

        let task = zones_0_hold_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Per hostname tls settings list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesPerHostnameSettingsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn per_hostname_tls_settings_list(
        &self,
        args: &PerHostnameTlsSettingsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesPerHostnameSettingsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = per_hostname_tls_settings_list_builder(
            &self.http_client,
            &args.zone_id,
            &args.setting_id,
        )
        .map_err(ProviderError::Api)?;

        let task = per_hostname_tls_settings_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Per hostname tls settings get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesPerHostnameSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn per_hostname_tls_settings_get(
        &self,
        args: &PerHostnameTlsSettingsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesPerHostnameSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = per_hostname_tls_settings_get_builder(
            &self.http_client,
            &args.zone_id,
            &args.setting_id,
            &args.hostname,
        )
        .map_err(ProviderError::Api)?;

        let task = per_hostname_tls_settings_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Per hostname tls settings put.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesPerHostnameSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn per_hostname_tls_settings_put(
        &self,
        args: &PerHostnameTlsSettingsPutArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesPerHostnameSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = per_hostname_tls_settings_put_builder(
            &self.http_client,
            &args.zone_id,
            &args.setting_id,
            &args.hostname,
        )
        .map_err(ProviderError::Api)?;

        let task = per_hostname_tls_settings_put_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Per hostname tls settings delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesPerHostnameSettingsResponseDelete result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn per_hostname_tls_settings_delete(
        &self,
        args: &PerHostnameTlsSettingsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesPerHostnameSettingsResponseDelete, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = per_hostname_tls_settings_delete_builder(
            &self.http_client,
            &args.zone_id,
            &args.setting_id,
            &args.hostname,
        )
        .map_err(ProviderError::Api)?;

        let task = per_hostname_tls_settings_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Keyless ssl for a zone list keyless ssl configurations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesKeylessResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn keyless_ssl_for_a_zone_list_keyless_ssl_configurations(
        &self,
        args: &KeylessSslForAZoneListKeylessSslConfigurationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesKeylessResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = keyless_ssl_for_a_zone_list_keyless_ssl_configurations_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = keyless_ssl_for_a_zone_list_keyless_ssl_configurations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Keyless ssl for a zone create keyless ssl configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesKeylessResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn keyless_ssl_for_a_zone_create_keyless_ssl_configuration(
        &self,
        args: &KeylessSslForAZoneCreateKeylessSslConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesKeylessResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = keyless_ssl_for_a_zone_create_keyless_ssl_configuration_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = keyless_ssl_for_a_zone_create_keyless_ssl_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Keyless ssl for a zone get keyless ssl configuration.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesKeylessResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn keyless_ssl_for_a_zone_get_keyless_ssl_configuration(
        &self,
        args: &KeylessSslForAZoneGetKeylessSslConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesKeylessResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = keyless_ssl_for_a_zone_get_keyless_ssl_configuration_builder(
            &self.http_client,
            &args.keyless_certificate_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = keyless_ssl_for_a_zone_get_keyless_ssl_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Keyless ssl for a zone edit keyless ssl configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesKeylessResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn keyless_ssl_for_a_zone_edit_keyless_ssl_configuration(
        &self,
        args: &KeylessSslForAZoneEditKeylessSslConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesKeylessResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = keyless_ssl_for_a_zone_edit_keyless_ssl_configuration_builder(
            &self.http_client,
            &args.keyless_certificate_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = keyless_ssl_for_a_zone_edit_keyless_ssl_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Keyless ssl for a zone delete keyless ssl configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesKeylessResponseSingleId result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn keyless_ssl_for_a_zone_delete_keyless_ssl_configuration(
        &self,
        args: &KeylessSslForAZoneDeleteKeylessSslConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesKeylessResponseSingleId, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = keyless_ssl_for_a_zone_delete_keyless_ssl_configuration_builder(
            &self.http_client,
            &args.keyless_certificate_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = keyless_ssl_for_a_zone_delete_keyless_ssl_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf product api leaked credentials get status.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafProductApiBundleResponseStatus result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waf_product_api_leaked_credentials_get_status(
        &self,
        args: &WafProductApiLeakedCredentialsGetStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafProductApiBundleResponseStatus, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_product_api_leaked_credentials_get_status_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_product_api_leaked_credentials_get_status_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf product api leaked credentials set status.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafProductApiBundleResponseStatus result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waf_product_api_leaked_credentials_set_status(
        &self,
        args: &WafProductApiLeakedCredentialsSetStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafProductApiBundleResponseStatus, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_product_api_leaked_credentials_set_status_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_product_api_leaked_credentials_set_status_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf product api leaked credentials list detections.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafProductApiBundleResponseCustomDetectionCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waf_product_api_leaked_credentials_list_detections(
        &self,
        args: &WafProductApiLeakedCredentialsListDetectionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafProductApiBundleResponseCustomDetectionCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_product_api_leaked_credentials_list_detections_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_product_api_leaked_credentials_list_detections_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf product api leaked credentials create detection.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafProductApiBundleResponseCustomDetection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waf_product_api_leaked_credentials_create_detection(
        &self,
        args: &WafProductApiLeakedCredentialsCreateDetectionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafProductApiBundleResponseCustomDetection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_product_api_leaked_credentials_create_detection_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_product_api_leaked_credentials_create_detection_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf product api leaked credentials get detection.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafProductApiBundleResponseCustomDetection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waf_product_api_leaked_credentials_get_detection(
        &self,
        args: &WafProductApiLeakedCredentialsGetDetectionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafProductApiBundleResponseCustomDetection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_product_api_leaked_credentials_get_detection_builder(
            &self.http_client,
            &args.zone_id,
            &args.detection_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_product_api_leaked_credentials_get_detection_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf product api leaked credentials update detection.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafProductApiBundleResponseCustomDetection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waf_product_api_leaked_credentials_update_detection(
        &self,
        args: &WafProductApiLeakedCredentialsUpdateDetectionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafProductApiBundleResponseCustomDetection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_product_api_leaked_credentials_update_detection_builder(
            &self.http_client,
            &args.zone_id,
            &args.detection_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_product_api_leaked_credentials_update_detection_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waf product api leaked credentials delete detection.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WafProductApiBundleApiResponseCommon result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waf_product_api_leaked_credentials_delete_detection(
        &self,
        args: &WafProductApiLeakedCredentialsDeleteDetectionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WafProductApiBundleApiResponseCommon, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waf_product_api_leaked_credentials_delete_detection_builder(
            &self.http_client,
            &args.zone_id,
            &args.detection_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waf_product_api_leaked_credentials_delete_detection_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancers list load balancers.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingLoadBalancerComponentsSchemasResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn load_balancers_list_load_balancers(
        &self,
        args: &LoadBalancersListLoadBalancersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingLoadBalancerComponentsSchemasResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancers_list_load_balancers_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancers_list_load_balancers_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancers create load balancer.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingLoadBalancerComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn load_balancers_create_load_balancer(
        &self,
        args: &LoadBalancersCreateLoadBalancerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingLoadBalancerComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancers_create_load_balancer_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancers_create_load_balancer_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancers load balancer details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingLoadBalancerComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn load_balancers_load_balancer_details(
        &self,
        args: &LoadBalancersLoadBalancerDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingLoadBalancerComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancers_load_balancer_details_builder(
            &self.http_client,
            &args.zone_id,
            &args.load_balancer_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancers_load_balancer_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancers update load balancer.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingLoadBalancerComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn load_balancers_update_load_balancer(
        &self,
        args: &LoadBalancersUpdateLoadBalancerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingLoadBalancerComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancers_update_load_balancer_builder(
            &self.http_client,
            &args.zone_id,
            &args.load_balancer_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancers_update_load_balancer_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancers patch load balancer.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingLoadBalancerComponentsSchemasSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn load_balancers_patch_load_balancer(
        &self,
        args: &LoadBalancersPatchLoadBalancerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingLoadBalancerComponentsSchemasSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancers_patch_load_balancer_builder(
            &self.http_client,
            &args.zone_id,
            &args.load_balancer_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancers_patch_load_balancer_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Load balancers delete load balancer.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LoadBalancingComponentsSchemasIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn load_balancers_delete_load_balancer(
        &self,
        args: &LoadBalancersDeleteLoadBalancerArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LoadBalancingComponentsSchemasIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = load_balancers_delete_load_balancer_builder(
            &self.http_client,
            &args.zone_id,
            &args.load_balancer_id,
        )
        .map_err(ProviderError::Api)?;

        let task = load_balancers_delete_load_balancer_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zones zone id logpush datasets dataset id fields.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushLogpushFieldResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zones_zone_id_logpush_datasets_dataset_id_fields(
        &self,
        args: &GetZonesZoneIdLogpushDatasetsDatasetIdFieldsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushLogpushFieldResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zones_zone_id_logpush_datasets_dataset_id_fields_builder(
            &self.http_client,
            &args.dataset_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zones_zone_id_logpush_datasets_dataset_id_fields_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zones zone id logpush datasets dataset id jobs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushLogpushJobResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zones_zone_id_logpush_datasets_dataset_id_jobs(
        &self,
        args: &GetZonesZoneIdLogpushDatasetsDatasetIdJobsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushLogpushJobResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zones_zone_id_logpush_datasets_dataset_id_jobs_builder(
            &self.http_client,
            &args.dataset_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zones_zone_id_logpush_datasets_dataset_id_jobs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zones zone id logpush edge jobs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushInstantLogsJobResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zones_zone_id_logpush_edge_jobs(
        &self,
        args: &GetZonesZoneIdLogpushEdgeJobsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushInstantLogsJobResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zones_zone_id_logpush_edge_jobs_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zones_zone_id_logpush_edge_jobs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post zones zone id logpush edge jobs.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushInstantLogsJobResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_zones_zone_id_logpush_edge_jobs(
        &self,
        args: &PostZonesZoneIdLogpushEdgeJobsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushInstantLogsJobResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_zones_zone_id_logpush_edge_jobs_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_zones_zone_id_logpush_edge_jobs_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zones zone id logpush jobs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushLogpushJobResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zones_zone_id_logpush_jobs(
        &self,
        args: &GetZonesZoneIdLogpushJobsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushLogpushJobResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zones_zone_id_logpush_jobs_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zones_zone_id_logpush_jobs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post zones zone id logpush jobs.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushLogpushJobResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_zones_zone_id_logpush_jobs(
        &self,
        args: &PostZonesZoneIdLogpushJobsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushLogpushJobResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_zones_zone_id_logpush_jobs_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_zones_zone_id_logpush_jobs_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zones zone id logpush jobs job id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushLogpushJobResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zones_zone_id_logpush_jobs_job_id(
        &self,
        args: &GetZonesZoneIdLogpushJobsJobIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushLogpushJobResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zones_zone_id_logpush_jobs_job_id_builder(
            &self.http_client,
            &args.job_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zones_zone_id_logpush_jobs_job_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Put zones zone id logpush jobs job id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushLogpushJobResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn put_zones_zone_id_logpush_jobs_job_id(
        &self,
        args: &PutZonesZoneIdLogpushJobsJobIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushLogpushJobResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = put_zones_zone_id_logpush_jobs_job_id_builder(
            &self.http_client,
            &args.job_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = put_zones_zone_id_logpush_jobs_job_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete zones zone id logpush jobs job id.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_zones_zone_id_logpush_jobs_job_id(
        &self,
        args: &DeleteZonesZoneIdLogpushJobsJobIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_zones_zone_id_logpush_jobs_job_id_builder(
            &self.http_client,
            &args.job_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_zones_zone_id_logpush_jobs_job_id_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post zones zone id logpush ownership.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushGetOwnershipResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_zones_zone_id_logpush_ownership(
        &self,
        args: &PostZonesZoneIdLogpushOwnershipArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushGetOwnershipResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_zones_zone_id_logpush_ownership_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_zones_zone_id_logpush_ownership_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post zones zone id logpush ownership validate.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushValidateOwnershipResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn post_zones_zone_id_logpush_ownership_validate(
        &self,
        args: &PostZonesZoneIdLogpushOwnershipValidateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushValidateOwnershipResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_zones_zone_id_logpush_ownership_validate_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_zones_zone_id_logpush_ownership_validate_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post zones zone id logpush validate destination.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushValidateResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn post_zones_zone_id_logpush_validate_destination(
        &self,
        args: &PostZonesZoneIdLogpushValidateDestinationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushValidateResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_zones_zone_id_logpush_validate_destination_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_zones_zone_id_logpush_validate_destination_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post zones zone id logpush validate destination exists.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushDestinationExistsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn post_zones_zone_id_logpush_validate_destination_exists(
        &self,
        args: &PostZonesZoneIdLogpushValidateDestinationExistsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushDestinationExistsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_zones_zone_id_logpush_validate_destination_exists_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_zones_zone_id_logpush_validate_destination_exists_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post zones zone id logpush validate origin.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogpushValidateResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn post_zones_zone_id_logpush_validate_origin(
        &self,
        args: &PostZonesZoneIdLogpushValidateOriginArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogpushValidateResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_zones_zone_id_logpush_validate_origin_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_zones_zone_id_logpush_validate_origin_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zones zone id logs control retention flag.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogcontrolRetentionFlagResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zones_zone_id_logs_control_retention_flag(
        &self,
        args: &GetZonesZoneIdLogsControlRetentionFlagArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogcontrolRetentionFlagResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zones_zone_id_logs_control_retention_flag_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zones_zone_id_logs_control_retention_flag_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post zones zone id logs control retention flag.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogcontrolRetentionFlagResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_zones_zone_id_logs_control_retention_flag(
        &self,
        args: &PostZonesZoneIdLogsControlRetentionFlagArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogcontrolRetentionFlagResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_zones_zone_id_logs_control_retention_flag_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_zones_zone_id_logs_control_retention_flag_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zones zone id logs rayids ray id.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogshareLogsResponseJsonLines result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zones_zone_id_logs_rayids_ray_id(
        &self,
        args: &GetZonesZoneIdLogsRayidsRayIdArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogshareLogsResponseJsonLines, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zones_zone_id_logs_rayids_ray_id_builder(
            &self.http_client,
            &args.zone_id,
            &args.ray_id,
            &args.fields,
            &args.timestamps,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zones_zone_id_logs_rayids_ray_id_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zones zone id logs received.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogshareLogsResponseJsonLines result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zones_zone_id_logs_received(
        &self,
        args: &GetZonesZoneIdLogsReceivedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogshareLogsResponseJsonLines, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zones_zone_id_logs_received_builder(
            &self.http_client,
            &args.zone_id,
            &args.start,
            &args.end,
            &args.fields,
            &args.sample,
            &args.count,
            &args.timestamps,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zones_zone_id_logs_received_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zones zone id logs received fields.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LogshareFieldsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zones_zone_id_logs_received_fields(
        &self,
        args: &GetZonesZoneIdLogsReceivedFieldsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LogshareFieldsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zones_zone_id_logs_received_fields_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zones_zone_id_logs_received_fields_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List managed transforms.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_managed_transforms(
        &self,
        args: &ListManagedTransformsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_managed_transforms_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = list_managed_transforms_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update managed transforms.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_managed_transforms(
        &self,
        args: &UpdateManagedTransformsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_managed_transforms_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_managed_transforms_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete managed transforms.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_managed_transforms(
        &self,
        args: &DeleteManagedTransformsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_managed_transforms_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_managed_transforms_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level authenticated origin pulls list certificates.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesComponentsSchemasCertificateResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_authenticated_origin_pulls_list_certificates(
        &self,
        args: &ZoneLevelAuthenticatedOriginPullsListCertificatesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesComponentsSchemasCertificateResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_authenticated_origin_pulls_list_certificates_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_authenticated_origin_pulls_list_certificates_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level authenticated origin pulls upload certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesComponentsSchemasCertificateResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_authenticated_origin_pulls_upload_certificate(
        &self,
        args: &ZoneLevelAuthenticatedOriginPullsUploadCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesComponentsSchemasCertificateResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_authenticated_origin_pulls_upload_certificate_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_authenticated_origin_pulls_upload_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Per hostname authenticated origin pull list hostname associations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesHostnameAssocResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn per_hostname_authenticated_origin_pull_list_hostname_associations(
        &self,
        args: &PerHostnameAuthenticatedOriginPullListHostnameAssociationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesHostnameAssocResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = per_hostname_authenticated_origin_pull_list_hostname_associations_builder(
            &self.http_client,
            &args.zone_id,
            &args.page,
            &args.per_page,
            &args.status,
        )
        .map_err(ProviderError::Api)?;

        let task = per_hostname_authenticated_origin_pull_list_hostname_associations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Per hostname authenticated origin pull enable or disable a hostname for client authentication.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesHostnameAopResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn per_hostname_authenticated_origin_pull_enable_or_disable_a_hostname_for_client_authentication(
        &self,
        args: &PerHostnameAuthenticatedOriginPullEnableOrDisableAHostnameForClientAuthenticationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesHostnameAopResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = per_hostname_authenticated_origin_pull_enable_or_disable_a_hostname_for_client_authentication_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = per_hostname_authenticated_origin_pull_enable_or_disable_a_hostname_for_client_authentication_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Per hostname authenticated origin pull list certificates.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesHostnameAuthenticatedOriginPullComponentsSchemasCertificateResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn per_hostname_authenticated_origin_pull_list_certificates(
        &self,
        args: &PerHostnameAuthenticatedOriginPullListCertificatesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesHostnameAuthenticatedOriginPullComponentsSchemasCertificateResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = per_hostname_authenticated_origin_pull_list_certificates_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = per_hostname_authenticated_origin_pull_list_certificates_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Per hostname authenticated origin pull upload a hostname client certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesHostnameAuthenticatedOriginPullComponentsSchemasCertificateResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn per_hostname_authenticated_origin_pull_upload_a_hostname_client_certificate(
        &self,
        args: &PerHostnameAuthenticatedOriginPullUploadAHostnameClientCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesHostnameAuthenticatedOriginPullComponentsSchemasCertificateResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = per_hostname_authenticated_origin_pull_upload_a_hostname_client_certificate_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = per_hostname_authenticated_origin_pull_upload_a_hostname_client_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Per hostname authenticated origin pull get the hostname client certificate.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesHostnameAuthenticatedOriginPullComponentsSchemasCertificateResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn per_hostname_authenticated_origin_pull_get_the_hostname_client_certificate(
        &self,
        args: &PerHostnameAuthenticatedOriginPullGetTheHostnameClientCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesHostnameAuthenticatedOriginPullComponentsSchemasCertificateResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = per_hostname_authenticated_origin_pull_get_the_hostname_client_certificate_builder(
            &self.http_client,
            &args.certificate_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = per_hostname_authenticated_origin_pull_get_the_hostname_client_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Per hostname authenticated origin pull delete hostname client certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesHostnameAuthenticatedOriginPullComponentsSchemasCertificateResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn per_hostname_authenticated_origin_pull_delete_hostname_client_certificate(
        &self,
        args: &PerHostnameAuthenticatedOriginPullDeleteHostnameClientCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesHostnameAuthenticatedOriginPullComponentsSchemasCertificateResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = per_hostname_authenticated_origin_pull_delete_hostname_client_certificate_builder(
            &self.http_client,
            &args.certificate_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = per_hostname_authenticated_origin_pull_delete_hostname_client_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Per hostname authenticated origin pull get the hostname status for client authentication.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesHostnameAopSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn per_hostname_authenticated_origin_pull_get_the_hostname_status_for_client_authentication(
        &self,
        args: &PerHostnameAuthenticatedOriginPullGetTheHostnameStatusForClientAuthenticationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesHostnameAopSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = per_hostname_authenticated_origin_pull_get_the_hostname_status_for_client_authentication_builder(
            &self.http_client,
            &args.hostname,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = per_hostname_authenticated_origin_pull_get_the_hostname_status_for_client_authentication_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level authenticated origin pulls get enablement setting for zone.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesEnabledResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_authenticated_origin_pulls_get_enablement_setting_for_zone(
        &self,
        args: &ZoneLevelAuthenticatedOriginPullsGetEnablementSettingForZoneArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesEnabledResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_authenticated_origin_pulls_get_enablement_setting_for_zone_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_authenticated_origin_pulls_get_enablement_setting_for_zone_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level authenticated origin pulls set enablement for zone.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesEnabledResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_authenticated_origin_pulls_set_enablement_for_zone(
        &self,
        args: &ZoneLevelAuthenticatedOriginPullsSetEnablementForZoneArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesEnabledResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_authenticated_origin_pulls_set_enablement_for_zone_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_authenticated_origin_pulls_set_enablement_for_zone_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level authenticated origin pulls get certificate details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesComponentsSchemasCertificateResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_level_authenticated_origin_pulls_get_certificate_details(
        &self,
        args: &ZoneLevelAuthenticatedOriginPullsGetCertificateDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesComponentsSchemasCertificateResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_authenticated_origin_pulls_get_certificate_details_builder(
            &self.http_client,
            &args.certificate_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_authenticated_origin_pulls_get_certificate_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone level authenticated origin pulls delete certificate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesComponentsSchemasCertificateResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_level_authenticated_origin_pulls_delete_certificate(
        &self,
        args: &ZoneLevelAuthenticatedOriginPullsDeleteCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesComponentsSchemasCertificateResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_level_authenticated_origin_pulls_delete_certificate_builder(
            &self.http_client,
            &args.certificate_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_level_authenticated_origin_pulls_delete_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Page shield get settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn page_shield_get_settings(
        &self,
        args: &PageShieldGetSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = page_shield_get_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = page_shield_get_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Page shield update settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn page_shield_update_settings(
        &self,
        args: &PageShieldUpdateSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = page_shield_update_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = page_shield_update_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Page shield list connections.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PageShieldListZoneConnectionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn page_shield_list_connections(
        &self,
        args: &PageShieldListConnectionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PageShieldListZoneConnectionsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = page_shield_list_connections_builder(
            &self.http_client,
            &args.zone_id,
            &args.exclude_urls,
            &args.urls,
            &args.hosts,
            &args.page,
            &args.per_page,
            &args.order_by,
            &args.direction,
            &args.prioritize_malicious,
            &args.exclude_cdn_cgi,
            &args.status,
            &args.page_url,
            &args.export,
        )
        .map_err(ProviderError::Api)?;

        let task = page_shield_list_connections_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Page shield get connection.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PageShieldGetZoneConnectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn page_shield_get_connection(
        &self,
        args: &PageShieldGetConnectionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PageShieldGetZoneConnectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = page_shield_get_connection_builder(
            &self.http_client,
            &args.zone_id,
            &args.connection_id,
        )
        .map_err(ProviderError::Api)?;

        let task = page_shield_get_connection_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Page shield list cookies.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PageShieldListZoneCookiesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn page_shield_list_cookies(
        &self,
        args: &PageShieldListCookiesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PageShieldListZoneCookiesResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = page_shield_list_cookies_builder(
            &self.http_client,
            &args.zone_id,
            &args.hosts,
            &args.page,
            &args.per_page,
            &args.order_by,
            &args.direction,
            &args.page_url,
            &args.export,
            &args.name,
            &args.secure,
            &args.http_only,
            &args.same_site,
            &args.type_rs,
            &args.path,
            &args.domain,
        )
        .map_err(ProviderError::Api)?;

        let task = page_shield_list_cookies_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Page shield get cookie.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PageShieldGetZoneCookieResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn page_shield_get_cookie(
        &self,
        args: &PageShieldGetCookieArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PageShieldGetZoneCookieResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = page_shield_get_cookie_builder(
            &self.http_client,
            &args.zone_id,
            &args.cookie_id,
        )
        .map_err(ProviderError::Api)?;

        let task = page_shield_get_cookie_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Page shield list policies.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PageShieldListZonePoliciesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn page_shield_list_policies(
        &self,
        args: &PageShieldListPoliciesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PageShieldListZonePoliciesResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = page_shield_list_policies_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = page_shield_list_policies_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Page shield create policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PageShieldGetZonePolicyResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn page_shield_create_policy(
        &self,
        args: &PageShieldCreatePolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PageShieldGetZonePolicyResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = page_shield_create_policy_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = page_shield_create_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Page shield get policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PageShieldGetZonePolicyResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn page_shield_get_policy(
        &self,
        args: &PageShieldGetPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PageShieldGetZonePolicyResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = page_shield_get_policy_builder(
            &self.http_client,
            &args.zone_id,
            &args.policy_id,
        )
        .map_err(ProviderError::Api)?;

        let task = page_shield_get_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Page shield update policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PageShieldGetZonePolicyResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn page_shield_update_policy(
        &self,
        args: &PageShieldUpdatePolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PageShieldGetZonePolicyResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = page_shield_update_policy_builder(
            &self.http_client,
            &args.zone_id,
            &args.policy_id,
        )
        .map_err(ProviderError::Api)?;

        let task = page_shield_update_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Page shield delete policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn page_shield_delete_policy(
        &self,
        args: &PageShieldDeletePolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = page_shield_delete_policy_builder(
            &self.http_client,
            &args.zone_id,
            &args.policy_id,
        )
        .map_err(ProviderError::Api)?;

        let task = page_shield_delete_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Page shield list scripts.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PageShieldListZoneScriptsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn page_shield_list_scripts(
        &self,
        args: &PageShieldListScriptsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PageShieldListZoneScriptsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = page_shield_list_scripts_builder(
            &self.http_client,
            &args.zone_id,
            &args.exclude_urls,
            &args.urls,
            &args.hosts,
            &args.page,
            &args.per_page,
            &args.order_by,
            &args.direction,
            &args.prioritize_malicious,
            &args.exclude_cdn_cgi,
            &args.exclude_duplicates,
            &args.status,
            &args.page_url,
            &args.export,
        )
        .map_err(ProviderError::Api)?;

        let task = page_shield_list_scripts_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Page shield get script.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PageShieldGetZoneScriptResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn page_shield_get_script(
        &self,
        args: &PageShieldGetScriptArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PageShieldGetZoneScriptResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = page_shield_get_script_builder(
            &self.http_client,
            &args.zone_id,
            &args.script_id,
        )
        .map_err(ProviderError::Api)?;

        let task = page_shield_get_script_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Page rules list page rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn page_rules_list_page_rules(
        &self,
        args: &PageRulesListPageRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = page_rules_list_page_rules_builder(
            &self.http_client,
            &args.zone_id,
            &args.order,
            &args.direction,
            &args.match_rs,
            &args.status,
        )
        .map_err(ProviderError::Api)?;

        let task = page_rules_list_page_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Page rules create a page rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn page_rules_create_a_page_rule(
        &self,
        args: &PageRulesCreateAPageRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = page_rules_create_a_page_rule_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = page_rules_create_a_page_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Available page rules settings list available page rules settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn available_page_rules_settings_list_available_page_rules_settings(
        &self,
        args: &AvailablePageRulesSettingsListAvailablePageRulesSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = available_page_rules_settings_list_available_page_rules_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = available_page_rules_settings_list_available_page_rules_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Page rules get a page rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn page_rules_get_a_page_rule(
        &self,
        args: &PageRulesGetAPageRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = page_rules_get_a_page_rule_builder(
            &self.http_client,
            &args.pagerule_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = page_rules_get_a_page_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Page rules update a page rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn page_rules_update_a_page_rule(
        &self,
        args: &PageRulesUpdateAPageRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = page_rules_update_a_page_rule_builder(
            &self.http_client,
            &args.pagerule_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = page_rules_update_a_page_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Page rules edit a page rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn page_rules_edit_a_page_rule(
        &self,
        args: &PageRulesEditAPageRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = page_rules_edit_a_page_rule_builder(
            &self.http_client,
            &args.pagerule_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = page_rules_edit_a_page_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Page rules delete a page rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZonesSchemasApiResponseSingleId result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn page_rules_delete_a_page_rule(
        &self,
        args: &PageRulesDeleteAPageRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZonesSchemasApiResponseSingleId, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = page_rules_delete_a_page_rule_builder(
            &self.http_client,
            &args.pagerule_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = page_rules_delete_a_page_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pay per crawl get config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PayPerCrawlGetConfigResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn pay_per_crawl_get_config(
        &self,
        args: &PayPerCrawlGetConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PayPerCrawlGetConfigResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pay_per_crawl_get_config_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pay_per_crawl_get_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pay per crawl create config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PayPerCrawlGetConfigResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn pay_per_crawl_create_config(
        &self,
        args: &PayPerCrawlCreateConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PayPerCrawlGetConfigResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pay_per_crawl_create_config_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pay_per_crawl_create_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Pay per crawl patch config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PayPerCrawlGetConfigResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn pay_per_crawl_patch_config(
        &self,
        args: &PayPerCrawlPatchConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PayPerCrawlGetConfigResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = pay_per_crawl_patch_config_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = pay_per_crawl_patch_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone purge.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CachePurgeApiResponseSingleId result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_purge(
        &self,
        args: &ZonePurgeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CachePurgeApiResponseSingleId, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_purge_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_purge_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Rate limits for a zone list rate limits.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallRatelimitResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn rate_limits_for_a_zone_list_rate_limits(
        &self,
        args: &RateLimitsForAZoneListRateLimitsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallRatelimitResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = rate_limits_for_a_zone_list_rate_limits_builder(
            &self.http_client,
            &args.zone_id,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = rate_limits_for_a_zone_list_rate_limits_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Rate limits for a zone create a rate limit.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallRatelimitResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn rate_limits_for_a_zone_create_a_rate_limit(
        &self,
        args: &RateLimitsForAZoneCreateARateLimitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallRatelimitResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = rate_limits_for_a_zone_create_a_rate_limit_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = rate_limits_for_a_zone_create_a_rate_limit_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Rate limits for a zone get a rate limit.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallRatelimitResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn rate_limits_for_a_zone_get_a_rate_limit(
        &self,
        args: &RateLimitsForAZoneGetARateLimitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallRatelimitResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = rate_limits_for_a_zone_get_a_rate_limit_builder(
            &self.http_client,
            &args.rate_limit_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = rate_limits_for_a_zone_get_a_rate_limit_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Rate limits for a zone update a rate limit.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallRatelimitResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn rate_limits_for_a_zone_update_a_rate_limit(
        &self,
        args: &RateLimitsForAZoneUpdateARateLimitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallRatelimitResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = rate_limits_for_a_zone_update_a_rate_limit_builder(
            &self.http_client,
            &args.rate_limit_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = rate_limits_for_a_zone_update_a_rate_limit_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Rate limits for a zone delete a rate limit.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn rate_limits_for_a_zone_delete_a_rate_limit(
        &self,
        args: &RateLimitsForAZoneDeleteARateLimitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = rate_limits_for_a_zone_delete_a_rate_limit_builder(
            &self.http_client,
            &args.rate_limit_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = rate_limits_for_a_zone_delete_a_rate_limit_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List zone rulesets.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_zone_rulesets(
        &self,
        args: &ListZoneRulesetsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_zone_rulesets_builder(
            &self.http_client,
            &args.zone_id,
            &args.cursor,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = list_zone_rulesets_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create zone ruleset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_zone_ruleset(
        &self,
        args: &CreateZoneRulesetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_zone_ruleset_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_zone_ruleset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zone entrypoint ruleset.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zone_entrypoint_ruleset(
        &self,
        args: &GetZoneEntrypointRulesetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zone_entrypoint_ruleset_builder(
            &self.http_client,
            &args.ruleset_phase,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zone_entrypoint_ruleset_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update zone entrypoint ruleset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_zone_entrypoint_ruleset(
        &self,
        args: &UpdateZoneEntrypointRulesetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_zone_entrypoint_ruleset_builder(
            &self.http_client,
            &args.ruleset_phase,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_zone_entrypoint_ruleset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List zone entrypoint ruleset versions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_zone_entrypoint_ruleset_versions(
        &self,
        args: &ListZoneEntrypointRulesetVersionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_zone_entrypoint_ruleset_versions_builder(
            &self.http_client,
            &args.ruleset_phase,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = list_zone_entrypoint_ruleset_versions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zone entrypoint ruleset version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zone_entrypoint_ruleset_version(
        &self,
        args: &GetZoneEntrypointRulesetVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zone_entrypoint_ruleset_version_builder(
            &self.http_client,
            &args.ruleset_version,
            &args.ruleset_phase,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zone_entrypoint_ruleset_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zone ruleset.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zone_ruleset(
        &self,
        args: &GetZoneRulesetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zone_ruleset_builder(
            &self.http_client,
            &args.ruleset_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zone_ruleset_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update zone ruleset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_zone_ruleset(
        &self,
        args: &UpdateZoneRulesetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_zone_ruleset_builder(
            &self.http_client,
            &args.ruleset_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_zone_ruleset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete zone ruleset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_zone_ruleset(
        &self,
        args: &DeleteZoneRulesetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_zone_ruleset_builder(
            &self.http_client,
            &args.ruleset_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_zone_ruleset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Create zone ruleset rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn create_zone_ruleset_rule(
        &self,
        args: &CreateZoneRulesetRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = create_zone_ruleset_rule_builder(
            &self.http_client,
            &args.ruleset_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = create_zone_ruleset_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update zone ruleset rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_zone_ruleset_rule(
        &self,
        args: &UpdateZoneRulesetRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_zone_ruleset_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.ruleset_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_zone_ruleset_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete zone ruleset rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_zone_ruleset_rule(
        &self,
        args: &DeleteZoneRulesetRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_zone_ruleset_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.ruleset_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_zone_ruleset_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List zone ruleset versions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_zone_ruleset_versions(
        &self,
        args: &ListZoneRulesetVersionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_zone_ruleset_versions_builder(
            &self.http_client,
            &args.ruleset_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = list_zone_ruleset_versions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zone ruleset version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zone_ruleset_version(
        &self,
        args: &GetZoneRulesetVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zone_ruleset_version_builder(
            &self.http_client,
            &args.ruleset_version,
            &args.ruleset_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zone_ruleset_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete zone ruleset version.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_zone_ruleset_version(
        &self,
        args: &DeleteZoneRulesetVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_zone_ruleset_version_builder(
            &self.http_client,
            &args.ruleset_version,
            &args.ruleset_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_zone_ruleset_version_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List zone ruleset version rules by tag.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_zone_ruleset_version_rules_by_tag(
        &self,
        args: &ListZoneRulesetVersionRulesByTagArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_zone_ruleset_version_rules_by_tag_builder(
            &self.http_client,
            &args.rule_tag,
            &args.ruleset_version,
            &args.ruleset_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = list_zone_ruleset_version_rules_by_tag_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Schema validation list schemas paginated.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn schema_validation_list_schemas_paginated(
        &self,
        args: &SchemaValidationListSchemasPaginatedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = schema_validation_list_schemas_paginated_builder(
            &self.http_client,
            &args.zone_id,
            &args.validation_enabled,
        )
        .map_err(ProviderError::Api)?;

        let task = schema_validation_list_schemas_paginated_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Schema validation create schema.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn schema_validation_create_schema(
        &self,
        args: &SchemaValidationCreateSchemaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = schema_validation_create_schema_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = schema_validation_create_schema_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Schema validation list schema hosts.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn schema_validation_list_schema_hosts(
        &self,
        args: &SchemaValidationListSchemaHostsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = schema_validation_list_schema_hosts_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = schema_validation_list_schema_hosts_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Schema validation get schema.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn schema_validation_get_schema(
        &self,
        args: &SchemaValidationGetSchemaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = schema_validation_get_schema_builder(
            &self.http_client,
            &args.zone_id,
            &args.schema_id,
        )
        .map_err(ProviderError::Api)?;

        let task = schema_validation_get_schema_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Schema validation edit schema.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn schema_validation_edit_schema(
        &self,
        args: &SchemaValidationEditSchemaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = schema_validation_edit_schema_builder(
            &self.http_client,
            &args.zone_id,
            &args.schema_id,
        )
        .map_err(ProviderError::Api)?;

        let task = schema_validation_edit_schema_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Schema validation delete schema.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn schema_validation_delete_schema(
        &self,
        args: &SchemaValidationDeleteSchemaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = schema_validation_delete_schema_builder(
            &self.http_client,
            &args.zone_id,
            &args.schema_id,
        )
        .map_err(ProviderError::Api)?;

        let task = schema_validation_delete_schema_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Schema validation extract operations from schema.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn schema_validation_extract_operations_from_schema(
        &self,
        args: &SchemaValidationExtractOperationsFromSchemaArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = schema_validation_extract_operations_from_schema_builder(
            &self.http_client,
            &args.zone_id,
            &args.schema_id,
            &args.operation_status,
        )
        .map_err(ProviderError::Api)?;

        let task = schema_validation_extract_operations_from_schema_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Schema validation get settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn schema_validation_get_settings(
        &self,
        args: &SchemaValidationGetSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = schema_validation_get_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = schema_validation_get_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Schema validation update settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn schema_validation_update_settings(
        &self,
        args: &SchemaValidationUpdateSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = schema_validation_update_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = schema_validation_update_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Schema validation edit settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn schema_validation_edit_settings(
        &self,
        args: &SchemaValidationEditSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = schema_validation_edit_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = schema_validation_edit_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Schema validation list per operation settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn schema_validation_list_per_operation_settings(
        &self,
        args: &SchemaValidationListPerOperationSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = schema_validation_list_per_operation_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = schema_validation_list_per_operation_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Schema validation bulk edit per operation settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn schema_validation_bulk_edit_per_operation_settings(
        &self,
        args: &SchemaValidationBulkEditPerOperationSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = schema_validation_bulk_edit_per_operation_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = schema_validation_bulk_edit_per_operation_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Schema validation get per operation setting.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn schema_validation_get_per_operation_setting(
        &self,
        args: &SchemaValidationGetPerOperationSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = schema_validation_get_per_operation_setting_builder(
            &self.http_client,
            &args.zone_id,
            &args.operation_id,
        )
        .map_err(ProviderError::Api)?;

        let task = schema_validation_get_per_operation_setting_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Schema validation update per operation setting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn schema_validation_update_per_operation_setting(
        &self,
        args: &SchemaValidationUpdatePerOperationSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = schema_validation_update_per_operation_setting_builder(
            &self.http_client,
            &args.zone_id,
            &args.operation_id,
        )
        .map_err(ProviderError::Api)?;

        let task = schema_validation_update_per_operation_setting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Schema validation delete per operation setting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn schema_validation_delete_per_operation_setting(
        &self,
        args: &SchemaValidationDeletePerOperationSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = schema_validation_delete_per_operation_setting_builder(
            &self.http_client,
            &args.zone_id,
            &args.operation_id,
        )
        .map_err(ProviderError::Api)?;

        let task = schema_validation_delete_per_operation_setting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns secondary zone force axfr.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsForceResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secondary_dns_secondary_zone_force_axfr(
        &self,
        args: &SecondaryDnsSecondaryZoneForceAxfrArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsForceResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_secondary_zone_force_axfr_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_secondary_zone_force_axfr_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns secondary zone secondary zone configuration details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsSingleResponseIncoming result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn secondary_dns_secondary_zone_secondary_zone_configuration_details(
        &self,
        args: &SecondaryDnsSecondaryZoneSecondaryZoneConfigurationDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsSingleResponseIncoming, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_secondary_zone_secondary_zone_configuration_details_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_secondary_zone_secondary_zone_configuration_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns secondary zone create secondary zone configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsSingleResponseIncoming result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secondary_dns_secondary_zone_create_secondary_zone_configuration(
        &self,
        args: &SecondaryDnsSecondaryZoneCreateSecondaryZoneConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsSingleResponseIncoming, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_secondary_zone_create_secondary_zone_configuration_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_secondary_zone_create_secondary_zone_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns secondary zone update secondary zone configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsSingleResponseIncoming result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secondary_dns_secondary_zone_update_secondary_zone_configuration(
        &self,
        args: &SecondaryDnsSecondaryZoneUpdateSecondaryZoneConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsSingleResponseIncoming, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_secondary_zone_update_secondary_zone_configuration_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_secondary_zone_update_secondary_zone_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns secondary zone delete secondary zone configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secondary_dns_secondary_zone_delete_secondary_zone_configuration(
        &self,
        args: &SecondaryDnsSecondaryZoneDeleteSecondaryZoneConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_secondary_zone_delete_secondary_zone_configuration_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_secondary_zone_delete_secondary_zone_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns primary zone primary zone configuration details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsSingleResponseOutgoing result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn secondary_dns_primary_zone_primary_zone_configuration_details(
        &self,
        args: &SecondaryDnsPrimaryZonePrimaryZoneConfigurationDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsSingleResponseOutgoing, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_primary_zone_primary_zone_configuration_details_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_primary_zone_primary_zone_configuration_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns primary zone create primary zone configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsSingleResponseOutgoing result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secondary_dns_primary_zone_create_primary_zone_configuration(
        &self,
        args: &SecondaryDnsPrimaryZoneCreatePrimaryZoneConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsSingleResponseOutgoing, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_primary_zone_create_primary_zone_configuration_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_primary_zone_create_primary_zone_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns primary zone update primary zone configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsSingleResponseOutgoing result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secondary_dns_primary_zone_update_primary_zone_configuration(
        &self,
        args: &SecondaryDnsPrimaryZoneUpdatePrimaryZoneConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsSingleResponseOutgoing, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_primary_zone_update_primary_zone_configuration_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_primary_zone_update_primary_zone_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns primary zone delete primary zone configuration.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secondary_dns_primary_zone_delete_primary_zone_configuration(
        &self,
        args: &SecondaryDnsPrimaryZoneDeletePrimaryZoneConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_primary_zone_delete_primary_zone_configuration_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_primary_zone_delete_primary_zone_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns primary zone disable outgoing zone transfers.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsDisableTransferResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secondary_dns_primary_zone_disable_outgoing_zone_transfers(
        &self,
        args: &SecondaryDnsPrimaryZoneDisableOutgoingZoneTransfersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsDisableTransferResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_primary_zone_disable_outgoing_zone_transfers_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_primary_zone_disable_outgoing_zone_transfers_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns primary zone enable outgoing zone transfers.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsEnableTransferResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secondary_dns_primary_zone_enable_outgoing_zone_transfers(
        &self,
        args: &SecondaryDnsPrimaryZoneEnableOutgoingZoneTransfersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsEnableTransferResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_primary_zone_enable_outgoing_zone_transfers_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_primary_zone_enable_outgoing_zone_transfers_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns primary zone force dns notify.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsSchemasForceResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn secondary_dns_primary_zone_force_dns_notify(
        &self,
        args: &SecondaryDnsPrimaryZoneForceDnsNotifyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsSchemasForceResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_primary_zone_force_dns_notify_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_primary_zone_force_dns_notify_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Secondary dns primary zone get outgoing zone transfer status.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecondaryDnsEnableTransferResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn secondary_dns_primary_zone_get_outgoing_zone_transfer_status(
        &self,
        args: &SecondaryDnsPrimaryZoneGetOutgoingZoneTransferStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecondaryDnsEnableTransferResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = secondary_dns_primary_zone_get_outgoing_zone_transfer_status_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = secondary_dns_primary_zone_get_outgoing_zone_transfer_status_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zone security center insights.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zone_security_center_insights(
        &self,
        args: &GetZoneSecurityCenterInsightsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zone_security_center_insights_builder(
            &self.http_client,
            &args.zone_id,
            &args.dismissed,
            &args.issue_class,
            &args.issue_type,
            &args.product,
            &args.severity,
            &args.subject,
            &args.issue_class_neq,
            &args.issue_type_neq,
            &args.product_neq,
            &args.severity_neq,
            &args.subject_neq,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zone_security_center_insights_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zone security center insight counts by class.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityCenterValueCountsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zone_security_center_insight_counts_by_class(
        &self,
        args: &GetZoneSecurityCenterInsightCountsByClassArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityCenterValueCountsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zone_security_center_insight_counts_by_class_builder(
            &self.http_client,
            &args.zone_id,
            &args.dismissed,
            &args.issue_class,
            &args.issue_type,
            &args.product,
            &args.severity,
            &args.subject,
            &args.issue_class_neq,
            &args.issue_type_neq,
            &args.product_neq,
            &args.severity_neq,
            &args.subject_neq,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zone_security_center_insight_counts_by_class_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zone security center insight counts by severity.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityCenterValueCountsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zone_security_center_insight_counts_by_severity(
        &self,
        args: &GetZoneSecurityCenterInsightCountsBySeverityArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityCenterValueCountsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zone_security_center_insight_counts_by_severity_builder(
            &self.http_client,
            &args.zone_id,
            &args.dismissed,
            &args.issue_class,
            &args.issue_type,
            &args.product,
            &args.severity,
            &args.subject,
            &args.issue_class_neq,
            &args.issue_type_neq,
            &args.product_neq,
            &args.severity_neq,
            &args.subject_neq,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zone_security_center_insight_counts_by_severity_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zone security center insight counts by type.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityCenterValueCountsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zone_security_center_insight_counts_by_type(
        &self,
        args: &GetZoneSecurityCenterInsightCountsByTypeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityCenterValueCountsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zone_security_center_insight_counts_by_type_builder(
            &self.http_client,
            &args.zone_id,
            &args.dismissed,
            &args.issue_class,
            &args.issue_type,
            &args.product,
            &args.severity,
            &args.subject,
            &args.issue_class_neq,
            &args.issue_type_neq,
            &args.product_neq,
            &args.severity_neq,
            &args.subject_neq,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zone_security_center_insight_counts_by_type_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Archive zone security center insight.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn archive_zone_security_center_insight(
        &self,
        args: &ArchiveZoneSecurityCenterInsightArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = archive_zone_security_center_insight_builder(
            &self.http_client,
            &args.zone_id,
            &args.issue_id,
        )
        .map_err(ProviderError::Api)?;

        let task = archive_zone_security_center_insight_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get security txt.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_security_txt(
        &self,
        args: &GetSecurityTxtArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_security_txt_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_security_txt_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update security txt.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_security_txt(
        &self,
        args: &UpdateSecurityTxtArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_security_txt_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_security_txt_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete security txt.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_security_txt(
        &self,
        args: &DeleteSecurityTxtArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_security_txt_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_security_txt_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone settings get all zone settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZonesZoneSettingsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_settings_get_all_zone_settings(
        &self,
        args: &ZoneSettingsGetAllZoneSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZonesZoneSettingsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_settings_get_all_zone_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_settings_get_all_zone_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone settings edit zone settings info.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZonesZoneSettingsResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_settings_edit_zone_settings_info(
        &self,
        args: &ZoneSettingsEditZoneSettingsInfoArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZonesZoneSettingsResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_settings_edit_zone_settings_info_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_settings_edit_zone_settings_info_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone cache settings get aegis setting.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_cache_settings_get_aegis_setting(
        &self,
        args: &ZoneCacheSettingsGetAegisSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_cache_settings_get_aegis_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_cache_settings_get_aegis_setting_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone cache settings change aegis setting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_cache_settings_change_aegis_setting(
        &self,
        args: &ZoneCacheSettingsChangeAegisSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_cache_settings_change_aegis_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_cache_settings_change_aegis_setting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone settings get fonts setting.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_settings_get_fonts_setting(
        &self,
        args: &ZoneSettingsGetFontsSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_settings_get_fonts_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_settings_get_fonts_setting_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone settings change fonts setting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_settings_change_fonts_setting(
        &self,
        args: &ZoneSettingsChangeFontsSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_settings_change_fonts_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_settings_change_fonts_setting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone settings get google tag gateway config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_settings_get_google_tag_gateway_config(
        &self,
        args: &ZoneSettingsGetGoogleTagGatewayConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_settings_get_google_tag_gateway_config_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_settings_get_google_tag_gateway_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone settings change google tag gateway config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_settings_change_google_tag_gateway_config(
        &self,
        args: &ZoneSettingsChangeGoogleTagGatewayConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_settings_change_google_tag_gateway_config_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_settings_change_google_tag_gateway_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone cache settings get origin h2 max streams setting.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CacheRulesOriginH2MaxStreamsResponseValue result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_cache_settings_get_origin_h2_max_streams_setting(
        &self,
        args: &ZoneCacheSettingsGetOriginH2MaxStreamsSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CacheRulesOriginH2MaxStreamsResponseValue, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_cache_settings_get_origin_h2_max_streams_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_cache_settings_get_origin_h2_max_streams_setting_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone cache settings change origin h2 max streams setting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CacheRulesOriginH2MaxStreamsResponseValue result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_cache_settings_change_origin_h2_max_streams_setting(
        &self,
        args: &ZoneCacheSettingsChangeOriginH2MaxStreamsSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CacheRulesOriginH2MaxStreamsResponseValue, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_cache_settings_change_origin_h2_max_streams_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_cache_settings_change_origin_h2_max_streams_setting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone cache settings get origin max http version setting.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_cache_settings_get_origin_max_http_version_setting(
        &self,
        args: &ZoneCacheSettingsGetOriginMaxHttpVersionSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_cache_settings_get_origin_max_http_version_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_cache_settings_get_origin_max_http_version_setting_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone cache settings change origin max http version setting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_cache_settings_change_origin_max_http_version_setting(
        &self,
        args: &ZoneCacheSettingsChangeOriginMaxHttpVersionSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_cache_settings_change_origin_max_http_version_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_cache_settings_change_origin_max_http_version_setting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web analytics get rum status.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RumRumSiteResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn web_analytics_get_rum_status(
        &self,
        args: &WebAnalyticsGetRumStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RumRumSiteResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web_analytics_get_rum_status_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web_analytics_get_rum_status_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web analytics toggle rum.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RumRumSiteResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn web_analytics_toggle_rum(
        &self,
        args: &WebAnalyticsToggleRumArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RumRumSiteResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web_analytics_toggle_rum_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web_analytics_toggle_rum_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone settings get speed brain setting.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_settings_get_speed_brain_setting(
        &self,
        args: &ZoneSettingsGetSpeedBrainSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_settings_get_speed_brain_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_settings_get_speed_brain_setting_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone settings change speed brain setting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_settings_change_speed_brain_setting(
        &self,
        args: &ZoneSettingsChangeSpeedBrainSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_settings_change_speed_brain_setting_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_settings_change_speed_brain_setting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ssl detector automatic mode get enrollment.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CacheApiResponseSingleId result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ssl_detector_automatic_mode_get_enrollment(
        &self,
        args: &SslDetectorAutomaticModeGetEnrollmentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CacheApiResponseSingleId, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ssl_detector_automatic_mode_get_enrollment_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ssl_detector_automatic_mode_get_enrollment_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ssl detector automatic mode patch enrollment.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CacheApiResponseSingleId result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ssl_detector_automatic_mode_patch_enrollment(
        &self,
        args: &SslDetectorAutomaticModePatchEnrollmentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CacheApiResponseSingleId, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ssl_detector_automatic_mode_patch_enrollment_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ssl_detector_automatic_mode_patch_enrollment_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zones zone identifier zaraz config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZarazZarazConfigResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zones_zone_identifier_zaraz_config(
        &self,
        args: &GetZonesZoneIdentifierZarazConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZarazZarazConfigResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zones_zone_identifier_zaraz_config_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zones_zone_identifier_zaraz_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Put zones zone identifier zaraz config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZarazZarazConfigResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn put_zones_zone_identifier_zaraz_config(
        &self,
        args: &PutZonesZoneIdentifierZarazConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZarazZarazConfigResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = put_zones_zone_identifier_zaraz_config_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = put_zones_zone_identifier_zaraz_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zones zone identifier zaraz default.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZarazZarazConfigResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zones_zone_identifier_zaraz_default(
        &self,
        args: &GetZonesZoneIdentifierZarazDefaultArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZarazZarazConfigResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zones_zone_identifier_zaraz_default_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zones_zone_identifier_zaraz_default_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zones zone identifier zaraz export.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZarazZarazConfigReturn result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zones_zone_identifier_zaraz_export(
        &self,
        args: &GetZonesZoneIdentifierZarazExportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZarazZarazConfigReturn, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zones_zone_identifier_zaraz_export_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zones_zone_identifier_zaraz_export_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zones zone identifier zaraz history.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZarazZarazHistoryResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zones_zone_identifier_zaraz_history(
        &self,
        args: &GetZonesZoneIdentifierZarazHistoryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZarazZarazHistoryResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zones_zone_identifier_zaraz_history_builder(
            &self.http_client,
            &args.zone_id,
            &args.offset,
            &args.limit,
            &args.sortField,
            &args.sortOrder,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zones_zone_identifier_zaraz_history_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Put zones zone identifier zaraz history.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZarazZarazConfigResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn put_zones_zone_identifier_zaraz_history(
        &self,
        args: &PutZonesZoneIdentifierZarazHistoryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZarazZarazConfigResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = put_zones_zone_identifier_zaraz_history_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = put_zones_zone_identifier_zaraz_history_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zones zone identifier zaraz config history.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZarazZarazConfigHistoryResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zones_zone_identifier_zaraz_config_history(
        &self,
        args: &GetZonesZoneIdentifierZarazConfigHistoryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZarazZarazConfigHistoryResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zones_zone_identifier_zaraz_config_history_builder(
            &self.http_client,
            &args.zone_id,
            &args.ids,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zones_zone_identifier_zaraz_config_history_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Post zones zone identifier zaraz publish.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn post_zones_zone_identifier_zaraz_publish(
        &self,
        args: &PostZonesZoneIdentifierZarazPublishArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = post_zones_zone_identifier_zaraz_publish_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = post_zones_zone_identifier_zaraz_publish_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zones zone identifier zaraz workflow.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZarazZarazWorkflowResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zones_zone_identifier_zaraz_workflow(
        &self,
        args: &GetZonesZoneIdentifierZarazWorkflowArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZarazZarazWorkflowResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zones_zone_identifier_zaraz_workflow_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zones_zone_identifier_zaraz_workflow_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Put zones zone identifier zaraz workflow.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZarazZarazWorkflowResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn put_zones_zone_identifier_zaraz_workflow(
        &self,
        args: &PutZonesZoneIdentifierZarazWorkflowArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZarazZarazWorkflowResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = put_zones_zone_identifier_zaraz_workflow_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = put_zones_zone_identifier_zaraz_workflow_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone settings get single setting.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_settings_get_single_setting(
        &self,
        args: &ZoneSettingsGetSingleSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_settings_get_single_setting_builder(
            &self.http_client,
            &args.zone_id,
            &args.setting_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_settings_get_single_setting_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone settings edit single setting.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_settings_edit_single_setting(
        &self,
        args: &ZoneSettingsEditSingleSettingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_settings_edit_single_setting_builder(
            &self.http_client,
            &args.zone_id,
            &args.setting_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_settings_edit_single_setting_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Smart shield get settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn smart_shield_get_settings(
        &self,
        args: &SmartShieldGetSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = smart_shield_get_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = smart_shield_get_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Smart shield patch settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn smart_shield_patch_settings(
        &self,
        args: &SmartShieldPatchSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = smart_shield_patch_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = smart_shield_patch_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Smart shield settings get cache reserve clear.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn smart_shield_settings_get_cache_reserve_clear(
        &self,
        args: &SmartShieldSettingsGetCacheReserveClearArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = smart_shield_settings_get_cache_reserve_clear_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = smart_shield_settings_get_cache_reserve_clear_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Smart shield settings start cache reserve clear.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn smart_shield_settings_start_cache_reserve_clear(
        &self,
        args: &SmartShieldSettingsStartCacheReserveClearArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = smart_shield_settings_start_cache_reserve_clear_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = smart_shield_settings_start_cache_reserve_clear_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Smart shield list health checks.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SmartshieldResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn smart_shield_list_health_checks(
        &self,
        args: &SmartShieldListHealthChecksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SmartshieldResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = smart_shield_list_health_checks_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = smart_shield_list_health_checks_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Smart shield create health check.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SmartshieldSingleHcResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn smart_shield_create_health_check(
        &self,
        args: &SmartShieldCreateHealthCheckArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SmartshieldSingleHcResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = smart_shield_create_health_check_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = smart_shield_create_health_check_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Smart shield health check details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SmartshieldSingleHcResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn smart_shield_health_check_details(
        &self,
        args: &SmartShieldHealthCheckDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SmartshieldSingleHcResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = smart_shield_health_check_details_builder(
            &self.http_client,
            &args.healthcheck_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = smart_shield_health_check_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Smart shield update health check.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SmartshieldSingleHcResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn smart_shield_update_health_check(
        &self,
        args: &SmartShieldUpdateHealthCheckArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SmartshieldSingleHcResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = smart_shield_update_health_check_builder(
            &self.http_client,
            &args.healthcheck_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = smart_shield_update_health_check_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Smart shield patch health check.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SmartshieldSingleHcResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn smart_shield_patch_health_check(
        &self,
        args: &SmartShieldPatchHealthCheckArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SmartshieldSingleHcResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = smart_shield_patch_health_check_builder(
            &self.http_client,
            &args.healthcheck_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = smart_shield_patch_health_check_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Smart shield delete health check.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SmartshieldSingleHcIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn smart_shield_delete_health_check(
        &self,
        args: &SmartShieldDeleteHealthCheckArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SmartshieldSingleHcIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = smart_shield_delete_health_check_builder(
            &self.http_client,
            &args.healthcheck_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = smart_shield_delete_health_check_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List zone snippets.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_zone_snippets(
        &self,
        args: &ListZoneSnippetsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_zone_snippets_builder(
            &self.http_client,
            &args.zone_id,
            &args.page,
            &args.per_page,
        )
        .map_err(ProviderError::Api)?;

        let task = list_zone_snippets_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// List zone snippet rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn list_zone_snippet_rules(
        &self,
        args: &ListZoneSnippetRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = list_zone_snippet_rules_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = list_zone_snippet_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update zone snippet rules.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_zone_snippet_rules(
        &self,
        args: &UpdateZoneSnippetRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_zone_snippet_rules_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_zone_snippet_rules_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete zone snippet rules.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_zone_snippet_rules(
        &self,
        args: &DeleteZoneSnippetRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_zone_snippet_rules_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_zone_snippet_rules_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zone snippet.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zone_snippet(
        &self,
        args: &GetZoneSnippetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zone_snippet_builder(
            &self.http_client,
            &args.zone_id,
            &args.snippet_name,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zone_snippet_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update zone snippet.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_zone_snippet(
        &self,
        args: &UpdateZoneSnippetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_zone_snippet_builder(
            &self.http_client,
            &args.zone_id,
            &args.snippet_name,
        )
        .map_err(ProviderError::Api)?;

        let task = update_zone_snippet_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete zone snippet.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_zone_snippet(
        &self,
        args: &DeleteZoneSnippetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_zone_snippet_builder(
            &self.http_client,
            &args.zone_id,
            &args.snippet_name,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_zone_snippet_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get zone snippet content.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_zone_snippet_content(
        &self,
        args: &GetZoneSnippetContentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_zone_snippet_content_builder(
            &self.http_client,
            &args.zone_id,
            &args.snippet_name,
        )
        .map_err(ProviderError::Api)?;

        let task = get_zone_snippet_content_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Spectrum aggregate analytics get current aggregated analytics.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SpectrumAnalyticsQueryResponseAggregate result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn spectrum_aggregate_analytics_get_current_aggregated_analytics(
        &self,
        args: &SpectrumAggregateAnalyticsGetCurrentAggregatedAnalyticsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SpectrumAnalyticsQueryResponseAggregate, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = spectrum_aggregate_analytics_get_current_aggregated_analytics_builder(
            &self.http_client,
            &args.zone_id,
            &args.appID,
            &args.colo_name,
        )
        .map_err(ProviderError::Api)?;

        let task = spectrum_aggregate_analytics_get_current_aggregated_analytics_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Spectrum analytics by time get analytics by time.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SpectrumAnalyticsQueryResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn spectrum_analytics_by_time_get_analytics_by_time(
        &self,
        args: &SpectrumAnalyticsByTimeGetAnalyticsByTimeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SpectrumAnalyticsQueryResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = spectrum_analytics_by_time_get_analytics_by_time_builder(
            &self.http_client,
            &args.zone_id,
            &args.dimensions,
            &args.sort,
            &args.until,
            &args.metrics,
            &args.filters,
            &args.since,
            &args.time_delta,
        )
        .map_err(ProviderError::Api)?;

        let task = spectrum_analytics_by_time_get_analytics_by_time_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Spectrum analytics summary get analytics summary.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SpectrumAnalyticsQueryResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn spectrum_analytics_summary_get_analytics_summary(
        &self,
        args: &SpectrumAnalyticsSummaryGetAnalyticsSummaryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SpectrumAnalyticsQueryResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = spectrum_analytics_summary_get_analytics_summary_builder(
            &self.http_client,
            &args.zone_id,
            &args.dimensions,
            &args.sort,
            &args.until,
            &args.metrics,
            &args.filters,
            &args.since,
        )
        .map_err(ProviderError::Api)?;

        let task = spectrum_analytics_summary_get_analytics_summary_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Spectrum applications list spectrum applications.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SpectrumConfigAppConfigCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn spectrum_applications_list_spectrum_applications(
        &self,
        args: &SpectrumApplicationsListSpectrumApplicationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SpectrumConfigAppConfigCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = spectrum_applications_list_spectrum_applications_builder(
            &self.http_client,
            &args.zone_id,
            &args.page,
            &args.per_page,
            &args.direction,
            &args.order,
        )
        .map_err(ProviderError::Api)?;

        let task = spectrum_applications_list_spectrum_applications_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Spectrum applications create spectrum application using a name for the origin.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SpectrumConfigAppConfigSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn spectrum_applications_create_spectrum_application_using_a_name_for_the_origin(
        &self,
        args: &SpectrumApplicationsCreateSpectrumApplicationUsingANameForTheOriginArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SpectrumConfigAppConfigSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = spectrum_applications_create_spectrum_application_using_a_name_for_the_origin_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = spectrum_applications_create_spectrum_application_using_a_name_for_the_origin_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Spectrum applications get spectrum application configuration.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SpectrumConfigAppConfigSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn spectrum_applications_get_spectrum_application_configuration(
        &self,
        args: &SpectrumApplicationsGetSpectrumApplicationConfigurationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SpectrumConfigAppConfigSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = spectrum_applications_get_spectrum_application_configuration_builder(
            &self.http_client,
            &args.app_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = spectrum_applications_get_spectrum_application_configuration_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Spectrum applications update spectrum application configuration using a name for the origin.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SpectrumConfigAppConfigSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn spectrum_applications_update_spectrum_application_configuration_using_a_name_for_the_origin(
        &self,
        args: &SpectrumApplicationsUpdateSpectrumApplicationConfigurationUsingANameForTheOriginArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SpectrumConfigAppConfigSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = spectrum_applications_update_spectrum_application_configuration_using_a_name_for_the_origin_builder(
            &self.http_client,
            &args.app_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = spectrum_applications_update_spectrum_application_configuration_using_a_name_for_the_origin_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Spectrum applications delete spectrum application.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SpectrumConfigApiResponseSingleId result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn spectrum_applications_delete_spectrum_application(
        &self,
        args: &SpectrumApplicationsDeleteSpectrumApplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SpectrumConfigApiResponseSingleId, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = spectrum_applications_delete_spectrum_application_builder(
            &self.http_client,
            &args.app_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = spectrum_applications_delete_spectrum_application_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Speed get availabilities.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ObservatoryAvailabilitiesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn speed_get_availabilities(
        &self,
        args: &SpeedGetAvailabilitiesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ObservatoryAvailabilitiesResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = speed_get_availabilities_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = speed_get_availabilities_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Speed list pages.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ObservatoryPagesResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn speed_list_pages(
        &self,
        args: &SpeedListPagesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ObservatoryPagesResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = speed_list_pages_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = speed_list_pages_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Speed list test history.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ObservatoryPageTestResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn speed_list_test_history(
        &self,
        args: &SpeedListTestHistoryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ObservatoryPageTestResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = speed_list_test_history_builder(
            &self.http_client,
            &args.zone_id,
            &args.url,
            &args.page,
            &args.per_page,
            &args.region,
        )
        .map_err(ProviderError::Api)?;

        let task = speed_list_test_history_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Speed create test.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ObservatoryPageTestResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn speed_create_test(
        &self,
        args: &SpeedCreateTestArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ObservatoryPageTestResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = speed_create_test_builder(
            &self.http_client,
            &args.zone_id,
            &args.url,
        )
        .map_err(ProviderError::Api)?;

        let task = speed_create_test_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Speed delete tests.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ObservatoryCountResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn speed_delete_tests(
        &self,
        args: &SpeedDeleteTestsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ObservatoryCountResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = speed_delete_tests_builder(
            &self.http_client,
            &args.zone_id,
            &args.url,
            &args.region,
        )
        .map_err(ProviderError::Api)?;

        let task = speed_delete_tests_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Speed get test.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ObservatoryPageTestResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn speed_get_test(
        &self,
        args: &SpeedGetTestArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ObservatoryPageTestResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = speed_get_test_builder(
            &self.http_client,
            &args.zone_id,
            &args.url,
            &args.test_id,
        )
        .map_err(ProviderError::Api)?;

        let task = speed_get_test_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Speed list page trend.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ObservatoryTrendResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn speed_list_page_trend(
        &self,
        args: &SpeedListPageTrendArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ObservatoryTrendResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = speed_list_page_trend_builder(
            &self.http_client,
            &args.zone_id,
            &args.url,
            &args.region,
            &args.deviceType,
            &args.start,
            &args.end,
            &args.tz,
            &args.metrics,
        )
        .map_err(ProviderError::Api)?;

        let task = speed_list_page_trend_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Speed get scheduled test.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ObservatoryScheduleResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn speed_get_scheduled_test(
        &self,
        args: &SpeedGetScheduledTestArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ObservatoryScheduleResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = speed_get_scheduled_test_builder(
            &self.http_client,
            &args.zone_id,
            &args.url,
            &args.region,
        )
        .map_err(ProviderError::Api)?;

        let task = speed_get_scheduled_test_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Speed create scheduled test.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ObservatoryCreateScheduleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn speed_create_scheduled_test(
        &self,
        args: &SpeedCreateScheduledTestArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ObservatoryCreateScheduleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = speed_create_scheduled_test_builder(
            &self.http_client,
            &args.zone_id,
            &args.url,
            &args.region,
            &args.frequency,
        )
        .map_err(ProviderError::Api)?;

        let task = speed_create_scheduled_test_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Speed delete test schedule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ObservatoryCountResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn speed_delete_test_schedule(
        &self,
        args: &SpeedDeleteTestScheduleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ObservatoryCountResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = speed_delete_test_schedule_builder(
            &self.http_client,
            &args.zone_id,
            &args.url,
            &args.region,
        )
        .map_err(ProviderError::Api)?;

        let task = speed_delete_test_schedule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Analyze certificate analyze certificate.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCertificateAnalyzeResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn analyze_certificate_analyze_certificate(
        &self,
        args: &AnalyzeCertificateAnalyzeCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCertificateAnalyzeResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = analyze_certificate_analyze_certificate_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = analyze_certificate_analyze_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Certificate packs list certificate packs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCertificatePackResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn certificate_packs_list_certificate_packs(
        &self,
        args: &CertificatePacksListCertificatePacksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCertificatePackResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = certificate_packs_list_certificate_packs_builder(
            &self.http_client,
            &args.zone_id,
            &args.page,
            &args.per_page,
            &args.status,
            &args.deploy,
        )
        .map_err(ProviderError::Api)?;

        let task = certificate_packs_list_certificate_packs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Certificate packs order advanced certificate manager certificate pack.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesAdvancedCertificatePackResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn certificate_packs_order_advanced_certificate_manager_certificate_pack(
        &self,
        args: &CertificatePacksOrderAdvancedCertificateManagerCertificatePackArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesAdvancedCertificatePackResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = certificate_packs_order_advanced_certificate_manager_certificate_pack_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = certificate_packs_order_advanced_certificate_manager_certificate_pack_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Certificate packs get certificate pack quotas.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCertificatePackQuotaResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn certificate_packs_get_certificate_pack_quotas(
        &self,
        args: &CertificatePacksGetCertificatePackQuotasArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCertificatePackQuotaResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = certificate_packs_get_certificate_pack_quotas_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = certificate_packs_get_certificate_pack_quotas_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Certificate packs get certificate pack.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesCertificatePackResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn certificate_packs_get_certificate_pack(
        &self,
        args: &CertificatePacksGetCertificatePackArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesCertificatePackResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = certificate_packs_get_certificate_pack_builder(
            &self.http_client,
            &args.certificate_pack_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = certificate_packs_get_certificate_pack_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Certificate packs restart validation for advanced certificate manager certificate pack.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesAdvancedCertificatePackResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn certificate_packs_restart_validation_for_advanced_certificate_manager_certificate_pack(
        &self,
        args: &CertificatePacksRestartValidationForAdvancedCertificateManagerCertificatePackArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesAdvancedCertificatePackResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = certificate_packs_restart_validation_for_advanced_certificate_manager_certificate_pack_builder(
            &self.http_client,
            &args.certificate_pack_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = certificate_packs_restart_validation_for_advanced_certificate_manager_certificate_pack_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Certificate packs delete advanced certificate manager certificate pack.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesDeleteAdvancedCertificatePackResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn certificate_packs_delete_advanced_certificate_manager_certificate_pack(
        &self,
        args: &CertificatePacksDeleteAdvancedCertificateManagerCertificatePackArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesDeleteAdvancedCertificatePackResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = certificate_packs_delete_advanced_certificate_manager_certificate_pack_builder(
            &self.http_client,
            &args.certificate_pack_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = certificate_packs_delete_advanced_certificate_manager_certificate_pack_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ssl tls mode recommendation ssl tls recommendation.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CacheApiResponseSingleId result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ssl_tls_mode_recommendation_ssl_tls_recommendation(
        &self,
        args: &SslTlsModeRecommendationSslTlsRecommendationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CacheApiResponseSingleId, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ssl_tls_mode_recommendation_ssl_tls_recommendation_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ssl_tls_mode_recommendation_ssl_tls_recommendation_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Universal ssl settings for a zone universal ssl settings details.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesSslUniversalSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn universal_ssl_settings_for_a_zone_universal_ssl_settings_details(
        &self,
        args: &UniversalSslSettingsForAZoneUniversalSslSettingsDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesSslUniversalSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = universal_ssl_settings_for_a_zone_universal_ssl_settings_details_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = universal_ssl_settings_for_a_zone_universal_ssl_settings_details_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Universal ssl settings for a zone edit universal ssl settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesSslUniversalSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn universal_ssl_settings_for_a_zone_edit_universal_ssl_settings(
        &self,
        args: &UniversalSslSettingsForAZoneEditUniversalSslSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesSslUniversalSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = universal_ssl_settings_for_a_zone_edit_universal_ssl_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = universal_ssl_settings_for_a_zone_edit_universal_ssl_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ssl verification ssl verification details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesSslVerificationResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn ssl_verification_ssl_verification_details(
        &self,
        args: &SslVerificationSslVerificationDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesSslVerificationResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ssl_verification_ssl_verification_details_builder(
            &self.http_client,
            &args.zone_id,
            &args.retry,
        )
        .map_err(ProviderError::Api)?;

        let task = ssl_verification_ssl_verification_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Ssl verification edit ssl certificate pack validation method.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsCertificatesAndHostnamesSslValidationMethodResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn ssl_verification_edit_ssl_certificate_pack_validation_method(
        &self,
        args: &SslVerificationEditSslCertificatePackValidationMethodArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsCertificatesAndHostnamesSslValidationMethodResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = ssl_verification_edit_ssl_certificate_pack_validation_method_builder(
            &self.http_client,
            &args.certificate_pack_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = ssl_verification_edit_ssl_certificate_pack_validation_method_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone subscription zone subscription details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BillSubsApiZoneSubscriptionResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn zone_subscription_zone_subscription_details(
        &self,
        args: &ZoneSubscriptionZoneSubscriptionDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BillSubsApiZoneSubscriptionResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_subscription_zone_subscription_details_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_subscription_zone_subscription_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone subscription create zone subscription.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BillSubsApiZoneSubscriptionResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_subscription_create_zone_subscription(
        &self,
        args: &ZoneSubscriptionCreateZoneSubscriptionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BillSubsApiZoneSubscriptionResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_subscription_create_zone_subscription_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_subscription_create_zone_subscription_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Zone subscription update zone subscription.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BillSubsApiZoneSubscriptionResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn zone_subscription_update_zone_subscription(
        &self,
        args: &ZoneSubscriptionUpdateZoneSubscriptionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BillSubsApiZoneSubscriptionResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = zone_subscription_update_zone_subscription_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = zone_subscription_update_zone_subscription_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tags zone get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceTaggingTaggedResourceResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn tags_zone_get(
        &self,
        args: &TagsZoneGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceTaggingTaggedResourceResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tags_zone_get_builder(
            &self.http_client,
            &args.zone_id,
            &args.resource_id,
            &args.resource_type,
            &args.access_application_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tags_zone_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tags zone set.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourceTaggingTaggedResourceResponseSingle result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn tags_zone_set(
        &self,
        args: &TagsZoneSetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourceTaggingTaggedResourceResponseSingle, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tags_zone_set_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tags_zone_set_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Tags zone delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn tags_zone_delete(
        &self,
        args: &TagsZoneDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = tags_zone_delete_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = tags_zone_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Token validation config list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn token_validation_config_list(
        &self,
        args: &TokenValidationConfigListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = token_validation_config_list_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = token_validation_config_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Token validation config create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn token_validation_config_create(
        &self,
        args: &TokenValidationConfigCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = token_validation_config_create_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = token_validation_config_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Token validation config get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn token_validation_config_get(
        &self,
        args: &TokenValidationConfigGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = token_validation_config_get_builder(
            &self.http_client,
            &args.zone_id,
            &args.config_id,
        )
        .map_err(ProviderError::Api)?;

        let task = token_validation_config_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Token validation config edit.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn token_validation_config_edit(
        &self,
        args: &TokenValidationConfigEditArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = token_validation_config_edit_builder(
            &self.http_client,
            &args.zone_id,
            &args.config_id,
        )
        .map_err(ProviderError::Api)?;

        let task = token_validation_config_edit_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Token validation config delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn token_validation_config_delete(
        &self,
        args: &TokenValidationConfigDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = token_validation_config_delete_builder(
            &self.http_client,
            &args.zone_id,
            &args.config_id,
        )
        .map_err(ProviderError::Api)?;

        let task = token_validation_config_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Token validation config credentials update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn token_validation_config_credentials_update(
        &self,
        args: &TokenValidationConfigCredentialsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = token_validation_config_credentials_update_builder(
            &self.http_client,
            &args.zone_id,
            &args.config_id,
        )
        .map_err(ProviderError::Api)?;

        let task = token_validation_config_credentials_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Token validation rules list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn token_validation_rules_list(
        &self,
        args: &TokenValidationRulesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = token_validation_rules_list_builder(
            &self.http_client,
            &args.zone_id,
            &args.token_configuration,
            &args.action,
            &args.enabled,
            &args.id,
            &args.rule_id,
            &args.host,
            &args.hostname,
        )
        .map_err(ProviderError::Api)?;

        let task = token_validation_rules_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Token validation rules create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn token_validation_rules_create(
        &self,
        args: &TokenValidationRulesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = token_validation_rules_create_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = token_validation_rules_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Token validation rules bulk create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn token_validation_rules_bulk_create(
        &self,
        args: &TokenValidationRulesBulkCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = token_validation_rules_bulk_create_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = token_validation_rules_bulk_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Token validation rules bulk edit.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn token_validation_rules_bulk_edit(
        &self,
        args: &TokenValidationRulesBulkEditArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = token_validation_rules_bulk_edit_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = token_validation_rules_bulk_edit_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Token validation rules preview.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn token_validation_rules_preview(
        &self,
        args: &TokenValidationRulesPreviewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = token_validation_rules_preview_builder(
            &self.http_client,
            &args.zone_id,
            &args.state,
            &args.host,
            &args.hostname,
            &args.method,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = token_validation_rules_preview_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Token validation rules get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn token_validation_rules_get(
        &self,
        args: &TokenValidationRulesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = token_validation_rules_get_builder(
            &self.http_client,
            &args.zone_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = token_validation_rules_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Token validation rules edit.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn token_validation_rules_edit(
        &self,
        args: &TokenValidationRulesEditArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = token_validation_rules_edit_builder(
            &self.http_client,
            &args.zone_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = token_validation_rules_edit_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Token validation rules delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn token_validation_rules_delete(
        &self,
        args: &TokenValidationRulesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = token_validation_rules_delete_builder(
            &self.http_client,
            &args.zone_id,
            &args.rule_id,
        )
        .map_err(ProviderError::Api)?;

        let task = token_validation_rules_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Get url normalization.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn get_url_normalization(
        &self,
        args: &GetUrlNormalizationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = get_url_normalization_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = get_url_normalization_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Update url normalization.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn update_url_normalization(
        &self,
        args: &UpdateUrlNormalizationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = update_url_normalization_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = update_url_normalization_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Delete url normalization.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the () result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn delete_url_normalization(
        &self,
        args: &DeleteUrlNormalizationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<(), ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = delete_url_normalization_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = delete_url_normalization_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room list waiting rooms.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waiting_room_list_waiting_rooms(
        &self,
        args: &WaitingRoomListWaitingRoomsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_list_waiting_rooms_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_list_waiting_rooms_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room create waiting room.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waiting_room_create_waiting_room(
        &self,
        args: &WaitingRoomCreateWaitingRoomArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_create_waiting_room_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_create_waiting_room_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room create a custom waiting room page preview.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomPreviewResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waiting_room_create_a_custom_waiting_room_page_preview(
        &self,
        args: &WaitingRoomCreateACustomWaitingRoomPagePreviewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomPreviewResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_create_a_custom_waiting_room_page_preview_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_create_a_custom_waiting_room_page_preview_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room get zone settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomZoneSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waiting_room_get_zone_settings(
        &self,
        args: &WaitingRoomGetZoneSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomZoneSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_get_zone_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_get_zone_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room update zone settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomZoneSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waiting_room_update_zone_settings(
        &self,
        args: &WaitingRoomUpdateZoneSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomZoneSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_update_zone_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_update_zone_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room patch zone settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomZoneSettingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waiting_room_patch_zone_settings(
        &self,
        args: &WaitingRoomPatchZoneSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomZoneSettingsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_patch_zone_settings_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_patch_zone_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room waiting room details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waiting_room_waiting_room_details(
        &self,
        args: &WaitingRoomWaitingRoomDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_waiting_room_details_builder(
            &self.http_client,
            &args.waiting_room_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_waiting_room_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room update waiting room.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waiting_room_update_waiting_room(
        &self,
        args: &WaitingRoomUpdateWaitingRoomArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_update_waiting_room_builder(
            &self.http_client,
            &args.waiting_room_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_update_waiting_room_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room patch waiting room.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomSingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waiting_room_patch_waiting_room(
        &self,
        args: &WaitingRoomPatchWaitingRoomArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomSingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_patch_waiting_room_builder(
            &self.http_client,
            &args.waiting_room_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_patch_waiting_room_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room delete waiting room.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomWaitingRoomIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waiting_room_delete_waiting_room(
        &self,
        args: &WaitingRoomDeleteWaitingRoomArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomWaitingRoomIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_delete_waiting_room_builder(
            &self.http_client,
            &args.waiting_room_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_delete_waiting_room_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room list events.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomEventResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waiting_room_list_events(
        &self,
        args: &WaitingRoomListEventsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomEventResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_list_events_builder(
            &self.http_client,
            &args.waiting_room_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_list_events_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room create event.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomEventResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waiting_room_create_event(
        &self,
        args: &WaitingRoomCreateEventArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomEventResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_create_event_builder(
            &self.http_client,
            &args.waiting_room_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_create_event_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room event details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomEventResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waiting_room_event_details(
        &self,
        args: &WaitingRoomEventDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomEventResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_event_details_builder(
            &self.http_client,
            &args.event_id,
            &args.waiting_room_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_event_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room update event.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomEventResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waiting_room_update_event(
        &self,
        args: &WaitingRoomUpdateEventArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomEventResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_update_event_builder(
            &self.http_client,
            &args.event_id,
            &args.waiting_room_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_update_event_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room patch event.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomEventResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waiting_room_patch_event(
        &self,
        args: &WaitingRoomPatchEventArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomEventResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_patch_event_builder(
            &self.http_client,
            &args.event_id,
            &args.waiting_room_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_patch_event_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room delete event.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomEventIdResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waiting_room_delete_event(
        &self,
        args: &WaitingRoomDeleteEventArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomEventIdResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_delete_event_builder(
            &self.http_client,
            &args.event_id,
            &args.waiting_room_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_delete_event_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room preview active event details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomEventDetailsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waiting_room_preview_active_event_details(
        &self,
        args: &WaitingRoomPreviewActiveEventDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomEventDetailsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_preview_active_event_details_builder(
            &self.http_client,
            &args.event_id,
            &args.waiting_room_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_preview_active_event_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room list waiting room rules.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomRulesResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waiting_room_list_waiting_room_rules(
        &self,
        args: &WaitingRoomListWaitingRoomRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomRulesResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_list_waiting_room_rules_builder(
            &self.http_client,
            &args.waiting_room_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_list_waiting_room_rules_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room create waiting room rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomRulesResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waiting_room_create_waiting_room_rule(
        &self,
        args: &WaitingRoomCreateWaitingRoomRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomRulesResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_create_waiting_room_rule_builder(
            &self.http_client,
            &args.waiting_room_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_create_waiting_room_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room replace waiting room rules.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomRulesResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waiting_room_replace_waiting_room_rules(
        &self,
        args: &WaitingRoomReplaceWaitingRoomRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomRulesResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_replace_waiting_room_rules_builder(
            &self.http_client,
            &args.waiting_room_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_replace_waiting_room_rules_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room patch waiting room rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomRulesResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waiting_room_patch_waiting_room_rule(
        &self,
        args: &WaitingRoomPatchWaitingRoomRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomRulesResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_patch_waiting_room_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.waiting_room_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_patch_waiting_room_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room delete waiting room rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomRulesResponseCollection result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn waiting_room_delete_waiting_room_rule(
        &self,
        args: &WaitingRoomDeleteWaitingRoomRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomRulesResponseCollection, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_delete_waiting_room_rule_builder(
            &self.http_client,
            &args.rule_id,
            &args.waiting_room_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_delete_waiting_room_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Waiting room get waiting room status.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WaitingroomStatusResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn waiting_room_get_waiting_room_status(
        &self,
        args: &WaitingRoomGetWaitingRoomStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WaitingroomStatusResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = waiting_room_get_waiting_room_status_builder(
            &self.http_client,
            &args.waiting_room_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = waiting_room_get_waiting_room_status_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web3 hostname list web3 hostnames.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Web3CollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn web3_hostname_list_web3_hostnames(
        &self,
        args: &Web3HostnameListWeb3HostnamesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Web3CollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web3_hostname_list_web3_hostnames_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web3_hostname_list_web3_hostnames_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web3 hostname create web3 hostname.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Web3SingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn web3_hostname_create_web3_hostname(
        &self,
        args: &Web3HostnameCreateWeb3HostnameArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Web3SingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web3_hostname_create_web3_hostname_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web3_hostname_create_web3_hostname_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web3 hostname web3 hostname details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Web3SingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn web3_hostname_web3_hostname_details(
        &self,
        args: &Web3HostnameWeb3HostnameDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Web3SingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web3_hostname_web3_hostname_details_builder(
            &self.http_client,
            &args.identifier,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web3_hostname_web3_hostname_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web3 hostname edit web3 hostname.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Web3SingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn web3_hostname_edit_web3_hostname(
        &self,
        args: &Web3HostnameEditWeb3HostnameArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Web3SingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web3_hostname_edit_web3_hostname_builder(
            &self.http_client,
            &args.identifier,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web3_hostname_edit_web3_hostname_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web3 hostname delete web3 hostname.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Web3ApiResponseSingleId result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn web3_hostname_delete_web3_hostname(
        &self,
        args: &Web3HostnameDeleteWeb3HostnameArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Web3ApiResponseSingleId, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web3_hostname_delete_web3_hostname_builder(
            &self.http_client,
            &args.identifier,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web3_hostname_delete_web3_hostname_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web3 hostname ipfs universal path gateway content list details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Web3ContentListDetailsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn web3_hostname_ipfs_universal_path_gateway_content_list_details(
        &self,
        args: &Web3HostnameIpfsUniversalPathGatewayContentListDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Web3ContentListDetailsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web3_hostname_ipfs_universal_path_gateway_content_list_details_builder(
            &self.http_client,
            &args.identifier,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web3_hostname_ipfs_universal_path_gateway_content_list_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web3 hostname update ipfs universal path gateway content list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Web3ContentListDetailsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn web3_hostname_update_ipfs_universal_path_gateway_content_list(
        &self,
        args: &Web3HostnameUpdateIpfsUniversalPathGatewayContentListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Web3ContentListDetailsResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web3_hostname_update_ipfs_universal_path_gateway_content_list_builder(
            &self.http_client,
            &args.identifier,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web3_hostname_update_ipfs_universal_path_gateway_content_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web3 hostname list ipfs universal path gateway content list entries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Web3ContentListEntryCollectionResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn web3_hostname_list_ipfs_universal_path_gateway_content_list_entries(
        &self,
        args: &Web3HostnameListIpfsUniversalPathGatewayContentListEntriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Web3ContentListEntryCollectionResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web3_hostname_list_ipfs_universal_path_gateway_content_list_entries_builder(
            &self.http_client,
            &args.identifier,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web3_hostname_list_ipfs_universal_path_gateway_content_list_entries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web3 hostname create ipfs universal path gateway content list entry.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Web3ContentListEntrySingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn web3_hostname_create_ipfs_universal_path_gateway_content_list_entry(
        &self,
        args: &Web3HostnameCreateIpfsUniversalPathGatewayContentListEntryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Web3ContentListEntrySingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web3_hostname_create_ipfs_universal_path_gateway_content_list_entry_builder(
            &self.http_client,
            &args.identifier,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web3_hostname_create_ipfs_universal_path_gateway_content_list_entry_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web3 hostname ipfs universal path gateway content list entry details.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Web3ContentListEntrySingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn web3_hostname_ipfs_universal_path_gateway_content_list_entry_details(
        &self,
        args: &Web3HostnameIpfsUniversalPathGatewayContentListEntryDetailsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Web3ContentListEntrySingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web3_hostname_ipfs_universal_path_gateway_content_list_entry_details_builder(
            &self.http_client,
            &args.content_list_entry_identifier,
            &args.identifier,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web3_hostname_ipfs_universal_path_gateway_content_list_entry_details_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web3 hostname edit ipfs universal path gateway content list entry.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Web3ContentListEntrySingleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn web3_hostname_edit_ipfs_universal_path_gateway_content_list_entry(
        &self,
        args: &Web3HostnameEditIpfsUniversalPathGatewayContentListEntryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Web3ContentListEntrySingleResponse, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web3_hostname_edit_ipfs_universal_path_gateway_content_list_entry_builder(
            &self.http_client,
            &args.content_list_entry_identifier,
            &args.identifier,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web3_hostname_edit_ipfs_universal_path_gateway_content_list_entry_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Web3 hostname delete ipfs universal path gateway content list entry.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Web3ApiResponseSingleId result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn web3_hostname_delete_ipfs_universal_path_gateway_content_list_entry(
        &self,
        args: &Web3HostnameDeleteIpfsUniversalPathGatewayContentListEntryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Web3ApiResponseSingleId, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = web3_hostname_delete_ipfs_universal_path_gateway_content_list_entry_builder(
            &self.http_client,
            &args.content_list_entry_identifier,
            &args.identifier,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = web3_hostname_delete_ipfs_universal_path_gateway_content_list_entry_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker routes list routes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_routes_list_routes(
        &self,
        args: &WorkerRoutesListRoutesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_routes_list_routes_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_routes_list_routes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker routes create route.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_routes_create_route(
        &self,
        args: &WorkerRoutesCreateRouteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_routes_create_route_builder(
            &self.http_client,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_routes_create_route_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker routes get route.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn worker_routes_get_route(
        &self,
        args: &WorkerRoutesGetRouteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_routes_get_route_builder(
            &self.http_client,
            &args.route_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_routes_get_route_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker routes update route.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_routes_update_route(
        &self,
        args: &WorkerRoutesUpdateRouteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_routes_update_route_builder(
            &self.http_client,
            &args.route_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_routes_update_route_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Worker routes delete route.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn worker_routes_delete_route(
        &self,
        args: &WorkerRoutesDeleteRouteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::cloudflare::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = worker_routes_delete_route_builder(
            &self.http_client,
            &args.route_id,
            &args.zone_id,
        )
        .map_err(ProviderError::Api)?;

        let task = worker_routes_delete_route_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

}
