//! Auto-generated API module for prisma_postgres databases.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `prisma_postgres_databases `

#![cfg(feature = "prisma_postgres_databases")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ProjectsDatabasesConnectionsItemEndpointsDirect` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProjectsDatabasesConnectionsItemEndpointsDirect {
    /// connectionString property.
    pub connection_string: Option<String>,
    /// host property.
    pub host: String,
    /// port property.
    pub port: f64,
}

/// `DatabasesConnectionsItem` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesConnectionsItem {
    /// createdAt property.
    pub created_at: String,
    /// database property.
    pub database: DatabasesConnectionsItemDatabase,
    /// directConnection property.
    pub direct_connection: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// endpoints property.
    pub endpoints: DatabasesConnectionsItemEndpoints,
    /// id property.
    pub id: String,
    /// kind property.
    pub kind: String,
    /// name property.
    pub name: String,
    /// type property.
    pub r#type: String,
    /// url property.
    pub url: String,
}

/// `DatabasesProject` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesProject {
    /// id property.
    pub id: String,
    /// name property.
    pub name: String,
    /// url property.
    pub url: String,
}

/// `ProjectsDatabasesPostRequest` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProjectsDatabasesPostRequest {
    /// fromDatabase property.
    pub from_database: Option<ProjectsDatabasesPostRequestFromDatabase>,
    /// isDefault property.
    pub is_default: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// source property.
    pub source: Option<serde_json::Value>,
}

/// `ProjectsDatabasesApiKeysItemEndpoints` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProjectsDatabasesApiKeysItemEndpoints {
    /// accelerate property.
    pub accelerate: Option<ProjectsDatabasesApiKeysItemEndpointsAccelerate>,
    /// direct property.
    pub direct: Option<ProjectsDatabasesApiKeysItemEndpointsDirect>,
    /// pooled property.
    pub pooled: Option<ProjectsDatabasesApiKeysItemEndpointsPooled>,
}

/// `DatabasesConnectionsItemDatabase` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesConnectionsItemDatabase {
    /// id property.
    pub id: String,
    /// name property.
    pub name: String,
    /// url property.
    pub url: String,
}

/// `DatabasesPatchResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesPatchResponse {
    /// data property.
    pub data: Databases,
}

/// `ProjectsDatabasesRegion` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProjectsDatabasesRegion {
    /// id property.
    pub id: String,
    /// name property.
    pub name: String,
}

/// `DatabasesPatchRequest` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesPatchRequest {
    /// name property.
    pub name: Option<String>,
}

/// `DatabasesConnectionsItemEndpointsPooled` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesConnectionsItemEndpointsPooled {
    /// host property.
    pub host: String,
    /// port property.
    pub port: f64,
}

/// `ProjectsDatabasesProject` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProjectsDatabasesProject {
    /// id property.
    pub id: String,
    /// name property.
    pub name: String,
    /// url property.
    pub url: String,
}

/// `ProjectsDatabasesApiKeysItemEndpointsAccelerate` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProjectsDatabasesApiKeysItemEndpointsAccelerate {
    /// host property.
    pub host: String,
    /// port property.
    pub port: f64,
}

/// `ProjectsDatabasesConnectionsItemEndpoints` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProjectsDatabasesConnectionsItemEndpoints {
    /// accelerate property.
    pub accelerate: Option<ProjectsDatabasesConnectionsItemEndpointsAccelerate>,
    /// direct property.
    pub direct: Option<ProjectsDatabasesConnectionsItemEndpointsDirect>,
    /// pooled property.
    pub pooled: Option<ProjectsDatabasesConnectionsItemEndpointsPooled>,
}

/// `DatabasesConnectionsItemEndpointsAccelerate` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesConnectionsItemEndpointsAccelerate {
    /// host property.
    pub host: String,
    /// port property.
    pub port: f64,
}

/// `ProjectsDatabasesPostRequestFromDatabase` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProjectsDatabasesPostRequestFromDatabase {
    /// backupId property.
    pub backup_id: Option<String>,
    /// id property.
    pub id: String,
}

/// `DatabasesPostResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesPostResponse {
    /// data property.
    pub data: Databases,
}

/// `Databases` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Databases {
    /// connections property.
    pub connections: Vec<DatabasesConnectionsItem>,
    /// createdAt property.
    pub created_at: String,
    /// defaultConnectionId property.
    pub default_connection_id: String,
    /// id property.
    pub id: String,
    /// isDefault property.
    pub is_default: bool,
    /// name property.
    pub name: String,
    /// project property.
    pub project: DatabasesProject,
    /// region property.
    pub region: std::collections::HashMap<String, serde_json::Value>,
    /// source property.
    pub source: serde_json::Value,
    /// status property.
    pub status: String,
    /// type property.
    pub r#type: String,
    /// url property.
    pub url: String,
}

/// `DatabasesConnectionsItemEndpointsDirect` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesConnectionsItemEndpointsDirect {
    /// host property.
    pub host: String,
    /// port property.
    pub port: f64,
}

/// `ProjectsDatabases` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProjectsDatabases {
    /// apiKeys property.
    pub api_keys: Vec<ProjectsDatabasesApiKeysItem>,
    /// connectionString property.
    pub connection_string: String,
    /// connections property.
    pub connections: Vec<ProjectsDatabasesConnectionsItem>,
    /// createdAt property.
    pub created_at: String,
    /// defaultConnectionId property.
    pub default_connection_id: String,
    /// directConnection property.
    pub direct_connection: std::collections::HashMap<String, serde_json::Value>,
    /// id property.
    pub id: String,
    /// isDefault property.
    pub is_default: bool,
    /// name property.
    pub name: String,
    /// project property.
    pub project: ProjectsDatabasesProject,
    /// region property.
    pub region: ProjectsDatabasesRegion,
    /// source property.
    pub source: serde_json::Value,
    /// status property.
    pub status: String,
    /// type property.
    pub r#type: String,
    /// url property.
    pub url: String,
}

/// `ProjectsDatabasesApiKeysItem` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProjectsDatabasesApiKeysItem {
    /// connectionString property.
    pub connection_string: String,
    /// createdAt property.
    pub created_at: String,
    /// directConnection property.
    pub direct_connection: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// endpoints property.
    pub endpoints: ProjectsDatabasesApiKeysItemEndpoints,
    /// id property.
    pub id: String,
    /// kind property.
    pub kind: String,
    /// name property.
    pub name: String,
    /// type property.
    pub r#type: String,
    /// url property.
    pub url: String,
}

/// `ProjectsDatabasesConnectionsItemEndpointsAccelerate` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProjectsDatabasesConnectionsItemEndpointsAccelerate {
    /// connectionString property.
    pub connection_string: Option<String>,
    /// host property.
    pub host: String,
    /// port property.
    pub port: f64,
}

/// `ProjectsDatabasesConnectionsItem` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProjectsDatabasesConnectionsItem {
    /// createdAt property.
    pub created_at: String,
    /// database property.
    pub database: ProjectsDatabasesConnectionsItemDatabase,
    /// directConnection property.
    pub direct_connection: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// endpoints property.
    pub endpoints: ProjectsDatabasesConnectionsItemEndpoints,
    /// id property.
    pub id: String,
    /// kind property.
    pub kind: String,
    /// name property.
    pub name: String,
    /// type property.
    pub r#type: String,
    /// url property.
    pub url: String,
}

/// `ProjectsDatabasesConnectionsItemDatabase` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProjectsDatabasesConnectionsItemDatabase {
    /// id property.
    pub id: String,
    /// name property.
    pub name: String,
    /// url property.
    pub url: String,
}

/// `ProjectsDatabasesConnectionsItemEndpointsPooled` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProjectsDatabasesConnectionsItemEndpointsPooled {
    /// connectionString property.
    pub connection_string: Option<String>,
    /// host property.
    pub host: String,
    /// port property.
    pub port: f64,
}

/// `ProjectsDatabasesApiKeysItemEndpointsPooled` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProjectsDatabasesApiKeysItemEndpointsPooled {
    /// host property.
    pub host: String,
    /// port property.
    pub port: f64,
}

/// `ProjectsDatabasesApiKeysItemEndpointsDirect` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProjectsDatabasesApiKeysItemEndpointsDirect {
    /// host property.
    pub host: String,
    /// port property.
    pub port: f64,
}

/// `ProjectsDatabasesPostResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProjectsDatabasesPostResponse {
    /// data property.
    pub data: ProjectsDatabases,
}

/// `DatabasesGetResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesGetResponse {
    /// data property.
    pub data: Databases,
}

/// `ProjectsDatabasesGetResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProjectsDatabasesGetResponse {
    /// data property.
    pub data: Vec<ProjectsDatabases>,
    /// pagination property.
    pub pagination: std::collections::HashMap<String, serde_json::Value>,
}

/// `DatabasesPostRequest` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesPostRequest {
    /// isDefault property.
    pub is_default: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// projectId property.
    pub project_id: String,
    /// region property.
    pub region: Option<String>,
    /// source property.
    pub source: Option<serde_json::Value>,
}

/// `DatabasesConnectionsItemEndpoints` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesConnectionsItemEndpoints {
    /// accelerate property.
    pub accelerate: Option<DatabasesConnectionsItemEndpointsAccelerate>,
    /// direct property.
    pub direct: Option<DatabasesConnectionsItemEndpointsDirect>,
    /// pooled property.
    pub pooled: Option<DatabasesConnectionsItemEndpointsPooled>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`getV1Databases_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1DatabasesArgs {
    /// Query parameter: `cursor`.
    pub cursor: Option<String>,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `projectId`.
    pub project_id: Option<String>,
}

/// Arguments for [`postV1Databases_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1DatabasesArgs {
    /// Request body.
    pub body: DatabasesPostRequest,
}

/// Arguments for [`getV1DatabasesByDatabaseId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1DatabasesByDatabaseIdArgs {
    /// Path parameter: `databaseId`.
    pub database_id: String,
}

/// Arguments for [`patchV1DatabasesByDatabaseId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchV1DatabasesByDatabaseIdArgs {
    /// Path parameter: `databaseId`.
    pub database_id: String,
    /// Request body.
    pub body: DatabasesPatchRequest,
}

/// Arguments for [`deleteV1DatabasesByDatabaseId_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1DatabasesByDatabaseIdArgs {
    /// Path parameter: `databaseId`.
    pub database_id: String,
}

/// Arguments for [`getV1ProjectsByProjectIdDatabases_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ProjectsByProjectIdDatabasesArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Query parameter: `cursor`.
    pub cursor: Option<String>,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
}

/// Arguments for [`postV1ProjectsByProjectIdDatabases_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ProjectsByProjectIdDatabasesArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Request body.
    pub body: ProjectsDatabasesPostRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET /v1/databases
// -----------------------------------------------------------------------------

/// GET /v1/databases.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_v1_databases_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_v1_databases_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatabasesGetResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.prisma.io/v1/databases",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DatabasesGetResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /v1/databases
// -----------------------------------------------------------------------------

/// POST /v1/databases.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = post_v1_databases_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn post_v1_databases_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PostV1DatabasesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatabasesPostResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.prisma.io/v1/databases",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DatabasesPostResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /v1/databases/{databaseId}
// -----------------------------------------------------------------------------

/// GET /v1/databases/{databaseId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_v1_databases_by_database_id_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_v1_databases_by_database_id_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GetV1DatabasesByDatabaseIdArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatabasesGetResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.prisma.io/v1/databases/{}", args.database_id,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DatabasesGetResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH /v1/databases/{databaseId}
// -----------------------------------------------------------------------------

/// PATCH /v1/databases/{databaseId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = patch_v1_databases_by_database_id_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn patch_v1_databases_by_database_id_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PatchV1DatabasesByDatabaseIdArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatabasesPatchResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.prisma.io/v1/databases/{}", args.database_id,);

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DatabasesPatchResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /v1/databases/{databaseId}
// -----------------------------------------------------------------------------

/// DELETE /v1/databases/{databaseId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = delete_v1_databases_by_database_id_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn delete_v1_databases_by_database_id_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeleteV1DatabasesByDatabaseIdArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.prisma.io/v1/databases/{}", args.database_id,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /v1/projects/{projectId}/databases
// -----------------------------------------------------------------------------

/// GET /v1/projects/{projectId}/databases.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_v1_projects_by_project_id_databases_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_v1_projects_by_project_id_databases_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GetV1ProjectsByProjectIdDatabasesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProjectsDatabasesGetResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.prisma.io/v1/projects/{}/databases",
        args.project_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProjectsDatabasesGetResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /v1/projects/{projectId}/databases
// -----------------------------------------------------------------------------

/// POST /v1/projects/{projectId}/databases.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = post_v1_projects_by_project_id_databases_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn post_v1_projects_by_project_id_databases_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PostV1ProjectsByProjectIdDatabasesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProjectsDatabasesPostResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.prisma.io/v1/projects/{}/databases",
        args.project_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProjectsDatabasesPostResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
