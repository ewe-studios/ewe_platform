//! Auto-generated API module for prisma_postgres usage.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `prisma_postgres_usage `

#![cfg(feature = "prisma_postgres_usage")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `DatabasesUsageGetResponsePeriod` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesUsageGetResponsePeriod {
    /// end property.
    pub end: String,
    /// start property.
    pub start: String,
}

/// `DatabasesUsageGetResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesUsageGetResponse {
    /// generatedAt property.
    pub generated_at: String,
    /// metrics property.
    pub metrics: DatabasesUsageGetResponseMetrics,
    /// period property.
    pub period: DatabasesUsageGetResponsePeriod,
}

/// `DatabasesUsageGetResponseMetricsOperations` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesUsageGetResponseMetricsOperations {
    /// unit property.
    pub unit: String,
    /// used property.
    pub used: f64,
}

/// `DatabasesUsageGetResponseMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesUsageGetResponseMetrics {
    /// operations property.
    pub operations: DatabasesUsageGetResponseMetricsOperations,
    /// storage property.
    pub storage: DatabasesUsageGetResponseMetricsStorage,
}

/// `DatabasesUsageGetResponseMetricsStorage` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesUsageGetResponseMetricsStorage {
    /// unit property.
    pub unit: String,
    /// used property.
    pub used: f64,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`getV1DatabasesByDatabaseIdUsage_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1DatabasesByDatabaseIdUsageArgs {
    /// Path parameter: `databaseId`.
    pub database_id: String,
    /// Query parameter: `startDate`.
    pub start_date: Option<String>,
    /// Query parameter: `endDate`.
    pub end_date: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET /v1/databases/{databaseId}/usage
// -----------------------------------------------------------------------------

/// GET /v1/databases/{databaseId}/usage.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_v1_databases_by_database_id_usage_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_v1_databases_by_database_id_usage_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GetV1DatabasesByDatabaseIdUsageArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatabasesUsageGetResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.prisma.io/v1/databases/{}/usage",
        args.database_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DatabasesUsageGetResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
