//! Auto-generated API module for prisma_postgres restore.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `prisma_postgres_restore `

#![cfg(feature = "prisma_postgres_restore")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `DatabasesRestorePostResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesRestorePostResponse {
    /// data property.
    pub data: DatabasesRestore,
}

/// `DatabasesRestoreConnectionsItem` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesRestoreConnectionsItem {
    /// createdAt property.
    pub created_at: String,
    /// database property.
    pub database: DatabasesRestoreConnectionsItemDatabase,
    /// directConnection property.
    pub direct_connection: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// endpoints property.
    pub endpoints: DatabasesRestoreConnectionsItemEndpoints,
    /// id property.
    pub id: String,
    /// kind property.
    pub kind: String,
    /// name property.
    pub name: String,
    /// type property.
    pub r#type: String,
    /// url property.
    pub url: String,
}

/// `DatabasesRestore` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesRestore {
    /// connections property.
    pub connections: Vec<DatabasesRestoreConnectionsItem>,
    /// createdAt property.
    pub created_at: String,
    /// defaultConnectionId property.
    pub default_connection_id: String,
    /// id property.
    pub id: String,
    /// isDefault property.
    pub is_default: bool,
    /// name property.
    pub name: String,
    /// project property.
    pub project: DatabasesRestoreProject,
    /// region property.
    pub region: std::collections::HashMap<String, serde_json::Value>,
    /// source property.
    pub source: DatabasesRestoreSource,
    /// status property.
    pub status: String,
    /// type property.
    pub r#type: String,
    /// url property.
    pub url: String,
}

/// `DatabasesRestoreConnectionsItemDatabase` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesRestoreConnectionsItemDatabase {
    /// id property.
    pub id: String,
    /// name property.
    pub name: String,
    /// url property.
    pub url: String,
}

/// `DatabasesRestoreProject` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesRestoreProject {
    /// id property.
    pub id: String,
    /// name property.
    pub name: String,
    /// url property.
    pub url: String,
}

/// `DatabasesRestorePostRequestSource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesRestorePostRequestSource {
    /// backupId property.
    pub backup_id: String,
    /// databaseId property.
    pub database_id: String,
    /// type property.
    pub r#type: String,
}

/// `DatabasesRestoreConnectionsItemEndpointsDirect` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesRestoreConnectionsItemEndpointsDirect {
    /// host property.
    pub host: String,
    /// port property.
    pub port: f64,
}

/// `DatabasesRestoreConnectionsItemEndpointsAccelerate` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesRestoreConnectionsItemEndpointsAccelerate {
    /// host property.
    pub host: String,
    /// port property.
    pub port: f64,
}

/// `DatabasesRestorePostRequest` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesRestorePostRequest {
    /// source property.
    pub source: DatabasesRestorePostRequestSource,
}

/// `DatabasesRestoreConnectionsItemEndpoints` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesRestoreConnectionsItemEndpoints {
    /// accelerate property.
    pub accelerate: Option<DatabasesRestoreConnectionsItemEndpointsAccelerate>,
    /// direct property.
    pub direct: Option<DatabasesRestoreConnectionsItemEndpointsDirect>,
    /// pooled property.
    pub pooled: Option<DatabasesRestoreConnectionsItemEndpointsPooled>,
}

/// `DatabasesRestoreConnectionsItemEndpointsPooled` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesRestoreConnectionsItemEndpointsPooled {
    /// host property.
    pub host: String,
    /// port property.
    pub port: f64,
}

/// `DatabasesRestoreSource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesRestoreSource {
    /// backupId property.
    pub backup_id: String,
    /// databaseId property.
    pub database_id: String,
    /// type property.
    pub r#type: String,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`postV1DatabasesByTargetDatabaseIdRestore_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1DatabasesByTargetDatabaseIdRestoreArgs {
    /// Path parameter: `targetDatabaseId`.
    pub target_database_id: String,
    /// Request body.
    pub body: DatabasesRestorePostRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST /v1/databases/{targetDatabaseId}/restore
// -----------------------------------------------------------------------------

/// POST /v1/databases/{targetDatabaseId}/restore.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = post_v1_databases_by_target_database_id_restore_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn post_v1_databases_by_target_database_id_restore_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PostV1DatabasesByTargetDatabaseIdRestoreArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatabasesRestorePostResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.prisma.io/v1/databases/{}/restore",
        args.target_database_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DatabasesRestorePostResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
