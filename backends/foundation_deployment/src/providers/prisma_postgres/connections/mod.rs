//! Auto-generated API module for prisma_postgres connections.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `prisma_postgres_connections `

#![cfg(feature = "prisma_postgres_connections")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `DatabasesConnectionsDatabase` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesConnectionsDatabase {
    /// id property.
    pub id: String,
    /// name property.
    pub name: String,
    /// url property.
    pub url: String,
}

/// `ConnectionsDatabase` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConnectionsDatabase {
    /// id property.
    pub id: String,
    /// name property.
    pub name: String,
    /// url property.
    pub url: String,
}

/// `DatabasesConnectionsPostResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesConnectionsPostResponse {
    /// data property.
    pub data: DatabasesConnections,
}

/// `ConnectionsGetResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConnectionsGetResponse {
    /// data property.
    pub data: Connections,
}

/// `Connections` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Connections {
    /// createdAt property.
    pub created_at: String,
    /// database property.
    pub database: ConnectionsDatabase,
    /// directConnection property.
    pub direct_connection: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// endpoints property.
    pub endpoints: ConnectionsEndpoints,
    /// id property.
    pub id: String,
    /// kind property.
    pub kind: String,
    /// name property.
    pub name: String,
    /// type property.
    pub r#type: String,
    /// url property.
    pub url: String,
}

/// `DatabasesConnectionsPostRequest` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesConnectionsPostRequest {
    /// name property.
    pub name: String,
}

/// `ConnectionsPostRequest` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConnectionsPostRequest {
    /// databaseId property.
    pub database_id: String,
    /// name property.
    pub name: String,
}

/// `DatabasesConnectionsEndpoints` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesConnectionsEndpoints {
    /// accelerate property.
    pub accelerate: Option<DatabasesConnectionsEndpointsAccelerate>,
    /// direct property.
    pub direct: Option<DatabasesConnectionsEndpointsDirect>,
    /// pooled property.
    pub pooled: Option<DatabasesConnectionsEndpointsPooled>,
}

/// `ConnectionsEndpoints` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConnectionsEndpoints {
    /// accelerate property.
    pub accelerate: Option<ConnectionsEndpointsAccelerate>,
    /// direct property.
    pub direct: Option<ConnectionsEndpointsDirect>,
    /// pooled property.
    pub pooled: Option<ConnectionsEndpointsPooled>,
}

/// `ConnectionsPostResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConnectionsPostResponse {
    /// data property.
    pub data: Connections,
}

/// `ConnectionsEndpointsAccelerate` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConnectionsEndpointsAccelerate {
    /// host property.
    pub host: String,
    /// port property.
    pub port: f64,
}

/// `ConnectionsEndpointsDirect` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConnectionsEndpointsDirect {
    /// host property.
    pub host: String,
    /// port property.
    pub port: f64,
}

/// `ConnectionsEndpointsPooled` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConnectionsEndpointsPooled {
    /// host property.
    pub host: String,
    /// port property.
    pub port: f64,
}

/// `DatabasesConnectionsGetResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesConnectionsGetResponse {
    /// data property.
    pub data: Vec<DatabasesConnections>,
    /// pagination property.
    pub pagination: std::collections::HashMap<String, serde_json::Value>,
}

/// `DatabasesConnectionsEndpointsPooled` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesConnectionsEndpointsPooled {
    /// connectionString property.
    pub connection_string: String,
    /// host property.
    pub host: String,
    /// port property.
    pub port: f64,
}

/// `DatabasesConnections` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesConnections {
    /// connectionString property.
    pub connection_string: String,
    /// createdAt property.
    pub created_at: String,
    /// database property.
    pub database: DatabasesConnectionsDatabase,
    /// directConnection property.
    pub direct_connection: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// endpoints property.
    pub endpoints: DatabasesConnectionsEndpoints,
    /// host property.
    pub host: String,
    /// id property.
    pub id: String,
    /// kind property.
    pub kind: String,
    /// name property.
    pub name: String,
    /// pass property.
    pub pass: String,
    /// type property.
    pub r#type: String,
    /// url property.
    pub url: String,
    /// user property.
    pub user: String,
}

/// `DatabasesConnectionsEndpointsAccelerate` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesConnectionsEndpointsAccelerate {
    /// connectionString property.
    pub connection_string: String,
    /// host property.
    pub host: String,
    /// port property.
    pub port: f64,
}

/// `DatabasesConnectionsEndpointsDirect` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabasesConnectionsEndpointsDirect {
    /// connectionString property.
    pub connection_string: String,
    /// host property.
    pub host: String,
    /// port property.
    pub port: f64,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`getV1Connections_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ConnectionsArgs {
    /// Query parameter: `cursor`.
    pub cursor: Option<String>,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `databaseId`.
    pub database_id: Option<String>,
}

/// Arguments for [`postV1Connections_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1ConnectionsArgs {
    /// Request body.
    pub body: ConnectionsPostRequest,
}

/// Arguments for [`getV1ConnectionsById_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1ConnectionsByIdArgs {
    /// Path parameter: `id`.
    pub id: String,
}

/// Arguments for [`deleteV1ConnectionsById_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteV1ConnectionsByIdArgs {
    /// Path parameter: `id`.
    pub id: String,
}

/// Arguments for [`getV1DatabasesByDatabaseIdConnections_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetV1DatabasesByDatabaseIdConnectionsArgs {
    /// Path parameter: `databaseId`.
    pub database_id: String,
    /// Query parameter: `cursor`.
    pub cursor: Option<String>,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
}

/// Arguments for [`postV1DatabasesByDatabaseIdConnections_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostV1DatabasesByDatabaseIdConnectionsArgs {
    /// Path parameter: `databaseId`.
    pub database_id: String,
    /// Request body.
    pub body: DatabasesConnectionsPostRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET /v1/connections
// -----------------------------------------------------------------------------

/// GET /v1/connections.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_v1_connections_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_v1_connections_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConnectionsGetResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.prisma.io/v1/connections",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ConnectionsGetResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /v1/connections
// -----------------------------------------------------------------------------

/// POST /v1/connections.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = post_v1_connections_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn post_v1_connections_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PostV1ConnectionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConnectionsPostResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.prisma.io/v1/connections",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ConnectionsPostResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /v1/connections/{id}
// -----------------------------------------------------------------------------

/// GET /v1/connections/{id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_v1_connections_by_id_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_v1_connections_by_id_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GetV1ConnectionsByIdArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConnectionsGetResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.prisma.io/v1/connections/{}", args.id,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ConnectionsGetResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /v1/connections/{id}
// -----------------------------------------------------------------------------

/// DELETE /v1/connections/{id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = delete_v1_connections_by_id_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn delete_v1_connections_by_id_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeleteV1ConnectionsByIdArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.prisma.io/v1/connections/{}", args.id,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /v1/databases/{databaseId}/connections
// -----------------------------------------------------------------------------

/// GET /v1/databases/{databaseId}/connections.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_v1_databases_by_database_id_connections_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_v1_databases_by_database_id_connections_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GetV1DatabasesByDatabaseIdConnectionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatabasesConnectionsGetResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.prisma.io/v1/databases/{}/connections",
        args.database_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DatabasesConnectionsGetResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /v1/databases/{databaseId}/connections
// -----------------------------------------------------------------------------

/// POST /v1/databases/{databaseId}/connections.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = post_v1_databases_by_database_id_connections_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn post_v1_databases_by_database_id_connections_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PostV1DatabasesByDatabaseIdConnectionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatabasesConnectionsPostResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.prisma.io/v1/databases/{}/connections",
        args.database_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DatabasesConnectionsPostResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
