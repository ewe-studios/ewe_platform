//! Auto-generated API module for neon me.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `neon_me `

#![cfg(feature = "neon_me")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `PaymentSource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PaymentSource {
    /// card property.
    pub card: Option<PaymentSourceBankCard>,
    /// type property.
    pub r#type: String,
}

/// `BillingAccountState` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BillingAccountState {
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `BillingSubscriptionType` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BillingSubscriptionType {
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `PaymentSourceBankCard` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PaymentSourceBankCard {
    /// brand property.
    pub brand: Option<String>,
    /// exp_month property.
    pub exp_month: Option<i64>,
    /// exp_year property.
    pub exp_year: Option<i64>,
    /// last4 property.
    pub last4: String,
}

/// `IdentityProviderId` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IdentityProviderId {
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `CurrentUserInfoResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CurrentUserInfoResponse {
    /// active_seconds_limit property.
    pub active_seconds_limit: i64,
    /// auth_accounts property.
    pub auth_accounts: Vec<CurrentUserAuthAccount>,
    /// billing_account property.
    pub billing_account: Option<BillingAccount>,
    /// branches_limit property.
    pub branches_limit: i64,
    /// compute_seconds_limit property.
    pub compute_seconds_limit: Option<i64>,
    /// email property.
    pub email: String,
    /// id property.
    pub id: String,
    /// image property.
    pub image: String,
    /// last_name property.
    pub last_name: String,
    /// login property.
    pub login: String,
    /// max_autoscaling_limit property.
    pub max_autoscaling_limit: f64,
    /// name property.
    pub name: String,
    /// plan property.
    pub plan: String,
    /// projects_limit property.
    pub projects_limit: i64,
}

/// `CurrentUserAuthAccount` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CurrentUserAuthAccount {
    /// email property.
    pub email: String,
    /// image property.
    pub image: String,
    /// login property.
    pub login: String,
    /// name property.
    pub name: String,
    /// provider property.
    pub provider: IdentityProviderId,
}

/// `BillingPaymentMethod` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BillingPaymentMethod {
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `BillingAccount` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BillingAccount {
    /// address_city property.
    pub address_city: String,
    /// address_country property.
    pub address_country: String,
    /// address_country_name property.
    pub address_country_name: Option<String>,
    /// address_line1 property.
    pub address_line1: String,
    /// address_line2 property.
    pub address_line2: String,
    /// address_postal_code property.
    pub address_postal_code: String,
    /// address_state property.
    pub address_state: String,
    /// email property.
    pub email: String,
    /// name property.
    pub name: String,
    /// orb_portal_url property.
    pub orb_portal_url: Option<String>,
    /// payment_method property.
    pub payment_method: BillingPaymentMethod,
    /// payment_source property.
    pub payment_source: PaymentSource,
    /// plan_details property.
    pub plan_details: Option<PlanDetails>,
    /// quota_reset_at_last property.
    pub quota_reset_at_last: String,
    /// state property.
    pub state: BillingAccountState,
    /// subscription_type property.
    pub subscription_type: BillingSubscriptionType,
    /// tax_id property.
    pub tax_id: Option<String>,
    /// tax_id_type property.
    pub tax_id_type: Option<String>,
}

/// `PlanVersion` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PlanVersion {
    /// major property.
    pub major: i64,
    /// minor property.
    pub minor: i64,
}

/// `PlanDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PlanDetails {
    /// name property.
    pub name: String,
    /// version property.
    pub version: Option<PlanVersion>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET /users/me
// -----------------------------------------------------------------------------

/// GET /users/me.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_current_user_info_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_current_user_info_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CurrentUserInfoResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://console.neon.tech/api/v2/users/me",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CurrentUserInfoResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
