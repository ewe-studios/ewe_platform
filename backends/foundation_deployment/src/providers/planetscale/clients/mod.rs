//! Auto-generated API clients for planetscale.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `planetscale`

#![cfg(feature = "planetscale")]

pub mod types;

use crate::providers::planetscale::clients::types::*;
use crate::providers::planetscale::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET /organizations
/// List organizations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_organizations_execute()` to send, or `list_organizations` for simplest API.

pub fn list_organizations_builder<R>(
    client: &SimpleHttpClient<R>,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://api.planetscale.com/v1/organizations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations
/// List organizations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_organizations_execute()` or `list_organizations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_organizations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_organizations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations
/// List organizations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_organizations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_organizations_task()`.
/// For the simplest API, use `list_organizations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_organizations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_organizations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_organizations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_organizations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListOrganizationsArgs {
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations
/// List organizations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_organizations_builder()` + `list_organizations_execute()`.
/// For task-level control, use `list_organizations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_organizations(
    client: &SimpleHttpClient,
    args: &ListOrganizationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_organizations_builder(client, &args.page, &args.per_page)?;
    list_organizations_execute(builder)
}

/// GET /organizations/{organization}
/// Get an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organization_execute()` to send, or `get_organization` for simplest API.

pub fn get_organization_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}",
        organization,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}
/// Get an organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organization_execute()` or `get_organization`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organization_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}
/// Get an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organization_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organization_task()`.
/// For the simplest API, use `get_organization()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organization_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organization`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationArgs {
    /// Path parameter: organization
    pub organization: String,
}

/// GET /organizations/{organization}
/// Get an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organization_builder()` + `get_organization_execute()`.
/// For task-level control, use `get_organization_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organization(
    client: &SimpleHttpClient,
    args: &GetOrganizationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organization_builder(client, &args.organization)?;
    get_organization_execute(builder)
}

/// PATCH /organizations/{organization}
/// Update an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_organization_execute()` to send, or `update_organization` for simplest API.

pub fn update_organization_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}",
        organization,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}
/// Update an organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_organization_execute()` or `update_organization`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_organization_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}
/// Update an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_organization_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_organization_task()`.
/// For the simplest API, use `update_organization()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_organization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_organization_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_organization`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateOrganizationArgs {
    /// Path parameter: organization
    pub organization: String,
}

/// PATCH /organizations/{organization}
/// Update an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_organization_builder()` + `update_organization_execute()`.
/// For task-level control, use `update_organization_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_organization(
    client: &SimpleHttpClient,
    args: &UpdateOrganizationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = update_organization_builder(client, &args.organization)?;
    update_organization_execute(builder)
}

/// GET /organizations/{organization}/audit-log
/// List audit logs
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_audit_logs_execute()` to send, or `list_audit_logs` for simplest API.

pub fn list_audit_logs_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    starting_after: &Option<String>,
    ending_before: &Option<String>,
    limit: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/audit-log",
        organization,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = starting_after.as_ref() {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = ending_before.as_ref() {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = limit.as_ref() {
        query_parts.push(format!("limit={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/audit-log
/// List audit logs
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_audit_logs_execute()` or `list_audit_logs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_audit_logs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_audit_logs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/audit-log
/// List audit logs
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_audit_logs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_audit_logs_task()`.
/// For the simplest API, use `list_audit_logs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_audit_logs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_audit_logs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_audit_logs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_audit_logs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListAuditLogsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
}

/// GET /organizations/{organization}/audit-log
/// List audit logs
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_audit_logs_builder()` + `list_audit_logs_execute()`.
/// For task-level control, use `list_audit_logs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_audit_logs(
    client: &SimpleHttpClient,
    args: &ListAuditLogsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_audit_logs_builder(
        client,
        &args.organization,
        &args.starting_after,
        &args.ending_before,
        &args.limit,
    )?;
    list_audit_logs_execute(builder)
}

/// GET /organizations/{organization}/cluster-size-skus
/// List available cluster sizes
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_cluster_size_skus_execute()` to send, or `list_cluster_size_skus` for simplest API.

pub fn list_cluster_size_skus_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    engine: &Option<String>,
    rates: &Option<String>,
    region: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/cluster-size-skus",
        organization,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = engine.as_ref() {
        query_parts.push(format!("engine={}", val));
    }
    if let Some(val) = rates.as_ref() {
        query_parts.push(format!("rates={}", val));
    }
    if let Some(val) = region.as_ref() {
        query_parts.push(format!("region={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/cluster-size-skus
/// List available cluster sizes
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_cluster_size_skus_execute()` or `list_cluster_size_skus`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_cluster_size_skus_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_cluster_size_skus_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/cluster-size-skus
/// List available cluster sizes
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_cluster_size_skus_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_cluster_size_skus_task()`.
/// For the simplest API, use `list_cluster_size_skus()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_cluster_size_skus_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_cluster_size_skus_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_cluster_size_skus_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_cluster_size_skus`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListClusterSizeSkusArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Query parameter: engine
    pub engine: Option<String>,
    /// Query parameter: rates
    pub rates: Option<String>,
    /// Query parameter: region
    pub region: Option<String>,
}

/// GET /organizations/{organization}/cluster-size-skus
/// List available cluster sizes
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_cluster_size_skus_builder()` + `list_cluster_size_skus_execute()`.
/// For task-level control, use `list_cluster_size_skus_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_cluster_size_skus(
    client: &SimpleHttpClient,
    args: &ListClusterSizeSkusArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_cluster_size_skus_builder(
        client,
        &args.organization,
        &args.engine,
        &args.rates,
        &args.region,
    )?;
    list_cluster_size_skus_execute(builder)
}

/// GET /organizations/{organization}/databases
/// List databases
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_databases_execute()` to send, or `list_databases` for simplest API.

pub fn list_databases_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    q: &Option<String>,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases",
        organization,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = q.as_ref() {
        query_parts.push(format!("q={}", val));
    }
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases
/// List databases
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_databases_execute()` or `list_databases`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_databases_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_databases_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases
/// List databases
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_databases_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_databases_task()`.
/// For the simplest API, use `list_databases()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_databases_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_databases_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_databases_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_databases`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListDatabasesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Query parameter: q
    pub q: Option<String>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases
/// List databases
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_databases_builder()` + `list_databases_execute()`.
/// For task-level control, use `list_databases_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_databases(
    client: &SimpleHttpClient,
    args: &ListDatabasesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_databases_builder(
        client,
        &args.organization,
        &args.q,
        &args.page,
        &args.per_page,
    )?;
    list_databases_execute(builder)
}

/// POST /organizations/{organization}/databases
/// Create a database
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `create_database_execute()` to send, or `create_database` for simplest API.

pub fn create_database_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases",
        organization,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases
/// Create a database
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `create_database_execute()` or `create_database`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_database_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_database_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases
/// Create a database
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `create_database_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `create_database_task()`.
/// For the simplest API, use `create_database()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_database_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn create_database_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = create_database_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`create_database`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CreateDatabaseArgs {
    /// Path parameter: organization
    pub organization: String,
}

/// POST /organizations/{organization}/databases
/// Create a database
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `create_database_builder()` + `create_database_execute()`.
/// For task-level control, use `create_database_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_database(
    client: &SimpleHttpClient,
    args: &CreateDatabaseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = create_database_builder(client, &args.organization)?;
    create_database_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}
/// Get a database
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_database_execute()` to send, or `get_database` for simplest API.

pub fn get_database_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}",
        organization, database,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}
/// Get a database
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_database_execute()` or `get_database`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_database_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_database_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}
/// Get a database
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_database_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_database_task()`.
/// For the simplest API, use `get_database()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_database_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_database_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_database_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_database`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetDatabaseArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// GET /organizations/{organization}/databases/{database}
/// Get a database
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_database_builder()` + `get_database_execute()`.
/// For task-level control, use `get_database_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_database(
    client: &SimpleHttpClient,
    args: &GetDatabaseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_database_builder(client, &args.organization, &args.database)?;
    get_database_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}
/// Update database settings
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_database_settings_execute()` to send, or `update_database_settings` for simplest API.

pub fn update_database_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}",
        organization, database,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}
/// Update database settings
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_database_settings_execute()` or `update_database_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_database_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_database_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}
/// Update database settings
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_database_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_database_settings_task()`.
/// For the simplest API, use `update_database_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_database_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_database_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_database_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_database_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateDatabaseSettingsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// PATCH /organizations/{organization}/databases/{database}
/// Update database settings
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_database_settings_builder()` + `update_database_settings_execute()`.
/// For task-level control, use `update_database_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_database_settings(
    client: &SimpleHttpClient,
    args: &UpdateDatabaseSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = update_database_settings_builder(client, &args.organization, &args.database)?;
    update_database_settings_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}
/// Delete a database
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_database_execute()` to send, or `delete_database` for simplest API.

pub fn delete_database_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}",
        organization, database,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}
/// Delete a database
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_database_execute()` or `delete_database`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_database_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_database_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}
/// Delete a database
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_database_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_database_task()`.
/// For the simplest API, use `delete_database()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_database_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_database_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_database_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_database`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteDatabaseArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// DELETE /organizations/{organization}/databases/{database}
/// Delete a database
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_database_builder()` + `delete_database_execute()`.
/// For task-level control, use `delete_database_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_database(
    client: &SimpleHttpClient,
    args: &DeleteDatabaseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_database_builder(client, &args.organization, &args.database)?;
    delete_database_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/backup-policies
/// List backup policies
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_backup_policies_execute()` to send, or `list_backup_policies` for simplest API.

pub fn list_backup_policies_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/backup-policies",
        organization, database,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/backup-policies
/// List backup policies
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_backup_policies_execute()` or `list_backup_policies`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_backup_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_backup_policies_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/backup-policies
/// List backup policies
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_backup_policies_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_backup_policies_task()`.
/// For the simplest API, use `list_backup_policies()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_backup_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_backup_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_backup_policies_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_backup_policies`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListBackupPoliciesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/backup-policies
/// List backup policies
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_backup_policies_builder()` + `list_backup_policies_execute()`.
/// For task-level control, use `list_backup_policies_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_backup_policies(
    client: &SimpleHttpClient,
    args: &ListBackupPoliciesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_backup_policies_builder(
        client,
        &args.organization,
        &args.database,
        &args.page,
        &args.per_page,
    )?;
    list_backup_policies_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/backup-policies
/// Create a backup policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `create_backup_policy_execute()` to send, or `create_backup_policy` for simplest API.

pub fn create_backup_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/backup-policies",
        organization, database,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/backup-policies
/// Create a backup policy
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `create_backup_policy_execute()` or `create_backup_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_backup_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_backup_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/backup-policies
/// Create a backup policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `create_backup_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `create_backup_policy_task()`.
/// For the simplest API, use `create_backup_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_backup_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn create_backup_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = create_backup_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`create_backup_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CreateBackupPolicyArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// POST /organizations/{organization}/databases/{database}/backup-policies
/// Create a backup policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `create_backup_policy_builder()` + `create_backup_policy_execute()`.
/// For task-level control, use `create_backup_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_backup_policy(
    client: &SimpleHttpClient,
    args: &CreateBackupPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = create_backup_policy_builder(client, &args.organization, &args.database)?;
    create_backup_policy_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Get a backup policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_backup_policy_execute()` to send, or `get_backup_policy` for simplest API.

pub fn get_backup_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    id: &String,
    organization: &String,
    database: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/backup-policies/{}",
        organization, database, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Get a backup policy
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_backup_policy_execute()` or `get_backup_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_backup_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_backup_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Get a backup policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_backup_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_backup_policy_task()`.
/// For the simplest API, use `get_backup_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_backup_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_backup_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_backup_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_backup_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetBackupPolicyArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// GET /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Get a backup policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_backup_policy_builder()` + `get_backup_policy_execute()`.
/// For task-level control, use `get_backup_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_backup_policy(
    client: &SimpleHttpClient,
    args: &GetBackupPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_backup_policy_builder(client, &args.id, &args.organization, &args.database)?;
    get_backup_policy_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Update a backup policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_backup_policy_execute()` to send, or `update_backup_policy` for simplest API.

pub fn update_backup_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    id: &String,
    organization: &String,
    database: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/backup-policies/{}",
        organization, database, id,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Update a backup policy
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_backup_policy_execute()` or `update_backup_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_backup_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_backup_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Update a backup policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_backup_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_backup_policy_task()`.
/// For the simplest API, use `update_backup_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_backup_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_backup_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_backup_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_backup_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateBackupPolicyArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// PATCH /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Update a backup policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_backup_policy_builder()` + `update_backup_policy_execute()`.
/// For task-level control, use `update_backup_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_backup_policy(
    client: &SimpleHttpClient,
    args: &UpdateBackupPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        update_backup_policy_builder(client, &args.id, &args.organization, &args.database)?;
    update_backup_policy_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Delete a backup policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_backup_policy_execute()` to send, or `delete_backup_policy` for simplest API.

pub fn delete_backup_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    id: &String,
    organization: &String,
    database: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/backup-policies/{}",
        organization, database, id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Delete a backup policy
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_backup_policy_execute()` or `delete_backup_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_backup_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_backup_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Delete a backup policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_backup_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_backup_policy_task()`.
/// For the simplest API, use `delete_backup_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_backup_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_backup_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_backup_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_backup_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteBackupPolicyArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// DELETE /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Delete a backup policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_backup_policy_builder()` + `delete_backup_policy_execute()`.
/// For task-level control, use `delete_backup_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_backup_policy(
    client: &SimpleHttpClient,
    args: &DeleteBackupPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        delete_backup_policy_builder(client, &args.id, &args.organization, &args.database)?;
    delete_backup_policy_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches
/// List branches
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_branches_execute()` to send, or `list_branches` for simplest API.

pub fn list_branches_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    q: &Option<String>,
    production: &Option<String>,
    safe_migrations: &Option<String>,
    order: &Option<String>,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches",
        organization, database,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = q.as_ref() {
        query_parts.push(format!("q={}", val));
    }
    if let Some(val) = production.as_ref() {
        query_parts.push(format!("production={}", val));
    }
    if let Some(val) = safe_migrations.as_ref() {
        query_parts.push(format!("safe_migrations={}", val));
    }
    if let Some(val) = order.as_ref() {
        query_parts.push(format!("order={}", val));
    }
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches
/// List branches
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_branches_execute()` or `list_branches`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_branches_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_branches_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches
/// List branches
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_branches_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_branches_task()`.
/// For the simplest API, use `list_branches()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_branches_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_branches_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_branches_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_branches`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListBranchesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: q
    pub q: Option<String>,
    /// Query parameter: production
    pub production: Option<String>,
    /// Query parameter: safe_migrations
    pub safe_migrations: Option<String>,
    /// Query parameter: order
    pub order: Option<String>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches
/// List branches
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_branches_builder()` + `list_branches_execute()`.
/// For task-level control, use `list_branches_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_branches(
    client: &SimpleHttpClient,
    args: &ListBranchesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_branches_builder(
        client,
        &args.organization,
        &args.database,
        &args.q,
        &args.production,
        &args.safe_migrations,
        &args.order,
        &args.page,
        &args.per_page,
    )?;
    list_branches_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches
/// Create a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `create_branch_execute()` to send, or `create_branch` for simplest API.

pub fn create_branch_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches",
        organization, database,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches
/// Create a branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `create_branch_execute()` or `create_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches
/// Create a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `create_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `create_branch_task()`.
/// For the simplest API, use `create_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn create_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = create_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`create_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CreateBranchArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// POST /organizations/{organization}/databases/{database}/branches
/// Create a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `create_branch_builder()` + `create_branch_execute()`.
/// For task-level control, use `create_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_branch(
    client: &SimpleHttpClient,
    args: &CreateBranchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = create_branch_builder(client, &args.organization, &args.database)?;
    create_branch_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}
/// Get a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_branch_execute()` to send, or `get_branch` for simplest API.

pub fn get_branch_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}",
        organization, database, branch,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}
/// Get a branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_branch_execute()` or `get_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}
/// Get a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_branch_task()`.
/// For the simplest API, use `get_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetBranchArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}
/// Get a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_branch_builder()` + `get_branch_execute()`.
/// For task-level control, use `get_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_branch(
    client: &SimpleHttpClient,
    args: &GetBranchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_branch_builder(client, &args.organization, &args.database, &args.branch)?;
    get_branch_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}
/// Update a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_branch_execute()` to send, or `update_branch` for simplest API.

pub fn update_branch_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}",
        organization, database, branch,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}
/// Update a branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_branch_execute()` or `update_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}
/// Update a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_branch_task()`.
/// For the simplest API, use `update_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateBranchArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}
/// Update a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_branch_builder()` + `update_branch_execute()`.
/// For task-level control, use `update_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_branch(
    client: &SimpleHttpClient,
    args: &UpdateBranchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = update_branch_builder(client, &args.organization, &args.database, &args.branch)?;
    update_branch_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}
/// Delete a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_branch_execute()` to send, or `delete_branch` for simplest API.

pub fn delete_branch_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    delete_descendants: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}",
        organization, database, branch,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = delete_descendants.as_ref() {
        query_parts.push(format!("delete_descendants={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}
/// Delete a branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_branch_execute()` or `delete_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}
/// Delete a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_branch_task()`.
/// For the simplest API, use `delete_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteBranchArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: delete_descendants
    pub delete_descendants: Option<String>,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}
/// Delete a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_branch_builder()` + `delete_branch_execute()`.
/// For task-level control, use `delete_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_branch(
    client: &SimpleHttpClient,
    args: &DeleteBranchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_branch_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.delete_descendants,
    )?;
    delete_branch_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// List backups
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_backups_execute()` to send, or `list_backups` for simplest API.

pub fn list_backups_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    all: &Option<String>,
    state: &Option<String>,
    policy: &Option<String>,
    from: &Option<String>,
    to: &Option<String>,
    running_at: &Option<String>,
    production: &Option<String>,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/backups",
        organization, database, branch,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = all.as_ref() {
        query_parts.push(format!("all={}", val));
    }
    if let Some(val) = state.as_ref() {
        query_parts.push(format!("state={}", val));
    }
    if let Some(val) = policy.as_ref() {
        query_parts.push(format!("policy={}", val));
    }
    if let Some(val) = from.as_ref() {
        query_parts.push(format!("from={}", val));
    }
    if let Some(val) = to.as_ref() {
        query_parts.push(format!("to={}", val));
    }
    if let Some(val) = running_at.as_ref() {
        query_parts.push(format!("running_at={}", val));
    }
    if let Some(val) = production.as_ref() {
        query_parts.push(format!("production={}", val));
    }
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// List backups
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_backups_execute()` or `list_backups`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_backups_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_backups_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// List backups
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_backups_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_backups_task()`.
/// For the simplest API, use `list_backups()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_backups_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_backups_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_backups_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_backups`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListBackupsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: all
    pub all: Option<String>,
    /// Query parameter: state
    pub state: Option<String>,
    /// Query parameter: policy
    pub policy: Option<String>,
    /// Query parameter: from
    pub from: Option<String>,
    /// Query parameter: to
    pub to: Option<String>,
    /// Query parameter: running_at
    pub running_at: Option<String>,
    /// Query parameter: production
    pub production: Option<String>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// List backups
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_backups_builder()` + `list_backups_execute()`.
/// For task-level control, use `list_backups_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_backups(
    client: &SimpleHttpClient,
    args: &ListBackupsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_backups_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.all,
        &args.state,
        &args.policy,
        &args.from,
        &args.to,
        &args.running_at,
        &args.production,
        &args.page,
        &args.per_page,
    )?;
    list_backups_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// Create a backup
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `create_backup_execute()` to send, or `create_backup` for simplest API.

pub fn create_backup_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/backups",
        organization, database, branch,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// Create a backup
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `create_backup_execute()` or `create_backup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_backup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// Create a backup
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `create_backup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `create_backup_task()`.
/// For the simplest API, use `create_backup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn create_backup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = create_backup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`create_backup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CreateBackupArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// Create a backup
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `create_backup_builder()` + `create_backup_execute()`.
/// For task-level control, use `create_backup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_backup(
    client: &SimpleHttpClient,
    args: &CreateBackupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = create_backup_builder(client, &args.organization, &args.database, &args.branch)?;
    create_backup_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Get a backup
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_backup_execute()` to send, or `get_backup` for simplest API.

pub fn get_backup_builder<R>(
    client: &SimpleHttpClient<R>,
    id: &String,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/backups/{}",
        organization, database, branch, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Get a backup
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_backup_execute()` or `get_backup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_backup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Get a backup
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_backup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_backup_task()`.
/// For the simplest API, use `get_backup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_backup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_backup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_backup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetBackupArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Get a backup
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_backup_builder()` + `get_backup_execute()`.
/// For task-level control, use `get_backup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_backup(
    client: &SimpleHttpClient,
    args: &GetBackupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_backup_builder(
        client,
        &args.id,
        &args.organization,
        &args.database,
        &args.branch,
    )?;
    get_backup_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Update a backup
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_backup_execute()` to send, or `update_backup` for simplest API.

pub fn update_backup_builder<R>(
    client: &SimpleHttpClient<R>,
    id: &String,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/backups/{}",
        organization, database, branch, id,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Update a backup
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_backup_execute()` or `update_backup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_backup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Update a backup
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_backup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_backup_task()`.
/// For the simplest API, use `update_backup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_backup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_backup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_backup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateBackupArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Update a backup
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_backup_builder()` + `update_backup_execute()`.
/// For task-level control, use `update_backup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_backup(
    client: &SimpleHttpClient,
    args: &UpdateBackupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = update_backup_builder(
        client,
        &args.id,
        &args.organization,
        &args.database,
        &args.branch,
    )?;
    update_backup_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Delete a backup
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_backup_execute()` to send, or `delete_backup` for simplest API.

pub fn delete_backup_builder<R>(
    client: &SimpleHttpClient<R>,
    id: &String,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/backups/{}",
        organization, database, branch, id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Delete a backup
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_backup_execute()` or `delete_backup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_backup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Delete a backup
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_backup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_backup_task()`.
/// For the simplest API, use `delete_backup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_backup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_backup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_backup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteBackupArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Delete a backup
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_backup_builder()` + `delete_backup_execute()`.
/// For task-level control, use `delete_backup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_backup(
    client: &SimpleHttpClient,
    args: &DeleteBackupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_backup_builder(
        client,
        &args.id,
        &args.organization,
        &args.database,
        &args.branch,
    )?;
    delete_backup_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncer-resizes
/// Get bouncer resize requests
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_branch_bouncer_resize_requests_execute()` to send, or `list_branch_bouncer_resize_requests` for simplest API.

pub fn list_branch_bouncer_resize_requests_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/bouncer-resizes",
        organization, database, branch,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncer-resizes
/// Get bouncer resize requests
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_branch_bouncer_resize_requests_execute()` or `list_branch_bouncer_resize_requests`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_branch_bouncer_resize_requests_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_branch_bouncer_resize_requests_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncer-resizes
/// Get bouncer resize requests
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_branch_bouncer_resize_requests_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_branch_bouncer_resize_requests_task()`.
/// For the simplest API, use `list_branch_bouncer_resize_requests()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_branch_bouncer_resize_requests_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_branch_bouncer_resize_requests_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_branch_bouncer_resize_requests_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_branch_bouncer_resize_requests`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListBranchBouncerResizeRequestsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncer-resizes
/// Get bouncer resize requests
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_branch_bouncer_resize_requests_builder()` + `list_branch_bouncer_resize_requests_execute()`.
/// For task-level control, use `list_branch_bouncer_resize_requests_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_branch_bouncer_resize_requests(
    client: &SimpleHttpClient,
    args: &ListBranchBouncerResizeRequestsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_branch_bouncer_resize_requests_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.page,
        &args.per_page,
    )?;
    list_branch_bouncer_resize_requests_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// List bouncers
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_bouncers_execute()` to send, or `list_bouncers` for simplest API.

pub fn list_bouncers_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/bouncers",
        organization, database, branch,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// List bouncers
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_bouncers_execute()` or `list_bouncers`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_bouncers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_bouncers_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// List bouncers
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_bouncers_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_bouncers_task()`.
/// For the simplest API, use `list_bouncers()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_bouncers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_bouncers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_bouncers_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_bouncers`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListBouncersArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// List bouncers
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_bouncers_builder()` + `list_bouncers_execute()`.
/// For task-level control, use `list_bouncers_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_bouncers(
    client: &SimpleHttpClient,
    args: &ListBouncersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_bouncers_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.page,
        &args.per_page,
    )?;
    list_bouncers_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// Create a bouncer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `create_bouncer_execute()` to send, or `create_bouncer` for simplest API.

pub fn create_bouncer_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/bouncers",
        organization, database, branch,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// Create a bouncer
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `create_bouncer_execute()` or `create_bouncer`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_bouncer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_bouncer_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// Create a bouncer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `create_bouncer_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `create_bouncer_task()`.
/// For the simplest API, use `create_bouncer()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_bouncer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn create_bouncer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = create_bouncer_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`create_bouncer`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CreateBouncerArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// Create a bouncer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `create_bouncer_builder()` + `create_bouncer_execute()`.
/// For task-level control, use `create_bouncer_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_bouncer(
    client: &SimpleHttpClient,
    args: &CreateBouncerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = create_bouncer_builder(client, &args.organization, &args.database, &args.branch)?;
    create_bouncer_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Get a bouncer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_bouncer_execute()` to send, or `get_bouncer` for simplest API.

pub fn get_bouncer_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    bouncer: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/bouncers/{}",
        organization, database, branch, bouncer,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Get a bouncer
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_bouncer_execute()` or `get_bouncer`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_bouncer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_bouncer_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Get a bouncer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_bouncer_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_bouncer_task()`.
/// For the simplest API, use `get_bouncer()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_bouncer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_bouncer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_bouncer_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_bouncer`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetBouncerArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: bouncer
    pub bouncer: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Get a bouncer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_bouncer_builder()` + `get_bouncer_execute()`.
/// For task-level control, use `get_bouncer_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_bouncer(
    client: &SimpleHttpClient,
    args: &GetBouncerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_bouncer_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.bouncer,
    )?;
    get_bouncer_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Delete a bouncer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_bouncer_execute()` to send, or `delete_bouncer` for simplest API.

pub fn delete_bouncer_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    bouncer: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/bouncers/{}",
        organization, database, branch, bouncer,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Delete a bouncer
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_bouncer_execute()` or `delete_bouncer`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_bouncer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_bouncer_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Delete a bouncer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_bouncer_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_bouncer_task()`.
/// For the simplest API, use `delete_bouncer()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_bouncer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_bouncer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_bouncer_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_bouncer`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteBouncerArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: bouncer
    pub bouncer: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Delete a bouncer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_bouncer_builder()` + `delete_bouncer_execute()`.
/// For task-level control, use `delete_bouncer_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_bouncer(
    client: &SimpleHttpClient,
    args: &DeleteBouncerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_bouncer_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.bouncer,
    )?;
    delete_bouncer_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Get bouncer resize requests
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_bouncer_resize_requests_execute()` to send, or `list_bouncer_resize_requests` for simplest API.

pub fn list_bouncer_resize_requests_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    bouncer: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/bouncers/{}/resizes",
        organization,
        database,
        branch,
        bouncer,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Get bouncer resize requests
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_bouncer_resize_requests_execute()` or `list_bouncer_resize_requests`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_bouncer_resize_requests_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_bouncer_resize_requests_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Get bouncer resize requests
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_bouncer_resize_requests_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_bouncer_resize_requests_task()`.
/// For the simplest API, use `list_bouncer_resize_requests()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_bouncer_resize_requests_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_bouncer_resize_requests_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_bouncer_resize_requests_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_bouncer_resize_requests`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListBouncerResizeRequestsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: bouncer
    pub bouncer: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Get bouncer resize requests
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_bouncer_resize_requests_builder()` + `list_bouncer_resize_requests_execute()`.
/// For task-level control, use `list_bouncer_resize_requests_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_bouncer_resize_requests(
    client: &SimpleHttpClient,
    args: &ListBouncerResizeRequestsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_bouncer_resize_requests_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.bouncer,
        &args.page,
        &args.per_page,
    )?;
    list_bouncer_resize_requests_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Upsert a bouncer resize request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_bouncer_resize_request_execute()` to send, or `update_bouncer_resize_request` for simplest API.

pub fn update_bouncer_resize_request_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    bouncer: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/bouncers/{}/resizes",
        organization,
        database,
        branch,
        bouncer,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Upsert a bouncer resize request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_bouncer_resize_request_execute()` or `update_bouncer_resize_request`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_bouncer_resize_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_bouncer_resize_request_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Upsert a bouncer resize request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_bouncer_resize_request_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_bouncer_resize_request_task()`.
/// For the simplest API, use `update_bouncer_resize_request()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_bouncer_resize_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_bouncer_resize_request_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_bouncer_resize_request_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_bouncer_resize_request`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateBouncerResizeRequestArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: bouncer
    pub bouncer: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Upsert a bouncer resize request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_bouncer_resize_request_builder()` + `update_bouncer_resize_request_execute()`.
/// For task-level control, use `update_bouncer_resize_request_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_bouncer_resize_request(
    client: &SimpleHttpClient,
    args: &UpdateBouncerResizeRequestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = update_bouncer_resize_request_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.bouncer,
    )?;
    update_bouncer_resize_request_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Cancel a resize request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cancel_bouncer_resize_request_execute()` to send, or `cancel_bouncer_resize_request` for simplest API.

pub fn cancel_bouncer_resize_request_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    bouncer: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/bouncers/{}/resizes",
        organization,
        database,
        branch,
        bouncer,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Cancel a resize request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cancel_bouncer_resize_request_execute()` or `cancel_bouncer_resize_request`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cancel_bouncer_resize_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cancel_bouncer_resize_request_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Cancel a resize request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cancel_bouncer_resize_request_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cancel_bouncer_resize_request_task()`.
/// For the simplest API, use `cancel_bouncer_resize_request()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cancel_bouncer_resize_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cancel_bouncer_resize_request_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cancel_bouncer_resize_request_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cancel_bouncer_resize_request`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CancelBouncerResizeRequestArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: bouncer
    pub bouncer: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Cancel a resize request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cancel_bouncer_resize_request_builder()` + `cancel_bouncer_resize_request_execute()`.
/// For task-level control, use `cancel_bouncer_resize_request_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cancel_bouncer_resize_request(
    client: &SimpleHttpClient,
    args: &CancelBouncerResizeRequestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cancel_bouncer_resize_request_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.bouncer,
    )?;
    cancel_bouncer_resize_request_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Get branch change requests
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_branch_change_requests_execute()` to send, or `list_branch_change_requests` for simplest API.

pub fn list_branch_change_requests_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/changes",
        organization, database, branch,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Get branch change requests
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_branch_change_requests_execute()` or `list_branch_change_requests`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_branch_change_requests_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_branch_change_requests_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Get branch change requests
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_branch_change_requests_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_branch_change_requests_task()`.
/// For the simplest API, use `list_branch_change_requests()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_branch_change_requests_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_branch_change_requests_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_branch_change_requests_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_branch_change_requests`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListBranchChangeRequestsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Get branch change requests
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_branch_change_requests_builder()` + `list_branch_change_requests_execute()`.
/// For task-level control, use `list_branch_change_requests_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_branch_change_requests(
    client: &SimpleHttpClient,
    args: &ListBranchChangeRequestsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_branch_change_requests_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.page,
        &args.per_page,
    )?;
    list_branch_change_requests_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Upsert a change request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_branch_change_request_execute()` to send, or `update_branch_change_request` for simplest API.

pub fn update_branch_change_request_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/changes",
        organization, database, branch,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Upsert a change request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_branch_change_request_execute()` or `update_branch_change_request`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_branch_change_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_branch_change_request_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Upsert a change request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_branch_change_request_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_branch_change_request_task()`.
/// For the simplest API, use `update_branch_change_request()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_branch_change_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_branch_change_request_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_branch_change_request_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_branch_change_request`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateBranchChangeRequestArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Upsert a change request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_branch_change_request_builder()` + `update_branch_change_request_execute()`.
/// For task-level control, use `update_branch_change_request_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_branch_change_request(
    client: &SimpleHttpClient,
    args: &UpdateBranchChangeRequestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = update_branch_change_request_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
    )?;
    update_branch_change_request_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes/{id}
/// Get a branch change request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_branch_change_request_execute()` to send, or `get_branch_change_request` for simplest API.

pub fn get_branch_change_request_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/changes/{}",
        organization, database, branch, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes/{id}
/// Get a branch change request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_branch_change_request_execute()` or `get_branch_change_request`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_branch_change_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_branch_change_request_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes/{id}
/// Get a branch change request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_branch_change_request_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_branch_change_request_task()`.
/// For the simplest API, use `get_branch_change_request()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_branch_change_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_branch_change_request_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_branch_change_request_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_branch_change_request`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetBranchChangeRequestArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes/{id}
/// Get a branch change request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_branch_change_request_builder()` + `get_branch_change_request_execute()`.
/// For task-level control, use `get_branch_change_request_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_branch_change_request(
    client: &SimpleHttpClient,
    args: &GetBranchChangeRequestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_branch_change_request_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.id,
    )?;
    get_branch_change_request_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/cluster
/// Change a branch cluster configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_branch_cluster_config_execute()` to send, or `update_branch_cluster_config` for simplest API.

pub fn update_branch_cluster_config_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/cluster",
        organization, database, branch,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/cluster
/// Change a branch cluster configuration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_branch_cluster_config_execute()` or `update_branch_cluster_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_branch_cluster_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_branch_cluster_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/cluster
/// Change a branch cluster configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_branch_cluster_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_branch_cluster_config_task()`.
/// For the simplest API, use `update_branch_cluster_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_branch_cluster_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_branch_cluster_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_branch_cluster_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_branch_cluster_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateBranchClusterConfigArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/cluster
/// Change a branch cluster configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_branch_cluster_config_builder()` + `update_branch_cluster_config_execute()`.
/// For task-level control, use `update_branch_cluster_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_branch_cluster_config(
    client: &SimpleHttpClient,
    args: &UpdateBranchClusterConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = update_branch_cluster_config_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
    )?;
    update_branch_cluster_config_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/demote
/// Demote a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `demote_branch_execute()` to send, or `demote_branch` for simplest API.

pub fn demote_branch_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/demote",
        organization, database, branch,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/demote
/// Demote a branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `demote_branch_execute()` or `demote_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `demote_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn demote_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/demote
/// Demote a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `demote_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `demote_branch_task()`.
/// For the simplest API, use `demote_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `demote_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn demote_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = demote_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`demote_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DemoteBranchArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/demote
/// Demote a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `demote_branch_builder()` + `demote_branch_execute()`.
/// For task-level control, use `demote_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn demote_branch(
    client: &SimpleHttpClient,
    args: &DemoteBranchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = demote_branch_builder(client, &args.organization, &args.database, &args.branch)?;
    demote_branch_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/extensions
/// List cluster extensions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_extensions_execute()` to send, or `list_extensions` for simplest API.

pub fn list_extensions_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/extensions",
        organization, database, branch,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/extensions
/// List cluster extensions
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_extensions_execute()` or `list_extensions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_extensions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_extensions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/extensions
/// List cluster extensions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_extensions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_extensions_task()`.
/// For the simplest API, use `list_extensions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_extensions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_extensions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_extensions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_extensions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListExtensionsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/extensions
/// List cluster extensions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_extensions_builder()` + `list_extensions_execute()`.
/// For task-level control, use `list_extensions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_extensions(
    client: &SimpleHttpClient,
    args: &ListExtensionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        list_extensions_builder(client, &args.organization, &args.database, &args.branch)?;
    list_extensions_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Get keyspaces
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_keyspaces_execute()` to send, or `list_keyspaces` for simplest API.

pub fn list_keyspaces_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/keyspaces",
        organization, database, branch,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Get keyspaces
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_keyspaces_execute()` or `list_keyspaces`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_keyspaces_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_keyspaces_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Get keyspaces
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_keyspaces_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_keyspaces_task()`.
/// For the simplest API, use `list_keyspaces()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_keyspaces_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_keyspaces_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_keyspaces_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_keyspaces`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListKeyspacesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Get keyspaces
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_keyspaces_builder()` + `list_keyspaces_execute()`.
/// For task-level control, use `list_keyspaces_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_keyspaces(
    client: &SimpleHttpClient,
    args: &ListKeyspacesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_keyspaces_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.page,
        &args.per_page,
    )?;
    list_keyspaces_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Create a keyspace
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `create_keyspace_execute()` to send, or `create_keyspace` for simplest API.

pub fn create_keyspace_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/keyspaces",
        organization, database, branch,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Create a keyspace
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `create_keyspace_execute()` or `create_keyspace`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_keyspace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_keyspace_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Create a keyspace
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `create_keyspace_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `create_keyspace_task()`.
/// For the simplest API, use `create_keyspace()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_keyspace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn create_keyspace_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = create_keyspace_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`create_keyspace`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CreateKeyspaceArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Create a keyspace
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `create_keyspace_builder()` + `create_keyspace_execute()`.
/// For task-level control, use `create_keyspace_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_keyspace(
    client: &SimpleHttpClient,
    args: &CreateKeyspaceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        create_keyspace_builder(client, &args.organization, &args.database, &args.branch)?;
    create_keyspace_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Get a keyspace
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_keyspace_execute()` to send, or `get_keyspace` for simplest API.

pub fn get_keyspace_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    keyspace: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/keyspaces/{}",
        organization, database, branch, keyspace,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Get a keyspace
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_keyspace_execute()` or `get_keyspace`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_keyspace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_keyspace_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Get a keyspace
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_keyspace_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_keyspace_task()`.
/// For the simplest API, use `get_keyspace()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_keyspace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_keyspace_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_keyspace_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_keyspace`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetKeyspaceArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: keyspace
    pub keyspace: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Get a keyspace
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_keyspace_builder()` + `get_keyspace_execute()`.
/// For task-level control, use `get_keyspace_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_keyspace(
    client: &SimpleHttpClient,
    args: &GetKeyspaceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_keyspace_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.keyspace,
    )?;
    get_keyspace_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Configure keyspace settings
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_keyspace_execute()` to send, or `update_keyspace` for simplest API.

pub fn update_keyspace_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    keyspace: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/keyspaces/{}",
        organization, database, branch, keyspace,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Configure keyspace settings
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_keyspace_execute()` or `update_keyspace`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_keyspace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_keyspace_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Configure keyspace settings
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_keyspace_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_keyspace_task()`.
/// For the simplest API, use `update_keyspace()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_keyspace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_keyspace_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_keyspace_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_keyspace`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateKeyspaceArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: keyspace
    pub keyspace: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Configure keyspace settings
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_keyspace_builder()` + `update_keyspace_execute()`.
/// For task-level control, use `update_keyspace_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_keyspace(
    client: &SimpleHttpClient,
    args: &UpdateKeyspaceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = update_keyspace_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.keyspace,
    )?;
    update_keyspace_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Delete a keyspace
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_keyspace_execute()` to send, or `delete_keyspace` for simplest API.

pub fn delete_keyspace_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    keyspace: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/keyspaces/{}",
        organization, database, branch, keyspace,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Delete a keyspace
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_keyspace_execute()` or `delete_keyspace`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_keyspace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_keyspace_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Delete a keyspace
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_keyspace_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_keyspace_task()`.
/// For the simplest API, use `delete_keyspace()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_keyspace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_keyspace_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_keyspace_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_keyspace`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteKeyspaceArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: keyspace
    pub keyspace: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Delete a keyspace
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_keyspace_builder()` + `delete_keyspace_execute()`.
/// For task-level control, use `delete_keyspace_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_keyspace(
    client: &SimpleHttpClient,
    args: &DeleteKeyspaceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_keyspace_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.keyspace,
    )?;
    delete_keyspace_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/rollout-status
/// Get keyspace rollout status
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_keyspace_rollout_status_execute()` to send, or `get_keyspace_rollout_status` for simplest API.

pub fn get_keyspace_rollout_status_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    keyspace: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/keyspaces/{}/rollout-status",
        organization,
        database,
        branch,
        keyspace,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/rollout-status
/// Get keyspace rollout status
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_keyspace_rollout_status_execute()` or `get_keyspace_rollout_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_keyspace_rollout_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_keyspace_rollout_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/rollout-status
/// Get keyspace rollout status
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_keyspace_rollout_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_keyspace_rollout_status_task()`.
/// For the simplest API, use `get_keyspace_rollout_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_keyspace_rollout_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_keyspace_rollout_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_keyspace_rollout_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_keyspace_rollout_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetKeyspaceRolloutStatusArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: keyspace
    pub keyspace: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/rollout-status
/// Get keyspace rollout status
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_keyspace_rollout_status_builder()` + `get_keyspace_rollout_status_execute()`.
/// For task-level control, use `get_keyspace_rollout_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_keyspace_rollout_status(
    client: &SimpleHttpClient,
    args: &GetKeyspaceRolloutStatusArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_keyspace_rollout_status_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.keyspace,
    )?;
    get_keyspace_rollout_status_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Get the VSchema for the keyspace
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_keyspace_vschema_execute()` to send, or `get_keyspace_vschema` for simplest API.

pub fn get_keyspace_vschema_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    keyspace: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/keyspaces/{}/vschema",
        organization,
        database,
        branch,
        keyspace,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Get the VSchema for the keyspace
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_keyspace_vschema_execute()` or `get_keyspace_vschema`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_keyspace_vschema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_keyspace_vschema_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Get the VSchema for the keyspace
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_keyspace_vschema_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_keyspace_vschema_task()`.
/// For the simplest API, use `get_keyspace_vschema()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_keyspace_vschema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_keyspace_vschema_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_keyspace_vschema_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_keyspace_vschema`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetKeyspaceVschemaArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: keyspace
    pub keyspace: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Get the VSchema for the keyspace
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_keyspace_vschema_builder()` + `get_keyspace_vschema_execute()`.
/// For task-level control, use `get_keyspace_vschema_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_keyspace_vschema(
    client: &SimpleHttpClient,
    args: &GetKeyspaceVschemaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_keyspace_vschema_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.keyspace,
    )?;
    get_keyspace_vschema_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Update the VSchema for the keyspace
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_keyspace_vschema_execute()` to send, or `update_keyspace_vschema` for simplest API.

pub fn update_keyspace_vschema_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    keyspace: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/keyspaces/{}/vschema",
        organization,
        database,
        branch,
        keyspace,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Update the VSchema for the keyspace
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_keyspace_vschema_execute()` or `update_keyspace_vschema`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_keyspace_vschema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_keyspace_vschema_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Update the VSchema for the keyspace
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_keyspace_vschema_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_keyspace_vschema_task()`.
/// For the simplest API, use `update_keyspace_vschema()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_keyspace_vschema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_keyspace_vschema_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_keyspace_vschema_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_keyspace_vschema`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateKeyspaceVschemaArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: keyspace
    pub keyspace: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Update the VSchema for the keyspace
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_keyspace_vschema_builder()` + `update_keyspace_vschema_execute()`.
/// For task-level control, use `update_keyspace_vschema_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_keyspace_vschema(
    client: &SimpleHttpClient,
    args: &UpdateKeyspaceVschemaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = update_keyspace_vschema_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.keyspace,
    )?;
    update_keyspace_vschema_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/parameters
/// List cluster parameters
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_parameters_execute()` to send, or `list_parameters` for simplest API.

pub fn list_parameters_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/parameters",
        organization, database, branch,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/parameters
/// List cluster parameters
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_parameters_execute()` or `list_parameters`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_parameters_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_parameters_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/parameters
/// List cluster parameters
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_parameters_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_parameters_task()`.
/// For the simplest API, use `list_parameters()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_parameters_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_parameters_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_parameters_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_parameters`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListParametersArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/parameters
/// List cluster parameters
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_parameters_builder()` + `list_parameters_execute()`.
/// For task-level control, use `list_parameters_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_parameters(
    client: &SimpleHttpClient,
    args: &ListParametersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        list_parameters_builder(client, &args.organization, &args.database, &args.branch)?;
    list_parameters_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// List passwords
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_passwords_execute()` to send, or `list_passwords` for simplest API.

pub fn list_passwords_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    read_only_region_id: &Option<String>,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/passwords",
        organization, database, branch,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = read_only_region_id.as_ref() {
        query_parts.push(format!("read_only_region_id={}", val));
    }
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// List passwords
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_passwords_execute()` or `list_passwords`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_passwords_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_passwords_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// List passwords
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_passwords_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_passwords_task()`.
/// For the simplest API, use `list_passwords()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_passwords_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_passwords_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_passwords_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_passwords`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListPasswordsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: read_only_region_id
    pub read_only_region_id: Option<String>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// List passwords
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_passwords_builder()` + `list_passwords_execute()`.
/// For task-level control, use `list_passwords_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_passwords(
    client: &SimpleHttpClient,
    args: &ListPasswordsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_passwords_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.read_only_region_id,
        &args.page,
        &args.per_page,
    )?;
    list_passwords_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// Create a password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `create_password_execute()` to send, or `create_password` for simplest API.

pub fn create_password_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/passwords",
        organization, database, branch,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// Create a password
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `create_password_execute()` or `create_password`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_password_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// Create a password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `create_password_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `create_password_task()`.
/// For the simplest API, use `create_password()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn create_password_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = create_password_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`create_password`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CreatePasswordArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// Create a password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `create_password_builder()` + `create_password_execute()`.
/// For task-level control, use `create_password_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_password(
    client: &SimpleHttpClient,
    args: &CreatePasswordArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        create_password_builder(client, &args.organization, &args.database, &args.branch)?;
    create_password_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Get a password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_password_execute()` to send, or `get_password` for simplest API.

pub fn get_password_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/passwords/{}",
        organization, database, branch, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Get a password
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_password_execute()` or `get_password`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_password_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Get a password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_password_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_password_task()`.
/// For the simplest API, use `get_password()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_password_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_password_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_password`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetPasswordArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Get a password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_password_builder()` + `get_password_execute()`.
/// For task-level control, use `get_password_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_password(
    client: &SimpleHttpClient,
    args: &GetPasswordArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_password_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.id,
    )?;
    get_password_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Update a password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_password_execute()` to send, or `update_password` for simplest API.

pub fn update_password_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/passwords/{}",
        organization, database, branch, id,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Update a password
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_password_execute()` or `update_password`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_password_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Update a password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_password_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_password_task()`.
/// For the simplest API, use `update_password()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_password_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_password_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_password`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdatePasswordArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Update a password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_password_builder()` + `update_password_execute()`.
/// For task-level control, use `update_password_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_password(
    client: &SimpleHttpClient,
    args: &UpdatePasswordArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = update_password_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.id,
    )?;
    update_password_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Delete a password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_password_execute()` to send, or `delete_password` for simplest API.

pub fn delete_password_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/passwords/{}",
        organization, database, branch, id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Delete a password
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_password_execute()` or `delete_password`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_password_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Delete a password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_password_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_password_task()`.
/// For the simplest API, use `delete_password()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_password_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_password_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_password`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeletePasswordArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Delete a password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_password_builder()` + `delete_password_execute()`.
/// For task-level control, use `delete_password_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_password(
    client: &SimpleHttpClient,
    args: &DeletePasswordArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_password_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.id,
    )?;
    delete_password_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}/renew
/// Renew a password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `renew_password_execute()` to send, or `renew_password` for simplest API.

pub fn renew_password_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/passwords/{}/renew",
        organization,
        database,
        branch,
        id,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}/renew
/// Renew a password
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `renew_password_execute()` or `renew_password`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `renew_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn renew_password_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}/renew
/// Renew a password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `renew_password_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `renew_password_task()`.
/// For the simplest API, use `renew_password()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `renew_password_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn renew_password_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = renew_password_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`renew_password`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RenewPasswordArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}/renew
/// Renew a password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `renew_password_builder()` + `renew_password_execute()`.
/// For task-level control, use `renew_password_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn renew_password(
    client: &SimpleHttpClient,
    args: &RenewPasswordArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = renew_password_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.id,
    )?;
    renew_password_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/promote
/// Promote a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `promote_branch_execute()` to send, or `promote_branch` for simplest API.

pub fn promote_branch_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/promote",
        organization, database, branch,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/promote
/// Promote a branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `promote_branch_execute()` or `promote_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `promote_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn promote_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/promote
/// Promote a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `promote_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `promote_branch_task()`.
/// For the simplest API, use `promote_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `promote_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn promote_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = promote_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`promote_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PromoteBranchArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/promote
/// Promote a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `promote_branch_builder()` + `promote_branch_execute()`.
/// For task-level control, use `promote_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn promote_branch(
    client: &SimpleHttpClient,
    args: &PromoteBranchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = promote_branch_builder(client, &args.organization, &args.database, &args.branch)?;
    promote_branch_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// List generated query patterns reports
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_generated_query_patterns_reports_execute()` to send, or `list_generated_query_patterns_reports` for simplest API.

pub fn list_generated_query_patterns_reports_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    starting_after: &Option<String>,
    ending_before: &Option<String>,
    limit: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/query-patterns",
        organization, database, branch,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = starting_after.as_ref() {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = ending_before.as_ref() {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = limit.as_ref() {
        query_parts.push(format!("limit={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// List generated query patterns reports
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_generated_query_patterns_reports_execute()` or `list_generated_query_patterns_reports`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_generated_query_patterns_reports_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_generated_query_patterns_reports_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// List generated query patterns reports
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_generated_query_patterns_reports_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_generated_query_patterns_reports_task()`.
/// For the simplest API, use `list_generated_query_patterns_reports()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_generated_query_patterns_reports_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_generated_query_patterns_reports_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_generated_query_patterns_reports_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_generated_query_patterns_reports`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListGeneratedQueryPatternsReportsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// List generated query patterns reports
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_generated_query_patterns_reports_builder()` + `list_generated_query_patterns_reports_execute()`.
/// For task-level control, use `list_generated_query_patterns_reports_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_generated_query_patterns_reports(
    client: &SimpleHttpClient,
    args: &ListGeneratedQueryPatternsReportsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_generated_query_patterns_reports_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.starting_after,
        &args.ending_before,
        &args.limit,
    )?;
    list_generated_query_patterns_reports_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// Create a new query patterns report
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `create_query_patterns_report_execute()` to send, or `create_query_patterns_report` for simplest API.

pub fn create_query_patterns_report_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/query-patterns",
        organization, database, branch,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// Create a new query patterns report
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `create_query_patterns_report_execute()` or `create_query_patterns_report`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_query_patterns_report_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_query_patterns_report_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// Create a new query patterns report
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `create_query_patterns_report_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `create_query_patterns_report_task()`.
/// For the simplest API, use `create_query_patterns_report()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_query_patterns_report_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn create_query_patterns_report_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = create_query_patterns_report_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`create_query_patterns_report`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CreateQueryPatternsReportArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// Create a new query patterns report
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `create_query_patterns_report_builder()` + `create_query_patterns_report_execute()`.
/// For task-level control, use `create_query_patterns_report_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_query_patterns_report(
    client: &SimpleHttpClient,
    args: &CreateQueryPatternsReportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = create_query_patterns_report_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
    )?;
    create_query_patterns_report_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Show the status of a query patterns report
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_query_patterns_report_status_execute()` to send, or `get_query_patterns_report_status` for simplest API.

pub fn get_query_patterns_report_status_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/query-patterns/{}",
        organization,
        database,
        branch,
        id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Show the status of a query patterns report
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_query_patterns_report_status_execute()` or `get_query_patterns_report_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_query_patterns_report_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_query_patterns_report_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Show the status of a query patterns report
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_query_patterns_report_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_query_patterns_report_status_task()`.
/// For the simplest API, use `get_query_patterns_report_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_query_patterns_report_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_query_patterns_report_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_query_patterns_report_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_query_patterns_report_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetQueryPatternsReportStatusArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Show the status of a query patterns report
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_query_patterns_report_status_builder()` + `get_query_patterns_report_status_execute()`.
/// For task-level control, use `get_query_patterns_report_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_query_patterns_report_status(
    client: &SimpleHttpClient,
    args: &GetQueryPatternsReportStatusArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_query_patterns_report_status_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.id,
    )?;
    get_query_patterns_report_status_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Delete a query patterns report
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_query_patterns_report_execute()` to send, or `delete_query_patterns_report` for simplest API.

pub fn delete_query_patterns_report_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/query-patterns/{}",
        organization,
        database,
        branch,
        id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Delete a query patterns report
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_query_patterns_report_execute()` or `delete_query_patterns_report`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_query_patterns_report_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_query_patterns_report_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Delete a query patterns report
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_query_patterns_report_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_query_patterns_report_task()`.
/// For the simplest API, use `delete_query_patterns_report()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_query_patterns_report_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_query_patterns_report_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_query_patterns_report_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_query_patterns_report`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteQueryPatternsReportArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Delete a query patterns report
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_query_patterns_report_builder()` + `delete_query_patterns_report_execute()`.
/// For task-level control, use `delete_query_patterns_report_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_query_patterns_report(
    client: &SimpleHttpClient,
    args: &DeleteQueryPatternsReportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_query_patterns_report_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.id,
    )?;
    delete_query_patterns_report_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}/download
/// Download a finished query patterns report
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_query_patterns_report_execute()` to send, or `get_query_patterns_report` for simplest API.

pub fn get_query_patterns_report_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/query-patterns/{}/download",
        organization,
        database,
        branch,
        id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}/download
/// Download a finished query patterns report
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_query_patterns_report_execute()` or `get_query_patterns_report`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_query_patterns_report_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_query_patterns_report_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}/download
/// Download a finished query patterns report
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_query_patterns_report_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_query_patterns_report_task()`.
/// For the simplest API, use `get_query_patterns_report()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_query_patterns_report_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_query_patterns_report_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_query_patterns_report_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_query_patterns_report`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetQueryPatternsReportArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}/download
/// Download a finished query patterns report
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_query_patterns_report_builder()` + `get_query_patterns_report_execute()`.
/// For task-level control, use `get_query_patterns_report_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_query_patterns_report(
    client: &SimpleHttpClient,
    args: &GetQueryPatternsReportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_query_patterns_report_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.id,
    )?;
    get_query_patterns_report_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/resizes
/// Cancel a change request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cancel_branch_change_request_execute()` to send, or `cancel_branch_change_request` for simplest API.

pub fn cancel_branch_change_request_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/resizes",
        organization, database, branch,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/resizes
/// Cancel a change request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cancel_branch_change_request_execute()` or `cancel_branch_change_request`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cancel_branch_change_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cancel_branch_change_request_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/resizes
/// Cancel a change request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cancel_branch_change_request_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cancel_branch_change_request_task()`.
/// For the simplest API, use `cancel_branch_change_request()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cancel_branch_change_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cancel_branch_change_request_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cancel_branch_change_request_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cancel_branch_change_request`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CancelBranchChangeRequestArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/resizes
/// Cancel a change request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cancel_branch_change_request_builder()` + `cancel_branch_change_request_execute()`.
/// For task-level control, use `cancel_branch_change_request_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cancel_branch_change_request(
    client: &SimpleHttpClient,
    args: &CancelBranchChangeRequestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cancel_branch_change_request_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
    )?;
    cancel_branch_change_request_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// List roles
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_roles_execute()` to send, or `list_roles` for simplest API.

pub fn list_roles_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles",
        organization, database, branch,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// List roles
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_roles_execute()` or `list_roles`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_roles_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_roles_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// List roles
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_roles_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_roles_task()`.
/// For the simplest API, use `list_roles()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_roles_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_roles_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_roles_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_roles`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListRolesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// List roles
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_roles_builder()` + `list_roles_execute()`.
/// For task-level control, use `list_roles_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_roles(
    client: &SimpleHttpClient,
    args: &ListRolesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_roles_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.page,
        &args.per_page,
    )?;
    list_roles_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// Create role credentials
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `create_role_execute()` to send, or `create_role` for simplest API.

pub fn create_role_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles",
        organization, database, branch,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// Create role credentials
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `create_role_execute()` or `create_role`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_role_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// Create role credentials
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `create_role_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `create_role_task()`.
/// For the simplest API, use `create_role()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn create_role_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = create_role_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`create_role`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CreateRoleArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// Create role credentials
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `create_role_builder()` + `create_role_execute()`.
/// For task-level control, use `create_role_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_role(
    client: &SimpleHttpClient,
    args: &CreateRoleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = create_role_builder(client, &args.organization, &args.database, &args.branch)?;
    create_role_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/default
/// Get the default postgres role
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_default_role_execute()` to send, or `get_default_role` for simplest API.

pub fn get_default_role_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles/default",
        organization, database, branch,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/default
/// Get the default postgres role
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_default_role_execute()` or `get_default_role`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_default_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_default_role_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/default
/// Get the default postgres role
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_default_role_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_default_role_task()`.
/// For the simplest API, use `get_default_role()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_default_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_default_role_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_default_role_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_default_role`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetDefaultRoleArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/default
/// Get the default postgres role
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_default_role_builder()` + `get_default_role_execute()`.
/// For task-level control, use `get_default_role_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_default_role(
    client: &SimpleHttpClient,
    args: &GetDefaultRoleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_default_role_builder(client, &args.organization, &args.database, &args.branch)?;
    get_default_role_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/reset-default
/// Reset default credentials
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reset_default_role_execute()` to send, or `reset_default_role` for simplest API.

pub fn reset_default_role_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles/reset-default",
        organization,
        database,
        branch,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/reset-default
/// Reset default credentials
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reset_default_role_execute()` or `reset_default_role`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reset_default_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reset_default_role_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/reset-default
/// Reset default credentials
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reset_default_role_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reset_default_role_task()`.
/// For the simplest API, use `reset_default_role()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reset_default_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reset_default_role_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = reset_default_role_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reset_default_role`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ResetDefaultRoleArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/reset-default
/// Reset default credentials
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reset_default_role_builder()` + `reset_default_role_execute()`.
/// For task-level control, use `reset_default_role_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reset_default_role(
    client: &SimpleHttpClient,
    args: &ResetDefaultRoleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        reset_default_role_builder(client, &args.organization, &args.database, &args.branch)?;
    reset_default_role_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Get a role
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_role_execute()` to send, or `get_role` for simplest API.

pub fn get_role_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles/{}",
        organization, database, branch, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Get a role
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_role_execute()` or `get_role`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_role_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Get a role
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_role_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_role_task()`.
/// For the simplest API, use `get_role()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_role_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_role_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_role`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetRoleArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Get a role
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_role_builder()` + `get_role_execute()`.
/// For task-level control, use `get_role_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_role(
    client: &SimpleHttpClient,
    args: &GetRoleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_role_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.id,
    )?;
    get_role_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Update role name
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_role_execute()` to send, or `update_role` for simplest API.

pub fn update_role_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles/{}",
        organization, database, branch, id,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Update role name
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_role_execute()` or `update_role`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_role_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Update role name
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_role_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_role_task()`.
/// For the simplest API, use `update_role()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_role_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_role_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_role`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateRoleArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Update role name
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_role_builder()` + `update_role_execute()`.
/// For task-level control, use `update_role_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_role(
    client: &SimpleHttpClient,
    args: &UpdateRoleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = update_role_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.id,
    )?;
    update_role_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Delete role credentials
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_role_execute()` to send, or `delete_role` for simplest API.

pub fn delete_role_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    id: &String,
    successor: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles/{}",
        organization, database, branch, id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = successor.as_ref() {
        query_parts.push(format!("successor={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Delete role credentials
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_role_execute()` or `delete_role`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_role_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Delete role credentials
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_role_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_role_task()`.
/// For the simplest API, use `delete_role()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_role_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_role_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_role`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteRoleArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: successor
    pub successor: Option<String>,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Delete role credentials
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_role_builder()` + `delete_role_execute()`.
/// For task-level control, use `delete_role_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_role(
    client: &SimpleHttpClient,
    args: &DeleteRoleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_role_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.id,
        &args.successor,
    )?;
    delete_role_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reassign
/// Reassign objects owned by one role to another role
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reassign_role_objects_execute()` to send, or `reassign_role_objects` for simplest API.

pub fn reassign_role_objects_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles/{}/reassign",
        organization,
        database,
        branch,
        id,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reassign
/// Reassign objects owned by one role to another role
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reassign_role_objects_execute()` or `reassign_role_objects`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reassign_role_objects_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reassign_role_objects_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reassign
/// Reassign objects owned by one role to another role
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reassign_role_objects_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reassign_role_objects_task()`.
/// For the simplest API, use `reassign_role_objects()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reassign_role_objects_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reassign_role_objects_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = reassign_role_objects_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reassign_role_objects`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ReassignRoleObjectsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reassign
/// Reassign objects owned by one role to another role
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reassign_role_objects_builder()` + `reassign_role_objects_execute()`.
/// For task-level control, use `reassign_role_objects_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reassign_role_objects(
    client: &SimpleHttpClient,
    args: &ReassignRoleObjectsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = reassign_role_objects_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.id,
    )?;
    reassign_role_objects_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/renew
/// Renew role expiration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `renew_role_execute()` to send, or `renew_role` for simplest API.

pub fn renew_role_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles/{}/renew",
        organization, database, branch, id,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/renew
/// Renew role expiration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `renew_role_execute()` or `renew_role`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `renew_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn renew_role_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/renew
/// Renew role expiration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `renew_role_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `renew_role_task()`.
/// For the simplest API, use `renew_role()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `renew_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn renew_role_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = renew_role_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`renew_role`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RenewRoleArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/renew
/// Renew role expiration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `renew_role_builder()` + `renew_role_execute()`.
/// For task-level control, use `renew_role_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn renew_role(
    client: &SimpleHttpClient,
    args: &RenewRoleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = renew_role_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.id,
    )?;
    renew_role_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reset
/// Reset a role's password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `reset_role_execute()` to send, or `reset_role` for simplest API.

pub fn reset_role_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles/{}/reset",
        organization, database, branch, id,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reset
/// Reset a role's password
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `reset_role_execute()` or `reset_role`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reset_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reset_role_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reset
/// Reset a role's password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `reset_role_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `reset_role_task()`.
/// For the simplest API, use `reset_role()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `reset_role_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn reset_role_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = reset_role_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`reset_role`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ResetRoleArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reset
/// Reset a role's password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `reset_role_builder()` + `reset_role_execute()`.
/// For task-level control, use `reset_role_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn reset_role(
    client: &SimpleHttpClient,
    args: &ResetRoleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = reset_role_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.id,
    )?;
    reset_role_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Enable safe migrations for a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `enable_safe_migrations_execute()` to send, or `enable_safe_migrations` for simplest API.

pub fn enable_safe_migrations_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/safe-migrations",
        organization, database, branch,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Enable safe migrations for a branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `enable_safe_migrations_execute()` or `enable_safe_migrations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `enable_safe_migrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn enable_safe_migrations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Enable safe migrations for a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `enable_safe_migrations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `enable_safe_migrations_task()`.
/// For the simplest API, use `enable_safe_migrations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `enable_safe_migrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn enable_safe_migrations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = enable_safe_migrations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`enable_safe_migrations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct EnableSafeMigrationsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Enable safe migrations for a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `enable_safe_migrations_builder()` + `enable_safe_migrations_execute()`.
/// For task-level control, use `enable_safe_migrations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn enable_safe_migrations(
    client: &SimpleHttpClient,
    args: &EnableSafeMigrationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        enable_safe_migrations_builder(client, &args.organization, &args.database, &args.branch)?;
    enable_safe_migrations_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Disable safe migrations for a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `disable_safe_migrations_execute()` to send, or `disable_safe_migrations` for simplest API.

pub fn disable_safe_migrations_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/safe-migrations",
        organization, database, branch,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Disable safe migrations for a branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `disable_safe_migrations_execute()` or `disable_safe_migrations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `disable_safe_migrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn disable_safe_migrations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Disable safe migrations for a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `disable_safe_migrations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `disable_safe_migrations_task()`.
/// For the simplest API, use `disable_safe_migrations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `disable_safe_migrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn disable_safe_migrations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = disable_safe_migrations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`disable_safe_migrations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisableSafeMigrationsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Disable safe migrations for a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `disable_safe_migrations_builder()` + `disable_safe_migrations_execute()`.
/// For task-level control, use `disable_safe_migrations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn disable_safe_migrations(
    client: &SimpleHttpClient,
    args: &DisableSafeMigrationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        disable_safe_migrations_builder(client, &args.organization, &args.database, &args.branch)?;
    disable_safe_migrations_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema
/// Get a branch schema
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_branch_schema_execute()` to send, or `get_branch_schema` for simplest API.

pub fn get_branch_schema_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    keyspace: &Option<String>,
    namespace: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/schema",
        organization, database, branch,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = keyspace.as_ref() {
        query_parts.push(format!("keyspace={}", val));
    }
    if let Some(val) = namespace.as_ref() {
        query_parts.push(format!("namespace={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema
/// Get a branch schema
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_branch_schema_execute()` or `get_branch_schema`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_branch_schema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_branch_schema_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema
/// Get a branch schema
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_branch_schema_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_branch_schema_task()`.
/// For the simplest API, use `get_branch_schema()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_branch_schema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_branch_schema_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_branch_schema_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_branch_schema`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetBranchSchemaArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: keyspace
    pub keyspace: Option<String>,
    /// Query parameter: namespace
    pub namespace: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema
/// Get a branch schema
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_branch_schema_builder()` + `get_branch_schema_execute()`.
/// For task-level control, use `get_branch_schema_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_branch_schema(
    client: &SimpleHttpClient,
    args: &GetBranchSchemaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_branch_schema_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.keyspace,
        &args.namespace,
    )?;
    get_branch_schema_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema/lint
/// Lint a branch schema
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `lint_branch_schema_execute()` to send, or `lint_branch_schema` for simplest API.

pub fn lint_branch_schema_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/schema/lint",
        organization, database, branch,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema/lint
/// Lint a branch schema
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `lint_branch_schema_execute()` or `lint_branch_schema`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `lint_branch_schema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn lint_branch_schema_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema/lint
/// Lint a branch schema
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `lint_branch_schema_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `lint_branch_schema_task()`.
/// For the simplest API, use `lint_branch_schema()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `lint_branch_schema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn lint_branch_schema_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = lint_branch_schema_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`lint_branch_schema`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LintBranchSchemaArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema/lint
/// Lint a branch schema
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `lint_branch_schema_builder()` + `lint_branch_schema_execute()`.
/// For task-level control, use `lint_branch_schema_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn lint_branch_schema(
    client: &SimpleHttpClient,
    args: &LintBranchSchemaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = lint_branch_schema_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.page,
        &args.per_page,
    )?;
    lint_branch_schema_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// List traffic budgets
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_traffic_budgets_execute()` to send, or `list_traffic_budgets` for simplest API.

pub fn list_traffic_budgets_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    page: &Option<String>,
    per_page: &Option<String>,
    period: &Option<String>,
    created_at: &Option<String>,
    fingerprint: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/traffic/budgets",
        organization, database, branch,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }
    if let Some(val) = period.as_ref() {
        query_parts.push(format!("period={}", val));
    }
    if let Some(val) = created_at.as_ref() {
        query_parts.push(format!("created_at={}", val));
    }
    if let Some(val) = fingerprint.as_ref() {
        query_parts.push(format!("fingerprint={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// List traffic budgets
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_traffic_budgets_execute()` or `list_traffic_budgets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_traffic_budgets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_traffic_budgets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// List traffic budgets
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_traffic_budgets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_traffic_budgets_task()`.
/// For the simplest API, use `list_traffic_budgets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_traffic_budgets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_traffic_budgets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_traffic_budgets_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_traffic_budgets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListTrafficBudgetsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
    /// Query parameter: period
    pub period: Option<String>,
    /// Query parameter: created_at
    pub created_at: Option<String>,
    /// Query parameter: fingerprint
    pub fingerprint: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// List traffic budgets
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_traffic_budgets_builder()` + `list_traffic_budgets_execute()`.
/// For task-level control, use `list_traffic_budgets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_traffic_budgets(
    client: &SimpleHttpClient,
    args: &ListTrafficBudgetsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_traffic_budgets_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.page,
        &args.per_page,
        &args.period,
        &args.created_at,
        &args.fingerprint,
    )?;
    list_traffic_budgets_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// Create a traffic budget
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `create_traffic_budget_execute()` to send, or `create_traffic_budget` for simplest API.

pub fn create_traffic_budget_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/traffic/budgets",
        organization, database, branch,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// Create a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `create_traffic_budget_execute()` or `create_traffic_budget`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_traffic_budget_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_traffic_budget_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// Create a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `create_traffic_budget_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `create_traffic_budget_task()`.
/// For the simplest API, use `create_traffic_budget()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_traffic_budget_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn create_traffic_budget_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = create_traffic_budget_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`create_traffic_budget`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CreateTrafficBudgetArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// Create a traffic budget
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `create_traffic_budget_builder()` + `create_traffic_budget_execute()`.
/// For task-level control, use `create_traffic_budget_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_traffic_budget(
    client: &SimpleHttpClient,
    args: &CreateTrafficBudgetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        create_traffic_budget_builder(client, &args.organization, &args.database, &args.branch)?;
    create_traffic_budget_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules
/// Create a traffic rule
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `create_traffic_rule_execute()` to send, or `create_traffic_rule` for simplest API.

pub fn create_traffic_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    budget_id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/traffic/budgets/{}/rules",
        organization,
        database,
        branch,
        budget_id,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules
/// Create a traffic rule
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `create_traffic_rule_execute()` or `create_traffic_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_traffic_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_traffic_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules
/// Create a traffic rule
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `create_traffic_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `create_traffic_rule_task()`.
/// For the simplest API, use `create_traffic_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_traffic_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn create_traffic_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = create_traffic_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`create_traffic_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CreateTrafficRuleArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: budget_id
    pub budget_id: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules
/// Create a traffic rule
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `create_traffic_rule_builder()` + `create_traffic_rule_execute()`.
/// For task-level control, use `create_traffic_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_traffic_rule(
    client: &SimpleHttpClient,
    args: &CreateTrafficRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = create_traffic_rule_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.budget_id,
    )?;
    create_traffic_rule_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules/{id}
/// Delete a traffic rule
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_traffic_rule_execute()` to send, or `delete_traffic_rule` for simplest API.

pub fn delete_traffic_rule_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    budget_id: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/traffic/budgets/{}/rules/{}",
        organization,
        database,
        branch,
        budget_id,
        id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules/{id}
/// Delete a traffic rule
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_traffic_rule_execute()` or `delete_traffic_rule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_traffic_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_traffic_rule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules/{id}
/// Delete a traffic rule
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_traffic_rule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_traffic_rule_task()`.
/// For the simplest API, use `delete_traffic_rule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_traffic_rule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_traffic_rule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_traffic_rule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_traffic_rule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteTrafficRuleArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: budget_id
    pub budget_id: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules/{id}
/// Delete a traffic rule
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_traffic_rule_builder()` + `delete_traffic_rule_execute()`.
/// For task-level control, use `delete_traffic_rule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_traffic_rule(
    client: &SimpleHttpClient,
    args: &DeleteTrafficRuleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_traffic_rule_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.budget_id,
        &args.id,
    )?;
    delete_traffic_rule_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Get a traffic budget
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_traffic_budget_execute()` to send, or `get_traffic_budget` for simplest API.

pub fn get_traffic_budget_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/traffic/budgets/{}",
        organization,
        database,
        branch,
        id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Get a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_traffic_budget_execute()` or `get_traffic_budget`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_traffic_budget_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_traffic_budget_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Get a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_traffic_budget_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_traffic_budget_task()`.
/// For the simplest API, use `get_traffic_budget()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_traffic_budget_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_traffic_budget_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_traffic_budget_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_traffic_budget`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetTrafficBudgetArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Get a traffic budget
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_traffic_budget_builder()` + `get_traffic_budget_execute()`.
/// For task-level control, use `get_traffic_budget_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_traffic_budget(
    client: &SimpleHttpClient,
    args: &GetTrafficBudgetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_traffic_budget_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.id,
    )?;
    get_traffic_budget_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Update a traffic budget
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_traffic_budget_execute()` to send, or `update_traffic_budget` for simplest API.

pub fn update_traffic_budget_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/traffic/budgets/{}",
        organization,
        database,
        branch,
        id,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Update a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_traffic_budget_execute()` or `update_traffic_budget`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_traffic_budget_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_traffic_budget_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Update a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_traffic_budget_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_traffic_budget_task()`.
/// For the simplest API, use `update_traffic_budget()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_traffic_budget_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_traffic_budget_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_traffic_budget_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_traffic_budget`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateTrafficBudgetArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Update a traffic budget
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_traffic_budget_builder()` + `update_traffic_budget_execute()`.
/// For task-level control, use `update_traffic_budget_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_traffic_budget(
    client: &SimpleHttpClient,
    args: &UpdateTrafficBudgetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = update_traffic_budget_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.id,
    )?;
    update_traffic_budget_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Delete a traffic budget
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_traffic_budget_execute()` to send, or `delete_traffic_budget` for simplest API.

pub fn delete_traffic_budget_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    branch: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/traffic/budgets/{}",
        organization,
        database,
        branch,
        id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Delete a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_traffic_budget_execute()` or `delete_traffic_budget`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_traffic_budget_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_traffic_budget_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Delete a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_traffic_budget_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_traffic_budget_task()`.
/// For the simplest API, use `delete_traffic_budget()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_traffic_budget_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_traffic_budget_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_traffic_budget_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_traffic_budget`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteTrafficBudgetArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Delete a traffic budget
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_traffic_budget_builder()` + `delete_traffic_budget_execute()`.
/// For task-level control, use `delete_traffic_budget_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_traffic_budget(
    client: &SimpleHttpClient,
    args: &DeleteTrafficBudgetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_traffic_budget_builder(
        client,
        &args.organization,
        &args.database,
        &args.branch,
        &args.id,
    )?;
    delete_traffic_budget_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/cidrs
/// List IP restriction entries
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_database_postgres_cidrs_execute()` to send, or `list_database_postgres_cidrs` for simplest API.

pub fn list_database_postgres_cidrs_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/cidrs",
        organization, database,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/cidrs
/// List IP restriction entries
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_database_postgres_cidrs_execute()` or `list_database_postgres_cidrs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_database_postgres_cidrs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_database_postgres_cidrs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/cidrs
/// List IP restriction entries
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_database_postgres_cidrs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_database_postgres_cidrs_task()`.
/// For the simplest API, use `list_database_postgres_cidrs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_database_postgres_cidrs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_database_postgres_cidrs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_database_postgres_cidrs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_database_postgres_cidrs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListDatabasePostgresCidrsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/cidrs
/// List IP restriction entries
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_database_postgres_cidrs_builder()` + `list_database_postgres_cidrs_execute()`.
/// For task-level control, use `list_database_postgres_cidrs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_database_postgres_cidrs(
    client: &SimpleHttpClient,
    args: &ListDatabasePostgresCidrsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_database_postgres_cidrs_builder(
        client,
        &args.organization,
        &args.database,
        &args.page,
        &args.per_page,
    )?;
    list_database_postgres_cidrs_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/cidrs
/// Create an IP restriction entry
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `create_database_postgres_cidr_execute()` to send, or `create_database_postgres_cidr` for simplest API.

pub fn create_database_postgres_cidr_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/cidrs",
        organization, database,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/cidrs
/// Create an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `create_database_postgres_cidr_execute()` or `create_database_postgres_cidr`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_database_postgres_cidr_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_database_postgres_cidr_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/cidrs
/// Create an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `create_database_postgres_cidr_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `create_database_postgres_cidr_task()`.
/// For the simplest API, use `create_database_postgres_cidr()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_database_postgres_cidr_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn create_database_postgres_cidr_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = create_database_postgres_cidr_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`create_database_postgres_cidr`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CreateDatabasePostgresCidrArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// POST /organizations/{organization}/databases/{database}/cidrs
/// Create an IP restriction entry
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `create_database_postgres_cidr_builder()` + `create_database_postgres_cidr_execute()`.
/// For task-level control, use `create_database_postgres_cidr_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_database_postgres_cidr(
    client: &SimpleHttpClient,
    args: &CreateDatabasePostgresCidrArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        create_database_postgres_cidr_builder(client, &args.organization, &args.database)?;
    create_database_postgres_cidr_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/cidrs/{id}
/// Get an IP restriction entry
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_database_postgres_cidr_execute()` to send, or `get_database_postgres_cidr` for simplest API.

pub fn get_database_postgres_cidr_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/cidrs/{}",
        organization, database, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/cidrs/{id}
/// Get an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_database_postgres_cidr_execute()` or `get_database_postgres_cidr`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_database_postgres_cidr_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_database_postgres_cidr_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/cidrs/{id}
/// Get an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_database_postgres_cidr_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_database_postgres_cidr_task()`.
/// For the simplest API, use `get_database_postgres_cidr()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_database_postgres_cidr_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_database_postgres_cidr_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_database_postgres_cidr_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_database_postgres_cidr`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetDatabasePostgresCidrArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/databases/{database}/cidrs/{id}
/// Get an IP restriction entry
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_database_postgres_cidr_builder()` + `get_database_postgres_cidr_execute()`.
/// For task-level control, use `get_database_postgres_cidr_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_database_postgres_cidr(
    client: &SimpleHttpClient,
    args: &GetDatabasePostgresCidrArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_database_postgres_cidr_builder(client, &args.organization, &args.database, &args.id)?;
    get_database_postgres_cidr_execute(builder)
}

/// PUT /organizations/{organization}/databases/{database}/cidrs/{id}
/// Update an IP restriction entry
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_database_postgres_cidr_execute()` to send, or `update_database_postgres_cidr` for simplest API.

pub fn update_database_postgres_cidr_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/cidrs/{}",
        organization, database, id,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT /organizations/{organization}/databases/{database}/cidrs/{id}
/// Update an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_database_postgres_cidr_execute()` or `update_database_postgres_cidr`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_database_postgres_cidr_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_database_postgres_cidr_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /organizations/{organization}/databases/{database}/cidrs/{id}
/// Update an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_database_postgres_cidr_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_database_postgres_cidr_task()`.
/// For the simplest API, use `update_database_postgres_cidr()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_database_postgres_cidr_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_database_postgres_cidr_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_database_postgres_cidr_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_database_postgres_cidr`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateDatabasePostgresCidrArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: id
    pub id: String,
}

/// PUT /organizations/{organization}/databases/{database}/cidrs/{id}
/// Update an IP restriction entry
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_database_postgres_cidr_builder()` + `update_database_postgres_cidr_execute()`.
/// For task-level control, use `update_database_postgres_cidr_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_database_postgres_cidr(
    client: &SimpleHttpClient,
    args: &UpdateDatabasePostgresCidrArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = update_database_postgres_cidr_builder(
        client,
        &args.organization,
        &args.database,
        &args.id,
    )?;
    update_database_postgres_cidr_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/cidrs/{id}
/// Delete an IP restriction entry
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_database_postgres_cidr_execute()` to send, or `delete_database_postgres_cidr` for simplest API.

pub fn delete_database_postgres_cidr_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/cidrs/{}",
        organization, database, id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/cidrs/{id}
/// Delete an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_database_postgres_cidr_execute()` or `delete_database_postgres_cidr`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_database_postgres_cidr_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_database_postgres_cidr_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/cidrs/{id}
/// Delete an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_database_postgres_cidr_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_database_postgres_cidr_task()`.
/// For the simplest API, use `delete_database_postgres_cidr()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_database_postgres_cidr_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_database_postgres_cidr_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_database_postgres_cidr_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_database_postgres_cidr`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteDatabasePostgresCidrArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /organizations/{organization}/databases/{database}/cidrs/{id}
/// Delete an IP restriction entry
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_database_postgres_cidr_builder()` + `delete_database_postgres_cidr_execute()`.
/// For task-level control, use `delete_database_postgres_cidr_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_database_postgres_cidr(
    client: &SimpleHttpClient,
    args: &DeleteDatabasePostgresCidrArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_database_postgres_cidr_builder(
        client,
        &args.organization,
        &args.database,
        &args.id,
    )?;
    delete_database_postgres_cidr_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-queue
/// Get the deploy queue
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_deploy_queue_execute()` to send, or `get_deploy_queue` for simplest API.

pub fn get_deploy_queue_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-queue",
        organization, database,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-queue
/// Get the deploy queue
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_deploy_queue_execute()` or `get_deploy_queue`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_deploy_queue_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_deploy_queue_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/deploy-queue
/// Get the deploy queue
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_deploy_queue_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_deploy_queue_task()`.
/// For the simplest API, use `get_deploy_queue()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_deploy_queue_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_deploy_queue_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_deploy_queue_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_deploy_queue`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetDeployQueueArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/deploy-queue
/// Get the deploy queue
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_deploy_queue_builder()` + `get_deploy_queue_execute()`.
/// For task-level control, use `get_deploy_queue_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_deploy_queue(
    client: &SimpleHttpClient,
    args: &GetDeployQueueArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_deploy_queue_builder(
        client,
        &args.organization,
        &args.database,
        &args.page,
        &args.per_page,
    )?;
    get_deploy_queue_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests
/// List deploy requests
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_deploy_requests_execute()` to send, or `list_deploy_requests` for simplest API.

pub fn list_deploy_requests_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    state: &Option<String>,
    branch: &Option<String>,
    into_branch: &Option<String>,
    deployed_at: &Option<String>,
    running_at: &Option<String>,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests",
        organization, database,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = state.as_ref() {
        query_parts.push(format!("state={}", val));
    }
    if let Some(val) = branch.as_ref() {
        query_parts.push(format!("branch={}", val));
    }
    if let Some(val) = into_branch.as_ref() {
        query_parts.push(format!("into_branch={}", val));
    }
    if let Some(val) = deployed_at.as_ref() {
        query_parts.push(format!("deployed_at={}", val));
    }
    if let Some(val) = running_at.as_ref() {
        query_parts.push(format!("running_at={}", val));
    }
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests
/// List deploy requests
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_deploy_requests_execute()` or `list_deploy_requests`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_deploy_requests_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_deploy_requests_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests
/// List deploy requests
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_deploy_requests_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_deploy_requests_task()`.
/// For the simplest API, use `list_deploy_requests()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_deploy_requests_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_deploy_requests_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_deploy_requests_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_deploy_requests`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListDeployRequestsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: state
    pub state: Option<String>,
    /// Query parameter: branch
    pub branch: Option<String>,
    /// Query parameter: into_branch
    pub into_branch: Option<String>,
    /// Query parameter: deployed_at
    pub deployed_at: Option<String>,
    /// Query parameter: running_at
    pub running_at: Option<String>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests
/// List deploy requests
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_deploy_requests_builder()` + `list_deploy_requests_execute()`.
/// For task-level control, use `list_deploy_requests_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_deploy_requests(
    client: &SimpleHttpClient,
    args: &ListDeployRequestsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_deploy_requests_builder(
        client,
        &args.organization,
        &args.database,
        &args.state,
        &args.branch,
        &args.into_branch,
        &args.deployed_at,
        &args.running_at,
        &args.page,
        &args.per_page,
    )?;
    list_deploy_requests_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests
/// Create a deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `create_deploy_request_execute()` to send, or `create_deploy_request` for simplest API.

pub fn create_deploy_request_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests",
        organization, database,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests
/// Create a deploy request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `create_deploy_request_execute()` or `create_deploy_request`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_deploy_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_deploy_request_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests
/// Create a deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `create_deploy_request_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `create_deploy_request_task()`.
/// For the simplest API, use `create_deploy_request()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_deploy_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn create_deploy_request_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = create_deploy_request_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`create_deploy_request`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CreateDeployRequestArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests
/// Create a deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `create_deploy_request_builder()` + `create_deploy_request_execute()`.
/// For task-level control, use `create_deploy_request_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_deploy_request(
    client: &SimpleHttpClient,
    args: &CreateDeployRequestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = create_deploy_request_builder(client, &args.organization, &args.database)?;
    create_deploy_request_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Get a deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_deploy_request_execute()` to send, or `get_deploy_request` for simplest API.

pub fn get_deploy_request_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}",
        organization, database, number,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Get a deploy request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_deploy_request_execute()` or `get_deploy_request`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_deploy_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_deploy_request_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Get a deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_deploy_request_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_deploy_request_task()`.
/// For the simplest API, use `get_deploy_request()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_deploy_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_deploy_request_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_deploy_request_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_deploy_request`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetDeployRequestArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Get a deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_deploy_request_builder()` + `get_deploy_request_execute()`.
/// For task-level control, use `get_deploy_request_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_deploy_request(
    client: &SimpleHttpClient,
    args: &GetDeployRequestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_deploy_request_builder(client, &args.organization, &args.database, &args.number)?;
    get_deploy_request_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Close a deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `close_deploy_request_execute()` to send, or `close_deploy_request` for simplest API.

pub fn close_deploy_request_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}",
        organization, database, number,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Close a deploy request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `close_deploy_request_execute()` or `close_deploy_request`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `close_deploy_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn close_deploy_request_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Close a deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `close_deploy_request_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `close_deploy_request_task()`.
/// For the simplest API, use `close_deploy_request()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `close_deploy_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn close_deploy_request_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = close_deploy_request_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`close_deploy_request`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloseDeployRequestArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Close a deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `close_deploy_request_builder()` + `close_deploy_request_execute()`.
/// For task-level control, use `close_deploy_request_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn close_deploy_request(
    client: &SimpleHttpClient,
    args: &CloseDeployRequestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        close_deploy_request_builder(client, &args.organization, &args.database, &args.number)?;
    close_deploy_request_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/apply-deploy
/// Complete a gated deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `complete_gated_deploy_request_execute()` to send, or `complete_gated_deploy_request` for simplest API.

pub fn complete_gated_deploy_request_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/apply-deploy",
        organization,
        database,
        number,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/apply-deploy
/// Complete a gated deploy request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `complete_gated_deploy_request_execute()` or `complete_gated_deploy_request`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `complete_gated_deploy_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn complete_gated_deploy_request_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/apply-deploy
/// Complete a gated deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `complete_gated_deploy_request_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `complete_gated_deploy_request_task()`.
/// For the simplest API, use `complete_gated_deploy_request()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `complete_gated_deploy_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn complete_gated_deploy_request_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = complete_gated_deploy_request_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`complete_gated_deploy_request`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CompleteGatedDeployRequestArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/apply-deploy
/// Complete a gated deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `complete_gated_deploy_request_builder()` + `complete_gated_deploy_request_execute()`.
/// For task-level control, use `complete_gated_deploy_request_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn complete_gated_deploy_request(
    client: &SimpleHttpClient,
    args: &CompleteGatedDeployRequestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = complete_gated_deploy_request_builder(
        client,
        &args.organization,
        &args.database,
        &args.number,
    )?;
    complete_gated_deploy_request_execute(builder)
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-apply
/// Update auto-apply for deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_auto_apply_execute()` to send, or `update_auto_apply` for simplest API.

pub fn update_auto_apply_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/auto-apply",
        organization,
        database,
        number,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-apply
/// Update auto-apply for deploy request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_auto_apply_execute()` or `update_auto_apply`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_auto_apply_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_auto_apply_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-apply
/// Update auto-apply for deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_auto_apply_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_auto_apply_task()`.
/// For the simplest API, use `update_auto_apply()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_auto_apply_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_auto_apply_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_auto_apply_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_auto_apply`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateAutoApplyArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-apply
/// Update auto-apply for deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_auto_apply_builder()` + `update_auto_apply_execute()`.
/// For task-level control, use `update_auto_apply_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_auto_apply(
    client: &SimpleHttpClient,
    args: &UpdateAutoApplyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        update_auto_apply_builder(client, &args.organization, &args.database, &args.number)?;
    update_auto_apply_execute(builder)
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-delete-branch
/// Update auto-delete branch for deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_auto_delete_branch_execute()` to send, or `update_auto_delete_branch` for simplest API.

pub fn update_auto_delete_branch_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/auto-delete-branch",
        organization,
        database,
        number,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-delete-branch
/// Update auto-delete branch for deploy request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_auto_delete_branch_execute()` or `update_auto_delete_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_auto_delete_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_auto_delete_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-delete-branch
/// Update auto-delete branch for deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_auto_delete_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_auto_delete_branch_task()`.
/// For the simplest API, use `update_auto_delete_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_auto_delete_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_auto_delete_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_auto_delete_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_auto_delete_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateAutoDeleteBranchArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-delete-branch
/// Update auto-delete branch for deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_auto_delete_branch_builder()` + `update_auto_delete_branch_execute()`.
/// For task-level control, use `update_auto_delete_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_auto_delete_branch(
    client: &SimpleHttpClient,
    args: &UpdateAutoDeleteBranchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = update_auto_delete_branch_builder(
        client,
        &args.organization,
        &args.database,
        &args.number,
    )?;
    update_auto_delete_branch_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/cancel
/// Cancel a queued deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cancel_deploy_request_execute()` to send, or `cancel_deploy_request` for simplest API.

pub fn cancel_deploy_request_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/cancel",
        organization, database, number,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/cancel
/// Cancel a queued deploy request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cancel_deploy_request_execute()` or `cancel_deploy_request`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cancel_deploy_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cancel_deploy_request_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/cancel
/// Cancel a queued deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cancel_deploy_request_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cancel_deploy_request_task()`.
/// For the simplest API, use `cancel_deploy_request()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cancel_deploy_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cancel_deploy_request_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cancel_deploy_request_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cancel_deploy_request`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CancelDeployRequestArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/cancel
/// Cancel a queued deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cancel_deploy_request_builder()` + `cancel_deploy_request_execute()`.
/// For task-level control, use `cancel_deploy_request_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cancel_deploy_request(
    client: &SimpleHttpClient,
    args: &CancelDeployRequestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cancel_deploy_request_builder(client, &args.organization, &args.database, &args.number)?;
    cancel_deploy_request_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/complete-deploy
/// Complete an errored deploy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `complete_errored_deploy_execute()` to send, or `complete_errored_deploy` for simplest API.

pub fn complete_errored_deploy_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/complete-deploy",
        organization,
        database,
        number,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/complete-deploy
/// Complete an errored deploy
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `complete_errored_deploy_execute()` or `complete_errored_deploy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `complete_errored_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn complete_errored_deploy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/complete-deploy
/// Complete an errored deploy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `complete_errored_deploy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `complete_errored_deploy_task()`.
/// For the simplest API, use `complete_errored_deploy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `complete_errored_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn complete_errored_deploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = complete_errored_deploy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`complete_errored_deploy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CompleteErroredDeployArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/complete-deploy
/// Complete an errored deploy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `complete_errored_deploy_builder()` + `complete_errored_deploy_execute()`.
/// For task-level control, use `complete_errored_deploy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn complete_errored_deploy(
    client: &SimpleHttpClient,
    args: &CompleteErroredDeployArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        complete_errored_deploy_builder(client, &args.organization, &args.database, &args.number)?;
    complete_errored_deploy_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/deploy
/// Queue a deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `queue_deploy_request_execute()` to send, or `queue_deploy_request` for simplest API.

pub fn queue_deploy_request_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/deploy",
        organization, database, number,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/deploy
/// Queue a deploy request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `queue_deploy_request_execute()` or `queue_deploy_request`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `queue_deploy_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn queue_deploy_request_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/deploy
/// Queue a deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `queue_deploy_request_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `queue_deploy_request_task()`.
/// For the simplest API, use `queue_deploy_request()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `queue_deploy_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn queue_deploy_request_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = queue_deploy_request_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`queue_deploy_request`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct QueueDeployRequestArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/deploy
/// Queue a deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `queue_deploy_request_builder()` + `queue_deploy_request_execute()`.
/// For task-level control, use `queue_deploy_request_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn queue_deploy_request(
    client: &SimpleHttpClient,
    args: &QueueDeployRequestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        queue_deploy_request_builder(client, &args.organization, &args.database, &args.number)?;
    queue_deploy_request_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/deployment
/// Get a deployment
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_deployment_execute()` to send, or `get_deployment` for simplest API.

pub fn get_deployment_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/deployment",
        organization,
        database,
        number,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/deployment
/// Get a deployment
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_deployment_execute()` or `get_deployment`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_deployment_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_deployment_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/deployment
/// Get a deployment
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_deployment_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_deployment_task()`.
/// For the simplest API, use `get_deployment()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_deployment_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_deployment_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_deployment_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_deployment`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetDeploymentArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/deployment
/// Get a deployment
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_deployment_builder()` + `get_deployment_execute()`.
/// For task-level control, use `get_deployment_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_deployment(
    client: &SimpleHttpClient,
    args: &GetDeploymentArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_deployment_builder(client, &args.organization, &args.database, &args.number)?;
    get_deployment_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/operations
/// List deploy operations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_deploy_operations_execute()` to send, or `list_deploy_operations` for simplest API.

pub fn list_deploy_operations_builder<R>(
    client: &SimpleHttpClient<R>,
    number: &String,
    organization: &String,
    database: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/operations",
        organization,
        database,
        number,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/operations
/// List deploy operations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_deploy_operations_execute()` or `list_deploy_operations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_deploy_operations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_deploy_operations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/operations
/// List deploy operations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_deploy_operations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_deploy_operations_task()`.
/// For the simplest API, use `list_deploy_operations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_deploy_operations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_deploy_operations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_deploy_operations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_deploy_operations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListDeployOperationsArgs {
    /// Path parameter: number
    pub number: String,
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/operations
/// List deploy operations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_deploy_operations_builder()` + `list_deploy_operations_execute()`.
/// For task-level control, use `list_deploy_operations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_deploy_operations(
    client: &SimpleHttpClient,
    args: &ListDeployOperationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_deploy_operations_builder(
        client,
        &args.number,
        &args.organization,
        &args.database,
        &args.page,
        &args.per_page,
    )?;
    list_deploy_operations_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/revert
/// Complete a revert
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `complete_revert_execute()` to send, or `complete_revert` for simplest API.

pub fn complete_revert_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/revert",
        organization, database, number,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/revert
/// Complete a revert
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `complete_revert_execute()` or `complete_revert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `complete_revert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn complete_revert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/revert
/// Complete a revert
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `complete_revert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `complete_revert_task()`.
/// For the simplest API, use `complete_revert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `complete_revert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn complete_revert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = complete_revert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`complete_revert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CompleteRevertArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/revert
/// Complete a revert
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `complete_revert_builder()` + `complete_revert_execute()`.
/// For task-level control, use `complete_revert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn complete_revert(
    client: &SimpleHttpClient,
    args: &CompleteRevertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        complete_revert_builder(client, &args.organization, &args.database, &args.number)?;
    complete_revert_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// List deploy request reviews
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_deploy_request_reviews_execute()` to send, or `list_deploy_request_reviews` for simplest API.

pub fn list_deploy_request_reviews_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/reviews",
        organization, database, number,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// List deploy request reviews
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_deploy_request_reviews_execute()` or `list_deploy_request_reviews`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_deploy_request_reviews_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_deploy_request_reviews_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// List deploy request reviews
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_deploy_request_reviews_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_deploy_request_reviews_task()`.
/// For the simplest API, use `list_deploy_request_reviews()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_deploy_request_reviews_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_deploy_request_reviews_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_deploy_request_reviews_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_deploy_request_reviews`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListDeployRequestReviewsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// List deploy request reviews
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_deploy_request_reviews_builder()` + `list_deploy_request_reviews_execute()`.
/// For task-level control, use `list_deploy_request_reviews_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_deploy_request_reviews(
    client: &SimpleHttpClient,
    args: &ListDeployRequestReviewsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_deploy_request_reviews_builder(
        client,
        &args.organization,
        &args.database,
        &args.number,
        &args.page,
        &args.per_page,
    )?;
    list_deploy_request_reviews_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// Review a deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `review_deploy_request_execute()` to send, or `review_deploy_request` for simplest API.

pub fn review_deploy_request_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/reviews",
        organization, database, number,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// Review a deploy request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `review_deploy_request_execute()` or `review_deploy_request`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `review_deploy_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn review_deploy_request_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// Review a deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `review_deploy_request_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `review_deploy_request_task()`.
/// For the simplest API, use `review_deploy_request()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `review_deploy_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn review_deploy_request_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = review_deploy_request_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`review_deploy_request`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ReviewDeployRequestArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// Review a deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `review_deploy_request_builder()` + `review_deploy_request_execute()`.
/// For task-level control, use `review_deploy_request_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn review_deploy_request(
    client: &SimpleHttpClient,
    args: &ReviewDeployRequestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        review_deploy_request_builder(client, &args.organization, &args.database, &args.number)?;
    review_deploy_request_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/skip-revert
/// Skip revert period
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `skip_revert_period_execute()` to send, or `skip_revert_period` for simplest API.

pub fn skip_revert_period_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/skip-revert",
        organization,
        database,
        number,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/skip-revert
/// Skip revert period
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `skip_revert_period_execute()` or `skip_revert_period`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `skip_revert_period_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn skip_revert_period_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/skip-revert
/// Skip revert period
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `skip_revert_period_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `skip_revert_period_task()`.
/// For the simplest API, use `skip_revert_period()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `skip_revert_period_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn skip_revert_period_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = skip_revert_period_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`skip_revert_period`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SkipRevertPeriodArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/skip-revert
/// Skip revert period
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `skip_revert_period_builder()` + `skip_revert_period_execute()`.
/// For task-level control, use `skip_revert_period_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn skip_revert_period(
    client: &SimpleHttpClient,
    args: &SkipRevertPeriodArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        skip_revert_period_builder(client, &args.organization, &args.database, &args.number)?;
    skip_revert_period_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/storage-check
/// Check deploy request storage
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `check_deploy_request_storage_execute()` to send, or `check_deploy_request_storage` for simplest API.

pub fn check_deploy_request_storage_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/storage-check",
        organization,
        database,
        number,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/storage-check
/// Check deploy request storage
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `check_deploy_request_storage_execute()` or `check_deploy_request_storage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `check_deploy_request_storage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn check_deploy_request_storage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/storage-check
/// Check deploy request storage
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `check_deploy_request_storage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `check_deploy_request_storage_task()`.
/// For the simplest API, use `check_deploy_request_storage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `check_deploy_request_storage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn check_deploy_request_storage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = check_deploy_request_storage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`check_deploy_request_storage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CheckDeployRequestStorageArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/storage-check
/// Check deploy request storage
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `check_deploy_request_storage_builder()` + `check_deploy_request_storage_execute()`.
/// For task-level control, use `check_deploy_request_storage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn check_deploy_request_storage(
    client: &SimpleHttpClient,
    args: &CheckDeployRequestStorageArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = check_deploy_request_storage_builder(
        client,
        &args.organization,
        &args.database,
        &args.number,
    )?;
    check_deploy_request_storage_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Get deploy request throttler configurations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_deploy_request_throttler_execute()` to send, or `get_deploy_request_throttler` for simplest API.

pub fn get_deploy_request_throttler_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/throttler",
        organization, database, number,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Get deploy request throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_deploy_request_throttler_execute()` or `get_deploy_request_throttler`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_deploy_request_throttler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_deploy_request_throttler_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Get deploy request throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_deploy_request_throttler_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_deploy_request_throttler_task()`.
/// For the simplest API, use `get_deploy_request_throttler()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_deploy_request_throttler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_deploy_request_throttler_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_deploy_request_throttler_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_deploy_request_throttler`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetDeployRequestThrottlerArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Get deploy request throttler configurations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_deploy_request_throttler_builder()` + `get_deploy_request_throttler_execute()`.
/// For task-level control, use `get_deploy_request_throttler_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_deploy_request_throttler(
    client: &SimpleHttpClient,
    args: &GetDeployRequestThrottlerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_deploy_request_throttler_builder(
        client,
        &args.organization,
        &args.database,
        &args.number,
    )?;
    get_deploy_request_throttler_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Update deploy request throttler configurations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_deploy_request_throttler_execute()` to send, or `update_deploy_request_throttler` for simplest API.

pub fn update_deploy_request_throttler_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/throttler",
        organization, database, number,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Update deploy request throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_deploy_request_throttler_execute()` or `update_deploy_request_throttler`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_deploy_request_throttler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_deploy_request_throttler_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Update deploy request throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_deploy_request_throttler_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_deploy_request_throttler_task()`.
/// For the simplest API, use `update_deploy_request_throttler()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_deploy_request_throttler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_deploy_request_throttler_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_deploy_request_throttler_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_deploy_request_throttler`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateDeployRequestThrottlerArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Update deploy request throttler configurations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_deploy_request_throttler_builder()` + `update_deploy_request_throttler_execute()`.
/// For task-level control, use `update_deploy_request_throttler_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_deploy_request_throttler(
    client: &SimpleHttpClient,
    args: &UpdateDeployRequestThrottlerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = update_deploy_request_throttler_builder(
        client,
        &args.organization,
        &args.database,
        &args.number,
    )?;
    update_deploy_request_throttler_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules
/// List maintenance schedules
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_maintenance_schedules_execute()` to send, or `list_maintenance_schedules` for simplest API.

pub fn list_maintenance_schedules_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/maintenance-schedules",
        organization, database,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules
/// List maintenance schedules
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_maintenance_schedules_execute()` or `list_maintenance_schedules`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_maintenance_schedules_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_maintenance_schedules_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules
/// List maintenance schedules
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_maintenance_schedules_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_maintenance_schedules_task()`.
/// For the simplest API, use `list_maintenance_schedules()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_maintenance_schedules_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_maintenance_schedules_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_maintenance_schedules_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_maintenance_schedules`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListMaintenanceSchedulesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules
/// List maintenance schedules
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_maintenance_schedules_builder()` + `list_maintenance_schedules_execute()`.
/// For task-level control, use `list_maintenance_schedules_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_maintenance_schedules(
    client: &SimpleHttpClient,
    args: &ListMaintenanceSchedulesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_maintenance_schedules_builder(
        client,
        &args.organization,
        &args.database,
        &args.page,
        &args.per_page,
    )?;
    list_maintenance_schedules_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}
/// Get a maintenance schedule
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_maintenance_schedule_execute()` to send, or `get_maintenance_schedule` for simplest API.

pub fn get_maintenance_schedule_builder<R>(
    client: &SimpleHttpClient<R>,
    id: &String,
    organization: &String,
    database: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/maintenance-schedules/{}",
        organization, database, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}
/// Get a maintenance schedule
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_maintenance_schedule_execute()` or `get_maintenance_schedule`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_maintenance_schedule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_maintenance_schedule_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}
/// Get a maintenance schedule
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_maintenance_schedule_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_maintenance_schedule_task()`.
/// For the simplest API, use `get_maintenance_schedule()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_maintenance_schedule_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_maintenance_schedule_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_maintenance_schedule_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_maintenance_schedule`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetMaintenanceScheduleArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}
/// Get a maintenance schedule
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_maintenance_schedule_builder()` + `get_maintenance_schedule_execute()`.
/// For task-level control, use `get_maintenance_schedule_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_maintenance_schedule(
    client: &SimpleHttpClient,
    args: &GetMaintenanceScheduleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_maintenance_schedule_builder(client, &args.id, &args.organization, &args.database)?;
    get_maintenance_schedule_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}/windows
/// List maintenance windows
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_maintenance_windows_execute()` to send, or `list_maintenance_windows` for simplest API.

pub fn list_maintenance_windows_builder<R>(
    client: &SimpleHttpClient<R>,
    id: &String,
    organization: &String,
    database: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/maintenance-schedules/{}/windows",
        organization,
        database,
        id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}/windows
/// List maintenance windows
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_maintenance_windows_execute()` or `list_maintenance_windows`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_maintenance_windows_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_maintenance_windows_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}/windows
/// List maintenance windows
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_maintenance_windows_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_maintenance_windows_task()`.
/// For the simplest API, use `list_maintenance_windows()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_maintenance_windows_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_maintenance_windows_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_maintenance_windows_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_maintenance_windows`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListMaintenanceWindowsArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}/windows
/// List maintenance windows
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_maintenance_windows_builder()` + `list_maintenance_windows_execute()`.
/// For task-level control, use `list_maintenance_windows_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_maintenance_windows(
    client: &SimpleHttpClient,
    args: &ListMaintenanceWindowsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_maintenance_windows_builder(
        client,
        &args.id,
        &args.organization,
        &args.database,
        &args.page,
        &args.per_page,
    )?;
    list_maintenance_windows_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/read-only-regions
/// List read-only regions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_read_only_regions_execute()` to send, or `list_read_only_regions` for simplest API.

pub fn list_read_only_regions_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/read-only-regions",
        organization, database,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/read-only-regions
/// List read-only regions
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_read_only_regions_execute()` or `list_read_only_regions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_read_only_regions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_read_only_regions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/read-only-regions
/// List read-only regions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_read_only_regions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_read_only_regions_task()`.
/// For the simplest API, use `list_read_only_regions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_read_only_regions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_read_only_regions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_read_only_regions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_read_only_regions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListReadOnlyRegionsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/read-only-regions
/// List read-only regions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_read_only_regions_builder()` + `list_read_only_regions_execute()`.
/// For task-level control, use `list_read_only_regions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_read_only_regions(
    client: &SimpleHttpClient,
    args: &ListReadOnlyRegionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_read_only_regions_builder(
        client,
        &args.organization,
        &args.database,
        &args.page,
        &args.per_page,
    )?;
    list_read_only_regions_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/regions
/// List database regions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_database_regions_execute()` to send, or `list_database_regions` for simplest API.

pub fn list_database_regions_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/regions",
        organization, database,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/regions
/// List database regions
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_database_regions_execute()` or `list_database_regions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_database_regions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_database_regions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/regions
/// List database regions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_database_regions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_database_regions_task()`.
/// For the simplest API, use `list_database_regions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_database_regions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_database_regions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_database_regions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_database_regions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListDatabaseRegionsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/regions
/// List database regions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_database_regions_builder()` + `list_database_regions_execute()`.
/// For task-level control, use `list_database_regions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_database_regions(
    client: &SimpleHttpClient,
    args: &ListDatabaseRegionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_database_regions_builder(
        client,
        &args.organization,
        &args.database,
        &args.page,
        &args.per_page,
    )?;
    list_database_regions_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations
/// List schema recommendations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_schema_recommendations_execute()` to send, or `list_schema_recommendations` for simplest API.

pub fn list_schema_recommendations_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    state: &Option<String>,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/schema-recommendations",
        organization, database,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = state.as_ref() {
        query_parts.push(format!("state={}", val));
    }
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations
/// List schema recommendations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_schema_recommendations_execute()` or `list_schema_recommendations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_schema_recommendations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_schema_recommendations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations
/// List schema recommendations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_schema_recommendations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_schema_recommendations_task()`.
/// For the simplest API, use `list_schema_recommendations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_schema_recommendations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_schema_recommendations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_schema_recommendations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_schema_recommendations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListSchemaRecommendationsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: state
    pub state: Option<String>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations
/// List schema recommendations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_schema_recommendations_builder()` + `list_schema_recommendations_execute()`.
/// For task-level control, use `list_schema_recommendations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_schema_recommendations(
    client: &SimpleHttpClient,
    args: &ListSchemaRecommendationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_schema_recommendations_builder(
        client,
        &args.organization,
        &args.database,
        &args.state,
        &args.page,
        &args.per_page,
    )?;
    list_schema_recommendations_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations/{number}
/// Get a schema recommendation
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_schema_recommendation_execute()` to send, or `get_schema_recommendation` for simplest API.

pub fn get_schema_recommendation_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/schema-recommendations/{}",
        organization, database, number,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations/{number}
/// Get a schema recommendation
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_schema_recommendation_execute()` or `get_schema_recommendation`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_schema_recommendation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_schema_recommendation_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations/{number}
/// Get a schema recommendation
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_schema_recommendation_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_schema_recommendation_task()`.
/// For the simplest API, use `get_schema_recommendation()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_schema_recommendation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_schema_recommendation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_schema_recommendation_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_schema_recommendation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetSchemaRecommendationArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations/{number}
/// Get a schema recommendation
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_schema_recommendation_builder()` + `get_schema_recommendation_execute()`.
/// For task-level control, use `get_schema_recommendation_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_schema_recommendation(
    client: &SimpleHttpClient,
    args: &GetSchemaRecommendationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_schema_recommendation_builder(
        client,
        &args.organization,
        &args.database,
        &args.number,
    )?;
    get_schema_recommendation_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/schema-recommendations/{number}/dismiss
/// Dismiss a schema recommendation
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dismiss_schema_recommendation_execute()` to send, or `dismiss_schema_recommendation` for simplest API.

pub fn dismiss_schema_recommendation_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/schema-recommendations/{}/dismiss",
        organization,
        database,
        number,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/schema-recommendations/{number}/dismiss
/// Dismiss a schema recommendation
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dismiss_schema_recommendation_execute()` or `dismiss_schema_recommendation`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dismiss_schema_recommendation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dismiss_schema_recommendation_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/schema-recommendations/{number}/dismiss
/// Dismiss a schema recommendation
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dismiss_schema_recommendation_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dismiss_schema_recommendation_task()`.
/// For the simplest API, use `dismiss_schema_recommendation()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dismiss_schema_recommendation_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dismiss_schema_recommendation_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dismiss_schema_recommendation_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dismiss_schema_recommendation`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DismissSchemaRecommendationArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// POST /organizations/{organization}/databases/{database}/schema-recommendations/{number}/dismiss
/// Dismiss a schema recommendation
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dismiss_schema_recommendation_builder()` + `dismiss_schema_recommendation_execute()`.
/// For task-level control, use `dismiss_schema_recommendation_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dismiss_schema_recommendation(
    client: &SimpleHttpClient,
    args: &DismissSchemaRecommendationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dismiss_schema_recommendation_builder(
        client,
        &args.organization,
        &args.database,
        &args.number,
    )?;
    dismiss_schema_recommendation_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/throttler
/// Get database throttler configurations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_database_throttler_execute()` to send, or `get_database_throttler` for simplest API.

pub fn get_database_throttler_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/throttler",
        organization, database,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/throttler
/// Get database throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_database_throttler_execute()` or `get_database_throttler`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_database_throttler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_database_throttler_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/throttler
/// Get database throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_database_throttler_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_database_throttler_task()`.
/// For the simplest API, use `get_database_throttler()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_database_throttler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_database_throttler_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_database_throttler_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_database_throttler`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetDatabaseThrottlerArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// GET /organizations/{organization}/databases/{database}/throttler
/// Get database throttler configurations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_database_throttler_builder()` + `get_database_throttler_execute()`.
/// For task-level control, use `get_database_throttler_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_database_throttler(
    client: &SimpleHttpClient,
    args: &GetDatabaseThrottlerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_database_throttler_builder(client, &args.organization, &args.database)?;
    get_database_throttler_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/throttler
/// Update database throttler configurations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_database_throttler_execute()` to send, or `update_database_throttler` for simplest API.

pub fn update_database_throttler_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/throttler",
        organization, database,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/throttler
/// Update database throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_database_throttler_execute()` or `update_database_throttler`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_database_throttler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_database_throttler_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/throttler
/// Update database throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_database_throttler_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_database_throttler_task()`.
/// For the simplest API, use `update_database_throttler()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_database_throttler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_database_throttler_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_database_throttler_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_database_throttler`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateDatabaseThrottlerArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// PATCH /organizations/{organization}/databases/{database}/throttler
/// Update database throttler configurations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_database_throttler_builder()` + `update_database_throttler_execute()`.
/// For task-level control, use `update_database_throttler_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_database_throttler(
    client: &SimpleHttpClient,
    args: &UpdateDatabaseThrottlerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = update_database_throttler_builder(client, &args.organization, &args.database)?;
    update_database_throttler_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/webhooks
/// List webhooks
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_webhooks_execute()` to send, or `list_webhooks` for simplest API.

pub fn list_webhooks_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/webhooks",
        organization, database,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/webhooks
/// List webhooks
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_webhooks_execute()` or `list_webhooks`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_webhooks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_webhooks_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/webhooks
/// List webhooks
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_webhooks_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_webhooks_task()`.
/// For the simplest API, use `list_webhooks()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_webhooks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_webhooks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_webhooks_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_webhooks`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListWebhooksArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/webhooks
/// List webhooks
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_webhooks_builder()` + `list_webhooks_execute()`.
/// For task-level control, use `list_webhooks_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_webhooks(
    client: &SimpleHttpClient,
    args: &ListWebhooksArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_webhooks_builder(
        client,
        &args.organization,
        &args.database,
        &args.page,
        &args.per_page,
    )?;
    list_webhooks_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/webhooks
/// Create a webhook
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `create_webhook_execute()` to send, or `create_webhook` for simplest API.

pub fn create_webhook_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/webhooks",
        organization, database,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/webhooks
/// Create a webhook
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `create_webhook_execute()` or `create_webhook`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_webhook_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_webhook_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/webhooks
/// Create a webhook
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `create_webhook_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `create_webhook_task()`.
/// For the simplest API, use `create_webhook()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_webhook_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn create_webhook_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = create_webhook_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`create_webhook`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CreateWebhookArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// POST /organizations/{organization}/databases/{database}/webhooks
/// Create a webhook
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `create_webhook_builder()` + `create_webhook_execute()`.
/// For task-level control, use `create_webhook_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_webhook(
    client: &SimpleHttpClient,
    args: &CreateWebhookArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = create_webhook_builder(client, &args.organization, &args.database)?;
    create_webhook_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/webhooks/{id}
/// Get a webhook
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_webhook_execute()` to send, or `get_webhook` for simplest API.

pub fn get_webhook_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/webhooks/{}",
        organization, database, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/webhooks/{id}
/// Get a webhook
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_webhook_execute()` or `get_webhook`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_webhook_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_webhook_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/webhooks/{id}
/// Get a webhook
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_webhook_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_webhook_task()`.
/// For the simplest API, use `get_webhook()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_webhook_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_webhook_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_webhook_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_webhook`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetWebhookArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/databases/{database}/webhooks/{id}
/// Get a webhook
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_webhook_builder()` + `get_webhook_execute()`.
/// For task-level control, use `get_webhook_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_webhook(
    client: &SimpleHttpClient,
    args: &GetWebhookArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_webhook_builder(client, &args.organization, &args.database, &args.id)?;
    get_webhook_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/webhooks/{id}
/// Update a webhook
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_webhook_execute()` to send, or `update_webhook` for simplest API.

pub fn update_webhook_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/webhooks/{}",
        organization, database, id,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/webhooks/{id}
/// Update a webhook
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_webhook_execute()` or `update_webhook`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_webhook_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_webhook_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/webhooks/{id}
/// Update a webhook
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_webhook_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_webhook_task()`.
/// For the simplest API, use `update_webhook()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_webhook_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_webhook_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_webhook_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_webhook`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateWebhookArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: id
    pub id: String,
}

/// PATCH /organizations/{organization}/databases/{database}/webhooks/{id}
/// Update a webhook
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_webhook_builder()` + `update_webhook_execute()`.
/// For task-level control, use `update_webhook_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_webhook(
    client: &SimpleHttpClient,
    args: &UpdateWebhookArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = update_webhook_builder(client, &args.organization, &args.database, &args.id)?;
    update_webhook_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/webhooks/{id}
/// Delete a webhook
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_webhook_execute()` to send, or `delete_webhook` for simplest API.

pub fn delete_webhook_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/webhooks/{}",
        organization, database, id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/webhooks/{id}
/// Delete a webhook
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_webhook_execute()` or `delete_webhook`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_webhook_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_webhook_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/webhooks/{id}
/// Delete a webhook
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_webhook_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_webhook_task()`.
/// For the simplest API, use `delete_webhook()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_webhook_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_webhook_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_webhook_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_webhook`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteWebhookArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /organizations/{organization}/databases/{database}/webhooks/{id}
/// Delete a webhook
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_webhook_builder()` + `delete_webhook_execute()`.
/// For task-level control, use `delete_webhook_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_webhook(
    client: &SimpleHttpClient,
    args: &DeleteWebhookArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_webhook_builder(client, &args.organization, &args.database, &args.id)?;
    delete_webhook_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/webhooks/{id}/test
/// Test a webhook
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `test_webhook_execute()` to send, or `test_webhook` for simplest API.

pub fn test_webhook_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/webhooks/{}/test",
        organization, database, id,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/webhooks/{id}/test
/// Test a webhook
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `test_webhook_execute()` or `test_webhook`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `test_webhook_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn test_webhook_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/webhooks/{id}/test
/// Test a webhook
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `test_webhook_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `test_webhook_task()`.
/// For the simplest API, use `test_webhook()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `test_webhook_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn test_webhook_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = test_webhook_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`test_webhook`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TestWebhookArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /organizations/{organization}/databases/{database}/webhooks/{id}/test
/// Test a webhook
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `test_webhook_builder()` + `test_webhook_execute()`.
/// For task-level control, use `test_webhook_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn test_webhook(
    client: &SimpleHttpClient,
    args: &TestWebhookArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = test_webhook_builder(client, &args.organization, &args.database, &args.id)?;
    test_webhook_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/workflows
/// List workflows
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_workflows_execute()` to send, or `list_workflows` for simplest API.

pub fn list_workflows_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    between: &Option<String>,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows",
        organization, database,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = between.as_ref() {
        query_parts.push(format!("between={}", val));
    }
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/workflows
/// List workflows
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_workflows_execute()` or `list_workflows`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_workflows_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_workflows_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/workflows
/// List workflows
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_workflows_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_workflows_task()`.
/// For the simplest API, use `list_workflows()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_workflows_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_workflows_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_workflows_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_workflows`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListWorkflowsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: between
    pub between: Option<String>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/workflows
/// List workflows
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_workflows_builder()` + `list_workflows_execute()`.
/// For task-level control, use `list_workflows_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_workflows(
    client: &SimpleHttpClient,
    args: &ListWorkflowsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_workflows_builder(
        client,
        &args.organization,
        &args.database,
        &args.between,
        &args.page,
        &args.per_page,
    )?;
    list_workflows_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/workflows
/// Create a workflow
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `create_workflow_execute()` to send, or `create_workflow` for simplest API.

pub fn create_workflow_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows",
        organization, database,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/workflows
/// Create a workflow
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `create_workflow_execute()` or `create_workflow`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_workflow_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_workflow_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/workflows
/// Create a workflow
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `create_workflow_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `create_workflow_task()`.
/// For the simplest API, use `create_workflow()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_workflow_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn create_workflow_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = create_workflow_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`create_workflow`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CreateWorkflowArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// POST /organizations/{organization}/databases/{database}/workflows
/// Create a workflow
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `create_workflow_builder()` + `create_workflow_execute()`.
/// For task-level control, use `create_workflow_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_workflow(
    client: &SimpleHttpClient,
    args: &CreateWorkflowArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = create_workflow_builder(client, &args.organization, &args.database)?;
    create_workflow_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/workflows/{number}
/// Get a workflow
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_workflow_execute()` to send, or `get_workflow` for simplest API.

pub fn get_workflow_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}",
        organization, database, number,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/workflows/{number}
/// Get a workflow
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_workflow_execute()` or `get_workflow`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_workflow_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_workflow_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/workflows/{number}
/// Get a workflow
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_workflow_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_workflow_task()`.
/// For the simplest API, use `get_workflow()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_workflow_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_workflow_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_workflow_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_workflow`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetWorkflowArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// GET /organizations/{organization}/databases/{database}/workflows/{number}
/// Get a workflow
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_workflow_builder()` + `get_workflow_execute()`.
/// For task-level control, use `get_workflow_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_workflow(
    client: &SimpleHttpClient,
    args: &GetWorkflowArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_workflow_builder(client, &args.organization, &args.database, &args.number)?;
    get_workflow_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/workflows/{number}
/// Cancel a workflow
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workflow_cancel_execute()` to send, or `workflow_cancel` for simplest API.

pub fn workflow_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}",
        organization, database, number,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/workflows/{number}
/// Cancel a workflow
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `workflow_cancel_execute()` or `workflow_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `workflow_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workflow_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/workflows/{number}
/// Cancel a workflow
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `workflow_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `workflow_cancel_task()`.
/// For the simplest API, use `workflow_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `workflow_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn workflow_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = workflow_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`workflow_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WorkflowCancelArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// DELETE /organizations/{organization}/databases/{database}/workflows/{number}
/// Cancel a workflow
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workflow_cancel_builder()` + `workflow_cancel_execute()`.
/// For task-level control, use `workflow_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workflow_cancel(
    client: &SimpleHttpClient,
    args: &WorkflowCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        workflow_cancel_builder(client, &args.organization, &args.database, &args.number)?;
    workflow_cancel_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/complete
/// Complete a workflow
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workflow_complete_execute()` to send, or `workflow_complete` for simplest API.

pub fn workflow_complete_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}/complete",
        organization, database, number,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/complete
/// Complete a workflow
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `workflow_complete_execute()` or `workflow_complete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `workflow_complete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workflow_complete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/complete
/// Complete a workflow
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `workflow_complete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `workflow_complete_task()`.
/// For the simplest API, use `workflow_complete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `workflow_complete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn workflow_complete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = workflow_complete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`workflow_complete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WorkflowCompleteArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/complete
/// Complete a workflow
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workflow_complete_builder()` + `workflow_complete_execute()`.
/// For task-level control, use `workflow_complete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workflow_complete(
    client: &SimpleHttpClient,
    args: &WorkflowCompleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        workflow_complete_builder(client, &args.organization, &args.database, &args.number)?;
    workflow_complete_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/cutover
/// Cutover traffic
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workflow_cutover_execute()` to send, or `workflow_cutover` for simplest API.

pub fn workflow_cutover_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}/cutover",
        organization, database, number,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/cutover
/// Cutover traffic
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `workflow_cutover_execute()` or `workflow_cutover`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `workflow_cutover_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workflow_cutover_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/cutover
/// Cutover traffic
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `workflow_cutover_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `workflow_cutover_task()`.
/// For the simplest API, use `workflow_cutover()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `workflow_cutover_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn workflow_cutover_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = workflow_cutover_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`workflow_cutover`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WorkflowCutoverArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/cutover
/// Cutover traffic
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workflow_cutover_builder()` + `workflow_cutover_execute()`.
/// For task-level control, use `workflow_cutover_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workflow_cutover(
    client: &SimpleHttpClient,
    args: &WorkflowCutoverArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        workflow_cutover_builder(client, &args.organization, &args.database, &args.number)?;
    workflow_cutover_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/retry
/// Retry a failed workflow
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workflow_retry_execute()` to send, or `workflow_retry` for simplest API.

pub fn workflow_retry_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}/retry",
        organization, database, number,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/retry
/// Retry a failed workflow
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `workflow_retry_execute()` or `workflow_retry`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `workflow_retry_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workflow_retry_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/retry
/// Retry a failed workflow
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `workflow_retry_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `workflow_retry_task()`.
/// For the simplest API, use `workflow_retry()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `workflow_retry_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn workflow_retry_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = workflow_retry_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`workflow_retry`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WorkflowRetryArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/retry
/// Retry a failed workflow
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workflow_retry_builder()` + `workflow_retry_execute()`.
/// For task-level control, use `workflow_retry_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workflow_retry(
    client: &SimpleHttpClient,
    args: &WorkflowRetryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = workflow_retry_builder(client, &args.organization, &args.database, &args.number)?;
    workflow_retry_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-cutover
/// Reverse traffic cutover
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workflow_reverse_cutover_execute()` to send, or `workflow_reverse_cutover` for simplest API.

pub fn workflow_reverse_cutover_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}/reverse-cutover",
        organization, database, number,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-cutover
/// Reverse traffic cutover
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `workflow_reverse_cutover_execute()` or `workflow_reverse_cutover`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `workflow_reverse_cutover_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workflow_reverse_cutover_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-cutover
/// Reverse traffic cutover
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `workflow_reverse_cutover_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `workflow_reverse_cutover_task()`.
/// For the simplest API, use `workflow_reverse_cutover()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `workflow_reverse_cutover_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn workflow_reverse_cutover_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = workflow_reverse_cutover_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`workflow_reverse_cutover`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WorkflowReverseCutoverArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-cutover
/// Reverse traffic cutover
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workflow_reverse_cutover_builder()` + `workflow_reverse_cutover_execute()`.
/// For task-level control, use `workflow_reverse_cutover_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workflow_reverse_cutover(
    client: &SimpleHttpClient,
    args: &WorkflowReverseCutoverArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        workflow_reverse_cutover_builder(client, &args.organization, &args.database, &args.number)?;
    workflow_reverse_cutover_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-traffic
/// Reverse traffic
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workflow_reverse_traffic_execute()` to send, or `workflow_reverse_traffic` for simplest API.

pub fn workflow_reverse_traffic_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}/reverse-traffic",
        organization, database, number,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-traffic
/// Reverse traffic
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `workflow_reverse_traffic_execute()` or `workflow_reverse_traffic`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `workflow_reverse_traffic_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workflow_reverse_traffic_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-traffic
/// Reverse traffic
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `workflow_reverse_traffic_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `workflow_reverse_traffic_task()`.
/// For the simplest API, use `workflow_reverse_traffic()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `workflow_reverse_traffic_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn workflow_reverse_traffic_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = workflow_reverse_traffic_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`workflow_reverse_traffic`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WorkflowReverseTrafficArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-traffic
/// Reverse traffic
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workflow_reverse_traffic_builder()` + `workflow_reverse_traffic_execute()`.
/// For task-level control, use `workflow_reverse_traffic_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workflow_reverse_traffic(
    client: &SimpleHttpClient,
    args: &WorkflowReverseTrafficArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        workflow_reverse_traffic_builder(client, &args.organization, &args.database, &args.number)?;
    workflow_reverse_traffic_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-primaries
/// Switch primary traffic
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workflow_switch_primaries_execute()` to send, or `workflow_switch_primaries` for simplest API.

pub fn workflow_switch_primaries_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}/switch-primaries",
        organization,
        database,
        number,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-primaries
/// Switch primary traffic
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `workflow_switch_primaries_execute()` or `workflow_switch_primaries`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `workflow_switch_primaries_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workflow_switch_primaries_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-primaries
/// Switch primary traffic
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `workflow_switch_primaries_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `workflow_switch_primaries_task()`.
/// For the simplest API, use `workflow_switch_primaries()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `workflow_switch_primaries_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn workflow_switch_primaries_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = workflow_switch_primaries_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`workflow_switch_primaries`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WorkflowSwitchPrimariesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-primaries
/// Switch primary traffic
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workflow_switch_primaries_builder()` + `workflow_switch_primaries_execute()`.
/// For task-level control, use `workflow_switch_primaries_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workflow_switch_primaries(
    client: &SimpleHttpClient,
    args: &WorkflowSwitchPrimariesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = workflow_switch_primaries_builder(
        client,
        &args.organization,
        &args.database,
        &args.number,
    )?;
    workflow_switch_primaries_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-replicas
/// Switch replica traffic
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `workflow_switch_replicas_execute()` to send, or `workflow_switch_replicas` for simplest API.

pub fn workflow_switch_replicas_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}/switch-replicas",
        organization, database, number,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-replicas
/// Switch replica traffic
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `workflow_switch_replicas_execute()` or `workflow_switch_replicas`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `workflow_switch_replicas_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workflow_switch_replicas_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-replicas
/// Switch replica traffic
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `workflow_switch_replicas_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `workflow_switch_replicas_task()`.
/// For the simplest API, use `workflow_switch_replicas()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `workflow_switch_replicas_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn workflow_switch_replicas_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = workflow_switch_replicas_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`workflow_switch_replicas`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WorkflowSwitchReplicasArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-replicas
/// Switch replica traffic
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `workflow_switch_replicas_builder()` + `workflow_switch_replicas_execute()`.
/// For task-level control, use `workflow_switch_replicas_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn workflow_switch_replicas(
    client: &SimpleHttpClient,
    args: &WorkflowSwitchReplicasArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        workflow_switch_replicas_builder(client, &args.organization, &args.database, &args.number)?;
    workflow_switch_replicas_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/verify-data
/// Verify workflow data
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `verify_workflow_execute()` to send, or `verify_workflow` for simplest API.

pub fn verify_workflow_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    database: &String,
    number: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}/verify-data",
        organization, database, number,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/verify-data
/// Verify workflow data
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `verify_workflow_execute()` or `verify_workflow`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `verify_workflow_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn verify_workflow_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/verify-data
/// Verify workflow data
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `verify_workflow_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `verify_workflow_task()`.
/// For the simplest API, use `verify_workflow()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `verify_workflow_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn verify_workflow_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = verify_workflow_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`verify_workflow`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VerifyWorkflowArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/verify-data
/// Verify workflow data
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `verify_workflow_builder()` + `verify_workflow_execute()`.
/// For task-level control, use `verify_workflow_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn verify_workflow(
    client: &SimpleHttpClient,
    args: &VerifyWorkflowArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        verify_workflow_builder(client, &args.organization, &args.database, &args.number)?;
    verify_workflow_execute(builder)
}

/// GET /organizations/{organization}/invoices
/// Get invoices
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_invoices_execute()` to send, or `list_invoices` for simplest API.

pub fn list_invoices_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/invoices",
        organization,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/invoices
/// Get invoices
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_invoices_execute()` or `list_invoices`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_invoices_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_invoices_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/invoices
/// Get invoices
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_invoices_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_invoices_task()`.
/// For the simplest API, use `list_invoices()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_invoices_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_invoices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_invoices_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_invoices`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListInvoicesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/invoices
/// Get invoices
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_invoices_builder()` + `list_invoices_execute()`.
/// For task-level control, use `list_invoices_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_invoices(
    client: &SimpleHttpClient,
    args: &ListInvoicesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_invoices_builder(client, &args.organization, &args.page, &args.per_page)?;
    list_invoices_execute(builder)
}

/// GET /organizations/{organization}/invoices/{id}
/// Get an invoice
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_invoice_execute()` to send, or `get_invoice` for simplest API.

pub fn get_invoice_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/invoices/{}",
        organization, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/invoices/{id}
/// Get an invoice
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_invoice_execute()` or `get_invoice`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_invoice_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_invoice_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/invoices/{id}
/// Get an invoice
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_invoice_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_invoice_task()`.
/// For the simplest API, use `get_invoice()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_invoice_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_invoice_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_invoice_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_invoice`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetInvoiceArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/invoices/{id}
/// Get an invoice
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_invoice_builder()` + `get_invoice_execute()`.
/// For task-level control, use `get_invoice_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_invoice(
    client: &SimpleHttpClient,
    args: &GetInvoiceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_invoice_builder(client, &args.organization, &args.id)?;
    get_invoice_execute(builder)
}

/// GET /organizations/{organization}/invoices/{id}/line-items
/// Get invoice line items
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_invoice_line_items_execute()` to send, or `get_invoice_line_items` for simplest API.

pub fn get_invoice_line_items_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    id: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/invoices/{}/line-items",
        organization, id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/invoices/{id}/line-items
/// Get invoice line items
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_invoice_line_items_execute()` or `get_invoice_line_items`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_invoice_line_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_invoice_line_items_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/invoices/{id}/line-items
/// Get invoice line items
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_invoice_line_items_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_invoice_line_items_task()`.
/// For the simplest API, use `get_invoice_line_items()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_invoice_line_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_invoice_line_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_invoice_line_items_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_invoice_line_items`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetInvoiceLineItemsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/invoices/{id}/line-items
/// Get invoice line items
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_invoice_line_items_builder()` + `get_invoice_line_items_execute()`.
/// For task-level control, use `get_invoice_line_items_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_invoice_line_items(
    client: &SimpleHttpClient,
    args: &GetInvoiceLineItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_invoice_line_items_builder(
        client,
        &args.organization,
        &args.id,
        &args.page,
        &args.per_page,
    )?;
    get_invoice_line_items_execute(builder)
}

/// GET /organizations/{organization}/members
/// List organization members
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_organization_members_execute()` to send, or `list_organization_members` for simplest API.

pub fn list_organization_members_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    q: &Option<String>,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/members",
        organization,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = q.as_ref() {
        query_parts.push(format!("q={}", val));
    }
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/members
/// List organization members
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_organization_members_execute()` or `list_organization_members`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_organization_members_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_organization_members_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/members
/// List organization members
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_organization_members_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_organization_members_task()`.
/// For the simplest API, use `list_organization_members()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_organization_members_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_organization_members_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_organization_members_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_organization_members`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListOrganizationMembersArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Query parameter: q
    pub q: Option<String>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/members
/// List organization members
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_organization_members_builder()` + `list_organization_members_execute()`.
/// For task-level control, use `list_organization_members_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_organization_members(
    client: &SimpleHttpClient,
    args: &ListOrganizationMembersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_organization_members_builder(
        client,
        &args.organization,
        &args.q,
        &args.page,
        &args.per_page,
    )?;
    list_organization_members_execute(builder)
}

/// GET /organizations/{organization}/members/{id}
/// Get an organization member
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organization_membership_execute()` to send, or `get_organization_membership` for simplest API.

pub fn get_organization_membership_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/members/{}",
        organization, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/members/{id}
/// Get an organization member
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organization_membership_execute()` or `get_organization_membership`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organization_membership_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organization_membership_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/members/{id}
/// Get an organization member
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organization_membership_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organization_membership_task()`.
/// For the simplest API, use `get_organization_membership()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organization_membership_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organization_membership_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organization_membership_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organization_membership`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationMembershipArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/members/{id}
/// Get an organization member
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organization_membership_builder()` + `get_organization_membership_execute()`.
/// For task-level control, use `get_organization_membership_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organization_membership(
    client: &SimpleHttpClient,
    args: &GetOrganizationMembershipArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organization_membership_builder(client, &args.organization, &args.id)?;
    get_organization_membership_execute(builder)
}

/// PATCH /organizations/{organization}/members/{id}
/// Update organization member role
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_organization_membership_execute()` to send, or `update_organization_membership` for simplest API.

pub fn update_organization_membership_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/members/{}",
        organization, id,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/members/{id}
/// Update organization member role
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_organization_membership_execute()` or `update_organization_membership`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_organization_membership_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_organization_membership_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/members/{id}
/// Update organization member role
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_organization_membership_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_organization_membership_task()`.
/// For the simplest API, use `update_organization_membership()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_organization_membership_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_organization_membership_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_organization_membership_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_organization_membership`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateOrganizationMembershipArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: id
    pub id: String,
}

/// PATCH /organizations/{organization}/members/{id}
/// Update organization member role
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_organization_membership_builder()` + `update_organization_membership_execute()`.
/// For task-level control, use `update_organization_membership_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_organization_membership(
    client: &SimpleHttpClient,
    args: &UpdateOrganizationMembershipArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = update_organization_membership_builder(client, &args.organization, &args.id)?;
    update_organization_membership_execute(builder)
}

/// DELETE /organizations/{organization}/members/{id}
/// Remove a member from an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `remove_organization_member_execute()` to send, or `remove_organization_member` for simplest API.

pub fn remove_organization_member_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    id: &String,
    delete_passwords: &Option<String>,
    delete_service_tokens: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/members/{}",
        organization, id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = delete_passwords.as_ref() {
        query_parts.push(format!("delete_passwords={}", val));
    }
    if let Some(val) = delete_service_tokens.as_ref() {
        query_parts.push(format!("delete_service_tokens={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/members/{id}
/// Remove a member from an organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `remove_organization_member_execute()` or `remove_organization_member`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `remove_organization_member_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn remove_organization_member_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/members/{id}
/// Remove a member from an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `remove_organization_member_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `remove_organization_member_task()`.
/// For the simplest API, use `remove_organization_member()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `remove_organization_member_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn remove_organization_member_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = remove_organization_member_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`remove_organization_member`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RemoveOrganizationMemberArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: delete_passwords
    pub delete_passwords: Option<String>,
    /// Query parameter: delete_service_tokens
    pub delete_service_tokens: Option<String>,
}

/// DELETE /organizations/{organization}/members/{id}
/// Remove a member from an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `remove_organization_member_builder()` + `remove_organization_member_execute()`.
/// For task-level control, use `remove_organization_member_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn remove_organization_member(
    client: &SimpleHttpClient,
    args: &RemoveOrganizationMemberArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = remove_organization_member_builder(
        client,
        &args.organization,
        &args.id,
        &args.delete_passwords,
        &args.delete_service_tokens,
    )?;
    remove_organization_member_execute(builder)
}

/// GET /organizations/{organization}/oauth-applications
/// List OAuth applications
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_oauth_applications_execute()` to send, or `list_oauth_applications` for simplest API.

pub fn list_oauth_applications_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/oauth-applications",
        organization,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/oauth-applications
/// List OAuth applications
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_oauth_applications_execute()` or `list_oauth_applications`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_oauth_applications_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_oauth_applications_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/oauth-applications
/// List OAuth applications
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_oauth_applications_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_oauth_applications_task()`.
/// For the simplest API, use `list_oauth_applications()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_oauth_applications_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_oauth_applications_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_oauth_applications_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_oauth_applications`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListOauthApplicationsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/oauth-applications
/// List OAuth applications
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_oauth_applications_builder()` + `list_oauth_applications_execute()`.
/// For task-level control, use `list_oauth_applications_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_oauth_applications(
    client: &SimpleHttpClient,
    args: &ListOauthApplicationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        list_oauth_applications_builder(client, &args.organization, &args.page, &args.per_page)?;
    list_oauth_applications_execute(builder)
}

/// GET /organizations/{organization}/oauth-applications/{application_id}
/// Get an OAuth application
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_oauth_application_execute()` to send, or `get_oauth_application` for simplest API.

pub fn get_oauth_application_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    application_id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/oauth-applications/{}",
        organization, application_id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/oauth-applications/{application_id}
/// Get an OAuth application
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_oauth_application_execute()` or `get_oauth_application`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_oauth_application_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_oauth_application_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/oauth-applications/{application_id}
/// Get an OAuth application
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_oauth_application_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_oauth_application_task()`.
/// For the simplest API, use `get_oauth_application()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_oauth_application_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_oauth_application_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_oauth_application_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_oauth_application`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOauthApplicationArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: application_id
    pub application_id: String,
}

/// GET /organizations/{organization}/oauth-applications/{application_id}
/// Get an OAuth application
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_oauth_application_builder()` + `get_oauth_application_execute()`.
/// For task-level control, use `get_oauth_application_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_oauth_application(
    client: &SimpleHttpClient,
    args: &GetOauthApplicationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_oauth_application_builder(client, &args.organization, &args.application_id)?;
    get_oauth_application_execute(builder)
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens
/// List OAuth tokens
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_oauth_tokens_execute()` to send, or `list_oauth_tokens` for simplest API.

pub fn list_oauth_tokens_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    application_id: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/oauth-applications/{}/tokens",
        organization, application_id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens
/// List OAuth tokens
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_oauth_tokens_execute()` or `list_oauth_tokens`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_oauth_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_oauth_tokens_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens
/// List OAuth tokens
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_oauth_tokens_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_oauth_tokens_task()`.
/// For the simplest API, use `list_oauth_tokens()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_oauth_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_oauth_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_oauth_tokens_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_oauth_tokens`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListOauthTokensArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: application_id
    pub application_id: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens
/// List OAuth tokens
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_oauth_tokens_builder()` + `list_oauth_tokens_execute()`.
/// For task-level control, use `list_oauth_tokens_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_oauth_tokens(
    client: &SimpleHttpClient,
    args: &ListOauthTokensArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_oauth_tokens_builder(
        client,
        &args.organization,
        &args.application_id,
        &args.page,
        &args.per_page,
    )?;
    list_oauth_tokens_execute(builder)
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Get an OAuth token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_oauth_token_execute()` to send, or `get_oauth_token` for simplest API.

pub fn get_oauth_token_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    application_id: &String,
    token_id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/oauth-applications/{}/tokens/{}",
        organization, application_id, token_id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Get an OAuth token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_oauth_token_execute()` or `get_oauth_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_oauth_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_oauth_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Get an OAuth token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_oauth_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_oauth_token_task()`.
/// For the simplest API, use `get_oauth_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_oauth_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_oauth_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_oauth_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_oauth_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOauthTokenArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: application_id
    pub application_id: String,
    /// Path parameter: token_id
    pub token_id: String,
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Get an OAuth token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_oauth_token_builder()` + `get_oauth_token_execute()`.
/// For task-level control, use `get_oauth_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_oauth_token(
    client: &SimpleHttpClient,
    args: &GetOauthTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_oauth_token_builder(
        client,
        &args.organization,
        &args.application_id,
        &args.token_id,
    )?;
    get_oauth_token_execute(builder)
}

/// DELETE /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Delete an OAuth token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_oauth_token_execute()` to send, or `delete_oauth_token` for simplest API.

pub fn delete_oauth_token_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    application_id: &String,
    token_id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/oauth-applications/{}/tokens/{}",
        organization, application_id, token_id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Delete an OAuth token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_oauth_token_execute()` or `delete_oauth_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_oauth_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_oauth_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Delete an OAuth token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_oauth_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_oauth_token_task()`.
/// For the simplest API, use `delete_oauth_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_oauth_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_oauth_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_oauth_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_oauth_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOauthTokenArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: application_id
    pub application_id: String,
    /// Path parameter: token_id
    pub token_id: String,
}

/// DELETE /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Delete an OAuth token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_oauth_token_builder()` + `delete_oauth_token_execute()`.
/// For task-level control, use `delete_oauth_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_oauth_token(
    client: &SimpleHttpClient,
    args: &DeleteOauthTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_oauth_token_builder(
        client,
        &args.organization,
        &args.application_id,
        &args.token_id,
    )?;
    delete_oauth_token_execute(builder)
}

/// POST /organizations/{organization}/oauth-applications/{id}/token
/// Create or renew an OAuth token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `create_oauth_token_execute()` to send, or `create_oauth_token` for simplest API.

pub fn create_oauth_token_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/oauth-applications/{}/token",
        organization, id,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/oauth-applications/{id}/token
/// Create or renew an OAuth token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `create_oauth_token_execute()` or `create_oauth_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_oauth_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_oauth_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/oauth-applications/{id}/token
/// Create or renew an OAuth token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `create_oauth_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `create_oauth_token_task()`.
/// For the simplest API, use `create_oauth_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_oauth_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn create_oauth_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = create_oauth_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`create_oauth_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CreateOauthTokenArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /organizations/{organization}/oauth-applications/{id}/token
/// Create or renew an OAuth token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `create_oauth_token_builder()` + `create_oauth_token_execute()`.
/// For task-level control, use `create_oauth_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_oauth_token(
    client: &SimpleHttpClient,
    args: &CreateOauthTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = create_oauth_token_builder(client, &args.organization, &args.id)?;
    create_oauth_token_execute(builder)
}

/// GET /organizations/{organization}/regions
/// List regions for an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_regions_for_organization_execute()` to send, or `list_regions_for_organization` for simplest API.

pub fn list_regions_for_organization_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/regions",
        organization,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/regions
/// List regions for an organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_regions_for_organization_execute()` or `list_regions_for_organization`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_regions_for_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_regions_for_organization_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/regions
/// List regions for an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_regions_for_organization_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_regions_for_organization_task()`.
/// For the simplest API, use `list_regions_for_organization()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_regions_for_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_regions_for_organization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_regions_for_organization_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_regions_for_organization`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListRegionsForOrganizationArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/regions
/// List regions for an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_regions_for_organization_builder()` + `list_regions_for_organization_execute()`.
/// For task-level control, use `list_regions_for_organization_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_regions_for_organization(
    client: &SimpleHttpClient,
    args: &ListRegionsForOrganizationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_regions_for_organization_builder(
        client,
        &args.organization,
        &args.page,
        &args.per_page,
    )?;
    list_regions_for_organization_execute(builder)
}

/// GET /organizations/{organization}/service-tokens
/// List service tokens
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_service_tokens_execute()` to send, or `list_service_tokens` for simplest API.

pub fn list_service_tokens_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/service-tokens",
        organization,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/service-tokens
/// List service tokens
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_service_tokens_execute()` or `list_service_tokens`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_service_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_service_tokens_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/service-tokens
/// List service tokens
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_service_tokens_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_service_tokens_task()`.
/// For the simplest API, use `list_service_tokens()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_service_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_service_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_service_tokens_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_service_tokens`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListServiceTokensArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/service-tokens
/// List service tokens
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_service_tokens_builder()` + `list_service_tokens_execute()`.
/// For task-level control, use `list_service_tokens_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_service_tokens(
    client: &SimpleHttpClient,
    args: &ListServiceTokensArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        list_service_tokens_builder(client, &args.organization, &args.page, &args.per_page)?;
    list_service_tokens_execute(builder)
}

/// POST /organizations/{organization}/service-tokens
/// Create a service token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `create_service_token_execute()` to send, or `create_service_token` for simplest API.

pub fn create_service_token_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/service-tokens",
        organization,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/service-tokens
/// Create a service token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `create_service_token_execute()` or `create_service_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_service_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_service_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/service-tokens
/// Create a service token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `create_service_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `create_service_token_task()`.
/// For the simplest API, use `create_service_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_service_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn create_service_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = create_service_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`create_service_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CreateServiceTokenArgs {
    /// Path parameter: organization
    pub organization: String,
}

/// POST /organizations/{organization}/service-tokens
/// Create a service token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `create_service_token_builder()` + `create_service_token_execute()`.
/// For task-level control, use `create_service_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_service_token(
    client: &SimpleHttpClient,
    args: &CreateServiceTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = create_service_token_builder(client, &args.organization)?;
    create_service_token_execute(builder)
}

/// GET /organizations/{organization}/service-tokens/{id}
/// Get a service token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_service_token_execute()` to send, or `get_service_token` for simplest API.

pub fn get_service_token_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/service-tokens/{}",
        organization, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/service-tokens/{id}
/// Get a service token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_service_token_execute()` or `get_service_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_service_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_service_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/service-tokens/{id}
/// Get a service token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_service_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_service_token_task()`.
/// For the simplest API, use `get_service_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_service_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_service_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_service_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_service_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetServiceTokenArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/service-tokens/{id}
/// Get a service token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_service_token_builder()` + `get_service_token_execute()`.
/// For task-level control, use `get_service_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_service_token(
    client: &SimpleHttpClient,
    args: &GetServiceTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_service_token_builder(client, &args.organization, &args.id)?;
    get_service_token_execute(builder)
}

/// DELETE /organizations/{organization}/service-tokens/{id}
/// Delete a service token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_service_token_execute()` to send, or `delete_service_token` for simplest API.

pub fn delete_service_token_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/service-tokens/{}",
        organization, id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/service-tokens/{id}
/// Delete a service token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_service_token_execute()` or `delete_service_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_service_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_service_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/service-tokens/{id}
/// Delete a service token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_service_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_service_token_task()`.
/// For the simplest API, use `delete_service_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_service_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_service_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_service_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_service_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteServiceTokenArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /organizations/{organization}/service-tokens/{id}
/// Delete a service token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_service_token_builder()` + `delete_service_token_execute()`.
/// For task-level control, use `delete_service_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_service_token(
    client: &SimpleHttpClient,
    args: &DeleteServiceTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_service_token_builder(client, &args.organization, &args.id)?;
    delete_service_token_execute(builder)
}

/// GET /organizations/{organization}/teams
/// List teams in an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_organization_teams_execute()` to send, or `list_organization_teams` for simplest API.

pub fn list_organization_teams_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    q: &Option<String>,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/teams",
        organization,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = q.as_ref() {
        query_parts.push(format!("q={}", val));
    }
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/teams
/// List teams in an organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_organization_teams_execute()` or `list_organization_teams`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_organization_teams_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_organization_teams_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/teams
/// List teams in an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_organization_teams_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_organization_teams_task()`.
/// For the simplest API, use `list_organization_teams()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_organization_teams_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_organization_teams_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_organization_teams_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_organization_teams`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListOrganizationTeamsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Query parameter: q
    pub q: Option<String>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/teams
/// List teams in an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_organization_teams_builder()` + `list_organization_teams_execute()`.
/// For task-level control, use `list_organization_teams_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_organization_teams(
    client: &SimpleHttpClient,
    args: &ListOrganizationTeamsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_organization_teams_builder(
        client,
        &args.organization,
        &args.q,
        &args.page,
        &args.per_page,
    )?;
    list_organization_teams_execute(builder)
}

/// POST /organizations/{organization}/teams
/// Create an organization team
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `create_organization_team_execute()` to send, or `create_organization_team` for simplest API.

pub fn create_organization_team_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/teams",
        organization,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/teams
/// Create an organization team
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `create_organization_team_execute()` or `create_organization_team`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_organization_team_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_organization_team_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/teams
/// Create an organization team
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `create_organization_team_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `create_organization_team_task()`.
/// For the simplest API, use `create_organization_team()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `create_organization_team_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn create_organization_team_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = create_organization_team_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`create_organization_team`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CreateOrganizationTeamArgs {
    /// Path parameter: organization
    pub organization: String,
}

/// POST /organizations/{organization}/teams
/// Create an organization team
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `create_organization_team_builder()` + `create_organization_team_execute()`.
/// For task-level control, use `create_organization_team_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn create_organization_team(
    client: &SimpleHttpClient,
    args: &CreateOrganizationTeamArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = create_organization_team_builder(client, &args.organization)?;
    create_organization_team_execute(builder)
}

/// GET /organizations/{organization}/teams/{team}
/// Get an organization team
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organization_team_execute()` to send, or `get_organization_team` for simplest API.

pub fn get_organization_team_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    team: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/teams/{}",
        organization, team,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/teams/{team}
/// Get an organization team
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organization_team_execute()` or `get_organization_team`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organization_team_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organization_team_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/teams/{team}
/// Get an organization team
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organization_team_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organization_team_task()`.
/// For the simplest API, use `get_organization_team()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organization_team_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organization_team_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organization_team_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organization_team`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationTeamArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: team
    pub team: String,
}

/// GET /organizations/{organization}/teams/{team}
/// Get an organization team
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organization_team_builder()` + `get_organization_team_execute()`.
/// For task-level control, use `get_organization_team_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organization_team(
    client: &SimpleHttpClient,
    args: &GetOrganizationTeamArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organization_team_builder(client, &args.organization, &args.team)?;
    get_organization_team_execute(builder)
}

/// PATCH /organizations/{organization}/teams/{team}
/// Update an organization team
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `update_organization_team_execute()` to send, or `update_organization_team` for simplest API.

pub fn update_organization_team_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    team: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/teams/{}",
        organization, team,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/teams/{team}
/// Update an organization team
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `update_organization_team_execute()` or `update_organization_team`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_organization_team_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_organization_team_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/teams/{team}
/// Update an organization team
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `update_organization_team_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `update_organization_team_task()`.
/// For the simplest API, use `update_organization_team()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `update_organization_team_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn update_organization_team_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = update_organization_team_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`update_organization_team`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct UpdateOrganizationTeamArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: team
    pub team: String,
}

/// PATCH /organizations/{organization}/teams/{team}
/// Update an organization team
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `update_organization_team_builder()` + `update_organization_team_execute()`.
/// For task-level control, use `update_organization_team_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn update_organization_team(
    client: &SimpleHttpClient,
    args: &UpdateOrganizationTeamArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = update_organization_team_builder(client, &args.organization, &args.team)?;
    update_organization_team_execute(builder)
}

/// DELETE /organizations/{organization}/teams/{team}
/// Delete an organization team
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organization_team_execute()` to send, or `delete_organization_team` for simplest API.

pub fn delete_organization_team_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    team: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/teams/{}",
        organization, team,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/teams/{team}
/// Delete an organization team
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organization_team_execute()` or `delete_organization_team`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organization_team_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organization_team_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/teams/{team}
/// Delete an organization team
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organization_team_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organization_team_task()`.
/// For the simplest API, use `delete_organization_team()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organization_team_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organization_team_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_organization_team_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organization_team`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationTeamArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: team
    pub team: String,
}

/// DELETE /organizations/{organization}/teams/{team}
/// Delete an organization team
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organization_team_builder()` + `delete_organization_team_execute()`.
/// For task-level control, use `delete_organization_team_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organization_team(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationTeamArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organization_team_builder(client, &args.organization, &args.team)?;
    delete_organization_team_execute(builder)
}

/// GET /organizations/{organization}/teams/{team}/members
/// List team members
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_organization_team_members_execute()` to send, or `list_organization_team_members` for simplest API.

pub fn list_organization_team_members_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    team: &String,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/teams/{}/members",
        organization, team,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/teams/{team}/members
/// List team members
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_organization_team_members_execute()` or `list_organization_team_members`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_organization_team_members_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_organization_team_members_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/teams/{team}/members
/// List team members
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_organization_team_members_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_organization_team_members_task()`.
/// For the simplest API, use `list_organization_team_members()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_organization_team_members_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_organization_team_members_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_organization_team_members_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_organization_team_members`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListOrganizationTeamMembersArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: team
    pub team: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/teams/{team}/members
/// List team members
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_organization_team_members_builder()` + `list_organization_team_members_execute()`.
/// For task-level control, use `list_organization_team_members_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_organization_team_members(
    client: &SimpleHttpClient,
    args: &ListOrganizationTeamMembersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_organization_team_members_builder(
        client,
        &args.organization,
        &args.team,
        &args.page,
        &args.per_page,
    )?;
    list_organization_team_members_execute(builder)
}

/// POST /organizations/{organization}/teams/{team}/members
/// Add a member to a team
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `add_organization_team_member_execute()` to send, or `add_organization_team_member` for simplest API.

pub fn add_organization_team_member_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    team: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/teams/{}/members",
        organization, team,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/teams/{team}/members
/// Add a member to a team
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `add_organization_team_member_execute()` or `add_organization_team_member`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `add_organization_team_member_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn add_organization_team_member_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/teams/{team}/members
/// Add a member to a team
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `add_organization_team_member_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `add_organization_team_member_task()`.
/// For the simplest API, use `add_organization_team_member()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `add_organization_team_member_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn add_organization_team_member_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = add_organization_team_member_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`add_organization_team_member`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AddOrganizationTeamMemberArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: team
    pub team: String,
}

/// POST /organizations/{organization}/teams/{team}/members
/// Add a member to a team
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `add_organization_team_member_builder()` + `add_organization_team_member_execute()`.
/// For task-level control, use `add_organization_team_member_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn add_organization_team_member(
    client: &SimpleHttpClient,
    args: &AddOrganizationTeamMemberArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = add_organization_team_member_builder(client, &args.organization, &args.team)?;
    add_organization_team_member_execute(builder)
}

/// GET /organizations/{organization}/teams/{team}/members/{id}
/// Get a team member
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organization_team_member_execute()` to send, or `get_organization_team_member` for simplest API.

pub fn get_organization_team_member_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    team: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/teams/{}/members/{}",
        organization, team, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/teams/{team}/members/{id}
/// Get a team member
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organization_team_member_execute()` or `get_organization_team_member`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organization_team_member_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organization_team_member_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/teams/{team}/members/{id}
/// Get a team member
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organization_team_member_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organization_team_member_task()`.
/// For the simplest API, use `get_organization_team_member()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organization_team_member_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organization_team_member_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organization_team_member_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organization_team_member`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationTeamMemberArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: team
    pub team: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/teams/{team}/members/{id}
/// Get a team member
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organization_team_member_builder()` + `get_organization_team_member_execute()`.
/// For task-level control, use `get_organization_team_member_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organization_team_member(
    client: &SimpleHttpClient,
    args: &GetOrganizationTeamMemberArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organization_team_member_builder(client, &args.organization, &args.team, &args.id)?;
    get_organization_team_member_execute(builder)
}

/// DELETE /organizations/{organization}/teams/{team}/members/{id}
/// Remove a member from a team
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `remove_organization_team_member_execute()` to send, or `remove_organization_team_member` for simplest API.

pub fn remove_organization_team_member_builder<R>(
    client: &SimpleHttpClient<R>,
    organization: &String,
    team: &String,
    id: &String,
    delete_passwords: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/teams/{}/members/{}",
        organization, team, id,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = delete_passwords.as_ref() {
        query_parts.push(format!("delete_passwords={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/teams/{team}/members/{id}
/// Remove a member from a team
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `remove_organization_team_member_execute()` or `remove_organization_team_member`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `remove_organization_team_member_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn remove_organization_team_member_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/teams/{team}/members/{id}
/// Remove a member from a team
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `remove_organization_team_member_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `remove_organization_team_member_task()`.
/// For the simplest API, use `remove_organization_team_member()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `remove_organization_team_member_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn remove_organization_team_member_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = remove_organization_team_member_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`remove_organization_team_member`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RemoveOrganizationTeamMemberArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: team
    pub team: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: delete_passwords
    pub delete_passwords: Option<String>,
}

/// DELETE /organizations/{organization}/teams/{team}/members/{id}
/// Remove a member from a team
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `remove_organization_team_member_builder()` + `remove_organization_team_member_execute()`.
/// For task-level control, use `remove_organization_team_member_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn remove_organization_team_member(
    client: &SimpleHttpClient,
    args: &RemoveOrganizationTeamMemberArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = remove_organization_team_member_builder(
        client,
        &args.organization,
        &args.team,
        &args.id,
        &args.delete_passwords,
    )?;
    remove_organization_team_member_execute(builder)
}

/// GET /regions
/// List public regions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `list_public_regions_execute()` to send, or `list_public_regions` for simplest API.

pub fn list_public_regions_builder<R>(
    client: &SimpleHttpClient<R>,
    page: &Option<String>,
    per_page: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://api.planetscale.com/v1/regions",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page.as_ref() {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page.as_ref() {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /regions
/// List public regions
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `list_public_regions_execute()` or `list_public_regions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_public_regions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_public_regions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /regions
/// List public regions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `list_public_regions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `list_public_regions_task()`.
/// For the simplest API, use `list_public_regions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `list_public_regions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn list_public_regions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = list_public_regions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`list_public_regions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ListPublicRegionsArgs {
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /regions
/// List public regions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `list_public_regions_builder()` + `list_public_regions_execute()`.
/// For task-level control, use `list_public_regions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn list_public_regions(
    client: &SimpleHttpClient,
    args: &ListPublicRegionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = list_public_regions_builder(client, &args.page, &args.per_page)?;
    list_public_regions_execute(builder)
}

/// GET /user
/// Get current user
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_current_user_execute()` to send, or `get_current_user` for simplest API.

pub fn get_current_user_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://api.planetscale.com/v1/user",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /user
/// Get current user
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_current_user_execute()` or `get_current_user`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_current_user_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_current_user_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /user
/// Get current user
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_current_user_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_current_user_task()`.
/// For the simplest API, use `get_current_user()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_current_user_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_current_user_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_current_user_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /user
/// Get current user
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_current_user_builder()` + `get_current_user_execute()`.
/// For task-level control, use `get_current_user_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_current_user(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_current_user_builder(client)?;
    get_current_user_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with DeleteDatabaseArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DeleteDatabaseArgs> for () {
    fn generate_resource_id(&self, input: &DeleteDatabaseArgs) -> String {
        format!("planetscale::()/{}/{}", input.organization, input.database)
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with DeleteBackupPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DeleteBackupPolicyArgs> for () {
    fn generate_resource_id(&self, input: &DeleteBackupPolicyArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}",
            input.id, input.organization, input.database
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with DeleteBranchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DeleteBranchArgs> for () {
    fn generate_resource_id(&self, input: &DeleteBranchArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}",
            input.organization, input.database, input.branch
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with DeleteBackupArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DeleteBackupArgs> for () {
    fn generate_resource_id(&self, input: &DeleteBackupArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}/{}",
            input.id, input.organization, input.database, input.branch
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with DeleteBouncerArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DeleteBouncerArgs> for () {
    fn generate_resource_id(&self, input: &DeleteBouncerArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}/{}",
            input.organization, input.database, input.branch, input.bouncer
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with CancelBouncerResizeRequestArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CancelBouncerResizeRequestArgs> for () {
    fn generate_resource_id(&self, input: &CancelBouncerResizeRequestArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}/{}",
            input.organization, input.database, input.branch, input.bouncer
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with UpdateBranchChangeRequestArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<UpdateBranchChangeRequestArgs> for () {
    fn generate_resource_id(&self, input: &UpdateBranchChangeRequestArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}",
            input.organization, input.database, input.branch
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with UpdateBranchClusterConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<UpdateBranchClusterConfigArgs> for () {
    fn generate_resource_id(&self, input: &UpdateBranchClusterConfigArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}",
            input.organization, input.database, input.branch
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with DeleteKeyspaceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DeleteKeyspaceArgs> for () {
    fn generate_resource_id(&self, input: &DeleteKeyspaceArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}/{}",
            input.organization, input.database, input.branch, input.keyspace
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with DeletePasswordArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DeletePasswordArgs> for () {
    fn generate_resource_id(&self, input: &DeletePasswordArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}/{}",
            input.organization, input.database, input.branch, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with DeleteQueryPatternsReportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DeleteQueryPatternsReportArgs> for () {
    fn generate_resource_id(&self, input: &DeleteQueryPatternsReportArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}/{}",
            input.organization, input.database, input.branch, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with CancelBranchChangeRequestArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CancelBranchChangeRequestArgs> for () {
    fn generate_resource_id(&self, input: &CancelBranchChangeRequestArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}",
            input.organization, input.database, input.branch
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with DeleteRoleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DeleteRoleArgs> for () {
    fn generate_resource_id(&self, input: &DeleteRoleArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}/{}",
            input.organization, input.database, input.branch, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with ReassignRoleObjectsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ReassignRoleObjectsArgs> for () {
    fn generate_resource_id(&self, input: &ReassignRoleObjectsArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}/{}",
            input.organization, input.database, input.branch, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with DeleteTrafficRuleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DeleteTrafficRuleArgs> for () {
    fn generate_resource_id(&self, input: &DeleteTrafficRuleArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}/{}/{}",
            input.organization, input.database, input.branch, input.budget_id, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with DeleteTrafficBudgetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DeleteTrafficBudgetArgs> for () {
    fn generate_resource_id(&self, input: &DeleteTrafficBudgetArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}/{}",
            input.organization, input.database, input.branch, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with DeleteDatabasePostgresCidrArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DeleteDatabasePostgresCidrArgs> for () {
    fn generate_resource_id(&self, input: &DeleteDatabasePostgresCidrArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}",
            input.organization, input.database, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with DeleteWebhookArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DeleteWebhookArgs> for () {
    fn generate_resource_id(&self, input: &DeleteWebhookArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}",
            input.organization, input.database, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with TestWebhookArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<TestWebhookArgs> for () {
    fn generate_resource_id(&self, input: &TestWebhookArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}",
            input.organization, input.database, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with RemoveOrganizationMemberArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RemoveOrganizationMemberArgs> for () {
    fn generate_resource_id(&self, input: &RemoveOrganizationMemberArgs) -> String {
        format!("planetscale::()/{}/{}", input.organization, input.id)
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with DeleteOauthTokenArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DeleteOauthTokenArgs> for () {
    fn generate_resource_id(&self, input: &DeleteOauthTokenArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}",
            input.organization, input.application_id, input.token_id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with DeleteServiceTokenArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DeleteServiceTokenArgs> for () {
    fn generate_resource_id(&self, input: &DeleteServiceTokenArgs) -> String {
        format!("planetscale::()/{}/{}", input.organization, input.id)
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with DeleteOrganizationTeamArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DeleteOrganizationTeamArgs> for () {
    fn generate_resource_id(&self, input: &DeleteOrganizationTeamArgs) -> String {
        format!("planetscale::()/{}/{}", input.organization, input.team)
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ()
// =============================================================================

/// ResourceIdentifier implementation for () with RemoveOrganizationTeamMemberArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RemoveOrganizationTeamMemberArgs> for () {
    fn generate_resource_id(&self, input: &RemoveOrganizationTeamMemberArgs) -> String {
        format!(
            "planetscale::()/{}/{}/{}",
            input.organization, input.team, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "planetscale::()"
    }

    fn provider(&self) -> &'static str {
        "planetscale"
    }
}
