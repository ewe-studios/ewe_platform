//! Auto-generated API clients for PlanetScale.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `planetscale`

#![cfg(feature = "planetscale")]

use crate::providers::planetscale::clients::types::*;
use crate::providers::planetscale::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET /organizations
/// List organizations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_execute()` to send, or `get_organizations` for simplest API.

pub fn get_organizations_builder(
    client: &SimpleHttpClient,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.com/organizations",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations
/// List organizations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations
/// List organizations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_builder()` + `get_organizations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations(
    client: &SimpleHttpClient,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_builder(client, page, per_page)?;
    get_organizations_execute(builder)
}

/// GET /organizations/{organization}
/// Get an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_execute()` to send, or `get_organizations_organization` for simplest API.

pub fn get_organizations_organization_builder(
    client: &SimpleHttpClient,
    organization: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.com/organizations/{}", organization,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}
/// Get an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}
/// Get an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_builder()` + `get_organizations_organization_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization(
    client: &SimpleHttpClient,
    organization: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_builder(client, organization)?;
    get_organizations_organization_execute(builder)
}

/// PATCH /organizations/{organization}
/// Update an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_execute()` to send, or `patch_organizations_organization` for simplest API.

pub fn patch_organizations_organization_builder(
    client: &SimpleHttpClient,
    organization: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.com/organizations/{}", organization,);

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}
/// Update an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}
/// Update an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_builder()` + `patch_organizations_organization_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization(
    client: &SimpleHttpClient,
    organization: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_builder(client, organization)?;
    patch_organizations_organization_execute(builder)
}

/// GET /organizations/{organization}/audit-log
/// List audit logs
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_audit_log_execute()` to send, or `get_organizations_organization_audit_log` for simplest API.

pub fn get_organizations_organization_audit_log_builder(
    client: &SimpleHttpClient,
    organization: &str,
    starting_after: Option<&str>,
    ending_before: Option<&str>,
    limit: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/audit-log",
        organization,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "starting_after",
            starting_after
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "ending_before",
            ending_before
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "limit",
            limit
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/audit-log
/// List audit logs
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_audit_log_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_audit_log_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_audit_log_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/audit-log
/// List audit logs
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_audit_log_builder()` + `get_organizations_organization_audit_log_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_audit_log(
    client: &SimpleHttpClient,
    organization: &str,
    starting_after: Option<&str>,
    ending_before: Option<&str>,
    limit: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_audit_log_builder(
        client,
        organization,
        starting_after,
        ending_before,
        limit,
    )?;
    get_organizations_organization_audit_log_execute(builder)
}

/// GET /organizations/{organization}/cluster-size-skus
/// List available cluster sizes
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_cluster_size_skus_execute()` to send, or `get_organizations_organization_cluster_size_skus` for simplest API.

pub fn get_organizations_organization_cluster_size_skus_builder(
    client: &SimpleHttpClient,
    organization: &str,
    engine: Option<&str>,
    rates: Option<&str>,
    region: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/cluster-size-skus",
        organization,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "engine",
            engine
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "rates",
            rates
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "region",
            region
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/cluster-size-skus
/// List available cluster sizes
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_cluster_size_skus_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_cluster_size_skus_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_cluster_size_skus_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/cluster-size-skus
/// List available cluster sizes
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_cluster_size_skus_builder()` + `get_organizations_organization_cluster_size_skus_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_cluster_size_skus(
    client: &SimpleHttpClient,
    organization: &str,
    engine: Option<&str>,
    rates: Option<&str>,
    region: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_cluster_size_skus_builder(
        client,
        organization,
        engine,
        rates,
        region,
    )?;
    get_organizations_organization_cluster_size_skus_execute(builder)
}

/// GET /organizations/{organization}/databases
/// List databases
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_execute()` to send, or `get_organizations_organization_databases` for simplest API.

pub fn get_organizations_organization_databases_builder(
    client: &SimpleHttpClient,
    organization: &str,
    q: Option<&str>,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases",
        organization,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "q",
            q.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases
/// List databases
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases
/// List databases
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_builder()` + `get_organizations_organization_databases_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases(
    client: &SimpleHttpClient,
    organization: &str,
    q: Option<&str>,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_builder(client, organization, q, page, per_page)?;
    get_organizations_organization_databases_execute(builder)
}

/// POST /organizations/{organization}/databases
/// Create a database
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_execute()` to send, or `post_organizations_organization_databases` for simplest API.

pub fn post_organizations_organization_databases_builder(
    client: &SimpleHttpClient,
    organization: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases",
        organization,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases
/// Create a database
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases
/// Create a database
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_builder()` + `post_organizations_organization_databases_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases(
    client: &SimpleHttpClient,
    organization: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_builder(client, organization)?;
    post_organizations_organization_databases_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}
/// Get a database
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_execute()` to send, or `get_organizations_organization_databases_database` for simplest API.

pub fn get_organizations_organization_databases_database_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}",
        organization, database,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}
/// Get a database
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}
/// Get a database
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_builder()` + `get_organizations_organization_databases_database_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_builder(client, organization, database)?;
    get_organizations_organization_databases_database_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}
/// Update database settings
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_execute()` to send, or `patch_organizations_organization_databases_database` for simplest API.

pub fn patch_organizations_organization_databases_database_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}",
        organization, database,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}
/// Update database settings
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}
/// Update database settings
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_builder()` + `patch_organizations_organization_databases_database_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_builder(
        client,
        organization,
        database,
    )?;
    patch_organizations_organization_databases_database_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}
/// Delete a database
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_execute()` to send, or `delete_organizations_organization_databases_database` for simplest API.

pub fn delete_organizations_organization_databases_database_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}",
        organization, database,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}
/// Delete a database
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_databases_database_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_databases_database_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}
/// Delete a database
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_builder()` + `delete_organizations_organization_databases_database_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_builder(
        client,
        organization,
        database,
    )?;
    delete_organizations_organization_databases_database_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/backup-policies
/// List backup policies
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_backup_policies_execute()` to send, or `get_organizations_organization_databases_database_backup_policies` for simplest API.

pub fn get_organizations_organization_databases_database_backup_policies_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/backup-policies",
        organization, database,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/backup-policies
/// List backup policies
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_backup_policies_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_backup_policies_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_backup_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/backup-policies
/// List backup policies
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_backup_policies_builder()` + `get_organizations_organization_databases_database_backup_policies_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_backup_policies(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_backup_policies_builder(
        client,
        organization,
        database,
        page,
        per_page,
    )?;
    get_organizations_organization_databases_database_backup_policies_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/backup-policies
/// Create a backup policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_backup_policies_execute()` to send, or `post_organizations_organization_databases_database_backup_policies` for simplest API.

pub fn post_organizations_organization_databases_database_backup_policies_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/backup-policies",
        organization, database,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/backup-policies
/// Create a backup policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_backup_policies_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_backup_policies_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_backup_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/backup-policies
/// Create a backup policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_backup_policies_builder()` + `post_organizations_organization_databases_database_backup_policies_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_backup_policies(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_backup_policies_builder(
        client,
        organization,
        database,
    )?;
    post_organizations_organization_databases_database_backup_policies_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Get a backup policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_backup_policies_id_execute()` to send, or `get_organizations_organization_databases_database_backup_policies_id` for simplest API.

pub fn get_organizations_organization_databases_database_backup_policies_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    organization: &str,
    database: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/backup-policies/{}",
        id, organization, database,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Get a backup policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_backup_policies_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_backup_policies_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_backup_policies_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Get a backup policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_backup_policies_id_builder()` + `get_organizations_organization_databases_database_backup_policies_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_backup_policies_id(
    client: &SimpleHttpClient,
    id: &str,
    organization: &str,
    database: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_backup_policies_id_builder(
        client,
        id,
        organization,
        database,
    )?;
    get_organizations_organization_databases_database_backup_policies_id_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Update a backup policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_backup_policies_id_execute()` to send, or `patch_organizations_organization_databases_database_backup_policies_id` for simplest API.

pub fn patch_organizations_organization_databases_database_backup_policies_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    organization: &str,
    database: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/backup-policies/{}",
        id, organization, database,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Update a backup policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_backup_policies_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_backup_policies_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_backup_policies_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Update a backup policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_backup_policies_id_builder()` + `patch_organizations_organization_databases_database_backup_policies_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_backup_policies_id(
    client: &SimpleHttpClient,
    id: &str,
    organization: &str,
    database: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_backup_policies_id_builder(
        client,
        id,
        organization,
        database,
    )?;
    patch_organizations_organization_databases_database_backup_policies_id_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Delete a backup policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_backup_policies_id_execute()` to send, or `delete_organizations_organization_databases_database_backup_policies_id` for simplest API.

pub fn delete_organizations_organization_databases_database_backup_policies_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    organization: &str,
    database: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/backup-policies/{}",
        id, organization, database,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Delete a backup policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_databases_database_backup_policies_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_databases_database_backup_policies_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_backup_policies_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Delete a backup policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_backup_policies_id_builder()` + `delete_organizations_organization_databases_database_backup_policies_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_backup_policies_id(
    client: &SimpleHttpClient,
    id: &str,
    organization: &str,
    database: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_backup_policies_id_builder(
        client,
        id,
        organization,
        database,
    )?;
    delete_organizations_organization_databases_database_backup_policies_id_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches
/// List branches
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_execute()` to send, or `get_organizations_organization_databases_database_branches` for simplest API.

pub fn get_organizations_organization_databases_database_branches_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    q: Option<&str>,
    production: Option<&str>,
    safe_migrations: Option<&str>,
    order: Option<&str>,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches",
        organization, database,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "q",
            q.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "production",
            production
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "safe_migrations",
            safe_migrations
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "order",
            order
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/branches
/// List branches
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches
/// List branches
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_builder()` + `get_organizations_organization_databases_database_branches_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    q: Option<&str>,
    production: Option<&str>,
    safe_migrations: Option<&str>,
    order: Option<&str>,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_builder(
        client,
        organization,
        database,
        q,
        production,
        safe_migrations,
        order,
        page,
        per_page,
    )?;
    get_organizations_organization_databases_database_branches_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches
/// Create a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_execute()` to send, or `post_organizations_organization_databases_database_branches` for simplest API.

pub fn post_organizations_organization_databases_database_branches_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches",
        organization, database,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches
/// Create a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_branches_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_branches_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches
/// Create a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_builder()` + `post_organizations_organization_databases_database_branches_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_branches_builder(
        client,
        organization,
        database,
    )?;
    post_organizations_organization_databases_database_branches_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}
/// Get a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_execute()` to send, or `get_organizations_organization_databases_database_branches_branch` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}",
        organization, database, branch,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}
/// Get a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}
/// Get a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_builder()` + `get_organizations_organization_databases_database_branches_branch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_builder(
        client,
        organization,
        database,
        branch,
    )?;
    get_organizations_organization_databases_database_branches_branch_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}
/// Update a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}",
        organization, database, branch,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}
/// Update a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_branches_branch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}
/// Update a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_builder()` + `patch_organizations_organization_databases_database_branches_branch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_branches_branch_builder(
        client,
        organization,
        database,
        branch,
    )?;
    patch_organizations_organization_databases_database_branches_branch_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}
/// Delete a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    delete_descendants: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}",
        organization, database, branch,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "delete_descendants",
            delete_descendants
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}
/// Delete a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_databases_database_branches_branch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}
/// Delete a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_builder()` + `delete_organizations_organization_databases_database_branches_branch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    delete_descendants: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_branches_branch_builder(
        client,
        organization,
        database,
        branch,
        delete_descendants,
    )?;
    delete_organizations_organization_databases_database_branches_branch_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// List backups
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_backups_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_backups` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_backups_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    all: Option<&str>,
    state: Option<&str>,
    policy: Option<&str>,
    from: Option<&str>,
    to: Option<&str>,
    running_at: Option<&str>,
    production: Option<&str>,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/backups",
        organization, database, branch,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "all",
            all.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "state",
            state
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "policy",
            policy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "from",
            from.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "to",
            to.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "running_at",
            running_at
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "production",
            production
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// List backups
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_backups_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_backups_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_backups_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// List backups
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_backups_builder()` + `get_organizations_organization_databases_database_branches_branch_backups_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_backups(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    all: Option<&str>,
    state: Option<&str>,
    policy: Option<&str>,
    from: Option<&str>,
    to: Option<&str>,
    running_at: Option<&str>,
    production: Option<&str>,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_backups_builder(
            client,
            organization,
            database,
            branch,
            all,
            state,
            policy,
            from,
            to,
            running_at,
            production,
            page,
            per_page,
        )?;
    get_organizations_organization_databases_database_branches_branch_backups_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// Create a backup
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_backups_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_backups` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_backups_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/backups",
        organization, database, branch,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// Create a backup
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_backups_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_branches_branch_backups_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_backups_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// Create a backup
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_backups_builder()` + `post_organizations_organization_databases_database_branches_branch_backups_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_backups(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_backups_builder(
            client,
            organization,
            database,
            branch,
        )?;
    post_organizations_organization_databases_database_branches_branch_backups_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Get a backup
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_backups_id_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_backups_id` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_backups_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/backups/{}",
        id, organization, database, branch,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Get a backup
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_backups_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_backups_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_backups_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Get a backup
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_backups_id_builder()` + `get_organizations_organization_databases_database_branches_branch_backups_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_backups_id(
    client: &SimpleHttpClient,
    id: &str,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_backups_id_builder(
            client,
            id,
            organization,
            database,
            branch,
        )?;
    get_organizations_organization_databases_database_branches_branch_backups_id_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Update a backup
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_backups_id_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch_backups_id` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_backups_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/backups/{}",
        id, organization, database, branch,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Update a backup
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_backups_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_branches_branch_backups_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_backups_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Update a backup
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_backups_id_builder()` + `patch_organizations_organization_databases_database_branches_branch_backups_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_backups_id(
    client: &SimpleHttpClient,
    id: &str,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_branches_branch_backups_id_builder(
            client,
            id,
            organization,
            database,
            branch,
        )?;
    patch_organizations_organization_databases_database_branches_branch_backups_id_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Delete a backup
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_backups_id_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_backups_id` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_backups_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/backups/{}",
        id, organization, database, branch,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Delete a backup
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_backups_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_databases_database_branches_branch_backups_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_backups_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Delete a backup
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_backups_id_builder()` + `delete_organizations_organization_databases_database_branches_branch_backups_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_backups_id(
    client: &SimpleHttpClient,
    id: &str,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        delete_organizations_organization_databases_database_branches_branch_backups_id_builder(
            client,
            id,
            organization,
            database,
            branch,
        )?;
    delete_organizations_organization_databases_database_branches_branch_backups_id_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncer-resizes
/// Get bouncer resize requests
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_bouncer_resizes_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_bouncer_resizes` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_bouncer_resizes_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/bouncer-resizes",
        organization, database, branch,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncer-resizes
/// Get bouncer resize requests
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_bouncer_resizes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_bouncer_resizes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_bouncer_resizes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncer-resizes
/// Get bouncer resize requests
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_bouncer_resizes_builder()` + `get_organizations_organization_databases_database_branches_branch_bouncer_resizes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_bouncer_resizes(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_bouncer_resizes_builder(
            client,
            organization,
            database,
            branch,
            page,
            per_page,
        )?;
    get_organizations_organization_databases_database_branches_branch_bouncer_resizes_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// List bouncers
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_bouncers_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_bouncers` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/bouncers",
        organization, database, branch,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// List bouncers
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_bouncers_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_bouncers_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// List bouncers
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_bouncers_builder()` + `get_organizations_organization_databases_database_branches_branch_bouncers_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_bouncers_builder(
            client,
            organization,
            database,
            branch,
            page,
            per_page,
        )?;
    get_organizations_organization_databases_database_branches_branch_bouncers_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// Create a bouncer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_bouncers_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_bouncers` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_bouncers_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/bouncers",
        organization, database, branch,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// Create a bouncer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_bouncers_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_branches_branch_bouncers_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_bouncers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// Create a bouncer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_bouncers_builder()` + `post_organizations_organization_databases_database_branches_branch_bouncers_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_bouncers(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_bouncers_builder(
            client,
            organization,
            database,
            branch,
        )?;
    post_organizations_organization_databases_database_branches_branch_bouncers_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Get a bouncer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    bouncer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/bouncers/{}",
        organization, database, branch, bouncer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Get a bouncer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Get a bouncer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder()` + `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers_bouncer(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    bouncer: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder(
            client,
            organization,
            database,
            branch,
            bouncer,
        )?;
    get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_execute(
        builder,
    )
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Delete a bouncer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    bouncer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/bouncers/{}",
        organization, database, branch, bouncer,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Delete a bouncer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Delete a bouncer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder()` + `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    bouncer: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder(client, organization, database, branch, bouncer)?;
    delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Get bouncer resize requests
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    bouncer: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/bouncers/{}/resizes",
        organization, database, branch, bouncer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Get bouncer resize requests
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Get bouncer resize requests
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()` + `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    bouncer: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder(client, organization, database, branch, bouncer, page, per_page)?;
    get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Upsert a bouncer resize request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    bouncer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/bouncers/{}/resizes",
        organization, database, branch, bouncer,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Upsert a bouncer resize request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Upsert a bouncer resize request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()` + `patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    bouncer: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder(client, organization, database, branch, bouncer)?;
    patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Cancel a resize request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    bouncer: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/bouncers/{}/resizes",
        organization, database, branch, bouncer,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Cancel a resize request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Cancel a resize request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()` + `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    bouncer: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder(client, organization, database, branch, bouncer)?;
    delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Get branch change requests
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_changes_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_changes` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_changes_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/changes",
        organization, database, branch,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Get branch change requests
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_changes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_changes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_changes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Get branch change requests
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_changes_builder()` + `get_organizations_organization_databases_database_branches_branch_changes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_changes(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_changes_builder(
            client,
            organization,
            database,
            branch,
            page,
            per_page,
        )?;
    get_organizations_organization_databases_database_branches_branch_changes_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Upsert a change request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_changes_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch_changes` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_changes_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/changes",
        organization, database, branch,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Upsert a change request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_changes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_branches_branch_changes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_changes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Upsert a change request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_changes_builder()` + `patch_organizations_organization_databases_database_branches_branch_changes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_changes(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_branches_branch_changes_builder(
            client,
            organization,
            database,
            branch,
        )?;
    patch_organizations_organization_databases_database_branches_branch_changes_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes/{id}
/// Get a branch change request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_changes_id_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_changes_id` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_changes_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/changes/{}",
        organization, database, branch, id,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes/{id}
/// Get a branch change request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_changes_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_changes_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_changes_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes/{id}
/// Get a branch change request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_changes_id_builder()` + `get_organizations_organization_databases_database_branches_branch_changes_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_changes_id(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_changes_id_builder(
            client,
            organization,
            database,
            branch,
            id,
        )?;
    get_organizations_organization_databases_database_branches_branch_changes_id_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/cluster
/// Change a branch cluster configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_cluster_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch_cluster` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_cluster_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/cluster",
        organization, database, branch,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/cluster
/// Change a branch cluster configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_cluster_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_branches_branch_cluster_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_cluster_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/cluster
/// Change a branch cluster configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_cluster_builder()` + `patch_organizations_organization_databases_database_branches_branch_cluster_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_cluster(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_branches_branch_cluster_builder(
            client,
            organization,
            database,
            branch,
        )?;
    patch_organizations_organization_databases_database_branches_branch_cluster_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/demote
/// Demote a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_demote_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_demote` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_demote_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/demote",
        organization, database, branch,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/demote
/// Demote a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_demote_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_branches_branch_demote_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_demote_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/demote
/// Demote a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_demote_builder()` + `post_organizations_organization_databases_database_branches_branch_demote_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_demote(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_demote_builder(
            client,
            organization,
            database,
            branch,
        )?;
    post_organizations_organization_databases_database_branches_branch_demote_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/extensions
/// List cluster extensions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_extensions_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_extensions` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_extensions_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/extensions",
        organization, database, branch,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/extensions
/// List cluster extensions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_extensions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_extensions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_extensions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/extensions
/// List cluster extensions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_extensions_builder()` + `get_organizations_organization_databases_database_branches_branch_extensions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_extensions(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_extensions_builder(
            client,
            organization,
            database,
            branch,
        )?;
    get_organizations_organization_databases_database_branches_branch_extensions_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Get keyspaces
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_keyspaces_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_keyspaces` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/keyspaces",
        organization, database, branch,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Get keyspaces
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_keyspaces_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_keyspaces_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Get keyspaces
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_keyspaces_builder()` + `get_organizations_organization_databases_database_branches_branch_keyspaces_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_keyspaces_builder(
            client,
            organization,
            database,
            branch,
            page,
            per_page,
        )?;
    get_organizations_organization_databases_database_branches_branch_keyspaces_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Create a keyspace
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_keyspaces_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_keyspaces` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_keyspaces_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/keyspaces",
        organization, database, branch,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Create a keyspace
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_keyspaces_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_branches_branch_keyspaces_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_keyspaces_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Create a keyspace
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_keyspaces_builder()` + `post_organizations_organization_databases_database_branches_branch_keyspaces_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_keyspaces(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_keyspaces_builder(
            client,
            organization,
            database,
            branch,
        )?;
    post_organizations_organization_databases_database_branches_branch_keyspaces_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Get a keyspace
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    keyspace: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/keyspaces/{}",
        organization, database, branch, keyspace,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Get a keyspace
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Get a keyspace
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()` + `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    keyspace: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder(client, organization, database, branch, keyspace)?;
    get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute(
        builder,
    )
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Configure keyspace settings
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    keyspace: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/keyspaces/{}",
        organization, database, branch, keyspace,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Configure keyspace settings
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Configure keyspace settings
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()` + `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    keyspace: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder(client, organization, database, branch, keyspace)?;
    patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute(
        builder,
    )
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Delete a keyspace
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    keyspace: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/keyspaces/{}",
        organization, database, branch, keyspace,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Delete a keyspace
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Delete a keyspace
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()` + `delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    keyspace: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder(client, organization, database, branch, keyspace)?;
    delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/rollout-status
/// Get keyspace rollout status
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    keyspace: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/keyspaces/{}/rollout-status",
        organization,
        database,
        branch,
        keyspace,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/rollout-status
/// Get keyspace rollout status
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/rollout-status
/// Get keyspace rollout status
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_builder()` + `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    keyspace: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_builder(client, organization, database, branch, keyspace)?;
    get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Get the VSchema for the keyspace
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    keyspace: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/keyspaces/{}/vschema",
        organization, database, branch, keyspace,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Get the VSchema for the keyspace
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Get the VSchema for the keyspace
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder()` + `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    keyspace: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder(client, organization, database, branch, keyspace)?;
    get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Update the VSchema for the keyspace
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    keyspace: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/keyspaces/{}/vschema",
        organization, database, branch, keyspace,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Update the VSchema for the keyspace
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Update the VSchema for the keyspace
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder()` + `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    keyspace: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder(client, organization, database, branch, keyspace)?;
    patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/parameters
/// List cluster parameters
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_parameters_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_parameters` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_parameters_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/parameters",
        organization, database, branch,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/parameters
/// List cluster parameters
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_parameters_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_parameters_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_parameters_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/parameters
/// List cluster parameters
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_parameters_builder()` + `get_organizations_organization_databases_database_branches_branch_parameters_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_parameters(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_parameters_builder(
            client,
            organization,
            database,
            branch,
        )?;
    get_organizations_organization_databases_database_branches_branch_parameters_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// List passwords
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_passwords_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_passwords` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_passwords_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    read_only_region_id: Option<&str>,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/passwords",
        organization, database, branch,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "read_only_region_id",
            read_only_region_id
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// List passwords
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_passwords_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_passwords_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_passwords_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// List passwords
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_passwords_builder()` + `get_organizations_organization_databases_database_branches_branch_passwords_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_passwords(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    read_only_region_id: Option<&str>,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_passwords_builder(
            client,
            organization,
            database,
            branch,
            read_only_region_id,
            page,
            per_page,
        )?;
    get_organizations_organization_databases_database_branches_branch_passwords_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// Create a password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_passwords_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_passwords` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_passwords_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/passwords",
        organization, database, branch,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// Create a password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_passwords_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_branches_branch_passwords_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_passwords_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// Create a password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_passwords_builder()` + `post_organizations_organization_databases_database_branches_branch_passwords_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_passwords(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_passwords_builder(
            client,
            organization,
            database,
            branch,
        )?;
    post_organizations_organization_databases_database_branches_branch_passwords_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Get a password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_passwords_id_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_passwords_id` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_passwords_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/passwords/{}",
        organization, database, branch, id,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Get a password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_passwords_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_passwords_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_passwords_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Get a password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_passwords_id_builder()` + `get_organizations_organization_databases_database_branches_branch_passwords_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_passwords_id(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_passwords_id_builder(
            client,
            organization,
            database,
            branch,
            id,
        )?;
    get_organizations_organization_databases_database_branches_branch_passwords_id_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Update a password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_passwords_id_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch_passwords_id` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_passwords_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/passwords/{}",
        organization, database, branch, id,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Update a password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_passwords_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_branches_branch_passwords_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_passwords_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Update a password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_passwords_id_builder()` + `patch_organizations_organization_databases_database_branches_branch_passwords_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_passwords_id(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_branches_branch_passwords_id_builder(
            client,
            organization,
            database,
            branch,
            id,
        )?;
    patch_organizations_organization_databases_database_branches_branch_passwords_id_execute(
        builder,
    )
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Delete a password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_passwords_id_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_passwords_id` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_passwords_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/passwords/{}",
        organization, database, branch, id,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Delete a password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_passwords_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_databases_database_branches_branch_passwords_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_passwords_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Delete a password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_passwords_id_builder()` + `delete_organizations_organization_databases_database_branches_branch_passwords_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_passwords_id(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        delete_organizations_organization_databases_database_branches_branch_passwords_id_builder(
            client,
            organization,
            database,
            branch,
            id,
        )?;
    delete_organizations_organization_databases_database_branches_branch_passwords_id_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}/renew
/// Renew a password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_passwords_id_renew_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_passwords_id_renew` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_passwords_id_renew_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/passwords/{}/renew",
        organization, database, branch, id,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}/renew
/// Renew a password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_passwords_id_renew_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_branches_branch_passwords_id_renew_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_passwords_id_renew_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}/renew
/// Renew a password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_passwords_id_renew_builder()` + `post_organizations_organization_databases_database_branches_branch_passwords_id_renew_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_passwords_id_renew(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_branches_branch_passwords_id_renew_builder(client, organization, database, branch, id)?;
    post_organizations_organization_databases_database_branches_branch_passwords_id_renew_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/promote
/// Promote a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_promote_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_promote` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_promote_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/promote",
        organization, database, branch,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/promote
/// Promote a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_promote_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_branches_branch_promote_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_promote_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/promote
/// Promote a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_promote_builder()` + `post_organizations_organization_databases_database_branches_branch_promote_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_promote(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_promote_builder(
            client,
            organization,
            database,
            branch,
        )?;
    post_organizations_organization_databases_database_branches_branch_promote_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// List generated query patterns reports
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_query_patterns_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_query_patterns` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    starting_after: Option<&str>,
    ending_before: Option<&str>,
    limit: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/query-patterns",
        organization, database, branch,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "starting_after",
            starting_after
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "ending_before",
            ending_before
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "limit",
            limit
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// List generated query patterns reports
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_query_patterns_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_query_patterns_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// List generated query patterns reports
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_query_patterns_builder()` + `get_organizations_organization_databases_database_branches_branch_query_patterns_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    starting_after: Option<&str>,
    ending_before: Option<&str>,
    limit: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_query_patterns_builder(
            client,
            organization,
            database,
            branch,
            starting_after,
            ending_before,
            limit,
        )?;
    get_organizations_organization_databases_database_branches_branch_query_patterns_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// Create a new query patterns report
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_query_patterns_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_query_patterns` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_query_patterns_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/query-patterns",
        organization, database, branch,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// Create a new query patterns report
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_query_patterns_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_branches_branch_query_patterns_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_query_patterns_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// Create a new query patterns report
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_query_patterns_builder()` + `post_organizations_organization_databases_database_branches_branch_query_patterns_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_query_patterns(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_query_patterns_builder(
            client,
            organization,
            database,
            branch,
        )?;
    post_organizations_organization_databases_database_branches_branch_query_patterns_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Show the status of a query patterns report
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_query_patterns_id_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_query_patterns_id` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/query-patterns/{}",
        organization, database, branch, id,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Show the status of a query patterns report
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_query_patterns_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_query_patterns_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Show the status of a query patterns report
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_query_patterns_id_builder()` + `get_organizations_organization_databases_database_branches_branch_query_patterns_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns_id(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_query_patterns_id_builder(client, organization, database, branch, id)?;
    get_organizations_organization_databases_database_branches_branch_query_patterns_id_execute(
        builder,
    )
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Delete a query patterns report
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_query_patterns_id_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_query_patterns_id` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_query_patterns_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/query-patterns/{}",
        organization, database, branch, id,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Delete a query patterns report
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_query_patterns_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_databases_database_branches_branch_query_patterns_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_query_patterns_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Delete a query patterns report
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_query_patterns_id_builder()` + `delete_organizations_organization_databases_database_branches_branch_query_patterns_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_query_patterns_id(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_branches_branch_query_patterns_id_builder(client, organization, database, branch, id)?;
    delete_organizations_organization_databases_database_branches_branch_query_patterns_id_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}/download
/// Download a finished query patterns report
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_query_patterns_id_download` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/query-patterns/{}/download",
        organization,
        database,
        branch,
        id,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}/download
/// Download a finished query patterns report
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}/download
/// Download a finished query patterns report
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_builder()` + `get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns_id_download(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_builder(client, organization, database, branch, id)?;
    get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/resizes
/// Cancel a change request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_resizes_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_resizes` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_resizes_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/resizes",
        organization, database, branch,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/resizes
/// Cancel a change request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_resizes_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_databases_database_branches_branch_resizes_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_resizes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/resizes
/// Cancel a change request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_resizes_builder()` + `delete_organizations_organization_databases_database_branches_branch_resizes_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_resizes(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        delete_organizations_organization_databases_database_branches_branch_resizes_builder(
            client,
            organization,
            database,
            branch,
        )?;
    delete_organizations_organization_databases_database_branches_branch_resizes_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// List roles
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_roles_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_roles` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_roles_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/roles",
        organization, database, branch,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// List roles
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_roles_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_roles_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_roles_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// List roles
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_roles_builder()` + `get_organizations_organization_databases_database_branches_branch_roles_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_roles(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_roles_builder(
        client,
        organization,
        database,
        branch,
        page,
        per_page,
    )?;
    get_organizations_organization_databases_database_branches_branch_roles_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// Create role credentials
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_roles_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_roles` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_roles_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/roles",
        organization, database, branch,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// Create role credentials
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_roles_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_branches_branch_roles_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// Create role credentials
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_roles_builder()` + `post_organizations_organization_databases_database_branches_branch_roles_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_branches_branch_roles_builder(
        client,
        organization,
        database,
        branch,
    )?;
    post_organizations_organization_databases_database_branches_branch_roles_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/default
/// Get the default postgres role
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_roles_default_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_roles_default` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_roles_default_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/roles/default",
        organization, database, branch,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/default
/// Get the default postgres role
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_roles_default_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_roles_default_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_roles_default_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/default
/// Get the default postgres role
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_roles_default_builder()` + `get_organizations_organization_databases_database_branches_branch_roles_default_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_roles_default(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_roles_default_builder(
            client,
            organization,
            database,
            branch,
        )?;
    get_organizations_organization_databases_database_branches_branch_roles_default_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/reset-default
/// Reset default credentials
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_roles_reset_default_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_roles_reset_default` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_roles_reset_default_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/roles/reset-default",
        organization, database, branch,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/reset-default
/// Reset default credentials
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_roles_reset_default_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_branches_branch_roles_reset_default_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles_reset_default_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/reset-default
/// Reset default credentials
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_roles_reset_default_builder()` + `post_organizations_organization_databases_database_branches_branch_roles_reset_default_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles_reset_default(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_branches_branch_roles_reset_default_builder(client, organization, database, branch)?;
    post_organizations_organization_databases_database_branches_branch_roles_reset_default_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Get a role
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_roles_id_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_roles_id` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_roles_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/roles/{}",
        organization, database, branch, id,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Get a role
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_roles_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_roles_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_roles_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Get a role
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_roles_id_builder()` + `get_organizations_organization_databases_database_branches_branch_roles_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_roles_id(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_roles_id_builder(
            client,
            organization,
            database,
            branch,
            id,
        )?;
    get_organizations_organization_databases_database_branches_branch_roles_id_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Update role name
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_roles_id_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch_roles_id` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_roles_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/roles/{}",
        organization, database, branch, id,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Update role name
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_roles_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_branches_branch_roles_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_roles_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Update role name
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_roles_id_builder()` + `patch_organizations_organization_databases_database_branches_branch_roles_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_roles_id(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_branches_branch_roles_id_builder(
            client,
            organization,
            database,
            branch,
            id,
        )?;
    patch_organizations_organization_databases_database_branches_branch_roles_id_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Delete role credentials
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_roles_id_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_roles_id` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_roles_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
    successor: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/roles/{}",
        organization, database, branch, id,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "successor",
            successor
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Delete role credentials
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_roles_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_databases_database_branches_branch_roles_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_roles_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Delete role credentials
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_roles_id_builder()` + `delete_organizations_organization_databases_database_branches_branch_roles_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_roles_id(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
    successor: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        delete_organizations_organization_databases_database_branches_branch_roles_id_builder(
            client,
            organization,
            database,
            branch,
            id,
            successor,
        )?;
    delete_organizations_organization_databases_database_branches_branch_roles_id_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reassign
/// Reassign objects owned by one role to another role
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_roles_id_reassign_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_roles_id_reassign` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_reassign_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/roles/{}/reassign",
        organization, database, branch, id,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reassign
/// Reassign objects owned by one role to another role
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_roles_id_reassign_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_branches_branch_roles_id_reassign_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_reassign_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reassign
/// Reassign objects owned by one role to another role
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_roles_id_reassign_builder()` + `post_organizations_organization_databases_database_branches_branch_roles_id_reassign_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_reassign(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_branches_branch_roles_id_reassign_builder(client, organization, database, branch, id)?;
    post_organizations_organization_databases_database_branches_branch_roles_id_reassign_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/renew
/// Renew role expiration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_roles_id_renew_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_roles_id_renew` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_renew_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/roles/{}/renew",
        organization, database, branch, id,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/renew
/// Renew role expiration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_roles_id_renew_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_branches_branch_roles_id_renew_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_renew_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/renew
/// Renew role expiration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_roles_id_renew_builder()` + `post_organizations_organization_databases_database_branches_branch_roles_id_renew_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_renew(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_roles_id_renew_builder(
            client,
            organization,
            database,
            branch,
            id,
        )?;
    post_organizations_organization_databases_database_branches_branch_roles_id_renew_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reset
/// Reset a role's password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_roles_id_reset_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_roles_id_reset` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_reset_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/roles/{}/reset",
        organization, database, branch, id,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reset
/// Reset a role's password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_roles_id_reset_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_branches_branch_roles_id_reset_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_reset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reset
/// Reset a role's password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_roles_id_reset_builder()` + `post_organizations_organization_databases_database_branches_branch_roles_id_reset_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_reset(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_roles_id_reset_builder(
            client,
            organization,
            database,
            branch,
            id,
        )?;
    post_organizations_organization_databases_database_branches_branch_roles_id_reset_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Enable safe migrations for a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_safe_migrations_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_safe_migrations` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_safe_migrations_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/safe-migrations",
        organization, database, branch,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Enable safe migrations for a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_safe_migrations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_branches_branch_safe_migrations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_safe_migrations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Enable safe migrations for a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_safe_migrations_builder()` + `post_organizations_organization_databases_database_branches_branch_safe_migrations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_safe_migrations(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_safe_migrations_builder(
            client,
            organization,
            database,
            branch,
        )?;
    post_organizations_organization_databases_database_branches_branch_safe_migrations_execute(
        builder,
    )
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Disable safe migrations for a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_safe_migrations_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_safe_migrations` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_safe_migrations_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/safe-migrations",
        organization, database, branch,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Disable safe migrations for a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_safe_migrations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_databases_database_branches_branch_safe_migrations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_safe_migrations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Disable safe migrations for a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_safe_migrations_builder()` + `delete_organizations_organization_databases_database_branches_branch_safe_migrations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_safe_migrations(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_branches_branch_safe_migrations_builder(client, organization, database, branch)?;
    delete_organizations_organization_databases_database_branches_branch_safe_migrations_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema
/// Get a branch schema
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_schema_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_schema` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_schema_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    keyspace: Option<&str>,
    namespace: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/schema",
        organization, database, branch,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "keyspace",
            keyspace
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "namespace",
            namespace
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema
/// Get a branch schema
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_schema_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_schema_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_schema_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema
/// Get a branch schema
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_schema_builder()` + `get_organizations_organization_databases_database_branches_branch_schema_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_schema(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    keyspace: Option<&str>,
    namespace: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_schema_builder(
        client,
        organization,
        database,
        branch,
        keyspace,
        namespace,
    )?;
    get_organizations_organization_databases_database_branches_branch_schema_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema/lint
/// Lint a branch schema
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_schema_lint_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_schema_lint` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_schema_lint_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/schema/lint",
        organization, database, branch,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema/lint
/// Lint a branch schema
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_schema_lint_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_schema_lint_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_schema_lint_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema/lint
/// Lint a branch schema
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_schema_lint_builder()` + `get_organizations_organization_databases_database_branches_branch_schema_lint_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_schema_lint(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_schema_lint_builder(
            client,
            organization,
            database,
            branch,
            page,
            per_page,
        )?;
    get_organizations_organization_databases_database_branches_branch_schema_lint_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// List traffic budgets
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_traffic_budgets_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_traffic_budgets` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_traffic_budgets_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    page: Option<&str>,
    per_page: Option<&str>,
    period: Option<&str>,
    created_at: Option<&str>,
    fingerprint: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/traffic/budgets",
        organization, database, branch,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "period",
            period
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "created_at",
            created_at
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "fingerprint",
            fingerprint
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// List traffic budgets
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_traffic_budgets_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_traffic_budgets_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_traffic_budgets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// List traffic budgets
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_traffic_budgets_builder()` + `get_organizations_organization_databases_database_branches_branch_traffic_budgets_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_traffic_budgets(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    page: Option<&str>,
    per_page: Option<&str>,
    period: Option<&str>,
    created_at: Option<&str>,
    fingerprint: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_traffic_budgets_builder(
            client,
            organization,
            database,
            branch,
            page,
            per_page,
            period,
            created_at,
            fingerprint,
        )?;
    get_organizations_organization_databases_database_branches_branch_traffic_budgets_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// Create a traffic budget
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_traffic_budgets_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_traffic_budgets` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_traffic_budgets_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/traffic/budgets",
        organization, database, branch,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// Create a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_traffic_budgets_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_branches_branch_traffic_budgets_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_traffic_budgets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// Create a traffic budget
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_traffic_budgets_builder()` + `post_organizations_organization_databases_database_branches_branch_traffic_budgets_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_traffic_budgets(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_traffic_budgets_builder(
            client,
            organization,
            database,
            branch,
        )?;
    post_organizations_organization_databases_database_branches_branch_traffic_budgets_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules
/// Create a traffic rule
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    budget_id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/traffic/budgets/{}/rules",
        organization,
        database,
        branch,
        budget_id,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules
/// Create a traffic rule
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules
/// Create a traffic rule
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_builder()` + `post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    budget_id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_builder(client, organization, database, branch, budget_id)?;
    post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules/{id}
/// Delete a traffic rule
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    budget_id: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/traffic/budgets/{}/rules/{}",
        organization,
        database,
        branch,
        budget_id,
        id,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules/{id}
/// Delete a traffic rule
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules/{id}
/// Delete a traffic rule
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_builder()` + `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    budget_id: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_builder(client, organization, database, branch, budget_id, id)?;
    delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Get a traffic budget
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_traffic_budgets_id` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/traffic/budgets/{}",
        organization, database, branch, id,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Get a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Get a traffic budget
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()` + `get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_traffic_budgets_id(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder(client, organization, database, branch, id)?;
    get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute(
        builder,
    )
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Update a traffic budget
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/traffic/budgets/{}",
        organization, database, branch, id,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Update a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Update a traffic budget
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()` + `patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder(client, organization, database, branch, id)?;
    patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute(
        builder,
    )
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Delete a traffic budget
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/branches/{}/traffic/budgets/{}",
        organization, database, branch, id,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Delete a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Delete a traffic budget
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()` + `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    branch: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder(client, organization, database, branch, id)?;
    delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/cidrs
/// List IP restriction entries
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_cidrs_execute()` to send, or `get_organizations_organization_databases_database_cidrs` for simplest API.

pub fn get_organizations_organization_databases_database_cidrs_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/cidrs",
        organization, database,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/cidrs
/// List IP restriction entries
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_cidrs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_cidrs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_cidrs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/cidrs
/// List IP restriction entries
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_cidrs_builder()` + `get_organizations_organization_databases_database_cidrs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_cidrs(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_cidrs_builder(
        client,
        organization,
        database,
        page,
        per_page,
    )?;
    get_organizations_organization_databases_database_cidrs_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/cidrs
/// Create an IP restriction entry
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_cidrs_execute()` to send, or `post_organizations_organization_databases_database_cidrs` for simplest API.

pub fn post_organizations_organization_databases_database_cidrs_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/cidrs",
        organization, database,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/cidrs
/// Create an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_cidrs_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_cidrs_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_cidrs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/cidrs
/// Create an IP restriction entry
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_cidrs_builder()` + `post_organizations_organization_databases_database_cidrs_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_cidrs(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_cidrs_builder(
        client,
        organization,
        database,
    )?;
    post_organizations_organization_databases_database_cidrs_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/cidrs/{id}
/// Get an IP restriction entry
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_cidrs_id_execute()` to send, or `get_organizations_organization_databases_database_cidrs_id` for simplest API.

pub fn get_organizations_organization_databases_database_cidrs_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/cidrs/{}",
        organization, database, id,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/cidrs/{id}
/// Get an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_cidrs_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_cidrs_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_cidrs_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/cidrs/{id}
/// Get an IP restriction entry
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_cidrs_id_builder()` + `get_organizations_organization_databases_database_cidrs_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_cidrs_id(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_cidrs_id_builder(
        client,
        organization,
        database,
        id,
    )?;
    get_organizations_organization_databases_database_cidrs_id_execute(builder)
}

/// PUT /organizations/{organization}/databases/{database}/cidrs/{id}
/// Update an IP restriction entry
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `put_organizations_organization_databases_database_cidrs_id_execute()` to send, or `put_organizations_organization_databases_database_cidrs_id` for simplest API.

pub fn put_organizations_organization_databases_database_cidrs_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/cidrs/{}",
        organization, database, id,
    );

    // Build request
    client
        .put(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /organizations/{organization}/databases/{database}/cidrs/{id}
/// Update an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `put_organizations_organization_databases_database_cidrs_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from put_organizations_organization_databases_database_cidrs_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_organizations_organization_databases_database_cidrs_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /organizations/{organization}/databases/{database}/cidrs/{id}
/// Update an IP restriction entry
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `put_organizations_organization_databases_database_cidrs_id_builder()` + `put_organizations_organization_databases_database_cidrs_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_organizations_organization_databases_database_cidrs_id(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = put_organizations_organization_databases_database_cidrs_id_builder(
        client,
        organization,
        database,
        id,
    )?;
    put_organizations_organization_databases_database_cidrs_id_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/cidrs/{id}
/// Delete an IP restriction entry
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_cidrs_id_execute()` to send, or `delete_organizations_organization_databases_database_cidrs_id` for simplest API.

pub fn delete_organizations_organization_databases_database_cidrs_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/cidrs/{}",
        organization, database, id,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/cidrs/{id}
/// Delete an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_databases_database_cidrs_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_databases_database_cidrs_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_cidrs_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/cidrs/{id}
/// Delete an IP restriction entry
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_cidrs_id_builder()` + `delete_organizations_organization_databases_database_cidrs_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_cidrs_id(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_cidrs_id_builder(
        client,
        organization,
        database,
        id,
    )?;
    delete_organizations_organization_databases_database_cidrs_id_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-queue
/// Get the deploy queue
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_deploy_queue_execute()` to send, or `get_organizations_organization_databases_database_deploy_queue` for simplest API.

pub fn get_organizations_organization_databases_database_deploy_queue_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-queue",
        organization, database,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/deploy-queue
/// Get the deploy queue
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_deploy_queue_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_deploy_queue_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_queue_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/deploy-queue
/// Get the deploy queue
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_deploy_queue_builder()` + `get_organizations_organization_databases_database_deploy_queue_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_queue(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_deploy_queue_builder(
        client,
        organization,
        database,
        page,
        per_page,
    )?;
    get_organizations_organization_databases_database_deploy_queue_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests
/// List deploy requests
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_deploy_requests_execute()` to send, or `get_organizations_organization_databases_database_deploy_requests` for simplest API.

pub fn get_organizations_organization_databases_database_deploy_requests_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    state: Option<&str>,
    branch: Option<&str>,
    into_branch: Option<&str>,
    deployed_at: Option<&str>,
    running_at: Option<&str>,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-requests",
        organization, database,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "state",
            state
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "branch",
            branch
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "into_branch",
            into_branch
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "deployed_at",
            deployed_at
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "running_at",
            running_at
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests
/// List deploy requests
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_deploy_requests_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests
/// List deploy requests
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_builder()` + `get_organizations_organization_databases_database_deploy_requests_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    state: Option<&str>,
    branch: Option<&str>,
    into_branch: Option<&str>,
    deployed_at: Option<&str>,
    running_at: Option<&str>,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_deploy_requests_builder(
        client,
        organization,
        database,
        state,
        branch,
        into_branch,
        deployed_at,
        running_at,
        page,
        per_page,
    )?;
    get_organizations_organization_databases_database_deploy_requests_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests
/// Create a deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_deploy_requests_execute()` to send, or `post_organizations_organization_databases_database_deploy_requests` for simplest API.

pub fn post_organizations_organization_databases_database_deploy_requests_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-requests",
        organization, database,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests
/// Create a deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_deploy_requests_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests
/// Create a deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_builder()` + `post_organizations_organization_databases_database_deploy_requests_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_deploy_requests_builder(
        client,
        organization,
        database,
    )?;
    post_organizations_organization_databases_database_deploy_requests_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Get a deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_deploy_requests_number_execute()` to send, or `get_organizations_organization_databases_database_deploy_requests_number` for simplest API.

pub fn get_organizations_organization_databases_database_deploy_requests_number_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-requests/{}",
        organization, database, number,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Get a deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_number_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_deploy_requests_number_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Get a deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_number_builder()` + `get_organizations_organization_databases_database_deploy_requests_number_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_deploy_requests_number_builder(
        client,
        organization,
        database,
        number,
    )?;
    get_organizations_organization_databases_database_deploy_requests_number_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Close a deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_deploy_requests_number_execute()` to send, or `patch_organizations_organization_databases_database_deploy_requests_number` for simplest API.

pub fn patch_organizations_organization_databases_database_deploy_requests_number_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-requests/{}",
        organization, database, number,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Close a deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_deploy_requests_number_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_deploy_requests_number_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_deploy_requests_number_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Close a deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_deploy_requests_number_builder()` + `patch_organizations_organization_databases_database_deploy_requests_number_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_deploy_requests_number(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_deploy_requests_number_builder(
            client,
            organization,
            database,
            number,
        )?;
    patch_organizations_organization_databases_database_deploy_requests_number_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/apply-deploy
/// Complete a gated deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_execute()` to send, or `post_organizations_organization_databases_database_deploy_requests_number_apply_deploy` for simplest API.

pub fn post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-requests/{}/apply-deploy",
        organization, database, number,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/apply-deploy
/// Complete a gated deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/apply-deploy
/// Complete a gated deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_builder()` + `post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_apply_deploy(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_builder(client, organization, database, number)?;
    post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_execute(
        builder,
    )
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-apply
/// Update auto-apply for deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `put_organizations_organization_databases_database_deploy_requests_number_auto_apply_execute()` to send, or `put_organizations_organization_databases_database_deploy_requests_number_auto_apply` for simplest API.

pub fn put_organizations_organization_databases_database_deploy_requests_number_auto_apply_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-requests/{}/auto-apply",
        organization, database, number,
    );

    // Build request
    client
        .put(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-apply
/// Update auto-apply for deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `put_organizations_organization_databases_database_deploy_requests_number_auto_apply_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from put_organizations_organization_databases_database_deploy_requests_number_auto_apply_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_organizations_organization_databases_database_deploy_requests_number_auto_apply_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-apply
/// Update auto-apply for deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `put_organizations_organization_databases_database_deploy_requests_number_auto_apply_builder()` + `put_organizations_organization_databases_database_deploy_requests_number_auto_apply_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_organizations_organization_databases_database_deploy_requests_number_auto_apply(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = put_organizations_organization_databases_database_deploy_requests_number_auto_apply_builder(client, organization, database, number)?;
    put_organizations_organization_databases_database_deploy_requests_number_auto_apply_execute(
        builder,
    )
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-delete-branch
/// Update auto-delete branch for deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_execute()` to send, or `put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch` for simplest API.

pub fn put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-requests/{}/auto-delete-branch",
        organization,
        database,
        number,
    );

    // Build request
    client
        .put(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-delete-branch
/// Update auto-delete branch for deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-delete-branch
/// Update auto-delete branch for deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_builder()` + `put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_builder(client, organization, database, number)?;
    put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/cancel
/// Cancel a queued deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_deploy_requests_number_cancel_execute()` to send, or `post_organizations_organization_databases_database_deploy_requests_number_cancel` for simplest API.

pub fn post_organizations_organization_databases_database_deploy_requests_number_cancel_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-requests/{}/cancel",
        organization, database, number,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/cancel
/// Cancel a queued deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_deploy_requests_number_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/cancel
/// Cancel a queued deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_cancel_builder()` + `post_organizations_organization_databases_database_deploy_requests_number_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_cancel(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_deploy_requests_number_cancel_builder(
            client,
            organization,
            database,
            number,
        )?;
    post_organizations_organization_databases_database_deploy_requests_number_cancel_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/complete-deploy
/// Complete an errored deploy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_execute()` to send, or `post_organizations_organization_databases_database_deploy_requests_number_complete_deploy` for simplest API.

pub fn post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-requests/{}/complete-deploy",
        organization, database, number,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/complete-deploy
/// Complete an errored deploy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/complete-deploy
/// Complete an errored deploy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_builder()` + `post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_complete_deploy(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_builder(client, organization, database, number)?;
    post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/deploy
/// Queue a deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_deploy_requests_number_deploy_execute()` to send, or `post_organizations_organization_databases_database_deploy_requests_number_deploy` for simplest API.

pub fn post_organizations_organization_databases_database_deploy_requests_number_deploy_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-requests/{}/deploy",
        organization, database, number,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/deploy
/// Queue a deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_deploy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_deploy_requests_number_deploy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_deploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/deploy
/// Queue a deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_deploy_builder()` + `post_organizations_organization_databases_database_deploy_requests_number_deploy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_deploy(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_deploy_requests_number_deploy_builder(
            client,
            organization,
            database,
            number,
        )?;
    post_organizations_organization_databases_database_deploy_requests_number_deploy_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/deployment
/// Get a deployment
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_deploy_requests_number_deployment_execute()` to send, or `get_organizations_organization_databases_database_deploy_requests_number_deployment` for simplest API.

pub fn get_organizations_organization_databases_database_deploy_requests_number_deployment_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-requests/{}/deployment",
        organization, database, number,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/deployment
/// Get a deployment
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_number_deployment_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_deploy_requests_number_deployment_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_deployment_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/deployment
/// Get a deployment
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_number_deployment_builder()` + `get_organizations_organization_databases_database_deploy_requests_number_deployment_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_deployment(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_deploy_requests_number_deployment_builder(client, organization, database, number)?;
    get_organizations_organization_databases_database_deploy_requests_number_deployment_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/operations
/// List deploy operations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_deploy_requests_number_operations_execute()` to send, or `get_organizations_organization_databases_database_deploy_requests_number_operations` for simplest API.

pub fn get_organizations_organization_databases_database_deploy_requests_number_operations_builder(
    client: &SimpleHttpClient,
    number: &str,
    organization: &str,
    database: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-requests/{}/operations",
        number, organization, database,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/operations
/// List deploy operations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_number_operations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_deploy_requests_number_operations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_operations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/operations
/// List deploy operations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_number_operations_builder()` + `get_organizations_organization_databases_database_deploy_requests_number_operations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_operations(
    client: &SimpleHttpClient,
    number: &str,
    organization: &str,
    database: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_deploy_requests_number_operations_builder(client, number, organization, database, page, per_page)?;
    get_organizations_organization_databases_database_deploy_requests_number_operations_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/revert
/// Complete a revert
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_deploy_requests_number_revert_execute()` to send, or `post_organizations_organization_databases_database_deploy_requests_number_revert` for simplest API.

pub fn post_organizations_organization_databases_database_deploy_requests_number_revert_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-requests/{}/revert",
        organization, database, number,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/revert
/// Complete a revert
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_revert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_deploy_requests_number_revert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_revert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/revert
/// Complete a revert
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_revert_builder()` + `post_organizations_organization_databases_database_deploy_requests_number_revert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_revert(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_deploy_requests_number_revert_builder(
            client,
            organization,
            database,
            number,
        )?;
    post_organizations_organization_databases_database_deploy_requests_number_revert_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// List deploy request reviews
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_deploy_requests_number_reviews_execute()` to send, or `get_organizations_organization_databases_database_deploy_requests_number_reviews` for simplest API.

pub fn get_organizations_organization_databases_database_deploy_requests_number_reviews_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-requests/{}/reviews",
        organization, database, number,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// List deploy request reviews
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_number_reviews_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_deploy_requests_number_reviews_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_reviews_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// List deploy request reviews
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_number_reviews_builder()` + `get_organizations_organization_databases_database_deploy_requests_number_reviews_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_reviews(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_deploy_requests_number_reviews_builder(
            client,
            organization,
            database,
            number,
            page,
            per_page,
        )?;
    get_organizations_organization_databases_database_deploy_requests_number_reviews_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// Review a deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_deploy_requests_number_reviews_execute()` to send, or `post_organizations_organization_databases_database_deploy_requests_number_reviews` for simplest API.

pub fn post_organizations_organization_databases_database_deploy_requests_number_reviews_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-requests/{}/reviews",
        organization, database, number,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// Review a deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_reviews_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_deploy_requests_number_reviews_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_reviews_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// Review a deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_reviews_builder()` + `post_organizations_organization_databases_database_deploy_requests_number_reviews_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_reviews(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_deploy_requests_number_reviews_builder(
            client,
            organization,
            database,
            number,
        )?;
    post_organizations_organization_databases_database_deploy_requests_number_reviews_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/skip-revert
/// Skip revert period
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_deploy_requests_number_skip_revert_execute()` to send, or `post_organizations_organization_databases_database_deploy_requests_number_skip_revert` for simplest API.

pub fn post_organizations_organization_databases_database_deploy_requests_number_skip_revert_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-requests/{}/skip-revert",
        organization, database, number,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/skip-revert
/// Skip revert period
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_skip_revert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_deploy_requests_number_skip_revert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_skip_revert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/skip-revert
/// Skip revert period
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_skip_revert_builder()` + `post_organizations_organization_databases_database_deploy_requests_number_skip_revert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_skip_revert(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_deploy_requests_number_skip_revert_builder(client, organization, database, number)?;
    post_organizations_organization_databases_database_deploy_requests_number_skip_revert_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/storage-check
/// Check deploy request storage
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_deploy_requests_number_storage_check_execute()` to send, or `get_organizations_organization_databases_database_deploy_requests_number_storage_check` for simplest API.

pub fn get_organizations_organization_databases_database_deploy_requests_number_storage_check_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-requests/{}/storage-check",
        organization, database, number,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/storage-check
/// Check deploy request storage
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_number_storage_check_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_deploy_requests_number_storage_check_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_storage_check_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/storage-check
/// Check deploy request storage
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_number_storage_check_builder()` + `get_organizations_organization_databases_database_deploy_requests_number_storage_check_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_storage_check(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_deploy_requests_number_storage_check_builder(client, organization, database, number)?;
    get_organizations_organization_databases_database_deploy_requests_number_storage_check_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Get deploy request throttler configurations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_deploy_requests_number_throttler_execute()` to send, or `get_organizations_organization_databases_database_deploy_requests_number_throttler` for simplest API.

pub fn get_organizations_organization_databases_database_deploy_requests_number_throttler_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-requests/{}/throttler",
        organization, database, number,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Get deploy request throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_number_throttler_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_deploy_requests_number_throttler_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_throttler_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Get deploy request throttler configurations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_number_throttler_builder()` + `get_organizations_organization_databases_database_deploy_requests_number_throttler_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_throttler(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_deploy_requests_number_throttler_builder(
            client,
            organization,
            database,
            number,
        )?;
    get_organizations_organization_databases_database_deploy_requests_number_throttler_execute(
        builder,
    )
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Update deploy request throttler configurations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_deploy_requests_number_throttler_execute()` to send, or `patch_organizations_organization_databases_database_deploy_requests_number_throttler` for simplest API.

pub fn patch_organizations_organization_databases_database_deploy_requests_number_throttler_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/deploy-requests/{}/throttler",
        organization, database, number,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Update deploy request throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_deploy_requests_number_throttler_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_deploy_requests_number_throttler_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_deploy_requests_number_throttler_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Update deploy request throttler configurations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_deploy_requests_number_throttler_builder()` + `patch_organizations_organization_databases_database_deploy_requests_number_throttler_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_deploy_requests_number_throttler(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_deploy_requests_number_throttler_builder(client, organization, database, number)?;
    patch_organizations_organization_databases_database_deploy_requests_number_throttler_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules
/// List maintenance schedules
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_maintenance_schedules_execute()` to send, or `get_organizations_organization_databases_database_maintenance_schedules` for simplest API.

pub fn get_organizations_organization_databases_database_maintenance_schedules_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/maintenance-schedules",
        organization, database,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules
/// List maintenance schedules
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_maintenance_schedules_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_maintenance_schedules_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_maintenance_schedules_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules
/// List maintenance schedules
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_maintenance_schedules_builder()` + `get_organizations_organization_databases_database_maintenance_schedules_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_maintenance_schedules(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_maintenance_schedules_builder(
        client,
        organization,
        database,
        page,
        per_page,
    )?;
    get_organizations_organization_databases_database_maintenance_schedules_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}
/// Get a maintenance schedule
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_maintenance_schedules_id_execute()` to send, or `get_organizations_organization_databases_database_maintenance_schedules_id` for simplest API.

pub fn get_organizations_organization_databases_database_maintenance_schedules_id_builder(
    client: &SimpleHttpClient,
    id: &str,
    organization: &str,
    database: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/maintenance-schedules/{}",
        id, organization, database,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}
/// Get a maintenance schedule
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_maintenance_schedules_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_maintenance_schedules_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_maintenance_schedules_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}
/// Get a maintenance schedule
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_maintenance_schedules_id_builder()` + `get_organizations_organization_databases_database_maintenance_schedules_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_maintenance_schedules_id(
    client: &SimpleHttpClient,
    id: &str,
    organization: &str,
    database: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_maintenance_schedules_id_builder(
            client,
            id,
            organization,
            database,
        )?;
    get_organizations_organization_databases_database_maintenance_schedules_id_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}/windows
/// List maintenance windows
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_maintenance_schedules_id_windows_execute()` to send, or `get_organizations_organization_databases_database_maintenance_schedules_id_windows` for simplest API.

pub fn get_organizations_organization_databases_database_maintenance_schedules_id_windows_builder(
    client: &SimpleHttpClient,
    id: &str,
    organization: &str,
    database: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/maintenance-schedules/{}/windows",
        id, organization, database,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}/windows
/// List maintenance windows
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_maintenance_schedules_id_windows_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_maintenance_schedules_id_windows_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_maintenance_schedules_id_windows_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}/windows
/// List maintenance windows
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_maintenance_schedules_id_windows_builder()` + `get_organizations_organization_databases_database_maintenance_schedules_id_windows_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_maintenance_schedules_id_windows(
    client: &SimpleHttpClient,
    id: &str,
    organization: &str,
    database: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_maintenance_schedules_id_windows_builder(
            client,
            id,
            organization,
            database,
            page,
            per_page,
        )?;
    get_organizations_organization_databases_database_maintenance_schedules_id_windows_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/read-only-regions
/// List read-only regions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_read_only_regions_execute()` to send, or `get_organizations_organization_databases_database_read_only_regions` for simplest API.

pub fn get_organizations_organization_databases_database_read_only_regions_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/read-only-regions",
        organization, database,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/read-only-regions
/// List read-only regions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_read_only_regions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_read_only_regions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_read_only_regions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/read-only-regions
/// List read-only regions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_read_only_regions_builder()` + `get_organizations_organization_databases_database_read_only_regions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_read_only_regions(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_read_only_regions_builder(
        client,
        organization,
        database,
        page,
        per_page,
    )?;
    get_organizations_organization_databases_database_read_only_regions_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/regions
/// List database regions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_regions_execute()` to send, or `get_organizations_organization_databases_database_regions` for simplest API.

pub fn get_organizations_organization_databases_database_regions_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/regions",
        organization, database,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/regions
/// List database regions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_regions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_regions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_regions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/regions
/// List database regions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_regions_builder()` + `get_organizations_organization_databases_database_regions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_regions(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_regions_builder(
        client,
        organization,
        database,
        page,
        per_page,
    )?;
    get_organizations_organization_databases_database_regions_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations
/// List schema recommendations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_schema_recommendations_execute()` to send, or `get_organizations_organization_databases_database_schema_recommendations` for simplest API.

pub fn get_organizations_organization_databases_database_schema_recommendations_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    state: Option<&str>,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/schema-recommendations",
        organization, database,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "state",
            state
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations
/// List schema recommendations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_schema_recommendations_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_schema_recommendations_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_schema_recommendations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations
/// List schema recommendations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_schema_recommendations_builder()` + `get_organizations_organization_databases_database_schema_recommendations_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_schema_recommendations(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    state: Option<&str>,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_schema_recommendations_builder(
        client,
        organization,
        database,
        state,
        page,
        per_page,
    )?;
    get_organizations_organization_databases_database_schema_recommendations_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations/{number}
/// Get a schema recommendation
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_schema_recommendations_number_execute()` to send, or `get_organizations_organization_databases_database_schema_recommendations_number` for simplest API.

pub fn get_organizations_organization_databases_database_schema_recommendations_number_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/schema-recommendations/{}",
        organization, database, number,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations/{number}
/// Get a schema recommendation
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_schema_recommendations_number_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_schema_recommendations_number_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_schema_recommendations_number_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations/{number}
/// Get a schema recommendation
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_schema_recommendations_number_builder()` + `get_organizations_organization_databases_database_schema_recommendations_number_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_schema_recommendations_number(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_schema_recommendations_number_builder(
            client,
            organization,
            database,
            number,
        )?;
    get_organizations_organization_databases_database_schema_recommendations_number_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/schema-recommendations/{number}/dismiss
/// Dismiss a schema recommendation
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_schema_recommendations_number_dismiss_execute()` to send, or `post_organizations_organization_databases_database_schema_recommendations_number_dismiss` for simplest API.

pub fn post_organizations_organization_databases_database_schema_recommendations_number_dismiss_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/schema-recommendations/{}/dismiss",
        organization, database, number,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/schema-recommendations/{number}/dismiss
/// Dismiss a schema recommendation
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_schema_recommendations_number_dismiss_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_schema_recommendations_number_dismiss_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_schema_recommendations_number_dismiss_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/schema-recommendations/{number}/dismiss
/// Dismiss a schema recommendation
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_schema_recommendations_number_dismiss_builder()` + `post_organizations_organization_databases_database_schema_recommendations_number_dismiss_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_schema_recommendations_number_dismiss(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_schema_recommendations_number_dismiss_builder(client, organization, database, number)?;
    post_organizations_organization_databases_database_schema_recommendations_number_dismiss_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/throttler
/// Get database throttler configurations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_throttler_execute()` to send, or `get_organizations_organization_databases_database_throttler` for simplest API.

pub fn get_organizations_organization_databases_database_throttler_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/throttler",
        organization, database,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/throttler
/// Get database throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_throttler_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_throttler_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_throttler_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/throttler
/// Get database throttler configurations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_throttler_builder()` + `get_organizations_organization_databases_database_throttler_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_throttler(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_throttler_builder(
        client,
        organization,
        database,
    )?;
    get_organizations_organization_databases_database_throttler_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/throttler
/// Update database throttler configurations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_throttler_execute()` to send, or `patch_organizations_organization_databases_database_throttler` for simplest API.

pub fn patch_organizations_organization_databases_database_throttler_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/throttler",
        organization, database,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/throttler
/// Update database throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_throttler_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_throttler_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_throttler_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/throttler
/// Update database throttler configurations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_throttler_builder()` + `patch_organizations_organization_databases_database_throttler_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_throttler(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_throttler_builder(
        client,
        organization,
        database,
    )?;
    patch_organizations_organization_databases_database_throttler_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/webhooks
/// List webhooks
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_webhooks_execute()` to send, or `get_organizations_organization_databases_database_webhooks` for simplest API.

pub fn get_organizations_organization_databases_database_webhooks_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/webhooks",
        organization, database,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/webhooks
/// List webhooks
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_webhooks_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_webhooks_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_webhooks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/webhooks
/// List webhooks
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_webhooks_builder()` + `get_organizations_organization_databases_database_webhooks_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_webhooks(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_webhooks_builder(
        client,
        organization,
        database,
        page,
        per_page,
    )?;
    get_organizations_organization_databases_database_webhooks_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/webhooks
/// Create a webhook
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_webhooks_execute()` to send, or `post_organizations_organization_databases_database_webhooks` for simplest API.

pub fn post_organizations_organization_databases_database_webhooks_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/webhooks",
        organization, database,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/webhooks
/// Create a webhook
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_webhooks_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_webhooks_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_webhooks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/webhooks
/// Create a webhook
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_webhooks_builder()` + `post_organizations_organization_databases_database_webhooks_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_webhooks(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_webhooks_builder(
        client,
        organization,
        database,
    )?;
    post_organizations_organization_databases_database_webhooks_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/webhooks/{id}
/// Get a webhook
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_webhooks_id_execute()` to send, or `get_organizations_organization_databases_database_webhooks_id` for simplest API.

pub fn get_organizations_organization_databases_database_webhooks_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/webhooks/{}",
        organization, database, id,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/webhooks/{id}
/// Get a webhook
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_webhooks_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_webhooks_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_webhooks_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/webhooks/{id}
/// Get a webhook
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_webhooks_id_builder()` + `get_organizations_organization_databases_database_webhooks_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_webhooks_id(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_webhooks_id_builder(
        client,
        organization,
        database,
        id,
    )?;
    get_organizations_organization_databases_database_webhooks_id_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/webhooks/{id}
/// Update a webhook
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_webhooks_id_execute()` to send, or `patch_organizations_organization_databases_database_webhooks_id` for simplest API.

pub fn patch_organizations_organization_databases_database_webhooks_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/webhooks/{}",
        organization, database, id,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/webhooks/{id}
/// Update a webhook
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_webhooks_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_webhooks_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_webhooks_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/webhooks/{id}
/// Update a webhook
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_webhooks_id_builder()` + `patch_organizations_organization_databases_database_webhooks_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_webhooks_id(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_webhooks_id_builder(
        client,
        organization,
        database,
        id,
    )?;
    patch_organizations_organization_databases_database_webhooks_id_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/webhooks/{id}
/// Delete a webhook
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_webhooks_id_execute()` to send, or `delete_organizations_organization_databases_database_webhooks_id` for simplest API.

pub fn delete_organizations_organization_databases_database_webhooks_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/webhooks/{}",
        organization, database, id,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/webhooks/{id}
/// Delete a webhook
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_databases_database_webhooks_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_databases_database_webhooks_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_webhooks_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/webhooks/{id}
/// Delete a webhook
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_webhooks_id_builder()` + `delete_organizations_organization_databases_database_webhooks_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_webhooks_id(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_webhooks_id_builder(
        client,
        organization,
        database,
        id,
    )?;
    delete_organizations_organization_databases_database_webhooks_id_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/webhooks/{id}/test
/// Test a webhook
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_webhooks_id_test_execute()` to send, or `post_organizations_organization_databases_database_webhooks_id_test` for simplest API.

pub fn post_organizations_organization_databases_database_webhooks_id_test_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/webhooks/{}/test",
        organization, database, id,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/webhooks/{id}/test
/// Test a webhook
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_webhooks_id_test_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_webhooks_id_test_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_webhooks_id_test_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/webhooks/{id}/test
/// Test a webhook
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_webhooks_id_test_builder()` + `post_organizations_organization_databases_database_webhooks_id_test_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_webhooks_id_test(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_webhooks_id_test_builder(
        client,
        organization,
        database,
        id,
    )?;
    post_organizations_organization_databases_database_webhooks_id_test_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/workflows
/// List workflows
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_workflows_execute()` to send, or `get_organizations_organization_databases_database_workflows` for simplest API.

pub fn get_organizations_organization_databases_database_workflows_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    between: Option<&str>,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/workflows",
        organization, database,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "between",
            between
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/databases/{database}/workflows
/// List workflows
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_workflows_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_workflows_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_workflows_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/workflows
/// List workflows
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_workflows_builder()` + `get_organizations_organization_databases_database_workflows_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_workflows(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    between: Option<&str>,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_workflows_builder(
        client,
        organization,
        database,
        between,
        page,
        per_page,
    )?;
    get_organizations_organization_databases_database_workflows_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/workflows
/// Create a workflow
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_workflows_execute()` to send, or `post_organizations_organization_databases_database_workflows` for simplest API.

pub fn post_organizations_organization_databases_database_workflows_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/workflows",
        organization, database,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/workflows
/// Create a workflow
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_databases_database_workflows_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_databases_database_workflows_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_workflows_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/databases/{database}/workflows
/// Create a workflow
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_workflows_builder()` + `post_organizations_organization_databases_database_workflows_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_workflows(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_workflows_builder(
        client,
        organization,
        database,
    )?;
    post_organizations_organization_databases_database_workflows_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/workflows/{number}
/// Get a workflow
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_workflows_number_execute()` to send, or `get_organizations_organization_databases_database_workflows_number` for simplest API.

pub fn get_organizations_organization_databases_database_workflows_number_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/workflows/{}",
        organization, database, number,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/workflows/{number}
/// Get a workflow
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_databases_database_workflows_number_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_databases_database_workflows_number_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_workflows_number_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/databases/{database}/workflows/{number}
/// Get a workflow
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_workflows_number_builder()` + `get_organizations_organization_databases_database_workflows_number_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_workflows_number(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_workflows_number_builder(
        client,
        organization,
        database,
        number,
    )?;
    get_organizations_organization_databases_database_workflows_number_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/workflows/{number}
/// Cancel a workflow
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_workflows_number_execute()` to send, or `delete_organizations_organization_databases_database_workflows_number` for simplest API.

pub fn delete_organizations_organization_databases_database_workflows_number_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/workflows/{}",
        organization, database, number,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/workflows/{number}
/// Cancel a workflow
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_databases_database_workflows_number_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_databases_database_workflows_number_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_workflows_number_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/databases/{database}/workflows/{number}
/// Cancel a workflow
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_workflows_number_builder()` + `delete_organizations_organization_databases_database_workflows_number_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_workflows_number(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_workflows_number_builder(
        client,
        organization,
        database,
        number,
    )?;
    delete_organizations_organization_databases_database_workflows_number_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/complete
/// Complete a workflow
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_workflows_number_complete_execute()` to send, or `patch_organizations_organization_databases_database_workflows_number_complete` for simplest API.

pub fn patch_organizations_organization_databases_database_workflows_number_complete_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/workflows/{}/complete",
        organization, database, number,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/complete
/// Complete a workflow
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_complete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_workflows_number_complete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_complete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/complete
/// Complete a workflow
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_complete_builder()` + `patch_organizations_organization_databases_database_workflows_number_complete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_complete(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_workflows_number_complete_builder(
            client,
            organization,
            database,
            number,
        )?;
    patch_organizations_organization_databases_database_workflows_number_complete_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/cutover
/// Cutover traffic
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_workflows_number_cutover_execute()` to send, or `patch_organizations_organization_databases_database_workflows_number_cutover` for simplest API.

pub fn patch_organizations_organization_databases_database_workflows_number_cutover_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/workflows/{}/cutover",
        organization, database, number,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/cutover
/// Cutover traffic
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_cutover_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_workflows_number_cutover_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_cutover_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/cutover
/// Cutover traffic
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_cutover_builder()` + `patch_organizations_organization_databases_database_workflows_number_cutover_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_cutover(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_workflows_number_cutover_builder(
            client,
            organization,
            database,
            number,
        )?;
    patch_organizations_organization_databases_database_workflows_number_cutover_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/retry
/// Retry a failed workflow
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_workflows_number_retry_execute()` to send, or `patch_organizations_organization_databases_database_workflows_number_retry` for simplest API.

pub fn patch_organizations_organization_databases_database_workflows_number_retry_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/workflows/{}/retry",
        organization, database, number,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/retry
/// Retry a failed workflow
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_retry_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_workflows_number_retry_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_retry_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/retry
/// Retry a failed workflow
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_retry_builder()` + `patch_organizations_organization_databases_database_workflows_number_retry_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_retry(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_workflows_number_retry_builder(
            client,
            organization,
            database,
            number,
        )?;
    patch_organizations_organization_databases_database_workflows_number_retry_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-cutover
/// Reverse traffic cutover
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_workflows_number_reverse_cutover_execute()` to send, or `patch_organizations_organization_databases_database_workflows_number_reverse_cutover` for simplest API.

pub fn patch_organizations_organization_databases_database_workflows_number_reverse_cutover_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/workflows/{}/reverse-cutover",
        organization, database, number,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-cutover
/// Reverse traffic cutover
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_reverse_cutover_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_workflows_number_reverse_cutover_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_reverse_cutover_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-cutover
/// Reverse traffic cutover
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_reverse_cutover_builder()` + `patch_organizations_organization_databases_database_workflows_number_reverse_cutover_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_reverse_cutover(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_workflows_number_reverse_cutover_builder(client, organization, database, number)?;
    patch_organizations_organization_databases_database_workflows_number_reverse_cutover_execute(
        builder,
    )
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-traffic
/// Reverse traffic
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_workflows_number_reverse_traffic_execute()` to send, or `patch_organizations_organization_databases_database_workflows_number_reverse_traffic` for simplest API.

pub fn patch_organizations_organization_databases_database_workflows_number_reverse_traffic_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/workflows/{}/reverse-traffic",
        organization, database, number,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-traffic
/// Reverse traffic
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_reverse_traffic_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_workflows_number_reverse_traffic_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_reverse_traffic_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-traffic
/// Reverse traffic
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_reverse_traffic_builder()` + `patch_organizations_organization_databases_database_workflows_number_reverse_traffic_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_reverse_traffic(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_workflows_number_reverse_traffic_builder(client, organization, database, number)?;
    patch_organizations_organization_databases_database_workflows_number_reverse_traffic_execute(
        builder,
    )
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-primaries
/// Switch primary traffic
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_workflows_number_switch_primaries_execute()` to send, or `patch_organizations_organization_databases_database_workflows_number_switch_primaries` for simplest API.

pub fn patch_organizations_organization_databases_database_workflows_number_switch_primaries_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/workflows/{}/switch-primaries",
        organization, database, number,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-primaries
/// Switch primary traffic
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_switch_primaries_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_workflows_number_switch_primaries_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_switch_primaries_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-primaries
/// Switch primary traffic
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_switch_primaries_builder()` + `patch_organizations_organization_databases_database_workflows_number_switch_primaries_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_switch_primaries(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_workflows_number_switch_primaries_builder(client, organization, database, number)?;
    patch_organizations_organization_databases_database_workflows_number_switch_primaries_execute(
        builder,
    )
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-replicas
/// Switch replica traffic
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_workflows_number_switch_replicas_execute()` to send, or `patch_organizations_organization_databases_database_workflows_number_switch_replicas` for simplest API.

pub fn patch_organizations_organization_databases_database_workflows_number_switch_replicas_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/workflows/{}/switch-replicas",
        organization, database, number,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-replicas
/// Switch replica traffic
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_switch_replicas_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_workflows_number_switch_replicas_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_switch_replicas_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-replicas
/// Switch replica traffic
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_switch_replicas_builder()` + `patch_organizations_organization_databases_database_workflows_number_switch_replicas_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_switch_replicas(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_workflows_number_switch_replicas_builder(client, organization, database, number)?;
    patch_organizations_organization_databases_database_workflows_number_switch_replicas_execute(
        builder,
    )
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/verify-data
/// Verify workflow data
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_workflows_number_verify_data_execute()` to send, or `patch_organizations_organization_databases_database_workflows_number_verify_data` for simplest API.

pub fn patch_organizations_organization_databases_database_workflows_number_verify_data_builder(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/databases/{}/workflows/{}/verify-data",
        organization, database, number,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/verify-data
/// Verify workflow data
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_verify_data_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_databases_database_workflows_number_verify_data_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_verify_data_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/verify-data
/// Verify workflow data
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_verify_data_builder()` + `patch_organizations_organization_databases_database_workflows_number_verify_data_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_verify_data(
    client: &SimpleHttpClient,
    organization: &str,
    database: &str,
    number: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_workflows_number_verify_data_builder(
            client,
            organization,
            database,
            number,
        )?;
    patch_organizations_organization_databases_database_workflows_number_verify_data_execute(
        builder,
    )
}

/// GET /organizations/{organization}/invoices
/// Get invoices
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_invoices_execute()` to send, or `get_organizations_organization_invoices` for simplest API.

pub fn get_organizations_organization_invoices_builder(
    client: &SimpleHttpClient,
    organization: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/invoices",
        organization,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/invoices
/// Get invoices
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_invoices_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_invoices_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_invoices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/invoices
/// Get invoices
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_invoices_builder()` + `get_organizations_organization_invoices_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_invoices(
    client: &SimpleHttpClient,
    organization: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_invoices_builder(client, organization, page, per_page)?;
    get_organizations_organization_invoices_execute(builder)
}

/// GET /organizations/{organization}/invoices/{id}
/// Get an invoice
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_invoices_id_execute()` to send, or `get_organizations_organization_invoices_id` for simplest API.

pub fn get_organizations_organization_invoices_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/invoices/{}",
        organization, id,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/invoices/{id}
/// Get an invoice
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_invoices_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_invoices_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_invoices_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/invoices/{id}
/// Get an invoice
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_invoices_id_builder()` + `get_organizations_organization_invoices_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_invoices_id(
    client: &SimpleHttpClient,
    organization: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_invoices_id_builder(client, organization, id)?;
    get_organizations_organization_invoices_id_execute(builder)
}

/// GET /organizations/{organization}/invoices/{id}/line-items
/// Get invoice line items
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_invoices_id_line_items_execute()` to send, or `get_organizations_organization_invoices_id_line_items` for simplest API.

pub fn get_organizations_organization_invoices_id_line_items_builder(
    client: &SimpleHttpClient,
    organization: &str,
    id: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/invoices/{}/line-items",
        organization, id,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/invoices/{id}/line-items
/// Get invoice line items
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_invoices_id_line_items_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_invoices_id_line_items_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_invoices_id_line_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/invoices/{id}/line-items
/// Get invoice line items
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_invoices_id_line_items_builder()` + `get_organizations_organization_invoices_id_line_items_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_invoices_id_line_items(
    client: &SimpleHttpClient,
    organization: &str,
    id: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_invoices_id_line_items_builder(
        client,
        organization,
        id,
        page,
        per_page,
    )?;
    get_organizations_organization_invoices_id_line_items_execute(builder)
}

/// GET /organizations/{organization}/members
/// List organization members
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_members_execute()` to send, or `get_organizations_organization_members` for simplest API.

pub fn get_organizations_organization_members_builder(
    client: &SimpleHttpClient,
    organization: &str,
    q: Option<&str>,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/members",
        organization,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "q",
            q.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/members
/// List organization members
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_members_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_members_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_members_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/members
/// List organization members
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_members_builder()` + `get_organizations_organization_members_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_members(
    client: &SimpleHttpClient,
    organization: &str,
    q: Option<&str>,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_members_builder(client, organization, q, page, per_page)?;
    get_organizations_organization_members_execute(builder)
}

/// GET /organizations/{organization}/members/{id}
/// Get an organization member
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_members_id_execute()` to send, or `get_organizations_organization_members_id` for simplest API.

pub fn get_organizations_organization_members_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/members/{}",
        organization, id,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/members/{id}
/// Get an organization member
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_members_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_members_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_members_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/members/{id}
/// Get an organization member
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_members_id_builder()` + `get_organizations_organization_members_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_members_id(
    client: &SimpleHttpClient,
    organization: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_members_id_builder(client, organization, id)?;
    get_organizations_organization_members_id_execute(builder)
}

/// PATCH /organizations/{organization}/members/{id}
/// Update organization member role
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_members_id_execute()` to send, or `patch_organizations_organization_members_id` for simplest API.

pub fn patch_organizations_organization_members_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/members/{}",
        organization, id,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/members/{id}
/// Update organization member role
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_members_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_members_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_members_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/members/{id}
/// Update organization member role
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_members_id_builder()` + `patch_organizations_organization_members_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_members_id(
    client: &SimpleHttpClient,
    organization: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_members_id_builder(client, organization, id)?;
    patch_organizations_organization_members_id_execute(builder)
}

/// DELETE /organizations/{organization}/members/{id}
/// Remove a member from an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_members_id_execute()` to send, or `delete_organizations_organization_members_id` for simplest API.

pub fn delete_organizations_organization_members_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    id: &str,
    delete_passwords: Option<&str>,
    delete_service_tokens: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/members/{}",
        organization, id,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "delete_passwords",
            delete_passwords
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "delete_service_tokens",
            delete_service_tokens
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// DELETE /organizations/{organization}/members/{id}
/// Remove a member from an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_members_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_members_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_members_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/members/{id}
/// Remove a member from an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_members_id_builder()` + `delete_organizations_organization_members_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_members_id(
    client: &SimpleHttpClient,
    organization: &str,
    id: &str,
    delete_passwords: Option<&str>,
    delete_service_tokens: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_members_id_builder(
        client,
        organization,
        id,
        delete_passwords,
        delete_service_tokens,
    )?;
    delete_organizations_organization_members_id_execute(builder)
}

/// GET /organizations/{organization}/oauth-applications
/// List OAuth applications
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_oauth_applications_execute()` to send, or `get_organizations_organization_oauth_applications` for simplest API.

pub fn get_organizations_organization_oauth_applications_builder(
    client: &SimpleHttpClient,
    organization: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/oauth-applications",
        organization,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/oauth-applications
/// List OAuth applications
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_oauth_applications_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_oauth_applications_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_oauth_applications_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/oauth-applications
/// List OAuth applications
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_oauth_applications_builder()` + `get_organizations_organization_oauth_applications_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_oauth_applications(
    client: &SimpleHttpClient,
    organization: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_oauth_applications_builder(
        client,
        organization,
        page,
        per_page,
    )?;
    get_organizations_organization_oauth_applications_execute(builder)
}

/// GET /organizations/{organization}/oauth-applications/{application_id}
/// Get an OAuth application
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_oauth_applications_application_id_execute()` to send, or `get_organizations_organization_oauth_applications_application_id` for simplest API.

pub fn get_organizations_organization_oauth_applications_application_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    application_id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/oauth-applications/{}",
        organization, application_id,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/oauth-applications/{application_id}
/// Get an OAuth application
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_oauth_applications_application_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_oauth_applications_application_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_oauth_applications_application_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/oauth-applications/{application_id}
/// Get an OAuth application
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_oauth_applications_application_id_builder()` + `get_organizations_organization_oauth_applications_application_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_oauth_applications_application_id(
    client: &SimpleHttpClient,
    organization: &str,
    application_id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_oauth_applications_application_id_builder(
        client,
        organization,
        application_id,
    )?;
    get_organizations_organization_oauth_applications_application_id_execute(builder)
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens
/// List OAuth tokens
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_oauth_applications_application_id_tokens_execute()` to send, or `get_organizations_organization_oauth_applications_application_id_tokens` for simplest API.

pub fn get_organizations_organization_oauth_applications_application_id_tokens_builder(
    client: &SimpleHttpClient,
    organization: &str,
    application_id: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/oauth-applications/{}/tokens",
        organization, application_id,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens
/// List OAuth tokens
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_oauth_applications_application_id_tokens_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_oauth_applications_application_id_tokens_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_oauth_applications_application_id_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens
/// List OAuth tokens
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_oauth_applications_application_id_tokens_builder()` + `get_organizations_organization_oauth_applications_application_id_tokens_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_oauth_applications_application_id_tokens(
    client: &SimpleHttpClient,
    organization: &str,
    application_id: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_oauth_applications_application_id_tokens_builder(
        client,
        organization,
        application_id,
        page,
        per_page,
    )?;
    get_organizations_organization_oauth_applications_application_id_tokens_execute(builder)
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Get an OAuth token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_oauth_applications_application_id_tokens_token_id_execute()` to send, or `get_organizations_organization_oauth_applications_application_id_tokens_token_id` for simplest API.

pub fn get_organizations_organization_oauth_applications_application_id_tokens_token_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    application_id: &str,
    token_id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/oauth-applications/{}/tokens/{}",
        organization, application_id, token_id,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Get an OAuth token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_oauth_applications_application_id_tokens_token_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_oauth_applications_application_id_tokens_token_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_oauth_applications_application_id_tokens_token_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Get an OAuth token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_oauth_applications_application_id_tokens_token_id_builder()` + `get_organizations_organization_oauth_applications_application_id_tokens_token_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_oauth_applications_application_id_tokens_token_id(
    client: &SimpleHttpClient,
    organization: &str,
    application_id: &str,
    token_id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_oauth_applications_application_id_tokens_token_id_builder(
            client,
            organization,
            application_id,
            token_id,
        )?;
    get_organizations_organization_oauth_applications_application_id_tokens_token_id_execute(
        builder,
    )
}

/// DELETE /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Delete an OAuth token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_oauth_applications_application_id_tokens_token_id_execute()` to send, or `delete_organizations_organization_oauth_applications_application_id_tokens_token_id` for simplest API.

pub fn delete_organizations_organization_oauth_applications_application_id_tokens_token_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    application_id: &str,
    token_id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/oauth-applications/{}/tokens/{}",
        organization, application_id, token_id,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Delete an OAuth token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_oauth_applications_application_id_tokens_token_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_oauth_applications_application_id_tokens_token_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_oauth_applications_application_id_tokens_token_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Delete an OAuth token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_oauth_applications_application_id_tokens_token_id_builder()` + `delete_organizations_organization_oauth_applications_application_id_tokens_token_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_oauth_applications_application_id_tokens_token_id(
    client: &SimpleHttpClient,
    organization: &str,
    application_id: &str,
    token_id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_oauth_applications_application_id_tokens_token_id_builder(client, organization, application_id, token_id)?;
    delete_organizations_organization_oauth_applications_application_id_tokens_token_id_execute(
        builder,
    )
}

/// POST /organizations/{organization}/oauth-applications/{id}/token
/// Create or renew an OAuth token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_oauth_applications_id_token_execute()` to send, or `post_organizations_organization_oauth_applications_id_token` for simplest API.

pub fn post_organizations_organization_oauth_applications_id_token_builder(
    client: &SimpleHttpClient,
    organization: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/oauth-applications/{}/token",
        organization, id,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/oauth-applications/{id}/token
/// Create or renew an OAuth token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_oauth_applications_id_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_oauth_applications_id_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_oauth_applications_id_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/oauth-applications/{id}/token
/// Create or renew an OAuth token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_oauth_applications_id_token_builder()` + `post_organizations_organization_oauth_applications_id_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_oauth_applications_id_token(
    client: &SimpleHttpClient,
    organization: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_oauth_applications_id_token_builder(
        client,
        organization,
        id,
    )?;
    post_organizations_organization_oauth_applications_id_token_execute(builder)
}

/// GET /organizations/{organization}/regions
/// List regions for an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_regions_execute()` to send, or `get_organizations_organization_regions` for simplest API.

pub fn get_organizations_organization_regions_builder(
    client: &SimpleHttpClient,
    organization: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/regions",
        organization,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/regions
/// List regions for an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_regions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_regions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_regions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/regions
/// List regions for an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_regions_builder()` + `get_organizations_organization_regions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_regions(
    client: &SimpleHttpClient,
    organization: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_regions_builder(client, organization, page, per_page)?;
    get_organizations_organization_regions_execute(builder)
}

/// GET /organizations/{organization}/service-tokens
/// List service tokens
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_service_tokens_execute()` to send, or `get_organizations_organization_service_tokens` for simplest API.

pub fn get_organizations_organization_service_tokens_builder(
    client: &SimpleHttpClient,
    organization: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/service-tokens",
        organization,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/service-tokens
/// List service tokens
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_service_tokens_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_service_tokens_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_service_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/service-tokens
/// List service tokens
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_service_tokens_builder()` + `get_organizations_organization_service_tokens_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_service_tokens(
    client: &SimpleHttpClient,
    organization: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_service_tokens_builder(
        client,
        organization,
        page,
        per_page,
    )?;
    get_organizations_organization_service_tokens_execute(builder)
}

/// POST /organizations/{organization}/service-tokens
/// Create a service token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_service_tokens_execute()` to send, or `post_organizations_organization_service_tokens` for simplest API.

pub fn post_organizations_organization_service_tokens_builder(
    client: &SimpleHttpClient,
    organization: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/service-tokens",
        organization,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/service-tokens
/// Create a service token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_service_tokens_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_service_tokens_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_service_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/service-tokens
/// Create a service token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_service_tokens_builder()` + `post_organizations_organization_service_tokens_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_service_tokens(
    client: &SimpleHttpClient,
    organization: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_service_tokens_builder(client, organization)?;
    post_organizations_organization_service_tokens_execute(builder)
}

/// GET /organizations/{organization}/service-tokens/{id}
/// Get a service token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_service_tokens_id_execute()` to send, or `get_organizations_organization_service_tokens_id` for simplest API.

pub fn get_organizations_organization_service_tokens_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/service-tokens/{}",
        organization, id,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/service-tokens/{id}
/// Get a service token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_service_tokens_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_service_tokens_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_service_tokens_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/service-tokens/{id}
/// Get a service token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_service_tokens_id_builder()` + `get_organizations_organization_service_tokens_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_service_tokens_id(
    client: &SimpleHttpClient,
    organization: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_service_tokens_id_builder(client, organization, id)?;
    get_organizations_organization_service_tokens_id_execute(builder)
}

/// DELETE /organizations/{organization}/service-tokens/{id}
/// Delete a service token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_service_tokens_id_execute()` to send, or `delete_organizations_organization_service_tokens_id` for simplest API.

pub fn delete_organizations_organization_service_tokens_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/service-tokens/{}",
        organization, id,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/service-tokens/{id}
/// Delete a service token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_service_tokens_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_service_tokens_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_service_tokens_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/service-tokens/{id}
/// Delete a service token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_service_tokens_id_builder()` + `delete_organizations_organization_service_tokens_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_service_tokens_id(
    client: &SimpleHttpClient,
    organization: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        delete_organizations_organization_service_tokens_id_builder(client, organization, id)?;
    delete_organizations_organization_service_tokens_id_execute(builder)
}

/// GET /organizations/{organization}/teams
/// List teams in an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_teams_execute()` to send, or `get_organizations_organization_teams` for simplest API.

pub fn get_organizations_organization_teams_builder(
    client: &SimpleHttpClient,
    organization: &str,
    q: Option<&str>,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/teams",
        organization,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "q",
            q.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/teams
/// List teams in an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_teams_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_teams_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_teams_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/teams
/// List teams in an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_teams_builder()` + `get_organizations_organization_teams_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_teams(
    client: &SimpleHttpClient,
    organization: &str,
    q: Option<&str>,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_teams_builder(client, organization, q, page, per_page)?;
    get_organizations_organization_teams_execute(builder)
}

/// POST /organizations/{organization}/teams
/// Create an organization team
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_teams_execute()` to send, or `post_organizations_organization_teams` for simplest API.

pub fn post_organizations_organization_teams_builder(
    client: &SimpleHttpClient,
    organization: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/teams",
        organization,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/teams
/// Create an organization team
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_teams_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_teams_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_teams_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/teams
/// Create an organization team
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_teams_builder()` + `post_organizations_organization_teams_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_teams(
    client: &SimpleHttpClient,
    organization: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_teams_builder(client, organization)?;
    post_organizations_organization_teams_execute(builder)
}

/// GET /organizations/{organization}/teams/{team}
/// Get an organization team
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_teams_team_execute()` to send, or `get_organizations_organization_teams_team` for simplest API.

pub fn get_organizations_organization_teams_team_builder(
    client: &SimpleHttpClient,
    organization: &str,
    team: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/teams/{}",
        organization, team,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/teams/{team}
/// Get an organization team
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_teams_team_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_teams_team_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_teams_team_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/teams/{team}
/// Get an organization team
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_teams_team_builder()` + `get_organizations_organization_teams_team_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_teams_team(
    client: &SimpleHttpClient,
    organization: &str,
    team: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_teams_team_builder(client, organization, team)?;
    get_organizations_organization_teams_team_execute(builder)
}

/// PATCH /organizations/{organization}/teams/{team}
/// Update an organization team
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_teams_team_execute()` to send, or `patch_organizations_organization_teams_team` for simplest API.

pub fn patch_organizations_organization_teams_team_builder(
    client: &SimpleHttpClient,
    organization: &str,
    team: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/teams/{}",
        organization, team,
    );

    // Build request
    client
        .patch(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/teams/{team}
/// Update an organization team
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `patch_organizations_organization_teams_team_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from patch_organizations_organization_teams_team_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_teams_team_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// PATCH /organizations/{organization}/teams/{team}
/// Update an organization team
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_teams_team_builder()` + `patch_organizations_organization_teams_team_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_teams_team(
    client: &SimpleHttpClient,
    organization: &str,
    team: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_teams_team_builder(client, organization, team)?;
    patch_organizations_organization_teams_team_execute(builder)
}

/// DELETE /organizations/{organization}/teams/{team}
/// Delete an organization team
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_teams_team_execute()` to send, or `delete_organizations_organization_teams_team` for simplest API.

pub fn delete_organizations_organization_teams_team_builder(
    client: &SimpleHttpClient,
    organization: &str,
    team: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/teams/{}",
        organization, team,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/teams/{team}
/// Delete an organization team
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_teams_team_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_teams_team_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_teams_team_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/teams/{team}
/// Delete an organization team
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_teams_team_builder()` + `delete_organizations_organization_teams_team_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_teams_team(
    client: &SimpleHttpClient,
    organization: &str,
    team: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_teams_team_builder(client, organization, team)?;
    delete_organizations_organization_teams_team_execute(builder)
}

/// GET /organizations/{organization}/teams/{team}/members
/// List team members
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_teams_team_members_execute()` to send, or `get_organizations_organization_teams_team_members` for simplest API.

pub fn get_organizations_organization_teams_team_members_builder(
    client: &SimpleHttpClient,
    organization: &str,
    team: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/teams/{}/members",
        organization, team,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /organizations/{organization}/teams/{team}/members
/// List team members
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_teams_team_members_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_teams_team_members_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_teams_team_members_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/teams/{team}/members
/// List team members
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_teams_team_members_builder()` + `get_organizations_organization_teams_team_members_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_teams_team_members(
    client: &SimpleHttpClient,
    organization: &str,
    team: &str,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_teams_team_members_builder(
        client,
        organization,
        team,
        page,
        per_page,
    )?;
    get_organizations_organization_teams_team_members_execute(builder)
}

/// POST /organizations/{organization}/teams/{team}/members
/// Add a member to a team
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_teams_team_members_execute()` to send, or `post_organizations_organization_teams_team_members` for simplest API.

pub fn post_organizations_organization_teams_team_members_builder(
    client: &SimpleHttpClient,
    organization: &str,
    team: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/teams/{}/members",
        organization, team,
    );

    // Build request
    client
        .post(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/teams/{team}/members
/// Add a member to a team
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `post_organizations_organization_teams_team_members_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from post_organizations_organization_teams_team_members_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_teams_team_members_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST /organizations/{organization}/teams/{team}/members
/// Add a member to a team
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_teams_team_members_builder()` + `post_organizations_organization_teams_team_members_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_teams_team_members(
    client: &SimpleHttpClient,
    organization: &str,
    team: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_teams_team_members_builder(client, organization, team)?;
    post_organizations_organization_teams_team_members_execute(builder)
}

/// GET /organizations/{organization}/teams/{team}/members/{id}
/// Get a team member
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_teams_team_members_id_execute()` to send, or `get_organizations_organization_teams_team_members_id` for simplest API.

pub fn get_organizations_organization_teams_team_members_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    team: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/teams/{}/members/{}",
        organization, team, id,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/teams/{team}/members/{id}
/// Get a team member
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_organizations_organization_teams_team_members_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_organizations_organization_teams_team_members_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_teams_team_members_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /organizations/{organization}/teams/{team}/members/{id}
/// Get a team member
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_teams_team_members_id_builder()` + `get_organizations_organization_teams_team_members_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_teams_team_members_id(
    client: &SimpleHttpClient,
    organization: &str,
    team: &str,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_teams_team_members_id_builder(
        client,
        organization,
        team,
        id,
    )?;
    get_organizations_organization_teams_team_members_id_execute(builder)
}

/// DELETE /organizations/{organization}/teams/{team}/members/{id}
/// Remove a member from a team
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_teams_team_members_id_execute()` to send, or `delete_organizations_organization_teams_team_members_id` for simplest API.

pub fn delete_organizations_organization_teams_team_members_id_builder(
    client: &SimpleHttpClient,
    organization: &str,
    team: &str,
    id: &str,
    delete_passwords: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.com/organizations/{}/teams/{}/members/{}",
        organization, team, id,
    );

    // Build request
    client
        .delete(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "delete_passwords",
            delete_passwords
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// DELETE /organizations/{organization}/teams/{team}/members/{id}
/// Remove a member from a team
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `delete_organizations_organization_teams_team_members_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from delete_organizations_organization_teams_team_members_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_teams_team_members_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// DELETE /organizations/{organization}/teams/{team}/members/{id}
/// Remove a member from a team
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_teams_team_members_id_builder()` + `delete_organizations_organization_teams_team_members_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_teams_team_members_id(
    client: &SimpleHttpClient,
    organization: &str,
    team: &str,
    id: &str,
    delete_passwords: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_teams_team_members_id_builder(
        client,
        organization,
        team,
        id,
        delete_passwords,
    )?;
    delete_organizations_organization_teams_team_members_id_execute(builder)
}

/// GET /regions
/// List public regions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_regions_execute()` to send, or `get_regions` for simplest API.

pub fn get_regions_builder(
    client: &SimpleHttpClient,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.com/regions",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "page",
            page.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "per_page",
            per_page
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET /regions
/// List public regions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_regions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_regions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_regions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /regions
/// List public regions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_regions_builder()` + `get_regions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_regions(
    client: &SimpleHttpClient,
    page: Option<&str>,
    per_page: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_regions_builder(client, page, per_page)?;
    get_regions_execute(builder)
}

/// GET /user
/// Get current user
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_user_execute()` to send, or `get_user` for simplest API.

pub fn get_user_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.com/user",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /user
/// Get current user
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `get_user_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from get_user_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_user_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /user
/// Get current user
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_user_builder()` + `get_user_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_user(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_user_builder(client)?;
    get_user_execute(builder)
}
