//! Auto-generated API clients for planetscale.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `planetscale`

#![cfg(feature = "planetscale")]

pub mod types;

use crate::providers::planetscale::clients::types::*;
use crate::providers::planetscale::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET /organizations
/// List organizations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_execute()` to send, or `get_organizations` for simplest API.

pub fn get_organizations_builder(
    client: &SimpleHttpClient,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://api.planetscale.com/v1/organizations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations
/// List organizations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_execute()` or `get_organizations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations
/// List organizations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_task()`.
/// For the simplest API, use `get_organizations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsArgs {
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations
/// List organizations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_builder()` + `get_organizations_execute()`.
/// For task-level control, use `get_organizations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations(
    client: &SimpleHttpClient,
    args: &GetOrganizationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_builder(client, args.page.clone(), args.per_page.clone())?;
    get_organizations_execute(builder)
}

/// GET /organizations/{organization}
/// Get an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_execute()` to send, or `get_organizations_organization` for simplest API.

pub fn get_organizations_organization_builder(
    client: &SimpleHttpClient,
    organization: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}",
        organization.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}
/// Get an organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_execute()` or `get_organizations_organization`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}
/// Get an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_task()`.
/// For the simplest API, use `get_organizations_organization()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationArgs {
    /// Path parameter: organization
    pub organization: String,
}

/// GET /organizations/{organization}
/// Get an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_builder()` + `get_organizations_organization_execute()`.
/// For task-level control, use `get_organizations_organization_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_builder(client, args.organization.clone())?;
    get_organizations_organization_execute(builder)
}

/// PATCH /organizations/{organization}
/// Update an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_execute()` to send, or `patch_organizations_organization` for simplest API.

pub fn patch_organizations_organization_builder(
    client: &SimpleHttpClient,
    organization: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}",
        organization.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}
/// Update an organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_execute()` or `patch_organizations_organization`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}
/// Update an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_task()`.
/// For the simplest API, use `patch_organizations_organization()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = patch_organizations_organization_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationArgs {
    /// Path parameter: organization
    pub organization: String,
}

/// PATCH /organizations/{organization}
/// Update an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_builder()` + `patch_organizations_organization_execute()`.
/// For task-level control, use `patch_organizations_organization_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_builder(client, args.organization.clone())?;
    patch_organizations_organization_execute(builder)
}

/// GET /organizations/{organization}/audit-log
/// List audit logs
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_audit_log_execute()` to send, or `get_organizations_organization_audit_log` for simplest API.

pub fn get_organizations_organization_audit_log_builder(
    client: &SimpleHttpClient,
    organization: String,
    starting_after: Option<String>,
    ending_before: Option<String>,
    limit: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/audit-log",
        organization.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/audit-log
/// List audit logs
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_audit_log_execute()` or `get_organizations_organization_audit_log`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_audit_log_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_audit_log_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/audit-log
/// List audit logs
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_audit_log_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_audit_log_task()`.
/// For the simplest API, use `get_organizations_organization_audit_log()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_audit_log_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_audit_log_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_audit_log_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_audit_log`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationAuditLogArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
}

/// GET /organizations/{organization}/audit-log
/// List audit logs
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_audit_log_builder()` + `get_organizations_organization_audit_log_execute()`.
/// For task-level control, use `get_organizations_organization_audit_log_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_audit_log(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationAuditLogArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_audit_log_builder(
        client,
        args.organization.clone(),
        args.starting_after.clone(),
        args.ending_before.clone(),
        args.limit.clone(),
    )?;
    get_organizations_organization_audit_log_execute(builder)
}

/// GET /organizations/{organization}/cluster-size-skus
/// List available cluster sizes
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_cluster_size_skus_execute()` to send, or `get_organizations_organization_cluster_size_skus` for simplest API.

pub fn get_organizations_organization_cluster_size_skus_builder(
    client: &SimpleHttpClient,
    organization: String,
    engine: Option<String>,
    rates: Option<String>,
    region: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/cluster-size-skus",
        organization.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = engine {
        query_parts.push(format!("engine={}", val));
    }
    if let Some(val) = rates {
        query_parts.push(format!("rates={}", val));
    }
    if let Some(val) = region {
        query_parts.push(format!("region={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/cluster-size-skus
/// List available cluster sizes
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_cluster_size_skus_execute()` or `get_organizations_organization_cluster_size_skus`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_cluster_size_skus_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_cluster_size_skus_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/cluster-size-skus
/// List available cluster sizes
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_cluster_size_skus_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_cluster_size_skus_task()`.
/// For the simplest API, use `get_organizations_organization_cluster_size_skus()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_cluster_size_skus_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_cluster_size_skus_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_cluster_size_skus_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_cluster_size_skus`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationClusterSizeSkusArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Query parameter: engine
    pub engine: Option<String>,
    /// Query parameter: rates
    pub rates: Option<String>,
    /// Query parameter: region
    pub region: Option<String>,
}

/// GET /organizations/{organization}/cluster-size-skus
/// List available cluster sizes
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_cluster_size_skus_builder()` + `get_organizations_organization_cluster_size_skus_execute()`.
/// For task-level control, use `get_organizations_organization_cluster_size_skus_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_cluster_size_skus(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationClusterSizeSkusArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_cluster_size_skus_builder(
        client,
        args.organization.clone(),
        args.engine.clone(),
        args.rates.clone(),
        args.region.clone(),
    )?;
    get_organizations_organization_cluster_size_skus_execute(builder)
}

/// GET /organizations/{organization}/databases
/// List databases
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_execute()` to send, or `get_organizations_organization_databases` for simplest API.

pub fn get_organizations_organization_databases_builder(
    client: &SimpleHttpClient,
    organization: String,
    q: Option<String>,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases",
        organization.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = q {
        query_parts.push(format!("q={}", val));
    }
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases
/// List databases
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_execute()` or `get_organizations_organization_databases`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases
/// List databases
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_task()`.
/// For the simplest API, use `get_organizations_organization_databases()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Query parameter: q
    pub q: Option<String>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases
/// List databases
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_builder()` + `get_organizations_organization_databases_execute()`.
/// For task-level control, use `get_organizations_organization_databases_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_builder(
        client,
        args.organization.clone(),
        args.q.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_databases_execute(builder)
}

/// POST /organizations/{organization}/databases
/// Create a database
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_execute()` to send, or `post_organizations_organization_databases` for simplest API.

pub fn post_organizations_organization_databases_builder(
    client: &SimpleHttpClient,
    organization: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases",
        organization.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases
/// Create a database
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_execute()` or `post_organizations_organization_databases`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases
/// Create a database
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_task()`.
/// For the simplest API, use `post_organizations_organization_databases()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_organizations_organization_databases_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesArgs {
    /// Path parameter: organization
    pub organization: String,
}

/// POST /organizations/{organization}/databases
/// Create a database
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_builder()` + `post_organizations_organization_databases_execute()`.
/// For task-level control, use `post_organizations_organization_databases_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_builder(client, args.organization.clone())?;
    post_organizations_organization_databases_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}
/// Get a database
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_execute()` to send, or `get_organizations_organization_databases_database` for simplest API.

pub fn get_organizations_organization_databases_database_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}
/// Get a database
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_execute()` or `get_organizations_organization_databases_database`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}
/// Get a database
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// GET /organizations/{organization}/databases/{database}
/// Get a database
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_builder()` + `get_organizations_organization_databases_database_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
    )?;
    get_organizations_organization_databases_database_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}
/// Update database settings
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_execute()` to send, or `patch_organizations_organization_databases_database` for simplest API.

pub fn patch_organizations_organization_databases_database_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}
/// Update database settings
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_execute()` or `patch_organizations_organization_databases_database`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}
/// Update database settings
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = patch_organizations_organization_databases_database_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// PATCH /organizations/{organization}/databases/{database}
/// Update database settings
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_builder()` + `patch_organizations_organization_databases_database_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
    )?;
    patch_organizations_organization_databases_database_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}
/// Delete a database
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_execute()` to send, or `delete_organizations_organization_databases_database` for simplest API.

pub fn delete_organizations_organization_databases_database_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}
/// Delete a database
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_databases_database_execute()` or `delete_organizations_organization_databases_database`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}
/// Delete a database
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_databases_database_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_databases_database_task()`.
/// For the simplest API, use `delete_organizations_organization_databases_database()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_databases_database_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_organizations_organization_databases_database_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_databases_database`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationDatabasesDatabaseArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// DELETE /organizations/{organization}/databases/{database}
/// Delete a database
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_builder()` + `delete_organizations_organization_databases_database_execute()`.
/// For task-level control, use `delete_organizations_organization_databases_database_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationDatabasesDatabaseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
    )?;
    delete_organizations_organization_databases_database_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/backup-policies
/// List backup policies
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_backup_policies_execute()` to send, or `get_organizations_organization_databases_database_backup_policies` for simplest API.

pub fn get_organizations_organization_databases_database_backup_policies_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/backup-policies",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/backup-policies
/// List backup policies
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_backup_policies_execute()` or `get_organizations_organization_databases_database_backup_policies`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_backup_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_backup_policies_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/backup-policies
/// List backup policies
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_backup_policies_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_backup_policies_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_backup_policies()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_backup_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_backup_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_backup_policies_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_backup_policies`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBackupPoliciesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/backup-policies
/// List backup policies
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_backup_policies_builder()` + `get_organizations_organization_databases_database_backup_policies_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_backup_policies_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_backup_policies(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBackupPoliciesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_backup_policies_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_databases_database_backup_policies_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/backup-policies
/// Create a backup policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_backup_policies_execute()` to send, or `post_organizations_organization_databases_database_backup_policies` for simplest API.

pub fn post_organizations_organization_databases_database_backup_policies_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/backup-policies",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/backup-policies
/// Create a backup policy
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_backup_policies_execute()` or `post_organizations_organization_databases_database_backup_policies`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_backup_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_backup_policies_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/backup-policies
/// Create a backup policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_backup_policies_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_backup_policies_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_backup_policies()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_backup_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_backup_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_organizations_organization_databases_database_backup_policies_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_backup_policies`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseBackupPoliciesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// POST /organizations/{organization}/databases/{database}/backup-policies
/// Create a backup policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_backup_policies_builder()` + `post_organizations_organization_databases_database_backup_policies_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_backup_policies_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_backup_policies(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseBackupPoliciesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_backup_policies_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
    )?;
    post_organizations_organization_databases_database_backup_policies_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Get a backup policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_backup_policies_id_execute()` to send, or `get_organizations_organization_databases_database_backup_policies_id` for simplest API.

pub fn get_organizations_organization_databases_database_backup_policies_id_builder(
    client: &SimpleHttpClient,
    id: String,
    organization: String,
    database: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/backup-policies/{}",
        organization.as_str(),
        database.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Get a backup policy
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_backup_policies_id_execute()` or `get_organizations_organization_databases_database_backup_policies_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_backup_policies_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_backup_policies_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Get a backup policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_backup_policies_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_backup_policies_id_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_backup_policies_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_backup_policies_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_backup_policies_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_backup_policies_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_backup_policies_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBackupPoliciesIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// GET /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Get a backup policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_backup_policies_id_builder()` + `get_organizations_organization_databases_database_backup_policies_id_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_backup_policies_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_backup_policies_id(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBackupPoliciesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_backup_policies_id_builder(
        client,
        args.id.clone(),
        args.organization.clone(),
        args.database.clone(),
    )?;
    get_organizations_organization_databases_database_backup_policies_id_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Update a backup policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_backup_policies_id_execute()` to send, or `patch_organizations_organization_databases_database_backup_policies_id` for simplest API.

pub fn patch_organizations_organization_databases_database_backup_policies_id_builder(
    client: &SimpleHttpClient,
    id: String,
    organization: String,
    database: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/backup-policies/{}",
        organization.as_str(),
        database.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Update a backup policy
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_backup_policies_id_execute()` or `patch_organizations_organization_databases_database_backup_policies_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_backup_policies_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_backup_policies_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Update a backup policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_backup_policies_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_backup_policies_id_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_backup_policies_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_backup_policies_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_backup_policies_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        patch_organizations_organization_databases_database_backup_policies_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_backup_policies_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseBackupPoliciesIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// PATCH /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Update a backup policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_backup_policies_id_builder()` + `patch_organizations_organization_databases_database_backup_policies_id_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_backup_policies_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_backup_policies_id(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseBackupPoliciesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_backup_policies_id_builder(
        client,
        args.id.clone(),
        args.organization.clone(),
        args.database.clone(),
    )?;
    patch_organizations_organization_databases_database_backup_policies_id_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Delete a backup policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_backup_policies_id_execute()` to send, or `delete_organizations_organization_databases_database_backup_policies_id` for simplest API.

pub fn delete_organizations_organization_databases_database_backup_policies_id_builder(
    client: &SimpleHttpClient,
    id: String,
    organization: String,
    database: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/backup-policies/{}",
        organization.as_str(),
        database.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Delete a backup policy
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_databases_database_backup_policies_id_execute()` or `delete_organizations_organization_databases_database_backup_policies_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_backup_policies_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_backup_policies_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Delete a backup policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_databases_database_backup_policies_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_databases_database_backup_policies_id_task()`.
/// For the simplest API, use `delete_organizations_organization_databases_database_backup_policies_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_backup_policies_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_databases_database_backup_policies_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        delete_organizations_organization_databases_database_backup_policies_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_databases_database_backup_policies_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationDatabasesDatabaseBackupPoliciesIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// DELETE /organizations/{organization}/databases/{database}/backup-policies/{id}
/// Delete a backup policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_backup_policies_id_builder()` + `delete_organizations_organization_databases_database_backup_policies_id_execute()`.
/// For task-level control, use `delete_organizations_organization_databases_database_backup_policies_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_backup_policies_id(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationDatabasesDatabaseBackupPoliciesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_backup_policies_id_builder(
        client,
        args.id.clone(),
        args.organization.clone(),
        args.database.clone(),
    )?;
    delete_organizations_organization_databases_database_backup_policies_id_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches
/// List branches
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_execute()` to send, or `get_organizations_organization_databases_database_branches` for simplest API.

pub fn get_organizations_organization_databases_database_branches_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    q: Option<String>,
    production: Option<String>,
    safe_migrations: Option<String>,
    order: Option<String>,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = q {
        query_parts.push(format!("q={}", val));
    }
    if let Some(val) = production {
        query_parts.push(format!("production={}", val));
    }
    if let Some(val) = safe_migrations {
        query_parts.push(format!("safe_migrations={}", val));
    }
    if let Some(val) = order {
        query_parts.push(format!("order={}", val));
    }
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches
/// List branches
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_execute()` or `get_organizations_organization_databases_database_branches`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches
/// List branches
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_branches_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: q
    pub q: Option<String>,
    /// Query parameter: production
    pub production: Option<String>,
    /// Query parameter: safe_migrations
    pub safe_migrations: Option<String>,
    /// Query parameter: order
    pub order: Option<String>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches
/// List branches
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_builder()` + `get_organizations_organization_databases_database_branches_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.q.clone(),
        args.production.clone(),
        args.safe_migrations.clone(),
        args.order.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_databases_database_branches_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches
/// Create a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_execute()` to send, or `post_organizations_organization_databases_database_branches` for simplest API.

pub fn post_organizations_organization_databases_database_branches_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches
/// Create a branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_branches_execute()` or `post_organizations_organization_databases_database_branches`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches
/// Create a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_branches_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_branches_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_branches()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_branches_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_organizations_organization_databases_database_branches_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_branches`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseBranchesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// POST /organizations/{organization}/databases/{database}/branches
/// Create a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_builder()` + `post_organizations_organization_databases_database_branches_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_branches_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseBranchesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_branches_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
    )?;
    post_organizations_organization_databases_database_branches_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}
/// Get a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_execute()` to send, or `get_organizations_organization_databases_database_branches_branch` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}
/// Get a branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_execute()` or `get_organizations_organization_databases_database_branches_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}
/// Get a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_branches_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}
/// Get a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_builder()` + `get_organizations_organization_databases_database_branches_branch_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.branch.clone(),
    )?;
    get_organizations_organization_databases_database_branches_branch_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}
/// Update a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}
/// Update a branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_branches_branch_execute()` or `patch_organizations_organization_databases_database_branches_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}
/// Update a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_branches_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_branches_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_branches_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = patch_organizations_organization_databases_database_branches_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_branches_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}
/// Update a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_builder()` + `patch_organizations_organization_databases_database_branches_branch_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_branches_branch_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.branch.clone(),
    )?;
    patch_organizations_organization_databases_database_branches_branch_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}
/// Delete a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    delete_descendants: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = delete_descendants {
        query_parts.push(format!("delete_descendants={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}
/// Delete a branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_databases_database_branches_branch_execute()` or `delete_organizations_organization_databases_database_branches_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}
/// Delete a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_databases_database_branches_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_task()`.
/// For the simplest API, use `delete_organizations_organization_databases_database_branches_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_databases_database_branches_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_organizations_organization_databases_database_branches_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_databases_database_branches_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: delete_descendants
    pub delete_descendants: Option<String>,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}
/// Delete a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_builder()` + `delete_organizations_organization_databases_database_branches_branch_execute()`.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_branches_branch_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.branch.clone(),
        args.delete_descendants.clone(),
    )?;
    delete_organizations_organization_databases_database_branches_branch_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// List backups
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_backups_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_backups` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_backups_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    all: Option<String>,
    state: Option<String>,
    policy: Option<String>,
    from: Option<String>,
    to: Option<String>,
    running_at: Option<String>,
    production: Option<String>,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/backups",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = all {
        query_parts.push(format!("all={}", val));
    }
    if let Some(val) = state {
        query_parts.push(format!("state={}", val));
    }
    if let Some(val) = policy {
        query_parts.push(format!("policy={}", val));
    }
    if let Some(val) = from {
        query_parts.push(format!("from={}", val));
    }
    if let Some(val) = to {
        query_parts.push(format!("to={}", val));
    }
    if let Some(val) = running_at {
        query_parts.push(format!("running_at={}", val));
    }
    if let Some(val) = production {
        query_parts.push(format!("production={}", val));
    }
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// List backups
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_backups_execute()` or `get_organizations_organization_databases_database_branches_branch_backups`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_backups_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_backups_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// List backups
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_backups_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_backups_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_backups()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_backups_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_backups_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_backups_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_backups`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchBackupsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: all
    pub all: Option<String>,
    /// Query parameter: state
    pub state: Option<String>,
    /// Query parameter: policy
    pub policy: Option<String>,
    /// Query parameter: from
    pub from: Option<String>,
    /// Query parameter: to
    pub to: Option<String>,
    /// Query parameter: running_at
    pub running_at: Option<String>,
    /// Query parameter: production
    pub production: Option<String>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// List backups
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_backups_builder()` + `get_organizations_organization_databases_database_branches_branch_backups_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_backups_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_backups(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchBackupsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_backups_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
            args.all.clone(),
            args.state.clone(),
            args.policy.clone(),
            args.from.clone(),
            args.to.clone(),
            args.running_at.clone(),
            args.production.clone(),
            args.page.clone(),
            args.per_page.clone(),
        )?;
    get_organizations_organization_databases_database_branches_branch_backups_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// Create a backup
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_backups_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_backups` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_backups_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/backups",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// Create a backup
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_branches_branch_backups_execute()` or `post_organizations_organization_databases_database_branches_branch_backups`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_backups_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_backups_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// Create a backup
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_branches_branch_backups_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_backups_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_branches_branch_backups()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_backups_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_branches_branch_backups_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        post_organizations_organization_databases_database_branches_branch_backups_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_branches_branch_backups`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchBackupsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/backups
/// Create a backup
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_backups_builder()` + `post_organizations_organization_databases_database_branches_branch_backups_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_backups_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_backups(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchBackupsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_backups_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
        )?;
    post_organizations_organization_databases_database_branches_branch_backups_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Get a backup
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_backups_id_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_backups_id` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_backups_id_builder(
    client: &SimpleHttpClient,
    id: String,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/backups/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Get a backup
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_backups_id_execute()` or `get_organizations_organization_databases_database_branches_branch_backups_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_backups_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_backups_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Get a backup
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_backups_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_backups_id_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_backups_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_backups_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_backups_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_backups_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_backups_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchBackupsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Get a backup
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_backups_id_builder()` + `get_organizations_organization_databases_database_branches_branch_backups_id_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_backups_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_backups_id(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchBackupsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_backups_id_builder(
            client,
            args.id.clone(),
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
        )?;
    get_organizations_organization_databases_database_branches_branch_backups_id_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Update a backup
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_backups_id_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch_backups_id` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_backups_id_builder(
    client: &SimpleHttpClient,
    id: String,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/backups/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Update a backup
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_branches_branch_backups_id_execute()` or `patch_organizations_organization_databases_database_branches_branch_backups_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_backups_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_backups_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Update a backup
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_branches_branch_backups_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_backups_id_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_branches_branch_backups_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_backups_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_branches_branch_backups_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = patch_organizations_organization_databases_database_branches_branch_backups_id_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_branches_branch_backups_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchBackupsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Update a backup
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_backups_id_builder()` + `patch_organizations_organization_databases_database_branches_branch_backups_id_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_backups_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_backups_id(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchBackupsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_branches_branch_backups_id_builder(
            client,
            args.id.clone(),
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
        )?;
    patch_organizations_organization_databases_database_branches_branch_backups_id_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Delete a backup
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_backups_id_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_backups_id` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_backups_id_builder(
    client: &SimpleHttpClient,
    id: String,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/backups/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Delete a backup
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_databases_database_branches_branch_backups_id_execute()` or `delete_organizations_organization_databases_database_branches_branch_backups_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_backups_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_backups_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Delete a backup
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_databases_database_branches_branch_backups_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_backups_id_task()`.
/// For the simplest API, use `delete_organizations_organization_databases_database_branches_branch_backups_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_backups_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_databases_database_branches_branch_backups_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        delete_organizations_organization_databases_database_branches_branch_backups_id_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_databases_database_branches_branch_backups_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchBackupsIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/backups/{id}
/// Delete a backup
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_backups_id_builder()` + `delete_organizations_organization_databases_database_branches_branch_backups_id_execute()`.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_backups_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_backups_id(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchBackupsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        delete_organizations_organization_databases_database_branches_branch_backups_id_builder(
            client,
            args.id.clone(),
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
        )?;
    delete_organizations_organization_databases_database_branches_branch_backups_id_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncer-resizes
/// Get bouncer resize requests
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_bouncer_resizes_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_bouncer_resizes` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_bouncer_resizes_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/bouncer-resizes",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncer-resizes
/// Get bouncer resize requests
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_bouncer_resizes_execute()` or `get_organizations_organization_databases_database_branches_branch_bouncer_resizes`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_bouncer_resizes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_bouncer_resizes_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncer-resizes
/// Get bouncer resize requests
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_bouncer_resizes_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_bouncer_resizes_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_bouncer_resizes()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_bouncer_resizes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_bouncer_resizes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_bouncer_resizes_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_bouncer_resizes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchBouncerResizesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncer-resizes
/// Get bouncer resize requests
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_bouncer_resizes_builder()` + `get_organizations_organization_databases_database_branches_branch_bouncer_resizes_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_bouncer_resizes_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_bouncer_resizes(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchBouncerResizesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_bouncer_resizes_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
            args.page.clone(),
            args.per_page.clone(),
        )?;
    get_organizations_organization_databases_database_branches_branch_bouncer_resizes_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// List bouncers
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_bouncers_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_bouncers` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/bouncers",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// List bouncers
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_bouncers_execute()` or `get_organizations_organization_databases_database_branches_branch_bouncers`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_bouncers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// List bouncers
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_bouncers_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_bouncers_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_bouncers()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_bouncers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_bouncers_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_bouncers`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchBouncersArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// List bouncers
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_bouncers_builder()` + `get_organizations_organization_databases_database_branches_branch_bouncers_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_bouncers_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchBouncersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_bouncers_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
            args.page.clone(),
            args.per_page.clone(),
        )?;
    get_organizations_organization_databases_database_branches_branch_bouncers_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// Create a bouncer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_bouncers_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_bouncers` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_bouncers_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/bouncers",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// Create a bouncer
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_branches_branch_bouncers_execute()` or `post_organizations_organization_databases_database_branches_branch_bouncers`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_bouncers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_bouncers_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// Create a bouncer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_branches_branch_bouncers_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_bouncers_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_branches_branch_bouncers()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_bouncers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_branches_branch_bouncers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        post_organizations_organization_databases_database_branches_branch_bouncers_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_branches_branch_bouncers`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchBouncersArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/bouncers
/// Create a bouncer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_bouncers_builder()` + `post_organizations_organization_databases_database_branches_branch_bouncers_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_bouncers_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_bouncers(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchBouncersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_bouncers_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
        )?;
    post_organizations_organization_databases_database_branches_branch_bouncers_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Get a bouncer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    bouncer: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/bouncers/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        bouncer.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Get a bouncer
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_execute()` or `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Get a bouncer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_bouncers_bouncer`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchBouncersBouncerArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: bouncer
    pub bouncer: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Get a bouncer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder()` + `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers_bouncer(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchBouncersBouncerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
            args.bouncer.clone(),
        )?;
    get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_execute(
        builder,
    )
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Delete a bouncer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    bouncer: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/bouncers/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        bouncer.as_str(),
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Delete a bouncer
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_execute()` or `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Delete a bouncer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_task()`.
/// For the simplest API, use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchBouncersBouncerArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: bouncer
    pub bouncer: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}
/// Delete a bouncer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder()` + `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_execute()`.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchBouncersBouncerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.bouncer.clone())?;
    delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Get bouncer resize requests
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    bouncer: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/bouncers/{}/resizes",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        bouncer.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Get bouncer resize requests
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute()` or `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Get bouncer resize requests
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchBouncersBouncerResizesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: bouncer
    pub bouncer: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Get bouncer resize requests
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()` + `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchBouncersBouncerResizesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.bouncer.clone(), args.page.clone(), args.per_page.clone())?;
    get_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Upsert a bouncer resize request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    bouncer: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/bouncers/{}/resizes",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        bouncer.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Upsert a bouncer resize request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute()` or `patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Upsert a bouncer resize request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchBouncersBouncerResizesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: bouncer
    pub bouncer: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Upsert a bouncer resize request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()` + `patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchBouncersBouncerResizesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.bouncer.clone())?;
    patch_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Cancel a resize request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    bouncer: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/bouncers/{}/resizes",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        bouncer.as_str(),
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Cancel a resize request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute()` or `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Cancel a resize request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_task()`.
/// For the simplest API, use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchBouncersBouncerResizesArgs
{
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: bouncer
    pub bouncer: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/bouncers/{bouncer}/resizes
/// Cancel a resize request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder()` + `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute()`.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchBouncersBouncerResizesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.bouncer.clone())?;
    delete_organizations_organization_databases_database_branches_branch_bouncers_bouncer_resizes_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Get branch change requests
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_changes_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_changes` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_changes_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/changes",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Get branch change requests
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_changes_execute()` or `get_organizations_organization_databases_database_branches_branch_changes`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_changes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_changes_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Get branch change requests
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_changes_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_changes_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_changes()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_changes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_changes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_changes_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_changes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchChangesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Get branch change requests
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_changes_builder()` + `get_organizations_organization_databases_database_branches_branch_changes_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_changes_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_changes(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchChangesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_changes_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
            args.page.clone(),
            args.per_page.clone(),
        )?;
    get_organizations_organization_databases_database_branches_branch_changes_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Upsert a change request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_changes_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch_changes` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_changes_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/changes",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Upsert a change request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_branches_branch_changes_execute()` or `patch_organizations_organization_databases_database_branches_branch_changes`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_changes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_changes_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Upsert a change request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_branches_branch_changes_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_changes_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_branches_branch_changes()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_changes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_branches_branch_changes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        patch_organizations_organization_databases_database_branches_branch_changes_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_branches_branch_changes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchChangesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/changes
/// Upsert a change request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_changes_builder()` + `patch_organizations_organization_databases_database_branches_branch_changes_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_changes_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_changes(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchChangesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_branches_branch_changes_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
        )?;
    patch_organizations_organization_databases_database_branches_branch_changes_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes/{id}
/// Get a branch change request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_changes_id_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_changes_id` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_changes_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/changes/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes/{id}
/// Get a branch change request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_changes_id_execute()` or `get_organizations_organization_databases_database_branches_branch_changes_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_changes_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_changes_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes/{id}
/// Get a branch change request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_changes_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_changes_id_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_changes_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_changes_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_changes_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_changes_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_changes_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchChangesIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/changes/{id}
/// Get a branch change request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_changes_id_builder()` + `get_organizations_organization_databases_database_branches_branch_changes_id_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_changes_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_changes_id(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchChangesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_changes_id_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
            args.id.clone(),
        )?;
    get_organizations_organization_databases_database_branches_branch_changes_id_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/cluster
/// Change a branch cluster configuration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_cluster_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch_cluster` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_cluster_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/cluster",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/cluster
/// Change a branch cluster configuration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_branches_branch_cluster_execute()` or `patch_organizations_organization_databases_database_branches_branch_cluster`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_cluster_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_cluster_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/cluster
/// Change a branch cluster configuration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_branches_branch_cluster_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_cluster_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_branches_branch_cluster()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_cluster_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_branches_branch_cluster_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        patch_organizations_organization_databases_database_branches_branch_cluster_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_branches_branch_cluster`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchClusterArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/cluster
/// Change a branch cluster configuration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_cluster_builder()` + `patch_organizations_organization_databases_database_branches_branch_cluster_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_cluster_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_cluster(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchClusterArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_branches_branch_cluster_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
        )?;
    patch_organizations_organization_databases_database_branches_branch_cluster_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/demote
/// Demote a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_demote_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_demote` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_demote_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/demote",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/demote
/// Demote a branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_branches_branch_demote_execute()` or `post_organizations_organization_databases_database_branches_branch_demote`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_demote_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_demote_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/demote
/// Demote a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_branches_branch_demote_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_demote_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_branches_branch_demote()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_demote_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_branches_branch_demote_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        post_organizations_organization_databases_database_branches_branch_demote_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_branches_branch_demote`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchDemoteArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/demote
/// Demote a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_demote_builder()` + `post_organizations_organization_databases_database_branches_branch_demote_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_demote_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_demote(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchDemoteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_demote_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
        )?;
    post_organizations_organization_databases_database_branches_branch_demote_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/extensions
/// List cluster extensions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_extensions_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_extensions` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_extensions_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/extensions",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/extensions
/// List cluster extensions
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_extensions_execute()` or `get_organizations_organization_databases_database_branches_branch_extensions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_extensions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_extensions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/extensions
/// List cluster extensions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_extensions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_extensions_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_extensions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_extensions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_extensions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_extensions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_extensions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchExtensionsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/extensions
/// List cluster extensions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_extensions_builder()` + `get_organizations_organization_databases_database_branches_branch_extensions_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_extensions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_extensions(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchExtensionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_extensions_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
        )?;
    get_organizations_organization_databases_database_branches_branch_extensions_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Get keyspaces
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_keyspaces_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_keyspaces` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/keyspaces",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Get keyspaces
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_keyspaces_execute()` or `get_organizations_organization_databases_database_branches_branch_keyspaces`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_keyspaces_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Get keyspaces
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_keyspaces_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_keyspaces_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_keyspaces()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_keyspaces_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_keyspaces_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_keyspaces`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchKeyspacesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Get keyspaces
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_keyspaces_builder()` + `get_organizations_organization_databases_database_branches_branch_keyspaces_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_keyspaces_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchKeyspacesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_keyspaces_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
            args.page.clone(),
            args.per_page.clone(),
        )?;
    get_organizations_organization_databases_database_branches_branch_keyspaces_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Create a keyspace
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_keyspaces_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_keyspaces` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_keyspaces_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/keyspaces",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Create a keyspace
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_branches_branch_keyspaces_execute()` or `post_organizations_organization_databases_database_branches_branch_keyspaces`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_keyspaces_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_keyspaces_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Create a keyspace
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_branches_branch_keyspaces_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_keyspaces_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_branches_branch_keyspaces()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_keyspaces_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_branches_branch_keyspaces_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        post_organizations_organization_databases_database_branches_branch_keyspaces_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_branches_branch_keyspaces`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchKeyspacesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces
/// Create a keyspace
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_keyspaces_builder()` + `post_organizations_organization_databases_database_branches_branch_keyspaces_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_keyspaces_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_keyspaces(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchKeyspacesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_keyspaces_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
        )?;
    post_organizations_organization_databases_database_branches_branch_keyspaces_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Get a keyspace
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    keyspace: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/keyspaces/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        keyspace.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Get a keyspace
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute()` or `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Get a keyspace
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchKeyspacesKeyspaceArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: keyspace
    pub keyspace: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Get a keyspace
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()` + `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchKeyspacesKeyspaceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.keyspace.clone())?;
    get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute(
        builder,
    )
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Configure keyspace settings
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    keyspace: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/keyspaces/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        keyspace.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Configure keyspace settings
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute()` or `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Configure keyspace settings
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchKeyspacesKeyspaceArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: keyspace
    pub keyspace: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Configure keyspace settings
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()` + `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchKeyspacesKeyspaceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.keyspace.clone())?;
    patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute(
        builder,
    )
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Delete a keyspace
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    keyspace: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/keyspaces/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        keyspace.as_str(),
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Delete a keyspace
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute()` or `delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Delete a keyspace
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_task()`.
/// For the simplest API, use `delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchKeyspacesKeyspaceArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: keyspace
    pub keyspace: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}
/// Delete a keyspace
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder()` + `delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute()`.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchKeyspacesKeyspaceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.keyspace.clone())?;
    delete_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/rollout-status
/// Get keyspace rollout status
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    keyspace: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/keyspaces/{}/rollout-status",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        keyspace.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/rollout-status
/// Get keyspace rollout status
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_execute()` or `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/rollout-status
/// Get keyspace rollout status
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchKeyspacesKeyspaceRolloutStatusArgs
{
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: keyspace
    pub keyspace: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/rollout-status
/// Get keyspace rollout status
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_builder()` + `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchKeyspacesKeyspaceRolloutStatusArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.keyspace.clone())?;
    get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_rollout_status_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Get the VSchema for the keyspace
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    keyspace: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/keyspaces/{}/vschema",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        keyspace.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Get the VSchema for the keyspace
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_execute()` or `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Get the VSchema for the keyspace
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchKeyspacesKeyspaceVschemaArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: keyspace
    pub keyspace: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Get the VSchema for the keyspace
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder()` + `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchKeyspacesKeyspaceVschemaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.keyspace.clone())?;
    get_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Update the VSchema for the keyspace
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    keyspace: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/keyspaces/{}/vschema",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        keyspace.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Update the VSchema for the keyspace
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_execute()` or `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Update the VSchema for the keyspace
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchKeyspacesKeyspaceVschemaArgs
{
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: keyspace
    pub keyspace: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/keyspaces/{keyspace}/vschema
/// Update the VSchema for the keyspace
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder()` + `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchKeyspacesKeyspaceVschemaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.keyspace.clone())?;
    patch_organizations_organization_databases_database_branches_branch_keyspaces_keyspace_vschema_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/parameters
/// List cluster parameters
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_parameters_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_parameters` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_parameters_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/parameters",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/parameters
/// List cluster parameters
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_parameters_execute()` or `get_organizations_organization_databases_database_branches_branch_parameters`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_parameters_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_parameters_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/parameters
/// List cluster parameters
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_parameters_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_parameters_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_parameters()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_parameters_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_parameters_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_parameters_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_parameters`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchParametersArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/parameters
/// List cluster parameters
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_parameters_builder()` + `get_organizations_organization_databases_database_branches_branch_parameters_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_parameters_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_parameters(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchParametersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_parameters_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
        )?;
    get_organizations_organization_databases_database_branches_branch_parameters_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// List passwords
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_passwords_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_passwords` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_passwords_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    read_only_region_id: Option<String>,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/passwords",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = read_only_region_id {
        query_parts.push(format!("read_only_region_id={}", val));
    }
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// List passwords
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_passwords_execute()` or `get_organizations_organization_databases_database_branches_branch_passwords`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_passwords_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_passwords_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// List passwords
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_passwords_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_passwords_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_passwords()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_passwords_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_passwords_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_passwords_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_passwords`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchPasswordsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: read_only_region_id
    pub read_only_region_id: Option<String>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// List passwords
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_passwords_builder()` + `get_organizations_organization_databases_database_branches_branch_passwords_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_passwords_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_passwords(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchPasswordsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_passwords_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
            args.read_only_region_id.clone(),
            args.page.clone(),
            args.per_page.clone(),
        )?;
    get_organizations_organization_databases_database_branches_branch_passwords_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// Create a password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_passwords_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_passwords` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_passwords_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/passwords",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// Create a password
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_branches_branch_passwords_execute()` or `post_organizations_organization_databases_database_branches_branch_passwords`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_passwords_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_passwords_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// Create a password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_branches_branch_passwords_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_passwords_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_branches_branch_passwords()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_passwords_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_branches_branch_passwords_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        post_organizations_organization_databases_database_branches_branch_passwords_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_branches_branch_passwords`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchPasswordsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords
/// Create a password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_passwords_builder()` + `post_organizations_organization_databases_database_branches_branch_passwords_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_passwords_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_passwords(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchPasswordsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_passwords_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
        )?;
    post_organizations_organization_databases_database_branches_branch_passwords_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Get a password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_passwords_id_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_passwords_id` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_passwords_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/passwords/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Get a password
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_passwords_id_execute()` or `get_organizations_organization_databases_database_branches_branch_passwords_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_passwords_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_passwords_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Get a password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_passwords_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_passwords_id_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_passwords_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_passwords_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_passwords_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_branches_branch_passwords_id_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_passwords_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchPasswordsIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Get a password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_passwords_id_builder()` + `get_organizations_organization_databases_database_branches_branch_passwords_id_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_passwords_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_passwords_id(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchPasswordsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_passwords_id_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
            args.id.clone(),
        )?;
    get_organizations_organization_databases_database_branches_branch_passwords_id_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Update a password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_passwords_id_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch_passwords_id` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_passwords_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/passwords/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Update a password
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_branches_branch_passwords_id_execute()` or `patch_organizations_organization_databases_database_branches_branch_passwords_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_passwords_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_passwords_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Update a password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_branches_branch_passwords_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_passwords_id_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_branches_branch_passwords_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_passwords_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_branches_branch_passwords_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        patch_organizations_organization_databases_database_branches_branch_passwords_id_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_branches_branch_passwords_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchPasswordsIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Update a password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_passwords_id_builder()` + `patch_organizations_organization_databases_database_branches_branch_passwords_id_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_passwords_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_passwords_id(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchPasswordsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_branches_branch_passwords_id_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
            args.id.clone(),
        )?;
    patch_organizations_organization_databases_database_branches_branch_passwords_id_execute(
        builder,
    )
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Delete a password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_passwords_id_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_passwords_id` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_passwords_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/passwords/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Delete a password
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_databases_database_branches_branch_passwords_id_execute()` or `delete_organizations_organization_databases_database_branches_branch_passwords_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_passwords_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_passwords_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Delete a password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_databases_database_branches_branch_passwords_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_passwords_id_task()`.
/// For the simplest API, use `delete_organizations_organization_databases_database_branches_branch_passwords_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_passwords_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_databases_database_branches_branch_passwords_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        delete_organizations_organization_databases_database_branches_branch_passwords_id_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_databases_database_branches_branch_passwords_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchPasswordsIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}
/// Delete a password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_passwords_id_builder()` + `delete_organizations_organization_databases_database_branches_branch_passwords_id_execute()`.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_passwords_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_passwords_id(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchPasswordsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        delete_organizations_organization_databases_database_branches_branch_passwords_id_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
            args.id.clone(),
        )?;
    delete_organizations_organization_databases_database_branches_branch_passwords_id_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}/renew
/// Renew a password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_passwords_id_renew_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_passwords_id_renew` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_passwords_id_renew_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/passwords/{}/renew",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}/renew
/// Renew a password
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_branches_branch_passwords_id_renew_execute()` or `post_organizations_organization_databases_database_branches_branch_passwords_id_renew`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_passwords_id_renew_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_passwords_id_renew_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}/renew
/// Renew a password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_branches_branch_passwords_id_renew_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_passwords_id_renew_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_branches_branch_passwords_id_renew()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_passwords_id_renew_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_branches_branch_passwords_id_renew_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        post_organizations_organization_databases_database_branches_branch_passwords_id_renew_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_branches_branch_passwords_id_renew`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchPasswordsIdRenewArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/passwords/{id}/renew
/// Renew a password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_passwords_id_renew_builder()` + `post_organizations_organization_databases_database_branches_branch_passwords_id_renew_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_passwords_id_renew_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_passwords_id_renew(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchPasswordsIdRenewArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_branches_branch_passwords_id_renew_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.id.clone())?;
    post_organizations_organization_databases_database_branches_branch_passwords_id_renew_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/promote
/// Promote a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_promote_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_promote` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_promote_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/promote",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/promote
/// Promote a branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_branches_branch_promote_execute()` or `post_organizations_organization_databases_database_branches_branch_promote`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_promote_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_promote_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/promote
/// Promote a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_branches_branch_promote_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_promote_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_branches_branch_promote()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_promote_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_branches_branch_promote_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        post_organizations_organization_databases_database_branches_branch_promote_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_branches_branch_promote`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchPromoteArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/promote
/// Promote a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_promote_builder()` + `post_organizations_organization_databases_database_branches_branch_promote_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_promote_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_promote(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchPromoteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_promote_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
        )?;
    post_organizations_organization_databases_database_branches_branch_promote_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// List generated query patterns reports
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_query_patterns_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_query_patterns` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    starting_after: Option<String>,
    ending_before: Option<String>,
    limit: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/query-patterns",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = starting_after {
        query_parts.push(format!("starting_after={}", val));
    }
    if let Some(val) = ending_before {
        query_parts.push(format!("ending_before={}", val));
    }
    if let Some(val) = limit {
        query_parts.push(format!("limit={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// List generated query patterns reports
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_query_patterns_execute()` or `get_organizations_organization_databases_database_branches_branch_query_patterns`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_query_patterns_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// List generated query patterns reports
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_query_patterns_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_query_patterns_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_query_patterns()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_query_patterns_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_query_patterns_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_query_patterns`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchQueryPatternsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: starting_after
    pub starting_after: Option<String>,
    /// Query parameter: ending_before
    pub ending_before: Option<String>,
    /// Query parameter: limit
    pub limit: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// List generated query patterns reports
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_query_patterns_builder()` + `get_organizations_organization_databases_database_branches_branch_query_patterns_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_query_patterns_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchQueryPatternsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_query_patterns_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
            args.starting_after.clone(),
            args.ending_before.clone(),
            args.limit.clone(),
        )?;
    get_organizations_organization_databases_database_branches_branch_query_patterns_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// Create a new query patterns report
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_query_patterns_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_query_patterns` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_query_patterns_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/query-patterns",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// Create a new query patterns report
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_branches_branch_query_patterns_execute()` or `post_organizations_organization_databases_database_branches_branch_query_patterns`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_query_patterns_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_query_patterns_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// Create a new query patterns report
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_branches_branch_query_patterns_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_query_patterns_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_branches_branch_query_patterns()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_query_patterns_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_branches_branch_query_patterns_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        post_organizations_organization_databases_database_branches_branch_query_patterns_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_branches_branch_query_patterns`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchQueryPatternsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns
/// Create a new query patterns report
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_query_patterns_builder()` + `post_organizations_organization_databases_database_branches_branch_query_patterns_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_query_patterns_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_query_patterns(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchQueryPatternsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_query_patterns_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
        )?;
    post_organizations_organization_databases_database_branches_branch_query_patterns_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Show the status of a query patterns report
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_query_patterns_id_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_query_patterns_id` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/query-patterns/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Show the status of a query patterns report
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_query_patterns_id_execute()` or `get_organizations_organization_databases_database_branches_branch_query_patterns_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_query_patterns_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Show the status of a query patterns report
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_query_patterns_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_query_patterns_id_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_query_patterns_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_query_patterns_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_query_patterns_id_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_query_patterns_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchQueryPatternsIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Show the status of a query patterns report
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_query_patterns_id_builder()` + `get_organizations_organization_databases_database_branches_branch_query_patterns_id_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_query_patterns_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns_id(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchQueryPatternsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_query_patterns_id_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.id.clone())?;
    get_organizations_organization_databases_database_branches_branch_query_patterns_id_execute(
        builder,
    )
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Delete a query patterns report
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_query_patterns_id_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_query_patterns_id` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_query_patterns_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/query-patterns/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Delete a query patterns report
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_databases_database_branches_branch_query_patterns_id_execute()` or `delete_organizations_organization_databases_database_branches_branch_query_patterns_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_query_patterns_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_query_patterns_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Delete a query patterns report
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_databases_database_branches_branch_query_patterns_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_query_patterns_id_task()`.
/// For the simplest API, use `delete_organizations_organization_databases_database_branches_branch_query_patterns_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_query_patterns_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_databases_database_branches_branch_query_patterns_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_organizations_organization_databases_database_branches_branch_query_patterns_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_databases_database_branches_branch_query_patterns_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchQueryPatternsIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}
/// Delete a query patterns report
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_query_patterns_id_builder()` + `delete_organizations_organization_databases_database_branches_branch_query_patterns_id_execute()`.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_query_patterns_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_query_patterns_id(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchQueryPatternsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_branches_branch_query_patterns_id_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.id.clone())?;
    delete_organizations_organization_databases_database_branches_branch_query_patterns_id_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}/download
/// Download a finished query patterns report
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_query_patterns_id_download` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/query-patterns/{}/download",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}/download
/// Download a finished query patterns report
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_execute()` or `get_organizations_organization_databases_database_branches_branch_query_patterns_id_download`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}/download
/// Download a finished query patterns report
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_query_patterns_id_download()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_query_patterns_id_download`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchQueryPatternsIdDownloadArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/query-patterns/{id}/download
/// Download a finished query patterns report
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_builder()` + `get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_query_patterns_id_download(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchQueryPatternsIdDownloadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.id.clone())?;
    get_organizations_organization_databases_database_branches_branch_query_patterns_id_download_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/resizes
/// Cancel a change request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_resizes_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_resizes` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_resizes_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/resizes",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/resizes
/// Cancel a change request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_databases_database_branches_branch_resizes_execute()` or `delete_organizations_organization_databases_database_branches_branch_resizes`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_resizes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_resizes_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/resizes
/// Cancel a change request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_databases_database_branches_branch_resizes_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_resizes_task()`.
/// For the simplest API, use `delete_organizations_organization_databases_database_branches_branch_resizes()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_resizes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_databases_database_branches_branch_resizes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        delete_organizations_organization_databases_database_branches_branch_resizes_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_databases_database_branches_branch_resizes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchResizesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/resizes
/// Cancel a change request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_resizes_builder()` + `delete_organizations_organization_databases_database_branches_branch_resizes_execute()`.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_resizes_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_resizes(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchResizesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        delete_organizations_organization_databases_database_branches_branch_resizes_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
        )?;
    delete_organizations_organization_databases_database_branches_branch_resizes_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// List roles
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_roles_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_roles` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_roles_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// List roles
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_roles_execute()` or `get_organizations_organization_databases_database_branches_branch_roles`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_roles_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_roles_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// List roles
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_roles_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_roles_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_roles()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_roles_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_roles_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_roles_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_roles`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// List roles
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_roles_builder()` + `get_organizations_organization_databases_database_branches_branch_roles_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_roles_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_roles(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_roles_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.branch.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_databases_database_branches_branch_roles_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// Create role credentials
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_roles_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_roles` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_roles_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// Create role credentials
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_branches_branch_roles_execute()` or `post_organizations_organization_databases_database_branches_branch_roles`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_roles_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// Create role credentials
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_branches_branch_roles_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_roles_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_branches_branch_roles()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_roles_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_branches_branch_roles_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        post_organizations_organization_databases_database_branches_branch_roles_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_branches_branch_roles`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles
/// Create role credentials
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_roles_builder()` + `post_organizations_organization_databases_database_branches_branch_roles_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_roles_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_branches_branch_roles_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.branch.clone(),
    )?;
    post_organizations_organization_databases_database_branches_branch_roles_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/default
/// Get the default postgres role
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_roles_default_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_roles_default` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_roles_default_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles/default",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/default
/// Get the default postgres role
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_roles_default_execute()` or `get_organizations_organization_databases_database_branches_branch_roles_default`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_roles_default_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_roles_default_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/default
/// Get the default postgres role
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_roles_default_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_roles_default_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_roles_default()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_roles_default_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_roles_default_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_roles_default_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_roles_default`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesDefaultArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/default
/// Get the default postgres role
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_roles_default_builder()` + `get_organizations_organization_databases_database_branches_branch_roles_default_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_roles_default_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_roles_default(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesDefaultArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_roles_default_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
        )?;
    get_organizations_organization_databases_database_branches_branch_roles_default_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/reset-default
/// Reset default credentials
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_roles_reset_default_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_roles_reset_default` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_roles_reset_default_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles/reset-default",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/reset-default
/// Reset default credentials
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_branches_branch_roles_reset_default_execute()` or `post_organizations_organization_databases_database_branches_branch_roles_reset_default`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_roles_reset_default_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles_reset_default_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/reset-default
/// Reset default credentials
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_branches_branch_roles_reset_default_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_roles_reset_default_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_branches_branch_roles_reset_default()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_roles_reset_default_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_branches_branch_roles_reset_default_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_organizations_organization_databases_database_branches_branch_roles_reset_default_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_branches_branch_roles_reset_default`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesResetDefaultArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/reset-default
/// Reset default credentials
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_roles_reset_default_builder()` + `post_organizations_organization_databases_database_branches_branch_roles_reset_default_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_roles_reset_default_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles_reset_default(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesResetDefaultArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_branches_branch_roles_reset_default_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone())?;
    post_organizations_organization_databases_database_branches_branch_roles_reset_default_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Get a role
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_roles_id_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_roles_id` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_roles_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Get a role
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_roles_id_execute()` or `get_organizations_organization_databases_database_branches_branch_roles_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_roles_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_roles_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Get a role
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_roles_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_roles_id_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_roles_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_roles_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_roles_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_roles_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_roles_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Get a role
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_roles_id_builder()` + `get_organizations_organization_databases_database_branches_branch_roles_id_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_roles_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_roles_id(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_roles_id_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
            args.id.clone(),
        )?;
    get_organizations_organization_databases_database_branches_branch_roles_id_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Update role name
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_roles_id_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch_roles_id` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_roles_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Update role name
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_branches_branch_roles_id_execute()` or `patch_organizations_organization_databases_database_branches_branch_roles_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_roles_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_roles_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Update role name
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_branches_branch_roles_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_roles_id_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_branches_branch_roles_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_roles_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_branches_branch_roles_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        patch_organizations_organization_databases_database_branches_branch_roles_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_branches_branch_roles_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Update role name
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_roles_id_builder()` + `patch_organizations_organization_databases_database_branches_branch_roles_id_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_roles_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_roles_id(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_branches_branch_roles_id_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
            args.id.clone(),
        )?;
    patch_organizations_organization_databases_database_branches_branch_roles_id_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Delete role credentials
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_roles_id_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_roles_id` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_roles_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    id: String,
    successor: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = successor {
        query_parts.push(format!("successor={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Delete role credentials
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_databases_database_branches_branch_roles_id_execute()` or `delete_organizations_organization_databases_database_branches_branch_roles_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_roles_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_roles_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Delete role credentials
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_databases_database_branches_branch_roles_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_roles_id_task()`.
/// For the simplest API, use `delete_organizations_organization_databases_database_branches_branch_roles_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_roles_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_databases_database_branches_branch_roles_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_organizations_organization_databases_database_branches_branch_roles_id_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_databases_database_branches_branch_roles_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: successor
    pub successor: Option<String>,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}
/// Delete role credentials
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_roles_id_builder()` + `delete_organizations_organization_databases_database_branches_branch_roles_id_execute()`.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_roles_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_roles_id(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        delete_organizations_organization_databases_database_branches_branch_roles_id_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
            args.id.clone(),
            args.successor.clone(),
        )?;
    delete_organizations_organization_databases_database_branches_branch_roles_id_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reassign
/// Reassign objects owned by one role to another role
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_roles_id_reassign_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_roles_id_reassign` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_reassign_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles/{}/reassign",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reassign
/// Reassign objects owned by one role to another role
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_branches_branch_roles_id_reassign_execute()` or `post_organizations_organization_databases_database_branches_branch_roles_id_reassign`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_roles_id_reassign_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_reassign_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reassign
/// Reassign objects owned by one role to another role
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_branches_branch_roles_id_reassign_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_roles_id_reassign_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_branches_branch_roles_id_reassign()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_roles_id_reassign_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_reassign_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        post_organizations_organization_databases_database_branches_branch_roles_id_reassign_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_branches_branch_roles_id_reassign`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesIdReassignArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reassign
/// Reassign objects owned by one role to another role
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_roles_id_reassign_builder()` + `post_organizations_organization_databases_database_branches_branch_roles_id_reassign_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_roles_id_reassign_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_reassign(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesIdReassignArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_branches_branch_roles_id_reassign_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.id.clone())?;
    post_organizations_organization_databases_database_branches_branch_roles_id_reassign_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/renew
/// Renew role expiration
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_roles_id_renew_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_roles_id_renew` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_renew_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles/{}/renew",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/renew
/// Renew role expiration
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_branches_branch_roles_id_renew_execute()` or `post_organizations_organization_databases_database_branches_branch_roles_id_renew`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_roles_id_renew_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_renew_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/renew
/// Renew role expiration
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_branches_branch_roles_id_renew_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_roles_id_renew_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_branches_branch_roles_id_renew()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_roles_id_renew_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_renew_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        post_organizations_organization_databases_database_branches_branch_roles_id_renew_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_branches_branch_roles_id_renew`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesIdRenewArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/renew
/// Renew role expiration
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_roles_id_renew_builder()` + `post_organizations_organization_databases_database_branches_branch_roles_id_renew_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_roles_id_renew_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_renew(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesIdRenewArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_roles_id_renew_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
            args.id.clone(),
        )?;
    post_organizations_organization_databases_database_branches_branch_roles_id_renew_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reset
/// Reset a role's password
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_roles_id_reset_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_roles_id_reset` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_reset_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/roles/{}/reset",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reset
/// Reset a role's password
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_branches_branch_roles_id_reset_execute()` or `post_organizations_organization_databases_database_branches_branch_roles_id_reset`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_roles_id_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_reset_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reset
/// Reset a role's password
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_branches_branch_roles_id_reset_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_roles_id_reset_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_branches_branch_roles_id_reset()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_roles_id_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_reset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        post_organizations_organization_databases_database_branches_branch_roles_id_reset_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_branches_branch_roles_id_reset`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesIdResetArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/roles/{id}/reset
/// Reset a role's password
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_roles_id_reset_builder()` + `post_organizations_organization_databases_database_branches_branch_roles_id_reset_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_roles_id_reset_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_roles_id_reset(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchRolesIdResetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_roles_id_reset_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
            args.id.clone(),
        )?;
    post_organizations_organization_databases_database_branches_branch_roles_id_reset_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Enable safe migrations for a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_safe_migrations_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_safe_migrations` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_safe_migrations_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/safe-migrations",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Enable safe migrations for a branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_branches_branch_safe_migrations_execute()` or `post_organizations_organization_databases_database_branches_branch_safe_migrations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_safe_migrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_safe_migrations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Enable safe migrations for a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_branches_branch_safe_migrations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_safe_migrations_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_branches_branch_safe_migrations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_safe_migrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_branches_branch_safe_migrations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        post_organizations_organization_databases_database_branches_branch_safe_migrations_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_branches_branch_safe_migrations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchSafeMigrationsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Enable safe migrations for a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_safe_migrations_builder()` + `post_organizations_organization_databases_database_branches_branch_safe_migrations_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_safe_migrations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_safe_migrations(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchSafeMigrationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_safe_migrations_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
        )?;
    post_organizations_organization_databases_database_branches_branch_safe_migrations_execute(
        builder,
    )
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Disable safe migrations for a branch
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_safe_migrations_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_safe_migrations` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_safe_migrations_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/safe-migrations",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Disable safe migrations for a branch
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_databases_database_branches_branch_safe_migrations_execute()` or `delete_organizations_organization_databases_database_branches_branch_safe_migrations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_safe_migrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_safe_migrations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Disable safe migrations for a branch
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_databases_database_branches_branch_safe_migrations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_safe_migrations_task()`.
/// For the simplest API, use `delete_organizations_organization_databases_database_branches_branch_safe_migrations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_safe_migrations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_databases_database_branches_branch_safe_migrations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        delete_organizations_organization_databases_database_branches_branch_safe_migrations_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_databases_database_branches_branch_safe_migrations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchSafeMigrationsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/safe-migrations
/// Disable safe migrations for a branch
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_safe_migrations_builder()` + `delete_organizations_organization_databases_database_branches_branch_safe_migrations_execute()`.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_safe_migrations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_safe_migrations(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchSafeMigrationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_branches_branch_safe_migrations_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone())?;
    delete_organizations_organization_databases_database_branches_branch_safe_migrations_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema
/// Get a branch schema
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_schema_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_schema` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_schema_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    keyspace: Option<String>,
    namespace: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/schema",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = keyspace {
        query_parts.push(format!("keyspace={}", val));
    }
    if let Some(val) = namespace {
        query_parts.push(format!("namespace={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema
/// Get a branch schema
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_schema_execute()` or `get_organizations_organization_databases_database_branches_branch_schema`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_schema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_schema_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema
/// Get a branch schema
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_schema_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_schema_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_schema()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_schema_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_schema_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_schema_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_schema`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchSchemaArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: keyspace
    pub keyspace: Option<String>,
    /// Query parameter: namespace
    pub namespace: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema
/// Get a branch schema
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_schema_builder()` + `get_organizations_organization_databases_database_branches_branch_schema_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_schema_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_schema(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchSchemaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_schema_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.branch.clone(),
        args.keyspace.clone(),
        args.namespace.clone(),
    )?;
    get_organizations_organization_databases_database_branches_branch_schema_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema/lint
/// Lint a branch schema
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_schema_lint_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_schema_lint` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_schema_lint_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/schema/lint",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema/lint
/// Lint a branch schema
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_schema_lint_execute()` or `get_organizations_organization_databases_database_branches_branch_schema_lint`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_schema_lint_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_schema_lint_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema/lint
/// Lint a branch schema
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_schema_lint_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_schema_lint_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_schema_lint()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_schema_lint_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_schema_lint_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_branches_branch_schema_lint_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_schema_lint`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchSchemaLintArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/schema/lint
/// Lint a branch schema
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_schema_lint_builder()` + `get_organizations_organization_databases_database_branches_branch_schema_lint_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_schema_lint_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_schema_lint(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchSchemaLintArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_schema_lint_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
            args.page.clone(),
            args.per_page.clone(),
        )?;
    get_organizations_organization_databases_database_branches_branch_schema_lint_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// List traffic budgets
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_traffic_budgets_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_traffic_budgets` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_traffic_budgets_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    page: Option<String>,
    per_page: Option<String>,
    period: Option<String>,
    created_at: Option<String>,
    fingerprint: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/traffic/budgets",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }
    if let Some(val) = period {
        query_parts.push(format!("period={}", val));
    }
    if let Some(val) = created_at {
        query_parts.push(format!("created_at={}", val));
    }
    if let Some(val) = fingerprint {
        query_parts.push(format!("fingerprint={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// List traffic budgets
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_traffic_budgets_execute()` or `get_organizations_organization_databases_database_branches_branch_traffic_budgets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_traffic_budgets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_traffic_budgets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// List traffic budgets
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_traffic_budgets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_traffic_budgets_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_traffic_budgets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_traffic_budgets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_traffic_budgets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_traffic_budgets_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_traffic_budgets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchTrafficBudgetsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
    /// Query parameter: period
    pub period: Option<String>,
    /// Query parameter: created_at
    pub created_at: Option<String>,
    /// Query parameter: fingerprint
    pub fingerprint: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// List traffic budgets
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_traffic_budgets_builder()` + `get_organizations_organization_databases_database_branches_branch_traffic_budgets_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_traffic_budgets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_traffic_budgets(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchTrafficBudgetsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_branches_branch_traffic_budgets_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
            args.page.clone(),
            args.per_page.clone(),
            args.period.clone(),
            args.created_at.clone(),
            args.fingerprint.clone(),
        )?;
    get_organizations_organization_databases_database_branches_branch_traffic_budgets_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// Create a traffic budget
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_traffic_budgets_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_traffic_budgets` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_traffic_budgets_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/traffic/budgets",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// Create a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_branches_branch_traffic_budgets_execute()` or `post_organizations_organization_databases_database_branches_branch_traffic_budgets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_traffic_budgets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_traffic_budgets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// Create a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_branches_branch_traffic_budgets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_traffic_budgets_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_branches_branch_traffic_budgets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_traffic_budgets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_branches_branch_traffic_budgets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        post_organizations_organization_databases_database_branches_branch_traffic_budgets_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_branches_branch_traffic_budgets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchTrafficBudgetsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets
/// Create a traffic budget
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_traffic_budgets_builder()` + `post_organizations_organization_databases_database_branches_branch_traffic_budgets_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_traffic_budgets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_traffic_budgets(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchTrafficBudgetsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_branches_branch_traffic_budgets_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.branch.clone(),
        )?;
    post_organizations_organization_databases_database_branches_branch_traffic_budgets_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules
/// Create a traffic rule
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_execute()` to send, or `post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules` for simplest API.

pub fn post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    budget_id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/traffic/budgets/{}/rules",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        budget_id.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules
/// Create a traffic rule
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_execute()` or `post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules
/// Create a traffic rule
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchTrafficBudgetsBudgetIdRulesArgs
{
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: budget_id
    pub budget_id: String,
}

/// POST /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules
/// Create a traffic rule
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_builder()` + `post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseBranchesBranchTrafficBudgetsBudgetIdRulesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.budget_id.clone())?;
    post_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules/{id}
/// Delete a traffic rule
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    budget_id: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/traffic/budgets/{}/rules/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        budget_id.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules/{id}
/// Delete a traffic rule
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_execute()` or `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules/{id}
/// Delete a traffic rule
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_task()`.
/// For the simplest API, use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchTrafficBudgetsBudgetIdRulesIdArgs
{
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: budget_id
    pub budget_id: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{budget_id}/rules/{id}
/// Delete a traffic rule
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_builder()` + `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_execute()`.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchTrafficBudgetsBudgetIdRulesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.budget_id.clone(), args.id.clone())?;
    delete_organizations_organization_databases_database_branches_branch_traffic_budgets_budget_id_rules_id_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Get a traffic budget
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute()` to send, or `get_organizations_organization_databases_database_branches_branch_traffic_budgets_id` for simplest API.

pub fn get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/traffic/budgets/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Get a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute()` or `get_organizations_organization_databases_database_branches_branch_traffic_budgets_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Get a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_branches_branch_traffic_budgets_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_branches_branch_traffic_budgets_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchTrafficBudgetsIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Get a traffic budget
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()` + `get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_branches_branch_traffic_budgets_id(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseBranchesBranchTrafficBudgetsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.id.clone())?;
    get_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute(
        builder,
    )
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Update a traffic budget
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute()` to send, or `patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id` for simplest API.

pub fn patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/traffic/budgets/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Update a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute()` or `patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Update a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchTrafficBudgetsIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// PATCH /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Update a traffic budget
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()` + `patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseBranchesBranchTrafficBudgetsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.id.clone())?;
    patch_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute(
        builder,
    )
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Delete a traffic budget
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute()` to send, or `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id` for simplest API.

pub fn delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    branch: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/branches/{}/traffic/budgets/{}",
        organization.as_str(),
        database.as_str(),
        branch.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Delete a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute()` or `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Delete a traffic budget
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_task()`.
/// For the simplest API, use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchTrafficBudgetsIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: branch
    pub branch: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /organizations/{organization}/databases/{database}/branches/{branch}/traffic/budgets/{id}
/// Delete a traffic budget
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder()` + `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute()`.
/// For task-level control, use `delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationDatabasesDatabaseBranchesBranchTrafficBudgetsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_builder(client, args.organization.clone(), args.database.clone(), args.branch.clone(), args.id.clone())?;
    delete_organizations_organization_databases_database_branches_branch_traffic_budgets_id_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/cidrs
/// List IP restriction entries
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_cidrs_execute()` to send, or `get_organizations_organization_databases_database_cidrs` for simplest API.

pub fn get_organizations_organization_databases_database_cidrs_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/cidrs",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/cidrs
/// List IP restriction entries
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_cidrs_execute()` or `get_organizations_organization_databases_database_cidrs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_cidrs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_cidrs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/cidrs
/// List IP restriction entries
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_cidrs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_cidrs_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_cidrs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_cidrs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_cidrs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_cidrs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_cidrs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseCidrsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/cidrs
/// List IP restriction entries
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_cidrs_builder()` + `get_organizations_organization_databases_database_cidrs_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_cidrs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_cidrs(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseCidrsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_cidrs_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_databases_database_cidrs_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/cidrs
/// Create an IP restriction entry
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_cidrs_execute()` to send, or `post_organizations_organization_databases_database_cidrs` for simplest API.

pub fn post_organizations_organization_databases_database_cidrs_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/cidrs",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/cidrs
/// Create an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_cidrs_execute()` or `post_organizations_organization_databases_database_cidrs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_cidrs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_cidrs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/cidrs
/// Create an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_cidrs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_cidrs_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_cidrs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_cidrs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_cidrs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_organizations_organization_databases_database_cidrs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_cidrs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseCidrsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// POST /organizations/{organization}/databases/{database}/cidrs
/// Create an IP restriction entry
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_cidrs_builder()` + `post_organizations_organization_databases_database_cidrs_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_cidrs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_cidrs(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseCidrsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_cidrs_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
    )?;
    post_organizations_organization_databases_database_cidrs_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/cidrs/{id}
/// Get an IP restriction entry
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_cidrs_id_execute()` to send, or `get_organizations_organization_databases_database_cidrs_id` for simplest API.

pub fn get_organizations_organization_databases_database_cidrs_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/cidrs/{}",
        organization.as_str(),
        database.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/cidrs/{id}
/// Get an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_cidrs_id_execute()` or `get_organizations_organization_databases_database_cidrs_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_cidrs_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_cidrs_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/cidrs/{id}
/// Get an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_cidrs_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_cidrs_id_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_cidrs_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_cidrs_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_cidrs_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_cidrs_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_cidrs_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseCidrsIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/databases/{database}/cidrs/{id}
/// Get an IP restriction entry
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_cidrs_id_builder()` + `get_organizations_organization_databases_database_cidrs_id_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_cidrs_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_cidrs_id(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseCidrsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_cidrs_id_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.id.clone(),
    )?;
    get_organizations_organization_databases_database_cidrs_id_execute(builder)
}

/// PUT /organizations/{organization}/databases/{database}/cidrs/{id}
/// Update an IP restriction entry
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `put_organizations_organization_databases_database_cidrs_id_execute()` to send, or `put_organizations_organization_databases_database_cidrs_id` for simplest API.

pub fn put_organizations_organization_databases_database_cidrs_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/cidrs/{}",
        organization.as_str(),
        database.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT /organizations/{organization}/databases/{database}/cidrs/{id}
/// Update an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `put_organizations_organization_databases_database_cidrs_id_execute()` or `put_organizations_organization_databases_database_cidrs_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_organizations_organization_databases_database_cidrs_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_organizations_organization_databases_database_cidrs_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /organizations/{organization}/databases/{database}/cidrs/{id}
/// Update an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `put_organizations_organization_databases_database_cidrs_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `put_organizations_organization_databases_database_cidrs_id_task()`.
/// For the simplest API, use `put_organizations_organization_databases_database_cidrs_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_organizations_organization_databases_database_cidrs_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn put_organizations_organization_databases_database_cidrs_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = put_organizations_organization_databases_database_cidrs_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`put_organizations_organization_databases_database_cidrs_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PutOrganizationsOrganizationDatabasesDatabaseCidrsIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: id
    pub id: String,
}

/// PUT /organizations/{organization}/databases/{database}/cidrs/{id}
/// Update an IP restriction entry
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `put_organizations_organization_databases_database_cidrs_id_builder()` + `put_organizations_organization_databases_database_cidrs_id_execute()`.
/// For task-level control, use `put_organizations_organization_databases_database_cidrs_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_organizations_organization_databases_database_cidrs_id(
    client: &SimpleHttpClient,
    args: &PutOrganizationsOrganizationDatabasesDatabaseCidrsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = put_organizations_organization_databases_database_cidrs_id_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.id.clone(),
    )?;
    put_organizations_organization_databases_database_cidrs_id_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/cidrs/{id}
/// Delete an IP restriction entry
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_cidrs_id_execute()` to send, or `delete_organizations_organization_databases_database_cidrs_id` for simplest API.

pub fn delete_organizations_organization_databases_database_cidrs_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/cidrs/{}",
        organization.as_str(),
        database.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/cidrs/{id}
/// Delete an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_databases_database_cidrs_id_execute()` or `delete_organizations_organization_databases_database_cidrs_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_cidrs_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_cidrs_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/cidrs/{id}
/// Delete an IP restriction entry
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_databases_database_cidrs_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_databases_database_cidrs_id_task()`.
/// For the simplest API, use `delete_organizations_organization_databases_database_cidrs_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_cidrs_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_databases_database_cidrs_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_organizations_organization_databases_database_cidrs_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_databases_database_cidrs_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationDatabasesDatabaseCidrsIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /organizations/{organization}/databases/{database}/cidrs/{id}
/// Delete an IP restriction entry
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_cidrs_id_builder()` + `delete_organizations_organization_databases_database_cidrs_id_execute()`.
/// For task-level control, use `delete_organizations_organization_databases_database_cidrs_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_cidrs_id(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationDatabasesDatabaseCidrsIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_cidrs_id_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.id.clone(),
    )?;
    delete_organizations_organization_databases_database_cidrs_id_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-queue
/// Get the deploy queue
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_deploy_queue_execute()` to send, or `get_organizations_organization_databases_database_deploy_queue` for simplest API.

pub fn get_organizations_organization_databases_database_deploy_queue_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-queue",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-queue
/// Get the deploy queue
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_deploy_queue_execute()` or `get_organizations_organization_databases_database_deploy_queue`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_deploy_queue_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_queue_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/deploy-queue
/// Get the deploy queue
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_deploy_queue_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_deploy_queue_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_deploy_queue()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_deploy_queue_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_deploy_queue_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_deploy_queue_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_deploy_queue`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseDeployQueueArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/deploy-queue
/// Get the deploy queue
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_deploy_queue_builder()` + `get_organizations_organization_databases_database_deploy_queue_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_deploy_queue_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_queue(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseDeployQueueArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_deploy_queue_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_databases_database_deploy_queue_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests
/// List deploy requests
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_deploy_requests_execute()` to send, or `get_organizations_organization_databases_database_deploy_requests` for simplest API.

pub fn get_organizations_organization_databases_database_deploy_requests_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    state: Option<String>,
    branch: Option<String>,
    into_branch: Option<String>,
    deployed_at: Option<String>,
    running_at: Option<String>,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = state {
        query_parts.push(format!("state={}", val));
    }
    if let Some(val) = branch {
        query_parts.push(format!("branch={}", val));
    }
    if let Some(val) = into_branch {
        query_parts.push(format!("into_branch={}", val));
    }
    if let Some(val) = deployed_at {
        query_parts.push(format!("deployed_at={}", val));
    }
    if let Some(val) = running_at {
        query_parts.push(format!("running_at={}", val));
    }
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests
/// List deploy requests
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_deploy_requests_execute()` or `get_organizations_organization_databases_database_deploy_requests`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_deploy_requests_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests
/// List deploy requests
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_deploy_requests_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_deploy_requests_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_deploy_requests()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_deploy_requests_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_deploy_requests_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_deploy_requests_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_deploy_requests`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseDeployRequestsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: state
    pub state: Option<String>,
    /// Query parameter: branch
    pub branch: Option<String>,
    /// Query parameter: into_branch
    pub into_branch: Option<String>,
    /// Query parameter: deployed_at
    pub deployed_at: Option<String>,
    /// Query parameter: running_at
    pub running_at: Option<String>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests
/// List deploy requests
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_builder()` + `get_organizations_organization_databases_database_deploy_requests_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_deploy_requests_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseDeployRequestsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_deploy_requests_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.state.clone(),
        args.branch.clone(),
        args.into_branch.clone(),
        args.deployed_at.clone(),
        args.running_at.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_databases_database_deploy_requests_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests
/// Create a deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_deploy_requests_execute()` to send, or `post_organizations_organization_databases_database_deploy_requests` for simplest API.

pub fn post_organizations_organization_databases_database_deploy_requests_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests
/// Create a deploy request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_deploy_requests_execute()` or `post_organizations_organization_databases_database_deploy_requests`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_deploy_requests_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests
/// Create a deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_deploy_requests_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_deploy_requests_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_deploy_requests()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_deploy_requests_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_deploy_requests_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_organizations_organization_databases_database_deploy_requests_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_deploy_requests`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseDeployRequestsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests
/// Create a deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_builder()` + `post_organizations_organization_databases_database_deploy_requests_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_deploy_requests_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseDeployRequestsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_deploy_requests_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
    )?;
    post_organizations_organization_databases_database_deploy_requests_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Get a deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_deploy_requests_number_execute()` to send, or `get_organizations_organization_databases_database_deploy_requests_number` for simplest API.

pub fn get_organizations_organization_databases_database_deploy_requests_number_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Get a deploy request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_deploy_requests_number_execute()` or `get_organizations_organization_databases_database_deploy_requests_number`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_deploy_requests_number_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Get a deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_deploy_requests_number_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_deploy_requests_number_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_deploy_requests_number()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_deploy_requests_number_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_deploy_requests_number_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_deploy_requests_number_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_deploy_requests_number`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Get a deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_number_builder()` + `get_organizations_organization_databases_database_deploy_requests_number_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_deploy_requests_number_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_deploy_requests_number_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.number.clone(),
    )?;
    get_organizations_organization_databases_database_deploy_requests_number_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Close a deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_deploy_requests_number_execute()` to send, or `patch_organizations_organization_databases_database_deploy_requests_number` for simplest API.

pub fn patch_organizations_organization_databases_database_deploy_requests_number_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Close a deploy request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_deploy_requests_number_execute()` or `patch_organizations_organization_databases_database_deploy_requests_number`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_deploy_requests_number_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_deploy_requests_number_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Close a deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_deploy_requests_number_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_deploy_requests_number_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_deploy_requests_number()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_deploy_requests_number_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_deploy_requests_number_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        patch_organizations_organization_databases_database_deploy_requests_number_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_deploy_requests_number`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}
/// Close a deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_deploy_requests_number_builder()` + `patch_organizations_organization_databases_database_deploy_requests_number_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_deploy_requests_number_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_deploy_requests_number(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_deploy_requests_number_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.number.clone(),
        )?;
    patch_organizations_organization_databases_database_deploy_requests_number_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/apply-deploy
/// Complete a gated deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_execute()` to send, or `post_organizations_organization_databases_database_deploy_requests_number_apply_deploy` for simplest API.

pub fn post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/apply-deploy",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/apply-deploy
/// Complete a gated deploy request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_execute()` or `post_organizations_organization_databases_database_deploy_requests_number_apply_deploy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/apply-deploy
/// Complete a gated deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_deploy_requests_number_apply_deploy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_deploy_requests_number_apply_deploy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberApplyDeployArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/apply-deploy
/// Complete a gated deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_builder()` + `post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_apply_deploy(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberApplyDeployArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_builder(client, args.organization.clone(), args.database.clone(), args.number.clone())?;
    post_organizations_organization_databases_database_deploy_requests_number_apply_deploy_execute(
        builder,
    )
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-apply
/// Update auto-apply for deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `put_organizations_organization_databases_database_deploy_requests_number_auto_apply_execute()` to send, or `put_organizations_organization_databases_database_deploy_requests_number_auto_apply` for simplest API.

pub fn put_organizations_organization_databases_database_deploy_requests_number_auto_apply_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/auto-apply",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-apply
/// Update auto-apply for deploy request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `put_organizations_organization_databases_database_deploy_requests_number_auto_apply_execute()` or `put_organizations_organization_databases_database_deploy_requests_number_auto_apply`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_organizations_organization_databases_database_deploy_requests_number_auto_apply_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_organizations_organization_databases_database_deploy_requests_number_auto_apply_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-apply
/// Update auto-apply for deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `put_organizations_organization_databases_database_deploy_requests_number_auto_apply_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `put_organizations_organization_databases_database_deploy_requests_number_auto_apply_task()`.
/// For the simplest API, use `put_organizations_organization_databases_database_deploy_requests_number_auto_apply()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_organizations_organization_databases_database_deploy_requests_number_auto_apply_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn put_organizations_organization_databases_database_deploy_requests_number_auto_apply_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        put_organizations_organization_databases_database_deploy_requests_number_auto_apply_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`put_organizations_organization_databases_database_deploy_requests_number_auto_apply`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PutOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberAutoApplyArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-apply
/// Update auto-apply for deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `put_organizations_organization_databases_database_deploy_requests_number_auto_apply_builder()` + `put_organizations_organization_databases_database_deploy_requests_number_auto_apply_execute()`.
/// For task-level control, use `put_organizations_organization_databases_database_deploy_requests_number_auto_apply_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_organizations_organization_databases_database_deploy_requests_number_auto_apply(
    client: &SimpleHttpClient,
    args: &PutOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberAutoApplyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = put_organizations_organization_databases_database_deploy_requests_number_auto_apply_builder(client, args.organization.clone(), args.database.clone(), args.number.clone())?;
    put_organizations_organization_databases_database_deploy_requests_number_auto_apply_execute(
        builder,
    )
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-delete-branch
/// Update auto-delete branch for deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_execute()` to send, or `put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch` for simplest API.

pub fn put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/auto-delete-branch",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-delete-branch
/// Update auto-delete branch for deploy request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_execute()` or `put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-delete-branch
/// Update auto-delete branch for deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_task()`.
/// For the simplest API, use `put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PutOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberAutoDeleteBranchArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PUT /organizations/{organization}/databases/{database}/deploy-requests/{number}/auto-delete-branch
/// Update auto-delete branch for deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_builder()` + `put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_execute()`.
/// For task-level control, use `put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch(
    client: &SimpleHttpClient,
    args: &PutOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberAutoDeleteBranchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_builder(client, args.organization.clone(), args.database.clone(), args.number.clone())?;
    put_organizations_organization_databases_database_deploy_requests_number_auto_delete_branch_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/cancel
/// Cancel a queued deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_deploy_requests_number_cancel_execute()` to send, or `post_organizations_organization_databases_database_deploy_requests_number_cancel` for simplest API.

pub fn post_organizations_organization_databases_database_deploy_requests_number_cancel_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/cancel",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/cancel
/// Cancel a queued deploy request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_deploy_requests_number_cancel_execute()` or `post_organizations_organization_databases_database_deploy_requests_number_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_deploy_requests_number_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/cancel
/// Cancel a queued deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_deploy_requests_number_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_deploy_requests_number_cancel_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_deploy_requests_number_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_deploy_requests_number_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_deploy_requests_number_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        post_organizations_organization_databases_database_deploy_requests_number_cancel_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_deploy_requests_number_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberCancelArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/cancel
/// Cancel a queued deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_cancel_builder()` + `post_organizations_organization_databases_database_deploy_requests_number_cancel_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_deploy_requests_number_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_cancel(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_deploy_requests_number_cancel_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.number.clone(),
        )?;
    post_organizations_organization_databases_database_deploy_requests_number_cancel_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/complete-deploy
/// Complete an errored deploy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_execute()` to send, or `post_organizations_organization_databases_database_deploy_requests_number_complete_deploy` for simplest API.

pub fn post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/complete-deploy",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/complete-deploy
/// Complete an errored deploy
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_execute()` or `post_organizations_organization_databases_database_deploy_requests_number_complete_deploy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/complete-deploy
/// Complete an errored deploy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_deploy_requests_number_complete_deploy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_deploy_requests_number_complete_deploy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberCompleteDeployArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/complete-deploy
/// Complete an errored deploy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_builder()` + `post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_complete_deploy(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberCompleteDeployArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_builder(client, args.organization.clone(), args.database.clone(), args.number.clone())?;
    post_organizations_organization_databases_database_deploy_requests_number_complete_deploy_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/deploy
/// Queue a deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_deploy_requests_number_deploy_execute()` to send, or `post_organizations_organization_databases_database_deploy_requests_number_deploy` for simplest API.

pub fn post_organizations_organization_databases_database_deploy_requests_number_deploy_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/deploy",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/deploy
/// Queue a deploy request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_deploy_requests_number_deploy_execute()` or `post_organizations_organization_databases_database_deploy_requests_number_deploy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_deploy_requests_number_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_deploy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/deploy
/// Queue a deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_deploy_requests_number_deploy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_deploy_requests_number_deploy_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_deploy_requests_number_deploy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_deploy_requests_number_deploy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_deploy_requests_number_deploy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        post_organizations_organization_databases_database_deploy_requests_number_deploy_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_deploy_requests_number_deploy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberDeployArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/deploy
/// Queue a deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_deploy_builder()` + `post_organizations_organization_databases_database_deploy_requests_number_deploy_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_deploy_requests_number_deploy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_deploy(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberDeployArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_deploy_requests_number_deploy_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.number.clone(),
        )?;
    post_organizations_organization_databases_database_deploy_requests_number_deploy_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/deployment
/// Get a deployment
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_deploy_requests_number_deployment_execute()` to send, or `get_organizations_organization_databases_database_deploy_requests_number_deployment` for simplest API.

pub fn get_organizations_organization_databases_database_deploy_requests_number_deployment_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/deployment",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/deployment
/// Get a deployment
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_deploy_requests_number_deployment_execute()` or `get_organizations_organization_databases_database_deploy_requests_number_deployment`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_deploy_requests_number_deployment_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_deployment_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/deployment
/// Get a deployment
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_deploy_requests_number_deployment_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_deploy_requests_number_deployment_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_deploy_requests_number_deployment()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_deploy_requests_number_deployment_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_deploy_requests_number_deployment_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_deploy_requests_number_deployment_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_deploy_requests_number_deployment`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberDeploymentArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/deployment
/// Get a deployment
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_number_deployment_builder()` + `get_organizations_organization_databases_database_deploy_requests_number_deployment_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_deploy_requests_number_deployment_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_deployment(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberDeploymentArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_deploy_requests_number_deployment_builder(client, args.organization.clone(), args.database.clone(), args.number.clone())?;
    get_organizations_organization_databases_database_deploy_requests_number_deployment_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/operations
/// List deploy operations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_deploy_requests_number_operations_execute()` to send, or `get_organizations_organization_databases_database_deploy_requests_number_operations` for simplest API.

pub fn get_organizations_organization_databases_database_deploy_requests_number_operations_builder(
    client: &SimpleHttpClient,
    number: String,
    organization: String,
    database: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/operations",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/operations
/// List deploy operations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_deploy_requests_number_operations_execute()` or `get_organizations_organization_databases_database_deploy_requests_number_operations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_deploy_requests_number_operations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_operations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/operations
/// List deploy operations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_deploy_requests_number_operations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_deploy_requests_number_operations_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_deploy_requests_number_operations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_deploy_requests_number_operations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_deploy_requests_number_operations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_deploy_requests_number_operations_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_deploy_requests_number_operations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberOperationsArgs {
    /// Path parameter: number
    pub number: String,
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/operations
/// List deploy operations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_number_operations_builder()` + `get_organizations_organization_databases_database_deploy_requests_number_operations_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_deploy_requests_number_operations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_operations(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberOperationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_deploy_requests_number_operations_builder(client, args.number.clone(), args.organization.clone(), args.database.clone(), args.page.clone(), args.per_page.clone())?;
    get_organizations_organization_databases_database_deploy_requests_number_operations_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/revert
/// Complete a revert
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_deploy_requests_number_revert_execute()` to send, or `post_organizations_organization_databases_database_deploy_requests_number_revert` for simplest API.

pub fn post_organizations_organization_databases_database_deploy_requests_number_revert_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/revert",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/revert
/// Complete a revert
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_deploy_requests_number_revert_execute()` or `post_organizations_organization_databases_database_deploy_requests_number_revert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_deploy_requests_number_revert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_revert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/revert
/// Complete a revert
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_deploy_requests_number_revert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_deploy_requests_number_revert_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_deploy_requests_number_revert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_deploy_requests_number_revert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_deploy_requests_number_revert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        post_organizations_organization_databases_database_deploy_requests_number_revert_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_deploy_requests_number_revert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberRevertArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/revert
/// Complete a revert
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_revert_builder()` + `post_organizations_organization_databases_database_deploy_requests_number_revert_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_deploy_requests_number_revert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_revert(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberRevertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_deploy_requests_number_revert_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.number.clone(),
        )?;
    post_organizations_organization_databases_database_deploy_requests_number_revert_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// List deploy request reviews
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_deploy_requests_number_reviews_execute()` to send, or `get_organizations_organization_databases_database_deploy_requests_number_reviews` for simplest API.

pub fn get_organizations_organization_databases_database_deploy_requests_number_reviews_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/reviews",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// List deploy request reviews
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_deploy_requests_number_reviews_execute()` or `get_organizations_organization_databases_database_deploy_requests_number_reviews`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_deploy_requests_number_reviews_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_reviews_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// List deploy request reviews
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_deploy_requests_number_reviews_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_deploy_requests_number_reviews_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_deploy_requests_number_reviews()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_deploy_requests_number_reviews_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_deploy_requests_number_reviews_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_deploy_requests_number_reviews_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_deploy_requests_number_reviews`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberReviewsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// List deploy request reviews
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_number_reviews_builder()` + `get_organizations_organization_databases_database_deploy_requests_number_reviews_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_deploy_requests_number_reviews_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_reviews(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberReviewsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_deploy_requests_number_reviews_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.number.clone(),
            args.page.clone(),
            args.per_page.clone(),
        )?;
    get_organizations_organization_databases_database_deploy_requests_number_reviews_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// Review a deploy request
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_deploy_requests_number_reviews_execute()` to send, or `post_organizations_organization_databases_database_deploy_requests_number_reviews` for simplest API.

pub fn post_organizations_organization_databases_database_deploy_requests_number_reviews_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/reviews",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// Review a deploy request
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_deploy_requests_number_reviews_execute()` or `post_organizations_organization_databases_database_deploy_requests_number_reviews`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_deploy_requests_number_reviews_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_reviews_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// Review a deploy request
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_deploy_requests_number_reviews_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_deploy_requests_number_reviews_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_deploy_requests_number_reviews()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_deploy_requests_number_reviews_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_deploy_requests_number_reviews_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        post_organizations_organization_databases_database_deploy_requests_number_reviews_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_deploy_requests_number_reviews`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberReviewsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/reviews
/// Review a deploy request
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_reviews_builder()` + `post_organizations_organization_databases_database_deploy_requests_number_reviews_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_deploy_requests_number_reviews_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_reviews(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberReviewsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_databases_database_deploy_requests_number_reviews_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.number.clone(),
        )?;
    post_organizations_organization_databases_database_deploy_requests_number_reviews_execute(
        builder,
    )
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/skip-revert
/// Skip revert period
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_deploy_requests_number_skip_revert_execute()` to send, or `post_organizations_organization_databases_database_deploy_requests_number_skip_revert` for simplest API.

pub fn post_organizations_organization_databases_database_deploy_requests_number_skip_revert_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/skip-revert",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/skip-revert
/// Skip revert period
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_deploy_requests_number_skip_revert_execute()` or `post_organizations_organization_databases_database_deploy_requests_number_skip_revert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_deploy_requests_number_skip_revert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_skip_revert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/skip-revert
/// Skip revert period
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_deploy_requests_number_skip_revert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_deploy_requests_number_skip_revert_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_deploy_requests_number_skip_revert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_deploy_requests_number_skip_revert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_deploy_requests_number_skip_revert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        post_organizations_organization_databases_database_deploy_requests_number_skip_revert_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_deploy_requests_number_skip_revert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberSkipRevertArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// POST /organizations/{organization}/databases/{database}/deploy-requests/{number}/skip-revert
/// Skip revert period
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_deploy_requests_number_skip_revert_builder()` + `post_organizations_organization_databases_database_deploy_requests_number_skip_revert_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_deploy_requests_number_skip_revert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_deploy_requests_number_skip_revert(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberSkipRevertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_deploy_requests_number_skip_revert_builder(client, args.organization.clone(), args.database.clone(), args.number.clone())?;
    post_organizations_organization_databases_database_deploy_requests_number_skip_revert_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/storage-check
/// Check deploy request storage
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_deploy_requests_number_storage_check_execute()` to send, or `get_organizations_organization_databases_database_deploy_requests_number_storage_check` for simplest API.

pub fn get_organizations_organization_databases_database_deploy_requests_number_storage_check_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/storage-check",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/storage-check
/// Check deploy request storage
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_deploy_requests_number_storage_check_execute()` or `get_organizations_organization_databases_database_deploy_requests_number_storage_check`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_deploy_requests_number_storage_check_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_storage_check_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/storage-check
/// Check deploy request storage
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_deploy_requests_number_storage_check_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_deploy_requests_number_storage_check_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_deploy_requests_number_storage_check()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_deploy_requests_number_storage_check_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_deploy_requests_number_storage_check_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_deploy_requests_number_storage_check_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_deploy_requests_number_storage_check`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberStorageCheckArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/storage-check
/// Check deploy request storage
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_number_storage_check_builder()` + `get_organizations_organization_databases_database_deploy_requests_number_storage_check_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_deploy_requests_number_storage_check_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_storage_check(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberStorageCheckArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_deploy_requests_number_storage_check_builder(client, args.organization.clone(), args.database.clone(), args.number.clone())?;
    get_organizations_organization_databases_database_deploy_requests_number_storage_check_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Get deploy request throttler configurations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_deploy_requests_number_throttler_execute()` to send, or `get_organizations_organization_databases_database_deploy_requests_number_throttler` for simplest API.

pub fn get_organizations_organization_databases_database_deploy_requests_number_throttler_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/throttler",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Get deploy request throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_deploy_requests_number_throttler_execute()` or `get_organizations_organization_databases_database_deploy_requests_number_throttler`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_deploy_requests_number_throttler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_throttler_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Get deploy request throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_deploy_requests_number_throttler_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_deploy_requests_number_throttler_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_deploy_requests_number_throttler()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_deploy_requests_number_throttler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_deploy_requests_number_throttler_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_deploy_requests_number_throttler_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_deploy_requests_number_throttler`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberThrottlerArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// GET /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Get deploy request throttler configurations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_deploy_requests_number_throttler_builder()` + `get_organizations_organization_databases_database_deploy_requests_number_throttler_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_deploy_requests_number_throttler_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_deploy_requests_number_throttler(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberThrottlerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_deploy_requests_number_throttler_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.number.clone(),
        )?;
    get_organizations_organization_databases_database_deploy_requests_number_throttler_execute(
        builder,
    )
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Update deploy request throttler configurations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_deploy_requests_number_throttler_execute()` to send, or `patch_organizations_organization_databases_database_deploy_requests_number_throttler` for simplest API.

pub fn patch_organizations_organization_databases_database_deploy_requests_number_throttler_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/deploy-requests/{}/throttler",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Update deploy request throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_deploy_requests_number_throttler_execute()` or `patch_organizations_organization_databases_database_deploy_requests_number_throttler`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_deploy_requests_number_throttler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_deploy_requests_number_throttler_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Update deploy request throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_deploy_requests_number_throttler_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_deploy_requests_number_throttler_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_deploy_requests_number_throttler()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_deploy_requests_number_throttler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_deploy_requests_number_throttler_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        patch_organizations_organization_databases_database_deploy_requests_number_throttler_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_deploy_requests_number_throttler`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberThrottlerArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/deploy-requests/{number}/throttler
/// Update deploy request throttler configurations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_deploy_requests_number_throttler_builder()` + `patch_organizations_organization_databases_database_deploy_requests_number_throttler_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_deploy_requests_number_throttler_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_deploy_requests_number_throttler(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseDeployRequestsNumberThrottlerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_deploy_requests_number_throttler_builder(client, args.organization.clone(), args.database.clone(), args.number.clone())?;
    patch_organizations_organization_databases_database_deploy_requests_number_throttler_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules
/// List maintenance schedules
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_maintenance_schedules_execute()` to send, or `get_organizations_organization_databases_database_maintenance_schedules` for simplest API.

pub fn get_organizations_organization_databases_database_maintenance_schedules_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/maintenance-schedules",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules
/// List maintenance schedules
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_maintenance_schedules_execute()` or `get_organizations_organization_databases_database_maintenance_schedules`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_maintenance_schedules_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_maintenance_schedules_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules
/// List maintenance schedules
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_maintenance_schedules_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_maintenance_schedules_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_maintenance_schedules()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_maintenance_schedules_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_maintenance_schedules_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_maintenance_schedules_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_maintenance_schedules`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseMaintenanceSchedulesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules
/// List maintenance schedules
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_maintenance_schedules_builder()` + `get_organizations_organization_databases_database_maintenance_schedules_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_maintenance_schedules_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_maintenance_schedules(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseMaintenanceSchedulesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_maintenance_schedules_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_databases_database_maintenance_schedules_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}
/// Get a maintenance schedule
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_maintenance_schedules_id_execute()` to send, or `get_organizations_organization_databases_database_maintenance_schedules_id` for simplest API.

pub fn get_organizations_organization_databases_database_maintenance_schedules_id_builder(
    client: &SimpleHttpClient,
    id: String,
    organization: String,
    database: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/maintenance-schedules/{}",
        organization.as_str(),
        database.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}
/// Get a maintenance schedule
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_maintenance_schedules_id_execute()` or `get_organizations_organization_databases_database_maintenance_schedules_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_maintenance_schedules_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_maintenance_schedules_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}
/// Get a maintenance schedule
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_maintenance_schedules_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_maintenance_schedules_id_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_maintenance_schedules_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_maintenance_schedules_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_maintenance_schedules_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_maintenance_schedules_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_maintenance_schedules_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseMaintenanceSchedulesIdArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}
/// Get a maintenance schedule
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_maintenance_schedules_id_builder()` + `get_organizations_organization_databases_database_maintenance_schedules_id_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_maintenance_schedules_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_maintenance_schedules_id(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseMaintenanceSchedulesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_maintenance_schedules_id_builder(
            client,
            args.id.clone(),
            args.organization.clone(),
            args.database.clone(),
        )?;
    get_organizations_organization_databases_database_maintenance_schedules_id_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}/windows
/// List maintenance windows
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_maintenance_schedules_id_windows_execute()` to send, or `get_organizations_organization_databases_database_maintenance_schedules_id_windows` for simplest API.

pub fn get_organizations_organization_databases_database_maintenance_schedules_id_windows_builder(
    client: &SimpleHttpClient,
    id: String,
    organization: String,
    database: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/maintenance-schedules/{}/windows",
        organization.as_str(),
        database.as_str(),
        id.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}/windows
/// List maintenance windows
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_maintenance_schedules_id_windows_execute()` or `get_organizations_organization_databases_database_maintenance_schedules_id_windows`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_maintenance_schedules_id_windows_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_maintenance_schedules_id_windows_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}/windows
/// List maintenance windows
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_maintenance_schedules_id_windows_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_maintenance_schedules_id_windows_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_maintenance_schedules_id_windows()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_maintenance_schedules_id_windows_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_maintenance_schedules_id_windows_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_maintenance_schedules_id_windows_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_maintenance_schedules_id_windows`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseMaintenanceSchedulesIdWindowsArgs {
    /// Path parameter: id
    pub id: String,
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/maintenance-schedules/{id}/windows
/// List maintenance windows
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_maintenance_schedules_id_windows_builder()` + `get_organizations_organization_databases_database_maintenance_schedules_id_windows_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_maintenance_schedules_id_windows_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_maintenance_schedules_id_windows(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseMaintenanceSchedulesIdWindowsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_maintenance_schedules_id_windows_builder(
            client,
            args.id.clone(),
            args.organization.clone(),
            args.database.clone(),
            args.page.clone(),
            args.per_page.clone(),
        )?;
    get_organizations_organization_databases_database_maintenance_schedules_id_windows_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/read-only-regions
/// List read-only regions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_read_only_regions_execute()` to send, or `get_organizations_organization_databases_database_read_only_regions` for simplest API.

pub fn get_organizations_organization_databases_database_read_only_regions_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/read-only-regions",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/read-only-regions
/// List read-only regions
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_read_only_regions_execute()` or `get_organizations_organization_databases_database_read_only_regions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_read_only_regions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_read_only_regions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/read-only-regions
/// List read-only regions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_read_only_regions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_read_only_regions_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_read_only_regions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_read_only_regions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_read_only_regions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_read_only_regions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_read_only_regions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseReadOnlyRegionsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/read-only-regions
/// List read-only regions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_read_only_regions_builder()` + `get_organizations_organization_databases_database_read_only_regions_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_read_only_regions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_read_only_regions(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseReadOnlyRegionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_read_only_regions_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_databases_database_read_only_regions_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/regions
/// List database regions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_regions_execute()` to send, or `get_organizations_organization_databases_database_regions` for simplest API.

pub fn get_organizations_organization_databases_database_regions_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/regions",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/regions
/// List database regions
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_regions_execute()` or `get_organizations_organization_databases_database_regions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_regions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_regions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/regions
/// List database regions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_regions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_regions_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_regions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_regions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_regions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_regions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_regions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseRegionsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/regions
/// List database regions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_regions_builder()` + `get_organizations_organization_databases_database_regions_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_regions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_regions(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseRegionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_regions_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_databases_database_regions_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations
/// List schema recommendations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_schema_recommendations_execute()` to send, or `get_organizations_organization_databases_database_schema_recommendations` for simplest API.

pub fn get_organizations_organization_databases_database_schema_recommendations_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    state: Option<String>,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/schema-recommendations",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = state {
        query_parts.push(format!("state={}", val));
    }
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations
/// List schema recommendations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_schema_recommendations_execute()` or `get_organizations_organization_databases_database_schema_recommendations`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_schema_recommendations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_schema_recommendations_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations
/// List schema recommendations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_schema_recommendations_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_schema_recommendations_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_schema_recommendations()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_schema_recommendations_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_schema_recommendations_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_schema_recommendations_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_schema_recommendations`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseSchemaRecommendationsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: state
    pub state: Option<String>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations
/// List schema recommendations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_schema_recommendations_builder()` + `get_organizations_organization_databases_database_schema_recommendations_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_schema_recommendations_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_schema_recommendations(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseSchemaRecommendationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_schema_recommendations_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.state.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_databases_database_schema_recommendations_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations/{number}
/// Get a schema recommendation
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_schema_recommendations_number_execute()` to send, or `get_organizations_organization_databases_database_schema_recommendations_number` for simplest API.

pub fn get_organizations_organization_databases_database_schema_recommendations_number_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/schema-recommendations/{}",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations/{number}
/// Get a schema recommendation
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_schema_recommendations_number_execute()` or `get_organizations_organization_databases_database_schema_recommendations_number`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_schema_recommendations_number_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_schema_recommendations_number_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations/{number}
/// Get a schema recommendation
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_schema_recommendations_number_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_schema_recommendations_number_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_schema_recommendations_number()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_schema_recommendations_number_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_schema_recommendations_number_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_databases_database_schema_recommendations_number_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_schema_recommendations_number`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseSchemaRecommendationsNumberArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// GET /organizations/{organization}/databases/{database}/schema-recommendations/{number}
/// Get a schema recommendation
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_schema_recommendations_number_builder()` + `get_organizations_organization_databases_database_schema_recommendations_number_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_schema_recommendations_number_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_schema_recommendations_number(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseSchemaRecommendationsNumberArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_databases_database_schema_recommendations_number_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.number.clone(),
        )?;
    get_organizations_organization_databases_database_schema_recommendations_number_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/schema-recommendations/{number}/dismiss
/// Dismiss a schema recommendation
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_schema_recommendations_number_dismiss_execute()` to send, or `post_organizations_organization_databases_database_schema_recommendations_number_dismiss` for simplest API.

pub fn post_organizations_organization_databases_database_schema_recommendations_number_dismiss_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/schema-recommendations/{}/dismiss",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/schema-recommendations/{number}/dismiss
/// Dismiss a schema recommendation
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_schema_recommendations_number_dismiss_execute()` or `post_organizations_organization_databases_database_schema_recommendations_number_dismiss`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_schema_recommendations_number_dismiss_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_schema_recommendations_number_dismiss_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/schema-recommendations/{number}/dismiss
/// Dismiss a schema recommendation
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_schema_recommendations_number_dismiss_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_schema_recommendations_number_dismiss_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_schema_recommendations_number_dismiss()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_schema_recommendations_number_dismiss_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_schema_recommendations_number_dismiss_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_organizations_organization_databases_database_schema_recommendations_number_dismiss_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_schema_recommendations_number_dismiss`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseSchemaRecommendationsNumberDismissArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// POST /organizations/{organization}/databases/{database}/schema-recommendations/{number}/dismiss
/// Dismiss a schema recommendation
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_schema_recommendations_number_dismiss_builder()` + `post_organizations_organization_databases_database_schema_recommendations_number_dismiss_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_schema_recommendations_number_dismiss_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_schema_recommendations_number_dismiss(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseSchemaRecommendationsNumberDismissArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_schema_recommendations_number_dismiss_builder(client, args.organization.clone(), args.database.clone(), args.number.clone())?;
    post_organizations_organization_databases_database_schema_recommendations_number_dismiss_execute(
        builder,
    )
}

/// GET /organizations/{organization}/databases/{database}/throttler
/// Get database throttler configurations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_throttler_execute()` to send, or `get_organizations_organization_databases_database_throttler` for simplest API.

pub fn get_organizations_organization_databases_database_throttler_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/throttler",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/throttler
/// Get database throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_throttler_execute()` or `get_organizations_organization_databases_database_throttler`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_throttler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_throttler_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/throttler
/// Get database throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_throttler_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_throttler_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_throttler()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_throttler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_throttler_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_throttler_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_throttler`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseThrottlerArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// GET /organizations/{organization}/databases/{database}/throttler
/// Get database throttler configurations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_throttler_builder()` + `get_organizations_organization_databases_database_throttler_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_throttler_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_throttler(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseThrottlerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_throttler_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
    )?;
    get_organizations_organization_databases_database_throttler_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/throttler
/// Update database throttler configurations
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_throttler_execute()` to send, or `patch_organizations_organization_databases_database_throttler` for simplest API.

pub fn patch_organizations_organization_databases_database_throttler_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/throttler",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/throttler
/// Update database throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_throttler_execute()` or `patch_organizations_organization_databases_database_throttler`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_throttler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_throttler_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/throttler
/// Update database throttler configurations
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_throttler_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_throttler_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_throttler()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_throttler_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_throttler_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = patch_organizations_organization_databases_database_throttler_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_throttler`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseThrottlerArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// PATCH /organizations/{organization}/databases/{database}/throttler
/// Update database throttler configurations
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_throttler_builder()` + `patch_organizations_organization_databases_database_throttler_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_throttler_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_throttler(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseThrottlerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_throttler_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
    )?;
    patch_organizations_organization_databases_database_throttler_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/webhooks
/// List webhooks
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_webhooks_execute()` to send, or `get_organizations_organization_databases_database_webhooks` for simplest API.

pub fn get_organizations_organization_databases_database_webhooks_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/webhooks",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/webhooks
/// List webhooks
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_webhooks_execute()` or `get_organizations_organization_databases_database_webhooks`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_webhooks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_webhooks_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/webhooks
/// List webhooks
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_webhooks_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_webhooks_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_webhooks()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_webhooks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_webhooks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_webhooks_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_webhooks`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseWebhooksArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/webhooks
/// List webhooks
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_webhooks_builder()` + `get_organizations_organization_databases_database_webhooks_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_webhooks_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_webhooks(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseWebhooksArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_webhooks_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_databases_database_webhooks_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/webhooks
/// Create a webhook
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_webhooks_execute()` to send, or `post_organizations_organization_databases_database_webhooks` for simplest API.

pub fn post_organizations_organization_databases_database_webhooks_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/webhooks",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/webhooks
/// Create a webhook
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_webhooks_execute()` or `post_organizations_organization_databases_database_webhooks`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_webhooks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_webhooks_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/webhooks
/// Create a webhook
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_webhooks_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_webhooks_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_webhooks()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_webhooks_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_webhooks_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_organizations_organization_databases_database_webhooks_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_webhooks`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseWebhooksArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// POST /organizations/{organization}/databases/{database}/webhooks
/// Create a webhook
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_webhooks_builder()` + `post_organizations_organization_databases_database_webhooks_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_webhooks_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_webhooks(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseWebhooksArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_webhooks_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
    )?;
    post_organizations_organization_databases_database_webhooks_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/webhooks/{id}
/// Get a webhook
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_webhooks_id_execute()` to send, or `get_organizations_organization_databases_database_webhooks_id` for simplest API.

pub fn get_organizations_organization_databases_database_webhooks_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/webhooks/{}",
        organization.as_str(),
        database.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/webhooks/{id}
/// Get a webhook
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_webhooks_id_execute()` or `get_organizations_organization_databases_database_webhooks_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_webhooks_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_webhooks_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/webhooks/{id}
/// Get a webhook
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_webhooks_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_webhooks_id_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_webhooks_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_webhooks_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_webhooks_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_webhooks_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_webhooks_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseWebhooksIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/databases/{database}/webhooks/{id}
/// Get a webhook
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_webhooks_id_builder()` + `get_organizations_organization_databases_database_webhooks_id_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_webhooks_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_webhooks_id(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseWebhooksIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_webhooks_id_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.id.clone(),
    )?;
    get_organizations_organization_databases_database_webhooks_id_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/webhooks/{id}
/// Update a webhook
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_webhooks_id_execute()` to send, or `patch_organizations_organization_databases_database_webhooks_id` for simplest API.

pub fn patch_organizations_organization_databases_database_webhooks_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/webhooks/{}",
        organization.as_str(),
        database.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/webhooks/{id}
/// Update a webhook
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_webhooks_id_execute()` or `patch_organizations_organization_databases_database_webhooks_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_webhooks_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_webhooks_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/webhooks/{id}
/// Update a webhook
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_webhooks_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_webhooks_id_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_webhooks_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_webhooks_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_webhooks_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = patch_organizations_organization_databases_database_webhooks_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_webhooks_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseWebhooksIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: id
    pub id: String,
}

/// PATCH /organizations/{organization}/databases/{database}/webhooks/{id}
/// Update a webhook
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_webhooks_id_builder()` + `patch_organizations_organization_databases_database_webhooks_id_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_webhooks_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_webhooks_id(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseWebhooksIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_webhooks_id_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.id.clone(),
    )?;
    patch_organizations_organization_databases_database_webhooks_id_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/webhooks/{id}
/// Delete a webhook
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_webhooks_id_execute()` to send, or `delete_organizations_organization_databases_database_webhooks_id` for simplest API.

pub fn delete_organizations_organization_databases_database_webhooks_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/webhooks/{}",
        organization.as_str(),
        database.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/webhooks/{id}
/// Delete a webhook
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_databases_database_webhooks_id_execute()` or `delete_organizations_organization_databases_database_webhooks_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_webhooks_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_webhooks_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/webhooks/{id}
/// Delete a webhook
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_databases_database_webhooks_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_databases_database_webhooks_id_task()`.
/// For the simplest API, use `delete_organizations_organization_databases_database_webhooks_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_webhooks_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_databases_database_webhooks_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_organizations_organization_databases_database_webhooks_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_databases_database_webhooks_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationDatabasesDatabaseWebhooksIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /organizations/{organization}/databases/{database}/webhooks/{id}
/// Delete a webhook
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_webhooks_id_builder()` + `delete_organizations_organization_databases_database_webhooks_id_execute()`.
/// For task-level control, use `delete_organizations_organization_databases_database_webhooks_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_webhooks_id(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationDatabasesDatabaseWebhooksIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_webhooks_id_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.id.clone(),
    )?;
    delete_organizations_organization_databases_database_webhooks_id_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/webhooks/{id}/test
/// Test a webhook
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_webhooks_id_test_execute()` to send, or `post_organizations_organization_databases_database_webhooks_id_test` for simplest API.

pub fn post_organizations_organization_databases_database_webhooks_id_test_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/webhooks/{}/test",
        organization.as_str(),
        database.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/webhooks/{id}/test
/// Test a webhook
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_webhooks_id_test_execute()` or `post_organizations_organization_databases_database_webhooks_id_test`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_webhooks_id_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_webhooks_id_test_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/webhooks/{id}/test
/// Test a webhook
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_webhooks_id_test_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_webhooks_id_test_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_webhooks_id_test()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_webhooks_id_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_webhooks_id_test_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_organizations_organization_databases_database_webhooks_id_test_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_webhooks_id_test`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseWebhooksIdTestArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /organizations/{organization}/databases/{database}/webhooks/{id}/test
/// Test a webhook
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_webhooks_id_test_builder()` + `post_organizations_organization_databases_database_webhooks_id_test_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_webhooks_id_test_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_webhooks_id_test(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseWebhooksIdTestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_webhooks_id_test_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.id.clone(),
    )?;
    post_organizations_organization_databases_database_webhooks_id_test_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/workflows
/// List workflows
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_workflows_execute()` to send, or `get_organizations_organization_databases_database_workflows` for simplest API.

pub fn get_organizations_organization_databases_database_workflows_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    between: Option<String>,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = between {
        query_parts.push(format!("between={}", val));
    }
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/workflows
/// List workflows
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_workflows_execute()` or `get_organizations_organization_databases_database_workflows`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_workflows_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_workflows_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/workflows
/// List workflows
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_workflows_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_workflows_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_workflows()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_workflows_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_workflows_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_workflows_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_workflows`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseWorkflowsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Query parameter: between
    pub between: Option<String>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/databases/{database}/workflows
/// List workflows
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_workflows_builder()` + `get_organizations_organization_databases_database_workflows_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_workflows_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_workflows(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseWorkflowsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_workflows_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.between.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_databases_database_workflows_execute(builder)
}

/// POST /organizations/{organization}/databases/{database}/workflows
/// Create a workflow
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_databases_database_workflows_execute()` to send, or `post_organizations_organization_databases_database_workflows` for simplest API.

pub fn post_organizations_organization_databases_database_workflows_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows",
        organization.as_str(),
        database.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/databases/{database}/workflows
/// Create a workflow
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_databases_database_workflows_execute()` or `post_organizations_organization_databases_database_workflows`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_workflows_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_workflows_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/databases/{database}/workflows
/// Create a workflow
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_databases_database_workflows_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_databases_database_workflows_task()`.
/// For the simplest API, use `post_organizations_organization_databases_database_workflows()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_databases_database_workflows_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_databases_database_workflows_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_organizations_organization_databases_database_workflows_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_databases_database_workflows`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationDatabasesDatabaseWorkflowsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
}

/// POST /organizations/{organization}/databases/{database}/workflows
/// Create a workflow
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_databases_database_workflows_builder()` + `post_organizations_organization_databases_database_workflows_execute()`.
/// For task-level control, use `post_organizations_organization_databases_database_workflows_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_databases_database_workflows(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationDatabasesDatabaseWorkflowsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_databases_database_workflows_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
    )?;
    post_organizations_organization_databases_database_workflows_execute(builder)
}

/// GET /organizations/{organization}/databases/{database}/workflows/{number}
/// Get a workflow
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_databases_database_workflows_number_execute()` to send, or `get_organizations_organization_databases_database_workflows_number` for simplest API.

pub fn get_organizations_organization_databases_database_workflows_number_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/databases/{database}/workflows/{number}
/// Get a workflow
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_databases_database_workflows_number_execute()` or `get_organizations_organization_databases_database_workflows_number`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_workflows_number_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_workflows_number_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/databases/{database}/workflows/{number}
/// Get a workflow
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_databases_database_workflows_number_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_databases_database_workflows_number_task()`.
/// For the simplest API, use `get_organizations_organization_databases_database_workflows_number()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_databases_database_workflows_number_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_databases_database_workflows_number_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_databases_database_workflows_number_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_databases_database_workflows_number`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// GET /organizations/{organization}/databases/{database}/workflows/{number}
/// Get a workflow
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_databases_database_workflows_number_builder()` + `get_organizations_organization_databases_database_workflows_number_execute()`.
/// For task-level control, use `get_organizations_organization_databases_database_workflows_number_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_databases_database_workflows_number(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_databases_database_workflows_number_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.number.clone(),
    )?;
    get_organizations_organization_databases_database_workflows_number_execute(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/workflows/{number}
/// Cancel a workflow
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_databases_database_workflows_number_execute()` to send, or `delete_organizations_organization_databases_database_workflows_number` for simplest API.

pub fn delete_organizations_organization_databases_database_workflows_number_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/databases/{database}/workflows/{number}
/// Cancel a workflow
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_databases_database_workflows_number_execute()` or `delete_organizations_organization_databases_database_workflows_number`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_workflows_number_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_workflows_number_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/databases/{database}/workflows/{number}
/// Cancel a workflow
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_databases_database_workflows_number_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_databases_database_workflows_number_task()`.
/// For the simplest API, use `delete_organizations_organization_databases_database_workflows_number()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_databases_database_workflows_number_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_databases_database_workflows_number_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_organizations_organization_databases_database_workflows_number_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_databases_database_workflows_number`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// DELETE /organizations/{organization}/databases/{database}/workflows/{number}
/// Cancel a workflow
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_databases_database_workflows_number_builder()` + `delete_organizations_organization_databases_database_workflows_number_execute()`.
/// For task-level control, use `delete_organizations_organization_databases_database_workflows_number_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_databases_database_workflows_number(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_databases_database_workflows_number_builder(
        client,
        args.organization.clone(),
        args.database.clone(),
        args.number.clone(),
    )?;
    delete_organizations_organization_databases_database_workflows_number_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/complete
/// Complete a workflow
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_workflows_number_complete_execute()` to send, or `patch_organizations_organization_databases_database_workflows_number_complete` for simplest API.

pub fn patch_organizations_organization_databases_database_workflows_number_complete_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}/complete",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/complete
/// Complete a workflow
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_workflows_number_complete_execute()` or `patch_organizations_organization_databases_database_workflows_number_complete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_workflows_number_complete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_complete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/complete
/// Complete a workflow
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_workflows_number_complete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_workflows_number_complete_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_workflows_number_complete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_workflows_number_complete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_workflows_number_complete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = patch_organizations_organization_databases_database_workflows_number_complete_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_workflows_number_complete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberCompleteArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/complete
/// Complete a workflow
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_complete_builder()` + `patch_organizations_organization_databases_database_workflows_number_complete_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_workflows_number_complete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_complete(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberCompleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_workflows_number_complete_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.number.clone(),
        )?;
    patch_organizations_organization_databases_database_workflows_number_complete_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/cutover
/// Cutover traffic
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_workflows_number_cutover_execute()` to send, or `patch_organizations_organization_databases_database_workflows_number_cutover` for simplest API.

pub fn patch_organizations_organization_databases_database_workflows_number_cutover_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}/cutover",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/cutover
/// Cutover traffic
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_workflows_number_cutover_execute()` or `patch_organizations_organization_databases_database_workflows_number_cutover`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_workflows_number_cutover_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_cutover_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/cutover
/// Cutover traffic
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_workflows_number_cutover_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_workflows_number_cutover_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_workflows_number_cutover()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_workflows_number_cutover_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_workflows_number_cutover_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        patch_organizations_organization_databases_database_workflows_number_cutover_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_workflows_number_cutover`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberCutoverArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/cutover
/// Cutover traffic
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_cutover_builder()` + `patch_organizations_organization_databases_database_workflows_number_cutover_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_workflows_number_cutover_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_cutover(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberCutoverArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_workflows_number_cutover_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.number.clone(),
        )?;
    patch_organizations_organization_databases_database_workflows_number_cutover_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/retry
/// Retry a failed workflow
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_workflows_number_retry_execute()` to send, or `patch_organizations_organization_databases_database_workflows_number_retry` for simplest API.

pub fn patch_organizations_organization_databases_database_workflows_number_retry_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}/retry",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/retry
/// Retry a failed workflow
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_workflows_number_retry_execute()` or `patch_organizations_organization_databases_database_workflows_number_retry`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_workflows_number_retry_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_retry_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/retry
/// Retry a failed workflow
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_workflows_number_retry_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_workflows_number_retry_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_workflows_number_retry()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_workflows_number_retry_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_workflows_number_retry_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        patch_organizations_organization_databases_database_workflows_number_retry_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_workflows_number_retry`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberRetryArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/retry
/// Retry a failed workflow
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_retry_builder()` + `patch_organizations_organization_databases_database_workflows_number_retry_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_workflows_number_retry_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_retry(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberRetryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_workflows_number_retry_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.number.clone(),
        )?;
    patch_organizations_organization_databases_database_workflows_number_retry_execute(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-cutover
/// Reverse traffic cutover
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_workflows_number_reverse_cutover_execute()` to send, or `patch_organizations_organization_databases_database_workflows_number_reverse_cutover` for simplest API.

pub fn patch_organizations_organization_databases_database_workflows_number_reverse_cutover_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}/reverse-cutover",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-cutover
/// Reverse traffic cutover
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_workflows_number_reverse_cutover_execute()` or `patch_organizations_organization_databases_database_workflows_number_reverse_cutover`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_workflows_number_reverse_cutover_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_reverse_cutover_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-cutover
/// Reverse traffic cutover
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_workflows_number_reverse_cutover_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_workflows_number_reverse_cutover_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_workflows_number_reverse_cutover()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_workflows_number_reverse_cutover_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_workflows_number_reverse_cutover_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        patch_organizations_organization_databases_database_workflows_number_reverse_cutover_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_workflows_number_reverse_cutover`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberReverseCutoverArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-cutover
/// Reverse traffic cutover
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_reverse_cutover_builder()` + `patch_organizations_organization_databases_database_workflows_number_reverse_cutover_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_workflows_number_reverse_cutover_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_reverse_cutover(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberReverseCutoverArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_workflows_number_reverse_cutover_builder(client, args.organization.clone(), args.database.clone(), args.number.clone())?;
    patch_organizations_organization_databases_database_workflows_number_reverse_cutover_execute(
        builder,
    )
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-traffic
/// Reverse traffic
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_workflows_number_reverse_traffic_execute()` to send, or `patch_organizations_organization_databases_database_workflows_number_reverse_traffic` for simplest API.

pub fn patch_organizations_organization_databases_database_workflows_number_reverse_traffic_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}/reverse-traffic",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-traffic
/// Reverse traffic
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_workflows_number_reverse_traffic_execute()` or `patch_organizations_organization_databases_database_workflows_number_reverse_traffic`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_workflows_number_reverse_traffic_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_reverse_traffic_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-traffic
/// Reverse traffic
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_workflows_number_reverse_traffic_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_workflows_number_reverse_traffic_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_workflows_number_reverse_traffic()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_workflows_number_reverse_traffic_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_workflows_number_reverse_traffic_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        patch_organizations_organization_databases_database_workflows_number_reverse_traffic_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_workflows_number_reverse_traffic`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberReverseTrafficArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/reverse-traffic
/// Reverse traffic
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_reverse_traffic_builder()` + `patch_organizations_organization_databases_database_workflows_number_reverse_traffic_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_workflows_number_reverse_traffic_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_reverse_traffic(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberReverseTrafficArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_workflows_number_reverse_traffic_builder(client, args.organization.clone(), args.database.clone(), args.number.clone())?;
    patch_organizations_organization_databases_database_workflows_number_reverse_traffic_execute(
        builder,
    )
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-primaries
/// Switch primary traffic
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_workflows_number_switch_primaries_execute()` to send, or `patch_organizations_organization_databases_database_workflows_number_switch_primaries` for simplest API.

pub fn patch_organizations_organization_databases_database_workflows_number_switch_primaries_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}/switch-primaries",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-primaries
/// Switch primary traffic
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_workflows_number_switch_primaries_execute()` or `patch_organizations_organization_databases_database_workflows_number_switch_primaries`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_workflows_number_switch_primaries_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_switch_primaries_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-primaries
/// Switch primary traffic
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_workflows_number_switch_primaries_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_workflows_number_switch_primaries_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_workflows_number_switch_primaries()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_workflows_number_switch_primaries_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_workflows_number_switch_primaries_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        patch_organizations_organization_databases_database_workflows_number_switch_primaries_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_workflows_number_switch_primaries`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberSwitchPrimariesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-primaries
/// Switch primary traffic
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_switch_primaries_builder()` + `patch_organizations_organization_databases_database_workflows_number_switch_primaries_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_workflows_number_switch_primaries_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_switch_primaries(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberSwitchPrimariesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_workflows_number_switch_primaries_builder(client, args.organization.clone(), args.database.clone(), args.number.clone())?;
    patch_organizations_organization_databases_database_workflows_number_switch_primaries_execute(
        builder,
    )
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-replicas
/// Switch replica traffic
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_workflows_number_switch_replicas_execute()` to send, or `patch_organizations_organization_databases_database_workflows_number_switch_replicas` for simplest API.

pub fn patch_organizations_organization_databases_database_workflows_number_switch_replicas_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}/switch-replicas",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-replicas
/// Switch replica traffic
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_workflows_number_switch_replicas_execute()` or `patch_organizations_organization_databases_database_workflows_number_switch_replicas`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_workflows_number_switch_replicas_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_switch_replicas_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-replicas
/// Switch replica traffic
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_workflows_number_switch_replicas_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_workflows_number_switch_replicas_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_workflows_number_switch_replicas()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_workflows_number_switch_replicas_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_workflows_number_switch_replicas_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        patch_organizations_organization_databases_database_workflows_number_switch_replicas_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_workflows_number_switch_replicas`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberSwitchReplicasArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/switch-replicas
/// Switch replica traffic
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_switch_replicas_builder()` + `patch_organizations_organization_databases_database_workflows_number_switch_replicas_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_workflows_number_switch_replicas_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_switch_replicas(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberSwitchReplicasArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_databases_database_workflows_number_switch_replicas_builder(client, args.organization.clone(), args.database.clone(), args.number.clone())?;
    patch_organizations_organization_databases_database_workflows_number_switch_replicas_execute(
        builder,
    )
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/verify-data
/// Verify workflow data
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_databases_database_workflows_number_verify_data_execute()` to send, or `patch_organizations_organization_databases_database_workflows_number_verify_data` for simplest API.

pub fn patch_organizations_organization_databases_database_workflows_number_verify_data_builder(
    client: &SimpleHttpClient,
    organization: String,
    database: String,
    number: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/databases/{}/workflows/{}/verify-data",
        organization.as_str(),
        database.as_str(),
        number.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/verify-data
/// Verify workflow data
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_databases_database_workflows_number_verify_data_execute()` or `patch_organizations_organization_databases_database_workflows_number_verify_data`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_workflows_number_verify_data_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_verify_data_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/verify-data
/// Verify workflow data
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_databases_database_workflows_number_verify_data_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_databases_database_workflows_number_verify_data_task()`.
/// For the simplest API, use `patch_organizations_organization_databases_database_workflows_number_verify_data()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_databases_database_workflows_number_verify_data_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_databases_database_workflows_number_verify_data_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        patch_organizations_organization_databases_database_workflows_number_verify_data_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_databases_database_workflows_number_verify_data`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberVerifyDataArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: database
    pub database: String,
    /// Path parameter: number
    pub number: String,
}

/// PATCH /organizations/{organization}/databases/{database}/workflows/{number}/verify-data
/// Verify workflow data
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_databases_database_workflows_number_verify_data_builder()` + `patch_organizations_organization_databases_database_workflows_number_verify_data_execute()`.
/// For task-level control, use `patch_organizations_organization_databases_database_workflows_number_verify_data_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_databases_database_workflows_number_verify_data(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationDatabasesDatabaseWorkflowsNumberVerifyDataArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        patch_organizations_organization_databases_database_workflows_number_verify_data_builder(
            client,
            args.organization.clone(),
            args.database.clone(),
            args.number.clone(),
        )?;
    patch_organizations_organization_databases_database_workflows_number_verify_data_execute(
        builder,
    )
}

/// GET /organizations/{organization}/invoices
/// Get invoices
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_invoices_execute()` to send, or `get_organizations_organization_invoices` for simplest API.

pub fn get_organizations_organization_invoices_builder(
    client: &SimpleHttpClient,
    organization: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/invoices",
        organization.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/invoices
/// Get invoices
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_invoices_execute()` or `get_organizations_organization_invoices`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_invoices_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_invoices_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/invoices
/// Get invoices
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_invoices_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_invoices_task()`.
/// For the simplest API, use `get_organizations_organization_invoices()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_invoices_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_invoices_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_invoices_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_invoices`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationInvoicesArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/invoices
/// Get invoices
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_invoices_builder()` + `get_organizations_organization_invoices_execute()`.
/// For task-level control, use `get_organizations_organization_invoices_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_invoices(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationInvoicesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_invoices_builder(
        client,
        args.organization.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_invoices_execute(builder)
}

/// GET /organizations/{organization}/invoices/{id}
/// Get an invoice
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_invoices_id_execute()` to send, or `get_organizations_organization_invoices_id` for simplest API.

pub fn get_organizations_organization_invoices_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/invoices/{}",
        organization.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/invoices/{id}
/// Get an invoice
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_invoices_id_execute()` or `get_organizations_organization_invoices_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_invoices_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_invoices_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/invoices/{id}
/// Get an invoice
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_invoices_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_invoices_id_task()`.
/// For the simplest API, use `get_organizations_organization_invoices_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_invoices_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_invoices_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_invoices_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_invoices_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationInvoicesIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/invoices/{id}
/// Get an invoice
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_invoices_id_builder()` + `get_organizations_organization_invoices_id_execute()`.
/// For task-level control, use `get_organizations_organization_invoices_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_invoices_id(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationInvoicesIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_invoices_id_builder(
        client,
        args.organization.clone(),
        args.id.clone(),
    )?;
    get_organizations_organization_invoices_id_execute(builder)
}

/// GET /organizations/{organization}/invoices/{id}/line-items
/// Get invoice line items
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_invoices_id_line_items_execute()` to send, or `get_organizations_organization_invoices_id_line_items` for simplest API.

pub fn get_organizations_organization_invoices_id_line_items_builder(
    client: &SimpleHttpClient,
    organization: String,
    id: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/invoices/{}/line-items",
        organization.as_str(),
        id.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/invoices/{id}/line-items
/// Get invoice line items
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_invoices_id_line_items_execute()` or `get_organizations_organization_invoices_id_line_items`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_invoices_id_line_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_invoices_id_line_items_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/invoices/{id}/line-items
/// Get invoice line items
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_invoices_id_line_items_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_invoices_id_line_items_task()`.
/// For the simplest API, use `get_organizations_organization_invoices_id_line_items()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_invoices_id_line_items_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_invoices_id_line_items_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_invoices_id_line_items_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_invoices_id_line_items`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationInvoicesIdLineItemsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/invoices/{id}/line-items
/// Get invoice line items
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_invoices_id_line_items_builder()` + `get_organizations_organization_invoices_id_line_items_execute()`.
/// For task-level control, use `get_organizations_organization_invoices_id_line_items_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_invoices_id_line_items(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationInvoicesIdLineItemsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_invoices_id_line_items_builder(
        client,
        args.organization.clone(),
        args.id.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_invoices_id_line_items_execute(builder)
}

/// GET /organizations/{organization}/members
/// List organization members
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_members_execute()` to send, or `get_organizations_organization_members` for simplest API.

pub fn get_organizations_organization_members_builder(
    client: &SimpleHttpClient,
    organization: String,
    q: Option<String>,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/members",
        organization.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = q {
        query_parts.push(format!("q={}", val));
    }
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/members
/// List organization members
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_members_execute()` or `get_organizations_organization_members`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_members_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_members_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/members
/// List organization members
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_members_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_members_task()`.
/// For the simplest API, use `get_organizations_organization_members()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_members_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_members_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_members_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_members`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationMembersArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Query parameter: q
    pub q: Option<String>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/members
/// List organization members
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_members_builder()` + `get_organizations_organization_members_execute()`.
/// For task-level control, use `get_organizations_organization_members_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_members(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationMembersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_members_builder(
        client,
        args.organization.clone(),
        args.q.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_members_execute(builder)
}

/// GET /organizations/{organization}/members/{id}
/// Get an organization member
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_members_id_execute()` to send, or `get_organizations_organization_members_id` for simplest API.

pub fn get_organizations_organization_members_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/members/{}",
        organization.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/members/{id}
/// Get an organization member
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_members_id_execute()` or `get_organizations_organization_members_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_members_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_members_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/members/{id}
/// Get an organization member
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_members_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_members_id_task()`.
/// For the simplest API, use `get_organizations_organization_members_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_members_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_members_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_members_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_members_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationMembersIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/members/{id}
/// Get an organization member
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_members_id_builder()` + `get_organizations_organization_members_id_execute()`.
/// For task-level control, use `get_organizations_organization_members_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_members_id(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationMembersIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_members_id_builder(
        client,
        args.organization.clone(),
        args.id.clone(),
    )?;
    get_organizations_organization_members_id_execute(builder)
}

/// PATCH /organizations/{organization}/members/{id}
/// Update organization member role
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_members_id_execute()` to send, or `patch_organizations_organization_members_id` for simplest API.

pub fn patch_organizations_organization_members_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/members/{}",
        organization.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/members/{id}
/// Update organization member role
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_members_id_execute()` or `patch_organizations_organization_members_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_members_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_members_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/members/{id}
/// Update organization member role
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_members_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_members_id_task()`.
/// For the simplest API, use `patch_organizations_organization_members_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_members_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_members_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = patch_organizations_organization_members_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_members_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationMembersIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: id
    pub id: String,
}

/// PATCH /organizations/{organization}/members/{id}
/// Update organization member role
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_members_id_builder()` + `patch_organizations_organization_members_id_execute()`.
/// For task-level control, use `patch_organizations_organization_members_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_members_id(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationMembersIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_members_id_builder(
        client,
        args.organization.clone(),
        args.id.clone(),
    )?;
    patch_organizations_organization_members_id_execute(builder)
}

/// DELETE /organizations/{organization}/members/{id}
/// Remove a member from an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_members_id_execute()` to send, or `delete_organizations_organization_members_id` for simplest API.

pub fn delete_organizations_organization_members_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    id: String,
    delete_passwords: Option<String>,
    delete_service_tokens: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/members/{}",
        organization.as_str(),
        id.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = delete_passwords {
        query_parts.push(format!("delete_passwords={}", val));
    }
    if let Some(val) = delete_service_tokens {
        query_parts.push(format!("delete_service_tokens={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/members/{id}
/// Remove a member from an organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_members_id_execute()` or `delete_organizations_organization_members_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_members_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_members_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/members/{id}
/// Remove a member from an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_members_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_members_id_task()`.
/// For the simplest API, use `delete_organizations_organization_members_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_members_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_members_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_organizations_organization_members_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_members_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationMembersIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: delete_passwords
    pub delete_passwords: Option<String>,
    /// Query parameter: delete_service_tokens
    pub delete_service_tokens: Option<String>,
}

/// DELETE /organizations/{organization}/members/{id}
/// Remove a member from an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_members_id_builder()` + `delete_organizations_organization_members_id_execute()`.
/// For task-level control, use `delete_organizations_organization_members_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_members_id(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationMembersIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_members_id_builder(
        client,
        args.organization.clone(),
        args.id.clone(),
        args.delete_passwords.clone(),
        args.delete_service_tokens.clone(),
    )?;
    delete_organizations_organization_members_id_execute(builder)
}

/// GET /organizations/{organization}/oauth-applications
/// List OAuth applications
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_oauth_applications_execute()` to send, or `get_organizations_organization_oauth_applications` for simplest API.

pub fn get_organizations_organization_oauth_applications_builder(
    client: &SimpleHttpClient,
    organization: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/oauth-applications",
        organization.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/oauth-applications
/// List OAuth applications
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_oauth_applications_execute()` or `get_organizations_organization_oauth_applications`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_oauth_applications_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_oauth_applications_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/oauth-applications
/// List OAuth applications
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_oauth_applications_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_oauth_applications_task()`.
/// For the simplest API, use `get_organizations_organization_oauth_applications()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_oauth_applications_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_oauth_applications_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_oauth_applications_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_oauth_applications`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationOauthApplicationsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/oauth-applications
/// List OAuth applications
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_oauth_applications_builder()` + `get_organizations_organization_oauth_applications_execute()`.
/// For task-level control, use `get_organizations_organization_oauth_applications_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_oauth_applications(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationOauthApplicationsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_oauth_applications_builder(
        client,
        args.organization.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_oauth_applications_execute(builder)
}

/// GET /organizations/{organization}/oauth-applications/{application_id}
/// Get an OAuth application
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_oauth_applications_application_id_execute()` to send, or `get_organizations_organization_oauth_applications_application_id` for simplest API.

pub fn get_organizations_organization_oauth_applications_application_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    application_id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/oauth-applications/{}",
        organization.as_str(),
        application_id.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/oauth-applications/{application_id}
/// Get an OAuth application
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_oauth_applications_application_id_execute()` or `get_organizations_organization_oauth_applications_application_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_oauth_applications_application_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_oauth_applications_application_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/oauth-applications/{application_id}
/// Get an OAuth application
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_oauth_applications_application_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_oauth_applications_application_id_task()`.
/// For the simplest API, use `get_organizations_organization_oauth_applications_application_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_oauth_applications_application_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_oauth_applications_application_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_oauth_applications_application_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_oauth_applications_application_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationOauthApplicationsApplicationIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: application_id
    pub application_id: String,
}

/// GET /organizations/{organization}/oauth-applications/{application_id}
/// Get an OAuth application
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_oauth_applications_application_id_builder()` + `get_organizations_organization_oauth_applications_application_id_execute()`.
/// For task-level control, use `get_organizations_organization_oauth_applications_application_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_oauth_applications_application_id(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationOauthApplicationsApplicationIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_oauth_applications_application_id_builder(
        client,
        args.organization.clone(),
        args.application_id.clone(),
    )?;
    get_organizations_organization_oauth_applications_application_id_execute(builder)
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens
/// List OAuth tokens
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_oauth_applications_application_id_tokens_execute()` to send, or `get_organizations_organization_oauth_applications_application_id_tokens` for simplest API.

pub fn get_organizations_organization_oauth_applications_application_id_tokens_builder(
    client: &SimpleHttpClient,
    organization: String,
    application_id: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/oauth-applications/{}/tokens",
        organization.as_str(),
        application_id.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens
/// List OAuth tokens
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_oauth_applications_application_id_tokens_execute()` or `get_organizations_organization_oauth_applications_application_id_tokens`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_oauth_applications_application_id_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_oauth_applications_application_id_tokens_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens
/// List OAuth tokens
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_oauth_applications_application_id_tokens_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_oauth_applications_application_id_tokens_task()`.
/// For the simplest API, use `get_organizations_organization_oauth_applications_application_id_tokens()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_oauth_applications_application_id_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_oauth_applications_application_id_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_oauth_applications_application_id_tokens_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_oauth_applications_application_id_tokens`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationOauthApplicationsApplicationIdTokensArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: application_id
    pub application_id: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens
/// List OAuth tokens
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_oauth_applications_application_id_tokens_builder()` + `get_organizations_organization_oauth_applications_application_id_tokens_execute()`.
/// For task-level control, use `get_organizations_organization_oauth_applications_application_id_tokens_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_oauth_applications_application_id_tokens(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationOauthApplicationsApplicationIdTokensArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_oauth_applications_application_id_tokens_builder(
        client,
        args.organization.clone(),
        args.application_id.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_oauth_applications_application_id_tokens_execute(builder)
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Get an OAuth token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_oauth_applications_application_id_tokens_token_id_execute()` to send, or `get_organizations_organization_oauth_applications_application_id_tokens_token_id` for simplest API.

pub fn get_organizations_organization_oauth_applications_application_id_tokens_token_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    application_id: String,
    token_id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/oauth-applications/{}/tokens/{}",
        organization.as_str(),
        application_id.as_str(),
        token_id.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Get an OAuth token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_oauth_applications_application_id_tokens_token_id_execute()` or `get_organizations_organization_oauth_applications_application_id_tokens_token_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_oauth_applications_application_id_tokens_token_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_oauth_applications_application_id_tokens_token_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Get an OAuth token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_oauth_applications_application_id_tokens_token_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_oauth_applications_application_id_tokens_token_id_task()`.
/// For the simplest API, use `get_organizations_organization_oauth_applications_application_id_tokens_token_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_oauth_applications_application_id_tokens_token_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_oauth_applications_application_id_tokens_token_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        get_organizations_organization_oauth_applications_application_id_tokens_token_id_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_oauth_applications_application_id_tokens_token_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationOauthApplicationsApplicationIdTokensTokenIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: application_id
    pub application_id: String,
    /// Path parameter: token_id
    pub token_id: String,
}

/// GET /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Get an OAuth token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_oauth_applications_application_id_tokens_token_id_builder()` + `get_organizations_organization_oauth_applications_application_id_tokens_token_id_execute()`.
/// For task-level control, use `get_organizations_organization_oauth_applications_application_id_tokens_token_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_oauth_applications_application_id_tokens_token_id(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationOauthApplicationsApplicationIdTokensTokenIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        get_organizations_organization_oauth_applications_application_id_tokens_token_id_builder(
            client,
            args.organization.clone(),
            args.application_id.clone(),
            args.token_id.clone(),
        )?;
    get_organizations_organization_oauth_applications_application_id_tokens_token_id_execute(
        builder,
    )
}

/// DELETE /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Delete an OAuth token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_oauth_applications_application_id_tokens_token_id_execute()` to send, or `delete_organizations_organization_oauth_applications_application_id_tokens_token_id` for simplest API.

pub fn delete_organizations_organization_oauth_applications_application_id_tokens_token_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    application_id: String,
    token_id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/oauth-applications/{}/tokens/{}",
        organization.as_str(),
        application_id.as_str(),
        token_id.as_str(),
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Delete an OAuth token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_oauth_applications_application_id_tokens_token_id_execute()` or `delete_organizations_organization_oauth_applications_application_id_tokens_token_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_oauth_applications_application_id_tokens_token_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_oauth_applications_application_id_tokens_token_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Delete an OAuth token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_oauth_applications_application_id_tokens_token_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_oauth_applications_application_id_tokens_token_id_task()`.
/// For the simplest API, use `delete_organizations_organization_oauth_applications_application_id_tokens_token_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_oauth_applications_application_id_tokens_token_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_oauth_applications_application_id_tokens_token_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        delete_organizations_organization_oauth_applications_application_id_tokens_token_id_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_oauth_applications_application_id_tokens_token_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationOauthApplicationsApplicationIdTokensTokenIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: application_id
    pub application_id: String,
    /// Path parameter: token_id
    pub token_id: String,
}

/// DELETE /organizations/{organization}/oauth-applications/{application_id}/tokens/{token_id}
/// Delete an OAuth token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_oauth_applications_application_id_tokens_token_id_builder()` + `delete_organizations_organization_oauth_applications_application_id_tokens_token_id_execute()`.
/// For task-level control, use `delete_organizations_organization_oauth_applications_application_id_tokens_token_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_oauth_applications_application_id_tokens_token_id(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationOauthApplicationsApplicationIdTokensTokenIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_oauth_applications_application_id_tokens_token_id_builder(client, args.organization.clone(), args.application_id.clone(), args.token_id.clone())?;
    delete_organizations_organization_oauth_applications_application_id_tokens_token_id_execute(
        builder,
    )
}

/// POST /organizations/{organization}/oauth-applications/{id}/token
/// Create or renew an OAuth token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_oauth_applications_id_token_execute()` to send, or `post_organizations_organization_oauth_applications_id_token` for simplest API.

pub fn post_organizations_organization_oauth_applications_id_token_builder(
    client: &SimpleHttpClient,
    organization: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/oauth-applications/{}/token",
        organization.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/oauth-applications/{id}/token
/// Create or renew an OAuth token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_oauth_applications_id_token_execute()` or `post_organizations_organization_oauth_applications_id_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_oauth_applications_id_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_oauth_applications_id_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/oauth-applications/{id}/token
/// Create or renew an OAuth token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_oauth_applications_id_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_oauth_applications_id_token_task()`.
/// For the simplest API, use `post_organizations_organization_oauth_applications_id_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_oauth_applications_id_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_oauth_applications_id_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_organizations_organization_oauth_applications_id_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_oauth_applications_id_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationOauthApplicationsIdTokenArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: id
    pub id: String,
}

/// POST /organizations/{organization}/oauth-applications/{id}/token
/// Create or renew an OAuth token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_oauth_applications_id_token_builder()` + `post_organizations_organization_oauth_applications_id_token_execute()`.
/// For task-level control, use `post_organizations_organization_oauth_applications_id_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_oauth_applications_id_token(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationOauthApplicationsIdTokenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_oauth_applications_id_token_builder(
        client,
        args.organization.clone(),
        args.id.clone(),
    )?;
    post_organizations_organization_oauth_applications_id_token_execute(builder)
}

/// GET /organizations/{organization}/regions
/// List regions for an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_regions_execute()` to send, or `get_organizations_organization_regions` for simplest API.

pub fn get_organizations_organization_regions_builder(
    client: &SimpleHttpClient,
    organization: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/regions",
        organization.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/regions
/// List regions for an organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_regions_execute()` or `get_organizations_organization_regions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_regions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_regions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/regions
/// List regions for an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_regions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_regions_task()`.
/// For the simplest API, use `get_organizations_organization_regions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_regions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_regions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_regions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_regions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationRegionsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/regions
/// List regions for an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_regions_builder()` + `get_organizations_organization_regions_execute()`.
/// For task-level control, use `get_organizations_organization_regions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_regions(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationRegionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_regions_builder(
        client,
        args.organization.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_regions_execute(builder)
}

/// GET /organizations/{organization}/service-tokens
/// List service tokens
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_service_tokens_execute()` to send, or `get_organizations_organization_service_tokens` for simplest API.

pub fn get_organizations_organization_service_tokens_builder(
    client: &SimpleHttpClient,
    organization: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/service-tokens",
        organization.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/service-tokens
/// List service tokens
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_service_tokens_execute()` or `get_organizations_organization_service_tokens`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_service_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_service_tokens_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/service-tokens
/// List service tokens
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_service_tokens_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_service_tokens_task()`.
/// For the simplest API, use `get_organizations_organization_service_tokens()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_service_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_service_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_service_tokens_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_service_tokens`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationServiceTokensArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/service-tokens
/// List service tokens
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_service_tokens_builder()` + `get_organizations_organization_service_tokens_execute()`.
/// For task-level control, use `get_organizations_organization_service_tokens_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_service_tokens(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationServiceTokensArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_service_tokens_builder(
        client,
        args.organization.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_service_tokens_execute(builder)
}

/// POST /organizations/{organization}/service-tokens
/// Create a service token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_service_tokens_execute()` to send, or `post_organizations_organization_service_tokens` for simplest API.

pub fn post_organizations_organization_service_tokens_builder(
    client: &SimpleHttpClient,
    organization: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/service-tokens",
        organization.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/service-tokens
/// Create a service token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_service_tokens_execute()` or `post_organizations_organization_service_tokens`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_service_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_service_tokens_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/service-tokens
/// Create a service token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_service_tokens_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_service_tokens_task()`.
/// For the simplest API, use `post_organizations_organization_service_tokens()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_service_tokens_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_service_tokens_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_organizations_organization_service_tokens_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_service_tokens`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationServiceTokensArgs {
    /// Path parameter: organization
    pub organization: String,
}

/// POST /organizations/{organization}/service-tokens
/// Create a service token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_service_tokens_builder()` + `post_organizations_organization_service_tokens_execute()`.
/// For task-level control, use `post_organizations_organization_service_tokens_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_service_tokens(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationServiceTokensArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        post_organizations_organization_service_tokens_builder(client, args.organization.clone())?;
    post_organizations_organization_service_tokens_execute(builder)
}

/// GET /organizations/{organization}/service-tokens/{id}
/// Get a service token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_service_tokens_id_execute()` to send, or `get_organizations_organization_service_tokens_id` for simplest API.

pub fn get_organizations_organization_service_tokens_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/service-tokens/{}",
        organization.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/service-tokens/{id}
/// Get a service token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_service_tokens_id_execute()` or `get_organizations_organization_service_tokens_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_service_tokens_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_service_tokens_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/service-tokens/{id}
/// Get a service token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_service_tokens_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_service_tokens_id_task()`.
/// For the simplest API, use `get_organizations_organization_service_tokens_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_service_tokens_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_service_tokens_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_service_tokens_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_service_tokens_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationServiceTokensIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/service-tokens/{id}
/// Get a service token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_service_tokens_id_builder()` + `get_organizations_organization_service_tokens_id_execute()`.
/// For task-level control, use `get_organizations_organization_service_tokens_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_service_tokens_id(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationServiceTokensIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_service_tokens_id_builder(
        client,
        args.organization.clone(),
        args.id.clone(),
    )?;
    get_organizations_organization_service_tokens_id_execute(builder)
}

/// DELETE /organizations/{organization}/service-tokens/{id}
/// Delete a service token
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_service_tokens_id_execute()` to send, or `delete_organizations_organization_service_tokens_id` for simplest API.

pub fn delete_organizations_organization_service_tokens_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/service-tokens/{}",
        organization.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/service-tokens/{id}
/// Delete a service token
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_service_tokens_id_execute()` or `delete_organizations_organization_service_tokens_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_service_tokens_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_service_tokens_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/service-tokens/{id}
/// Delete a service token
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_service_tokens_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_service_tokens_id_task()`.
/// For the simplest API, use `delete_organizations_organization_service_tokens_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_service_tokens_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_service_tokens_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_organizations_organization_service_tokens_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_service_tokens_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationServiceTokensIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE /organizations/{organization}/service-tokens/{id}
/// Delete a service token
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_service_tokens_id_builder()` + `delete_organizations_organization_service_tokens_id_execute()`.
/// For task-level control, use `delete_organizations_organization_service_tokens_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_service_tokens_id(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationServiceTokensIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_service_tokens_id_builder(
        client,
        args.organization.clone(),
        args.id.clone(),
    )?;
    delete_organizations_organization_service_tokens_id_execute(builder)
}

/// GET /organizations/{organization}/teams
/// List teams in an organization
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_teams_execute()` to send, or `get_organizations_organization_teams` for simplest API.

pub fn get_organizations_organization_teams_builder(
    client: &SimpleHttpClient,
    organization: String,
    q: Option<String>,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/teams",
        organization.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = q {
        query_parts.push(format!("q={}", val));
    }
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/teams
/// List teams in an organization
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_teams_execute()` or `get_organizations_organization_teams`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_teams_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_teams_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/teams
/// List teams in an organization
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_teams_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_teams_task()`.
/// For the simplest API, use `get_organizations_organization_teams()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_teams_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_teams_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_teams_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_teams`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationTeamsArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Query parameter: q
    pub q: Option<String>,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/teams
/// List teams in an organization
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_teams_builder()` + `get_organizations_organization_teams_execute()`.
/// For task-level control, use `get_organizations_organization_teams_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_teams(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationTeamsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_teams_builder(
        client,
        args.organization.clone(),
        args.q.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_teams_execute(builder)
}

/// POST /organizations/{organization}/teams
/// Create an organization team
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_teams_execute()` to send, or `post_organizations_organization_teams` for simplest API.

pub fn post_organizations_organization_teams_builder(
    client: &SimpleHttpClient,
    organization: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/teams",
        organization.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/teams
/// Create an organization team
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_teams_execute()` or `post_organizations_organization_teams`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_teams_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_teams_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/teams
/// Create an organization team
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_teams_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_teams_task()`.
/// For the simplest API, use `post_organizations_organization_teams()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_teams_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_teams_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_organizations_organization_teams_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_teams`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationTeamsArgs {
    /// Path parameter: organization
    pub organization: String,
}

/// POST /organizations/{organization}/teams
/// Create an organization team
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_teams_builder()` + `post_organizations_organization_teams_execute()`.
/// For task-level control, use `post_organizations_organization_teams_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_teams(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationTeamsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_teams_builder(client, args.organization.clone())?;
    post_organizations_organization_teams_execute(builder)
}

/// GET /organizations/{organization}/teams/{team}
/// Get an organization team
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_teams_team_execute()` to send, or `get_organizations_organization_teams_team` for simplest API.

pub fn get_organizations_organization_teams_team_builder(
    client: &SimpleHttpClient,
    organization: String,
    team: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/teams/{}",
        organization.as_str(),
        team.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/teams/{team}
/// Get an organization team
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_teams_team_execute()` or `get_organizations_organization_teams_team`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_teams_team_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_teams_team_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/teams/{team}
/// Get an organization team
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_teams_team_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_teams_team_task()`.
/// For the simplest API, use `get_organizations_organization_teams_team()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_teams_team_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_teams_team_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_teams_team_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_teams_team`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationTeamsTeamArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: team
    pub team: String,
}

/// GET /organizations/{organization}/teams/{team}
/// Get an organization team
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_teams_team_builder()` + `get_organizations_organization_teams_team_execute()`.
/// For task-level control, use `get_organizations_organization_teams_team_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_teams_team(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationTeamsTeamArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_teams_team_builder(
        client,
        args.organization.clone(),
        args.team.clone(),
    )?;
    get_organizations_organization_teams_team_execute(builder)
}

/// PATCH /organizations/{organization}/teams/{team}
/// Update an organization team
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `patch_organizations_organization_teams_team_execute()` to send, or `patch_organizations_organization_teams_team` for simplest API.

pub fn patch_organizations_organization_teams_team_builder(
    client: &SimpleHttpClient,
    organization: String,
    team: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/teams/{}",
        organization.as_str(),
        team.as_str(),
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH /organizations/{organization}/teams/{team}
/// Update an organization team
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `patch_organizations_organization_teams_team_execute()` or `patch_organizations_organization_teams_team`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_teams_team_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_teams_team_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH /organizations/{organization}/teams/{team}
/// Update an organization team
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `patch_organizations_organization_teams_team_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `patch_organizations_organization_teams_team_task()`.
/// For the simplest API, use `patch_organizations_organization_teams_team()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `patch_organizations_organization_teams_team_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn patch_organizations_organization_teams_team_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = patch_organizations_organization_teams_team_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`patch_organizations_organization_teams_team`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PatchOrganizationsOrganizationTeamsTeamArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: team
    pub team: String,
}

/// PATCH /organizations/{organization}/teams/{team}
/// Update an organization team
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `patch_organizations_organization_teams_team_builder()` + `patch_organizations_organization_teams_team_execute()`.
/// For task-level control, use `patch_organizations_organization_teams_team_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn patch_organizations_organization_teams_team(
    client: &SimpleHttpClient,
    args: &PatchOrganizationsOrganizationTeamsTeamArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = patch_organizations_organization_teams_team_builder(
        client,
        args.organization.clone(),
        args.team.clone(),
    )?;
    patch_organizations_organization_teams_team_execute(builder)
}

/// DELETE /organizations/{organization}/teams/{team}
/// Delete an organization team
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_teams_team_execute()` to send, or `delete_organizations_organization_teams_team` for simplest API.

pub fn delete_organizations_organization_teams_team_builder(
    client: &SimpleHttpClient,
    organization: String,
    team: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/teams/{}",
        organization.as_str(),
        team.as_str(),
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/teams/{team}
/// Delete an organization team
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_teams_team_execute()` or `delete_organizations_organization_teams_team`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_teams_team_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_teams_team_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/teams/{team}
/// Delete an organization team
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_teams_team_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_teams_team_task()`.
/// For the simplest API, use `delete_organizations_organization_teams_team()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_teams_team_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_teams_team_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_organizations_organization_teams_team_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_teams_team`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationTeamsTeamArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: team
    pub team: String,
}

/// DELETE /organizations/{organization}/teams/{team}
/// Delete an organization team
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_teams_team_builder()` + `delete_organizations_organization_teams_team_execute()`.
/// For task-level control, use `delete_organizations_organization_teams_team_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_teams_team(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationTeamsTeamArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_teams_team_builder(
        client,
        args.organization.clone(),
        args.team.clone(),
    )?;
    delete_organizations_organization_teams_team_execute(builder)
}

/// GET /organizations/{organization}/teams/{team}/members
/// List team members
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_teams_team_members_execute()` to send, or `get_organizations_organization_teams_team_members` for simplest API.

pub fn get_organizations_organization_teams_team_members_builder(
    client: &SimpleHttpClient,
    organization: String,
    team: String,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/teams/{}/members",
        organization.as_str(),
        team.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/teams/{team}/members
/// List team members
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_teams_team_members_execute()` or `get_organizations_organization_teams_team_members`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_teams_team_members_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_teams_team_members_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/teams/{team}/members
/// List team members
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_teams_team_members_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_teams_team_members_task()`.
/// For the simplest API, use `get_organizations_organization_teams_team_members()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_teams_team_members_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_teams_team_members_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_teams_team_members_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_teams_team_members`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationTeamsTeamMembersArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: team
    pub team: String,
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /organizations/{organization}/teams/{team}/members
/// List team members
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_teams_team_members_builder()` + `get_organizations_organization_teams_team_members_execute()`.
/// For task-level control, use `get_organizations_organization_teams_team_members_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_teams_team_members(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationTeamsTeamMembersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_teams_team_members_builder(
        client,
        args.organization.clone(),
        args.team.clone(),
        args.page.clone(),
        args.per_page.clone(),
    )?;
    get_organizations_organization_teams_team_members_execute(builder)
}

/// POST /organizations/{organization}/teams/{team}/members
/// Add a member to a team
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `post_organizations_organization_teams_team_members_execute()` to send, or `post_organizations_organization_teams_team_members` for simplest API.

pub fn post_organizations_organization_teams_team_members_builder(
    client: &SimpleHttpClient,
    organization: String,
    team: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/teams/{}/members",
        organization.as_str(),
        team.as_str(),
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST /organizations/{organization}/teams/{team}/members
/// Add a member to a team
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `post_organizations_organization_teams_team_members_execute()` or `post_organizations_organization_teams_team_members`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_teams_team_members_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_teams_team_members_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST /organizations/{organization}/teams/{team}/members
/// Add a member to a team
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `post_organizations_organization_teams_team_members_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `post_organizations_organization_teams_team_members_task()`.
/// For the simplest API, use `post_organizations_organization_teams_team_members()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `post_organizations_organization_teams_team_members_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn post_organizations_organization_teams_team_members_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = post_organizations_organization_teams_team_members_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`post_organizations_organization_teams_team_members`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PostOrganizationsOrganizationTeamsTeamMembersArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: team
    pub team: String,
}

/// POST /organizations/{organization}/teams/{team}/members
/// Add a member to a team
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `post_organizations_organization_teams_team_members_builder()` + `post_organizations_organization_teams_team_members_execute()`.
/// For task-level control, use `post_organizations_organization_teams_team_members_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn post_organizations_organization_teams_team_members(
    client: &SimpleHttpClient,
    args: &PostOrganizationsOrganizationTeamsTeamMembersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = post_organizations_organization_teams_team_members_builder(
        client,
        args.organization.clone(),
        args.team.clone(),
    )?;
    post_organizations_organization_teams_team_members_execute(builder)
}

/// GET /organizations/{organization}/teams/{team}/members/{id}
/// Get a team member
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_organizations_organization_teams_team_members_id_execute()` to send, or `get_organizations_organization_teams_team_members_id` for simplest API.

pub fn get_organizations_organization_teams_team_members_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    team: String,
    id: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/teams/{}/members/{}",
        organization.as_str(),
        team.as_str(),
        id.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /organizations/{organization}/teams/{team}/members/{id}
/// Get a team member
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_organizations_organization_teams_team_members_id_execute()` or `get_organizations_organization_teams_team_members_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_teams_team_members_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_teams_team_members_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /organizations/{organization}/teams/{team}/members/{id}
/// Get a team member
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_organizations_organization_teams_team_members_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_organizations_organization_teams_team_members_id_task()`.
/// For the simplest API, use `get_organizations_organization_teams_team_members_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_organizations_organization_teams_team_members_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_organizations_organization_teams_team_members_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_organizations_organization_teams_team_members_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_organizations_organization_teams_team_members_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetOrganizationsOrganizationTeamsTeamMembersIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: team
    pub team: String,
    /// Path parameter: id
    pub id: String,
}

/// GET /organizations/{organization}/teams/{team}/members/{id}
/// Get a team member
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_organizations_organization_teams_team_members_id_builder()` + `get_organizations_organization_teams_team_members_id_execute()`.
/// For task-level control, use `get_organizations_organization_teams_team_members_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_organizations_organization_teams_team_members_id(
    client: &SimpleHttpClient,
    args: &GetOrganizationsOrganizationTeamsTeamMembersIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_organizations_organization_teams_team_members_id_builder(
        client,
        args.organization.clone(),
        args.team.clone(),
        args.id.clone(),
    )?;
    get_organizations_organization_teams_team_members_id_execute(builder)
}

/// DELETE /organizations/{organization}/teams/{team}/members/{id}
/// Remove a member from a team
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `delete_organizations_organization_teams_team_members_id_execute()` to send, or `delete_organizations_organization_teams_team_members_id` for simplest API.

pub fn delete_organizations_organization_teams_team_members_id_builder(
    client: &SimpleHttpClient,
    organization: String,
    team: String,
    id: String,
    delete_passwords: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://api.planetscale.com/v1/organizations/{}/teams/{}/members/{}",
        organization.as_str(),
        team.as_str(),
        id.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = delete_passwords {
        query_parts.push(format!("delete_passwords={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE /organizations/{organization}/teams/{team}/members/{id}
/// Remove a member from a team
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `delete_organizations_organization_teams_team_members_id_execute()` or `delete_organizations_organization_teams_team_members_id`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_teams_team_members_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_teams_team_members_id_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE /organizations/{organization}/teams/{team}/members/{id}
/// Remove a member from a team
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `delete_organizations_organization_teams_team_members_id_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `delete_organizations_organization_teams_team_members_id_task()`.
/// For the simplest API, use `delete_organizations_organization_teams_team_members_id()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `delete_organizations_organization_teams_team_members_id_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn delete_organizations_organization_teams_team_members_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = delete_organizations_organization_teams_team_members_id_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`delete_organizations_organization_teams_team_members_id`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeleteOrganizationsOrganizationTeamsTeamMembersIdArgs {
    /// Path parameter: organization
    pub organization: String,
    /// Path parameter: team
    pub team: String,
    /// Path parameter: id
    pub id: String,
    /// Query parameter: delete_passwords
    pub delete_passwords: Option<String>,
}

/// DELETE /organizations/{organization}/teams/{team}/members/{id}
/// Remove a member from a team
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `delete_organizations_organization_teams_team_members_id_builder()` + `delete_organizations_organization_teams_team_members_id_execute()`.
/// For task-level control, use `delete_organizations_organization_teams_team_members_id_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn delete_organizations_organization_teams_team_members_id(
    client: &SimpleHttpClient,
    args: &DeleteOrganizationsOrganizationTeamsTeamMembersIdArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = delete_organizations_organization_teams_team_members_id_builder(
        client,
        args.organization.clone(),
        args.team.clone(),
        args.id.clone(),
        args.delete_passwords.clone(),
    )?;
    delete_organizations_organization_teams_team_members_id_execute(builder)
}

/// GET /regions
/// List public regions
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_regions_execute()` to send, or `get_regions` for simplest API.

pub fn get_regions_builder(
    client: &SimpleHttpClient,
    page: Option<String>,
    per_page: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://api.planetscale.com/v1/regions",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = page {
        query_parts.push(format!("page={}", val));
    }
    if let Some(val) = per_page {
        query_parts.push(format!("per_page={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /regions
/// List public regions
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_regions_execute()` or `get_regions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_regions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_regions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /regions
/// List public regions
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_regions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_regions_task()`.
/// For the simplest API, use `get_regions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_regions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_regions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_regions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`get_regions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GetRegionsArgs {
    /// Query parameter: page
    pub page: Option<String>,
    /// Query parameter: per_page
    pub per_page: Option<String>,
}

/// GET /regions
/// List public regions
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_regions_builder()` + `get_regions_execute()`.
/// For task-level control, use `get_regions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_regions(
    client: &SimpleHttpClient,
    args: &GetRegionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_regions_builder(client, args.page.clone(), args.per_page.clone())?;
    get_regions_execute(builder)
}

/// GET /user
/// Get current user
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `get_user_execute()` to send, or `get_user` for simplest API.

pub fn get_user_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://api.planetscale.com/v1/user",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET /user
/// Get current user
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `get_user_execute()` or `get_user`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_user_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_user_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET /user
/// Get current user
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `get_user_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `get_user_task()`.
/// For the simplest API, use `get_user()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `get_user_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn get_user_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = get_user_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET /user
/// Get current user
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `get_user_builder()` + `get_user_execute()`.
/// For task-level control, use `get_user_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn get_user(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = get_user_builder(client)?;
    get_user_execute(builder)
}
