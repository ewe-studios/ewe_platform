[package]
name = "system_operations"
version = "0.1.0"
description = "System operations for the ewe platform — WASM binary generation, crate scanning, and build tooling"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
keywords = ["system-operations", "wasm", "codegen"]

[dependencies]
foundation_codegen = { workspace = true }
toml = { workspace = true }
toml_edit = { workspace = true }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
tempfile = "3.10"

[lints]
workspace = true
