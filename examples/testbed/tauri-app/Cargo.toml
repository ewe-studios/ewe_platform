[package]
name = "tauri-e2e-test"
version = "0.1.0"
edition = "2021"

# Standalone workspace to avoid inheriting parent workspace's experimental features
[workspace]
resolver = "2"
members = ["."]

[dependencies]
tauri = { version = "2", features = [] }

[build-dependencies]
tauri-build = { version = "2", features = [] }
